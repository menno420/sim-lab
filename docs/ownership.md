# sim-lab — ownership

> **Status:** `binding`
>
> Generated by substrate-kit. Which area / service / pipeline owns each
> table, event, and write path. **NOT SOURCE OF TRUTH** for code — source
> files always win.

> **Steady state:** this doc's table is **generated** from store / manifest
> specs where those exist — a projection, not hand-prose. This skeleton is
> the interim hand-maintained form until that projection lands.

## Ownership model

Single-writer: agents of this Project write ONLY sim-lab (fleet rule Q-0260); cross-repo state is read via the public raw path. The Idea Engine (menno420/idea-engine) owns the standing intake feed (its control/outbox.md, read-only pull); the fleet manager owns routing — finalized verdicts leave via control/outbox.md addressed to it, never dispatched to lanes directly.

## Ownership table

| Area | Owner (module / service) | Writes it owns | Notes |
|---|---|---|---|
| (one row per owned area) | | | |

## New areas

A new idea under evidence = a new sims/<idea-slug>/ subtree born complete (model, seeds, run command, README, results report). A harness/ change ships only with a consumer sim proving it in the same PR. No other top-level areas without a control/inbox.md ORDER naming one.

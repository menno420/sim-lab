# VERDICT 065 — the illustration gate (idea-engine PROPOSAL 054)

Prices the venture-lab publishing plan's §3 kids-book park-everything default
(the blocking gate on three live owner-queue publish sequences) against the
plan's OWN committed alternatives — Commission at the committed anchors, AI
art at the committed tool cost with disclosure, and the seat's own
AI-pilot-one hedge — as a 7-policy expected-net-dollars decision over the
pre-registered 9-cell dead-share-belief × art-cost grid (π_L ∈ {3/4, 1/2,
1/4} × c ∈ {1500, 3245, 4950}) at the plan's own committed unit economics
(r = 2097/500 exactly), per the registration (PROPOSAL 054, idea-engine
`ideas/venture-lab/illustration-gate-park-vs-pilot-2026-07-14.md` @ main
ca71997; claim idea-engine PR #384).

Run: `python3 sims/verdict-065-illustration-gate/illustration_gate_sim.py`

- `fixtures.json` — every registered constant verbatim from the PROPOSAL 054
  block / idea doc, committed BEFORE the runner; every fixture-level
  implementation choice disclosed here first (pilot-window convention,
  categorical/line-state/coin/trial draw conventions and the CRN union, the
  dead-line one-draw short-circuit, the bulk draw-count sentinel mechanism,
  the exact-integer estimator units (1/500 dollar), the SE/float-comparison
  conventions, the agreement-gate reading, the reporting-leg N, the a = 150
  per-title accounting split, the twin-evaluator structure).
- `illustration_gate_sim.py` — Arm A (DECISION: seedless exact
  `fractions.Fraction` expected-value tree — per-title gross f·p·r·days by
  linearity, pilot branches weighted by the exact commit probability
  1−(1−f·p)^W mixed over the prior, posterior alive-certainty proven by
  exact Bayes collapsing the continuation to V_cont; alone decision-bearing)
  + Arm S (confirmation: seed 20261353, N = 200,000 common-random-numbers
  scenarios per cell, pinned draw order line-state → K p-draws → removal
  coins → day-by-day Bernoulli trials) + stability leg (seed 20261354,
  20,000/cell) + 12 reporting worlds (seed 20261355, 20,000/cell/world) +
  aux seed 20261356 (constructed last, never read, fresh-state asserted) +
  gates F1–F6 + twin independently-written decision evaluators.
- `results.json`, `run-stdout.txt` — the accepted run (byte-identical across
  two full process runs by external sha256; hashes in REPORT.md; every
  decision number an exact rational in results.json).
- `REPORT.md` — ruling, the 9 × 7 NET table, the Δ column with families,
  gates, the three registration-defect anomalies, sensitivity legs, the
  drafter-reference comparison, boundaries, the five validity-gate answers.

Ruling: see REPORT.md (rules applied in the registered order — REJECT
checked first; the registered validity gates rule the classification).
Hermetic, stdlib-only, CPython 3.11 pinned, no wall-clock in any output.

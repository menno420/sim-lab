# Probe report — VERDICT 163 · founder-dilution liquidation waterfall (P150 → V163, +13)

**Verdict: APPROVE** (exact reproduction, clean) — byte-identical verifier copy (`diff` exit 0) + exact digest reproduction across a cross-invocation double-run, the script's in-process double-run assert, and the byte-identical copy + all three gates PASS in order G1 → G2 → G3, exit 0. Clean (not a defect / not QUALIFIED): the only limitation — Offer B wins above the $200M analytic crossover, and under the heavier-tailed Dist B the raw full-distribution MEAN flips to Offer B — is SELF-DISCLOSED by the proposal as a descriptive field of a BODY/typical-exit claim and touches NO gate. Both offers are evaluated on the SAME paired exit draw per trial (one `random.Random(SEED)` stream, fixed order Dist A then Dist B); only the term-sheet STRUCTURE differs, so the founder-proceeds contrast is attributable to the participation multiple alone, not to any difference in the exit values sampled. Born-red card flip is the deliberate LAST commit releasing the substrate-gate HOLD; landing via merge-on-green, zero agent merge calls.

Source: idea-engine `ideas/venture-lab/founder_dilution_waterfall.py` at `7d53a64` (PROPOSAL 150, round-35 VENTURE slot, landed via idea-engine #604). Permalink: https://github.com/menno420/idea-engine/blob/7d53a64/ideas/venture-lab/founder_dilution_waterfall.py — blob at that commit `2bb413badfe75bdbed32298179a9340cd43ee348` (verified `git rev-parse 7d53a64:ideas/venture-lab/founder_dilution_waterfall.py` == idea-engine HEAD blob).

## Reproduction posture
- Verifier copied **byte-identical** from idea-engine `ideas/venture-lab/founder_dilution_waterfall.py` — `diff` exit **0**, file sha256 `daad631413974ffcbf0eb6951c35bdf64ec970f6c0ce1be7140a88d532026100`, git blob `2bb413badfe75bdbed32298179a9340cd43ee348`, **12093** bytes, 287 lines. Stdlib-only (hashlib, json, math, random); no numpy/scipy.
- Pinned world: **SEED=20260717**, RAISE=$10M, PRE_A=30 / PRE_B=50 → post 40 / 60, MULT_A=1x / MULT_B=2x, PARTICIPATING_A=False / PARTICIPATING_B=True, f_A=RAISE/(PRE_A+RAISE)=**0.25**, f_B=RAISE/(PRE_B+RAISE)=**0.166667** (1/6), X_CROSS=**200.0**, exit distributions log-normal Dist A median 80 log-sigma 1.0 (PRIMARY) and Dist B median 120 log-sigma 1.3 (ROBUSTNESS), M_TRIALS=**20000** exit draws per distribution, SIGMA=3.0. One `random.Random(SEED)` stream drawn in fixed order Dist A then Dist B; both offers scored on the SAME paired draw each trial.
- Method: ONE company raises $10M under two term sheets scored on the same exit draw. Offer A (LOWER headline valuation, CLEAN terms): founder_A(X)=X−max(1x pref, f_A·X)=X−max(10, 0.25X) — non-participating, investor takes the GREATER of preference or as-converted common. Offer B (HIGHER headline valuation, STACKED terms): founder_B(X)=(1−f_B)·max(0, X−2·pref)=(1−1/6)(X−20) for X>20 — 2x participating preferred takes the multiple off the top THEN shares the remainder pro-rata (double-dip). Analytic crossover X*=200 exactly (0.75X = 0.8333(X−20)). Gates: z on the /se convention, z=(mean−H0)/se, H0=0 (G2/G3 gap) or 0.5 (G1 win-rate binomial se), z_gate=3.0, require z≥3 one-sided.
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** — results dict carries NO `results_sha256` field; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent results dict and asserts `digest==digest2` (in-process double-run byte-identity), prints the gate lines + the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256` line, writes NO file; floats rounded 6 dp. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 dump.

## Digest — reproduced == disclosed
| run | digest |
|---|---|
| disclosed (P150 outbox done-when line @ `7d53a64`) | `bf4042bdc18bf2c5ff3d1530a97ad2a7ead43038407f095ff2b5304df4f3b7c1` |
| cross-invocation A (committed `run-stdout.txt`) | `bf4042bdc18bf2c5ff3d1530a97ad2a7ead43038407f095ff2b5304df4f3b7c1` |
| cross-invocation B (fresh `python3`) | `bf4042bdc18bf2c5ff3d1530a97ad2a7ead43038407f095ff2b5304df4f3b7c1` |
| in-process double-run assert (script's own `digest==digest2`) | PASS |

**All reproduced digests == the disclosed digest EXACTLY** (all 64 hex). all_pass=**true**, first_failing_gate=**null**, exit **0** on every invocation; stdout byte-identical across the two fresh cross-invocations (`diff` exit 0). The pretty indent=2 stdout dump is a distinct serialization of the same dict; the digest preimage is the compact-canonical form.

## Gates (disclosed → reproduced), order G1 → G2 → G3
| gate | criterion | disclosed | reproduced | result |
|---|---|---|---|---|
| **G1** majority-inversion | win_rate=frac(founder_A>founder_B) > 0.5, z ≥ 3 one-sided | win_rate **0.7983** se **0.002837** z **+105.128721** | identical to the digit | **PASS** |
| **G2** typical-outcome gap | mean(founder_A−founder_B \| X < X_CROSS) > 0, z ≥ 3 one-sided | gap_mean **+8.787216** se **0.029238** z **+300.536834**, frac_below_cross **0.8153** | identical to the digit | **PASS** |
| **G3** robustness (Dist B) | re-run G1 and G2 on heavier-tailed Dist B, z ≥ 3 on BOTH | win_rate_B **0.62305** z **+35.907274**; gap_mean_B **+8.22916** se **0.03502** z **+234.981636** | identical to the digit | **PASS** |

First-failing gate: **none**. Order G1 → G2 → G3 held; every disclosed statistic reproduced to the digit. G3 re-runs both effects on the case most favourable to Offer B (Dist B = more moonshots) and both survive.

## Descriptive (all match disclosed exactly)
- Dist A: median_founder_A = **60.243238** vs median_founder_B = **50.270264** (the lower-valuation clean sheet pays the typical founder ~$10M more); mean_founder_A = **99.309912** vs mean_founder_B = **95.116126**. win_rate **0.7983** (Offer A wins ~80% of exits), frac_below_cross **0.8153**.
- DISCLOSED CAVEAT (confirmed, honestly stated): above the ~$200M analytic crossover the higher-headline participating Offer B wins, and under the heavier-tailed Dist B the raw full-distribution MEAN flips to Offer B (mean_founder_A = **207.803124** vs mean_founder_B = **215.568532** — the moonshot tail pays B more on average) EVEN THOUGH the Dist-B MEDIAN still favours A (median_founder_A = **90.221586** vs median_founder_B = **83.57954**) and A still wins the majority (win_rate_B **0.62305**). The proposal itself discloses this as a descriptive field; the GATED claim is scoped to typical/median founder outcomes below the crossover, and no gate reads the full-distribution mean.
- Params reproduced: seed **20260717**, m_trials **20000**, raise **10.0**, pre_A **30.0** / pre_B **50.0**, mult_A **1.0** / mult_B **2.0**, participating_A **false** / participating_B **true**, f_A **0.25** / f_B **0.166667**, med_A **80.0** / med_B **120.0**, sig_A **1.0** / sig_B **1.3**, x_cross **200.0**, sigma **3.0**; proposal **150**, slot **round-35 venture**, all_pass **true**, first_failing_gate **null**, exit **0**.

## Why APPROVE (clean), not a defect / QUALIFIED
The proposal's head is explicitly a BODY / typical-exit claim, and its Model basis flags — not hides — the boundary: (i) participation is UNCAPPED; a 3x cap only WEAKENS Offer B's tail win, so the below-crossover founder-A advantage is a floor, not a fragile point. (ii) A single preferred round / no option pool; a multi-round stack only DEEPENS the drag on common, never reverses the sign. (iii) Log-normal exit calibration — the signs are calibration-general, proven directly by G3 re-running both effects on an independent, heavier-tailed second distribution. The only reversal (Offer B above X*=200, and the Dist-B raw-mean flip to B) is a DISCLOSED descriptive field of the results dict, not a hidden regime that a gate silently averages over — the crossover is analytic (X*=200 exactly), the head is scoped below it, and every gate is a definitional contrast on the SAME paired exit draws. None of the three declared choices flips a gate SIGN or the order-of-magnitude of the below-crossover founder-A advantage. So the ~$8.8M typical-exit gap and the ~80% majority-inversion rest on the term-sheet STRUCTURE (a participation multiple is a first-order founder haircut; a headline-valuation bump is a small tail-only credit) over byte-identical inputs. No gated headline is regime-contingent → clean APPROVE.

## Probe questions — answered
**1. Digest reproduces exactly (compact-canonical, P127+ twist)?** YES. Reproduced digest `bf4042bdc18bf2c5ff3d1530a97ad2a7ead43038407f095ff2b5304df4f3b7c1` == disclosed EXACTLY (all 64 hex) across cross-invocation A (committed `run-stdout.txt`), a fresh `python3` invocation B (stdout byte-identical, `diff` exit 0), and the script's own in-process `digest==digest2` assert (PASS). Computed over the COMPACT-canonical serialization, not the pretty indent=2 stdout dump.

**2. Verifier byte-identical to P150 source?** YES. `diff` against the copy exit **0**; file sha256 `daad631413974ffcbf0eb6951c35bdf64ec970f6c0ce1be7140a88d532026100`, git blob `2bb413badfe75bdbed32298179a9340cd43ee348`, **12093** bytes, **287** lines — all match the pinned P150 source at idea-engine `7d53a64` (== idea-engine HEAD blob). Both fresh runs exit **0**.

**3. All three gates PASS in order G1 → G2 → G3 with the disclosed statistics?** YES. G1 win_rate=0.7983 z=+105.128721; G2 gap_mean=+8.787216 z=+300.536834 frac_below_cross=0.8153; G3 Dist-B win_rate=0.62305 z=+35.907274 and gap_mean=+8.22916 z=+234.981636 — all ≥ 3σ one-sided, all_pass=**true**, first_failing_gate=**null**, exit **0**.

**4. Headline read correctly (BODY/typical claim, term-sheet structure, not "higher valuation always worse")?** YES. The higher-valuation 2x-participating Offer B pays founders LESS than the lower-valuation clean Offer A in ~80% of exits (win_rate 0.7983) and by ~$8.8M at the typical X<$200M exit (median founder proceeds A 60.243238 vs B 50.270264), produced by the participation multiple on the SAME paired exit draws. Offer B wins only above the $200M analytic crossover — the claim is scoped to the body / typical exit, not universal.

**5. Model-basis caveat weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (clean APPROVE)?** YES. The above-X* Offer-B win and the Dist-B raw-mean flip to B (mean_founder_A 207.803124 vs mean_founder_B 215.568532) are DISCLOSED descriptive fields of a body/typical-exit head; uncapped participation, single round / no option pool, and log-normal calibration are conservative-direction (a cap / a stack / a second distribution each preserve the sign, the last proven by G3) — none flips a gate sign or the order-of-magnitude. No gate reads the full-distribution mean.

**6. Could the majority-inversion be a seed fluke?** NO. 20000 paired exit draws per distribution; G1 clears at z ≈ 105σ, G2 at z ≈ 301σ, and both survive on the independent heavier-tailed Dist B (G3 win z ≈ 36σ, gap z ≈ 235σ) — not a fluke.

## Transferable correction (lane consumer, Q-0264)
Lane CONSUMER = any founder, operator, or board member comparing two term sheets that raise the SAME cash for the SAME company. The transferable correction: **a higher headline pre-money valuation is nearly ORTHOGONAL to realised founder proceeds once the liquidation waterfall carries a PARTICIPATING preferred with a multiple — the participation multiple is a large FIRST-ORDER haircut on common (a fixed multiple off the top, THEN the investor also shares the remainder pro-rata), while the smaller ownership fraction a higher valuation buys is a small TAIL-ONLY credit that only pays off in a moonshot above the analytic crossover (here $200M).** To act on it: price the two sheets on the SAME distribution of realistic exits through the FULL waterfall (preference multiple, participation, cap, stack) — do not compare headline valuations or dilution fractions in isolation; below the crossover the clean 1x non-participating sheet at the lower valuation pays the typical founder more. The FIRST cap-table head in the venture lane (every prior venture head is an operating-metric / growth / revenue / cash / sales-ops result). DISTINCT from a fire-sale intuition ("liquidation preferences only bite in a down exit"): here the participation drag dominates across a strict MAJORITY of realistic outcomes and at the median, not just the downside.

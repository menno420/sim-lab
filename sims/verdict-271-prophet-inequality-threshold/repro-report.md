# VERDICT 271 reproduction mirror — Prophet inequality single-threshold 1/2 guarantee: E[ALG_τ] ≥ (1/2)E[max] for ALG_τ accepting the first X_i ≥ τ (else the last) with τ = E[max]/2, exact Fraction enumeration, seeded MC agreement, positive-scale invariance, and the gambler-matches-prophet falsification, PROPOSAL 258

> **Status:** reproduction-only — the canonical APPROVE/QUALIFIED/REJECT ruling is deferred to the coordinator-driven VERDICT 271 slice. This mirror does NOT advance the verdict high-water and does NOT append a VERDICT block. `control/status.md` is untouched.

**Source proposal:** idea-engine PROPOSAL 258 (fleet · optimal stopping / online algorithms · the single-item prophet inequality with an explicit single-threshold rule: for independent nonnegative `X_1..X_n` revealed in order, `ALG_τ` accepts the first `X_i ≥ τ` and stops, else takes the last variable `X_n`, with `τ = E[max_i X_i]/2`, and satisfies `E[ALG_τ] ≥ (1/2)·E[max_i X_i]`; the constant `1/2` is optimal — the tight family `X_1=1, X_2=M` w.p. `1/M` gives ratio `1/(2−1/M) → 1/2`, while greedy take-first collapses to `1/10` on the teeth instance)
**Lane:** P258 → V271 (+13 offset)
**Verifier:** `verify_258_prophet_inequality_threshold.py` (byte-identical copy of the firsthand idea-engine verifier; stdlib only: fractions, hashlib, itertools, json, math, random, sys)
**SEED:** 20260721
**Command:** `python3 verify_258_prophet_inequality_threshold.py` (run from inside this directory; captured in `run-stdout.txt`)

## Reproduction result

Ran the byte-identical verifier here; `run-stdout.txt` holds the full output.

- `results_sha256 = 1c235e4991e2775e0c5813966f6cec313c296580c83a27195b287d9e4e3068fb` — byte-identical to the idea-engine source digest.
- in-process double-run: IDENTICAL · `--selfcheck`: byte-identical (`selfcheck OK 1c235e4991e2775e0c5813966f6cec313c296580c83a27195b287d9e4e3068fb`) · separate re-invocation: byte-identical
- **G1 EXACT** (`fractions.Fraction`, zero tolerance) — exact product-support enumeration of `E[max]`, `E[ALG_τ]`, and the ratio for six instances (generic + tight `M∈{2,3,4,5}` + teeth); the guarantee `E[ALG_τ] ≥ E[max]/2` holds on all six. Headline fractions assert exactly: generic `(E[max]=5/4, τ=5/8, E[ALG_τ]=1, ratio 4/5)`, tight `M=4` `(7/4, 7/8, 1, 4/7)`, teeth `(10, 5, 10, 1)`. Threshold-necessity: greedy take-first ratio `= 1/10 < 1/2` on the teeth instance. `z` not applicable, reported `"exact"` → PASS
- **G2 Monte-Carlo agreement** (i.i.d. draws) — `N = 200000` seeded draws of the generic instance (`random.Random(SEED)`, all variables sampled in order) run through the same `ALG_τ` rule; sample mean `0.9969950000` estimates the exact `E[ALG_τ] = 1`, plain iid `SE = 0.0015786481`, `z = (mean − 1)/SE = −1.903527`, `|z| < 3` (Z_ACCEPT = 3.0). i.i.d. draws → the plain iid SE is honest (no batch means / thinning; stated in the verifier docstring) → PASS
- **G3 invariance** (positive scale, own direction, EXACT) — the ratio `E[ALG_τ]/E[max]` is invariant under `X → λ·X` for every `λ > 0`: for `λ ∈ {2, 3, 100, 1/7}` the generic-instance ratio stays exactly `4/5` via `Fraction` (`τ` scales with the values, so acceptance decisions are unchanged). Genuine content — the guarantee depends on the shape of the distributions, not their units. → PASS
- **G4 falsifiability** (own direction, SAME MC sample as G2) — the pre-registered naive foil "the gambler matches the prophet" (`E[ALG_τ] == E[max] = 1.25`) is REJECTED on the same `N = 200000` sample at `z_foil = (mean − 1.25)/SE = −160.266876`, `|z_foil| > 6`, while the same sample AGREES with the true `E[ALG_τ] = 1` at `z = −1.903527`. Corroboration: the tight family ratios `2/3, 3/5, 4/7, 5/9` are monotone decreasing toward `1/2` and strictly above `1/2` (`r_5 < r_2`, both `> 1/2`), confirming `1/2` is approached but not improvable. → PASS
- all_gates_pass = true · first_failing_gate = null · decision = PASS

## Claim reproduced

For independent nonnegative random variables `X_1..X_n` revealed in order, the single-threshold rule `ALG_τ` that accepts the first `X_i ≥ τ` (else takes the last variable `X_n`), with `τ = E[max_i X_i]/2`, satisfies `E[ALG_τ] ≥ (1/2)·E[max_i X_i]`, and the constant `1/2` is optimal. The generic instance (`X_1 ∈ {1,0}` each w.p. `1/2`, `X_2 ∈ {2,0}` each w.p. `1/2`) gives `E[max] = 5/4`, `τ = 5/8`, `E[ALG_τ] = 1`, ratio `4/5`. The tight family (`X_1 = 1` sure, `X_2 = M` w.p. `1/M`) has ratio `M/(2M−1) = 1/(2 − 1/M)`, decreasing `2/3, 3/5, 4/7, 5/9` toward `1/2`, so no online rule beats `1/2`. The teeth instance (`1` sure, then `10` sure) has threshold ratio `1` while greedy take-first collapses to `1/10`, quantifying threshold-necessity. The whole rational backbone is verified end-to-end in `fractions.Fraction` (six-instance guarantee sweep + exact headline fractions + take-first necessity `1/10`, mismatches `= 0`); a seeded Monte-Carlo estimator agrees with the exact `E[ALG_τ] = 1` at `z = −1.90`; the ratio is exactly scale-invariant (`4/5` under `λ ∈ {2,3,100,1/7}`); and the single most plausible confusion — the gambler matching the prophet (`E[ALG_τ] = E[max]`) — is shown false on the same evidence (rejected at `|z_foil| = 160.27`).

## Verifier source integrity

Byte-identical to idea-engine's firsthand verifier `ideas/fleet/verify_258_prophet_inequality_threshold.py` (verifier file sha256 `e3a0f0a271920b0ac91c062a26469515c2f2edefac5dbdebfbf1ca37bbecf1c9`). The copy in this directory carries the same sha256 and `diff` against the source is empty (exit 0), confirming a byte-for-byte reproduction of the source verifier; stdlib-only (fractions, hashlib, itertools, json, math, random, sys), no network, SEED hardcoded. The hashed results payload is a pure function of SEED and the module constants (exact ratios enter the payload as `str(Fraction)` via `"%d/%d"`, every float via a fixed `"%.6f"` / `"%.10f"` string; no wall-clock / PID / unordered-set iteration enters the hashed payload), so the in-process double-run, the `--selfcheck` path, and the separate subprocess re-invocation are byte-identical and the `results_sha256` reproduces exactly.

## Grounding (carried from the source proposal, not re-adjudicated here)

The source proposal pins Wikipedia "Prophet inequality" oldid 1329732914 (raw-wikitext sha1 `2630c796b06718744230a4e60e007f1bda1d9edb`, 8483 bytes; API `revisions.sha1` == self-computed `hashlib.sha1`; the article exists and is not a redirect). The page is rich for this head: the single-threshold rule `τ = \tfrac12 E[max_i X_i]`, the `1/2` guarantee ("at least half that of the prophet"), and its optimality ("No algorithm can achieve a greater expected value for all distributions") are all QUOTED verbatim, alongside the name "prophet inequality" (grep 6), "optimal stopping" (grep 3), "threshold" (grep 5), and the originators Krengel / Sucheston / Garling. DERIVED firsthand (grep 0): the literal string `1/2`, the six enumerated instances and every rational in the reference table (`5/4, 4/5, 7/4, 4/7, 1/10`), the numeric application of `τ = E[max]/2`, the greedy take-first foil, the seeded Monte-Carlo estimator + every z-value, the `fractions.Fraction` enumeration, the positive-scale-invariance leg, SEED `20260721`, and the digest. This mirror reproduces the computation only; the honest quoted-vs-derived split is carried from the proposal for the coordinator's VERDICT 271 slice to adjudicate. The prophet inequality is textbook (Krengel–Sucheston 1978; Garling's tight form; the pinned page), cited as such — nothing oversold as novel.

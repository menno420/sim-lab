# VERDICT 270 reproduction mirror — Valiant two-phase (random-intermediate) oblivious routing on the hypercube Q_n: phase-1 route length L = popcount(s^d) is exactly Binomial(n, 1/2), source-invariant, PGF G(x) = ((1+x)/2)^n, average directed-edge congestion = 1/2, PROPOSAL 257

> **Status:** reproduction-only — the canonical APPROVE/QUALIFIED/REJECT ruling is deferred to the coordinator-driven VERDICT 270 slice. This mirror does NOT advance the verdict high-water and does NOT append a VERDICT block. `control/status.md` is untouched.

**Source proposal:** idea-engine PROPOSAL 257 (fleet · combinatorics / randomized routing / Valiant's random-intermediate scheme on the hypercube — a self-contained closed form: each packet is first routed from its source `s` to a UNIFORMLY RANDOM intermediate `d`, then on to its destination by left-to-right bit-fixing; because `d` is uniform and independent of `s`, the XOR `s^d` is uniform on `{0,1}^n`, so phase-1 length `L = popcount(s^d)` is EXACTLY `Binomial(n, 1/2)`, `E[L] = n/2`, `Var[L] = n/4`, INDEPENDENT of the source)
**Lane:** P257 → V270 (+13 offset)
**Verifier:** `verify_257_valiant_permutation_routing.py` (byte-identical copy of the firsthand idea-engine verifier; stdlib only: json, hashlib, math, random, fractions, sys)
**SEED:** 20260717
**Command:** `python3 verify_257_valiant_permutation_routing.py` (run from inside this directory; captured in `run-stdout.txt`)

## Reproduction result

Ran the byte-identical verifier here; `run-stdout.txt` holds the full output.

- `results_sha256 = fd0d970057a36cfb2f2ad3dfe200acd09d3ce303d8c1eb37e88f6d3330fa759f` — byte-identical to the idea-engine source digest.
- in-process double-run: IDENTICAL · `--selfcheck`: byte-identical · separate re-invocation: byte-identical
- **G1 EXACT** (`fractions.Fraction`, zero tolerance) — the exact combinatorial backbone of the phase-1 length distribution: (a) the PGF identity `Σ_k C(n,k) x^k = (1+x)^n`, hence `G(x) = E[x^L] = ((1+x)/2)^n`, checked at several rational `x` for `n ∈ {1, 2, 3, 8, 12}`; (b) `E[L] = n/2 = G'(1)` (`E_L = 6` at `n = 12`); (c) `Var[L] = n/4` from `G''(1) + G'(1) − G'(1)^2` (`Var_L = 3` at `n = 12`); (d) the pmf `p_k = C(n,k)/2^n` sums to 1 and is symmetric `p_k = p_{n−k}`; (e) average directed-edge congestion `= (N·(n/2))/(N·n) = 1/2` exact. `checked = 75`, `mismatches = 0`. `z` not applicable, reported `"exact"` → PASS
- **G2 Monte-Carlo agreement** (i.i.d. draws) — draw `N_MC = 400000` uniform 12-bit pairs `(s, d)` over the `N = 4096` nodes; route length `= popcount(s^d)`, an i.i.d. `Binomial(12, 1/2)` draw. Empirical `mean_len = 5.997945` vs `E[L] = 6.0`, `SE = 0.00274` (plain i.i.d. SE, honest because the draws are i.i.d. — no batch-means/thinning), `z = (mean − 6.0)/SE = −0.749864`, `|z| < 3` (Z_ACCEPT = 3.0). Derived edge-congestion estimate `congestion_est = mean/n = 0.499829` vs true `0.5` (same statistic rescaled, `z_congestion = −0.749864`) → PASS
- **G3 robustness / invariance** (own direction) — two legs: (a) **source-permutation invariance** (Valiant's core): re-estimate the mean route length with the sources taken from the ADVERSARIAL bit-reversal permutation of the node index, paired with uniform-random intermediates — the adversary cannot inflate phase-1 length because the intermediate is uniform and independent of the source, so `adv_mean_len = 6.002165` still agrees with `n/2` at `adv_z = 0.79122` (`|z| < 3`). (b) **n-invariance**: `E[L]/n = 1/2` (edge congestion `1/2`) across `n ∈ {8, 10, 12, 14}`, each `|z| < 3` (`z = −0.215486, −0.112088, 1.232978, 2.686444`; ratios `0.49994, 0.499972, 0.500282, 0.500569`). → PASS
- **G4 falsifiability** (own direction, SAME MC sample as G2) — the plausible naive foils. Primary: "every packet crosses all `n` dimensions, so route length `= n = 12`" predicts mean `12.0`; on the same N=400000 sample that AGREES with the true mean `6.0` at `z = −0.749864`, the foil is REJECTED at `z_foil = −2190.134211`, `|z_foil| ≫ Z_REJECT = 50.0`. Secondary: "each edge traversed deterministically → congestion `= 1.0` not `1/2`" is rejected at `z_congestion_foil = −2190.134211` vs the measured `≈0.5`. The teeth: the same sample discriminates the true `Binomial(n, 1/2)` / `1/2`-congestion scaffold from both over-counting foils → PASS
- all_pass = true · first_failing_gate = null · decision = PASS

## Claim reproduced

Valiant's two-phase (random-intermediate) oblivious routing on the hypercube `Q_n` (`N = 2^n` nodes). Routing each packet first to a uniformly random intermediate `d` makes the phase-1 route length `L = H(s, d) = popcount(s^d)` exactly `Binomial(n, 1/2)`: `p_k = C(n,k)/2^n`, `E[L] = n/2`, `Var[L] = n/4`, with probability generating function `G(x) = ((1+x)/2)^n` — and crucially INDEPENDENT of the source `s`, which is exactly why the random intermediate defeats adversarial permutations. The average congestion over the `N·n` directed edges of `Q_n` is `(N·(n/2))/(N·n) = 1/2` exactly. The whole combinatorial backbone is verified end-to-end in `fractions.Fraction` (PGF identity → `E[L]` → `Var[L]` → pmf sum/symmetry → congestion, `checked = 75`, `mismatches = 0`); an i.i.d. Monte-Carlo estimator over 12-bit pairs agrees with `E[L] = 6.0` at `z = −0.75`; the adversarial bit-reversal source permutation still agrees at `z = 0.79` (source-invariance holds) and `E[L]/n = 1/2` holds across `n ∈ {8, 10, 12, 14}`; and the single most plausible confusion — "every packet crosses all `n` dimensions, route length `= n`" — is shown false on the same evidence (rejected at `|z_foil| = 2190.1`).

## Verifier source integrity

Byte-identical to idea-engine's firsthand verifier `ideas/fleet/verify_257_valiant_permutation_routing.py` (verifier file sha256 `7e204af1e5ee84e8c89fccc975d6e3ffa3440571e5d049f16c5af3faf819c7d4`). The copy in this directory carries the same sha256 and `diff` against the source is empty (exit 0), confirming a byte-for-byte reproduction of the source verifier; stdlib-only (json, hashlib, math, random, fractions, sys), no network, SEED hardcoded. The hashed results payload is a pure function of SEED and the module constants (`random.seed(SEED)` is re-seeded at the START of each Monte-Carlo gate so gate order cannot perturb the payload; every Monte-Carlo-derived float enters the hashed payload rounded to 6 decimals and every exact ratio as a `str(Fraction)`; no wall-clock / PID / unordered-set iteration enters the hashed payload), so the in-process double-run, the `--selfcheck` path, and the separate subprocess re-invocation are byte-identical and the `results_sha256 = fd0d970057a36cfb2f2ad3dfe200acd09d3ce303d8c1eb37e88f6d3330fa759f` reproduces exactly.

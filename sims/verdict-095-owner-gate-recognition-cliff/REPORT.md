# VERDICT 095 — REJECT — the fail-safe with one eye (P082, owner-gate-recognition-cliff)

**Ruling: REJECT** (the sold fail-safe sentence — GOTCHAS.md #4: "a stray
checkbox edit (agents reflow markdown all the time) can only ever RE-QUEUE
an owner action, never silently drop one" — is wrong as doctrine: the
both-marks asymmetry protects DISPOSITION only. Recognition is an
unguarded four-way conjunction (⚑-first-after-lstrip ∧ literal
`**Owner:**` ∧ `^- \[[ xX]\]\s+` shape ∧ flush-left position) where 18 of
the 26 registered single-edit cells silently drop an owner click, a queue
decision, a DONE row, or an armed kill-clock checkpoint — every one with
lint 0 AND manual 0, because every lint error class fires only AFTER
recognition: the strict lint (the mode "you gate a pull request on")
shares the tolerant deriver's recognition predicate and is structurally
blind exactly where derive drops. One character on the DONE row disarms
BOTH armed kill-clock checkpoints in the same silent edit, and the parser
fails safe at FILE granularity while failing open at ROW granularity) —
per the pre-registered rule applied in the registered order REJECT →
INVALID → APPROVE → NULL (REJECT evaluated FIRST and fires on all four
clauses R1–R4; twin independently-written decision evaluators agree
REJECT/REJECT over an ENUMERATED 64-row boolean input set; every decision
number a seedless exact parse census over the registered finite grid).

43 self-checks, 43 passed, 0 failed; exit 0 on the accepted run (the first
complete in-repo run of the committed pipeline — rehearsed to green on
untracked outputs before the runner commit: **41/43 on the first rehearsal
run, TWO disclosed rehearsal fixes** — the LOSSY-definition refinement
(retire-to-Live is disposition, not loss; fixed in fixtures BEFORE the
runner commit, changing no grid or Arm-R class — both registered
class-stream digests reproduced before and after) and the expected-heads
map for the section-destroying OP13 cell — then 43/43, byte-identity also
rehearsed; **zero fix-forwards** after the runner landed); ≈ 2.7 s/run;
hermetic — shipped-parser replay head, stdlib only, zero repo/network
reads at verdict time (the model replays FIVE pinned source behaviors read
from ~340 lines of parser source across two verbatim-identical copies at
the grounding HEAD venture-lab @ 95e1846 — the source-semantics NULL axis
priced exactly that, and did not fire: every census derived from the
pinned semantics landed on the registered values, including both Arm-R
class-stream digests). CPython 3.11 pinned and asserted (Arms A/B are
seedless exact parsing and integer counting, platform-independent; only
reporting-only Arm R and the presentation shuffle touch the pinned minor's
`random` module). stdout + results.json byte-identical across two full
in-repo process runs by external diff + sha256:

- `results.json` sha256 `34a8bc8e6fd347428f97559f18b671a9cc1f9409e002d263dca26053998a0aa6`
- `run-stdout.txt` sha256 `8763f97e735b4de50f4618836a5bef40bf3bb3da8ab3d0c1aaefa8216648401d`

**Anomaly census (structured by disclosure coverage, the V087–V094
convention): 145 compared-and-matched, 0 mismatched, 7 vacant.** Every
registered numeral in the P082 registration — decision AND reporting —
reproduced exactly from scratch: the C0 baseline tuple (3 steps · 1
decision · 4 pending · 1 done · 1 non-owner · 2 checkpoints parsed, 2
armed · blocked · lint 0 · manual 0 · conservation 4+1+1 == 6 checkbox
rows, 11 components), the C1 once-live baseline (parsed 2 / armed 0,
pending 4, done 0, lint 0 — 5), the C2 two-decision baseline (decisions 2,
lint 0 — 2), the class totals {SILENT-LOSSY 18, ALARMED-LOSSY 3,
ALARMED-SAFE 4, SILENT-REGRADE 1} (4), the 18-cell SILENT-LOSSY membership
list verbatim with cell coordinates (18), the nine key-cell tuples ×4
components (OP1@r1 [3,1,2,0] · OP1@r5/OP2@r5/OP4@r5 [4,0,0,0] · OP3@r1
[3,1,2,0] vanish · OP3@r2 [3,1,2,0] merge · OP3@r5 [4,0,0,1] · OP11@r5
[5,0,0,1] · OP10@r5 [4,1,2,1] — 36), the OP12 tuple parsed 0/armed 0/lint
0 (3), the OP13 tuple pending 0/manual 1/lint 1 (3), the disposition 2×2
×4 components (NO-OP [4,1,2,0] · queue+lint [4,1,2,1] · queue+lint
[4,1,2,1] · RETIRE-LIVE [3,2,2,0] — 16), the position law (vanish pending
3 · merge pending 3 · merged-WHAT containment — 3), the repair catch
census (18/18 silent caught + extras exactly {OP3@r5, OP13@heading} + 0
false positives on {C0, C1, C2} + missed set exactly {OP14@cont4} — 24),
the four F4 pencil worlds (4), and Arm R — both preview censuses at all
six classes each (SILENT-LOSSY 12,856 · ALARMED-SAFE 2,770 · ALARMED-LOSSY
1,820 · SILENT-REGRADE 1,293 · NO-EDIT 1,004 · INAPPLICABLE 257 @
20261722; 5,203 / 1,095 / 694 / 516 / 407 / 85 @ 20261723), both
registered class-stream digests (`e8d8812b3f9c`, `fc2709073718`), and both
draw sentinels (60,000 / 24,000) — 16. The seven honest vacancies
(sim-chosen realizations of shape-registered slots, disclosed in
fixtures.json before the runner, ledgered as vacancy-derived disclosures —
never match claims): the corpus WHAT wording (the registration pins C0's
structure and every C0-derived observable but not the literal click
wording; the witness that the realization is exact is the F3 battery
INCLUDING both class-stream digests, functions of the full 78-pair class
map), the target-line resolution convention (targets pinned as C0 1-based
line numbers applied at the same index in every corpus; out-of-range =
INAPPLICABLE, anchor-absent = NO-EDIT), the 2×2 realization on r1 with
date 2026-07-14, the class-stream digest procedure (sha256 of the
concatenated class labels, hexdigest[:12]), the salt-logging realization
(sum + first 5 per seed), the presentation-shuffle target (the stdout
census-table display order), and the merged/vanished-row accounting used
by the F1 conservation identity.

## The decision clauses (all exact)

- **R1 — THE TRUE SENTENCE SURVIVES (fires; checked, not assumed).** The
  disposition 2×2 on the sentence's own grid is exactly fail-safe:
  (unchecked, no DONE) NO-OP [4,1,2,0] · (checked, no DONE) still queues +
  lint 1 [4,1,2,1] · (unchecked, DONE) still queues + lint 1 [4,1,2,1] ·
  (checked, valid DONE) retires to Live [3,2,2,0] with lint 0 — ZERO
  silent cells where the GOTCHA actually looks, and lowercase `done`
  (OP11) re-queues the CLICK exactly as promised (pending 4 → 5, lint 1)
  while silently disarming the clock it rode (done 0, armed 0). The
  verdict prices the doctrine, not the sentence's own grid.
- **R2 — THE RECOGNITION CLIFF (fires).** The 26-cell census lands exactly
  at the registered class totals **{SILENT-LOSSY 18, ALARMED-LOSSY 3,
  ALARMED-SAFE 4, SILENT-REGRADE 1}** with the SILENT-LOSSY membership
  list verbatim (OP1@r1–r5, OP2@r1–r5, OP3@r1, OP3@r2, OP4@r1, OP4@r3,
  OP4@r5, OP5@r1, OP6@step2, OP12@kill), and the lint-downstream law holds
  on every one: **all 18 SILENT cells land lint == 0 AND manual == 0** —
  one lost asterisk drops the row at all 5 targets, bolding the flag drops
  all 5, one leading space vanishes r1 but MERGES r2 into the click above
  (the position law: both pending 3, the merged WHAT containing both
  clicks' text — containment asserted), a bullet swap drops all 3 targets,
  bracket damage drops, deleting the step's ⚑ silently deletes the
  DECISION, and `KILL-CHECK:` → `KILL CHECK:` silently disarms BOTH
  checkpoints (parsed 0, armed 0, lint 0). The guard and the hazard share
  one predicate.
- **R3 — THE CASCADE + ONCE-LIVE (fires).** OP1@r5, OP2@r5, OP4@r5 each
  land **done 0 · armed 0 · lint 0** (pending 4) — one character on one
  row disarms every kill-clock checkpoint of the launch, because arming is
  gated on a recognized DONE row; the no-DONE control corpus C1 registers
  the once-live rule directly at **parsed 2 / armed 0** with everything
  else unchanged (same 4 pending texts, lint 0). check_kill_clocks.py
  reads checkpoints through this exact parser, so the T+7/T+30 clocks go
  dark in the same silent edit.
- **R4 — THE GRANULARITY INVERSION + PRICED REPAIR (fires).** Breaking the
  FILE (OP13 heading demote) is GUARDED — manual 1, lint 1, the whole file
  lands in Manual review — while the R2 row-level breaks are silent: the
  parser fails safe at file granularity and fails open at row granularity.
  The conservation-lint repair arm reproduces its registered catch census
  exactly: a LOOSER net the deriver does not use (owner-ish bullet/⚑
  lines, default-ish steps, kill-ish ⏲ lines) catches **18/18
  SILENT-LOSSY cells plus {OP3@r5, OP13@heading}**, with **0 false
  positives on all three clean corpora**, missing only the one
  SILENT-REGRADE cell (OP14: deleting "(blocking)" silently un-gates the
  ordering — a semantic edit no net predicate sees; disclosed open).
- **APPROVE (does not fire, and is arithmetically excluded):** APPROVE
  requires ZERO SILENT-LOSSY cells on the registered grid; the measured
  census has 18, and the OP1@r1 pencil cell alone excludes it
  (`**Owner:**` is not a substring of `*Owner:**` — computed in-sim as
  F4a). Mutually exclusive with REJECT, as registered.

## Twin arms and contacts

Arm A (regex-faithful replay of the five pinned behaviors — recognition
predicate, row-shape/DONE/KILL-CHECK/gate-heading regexes, the fold rule
with its closure/invisibility clauses, checked-only case-sensitive DONE
disposition, gate-section + once-live arming — plus the strict lint with
every error class conditioned on recognition, and the conservation-lint
repair arm) and Arm B (INDEPENDENTLY-WRITTEN character-level twin: no row
regexes — per-character line classification plus an explicit index fold
walk over parallel state/text lists) are tied through the four typed
must-equal contacts: **C1** Arm B == Arm A on the three baseline tuples,
**C2** Arm B == Arm A on all 26 grid cells and all four 2×2 cells (full
observable tuple including merged/vanished accounting), **C3** the two
independently-written decision evaluators agree on the ruling token over
the ENUMERATED 64-row boolean input set (REJECT/REJECT on the measured
inputs), **C4** Arm R per-trace A == B on every trace of both seeds, draw
counters exactly 3N (60,000 / 24,000), and both registered class-stream
digests reproduced (`e8d8812b3f9c` @ 20261722, `fc2709073718` @ 20261723),
with in-process double-run determinism verified per seed. Presentation
seed 20261724 read by the presentation leg only (the census-table display
order), after every decision leg finished; aux seed 20261725 never read.

## Margin ledger (typed — the V086 convention)

- **Exact-equality cells BY REGISTRATION (the head's own subject):** the
  three baseline tuples, the 26-cell class census and its totals
  {18, 3, 4, 1}, the SILENT-LOSSY membership list (verbatim, with cell
  coordinates), the nine key-cell tuples, the OP12/OP13 tuples, the 2×2
  row, the cascade triple, the once-live pair (2, 0), the position-law
  pair with its containment assertion, the repair catch census
  (18/18 + 2 + 0 FP + missed {OP14}), both Arm-R censuses, both
  class-stream digests, and both 3N draw sentinels. All integer censuses
  or exact string equality — no tolerance anywhere.
- **The margin-0 pairs, registered AS margin-0:** the recognition
  conjuncts themselves — one asterisk (OP1), one bold wrap (OP2), one
  leading space (OP3), one bullet character (OP4), one bracket space
  (OP5), one ⚑ (OP6), one hyphen in `KILL-CHECK:` (OP12) — each single
  edit lands on the opposite side of recognition, which IS the claim; and
  the case pair DONE/done (OP11), one case flip disarming the clock while
  re-queueing the click.
- **The strict inequality:** the evaluator's SILENT-LOSSY ≥ 15 threshold
  carries margin 3 below the measured-and-registered 18.
- No UNregistered decision comparison sits at margin 0; every clause input
  is an exact parse census over the registered finite grid.

## Falsifiability (was real)

The source-semantics axis was live: the five behaviors were pinned by
READING ~340 lines of parser source across two copies, and one misread of
the fold-closure rule, the checked-only DONE rule, or the
substring-vs-token recognition would have moved the position cells, the
cascade cells, and the 2×2 together — the sim re-derived all of them from
the pinned semantics with an independently-written character-level twin
(Arm B exists because regex-reading is exactly where a misread hides), and
every census landed on the registered values. The guarded world was one
real code path away: had lint carried any recognition-independent error
class the drafter missed, the SILENT census would have collapsed and
APPROVE's zero-silent condition come live — it did not (18/18 silent cells
lint 0, manual 0). The corpus world was priced by the corpus-convention
NULL: a registered cell census failing under the corpus as pinned would
have shipped the corrected cell — none failed, and the strongest available
witness that the reconstructed corpus realizes the registered one exactly
is the pair of Arm-R class-stream digests (functions of the full 78-pair
class map over all three corpora), both reproduced. A single moved census
would have killed its clause and the registered rule would have issued
INVALID or the honest NULL; none moved.

## Scope boundaries (stated, per the registration)

- **The source-semantics boundary:** the model replays five PINNED
  behaviors read from ~340 lines across the two shipped copies
  (candidates/owner-click-queue-kit/ocq.py and
  scripts/derive_owner_queue.py, recognition predicate verbatim-identical)
  at the grounding HEAD venture-lab @ 95e1846, not the function objects;
  the ruling binds the shipped parser pair as pinned.
- **The witness-space boundary:** the 26-cell grid is a WITNESS space
  registered to exercise each recognition conjunct and each disposition
  bit — the decision claim is existential (silent-drop cells EXIST and the
  lint is blind on all of them); prevalence rides only the reporting-only
  Arm R and is stated as such.
- **The corpus boundary:** C0 is synthetic-canonical (modeled on the OCQK
  packet's own §7 block, not copied from it); cell censuses are
  corpus-relative, and the two variant corpora control the two structural
  toggles (a DONE row's presence; decision multiplicity).
- **The scope boundary:** the ruling binds the shipped parser pair at the
  grounding HEAD; the render layer, the discovery walk, and the GOTCHAS #2
  union re-derive race are named follow-ups, none in scope.

## Consequence hand-off (pre-registered; routing is the manager's per Q-0260)

Verdict consumer: venture-lab — the lane owns both parser copies, the
derived queue (docs/publishing/OWNER-QUEUE.md, 26 vetting packets),
scripts/check_kill_clocks.py (imports the same parse),
scripts/lint_owner_gates.py, the GOTCHAS.md #4 sentence, and the $19
Owner-Click Queue Kit whose zip sells the sentence and the parser
together. Per the proposal's pre-registered REJECT consequence,
paste-ready structured choice, recommendation first (Q-0263.2):
**(a, recommended)** the conservation lint, zero recognition change: one
function per lint path (ocq.py `cmd_lint` + scripts/lint_owner_gates.py)
counting the LOOSER net (bullet-ish or ⚑ line whose markup-stripped text
contains "owner:"; numbered step containing "(default"; ⏲ line containing
"kill") and erroring when strict recognition counts fewer — priced on the
registered grid: 18/18 silent cells caught, 0 false positives on all three
corpora, the one semantic-regrade cell disclosed as out of reach; plus
replace the GOTCHAS #4 sentence with the two-sided disclosure (disposition
fail-safe TRUE / recognition unguarded / the lint shares the predicate —
three sentences). **(b)** recognition-weakening (accept unbolded `Owner:`,
the flag anywhere before the token, `*` bullets, one-space indents) —
shrinks the silent space at the price of false-positive recognition; the
seat-row exclusion is BY DESIGN (`test_only_owner_rows_become_clicks` pins
it), and the exclusion-vs-recognition tension is why (a) dominates.
**(c)** status quo + document: amend GOTCHAS #4 and the packet's §1 test
claim with the boundary sentence, and kill-clock consumers add their own
checkpoint-count check. Known co-consumers: the kill-clock verdict pair's
laws (P050/P066 priced the clock's horizon and anchor ASSUMING the
checkpoint is read — a silent disarm is upstream of both), the OCQK buyer
population, idea-engine's own scripts/check_sections.py (ASK 005's roster
breakage is the same failure class — recognition drift in a consumer
parser; the second-net conservation repair transfers), and — the
transferable audit — every fleet surface pairing a tolerant deriver with a
strict lint: a lint that shares the deriver's recognition predicate can
NEVER see recognition loss; conservation checks need a predicate the
deriver does NOT use. Named follow-ups, none in scope: the render layer,
the discovery walk, the GOTCHAS #2 union re-derive race.

## Seeds

Arm-R reporting-only: 20261722 (N = 20,000), 20261723 (N = 8,000) under
the registered draw-order grammar (per trace exactly 3 `randint` draws in
order corpus ∈ [0,2], cell ∈ [1,26], salt ∈ [1,1000] drawn-and-logged; one
`random.Random` per seed; draw-count sentinels asserted at exactly 3N);
presentation shuffle 20261724 (presentation leg only); aux 20261725
reserved and never read. Seeds 20261718–721 are P081/V094's registered
set — untouched; the allocation started at 20261722 per the V094 heartbeat
baton, and the next free block starts at **20261726**. No seed touches any
decision arm.

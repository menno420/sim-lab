# VERDICT 092 — REJECT — the milestone that cannot move (P079, owned-track launch-flag dial)

**Ruling: REJECT** (the owned track, as shipped, is a launch-configuration
predicate, not a progression surface — achievements-v0's progression
framing and play.py's "touches no economy constant" sentence cannot both
stand) — per the pre-registered rule applied in the registered order
REJECT → INVALID → APPROVE → NULL (REJECT evaluated FIRST and fires on
all four clauses R1–R4; twin independently-written decision evaluators
agree REJECT/REJECT over an ENUMERATED boolean input set; every decision
number an exact integer on the vendored engine).

61 self-checks, 61 passed, 0 failed; exit 0 on the accepted run (the
first complete in-repo run of the committed pipeline — rehearsed to green
on untracked outputs before the runner commit: 61/61 on the first
rehearsal run, ONE rehearsal fix, bytecode-cache hygiene only, no
decision value touched; zero fix-forwards after the runner landed);
≈ 7 s/run; hermetic — the engine + runtime + catalog + gate + schema are
a byte-for-byte vendored copy of superbot-idle @
`884aeae9687742a389a2e2086a4cc930e5a4f3ee` (35 files, sha256-manifest
verified at every run start against fixtures.json; the pin re-fetched
FIRSTHAND this session on a fresh public-HTTPS clone — the lane had not
moved off the drafter's registered HEAD, and every committed constant was
re-verified on that clone BEFORE the fixture was written). CPython 3.11
pinned and asserted. Environment: PyYAML + jsonschema present (the
vendored lane's own committed dependencies — theme_gate.py's docstring
pins the pip-install convention; jsonschema was pip-installed once in
this session, recorded here); no third-party call sits in any decision
path of the runner itself. stdout + results.json byte-identical across
two full in-repo process runs by external diff + sha256:

- `results.json` sha256 `ea112279781ec0376c442686ab60ca782df2d8f7ab9178e4393c409d1de8ba51`
- `run-stdout.txt` sha256 `1d60cca06e1b3c30e3de8bcbe1affa7a53074461282db379577cdb1c6d259f78`

**Anomaly census (structured by disclosure coverage, the V087–V091
convention): 40 compared-and-matched, 0 mismatched, 5 vacant.** Every
registered numeral in the P079 registration — decision AND reporting —
reproduced exactly from scratch: the seven committed-sentence pins
(whitespace-normalized, present verbatim at the vendored pin), the
catalog censuses (18 packs, roster multiset {1×1, 2×17}, base-rate
multiset {1×18, 5×17}, zero balance blocks, 54 = 18·3 skinned owned
slots), the identities 216 = 18·12 and 648 = 216·3, the ladder constants
(10, 100, 1_000) at +5%, invariance 216/216 (zero violations at every
boundary of the 11-command corpus), dichotomy 216/216 at the first
boundary AND 216/216 post-corpus, the C4 contact 648/648, the prestige
re-grant fixed point ({tier1: 5, tier2: 5} at the registered cell and on
all 198 performed cycles, with all 18 s=0 refusals), the margin-0 flip
quadruple (s·n == θ exactly; earned at s\*, refused at s\*−1 — quantified
over the WHOLE pack class: 5/50/500 on all 17 two-generator packs,
10 on egg-farm), the lock lines exactly "🔒 2 / 10" (17 packs) and
"🔒 1 / 10" (egg-farm) with all 54 owned fields byte-stable across the
corpus, the dial cells (10→10, 19→19, 20→21; 30→31 with the 5→5
floor-eaten / 25→26 split; 3000→3450 = +15% exactly on the registered
owned-only pinned states, wedge percents [100, 105, 115]), the lag pairs
(900, 930) and (108,000, 118,800) at second-span pct 115, the theme gate
exit 0 over the assembled 20-pack catalog, the born-blessed 210 vs 180
witness rates, and all three Arm-R preview triples ((0, 1392, 12874) /
(0, 1395, 12779) / (0, 1415, 13179)) under the registered draw-order
grammar. The five honest vacancies (sim-chosen realizations of
shape-registered slots, disclosed in fixtures.json, ledgered as
vacancy-derived disclosures — never match claims): the exact 11-command
corpus listing, the Arm-R blessed/buy-max byte conventions (the
registered TRIPLES are the match claims, and they reproduce exactly —
the blessed closed form was additionally asserted equal to the live
earned-set census on all 6,000 traces), the witness-pack ids/nouns, the
owned-2/owned-3 lock-line byte-forms ("🔒 2 / 100", "🔒 2 / 1,000" —
same frozen metric, engine `_format_amount` spelling), and the
extension/grid-bump control values.

## The decision clauses (all exact)

- **R1 — INVARIANCE + DICHOTOMY (fires).** Total owned == s·n at every
  action boundary of the 11-command canonical corpus (a full prestige
  cycle included) on all 216 cells — zero violations, plus zero at all
  38,832 random Arm-R boundaries ×2 runs. The earned owned set equals the
  closed-form predicate {owned-k : s·n ≥ θ_k} at the FIRST boundary
  (before any player decision) and after the full corpus on all 216
  cells; the prestige wipe + runtime re-grant is a verified fixed point.
  Structural core confirmed live: `apply_prestige` (wipe) and the
  runtime re-grant are the only owned writers on the no-load surface.
- **R2 — CATALOG CENSUS (fires).** The shipped catalog lands exactly
  (18 packs, {1×1, 2×17}, 54 skinned owned slots); at the default flag
  every pack's total owned is 1 or 2, so ALL 54 owned slots are
  unreachable, rendered as immovable live lock lines "🔒 2 / 10" /
  "🔒 1 / 10", byte-stable across the whole corpus including the
  prestige reset.
- **R3 — THE DIAL (fires).** Flip thresholds exactly ceil(θ/n) —
  10/100/1,000 on egg-farm, 5/50/500 on every two-generator pack — with
  the four margin-0 rows earned-at-s\*/refused-at-s\*−1. The flag is a
  permanent global production dial: s=500 on a two-generator pack births
  all three rungs and moves 3,000 → 3,450/s (+15% exactly, no floor loss
  at that fold: 575 + 2875). The dial is floor-gated (V038's committed
  "felt = integer rate delta" imported): egg-farm s ∈ {10, 19} EARNS
  owned-1 whose +5% pays ZERO (10→10, 19→19; first paying flag exactly
  20: 20→21); at the two-gen flip s=5 the whole payout is +1/s, all on
  tier-2 (25→26), tier-1 floor-eaten (5→5), total 30→31. The award is
  late even when free: first 30 s span pays 900 (30/s), second 930
  (31/s); a 3,600 s first span compounds the lag — 108,000 then 118,800
  (33/s at pct 115, owned-1 + lifetime-1 + lifetime-2 banked together).
- **R4 — THE ROSTER BACK DOOR (fires).** The constructed 10-generator
  base-20 pack and its 9-generator twin (differing ONLY in roster
  length, asserted field-by-field) BOTH pass the committed theme gate —
  exit 0 on the assembled 20-pack catalog, "theme-gate: all 20 pack(s)
  valid (schema v1)" — and at the DEFAULT flag the 10-gen pack is born
  owning 10, earns owned-1 at its first boundary, and runs 210/s vs the
  twin's 180/s, permanently: a +5% channel the theme-balance neutrality
  pin cannot see (it audits the declared knob, not roster cardinality).
- **APPROVE (does not fire, and the witness is real):** no committed
  no-load command sequence at the default flag changed total owned or
  earned any owned rung (measured over the corpus on all 18 packs); the
  APPROVE witness world (an engine carrying V017's unlock-only purchase
  verb) is constructible, so the ruling genuinely separates the shipped
  game from its planned successor.

## Twin arms and contacts

Arm A (seedless exact integer closed forms) and Arm B (the REAL vendored
engine + runtime driven through `play.dispatch` / `play.advance` by an
independently-written walker with its own bookkeeping) are tied through
the four typed must-equal contacts: C1 (18 == pack glob == census rows),
C2 (54 == 18·3 == frozen rendered owned fields at the default flag), C3
(the four margin-0 rows: s·n == θ exactly AND earned at s\* AND refused
at s\*−1), C4 (Arm-A predicate == Arm-B live award on all 648 cell-rungs,
first boundary and post-corpus). Arm R (seeds 20261710/711/712, 2,000
traces each, REPORTING-ONLY, no statistical gate) reproduced all three
registered preview triples exactly under the registered draw-order
grammar, with draw-count sentinels (3 + 2L per trace) asserted on every
trace and in-process double-run determinism verified per seed; aux seed
20261713 never read.

## Margin ledger (typed — the V086 convention)

- **Margin-0 BY REGISTRATION (the head's own subject):** the four flip
  contacts s·n == θ exactly ((2-gen, s=5, θ=10), (egg-farm, s=10, θ=10),
  (2-gen, s=50, θ=100), (2-gen, s=500, θ=1000)) — each verified earned
  at s\* and refused at s\*−1, so the equality is load-bearing and
  two-sided, not a knife-edge left dangling.
- **Floor-eaten zero-margin cells (registered as such):** egg-farm
  s ∈ {10, 19} (+5% pays exactly 0), the tier-1 split 5→5 at the two-gen
  flip; first paying flag exactly 20 (delta exactly 1); the witness
  base_rate 20 chosen ledger-clean (20·105//100 == 21 exactly).
- **No-floor-loss contact:** 3450 == 3000·115//100 with per-tier
  575 + 2875 exact.
- No UNregistered decision comparison sits at margin 0; every clause
  input is an exact integer equality or a strict integer inequality.

## Falsifiability (was real)

A second committed owned-writer anywhere in the play surface would have
dissolved R1 (none exists on the no-load surface — verified live across
the corpus, the extension control, and 38,832 random boundaries); a
committed pack with ≥ 10 generators would have flipped R2's census from
"unreachable" to "born-blessed" (the catalog sweep found max roster 2 —
and that flip is exactly T5's point, demonstrated on the constructed
witness); a runtime awarding milestones inside spans would have dissolved
the lag pairs (measured: 900 ≠ 930 and 108,000 ≠ 118,800 — awarding is
boundary-only, as committed); the NULL convention axes were swept and
non-flipping (corpus extension to 16 commands: invariance holds on all
216 cells; grid bump s ∈ {2, 7, 1000}: invariance + dichotomy hold on
all 18 packs; s=0 degenerate: empty grant, zero production, prestige
refused, invariant 0).

## Scope boundaries (stated, per the registration)

- **The load boundary:** `load` is blob-authoritative by committed design
  (persistence validates types, not rosters), so every reachability claim
  is scoped to the no-load command surface — disclosed NULL axis, not a
  defect.
- **The shipped-surface boundary:** the ruling binds superbot-idle @
  `884aeae` only; a generator purchase verb landing is the named re-run
  event (V017's conditional registration row is the arming event), never
  a refutation.
- **The perception boundary:** the T4 zero-pay rows read "pays" as
  integer rate delta (V038's committed definition); the engine exposes no
  fractional-rate UI.

## Consequence hand-off (pre-registered; routing is the manager's per Q-0260)

REJECT → paste-ready structured choice to the superbot-idle lane,
recommendation first (Q-0263.2), timed to land BEFORE the
generator-purchase build slice re-registers the economy (V017's
conditional row is already waiting):

- **(a, recommended)** re-scope the owned track NOW, one doc revision +
  one docstring amendment, zero engine code: achievements-v0.md
  pre-registers the owned rungs as DORMANT until the purchase verb lands
  (V017's conditional registration row named as the arming event);
  play.py's grant docstring drops "touches no economy constant" and
  states the true coupling ("the grant sets the owned-milestone birth
  predicate; values ≥ 5 on a two-generator pack pre-earn owned-1");
  theme-balance-v0.md gains the roster-cardinality note (a ≥ 10-generator
  pack is a balance change and routes through Q-0264 — closing the T5
  back door by pre-registration, not code).
- **(b)** make the UI honest instead: render the three owned slots as
  "awaits a future mechanic" (a render.py presentation change) —
  smallest player-facing fix, leaves both committed sentences false at
  the code level.
- **(c)** enforce at the gate: theme-gate rejects packs with ≥ 10
  declared generators and the runtime clamps `--start-count` below the
  first flip threshold until the purchase slice lands — at the price of
  two new committed constants the purchase slice must immediately unwind.

Known co-consumers: V017's open conditional registration (the
owned-ladder renumbering it flagged), V070's application guard (its
"a multi-generator world re-opens the owned rungs" question now has its
census: re-opened and still frozen), V090's smallest-pre-floor-product
audit (the T4 zero-pay dial rows), and every fleet lane shipping a
progress UI over a metric no shipped verb can move — the transferable
audit: for each rendered progress bar, name the committed verb that
increments its metric; a bar with no verb is configuration wearing a
goal's clothes, and its reward is a dial someone controls from outside
the game.

## Seeds

Arm-R reporting-only: 20261710, 20261711, 20261712 (2,000 traces each,
the registered draw-order grammar, draw-count sentinels asserted); aux
20261713 reserved and never read. The gap 20261704–709 is the disclosed
in-flight buffer (P078/V091's registered set ends at 20261703). No seed
touches any decision arm.

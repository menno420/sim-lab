# verdict-080 — the stale-ink mirror (INTAKE 067)

Prices idea-engine PROPOSAL 067's harvested incoherence where it lives:
Wickroad's committed hook ("every trip is a bet on your own stale
information") against the same tree's committed price law (a 12-day integer
triangle wave at a fixed world seed). Three registered integer theorems —
photograph at lag 12 (T1), exact mirror at lag 6 (T2), and the two-morning
(base, phase) decode (T3) — plus the exact E/I ink-error/inversion lag
surfaces and a four-arm belief-controlled greedy planner census
(ORACLE/SCRYER/INKTRUTH/BLIND, shared scaffold, only the belief function
varies) on the shipped world, decks ON at the decision cell. Judged against
pre-registered bands, REJECT-first (R1 mirror identities exact; R2 SCRYER ≥
(9/10)·ORACLE; R3 the theorem conjunct), then INVALID, then APPROVE, then
NULL on named axes n1–n4. Fully hermetic: the runner reads ONLY
`fixtures.json` (committed before the runner); zero repo/network reads at
verdict time; every constant was re-verified FIRSTHAND against Wickroad's
source at the registered pin gba-homebrew `c0c6882` before the fixtures were
written.

## Run

```
python3 sims/verdict-080-stale-ink-mirror/stale_ink_mirror_sim.py
```

CPython 3.11 pinned and asserted. Deterministic: stdout and `results.json`
are byte-identical across process runs (verified by external diff + sha256 —
see REPORT.md). Stdlib-only. Every decision number is an exact integer or
exact rational; the only seeded arm (R) draws alternative world seeds — each
per-world row is itself a deterministic exact planner run.

## Files

- `fixtures.json` — every registered constant verbatim from the PROPOSAL 067
  block / idea doc (the committed world: 5 towns × 4 goods, price bands,
  drift steps, produce gaps, the xorshift32 init at seed 0x5749434B with its
  fixed consumption order and rotation; tri12; the three authored decks;
  the F1 anchor tables; the F3 closed forms; the shared greedy scaffold and
  the four belief functions; decision constants {9/10, 3/4, 11/10, 1/2};
  Arm-R parameters; seeds 20261590–593; the harvested sentences with the
  FIRSTHAND pin), plus fixture-level conventions C1–C12 — committed BEFORE
  the runner existed.
- `stale_ink_mirror_sim.py` — three-arm runner: Arm A seedless exact-integer
  surfaces + theorem enumerations + greedy census (decision-bearing), Arm B
  independently-written literal day-walk twin (exact-equal on every
  published number; second decision evaluator), Arm R seeded 200-world
  census (per-world rows exact; read by the registered n3 axis per C11).
- `results.json`, `run-stdout.txt` — the accepted run's outputs.
- `REPORT.md` — ruling, theorem verdicts (including the T3b counterexample
  census), the four-arm tables, Arm R, gates, anomalies, boundaries, the
  pre-registered consequence.

## Ruling

**NULL on registered axes n4 + n3** (see REPORT.md; the ruling token is
issued by the twin evaluators per the pre-registered order REJECT → INVALID
→ APPROVE → NULL). R1 and R2 hold exactly on the committed world; the
REJECT is blocked by R3 — the registered two-morning decode theorem (T3b)
is FALSE (0 of 24,360 unclamped consecutive-pair cases decode uniquely;
candidate counts 2–6) — and by the registered world-fragility axis (R2
replicates on only 56/200 alternative worlds; stability set 41/200).

# VERDICT 090 — REJECT — the festival the engine cannot show (P077, event-fold visibility floor)

**Ruling: REJECT** — per the pre-registered rule applied in the registered
order REJECT → INVALID → APPROVE → NULL (REJECT evaluated FIRST and fires
on all three clauses; twin independently-written decision evaluators agree
REJECT/REJECT; every decision number an exact integer or Fraction).

74 self-checks, 74 passed, 0 failed; exit 0 on the accepted run; ~15 s/run
(the Arm-B Fraction walk is the heaviest leg); stdlib-only; hermetic
(reads only its own fixtures.json — every COMMITTED external constant was
re-verified firsthand at superbot-idle
`884aeae9687742a389a2e2086a4cc930e5a4f3ee` BEFORE the fixture was
written: the § 2 fold + graduation identity verbatim, § 1a's promise
sentence, SE-2, § 3's "state_version stays 2", § 6's "nothing is built",
engine.py's single-floor fold with count INSIDE, economy.py's 25/10/5,
the 18-pack {1×18, 5×17} base-rate multiset with zero balance blocks,
state.py's 90..110 bounds — zero harvest anomalies); CPython 3.11 pinned
and asserted. stdout + results.json byte-identical across two full
in-repo process runs by external diff + sha256:

- `results.json` sha256 `f16a1ca9fc67edb695e98eefff63e2fbcc6453f4d8bf966e6344d51f298deaf5`
- `run-stdout.txt` sha256 `84b7f06d5299f74ec9e228a2bb220427952da7bb5f649d78120e4ddf07503813`

**Anomaly census (structured, the V087–V089 coverage convention): 20
compared-and-matched, 0 mismatched, 5 vacant.** Every disclosed numeral
in the P077 registration — decision AND reporting — reproduced exactly
from scratch: the lattice 214,500 = 2·25·13·11·10·3 with 214,496 alive
and the 4 zero-rate theme-90 cells enumerated by name, the dead-census
row (4,151 / 675 / 56 / 14 / 0), the dead-at-rate-≥20 row (0/0/0/0/0),
the 99/99 dead band with first-paying E exactly 200, the jackpot 20/11
on the registered witness (1,1,0,2,8,110), the minimum-ratio row (10/11,
4/5, 2/3, 4/7, 1), the envelope with both edges attained at every grid
E, all three repair-arm prices (20/11 · census 0 with certificate ≥ 99
and canonical minimum exactly 100 · deviation exactly 1/22 attained at
×1.10 on a rate-20 cell), the pencil worlds (0-pay and 3600-pay
festivals; 1.375·1.5 = 2.0625), the four typed contacts — **and all
three seeded Arm-R previews reproduced EXACTLY under the REGISTERED
draw-order grammar (the V089 💡 applied by the drafter): seed 20261690 →
(49, 40,533,190,487), 20261691 → (41, 39,898,597,051), 20261692 → (43,
39,906,644,697), 30,000 draws per seed counted and asserted.** The
drafter's NO-DERIVED-LITERALS claim survived a zero-trust re-derivation
intact. The five VACANT rows, stated as themselves (values the
registration left unpinned, disclosed in fixtures.json/results.json,
never match claims): the four pencil-calendar cells (registered by
COUNT only — sim-chosen: canonical, jackpot witness, one-upgrade
neighbour, lattice max), the envelope edge-attainment counts, the per-E
realized/nominal maxima beyond ×1.10, the theme-100 sub-lattice census
values, and the alt-fold named control's value.

## The decision clauses (all exact)

**R1 — THE DEAD BAND (fires).** The canonical start cell — every pack's
tier-1 generator at birth (base 1, count 1, L0/u0/m0, theme 100) — has
pre-floor product exactly 10^8 and committed rate exactly 1/s. Under the
doc's own fold the event delta is 0 for **all 99 integer E in 101..199**
(typed contact: 99 = 199 − 101 + 1); the first paying multiplier is
**exactly ×2.00** (E = 200, rate 1 → 2, the registered margin-0
contact); one upgrade is still dead at ×1.5 (1.25·1.5 = 1.875 → 1); the
first ×1.5-paying neighbour needs upgrade + two milestones (x =
137,500,000; x·150 = 20,625,000,000 → rate 2). A 3600 s festival at any
sane bonus pays this player exactly **0** extra units — the precise
audience a re-engagement lever targets. R1 is cap-free (one cell + an
all-E sweep).

**R2 — CENSUS + JACKPOT (fires).** Over the committed lattice (base
{1,5} × count 1..25 × L 0..12 × u 0..10 × m 0..9 × theme {90,100,110} =
214,500 cells; graduation identity asserted on every one; 214,496 alive;
zero-rate cells exactly [(1,1,0,0,0,90), (1,1,0,0,1,90), (1,1,0,0,2,90),
(1,1,0,1,0,90)]): alive-but-event-dead cells number **4,151 (×1.10,
1.94%) / 675 (×1.25) / 56 (×1.50) / 14 (×1.75) / 0 (×2.00)** — the
×2.00 zero certified by floor(2r) ≥ 2·floor(r) > floor(r) for r ≥ 1 on
every alive cell, and payers-at-×2.00 == alive == 214,496 exactly (typed
contact 4). The realized/nominal ratio maxes at **exactly 20/11 at
×1.10** on the rate-1 witness (1,1,0,2,8,110) — a +10% window delivered
as +100% — and mins at 10/11 (the dead start cell: +10% delivered as
+0%); the staircase envelope floor(R·E/100) ≤ RE ≤ ceil((R+1)·E/100) − 1
holds on every alive cell with **both edges attained at every grid E**
(edge-hit counts in results.json). A +10% event at rate 1 is never
delivered as +10% — only as one of the two staircase steps.

**R3 — THE REPAIR FORK (fires; each arm priced, each breaking a
different committed sentence).** (a) V038's registered min-visible-delta
fallback (max(RE, R0+1)): kills deadness by construction but overshoots
the canonical cell at ×1.10 by **exactly the jackpot's 20/11** (typed
contact 2: overshoot == jackpot, both 20/11) — the T6 proportion dies in
the opposite direction. (b) Milli-granularity migration (G = 1000): dead
census **0 across the entire lattice × grid** (certificate: alive ⇒ x ≥
10^8 ⇒ milli-delta at ×1.10 ≥ 99; the lattice minimum IS the canonical
cell's exactly **100**) — at the price of the doc's own § 3 "zero new
save state / state_version stays 2" promise (save v3 + golden-corpus
same-PR policy + economy re-registration). (c) A registered rate-floor
precondition R\* = 20: zero code, dead census 0 above the floor (zero
dead cells at rate ≥ 20 at any grid E), maximum deviation **exactly
1/22 (4.545%)**, attained at ×1.10 on a rate-20 cell — but the start
state stays dead: "for idlers and actives alike" is conceded exactly
where re-engagement aims.

**T5 — the TRUE SENTENCE, confirmed.** Partition equivalence — piecewise
closed-form credit equals the 1-second tick loop byte-for-byte — holds
on the five-segment pencil calendar (7,off)(5,on)(11,off)(3,on)(2,off)
across four cells × all five grid multipliers, including the degenerate
all-zero-delta trace (the canonical cell at ×1.10, which credits exactly
nothing, exactly). The doc's EXACTNESS sentence survives; only its
proportionality sentence dies — the REJECT binds Horn 2, not Horn 1.

## Controls and NULL axes (all landed non-flipping; no NULL issued)

- **Theme-100 committed-packs sub-lattice (NULL axis i):** the canonical
  cell is a theme-100 cell, dead at ×1.10/×1.25/×1.50/×1.75 and paying
  at ×2.00; sub-lattice dead censuses 1,390 / 224 / 18 / 4 / 0 —
  deadness persists in the world the shipped packs actually inhabit.
  Non-flipping, as pre-checked at drafting.
- **Axis-cap bump (NULL axis ii):** count/L/u caps moved one notch
  (26/13/11): arm (c) deviation still exactly 1/22, arm (b) census still
  0, dead-at-rate-≥20 still 0; R1 is cap-free by construction.
  Non-flipping.
- **E-grid convention (NULL axis iii):** T1's sweep is grid-free
  (all integer E in 101..199); census rows are per-E findings, never
  aggregated.
- **E = 100 neutrality:** event delta identically 0 on every alive cell
  (the graduation identity restated).
- **Alt-fold named control (reporting-only):** folding count OUTSIDE the
  single floor is a genuinely DIFFERENT world (×1.10 dead census 60,824
  vs the committed fold's 4,151) — the committed reading (count inside,
  pinned by the doc's code block and engine.py's shipped precedent) is
  load-bearing, and the sim carries the alternative as the registered
  named control.

## Margin ledger (typed — every registered cell an exact equality/zero/saturation)

| cell | type | landed |
| --- | --- | --- |
| first-paying E = 200 | margin-0 contact | rate 1 → 2, delta exactly 1 |
| dead band 99/99 | saturated (whole sane band) | 99 = 199 − 101 + 1 |
| ×2.00 dead census 0 | zero cell, certified | floor(2r) ≥ 2·floor(r) > floor(r) |
| jackpot 20/11 | equality, not clearance | exact, witness (1,1,0,2,8,110) |
| overshoot == jackpot | typed must-equal contact | both exactly 20/11 |
| deviation 1/22 | equality, attained | at ×1.10 on a rate-20 cell |
| milli minimum 100 | equality (canonical); lattice min clearance ≥ 99 | min IS the canonical 100 |
| payers-at-×2 == alive | saturated (every alive cell pays) | 214,496 == 214,496 |

## Falsifiability (was real)

The APPROVE witness world is constructible and named — the arm-(b)
milli-granular engine satisfies the registered APPROVE clause (dead
census 0 at every grid E, realized/nominal within [20/21, 21/20]) and is
mutually exclusive with REJECT by arithmetic; this run measured the
committed engine, not the repaired sibling, and REJECT fired. A
different fold reading (count outside the floor) would have dissolved
T1 — the named control shows that world differs and the committed
reading is pinned verbatim. The perception boundary is V038's committed
"felt = integer rate delta > 0" (a fractional-rate UI would change the
question; the engine exposes none). The value boundary holds: the doc
registers NO bonus values by design; every theorem is per-E, T1
quantifies over the whole sane band, and no ruling here proposes or
blesses a bonus value.

## Consequence hand-off (pre-registered; routing is the manager's per Q-0260)

REJECT → the paste-ready structured choice to the superbot-idle lane,
recommendation first (Q-0263.2), timed to land BEFORE the doc's step-2
build slice re-registers shapes: **(a, recommended)** keep candidate
(a)'s fold UNCHANGED and amend § 1a + SE-2 with the rate-floor scope
(R\* = 20 ⇒ deviation ≤ 1/22) plus the staircase disclosure ("below rate
20 an event pays in integer steps; below ×2 the rate-1 start state pays
zero") — one doc revision, zero code, zero save-state cost; **(b)**
milli-granularity migration in the event-fold PR — true proportionality
within 1/1000 at the committed price of save v3 + the golden-corpus
same-PR policy + economy re-registration; **(c)** extend V038's
min-delta floor to the event fold — no dead cells, low-rate windows
over-pay up to exactly 20/11, the overshoot table attached so the choice
is priced, not vibed. Known co-consumers: the doc's SE-1..SE-5
registration (SE-2 inherits the T1 caveat verbatim), V038's open
min-delta-floor sizing thread (arm (a)'s overshoot column), the
theme-balance gate (the 4 zero-rate theme-90 cells), and every fleet
surface folding stacked integer percents through one floor — the
transferable audit: before promising a PERCENT through a single-floor
integer fold, compute the smallest pre-floor product it serves; if
product × (pct−100)/100 < the fold denominator, the promise is a
staircase, not a proportion.

## Seeds

Registered set 20261690–693 (allocation started at 20261690;
20261684–689 is the disclosed in-flight buffer after P076/V089's
20261680–683): Arm-R reporting-only 20261690/691/692 (10,000 traces
each, 30,000 draws each, counted and asserted; each seed reproduces
itself in-process; previews matched exactly), aux 20261693 reserved and
asserted NEVER read by any leg. No statistical gate anywhere; Arms A/B
are seedless.

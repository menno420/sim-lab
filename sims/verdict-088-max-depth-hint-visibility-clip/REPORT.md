# VERDICT 088 — REJECT — the hint that hides (P075, max_depth hint visibility clip)

**Ruling: REJECT** — per the pre-registered rule applied in the registered
order REJECT → INVALID → APPROVE → NULL (REJECT evaluated FIRST and fires
on all three clauses; twin independently-written decision evaluators agree
REJECT/REJECT; every decision number an exact small integer).

553 self-checks, 553 passed, 0 failed; exit 0 on the accepted run;
~0.1 s/run; stdlib-only; hermetic (reads only its own fixtures.json);
CPython 3.11 pinned and asserted. stdout + results.json byte-identical
across two full in-repo process runs by external diff + sha256:

- `results.json` sha256 `71f5d54c371616f123a52b666b8f5f5a1fec021f3465f83623ec722ac227a1b3`
- `run-stdout.txt` sha256 `3fa6793333f67f32c6849ee33cd353d76ab3d78f606ed7fca82e106439abe6de`

**Anomaly census (structured, the V087 coverage convention): 29
compared-and-matched, 0 mismatched, 1 not-disclosed.** Every disclosed
numeral in the P075 registration — decision AND reporting — reproduced
exactly from scratch: the per-hint invisible census {9, 4, 1, 0} with its
(3−m)² closed form, the 14/24/10 visibility censuses, the 20/12/24 badge
censuses, the m = 0 inversion, the T3 witness facts (both boards, all
four world-view surfaces, the badge inversion, the absent-hint triple
flip), the T4 strict-on-exactly-14 domination, the five G5 hand-world
facts, the G6 repair-world numbers 46 / 0 / 44 with the m_eff = 3
byte-identity, and the meter rows 150 / 300 / falsy-0. The drafter's
NO-DERIVED-LITERALS claim survived a zero-trust re-derivation intact.
The one NOT-DISCLOSED class: the registration disclosed no seeded Arm-R
counts (estimand + seeds only), so the seeded rates print beside the
exact expectations with nothing to compare — vacancy, not match, stated
as itself.

## The decision clauses (all exact)

**R1 — THE FULLY-INVISIBLE CENSUS (fires).** Over the 80-cell lattice the
committed validator accepts (re-derived acceptance: 80/80; the pinned
applicator inventory is EMPTY — no `if`/`then`/`allOf`/`anyOf`/`oneOf`/
`not`/`dependent*` key anywhere in the schema):

- a miner is fully invisible in the world views — no `here` row, no
  `record_only` chip, no minimap `points` entry, **not even
  `unplotted`** — on exactly **14/80 cells**, per present hint
  **(3−m)²** = {m=0: 9, m=1: 4, m=2: 1, m=3: 0};
- every one of the 14 was verified absent from all four served surfaces
  individually (the R1 clause as registered);
- minimap-invisible: **24/80**; ghost-chip-only (a stale
  "Name · record" chip at a shallow band is the ONLY trace): **10/80**.
  The same file's committed doctrine is "listed honestly, never
  silently dropped" (`views.py:310-311` — structurally unable to reach
  a miner whose band is never rendered).

**R2 — THE NON-MONOTONE BADGE (fires).** deep_diver ⇔ r = m_eff on all
80 cells (20/80 earn):

- **12 flip cells** (m ∈ {0, 1, 2} × d ∈ 0..3): raising the record from
  r = m to r = m + 1 — digging ONE band deeper — LOSES the badge whose
  catalog description reads "record depth equals the world's max depth";
- **24 overshoot denials**: the record strictly exceeds the stated world
  max and the badge is denied;
- the **m = 0 inversion**: in a valid surface-only world the never-dug
  newcomer (r = 0) EARNS Deep Diver while the r = 3 explorer is denied.

**R3 — HINT DOMINATION (fires).** Over all 64 (m present, d, r)
comparisons against the absent-hint twin: visibility(absent) ≥
visibility(m) EVERYWHERE, strictly greater on exactly the 14
fully-invisible cells (the strict set equals the invisible set,
cell-for-cell). The optional field can only remove miners from the world
views, never add one — the contract's mandated graceful-absence path is
strictly safer than the informative path.

## The composite witness (T3, transcript in results.json)

One document, accepted by the re-derived validator: m = 2, miner A
(d = 3, r = 3, top coins/XP), miner B (d = 2, r = 2). Served result:
A is **#1 on the depth board AND #1 on the coins board** (the
leaderboards never read the hint — `views.py:245-272`) while appearing
in ZERO ladder bands and ZERO minimap panels; A is NOT deep_diver while
the strictly shallower B IS. Omitting the hint from the SAME document
flips all three at once. Three served surfaces disagree about who is
deepest, inside one response body.

## The hand world (G5, pencil-checkable)

m = 0, four miners (d, r) = P(0,0), Q(1,0), S(2,2), T(3,3): ONE band
rendered; P live; Q's only trace is a ghost chip "Q · record" at the
surface band; S and T fully invisible; deep_diver = {P, Q} — the
never-dug pair holds "Deep Diver", the two actual divers are denied.
Five facts, all verified against the rendered structures.

## Repairs (G6, falsifiability live)

- **(a) conformance applicator** (require d ≤ m ∧ r ≤ m when the hint is
  present): accepted lattice 80 → **46** (16 absent + Σ(m+1)² = 30),
  every invisibility census ZERO, zero surviving flip pairs — the
  registered APPROVE clause fires, live.
- **(b) overflow bands** (render `range(max(m_eff, deepest d or r) + 1)`):
  fully-invisible **0/80** — R1 dies.
- **(c) deep_diver as `>=`**: earns 44/80, ZERO non-monotone flips, and
  is **byte-identical to the committed `==` at m_eff = 3** (r ≤ 3 by
  schema) — R2 dies with today's sample semantics provably untouched.
  Today's committed sample sits exactly ON the degeneracy cell (sample
  m = 3, where every census term is zero), which is why the sample
  calibration cannot see the fault.

## Typed margin ledger (fixtures C5, the V086 convention)

Must-equal registered contacts, all gated two-sided and all exact: the
census set {14, {9,4,1,0}, 24, 10, 20, 12, 24}, strict-domination
count = 14, the G6 numbers {46, 0, 44}, the m_eff = 3 byte-identity
(the registered margin-0 contact (iii) — the head's thesis that today's
world hides the bug), and the meter rows {150, 300, 0}. Registered
knife-edges gated as their exact two-sided laws: (i) d = m_eff is the
LAST visible band (live at contact, invisible one integer past it, at
every m < 3); (ii) the badge equality is two-sided margin-0 (r = m
earns; r = m ± 1 does not, wherever the neighbour exists); (iv) the
m = 0 meter row rides the falsy-zero boundary (modeled as the committed
JS ternary, reporting-only). No unregistered decision comparison sits
at margin 0.

## Arms

- **Arm A** — seedless closed forms; decision-bearing.
- **Arm B** — independently-written exhaustive dict-of-cells enumerator
  (direct predicate scans over explicitly constructed band/panel index
  sets); powers the second decision evaluator; equals Arm A on every
  census, closed form, and cell verdict.
- **Arm F** — the committed band loop, per-band `elif`, minimap band
  filter, badge equality, and absence fallback transcribed VERBATIM from
  the pinned quotes, run over real document dicts; equals Arm B
  cell-by-cell on all 80 × 4 outputs (G1).
- **Arm R** — reporting-only seeded documents (k ∈ 1..7 miners, uniform
  valid fields): main seed 20261670 (10,000 docs, 39,783 miners) —
  invisible 6816/39783 beside exact 14/80, ghost-only 4850 beside 10/80,
  deep_diver 9946 beside 20/80, minimap-invisible|hint 11666/31862
  beside 24/64; stability seed 20261671 (4,000 docs, 15,939 miners) —
  2879 / 1996 / 4031 / 4875/12806; presentation shuffle 20261672;
  aux 20261673 asserted never read (constructor registry).

## Scope

The lattice enumerates documents the committed validator ACCEPTS — the
conformance boundary — not documents a truthful atomic exporter would
emit (the P075 Model-basis declaration, carried): with a single-oracle
atomic export d ≤ m holds by construction, and the priced object is the
committed pair's behavior over its own accepted input space — exactly
the boundary the unbuilt FLAG-1 exporter, the sequence-key-free
last-write-wins ingest, and any future world reshape will exercise
first. Frontend rows (meter percentages, chip copy) are REPORTING; every
decision number is server-side. The REJECT indicts the unvalidated
cross-field trust and the exact-equality badge law — never the hint
field itself, never the absence fallback (correct, and pinned by the
repo's own test), never the game.

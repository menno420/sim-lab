# VERDICT 083 — the sample-window front-matter toll (idea-engine PROPOSAL 070) — REPORT

## Ruling

**REJECT** (per the pre-registered rule applied in the registered order
REJECT -> INVALID -> APPROVE -> NULL; both independently-written decision
evaluators agree REJECT/REJECT; every decision number is an exact
rational). The drafter's disclosed landing reproduces from scratch on
every value — **zero anomalies** (all G6 census anchors match exactly;
the two disclosed-rounded T3 endpoints match at stated precision).

The front-loaded assembly default does not survive its own arithmetic at
the decision cell (alpha = 1/10, q = 19/20, k* = 3 screens / 1 spread):

- **Clause (i) — the cliff census**: FRONT-LOADED (F = 4) puts C = 0
  exactly on **3 of the 4 committed formats** — EPISODE (B = 4 screens,
  story screens in sample = 0, hook needs 3), PB15 (B = 4 pages, story
  pages = 0, hook needs 2), PB12 (B = 3 pages, story pages = 0). The
  NOVELLA survives (B = 12, story 8 >= 3) and pays the toll instead.
  Margins on the cliffed cells: 3 / 2 / 2 story units short — no clause
  cell is a knife-edge.
- **Clause (ii) — the exponential toll**: the NOVELLA's front-loaded toll
  C(STORY-FIRST)/C(FRONT-LOADED) = C(F=1)/C(F=4) = q^-3 =
  **8000/6859 ~ 1.16635 >= 11/10** (1.06x over the band). Three front
  screens cost an exact 16.6% of every conversion the sample would have
  produced.

Both clauses fire; REJECT is checked first and rules. APPROVE was
mutually exclusive by arithmetic; both APPROVE-side witnesses were live
(below), so the run was never theater.

## The three structure theorems — verified exact as gates

- **T1 — exponential toll (G2)**: within the budget, C(F)/C(0) = q^F
  exactly and C(F+1)/C(F) = q constant across F, at every format x alpha
  x q x k* pair where both cells are within budget (F = 0 is a theorem
  reference point only, convention C2). Front matter is a multiplicative
  per-unit tax — never additive.
- **T2 — viability cliff and the 9x lever (G3)**: the within-budget
  indicator is single-threshold in F (scanned F = 0..12, every format x
  alpha x k*); the closed form S* = floor((F + k* - 1)/alpha - F) + 1
  matches the brute S = 1..400 scan at every (grid alpha, F in 1..5,
  k* in {1, 2, 3, 5}), and the alpha = 1/10 specialization is
  **S* = 9F + 10k* - 9 exactly**: each front unit raises the minimum
  viable book by nine units. Key rows: S*(F=2, k*=1 spread) = **29
  pages** (the committed 15-spread format, 30 story pages, clears by ONE
  page); S*(F=3) = **38 pages** (19 spreads — one extra front page puts
  the threshold four spreads past the committed pin); prose k* = 3:
  S* = 30 / 39 / 57 screens at F = 1 / 2 / 4.
- **T3 — mixture log-convexity (G4)**: pinned two-point mixture (3/5 and
  49/50, half each): S(n)S(n+2) > S(n+1)^2 strictly for n in 0..29;
  S(n) > S(1)^n for n in 2..29 (single-q deep-toll numbers are
  overstatement bounds — the REJECT direction is conservative);
  depth-toll S(n)/S(n+3) strictly decreasing over n in {1, 4, 8, 16}:
  1.5019 -> 1.1739 -> 1.0786 -> 1.0628 (the first front unit always
  costs the most; increasing measured hazard falsifies the class).

## Census and surfaces (Arm A = Arm B exactly, every cell)

- C(NOVELLA, F=1) = **130321/160000 ~ 0.814506**; C(NOVELLA, F=4) =
  **893871739/1280000000 ~ 0.698337** (the disclosed anchors, exact).
- PB12 (dormouse, 24 story pages) is **under the cliff under ANY
  assembly** at alpha = 1/10: F = 2 legal minimum gives B = 3, ONE story
  page in the sample against a two-page hook; cliffed at every
  F in {2, 3, 4}. S*(F=2) = 29 > 24: a format decision, not an assembly
  one.
- EPISODE cliffs from F = 2 on; at F = 1 it survives at **margin 0**
  (B = 4, story = 3 = k* exactly) — the registered fragile cell,
  excluded from decision clauses (and in fact no decision clause touches
  it: clause (i) reads F = 4, clause (ii) reads the NOVELLA only).
- **Falsifiability was real on both axes**: at alpha = 1/5 every
  committed format clears even front-loaded (cliff-clause witness); at
  q = 49/50 the toll is 125000/117649 ~ 1.0625 < 11/10 (toll-clause
  witness — clause (ii) dies one q step from the decision cell).

## The knife-edge ledger (C5) — the honest-scrutiny finding

The runner enumerates EVERY margin-0 surviving cell over the full grid
(10 cells, listed in results.json) and re-verifies that **no decision
clause rests on any of them** (gated). Two are worth naming beyond the
registered EPISODE F=1 instance:

- **PB15 story-first at the decision cell is itself margin-0**
  (`PB15|a=1/10|k=1|F=2`: B = 4, F + k* = 4). The "clears S* = 29 by one
  page" headline IS this cell: the one-page clearance buys zero sample
  slack — S in 29..38 all land margin-0 at F = 2. Decision-safe (not a
  clause cell), but the pre-registered RECOMMENDATION's "story-first
  makes PB15 publishable" leg inherits a fragile cell: one page of
  quantization drift or one extra front page kills it. Named for the
  manager beside the recommendation, per the proposal's own
  knife-edge doctrine.
- **The alpha = 1/5 APPROVE witness rests on a margin-0 cell for PB12**
  (`PB12|a=1/5|k=1|F=4`: B = 6, F + k* = 6). "At alpha = 1/5 everything
  clears" is true but PB12 clears with zero slack; the witness's
  liveness is carried with real margin by EPISODE (margin 1) and PB15
  (margin 1) and NOVELLA (margin 17).

Neither finding moves the ruling: clause margins are 3/2/2 story units
(census) and 1.06x (toll); the quantization NULL axis stays disarmed
because no decision band is straddled by any knife-edge cell.

## Hand worlds (G5) and battery (G6)

- G5: S = 10, alpha = 1/2, k* = 2, q = 1/2: F = 1 -> B = 6, C = 1/8;
  F = 5 -> B = 8, C = 1/128; toll = 16 — reproduced exactly, both arms.
- G6: q = 1 gives C = 1 on every within-budget cell and toll 1;
  alpha = 1 gives no cliff anywhere with S >= k*; C is monotone
  non-increasing in F at every format x alpha x k* x q; all census
  anchors verbatim (G6 gating of the disclosed census is P070's own
  registered text — a registered departure from the V082-C10 never-gate
  norm, honored as registered and doubly logged as anomalies, of which
  there were none).

## Arm R (reporting-only) — seeded reader traces beside the exact values

Three surviving decision-grid cells, 100,000 main traces (seed 20261620)
+ 20,000 stability traces (20261621), row order shuffled by presentation
seed 20261622; aux 20261623 asserted never read (constructor registry):

| leg | C_hat | exact C | draws |
|---|---|---|---|
| main/NOVELLA-F1 | 0.81517 | 130321/160000 ~ 0.814506 | 370,958 |
| main/NOVELLA-F4 | 0.69935 | 893871739/1280000000 ~ 0.698337 | 603,800 |
| main/EPISODE-F1 | 0.81653 | 130321/160000 ~ 0.814506 | 371,261 |
| stability/NOVELLA-F1 | 0.81135 | same | 74,187 |
| stability/NOVELLA-F4 | 0.70300 | same | 120,887 |
| stability/EPISODE-F1 | 0.81945 | same | 74,410 |

(EPISODE F=1 and NOVELLA F=1 share the exact value q^4 — same F + k*;
the budget geometry differs, the conversion law does not. No statistical
gate rides any of this.)

## Reproducibility

- One command, no flags, stdlib-only, hermetic (reads only its own
  `fixtures.json`); CPython 3.11 pinned and asserted.
- **2267 self-checks, 0 failed**, exit 0, ~0.3 s/run.
- stdout + results.json **byte-identical across two full in-repo process
  runs** (external diff + sha256):
  - `results.json` `bd2cc6f640a85a0ef8172b81ba0c4e736fd8996c4d68b59fe65488aa1007c553`
  - `run-stdout.txt` `bfcc74ceb2e0f6ff9a98e005d67b726c7b0c6d7de8c45e933caf6fdbf99db590`
- Commit order: born-red card -> fixtures -> runner -> accepted run (no
  fix-forwards after the runner landed; the first complete in-repo run
  of the committed pipeline is the accepted run).
- Seeds: 20261620 (main) / 20261621 (stability) / 20261622
  (presentation) are the ONLY RNGs constructed, in pinned order; aux
  20261623 never read. The drafter's registered set IS the session seed
  set (V077-V082 precedent; no new allocation); 20261614-619 stays the
  disclosed in-flight buffer, unused. Registry high-water after this
  slice: **20261623** (reserved; highest read 20261622).

## Boundaries (named beside the ruling, per the registration)

- **alpha is invented-but-pinned**: no committed source prices the real
  platform sampler; the grid brackets 1/20..1/5 and the named NULL-axis
  probe is ONE free observation of an actual Look-Inside cut (no owner
  spend). At alpha = 1/20 even the NOVELLA cliffs front-loaded; at
  alpha = 1/5 everything clears (PB12 at margin 0).
- **Memoryless attrition is a declared model**: T3 signs the boundary —
  any nondegenerate mixture makes single-q deep-toll numbers an
  overstatement bound, and increasing measured hazard falsifies the
  whole class.
- **Hook model and quantization are declared conventions**: T1 and the
  S* law are hook-model-free within budget; the knife-edge ledger above
  is the quantization sensitivity, published in full.
- **Conversion is priced up to a positive browse rate** that cancels
  from every ratio and indicator the decision reads.

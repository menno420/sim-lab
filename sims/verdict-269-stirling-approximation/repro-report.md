# VERDICT 269 reproduction mirror — Stirling's approximation lim n!/(√(2πn)(n/e)^n) = 1: exact rational correction series (Bernoulli → c_k → a_k), Gamma-integral MC agreement, Robbins' exact bracket, and √(πn)-prefactor falsification, PROPOSAL 256

> **Status:** reproduction-only — the canonical APPROVE/QUALIFIED/REJECT ruling is deferred to the coordinator-driven VERDICT 269 slice. This mirror does NOT advance the verdict high-water and does NOT append a VERDICT block. `control/status.md` is untouched.

**Source proposal:** idea-engine PROPOSAL 256 (fleet · pure mathematics / analysis / Stirling's approximation for the factorial — a self-contained closed form outside the fleet/venture/game domains: `lim n!/(√(2πn)(n/e)^n) = 1`, verified through the exact rational correction series `c_k = B_{2k}/(2k(2k−1))` and `a_k = 1, 1/12, 1/288, −139/51840, …` plus Robbins' exact bracket `1/(12n+1) < ln(r_n) < 1/(12n)`)
**Lane:** P256 → V269 (+13 offset)
**Verifier:** `verify_256_stirling_approximation.py` (byte-identical copy of the firsthand idea-engine verifier; stdlib only: json, hashlib, math, random, fractions, decimal, sys)
**SEED:** 20260717
**Command:** `python3 verify_256_stirling_approximation.py` (run from inside this directory; captured in `run-stdout.txt`)

## Reproduction result

Ran the byte-identical verifier here; `run-stdout.txt` holds the full output.

- `results_sha256 = f6511c51458be68eb564460ed1229456b0e7fd0aff2733e75c47e22f4066d267` — byte-identical to the idea-engine source digest.
- in-process double-run: IDENTICAL · `--selfcheck`: byte-identical · separate re-invocation: byte-identical
- **G1 EXACT** (`fractions.Fraction`, zero tolerance) — the exact rational backbone of the Stirling series: (i) the Bernoulli numbers `B_0..B_14` from the recurrence `Σ_{k=0}^{m} C(m+1,k)B_k = 0` match their known values; (ii) the log-series correction coefficients `c_k = B_{2k}/(2k(2k−1))` match `{1/12, −1/360, 1/1260, −1/1680, 1/1188, −691/360360, 1/156}`; (iii) the ratio-series coefficients `a_0..a_6`, built by the exp-of-series recurrence `a_k = (1/k)Σ_j j·l_j·a_{k−j}`, satisfy the INVERSE log-of-series (recovers `l_j` back, 0 residual) AND match `{1, 1/12, 1/288, −139/51840, −571/2488320, 163879/209018880, 5246819/75246796800}`. `checked = 29`, `mismatches = 0`. `z` not applicable, reported `"exact"` → PASS
- **G2 Monte-Carlo agreement** (i.i.d. importance draws) — estimate `n! = ∫_0^∞ x^n e^{−x} dx` at `n = 10` by importance sampling from a `Gamma(shape=10, 1)` proposal (stdlib `random.gammavariate`), `N = 400000`. The proposal normaliser is the exact integer `(n−1)!` (the proposal's own, NOT the answer `n!`), so the per-draw estimator `(n−1)!·x` is a genuine unbiased estimator of `n!`; the ratio `r̂ = n!/D`, `D = √(2πn)(n/e)^n`, is formed in log-space via `lgamma` (no huge integer/float). Sample `r̂ = 1.008`, honest target = the closed-form Stirling prediction `r_stir = exp(Σ_{k=1}^4 c_k/n^{2k−1})`, `z = (r̂ − r_stir)/SE = 1.3029`, `|z| < 3` (Z_ACCEPT = 3.0). i.i.d. draws → plain i.i.d. SE is honest (no batch means / thinning; stated in the verifier docstring) → PASS
- **G3 robustness / invariance** (own direction) — two legs: (a) **Robbins' exact rational bracket** (high-precision `Decimal`, `prec = 60`) — `1/(12n+1) < ln(r_n) < 1/(12n)` for **all** `n = 1..64`, `robbins_violations = 0` (tightest upper/lower gaps disclosed in the payload); the bound expressions are exact rationals and the 60-digit `ln(r_n)` lands strictly inside every time. (b) **Estimator invariance** (`|z| < 3`) — the SAME `r_10` re-estimated from a DIFFERENT proposal `Gamma(shape = n+2, 1)` (per-draw estimator `(n+1)!/x`) agrees with `r_stir` at `z = −1.4413`, so the estimate is invariant to the nuisance proposal parameter. → PASS
- **G4 falsifiability** (own direction, SAME MC sample as G2) — the naive foil using the `√(πn)` prefactor (the factor-2-under-the-root error, `√(πn)` instead of `√(2πn)`) predicts the ratio `n!/(√(2πn)(n/e)^n) = √(πn)/√(2πn) = 1/√2 ≈ 0.707107`. On the same N=400000 sample, `z_foil = (r̂ − 1/√2)/SE = 598.9410`, `|z_foil| ≫ Z_REJECT = 50.0` → REJECTED, while the same sample AGREES with the true ratio at `z = 1.3029`. Secondary corroboration: dropping the `1/(12n)` correction (ratio `= 1`) is rejected at `z = 17.8982` (`> Z_ACCEPT`). The teeth: the factor-2-under-the-root genuinely changes the prefactor, and the same sample discriminates the true `√(2πn)` scaffold from the `√(πn)` error → PASS
- all_pass = true · first_failing_gate = null · decision = PASS

## Claim reproduced

Stirling's approximation `lim_{n→∞} n!/(√(2πn)(n/e)^n) = 1`. Because the limit is `1`, the exact teeth come from the correction series: writing `r_n = n!/(√(2πn)(n/e)^n)`, the log-ratio `ln(r_n) = Σ_{k≥1} c_k/n^{2k−1}` has exact-rational coefficients `c_k = B_{2k}/(2k(2k−1))` built from Bernoulli numbers, and the ratio expands as `r_n ~ Σ a_k/n^k` with exact `a_k = 1, 1/12, 1/288, −139/51840, …`. Robbins' theorem gives the exact two-sided rational bracket `1/(12n+1) < ln(r_n) < 1/(12n)`, valid for every `n ≥ 1`. The whole rational backbone is verified end-to-end in `fractions.Fraction` (Bernoulli recurrence → `c_k` → `a_k`, exp/log inverse-consistent, `checked = 29`, `mismatches = 0`); a Gamma-importance-sampling Monte-Carlo estimator of `n!` agrees with the closed-form ratio at `z = 1.30`; the Robbins bracket holds with `0` violations across `n = 1..64` in 60-digit `Decimal`; and the single most plausible confusion — the `√(πn)` factor-2-under-the-root prefactor, predicting ratio `1/√2` — is shown false on the same evidence (rejected at `|z_foil| = 598.9`).

## Verifier source integrity

Byte-identical to idea-engine's firsthand verifier `ideas/fleet/verify_256_stirling_approximation.py` (verifier file sha256 `d2e5054bcc38b0367152832fd9d21bd50a9cd91db2a291bf5479ea875bdfd407`). The copy in this directory carries the same sha256 and `diff` against the source is empty (exit 0), confirming a byte-for-byte reproduction of the source verifier; stdlib-only (json, hashlib, math, random, fractions, decimal, sys), no network, SEED hardcoded. The hashed results payload is a pure function of SEED and the module constants (`random.seed(SEED)` is re-seeded at the START of each Monte-Carlo gate so gate order cannot perturb the payload; every float enters the hashed payload via a fixed `f"{x:.6f}"` / `f"{z:.4f}"` / `f"{x:.3e}"` string and every exact ratio as a `str(Fraction)`; no wall-clock / PID / unordered-set iteration enters the hashed payload), so the in-process double-run, the `--selfcheck` path, and the separate subprocess re-invocation are byte-identical and the `results_sha256` reproduces exactly.

## Grounding (carried from the source proposal, not re-adjudicated here)

The source proposal pins Wikipedia "Stirling's approximation" oldid 1360453793 (raw-wikitext sha1 `5b7a91f2e93f54a950126ac06be9d35bcf4d8095`, 34026 bytes; API `revisions.sha1` == self-computed `hashlib.sha1`). The page is rich: the main asymptotic `n! ~ √(2πn)(n/e)^n`, the full ratio-series (`a_1..a_5`), the `c_k = B_{2k}/(2k(2k−1))` formula, and Robbins' bracket are all QUOTED verbatim. DERIVED firsthand (grep 0): the coefficient `a_6 = 5246819/75246796800`, the Bernoulli-recurrence route, the exp/log inverse-consistency check, the Monte-Carlo Gamma-integral estimator + every z-value, the `√(πn)` foil, the numeric Robbins sweep across `n = 1..64`, SEED, and the digest. This mirror reproduces the computation only; the honest quoted-vs-derived split is carried from the proposal for the coordinator's VERDICT 269 slice to adjudicate.

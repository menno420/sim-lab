# VERDICT 128 — the compounding-reward inequality engine: an in-game economy gives every player the SAME fair daily percentage bonus — each day holdings change by an iid factor exp(g), g~Normal(MU_G,SIGMA_G) with a small positive average, NO skill, NO choice, NO edge — and lets it COMPOUND on the running total, so after DAYS days terminal wealth W=W0·exp(S) with S=Σ_d g_d ~ Normal(DAYS·MU_G, DAYS·SIGMA_G²) (exact sum-of-independent-normals) is LOG-NORMAL with terminal log-sd σ_T=SIGMA_G·√DAYS; concentration is scale-free in σ_T alone (Gini=2·Φ(σ_T/√2)−1, top-p share=Φ(σ_T−Φ⁻¹(1−p)), median/mean=exp(−σ_T²/2)) and grows without bound with the horizon, so IDENTICAL, skill-less, choice-less players end up wildly unequal — the top decile owns ~55%, the Gini ~0.68, the median player at ~37% of the average — purely because a lucky early factor is multiplied by every later one (the running total carries memory); the trap is that designers reach for percentage/compounding rewards (interest mechanics, multiplicative loot/XP multipliers, "+X% of holdings" logins, idle-prestige multipliers) because they FEEL fair and self-balancing and read a widening wealth gap as skill, but the gap is manufactured by the multiplicative structure regardless of skill — isolated by a SAME-SHOCKS ADDITIVE control W=W0·(1+S) (the identical S paid on the starting stake, no compounding) at which the concentration collapses to the fair share (additive top-decile ~0.15); the transferable correction is that compounding an iid multiplicative reward makes wealth log-normal and concentrates it (Gini→1, median/mean→0 with the horizon) among IDENTICAL players regardless of skill while the same shocks paid additively stay fair, so an egalitarian/bounded economy needs a non-compounding (fixed-base/additive) reward, a capped compounding horizon, or a periodic rebase, never an uncapped compounding bonus; a game-domain / reward-design head grounded in multiplicative stochastic wealth dynamics (Gibrat 1931), round-26 GAME slot, P115 → V128 (+13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · high · review/verify

Born in-progress as this session's FIRST commit (born-red HOLD); it stays in-progress until the reproduction artifacts land and the verdict is written — the flip to `complete` is a deliberate LATER step, not part of this born-red opening commit. The `in-progress` badge is precisely what holds `substrate-gate` red so the PR cannot merge before the deliberate flip. **Outcome below is PENDING reproduction — see the run commit on this branch.**

## Objective
Reproduce idea-engine PROPOSAL 115 (outbox `## PROPOSAL 115 · 2026-07-18T04:23:26Z · status: sim-ready`, round-26 GAME-slot head), offset +13 (P115 → V128): on the pinned world (SEED=20260717, N_PLAYERS=5000, DAYS=200, MU_G=0.02, SIGMA_G=0.10, W0=100.0, TRIALS=200, SIGMA_GATE=3.0, TOP_FRAC=0.10; thresholds SHARE_MIN=0.40 (G1), ADD_SHARE_MAX=0.20 + DELTA_MIN=0.25 (G2); closed-form anchors σ_T=SIGMA_G·√DAYS=1.414214, Gini=2·Φ(σ_T/√2)−1=0.682689, top-decile share=Φ(σ_T−Φ⁻¹(0.90))=0.552770, median/mean=exp(−σ_T²/2)=0.367879), draw each player's cumulative daily-log-return S as the exact sum-of-independent-normals of DAYS iid Normal(MU_G,SIGMA_G) draws, read the SAME S under the COMPOUND regime W=W0·exp(S) and the SAME-SHOCKS ADDITIVE control W=max(0,W0·(1+S)), compute the COMPOUND Gini + top-decile share and the ADDITIVE top-decile share over TRIALS replications with z on the estimated mean via se=std/√TRIALS, and show (G1) concentration (headline) — the COMPOUND top-decile wealth share ≥ SHARE_MIN=0.40 by ≥3σ (identical players, fair rewards, yet the top 10% own most of the wealth); (G2) compounding-causes-it control — the ADDITIVE top-decile share ≤ ADD_SHARE_MAX=0.20 AND the (COMPOUND−ADDITIVE) top-decile share gap ≥ DELTA_MIN=0.25, both by ≥3σ (freeze the compounding, keep the same shocks, and the concentration collapses to the fair share, isolating compounding not luck as the cause); and (G3) closed-form anchor MATCH — the mean COMPOUND Gini reproduces 2·Φ(σ_T/√2)−1 AND the mean COMPOUND top-decile share reproduces Φ(σ_T−Φ⁻¹(0.90)) within 3σ (|z|<3 each), so the effect IS the log-normal compounding mechanism. Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/superbot-games/compounding_reward_inequality.py`) VERBATIM under SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, confirm two runs are byte-identical, and confirm the results-dict sha256 reproduces `a9dbb30f38b77e342f03e9ced2af204d95aef624902ecd845d107b99c9140b1e` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 115 @ `control/outbox.md` source header verbatim `## PROPOSAL 115 · 2026-07-18T04:23:26Z · status: sim-ready` (read at idea-engine HEAD `eb76189f306db3f0924a1e4d49d0fbb74cffe066`, the PR #528 merge commit that added the verifier); reference verifier `ideas/superbot-games/compounding_reward_inequality.py` present at that HEAD, git blob sha `54f339d8835bb5c1f1e0436b4105a36a58ae4bcf` (`git hash-object`), file sha256 `2d058b2c964e1096997cd4f213c6dbe65645daed117e76acad1dc1f62bdbe5b8` (`sha256sum`), 253 lines (`wc -l`, 11589 bytes), stdlib-only (random, math, json, hashlib — no numpy/scipy; Φ via `math.erf`, Φ⁻¹ via Acklam's rational approximation used only for the closed-form top-share anchor, never inside the Monte Carlo draw), SEED=20260717 pinned in-file. Real source URLs: https://github.com/menno420/idea-engine/pull/528 and blob https://github.com/menno420/idea-engine/blob/eb76189f306db3f0924a1e4d49d0fbb74cffe066/ideas/superbot-games/compounding_reward_inequality.py . sim-lab work branch `claude/verdict-128-compounding-reward-inequality` off origin/main HEAD `4d3a0345e9d34eca7bad23c8361c636bb675d3c0`. The verifier is copied BYTE-IDENTICAL into `sims/verdict-128-compounding-reward-inequality/compounding_reward_inequality.py` in the reproduction commit on this branch (diff exit 0 vs the idea-engine source + matching `git hash-object` blob `54f339d8…` + matching `sha256sum` `2d058b2c…` — proof recorded in the Outcome section after the run). DIGEST POSTURE = (a) SELF-FIELD: `main()` computes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` then `digest = sha256(canonical)` BEFORE `results["results_sha256"] = digest` is inserted (L240–242), so the disclosed digest `a9dbb30f…` is over the results dict WITHOUT the `results_sha256` key; the on-disk pretty `compounding_reward_inequality_results.json` (indent=2, sort_keys, WITH the appended `results_sha256`) has its own `sha256sum` that is NOT the disclosed digest. Offset authority = idea-engine control/outbox.md per-block ledger (`depends:` line 1145: "sim-lab consumes at +13 (PROPOSAL 115 → VERDICT 128). Offset pinned") + control/status.md baton ("next: V128 for P115; then round-26 unrelated-slot P116") — docs/current-state.md is a SEAT-DORMANT snapshot through V059 and carries NO P115/V128 row. P115 ↔ V128 confirmed +13.

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the results dict / digest enforced (the seeded run is reproduced across two separate `python3` invocations; cross-invocation determinism recorded in the Outcome).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114–V127, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.
- Digest recomputed under the SELF-FIELD posture guard — the results dict is hashed WITHOUT the `results_sha256` key (the verifier inserts it after hashing), matching the disclosed compact-canonical digest, not the on-disk pretty file's own sha256sum.

## Gate plan (proposal labels G1/G2/G3, evaluated in order G1→G2→G3 at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104/P105/P106/P107/P108/P109/P110/P111/P112/P113/P114 /se convention)
- G1 concentration (headline): COMPOUND top-decile wealth share ≥ SHARE_MIN=0.40 by ≥3σ (disclosed compound_top_share 0.553674, se 0.000955, z=160.8932σ) — identical, skill-less players, fair rewards, yet the top 10% own most of the wealth.
- G2 compounding-causes-it control: ADDITIVE top-decile share ≤ ADD_SHARE_MAX=0.20 AND the (COMPOUND−ADDITIVE) top-decile share gap ≥ DELTA_MIN=0.25, both by ≥3σ (disclosed additive_top_share 0.149729, se 0.000052, z_additive=964.4786σ; share_gap 0.403945, se 0.000916, z_gap=168.0571σ) — freeze the compounding, keep the same shocks, and the concentration collapses to the fair share, so it is caused by COMPOUNDING, not luck (the load-bearing control).
- G3 closed-form anchor MATCH: mean COMPOUND Gini reproduces 2·Φ(σ_T/√2)−1 AND mean COMPOUND top-decile share reproduces Φ(σ_T−Φ⁻¹(0.90)) within 3σ (|z|<3 each) (disclosed Gini 0.683305 vs 0.682689 z=0.9362; top-decile share 0.553674 vs 0.552770 z=0.9466) — reproduces the log-normal closed forms, so the effect IS the compounding-inequality mechanism itself.
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order (the verifier's `all_pass` field encodes exactly G1 ∧ G2 ∧ G3); verdict never softened.

## Outcome — pending reproduction — see run below
PENDING (born-red HOLD). The verbatim-verifier copy, byte-identity proof (`diff` exit 0 + matching `git hash-object` blob `54f339d8…` + matching `sha256sum` `2d058b2c…`), the two-cross-invocation determinism check, the SELF-FIELD results-dict sha256 recompute vs the disclosed `a9dbb30f38b77e342f03e9ced2af204d95aef624902ecd845d107b99c9140b1e`, and the per-gate PASS/FAIL land in the reproduction commit on this branch (`sims/verdict-128-compounding-reward-inequality/` — the verbatim `.py`, the `_results.json`, `run-stdout.txt`). The verdict recommendation (APPROVE iff diff exit 0 AND digest exact-match AND G1∧G2∧G3 in order, never softened) and the deliberate flip to `complete` are a LATER step; this card is committed FIRST at `in-progress` to hold `substrate-gate` red.

## ⟲ Previous-session review
Prior loop P114 → V127 (the freemium support-cost inversion — a free tier carries every free user at a fixed cost c and earns V·p only on the fraction that converts, free users arrive in INTENT ORDER so p(i)=P0·e^{−K·i} declines with volume, marginal V·P0·e^{−K·i}−c crosses zero at n*=ln(V·P0/c)/K and cumulative contribution C(n)=V·S(n)−c·n is CONCAVE — rises to a peak at n* then falls NEGATIVE, so scaling signups past n* destroys value while every headline metric rises) landed APPROVE — sims/verdict-127-freemium-support-cost-inversion/, reproduced the committed verifier byte-identical with results-dict sha256 `884242e6ed6d9b7cf8affef7400526985e8244b4b3541319f33653dce6639b38` matching the disclosed digest EXACTLY, all three gates PASS (G1 z=271.4684σ, G2 z=369.4113σ, G3 |z|=0.0140 / 0.4226) — PR #201 @4d3a034. Load-bearing carry-forward is the DIGEST-POSTURE guard, which P115 shifts BACK to posture (a): classify the verifier's `main()` before recomputing any digest — (a) SELF-FIELD (`results_sha256` inserted AFTER hashing → omit it before recompute; P104/P107/P108/P111/P112, and P115 HERE), (b) SIBLING-key (`{"results":…, "sha256":…}` wrapper, hash `results` as-is; P105/P109/P110), or (c) WHOLE-DICT / NO-SELF-FIELD (dict never carries a digest key, hash the whole thing; P114/V127). The on-disk pretty `_results.json`'s own `sha256sum` equals the disclosed digest in NONE of the three (canonical is compact `separators=(",",":")`; the on-disk file is pretty indent=2 with an ADDED `results_sha256` field for readability). P115's `main()` is unambiguously posture (a) — L240–242 hash the compact-canonical dict, THEN insert `results["results_sha256"]` — so this slice recomputes the digest over the dict WITHOUT that key, and the `_results.json` follows the V125/V127 exemplar (compact-canonical dict pretty-printed with the appended `results_sha256` = the disclosed digest, self-documenting without claiming its own file-hash equals it). Carry-forward: round-26 rotation — fleet P113→V126, venture P114→V127, THIS game P115→V128; the next pull after this is round-26 at the UNRELATED slot, PROPOSAL 116 (rotation game→unrelated). Fleet-lane note (from recon): sim-lab's `sims/` jumps verdict-125 → verdict-127 with NO verdict-126 dir — V126 (P113 fleet) has not landed on sim-lab while V127 merged; that is a gap in the fleet lane, not in this P115→V128 game lane. The coordinator failsafe cron stays armed (coordinator-managed, NOT re-armed by this seat).

## 💡 Session idea
This slice pins the inequality on a STATIONARY, HOMOGENEOUS multiplicative process — every player draws the SAME iid Normal(MU_G, SIGMA_G) daily log-return, so terminal wealth is exactly log-normal with a single scale-free parameter σ_T=SIGMA_G·√DAYS and the concentration is a clean closed form (Gini=2Φ(σ_T/√2)−1, unbounded in the horizon). The correction it proves — "pay on a fixed base / cap the horizon / rebase" — is exact precisely because the process has no floor, no reinjection, and no cross-player coupling. But real game economies almost never let wealth compound freely to infinity: they SINK it (consumption, decay, taxes, upkeep that scale with holdings) and they FLOOR it (a UBI-style daily grant, a bankruptcy reset, a soft-cap diminishing-returns curve on the multiplier). A multiplicative process with a proportional sink and an additive reinjection is no longer log-normal-to-infinity — it is a Kesten process, whose stationary wealth distribution is POWER-LAW (Pareto) with a tail exponent set by the ratio of the multiplicative variance to the sink/reinjection strength, so inequality STABILIZES at a finite Gini instead of diverging. The load-bearing generalization the current gate does NOT exercise is that the design lever is not binary (compound vs additive) but CONTINUOUS: a small proportional wealth sink (a "% of holdings" upkeep/decay, the mirror image of a "% of holdings" bonus) plus a flat floor grant converts the divergent log-normal into a convergent power law and TUNES the steady-state Gini to a target. A genuinely different next GAME slice pins the Kesten-economy mechanism: hold the multiplicative daily update W←W·exp(g) but add a proportional sink (W←(1−τ)·W) and a flat floor grant (W←W+b), draw to the stationary distribution, and pre-register (G-a) a tail-law gate — the stationary wealth tail is Pareto with exponent α matching the closed-form Kesten root (E[((1−τ)e^g)^α]=1) within 3σ, NOT log-normal (a divergent-vs-convergent discriminator); (G-b) a Gini-stabilization gate — under the sink+floor the Gini converges to a finite steady state independent of the horizon (mean Gini at DAYS vs 2·DAYS within 3σ), whereas the pure-compound control's Gini keeps rising with the horizon by ≥3σ (isolating the sink as what bounds inequality); and (G-c) a lever-monotonicity gate — the steady-state Gini is monotone decreasing in the sink rate τ by ≥3σ across a τ-grid (so an operator can dial a target Gini with the upkeep rate) — converting this slice's binary "never uncapped compounding" into the actionable, ≥3σ-gateable "add a proportional sink + floor to turn a divergent log-normal economy into a convergent power-law one whose Gini you tune with the sink rate" — a seed-pinnable Kesten-process economics mechanism distinct from this slice's floorless homogeneous log-normal and from the outbox's compound-vs-additive single-lever frame.

# Session — VERDICT 009 — owner-001 superbot-next settings/command/UX surface

> **Status:** `complete`
> 📊 Model: opus-4.8 · 2026-07-11 · VERDICT 009 (owner-001) finalizer session
> Objective: fold the independent AI-panel audit into the already-built owner-001 sim
> (`sims/owner-001-superbot-next-settings-ux/`, branch `sim/owner-001-superbot-next-settings-ux`
> @ 3a25330), adjust the verdict from the builder's draft `approve-with-changes` to the honest
> house call `needs-more-evidence` / ruling `approve-the-direction + ship the named changes`,
> write the control bus (outbox VERDICT 009 + inbox INTAKE owner-001 + status heartbeat), open a
> READY PR, arm auto-merge (REST merge-on-green fallback), and post the mandatory @codex comment.

## What happened

OWNER-DIRECT intake (not the idea-engine queue): the owner asked for a simulation of superbot-next's
functions/settings vs old superbot — redundant/combinable settings, per-setting reachability,
AI-panel improvables, and the UX delta. The numeric/structural sim was already built and pushed
(556 self-checks, byte-identical `results.json`). This finalizer session did the close-out.

**AI-panel audit folded in** (independent, read-only source audit of `sb/domain/ai/*` +
`sb/kernel/panels/*` vs OLD; 8 findings — 3 high / 4 medium / 1 low — each cited `file:line`):
the marker line in `REPORT.md` was replaced with a ranked findings table + flow characterization +
old-vs-new comparison. The 3 high defects (AIP-01 Tools chooser dead-ends, AIP-02 `ai.card` strands
with no back/home/help, AIP-03 doubled-prefix `ai.ai_enabled` ack) are now named changes in the verdict.

**Verdict adjusted to the honest house call.** The settings-design axis (redundancy, combinability,
reachability) is MEASURED and settled; the "overall UX improved / by how much" axis is JUDGMENT-ONLY
(no live users/telemetry reachable from code), so the magnitude is a hypothesis — the only reason this
is `needs-more-evidence` and not `approve`. Lane precedent: VERDICT 003 / 007 (a measured core carrying
a JUDGMENT-ONLY value axis takes the weakest-for-the-question label). Gate stays PASS.

**Headline measured result:** NEW config surface 125 settings — 8 DEAD (read nowhere) + 19 DISPLAY-ONLY
(all `image_moderation`(8) + `security`(11) render in panels, no engine reads them) vs OLD 118 (5 dead,
0 display-only); commands −117 (367 vs 484). Discoverability WIN: 100% reachable from `/settings`
(125/125) vs OLD 91.5% (108/118, 10 unreachable → 0). Combinability: NEW already de-monolithized OLD's
subsystem-wide config blobs → redundancy here is REMOVE/WIRE, not combine. "Fewer knobs" is false —
the surface GREW; the win is discoverability + granularity.

## Run command

```
python3 sims/owner-001-superbot-next-settings-ux/settings_ux_sim.py
```
Deterministic, stdlib-only, exit 0 iff every self-check passes. Prints the full inventory diff,
redundancy/combinability/reachability tables, and the terminal `SELF-CHECKS: 556 passed, 0 failed`;
`results.json` is proven byte-identical on re-run. Not re-generated this session (no change to the sim).

## 💡 Session idea

The **static surface-inventory diff** pattern (settings/commands/panels JSON at pinned SHAs → measured
redundancy via `read_where`, reachability via a BFS click-graph, combinability via co-read collapse) is
a `harness/` candidate for any "did surface B improve over surface A" question — a reproducible
structural core (dead/display-only/discoverability facts) that can carry a JUDGMENT-ONLY value axis
without laundering the hypothesis into the evidence. Pairs naturally with an independent, cited
`file:line` UX audit for the non-structural findings the inventory can't see.

## ⟲ Previous-session review

VERDICT 007 fixed the mixed-evidence labeling discipline (a JUDGMENT-ONLY value axis riding a
reproducible rung-2 measured core takes the weakest label); VERDICT 009 reuses it exactly — the
settings-design axis is measured and settled, the overall-UX magnitude is JUDGMENT-ONLY, so the verdict
is `needs-more-evidence` with the measured findings still shipped as named changes. The discipline
carried forward: report the FULL structural tables, cite every AI-panel claim `file:line`, and never
assert the panel-inventory-granularity artifact (the AI +1-click "regression") as a real finding.

# Session — VERDICT 080 — the stale-ink mirror: Wickroad's "bet on your own stale information" against its own committed 12-day triangle law (idea-engine PROPOSAL 067, the round-13 GAME-MECHANICS slot, gba-homebrew source; P067 → V080 under the +13 offset)

> **Status:** complete
> 📊 Model: Claude Fable family · high · verdict-sim slice-worker session

Objective: produce VERDICT 080 for idea-engine PROPOSAL 067 (the stale-ink
mirror, `ideas/gba-homebrew/wickroad-stale-ink-mirror-2026-07-15.md`, read at
idea-engine origin/main `1f304b0`, landed via idea-engine PR #430 merged
2026-07-15T06:26:58Z; outbox block stamped 2026-07-15T06:22:36Z — the block
lives in the LIVE `control/outbox.md` post-ROLLOVER-002). One slice, one
branch (`claude/2026-07-15-verdict-080-stale-ink-mirror`, PR #149), one
verdict. NUMBERING, verified literally at sim-lab origin/main `532649b`
(VERDICT 079 merged via PR #148): newest `## VERDICT` header was 079,
`## VERDICT 080` and `## INTAKE 067` collision-grepped clean (outbox +
archives + current-state + remote refs — no competing branch or PR), so
idea-engine PROPOSAL 067 → **VERDICT 080**, the +13 offset's fourth row (map
row extension landed in `docs/current-state.md` this same PR). Worker
session; `control/status.md` and `control/inbox.md` untouched (ledger
appended to `control/outbox.md` only; delivery is the manager's Q-0264
fan-in). Wickroad ground truth re-verified FIRSTHAND this session on a
read-only clone at the registered pin gba-homebrew `c0c6882` before any
fixture was written (every constant and all four harvested sentences matched
the tree; gba-homebrew moved to v0.5 AFTER the pin — disclosed, the verdict
conditions on v0.4 per the registered guard). This card held the substrate
gate red deliberately until this flip (the born-red discipline — the
designed hold was the only red this branch produced itself).

## What happened

Built `sims/verdict-080-stale-ink-mirror/` under the standing discipline:
fixtures.json committed BEFORE the runner (card f3d7676 → fixtures 6ad8134 →
runner 1380d42 → accepted run 110ff45; one disclosed pre-run correction —
the fixtures' decimal seed transcription vs the authoritative hex, caught by
F1's own gate in rehearsal), fully hermetic, three-arm: Arm A seedless
exact-integer surfaces + theorem enumerations + belief-controlled greedy
census (decision-bearing), Arm B independently-written literal day-walk twin
(exact-equal on every published number incl. all 400 Arm-R world rows), Arm
R seeded 200+200-world census (seeds 20261590–592 constructed, aux 20261593
asserted never read — the drafter's registered allocation IS the session
seed set, the V077–V079 precedent; the 20261584–589 gap is the drafter's
disclosed V079 buffer, unused; registry high-water 20261593). 31 self-checks
0 failed, exit 0, ~12 s/run; byte-identical double run (sha256 in
REPORT.md); CPython 3.11 asserted; no fix-forwards after the runner landed.

**VERDICT 080 — NULL on registered axes n4 + n3 (the drafter's predicted
REJECT did NOT fire — the honest-null discipline, exercised for real).**
n4: the two-morning decode theorem (T3b, the R3 conjunct's third leg and
the proposal's headline) is FALSE as registered — 0 of 24,360 unclamped
consecutive-pair cases decode uniquely (candidates 2–6; canonical
counterexample TALLOW (7,0) day 5, readings (9,10) → six indistinguishable
clamp-free worlds); the distinct-tri-pairs step does not survive the
base-translation/phase-shift composition. Corrected law shipped: 7
consecutive mornings always decode, and the MIRROR is the fastest decoder —
two mornings six days apart pin base exactly (T2 corollary, all 18,624
unclamped anti-phase pairs). n3: with R1/R2 holding on the committed world,
R2 replicates on only 56/200 registered alt-worlds (stability 41/200;
drafter's own drafting row 9/20 — also sub-1/2). What held, exact: I(6) =
300/300, I(12) = 0/300, E(12) = 0/360 clamps included; committed-world
SCRYER/ORACLE = 1278/1311 ≈ 0.9748 (the disclosed 1.0194× knife-edge
WIDENED to 1.0831× under the registered-text scaffold). Ten first-class
anomalies vs the disclosed landing, each with a diagnosis — headline: the
drafter's four-arm table reproduces EXACTLY under reserve = toll·dist +
toll where the registered prose says + guard fee (A1), and the disclosed
T3b drafting verification cannot have run as described (A3 = the n4
finding).

## Previous-session review

**Previous-session review** (previous session = VERDICT 079, sim-lab
PR #148): V079's 💡 — a structured machine-readable disclosed-landing
schema (value, exact form, precision, comparison operator) — was tested by
this slice against a harder failure surface, and its scope line is now
measured: it WOULD have caught A1 at drafting (a machine-emitted scaffold
constant table cannot say "guard fee" while the prototype reserves a toll)
and would have forced the A2 fallback convention to be pinned; it would NOT
have caught A3/T3b, because no disclosed VALUE was wrong — a quantified
CLAIM was disclosed as verified when the verifying enumeration checked a
strictly weaker statement. V079's other export (the born-red card /
collision-grep / no-claim-file reservation) carried unchanged. The gap
V079 left — claims, not values — is exactly what this session's 💡 fills.

💡 **Session idea (genuine, this session):** REGISTRATION-TIME EXECUTABLE
THEOREM STATEMENTS — a pre-registered theorem should ship in the proposal
as a small runnable enumeration block (domain, quantifier, predicate — a
dozen lines of stdlib python beside the prose), and the verdict runner
executes the DRAFTER'S OWN statement verbatim instead of re-formalizing
prose. Two measured prompts from this slice: (a) P067's T3b was disclosed
as "verified by enumeration in the theorem gate" yet fails at literally
every case — the drafting enumeration provably checked a different, weaker
statement (T3a), and nothing in the registration pipeline could see that,
because the claim existed only as prose; (b) this session had to re-derive
the achievable-(base, phase) candidate grid from the generator text to even
state T3b precisely — a formalization choice the drafter never signed,
disclosed here as a convention, that an executable statement would have
made the drafter's own. Dedup: distinct from V077's 💡 (seeded-arm
representation equivalence), V078's 💡 (self-evaluating inline arithmetic),
and V079's 💡 (machine-readable disclosed VALUES) — this is the CLAIMS
layer those three don't cover: values can match perfectly while the
registered theorem is false, which is precisely what happened here.
Concretely for the kit: the proposal template's theorem-gate section gains
a fenced `theorem:` code block per claim; the verdict runner imports and
runs those blocks as F2 unchanged; a claim with no block is BY CONVENTION
n4-armed prose, and the drafter knows it at drafting time.

📊 Model: Claude Fable family (self-reported by this session's harness;
family-level name only per the standing attribution doctrine).

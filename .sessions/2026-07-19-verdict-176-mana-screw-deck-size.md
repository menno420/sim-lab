# VERDICT 176 — mana-screw-deck-size: deck SIZE, not land ratio, governs opening-hand mana consistency — at a matched land fraction the SMALLER deck has strictly lower opening-hand land-count variance because the hypergeometric finite-population correction (N−n)/(N−1) shrinks as N grows, so a 40-card deck at 40% lands is more consistent than a 60-card deck at 40% lands despite equal expected lands (n·p = 2.8)

Reproduce PROPOSAL 163 (P163 → V176, +13): mana-screw deck size — in a trading-card GAME the opening-hand land count is a hypergeometric draw of HAND=7 from a deck of N cards with a fixed land fraction p, so at a MATCHED fraction the expected land count n·p = 2.8 is identical for a 40-card and a 60-card deck, yet the two decks are NOT equally consistent. Under the pinned world the opening-hand land count has hypergeometric variance n·p·(1−p)·(N−n)/(N−1), and the finite-population correction FPC = (N−n)/(N−1) is the ONLY term that depends on deck SIZE at a matched fraction; the FPC shrinks as N grows, so the smaller deck has strictly lower opening-hand land-count variance and lands off-curve less often despite the equal expected 2.8 lands. The folk belief (inverted here): "mana consistency is governed by the land RATIO — once you fix the fraction of lands, a 40-card deck and a 60-card deck at 40% lands are equally consistent, and deck size is irrelevant." It is NOT: at a matched land fraction the deck SIZE governs opening-hand mana consistency through the hypergeometric FPC (N−n)/(N−1), which shrinks as N grows, so the SMALLER (40-card) deck has strictly LOWER opening-hand land-count variance and goes off-curve less often than the 60-card deck even though both expect 2.8 lands. Three ordered z-gates (z_gate=3.0), measured on 400000 Monte-Carlo opening hands per regime: G1 head_deck_size (the 60-card deck lands off-curve MORE often than the 40-card deck at the matched 40% fraction — a one-sided z ≥ 3 contrast on the off-curve rates) → G2 mechanism_fpc (ANCHOR / match gate: the measured variance ratio B/A equals the closed-form FPC ratio to Monte-Carlo precision — pass condition |z| < 3, NOT a directional z ≥ 3) → G3 robustness_shift (the size-driven consistency gap survives a shifted land fraction p=0.30 with the keep window recentered to [1,3]). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (an i.i.d. hypergeometric opening-hand draw with NO mulligan, consistency measured as opening-hand land-count variance / off-curve rate) and a variance SIGN claim — the SIGN (at a matched fraction the smaller deck has strictly lower opening-hand land-count variance via the FPC) survives, but the exact off-curve figures are a property of the no-mulligan opening-hand model; London-mulligan digging dynamics are disclosed descriptive fields, not hidden defects.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P163 reference verifier byte-identical under its pinned world (SEED=20260717; TRIALS=400000 Monte-Carlo opening hands per regime; HAND=7; z_gate=3.0; deck A N=40/lands=16/p=0.40 and deck B N=60/lands=24/p=0.40 at keep window [2,4]; shifted A′ N=40/lands=12 and B′ N=60/lands=18 at p=0.30 keep window [1,3]; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `1280ae6278b508f00d3737841768181288e7ed31f12a443d5982b5f03b898e33`, (b) all three gates hold in order G1 head_deck_size → G2 mechanism_fpc → G3 robustness_shift, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (at a matched land fraction the SMALLER deck has strictly lower opening-hand land-count variance via the hypergeometric FPC (N−n)/(N−1), so a 40-card deck at 40% lands is more consistent than a 60-card deck at 40% lands despite equal expected lands n·p = 2.8). Pre-registered decision rule (P163): APPROVE VERDICT 176 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the exact off-curve figures being a property of the no-mulligan opening-hand model (London-mulligan digging as disclosed descriptive fields) is not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 176, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: `ideas/superbot-games/mana_screw_deck_size.py` @ idea-engine — git blob `3f3e60326432260c5fd5abe1c7e639af15b9aca2`, file sha256 `3eb604e18fa42fca7a085ed27211a301c119d93bf419d97754533f3f4d2f788d` (11685 bytes, 283 lines). The committed P163 source in its superbot-games fleet lane, copied byte-identically (`diff` exit 0, copy sha256 == source, copy git blob == source blob) into the reproduction commit at `sims/verdict-176-mana-screw-deck-size/mana_screw_deck_size.py` — copy sha256 `3eb604e18fa42fca7a085ed27211a301c119d93bf419d97754533f3f4d2f788d`, copy git blob `3f3e60326432260c5fd5abe1c7e639af15b9aca2` (11685 bytes, 283 lines). Landed idea-engine PR #636, merge commit `60ebbb0206b04da689dc00e85e203538a300f35f`.
- Offset authority: +13 (P163 → V176), round-38 GAME slot (trading-card-game / opening-hand mana consistency). Per idea-engine `control/outbox.md` P163 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: the hypergeometric distribution — the opening-hand land count is a draw of n=7 without replacement from N cards of which `lands` are lands, with variance n·p·(1−p)·(N−n)/(N−1); the finite-population correction FPC = (N−n)/(N−1) is the only size-dependent term at a matched fraction and shrinks as N grows, so the smaller deck has strictly lower opening-hand land-count variance despite the equal expected 2.8 lands. Grounding: https://en.wikipedia.org/wiki/Hypergeometric_distribution — the domain reference resolved LIVE this session (HTTP 200); the closed forms are hand-checked against the reproduced numbers in the Outcome below.
- Disclosed digest: compact-canonical results-dict sha256 `1280ae6278b508f00d3737841768181288e7ed31f12a443d5982b5f03b898e33`, SEED=20260717 — reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (confirmed): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V171–V175). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent run and asserts the two canonical digests are equal (in-process double-run byte-identity), prints a pretty indent=2 dump plus the sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. Confirmed byte-exact in the reproduction commit.

## Constraints honored
Stdlib-only (math, json, hashlib, random); verifier copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The off-curve rates and the opening-hand land-count variances are measured from the drawn cap of TRIALS=400000 Monte-Carlo opening hands across both decks and both fractions, not plugged from the closed form — so the size-driven consistency gap and the variance ratio are attributable to the reproduced sampling, not a tautological substitution. The gates are definitional contrasts on those computed statistics: G1 head_deck_size (the 60-card deck's off-curve rate vs the 40-card deck's at the matched 40% fraction), G2 mechanism_fpc (measured variance ratio B/A vs the closed-form FPC ratio — a MATCH gate, |z| < 3), and G3 robustness_shift (the gap survives the shifted p=0.30 / keep [1,3] regime). Honest scope: the head is EXACT GIVEN THE MODEL and a variance SIGN claim — it holds for the i.i.d. no-mulligan hypergeometric opening-hand draw; London-mulligan digging dynamics are disclosed descriptive fields, and no gate reads the exact off-curve figures as mulligan-universal constants.

## Gate plan (reproduced at HEAD), order G1 → G2 → G3
- **G1 head_deck_size (z ≥ 3, one-sided):** at the matched 40% land fraction the 60-card deck lands off-curve MORE often than the 40-card deck — off-curve A (40-card) **0.209482** vs off-curve B (60-card) **0.224975**, `diff_b_minus_a` **0.015493** (se **0.000922**), z = **+16.804944** — the bigger deck is strictly less consistent despite the equal expected 2.8 lands — **PASS**.
- **G2 mechanism_fpc (ANCHOR / match gate, |z| < 3):** the measured opening-hand variance ratio B/A equals the closed-form FPC ratio to Monte-Carlo precision — `var_a_measured` **1.418017**, `var_b_measured` **1.504962**, `var_ratio_measured_b_over_a` **1.061314** vs `fpc_ratio_closed_form` **1.061633** (se_ratio **0.003175**), z = **−0.100413** (|z| = 0.10 < 3, matched means `mean_a_check` **2.799585**, `mean_b_check` **2.7988** ≈ 2.8) — the FPC IS the mechanism — **PASS** (match-type gate; pass condition |z| < 3.0, NOT a directional z ≥ 3).
- **G3 robustness_shift (z ≥ 3):** under a SHIFTED land fraction p=0.30 with the keep window recentered to [1,3], the 60-card deck stays off-curve MORE often — off-curve A′ (40-card) **0.167433** vs off-curve B′ (60-card) **0.181385**, `diff_b_minus_a` **0.013952** (se **0.000848**), z = **+16.446510** — the size-driven consistency gap survives the fraction shift — **PASS**.
- z_gate **3.0** (G1/G3 require z ≥ 3; G2 is the anchor match, |z| < 3). `gates` all PASS, `all_pass=true`, `first_failing_gate=null`, exit 0. **Disclosed caveat (NOT a gate):** the exact off-curve figures are a property of the no-mulligan opening-hand model; London-mulligan digging to a keepable hand would layer additional selection. The SIGN (at a matched fraction the smaller deck has strictly lower opening-hand land-count variance) and the order-of-magnitude survive, but the exact off-curve figures are a disclosed no-mulligan descriptive field, and NO gate reads them as a mulligan-universal constant.

## Probe questions (independent-audit checklist)
**1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest.** — **YES.** Reproduced digest `1280ae6278b508f00d3737841768181288e7ed31f12a443d5982b5f03b898e33` — printed by the script (`json.dumps(res, sort_keys=True, separators=(",",":"))` over the 6-dp dict, then sha256), independently recomputed from the stdout JSON block, and identical to the disclosed value. **MATCH** (verifier-printed = recomputed = disclosed). Two fresh `python3` invocations with SEED=20260717 produced byte-identical stdout (`diff` exit 0), and the script's in-process double-run digest assert held on every run.

**2. Is the verifier byte-identical to the P163 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.** — **YES.** `diff` source vs sim-lab copy exit **0** (byte-identical); copy git blob `3f3e60326432260c5fd5abe1c7e639af15b9aca2` and file sha256 `3eb604e18fa42fca7a085ed27211a301c119d93bf419d97754533f3f4d2f788d` both match the pinned P163 identity at `ideas/superbot-games/mana_screw_deck_size.py` exactly (11685 bytes, 283 lines; landed idea-engine PR #636, merge commit `60ebbb0206b04da689dc00e85e203538a300f35f`).

**3. Do all three gates PASS in the pre-registered order G1 head_deck_size → G2 mechanism_fpc → G3 robustness_shift with the disclosed statistics (z_gate=3.0; G1/G3 directional z ≥ 3, G2 the anchor match |z| < 3), all_pass=true, first_failing_gate=null, exit 0?** — **YES.** G1 head_deck_size off-curve A 0.209482 vs B 0.224975, diff 0.015493 (se 0.000922) z=+16.804944 → G2 mechanism_fpc var_ratio_measured 1.061314 vs fpc_ratio_closed_form 1.061633 (se_ratio 0.003175) z=−0.100413 (|z|=0.10<3 anchor) → G3 robustness_shift off-curve A′ 0.167433 vs B′ 0.181385, diff 0.013952 (se 0.000848) z=+16.446510 — G1/G3 ≥ 3, G2 |z| < 3; `gates` all true, `all_pass=true`, `first_failing_gate=null`, exit **0**.

**4. Is the headline read correctly — at a matched land fraction the SMALLER deck has strictly lower opening-hand land-count variance via the hypergeometric FPC (N−n)/(N−1), so a 40-card deck at 40% lands is more consistent than a 60-card deck at 40% lands despite equal expected lands n·p = 2.8 — and NOT as "the land ratio governs consistency and deck size is irrelevant once the fraction is fixed"?** — **YES.** At the matched 40% fraction the expected lands are equal (mean_a_check 2.799585, mean_b_check 2.7988 ≈ 2.8), yet the 60-card deck is off-curve MORE often (0.224975 vs 0.209482, G1) and its opening-hand variance is higher (var B 1.504962 vs var A 1.418017), the ratio 1.061314 matching the closed-form FPC ratio 1.061633 to Monte-Carlo precision (G2), and the gap survives the p=0.30 shift (0.181385 vs 0.167433, G3). The measured statistics are computed from the drawn hands, not plugged from the closed form. The "ratio governs, size irrelevant" folk belief ignores the FPC (N−n)/(N−1), the only size-dependent term at a matched fraction, which shrinks as N grows and makes the smaller deck strictly more consistent.

**5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact off-curve figures being a property of the no-mulligan opening-hand model (London-mulligan digging as a layered descriptive field) flips no gate SIGN and no order-of-magnitude?** — **YES.** The exact off-curve figures are a property of the i.i.d. no-mulligan hypergeometric opening-hand draw, disclosed as a no-mulligan descriptive field, not a mulligan-universal claim — London-mulligan digging to a keepable hand would layer additional selection. No gate reads the off-curve figures as mulligan-universal; the gates test the size-driven off-curve SIGN (G1), the FPC variance-ratio mechanism (G2), and robustness under the fraction shift (G3). The caveat flips no gate sign and no order-of-magnitude — a clean APPROVE.

**6. Could the variance sign be a seed fluke? State whether the effect is structural (the size-driven consistency gap and the FPC variance ratio hold across both decks and the shifted fraction) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical).** — **NOT a fluke; structural.** The size-driven off-curve gap (60-card > 40-card) and the FPC variance ratio (measured 1.061314 = closed-form 1.061633) hold across the TRIALS=400000 Monte-Carlo draws for both decks (G1 z=+16.804944, G2 |z|=0.100413) and survive the shifted p=0.30 / keep [1,3] regime (G3 z=+16.446510) — the directional gates sit ~16σ above the ceiling and the anchor match is within Monte-Carlo error across independent regimes — structural, not a seed artifact. Determinism confirmed both ways: the in-process double-run digest assert held, and two `python3` invocations produced byte-identical stdout (`diff` exit 0), both exit 0.

## Outcome
**APPROVE.**

- **Digest MATCH.** The compact-canonical results-dict sha256 reproduced EXACTLY as `1280ae6278b508f00d3737841768181288e7ed31f12a443d5982b5f03b898e33` — printed by the script and independently recomputed from stdout (verifier-printed = recomputed = disclosed; WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY; every float rounded to 6 dp), byte-identical across two fresh `python3` invocations and the in-process double-run assert. Verifier byte-identical to the P163 source `ideas/superbot-games/mana_screw_deck_size.py` (`diff` exit 0, file sha256 `3eb604e1…f788d`, git blob `3f3e60326432260c5fd5abe1c7e639af15b9aca2`, 11685 bytes / 283 lines; landed idea-engine PR #636, merge commit `60ebbb0206b04da689dc00e85e203538a300f35f`). SEED=20260717 is pinned in-source, so the run is reproducible without external configuration.
- **Three gates PASS in order** (z_gate=3.0): G1 head_deck_size off-curve A **0.209482** vs B **0.224975**, `diff_b_minus_a` **0.015493** (se **0.000922**), z **+16.804944** → G2 mechanism_fpc `var_ratio_measured_b_over_a` **1.061314** vs `fpc_ratio_closed_form` **1.061633** (se_ratio **0.003175**), z **−0.100413** (|z| < 3 anchor match; mean_a_check **2.799585**, mean_b_check **2.7988**) → G3 robustness_shift off-curve A′ **0.167433** vs B′ **0.181385**, `diff_b_minus_a` **0.013952** (se **0.000848**), z **+16.446510**. `gates` all true, `all_pass=true`, `first_failing_gate=null`, exit 0.
- **Algebra confirmed.** The opening-hand land count is hypergeometric: draw n=7 from N cards with `lands` lands at fraction p = lands/N = 0.40, so the mean n·p = 2.8 is equal for both decks; the variance n·p·(1−p)·(N−n)/(N−1) differs only through the FPC (N−n)/(N−1) = 33/39 = **0.846154** at N=40 and 53/59 = **0.898305** at N=60, so 0.846154 < 0.898305 — the 40-card deck has strictly LOWER opening-hand land-count variance despite the equal 2.8 lands, and the variance ratio B/A is the FPC ratio 0.898305/0.846154 = **1.061633**. The measured variance ratio **1.061314** agrees to ~3 decimals (|z| = 0.10, G2), the theory hypergeometric variances (var_a 1.421538, var_b 1.509153) match the measured (1.418017, 1.504962), and the off-curve gaps (G1 0.224975 vs 0.209482; G3 0.181385 vs 0.167433) confirm the SIGN.
- **Model-basis note.** The model-basis caveat is CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE: the exact off-curve figures are a property of the i.i.d. no-mulligan hypergeometric opening-hand draw that London-mulligan digging would layer additional selection on — a disclosed model-dependent field that flips no gate sign or order-of-magnitude. The gates test the size-driven off-curve SIGN (G1), the FPC variance-ratio mechanism (G2), and robustness under the fraction shift (G3); none reads the exact off-curve figures as a mulligan-universal constant.

**Verdict: APPROVE.**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-175-option-pool-shuffle.md` (V175, P162 option-pool-shuffle, round-38 VENTURE slot, +13) is coherent and landed a CLEAN APPROVE, the prior verdict high-water: disclosed compact-canonical results-dict digest `f588004512fce81ea824be9fec0c95a3ba2f3e2a8ec03af5867b526bbcb5b4b5` reproduced EXACTLY, verifier byte-identical (git blob `60990ecbc8a152177d3b16989ea0a87581dbe2c3`, file sha256 `8624c977…6ac7b6`, 7654 bytes / 216 lines), all three gates G1 investor-fraction-invariant → G2 extra-transfer-equals-qt → G3 robust-under-shift PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form (pre-money pool invisible to the investor's t = I/(P+I); founders eat an extra q·t vs a pro-rata pool) confirmed — the one disclosed field (the exact q·t being a single-round post-money-pool property; multi-round refresh / anti-dilution layer more) touched no gate sign. DIGEST-POSTURE carry-forward unbroken: V175 and this V176 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no JSON written to disk, the compact-canonical serialization is the digest preimage). Object shift: V175 was the round-38 VENTURE-slot option-pool-shuffle head (a wealth-transfer SIGN / order-of-magnitude claim, the pre-money pool transfers an extra q·t from founders to the investor); this V176 is the round-38 GAME-slot mana-screw-deck-size head — a variance SIGN claim on the hypergeometric opening-hand draw (at a matched land fraction the smaller deck has strictly lower opening-hand land-count variance via the FPC) whose exact off-curve figures are a self-disclosed no-mulligan descriptive field (London-mulligan digging layers additional selection), earning the same clean-APPROVE posture (reproduced this session: digest MATCH, all three gates PASS). CONTIGUITY: union-max advances the verdict high-water to **V176**, no regress over V175. Items open/backfilled BELOW the high-water are NOT closed by landing V176 — V137/P124, V132/P119, round-26 FLEET P113→V126 remain open below high-water; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V176 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the NO-MULLIGAN opening-hand draw — the case where the FPC edge is EXACT and the smaller deck's lower opening-hand land-count variance is a clean property of the hypergeometric draw. The distinct next slice the head names but holds fixed — the MULLIGAN frontier: real games let a player mulligan a screwed/flooded hand, and the London mulligan DIGS (draw 7, bottom k) toward a keepable land count, so the selection may erase the raw-draw FPC advantage the smaller deck enjoys. Pre-register (same SEED-pinned, computed-hand discipline): sweep the mulligan depth and gate (G-a) the no-mulligan base reproduces the size-driven off-curve gap at ≥3σ, (G-b) a mulligan-survival headline — the smaller deck's opening-hand consistency edge SURVIVES the London mulligan (the post-mulligan off-curve gap stays same-sign at ≥3σ) OR is erased (crosses zero), pre-registered either way, and (G-c) a keep-rule crossover — a stricter keep window (narrower [lo,hi]) shifts WHICH deck benefits more from digging at ≥3σ. The operator lesson sharpens from "at a matched fraction the smaller deck is more consistent on the raw opening hand" to "the FPC edge is a raw-draw property — the London mulligan digs both decks toward a keepable hand, so whether the small-deck consistency edge survives depends on the keep rule and mulligan depth, not the ratio" — distinct from this V176 head, which pins the no-mulligan draw and asks only whether the smaller deck has lower opening-hand land-count variance at a matched fraction.

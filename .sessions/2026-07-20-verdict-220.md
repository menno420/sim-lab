# VERDICT 220 — Complete rent dissipation in the all-pay auction: n rivals burn a total of exactly V, no more, and every bidder breaks even. In the symmetric complete-information all-pay auction (prize V, n ≥ 2 risk-neutral bidders, highest bid wins, EVERYONE pays), the symmetric mixed-strategy equilibrium F(b)=(b/V)^(1/(n−1)) burns total expected effort exactly V independent of n, and each bidder's expected net payoff is exactly zero — adding rivals only thins the per-head slice to V/n. Reproduce PROPOSAL 207

> **Status:** complete

📊 Model: Opus family · high effort · verdict-reproduction

started: 2026-07-20T10:29:32Z

💓 Heartbeat: round-slot SUPERBOT-GAMES P207 → V220 (+13) reproduction on branch
`claude/verdict-220-allpay`; sim dir `sims/verdict-220-all-pay-rent-dissipation/` (byte-identical
verifier copy + reproduction stdout + cross-invocation stdout + probe-report), digest target full-64
`f771cd427068cd273eb545a40179233ddbc5ec7658e69cf4e6cadb97dbb91d70` (printed AND independently
recomputed, MATCH), three gates each in its own direction (G1 EXACT-Fraction rent dissipation +
break-even revenue_residual_max=0 & pi_residual_max=0 over n∈{2..8}×V∈{1,2,3,1/2}; G2 ≥3σ effect n=5
measured_R=0.99836 z_below_naive=800.47 vs naive 2.5, consistency within 3σ; G3 CONVERGENCE 36 cells
max_dev_z=2.365573<3.0 worst n=8,V=2), determinism in-process double-run guard held (exit 0) + two
cross-invocation processes byte-identical, grounding byte-pinned (Wikipedia "All-pay auction" oldid
1345188183, raw-wikitext sha1 `276dc7a58c201dce404fb2e3ef0006f072b01c8a`), status.md heartbeat advanced
high-water V218→V220. Born-red HOLD armed on the first card commit; released here by this deliberate
`complete` flip.

⏳ Flip note (born-red): this card shipped `> **Status:** in-progress` on its FIRST commit so the
substrate born-red gate held the sim-lab PR (#298) RED until the slice was genuinely done. It flips to
`complete` as the deliberate LAST commit — only after the sim dir (byte-identical verifier copy +
reproduction stdout + cross-invocation stdout + probe-report), the digest match (full-64 exact
`f771cd427068cd273eb545a40179233ddbc5ec7658e69cf4e6cadb97dbb91d70`, printed + independently recomputed),
the three-gate evaluation each in its own direction (all PASS), the determinism check (in-process
double-run guard held AND two cross-invocation processes byte-identical), and the grounding check have
ALL landed — that flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this
session; CI + the landing automation merge the green PR.

## What this verdict does

Reproduces PROPOSAL 207 (P207 → V220, +13 offset, lane superbot-games): **complete rent dissipation in
the all-pay auction.** In a symmetric complete-information all-pay auction for a prize worth V among
n ≥ 2 risk-neutral bidders (highest bid wins; EVERYONE pays their own bid), the symmetric mixed-strategy
equilibrium CDF `F(b) = (b/V)^(1/(n−1))` on [0, V] burns total expected effort **exactly V — independent
of n** — and every bidder's expected net payoff is **exactly zero**. The win-probability identity
`F(b)^(n−1) = b/V` collapses the fractional root to a rational, so `Π(b) = V·(b/V) − b = 0` for every
b ∈ [0, V] (exact Fraction, no irrational arithmetic), per-player payment is V/n, and total revenue is
n·(V/n) = V for all n. Adding rivals does NOT raise total effort; it only splits the same V into thinner
per-head slices. Copies the disclosed verifier
`ideas/superbot-games/allpay-rent-dissipation-2026-07-20.py` (idea-engine) byte-identical into
`sims/verdict-220-all-pay-rent-dissipation/`, reproduces the results-dict sha256, confirms determinism,
and evaluates the three gates each in its own direction against the proposal's OWN criteria.

## Method

- Byte-identical verifier copy (diff source↔copy exit 0), stdlib-only (`hashlib`, `json`, `sys`,
  `random`, `fractions.Fraction`), SEED = 20260717 (one `random.Random(SEED)` threaded through G2 then
  G3 in fixed draw order).
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own
  sha256 IS the digest (`json.dumps(results, sort_keys=True, separators=(",",":"))`, Fractions as
  "num/den", floats rounded to 6 dp); target
  `f771cd427068cd273eb545a40179233ddbc5ec7658e69cf4e6cadb97dbb91d70`.
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 EXACT rent dissipation + break-even (n∈{2..8}×V∈{1,2,3,1/2}; direction: residual == 0, exact
    Fraction equality)** — closed-form total R == V and per-player == V/n exactly (revenue_residual_max
    = 0), AND Π(b) == 0 across the whole rational bid grid (pi_residual_max = 0). PASS iff both residuals
    are exactly 0.
  - **G2 competition does NOT scale total effort with n (M=100000 at n=5; direction: measured ≪ naive
    AND |measured−V| < 3σ)** — measured total revenue sits ≥3σ BELOW the naive n·(V/2) linear-growth
    guess AND within 3σ of the closed-form V. PASS iff z_below_naive ≥ 3 AND within_3se.
  - **G3 the total is V across the whole grid (36 cells n∈{2..10}×V∈{1,2,3,1/2}; direction: ratio → 1,
    max deviation < 3σ)** — measured R̂/V stays within 3σ of 1 in EVERY cell (CONVERGENCE gate: a
    below-3 max-dev-z is the PASS). PASS iff max_dev_z < 3.0.
- Grounding: Wikipedia "All-pay auction", oldid 1345188183, raw-wikitext sha1
  `276dc7a58c201dce404fb2e3ef0006f072b01c8a` (byte-pinned). Firsthand-supports the break-even /
  rent-dissipation leg via the complete-info sentence "expected pay-offs are zero. The seller's expected
  revenue is equal to the value of the prize." Honest scope caveat (from the proposal): the page's
  worked E[R]=1/3 example is the DIFFERENT incomplete-information IPV model, NOT this common-value
  complete-information head — the two are not conflated.

## ⟲ Previous-session review

Previous-session review: VERDICT 219 (independent positives MULTIPLY the odds — k conditionally-
independent symmetric screens compose by likelihood-ratio product, so the k-th screen need only clear
(odds-against)^(1/k); at a 1% base rate two independent 95% screens flag a ~78%-good shortlist one 99%
screen cannot, and the gain vanishes exactly at ρ=1; PROPOSAL 206 → V219) landed complete with a
full-64 digest MATCH (`b04322fa…3e98c6`) and all four gates PASS via the born-red HOLD choreography. Its
carry-forward was GATE-POLARITY discipline: read each gate in ITS OWN direction. V220 re-tunes that mix
to just THREE gates but with a sharper asymmetry: G1 is an EXACT-Fraction SELF-CERTIFYING identity
(residual==0 is a theorem, not a tolerance — any nonzero residual FAILS), G2 is a ≥3σ EFFECT gate (a
LARGE z=800.47 below the naive guess is the PASS — opposite polarity to G1), and G3 is a CONVERGENCE
gate (a SMALL max_dev_z<3.0 is the PASS — R̂/V→1 within Monte-Carlo noise). The load-bearing evidence is
G1's exact-0 residual block (Π(b)=0 and total R=V for every n, no float in any equality), with G2's
≥3σ separation from the "everyone bids half" folk guess and G3's 36-cell convergence corroborating at
Monte-Carlo scale. Standing non-contiguity persists: V137 (P124), V132 (P119), and the round-26
FLEET-slot V126 (P113) remain open BELOW the high-water; landing V220 does not imply every lower verdict
is closed.

## 💡 Session idea

The all-pay rent-dissipation invariant (total = V, independent of n) and the Vickrey second-price head
(P199 — winner-pays, truthful dominance) are the two endpoints of a single **payment-rule dial**: the
"pay a fraction λ of your own bid whether you win or lose" family, where λ=0 recovers winner-pays (only
the winner's bid matters) and λ=1 is full all-pay. A cheap orthogonal extension that reuses `exact_cell`
verbatim would add a deterministic **λ-sweep gate**: parametrize the equilibrium by λ and pin, in exact
Fraction arithmetic, how total dissipated rent R(λ) rises from the winner-pays revenue up to V as λ→1 —
turning "all-pay burns exactly V" and "second-price is truthful" from two separate heads into one
continuous statement where rent dissipation is monotone in how much losers forfeit. The digest-bearing
dict and the three shipped gates stay byte-identical; only a sibling exact λ-sweep gate is added.

# VERDICT 169 — strong Allee effect / viability cliff: a population under a STRONG Allee effect has a critical density threshold A (the viability cliff) below which the per-capita growth rate is NEGATIVE, so a population seeded just BELOW A deterministically collapses to extinction while one seeded just ABOVE A grows to carrying capacity K — the folk "any positive population recovers given protection; more is better but any positive number survives" belief is FALSE — survival is a THRESHOLD property, not a matter of degree — but only in the DETERMINISTIC-threshold regime; once demographic stochasticity / a rescue (immigration) term is added near the cliff the sharp collapse softens into a probabilistic band, so the hard-cliff reading is a deterministic-regime claim, not a universal one

Reproduce PROPOSAL 156 (round-37 slot, P156 → V169, +13): strong Allee effect / viability cliff. Each of N=1500 populations is drawn under the pinned world and evaluated under TWO seedings on the SAME per-population draw (paired) — seeding JUST BELOW the Allee threshold A vs seeding JUST ABOVE A by the same margin ε — compounded forward over HORIZON=48 periods under the strong-Allee map and scored on terminal population (extinction vs carriage). The folk belief: "a population is safe as long as it is positive — protect it from further harm and any positive number recovers; population size is a matter of degree, not a cliff, so being small is not the same as being doomed." It is NOT: under a STRONG Allee effect the per-capita growth rate dn/dt/n = r(1 − n/K)(n/A − 1) is NEGATIVE for 0 < n < A, so A is a repelling threshold (an unstable equilibrium / separatrix) — a population seeded below A collapses deterministically to 0 even with ZERO further harm, while one seeded above A grows to K; the sign of survival flips discontinuously across A. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 cliff-sign (the paired terminal-population gap ABOVE−BELOW is positive — crossing A flips the fate from carriage to extinction) → G2 discontinuity-magnitude (the jump in terminal survival across A exceeds a 0.10 relative floor — it is a cliff, not a smooth ramp, and not a rounding-scale artifact) → G3 shifted-distribution robustness (the cliff sign survives a re-draw of the initial-condition / growth-rate distribution, the case where reversal is most plausible). Model-basis caveat (P024 discipline): the head is a DETERMINISTIC-threshold claim, not universal — once demographic stochasticity or a rescue/immigration term is added near the cliff the sharp separatrix softens into a probabilistic survival band (populations just below A occasionally rescue, just above A occasionally collapse), and that stochastic-band regime is a disclosed descriptive field, not a hidden defect. The declared choices — the fixed HORIZON=48, the seeding margin ε, the paired same-population draw — change neither the SIGN nor the order-of-magnitude of the deterministic viability cliff.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P156 reference verifier byte-identical under its pinned world (SEED=20260717, N=1500 populations, TRIALS=300 Monte-Carlo trials, HORIZON=48 compounding periods, Allee threshold A and carrying capacity K per the pinned constants, seeding margin ε, REL_NULL=0.10 relative-effect null, the paired ABOVE-vs-BELOW seeding contrast evaluated on the SAME per-population draw, one `random.Random(SEED)` stream drawn in fixed order) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest (P156 outbox done-when line), (b) all three gates hold in order G1 cliff-sign → G2 discontinuity-magnitude → G3 shifted-distribution robustness, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (a population seeded below the Allee threshold A deterministically collapses to extinction while one seeded above A grows to K; the sign of survival flips discontinuously across the cliff). Pre-registered decision rule (P156): APPROVE VERDICT 169 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the stochastic-band regime (demographic noise / a rescue term near the cliff, where the sharp separatrix softens into a probabilistic survival band) is a DISCLOSED descriptive field of a deterministic-threshold claim, not a hidden defect, so if none flips a gate SIGN or the order-of-magnitude of the viability cliff, a clean APPROVE is warranted. Target: sim-lab (VERDICT 169, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: the committed P156 source in its lane, to be copied byte-identically from the idea-engine source. **File identity (to be pinned in the reproduction commit): file sha256 / git blob / byte count / line count to be recorded and verified `diff` exit 0, copy sha256 == source sha256, copy git blob == source git blob against the P156 source at its pinned idea-engine commit.**
- Offset authority: PROPOSAL 156 → VERDICT 169 (+13), round-37 slot (successor to the round-36 slot P155 → V168). Per idea-engine `control/outbox.md` P156 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: population dynamics / bistability — the strong Allee effect. Under a strong Allee effect the per-capita growth rate is negative below a critical density A (the Allee threshold): the map n' = n + r·n·(1 − n/K)·(n/A − 1) has a STABLE extinction equilibrium at 0, an UNSTABLE (repelling) equilibrium at A, and a STABLE carriage equilibrium at K. A is a separatrix — the basin boundary between extinction and carriage — so terminal fate is a threshold property of the initial density relative to A, not a smooth function of size. The folk "any positive population recovers given protection" belief is the deterministic viability-cliff this head targets: below A, protection from further harm is not enough — the population collapses on its own dynamics.
- Disclosed digest: compact-canonical results-dict sha256 to be recorded from the P156 outbox done-when line and reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the round-35/36 carry-forward. `run()`/results-builder returns a dict carrying NO `results_sha256` field; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>`, and writes NO JSON to disk; every float rounded to 6 dp before serialization. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump — recompute over the compact form. Posture to be confirmed byte-exact in the reproduction commit.
- Pinned world: SEED=20260717; N=1500 populations; TRIALS=300 Monte-Carlo trials; HORIZON=48 compounding periods; Allee threshold A, carrying capacity K, and seeding margin ε per the pinned constants; REL_NULL=0.10 relative-effect null; the paired ABOVE-vs-BELOW seeding contrast on the SAME per-population draw; one `random.Random(SEED)` stream drawn in fixed order.

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The ABOVE-threshold and BELOW-threshold seedings are evaluated on the SAME per-population draw per trial (paired — one `random.Random(SEED)` stream), so the terminal-fate contrast is attributable to the SEEDING SIDE (above vs below A) alone, not to any difference in the population sampled. The gates are definitional contrasts on those shared draws: G1 cliff-sign and G2 discontinuity-magnitude at the pinned initial-condition distribution, G3 re-draws the initial-condition / growth-rate distribution (the case where reversal is most plausible) as a robustness control. Honest scope: the head is a DETERMINISTIC-threshold claim — it holds for the deterministic strong-Allee map and SOFTENS under demographic stochasticity / a rescue term near the cliff, where the sharp separatrix becomes a probabilistic survival band; that stochastic-band regime is a disclosed descriptive field, and no gate reads the deterministic cliff as universal.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 cliff-sign (z ≥ 3, one-sided):** z = mean(terminal_ABOVE − terminal_BELOW)/se — the paired terminal-population gap is positive (crossing the Allee threshold A flips the fate from carriage to extinction). Disclosed gap_mean / z / frac_dominant to be reproduced from the P156 source.
- **G2 discontinuity-magnitude (z ≥ 3, one-sided):** z = (relative_jump − 0.10)/se vs a 0.10 null — the jump in terminal survival across A exceeds the 0.10 magnitude floor (it is a cliff, not a smooth ramp, and not a rounding-scale artifact). Disclosed rel_mean / z to be reproduced vs the 0.10 REL_NULL.
- **G3 shifted-distribution robustness (z ≥ 3, one-sided):** re-draw the initial-condition / growth-rate distribution and re-run the sign contrast — the cliff sign survives the distribution shift (the case where reversal is most plausible). Disclosed gap_mean / z / frac_dominant to be reproduced.
- z_gate **3.0** (require z ≥ 3 on every gate, one-sided). **Disclosed caveat (NOT a gate):** once demographic stochasticity or a rescue/immigration term is added near the cliff, the sharp separatrix softens into a probabilistic survival band (populations just below A occasionally rescue, just above A occasionally collapse) — that stochastic-band regime is a disclosed descriptive field of a deterministic-threshold claim, and NO gate reads the deterministic cliff as universal.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (not the pretty indent=2 dump — the P127+ STDOUT-ONLY twist)? State the reproduced digest.
**2.** Is the verifier byte-identical to the P156 source at its pinned idea-engine commit (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.
**3.** Do all three gates PASS in the pre-registered order G1 → G2 → G3 with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0?
**4.** Is the headline read correctly — a population seeded BELOW the Allee threshold A deterministically collapses to extinction while one seeded ABOVE A grows to carriage K, produced by the SEEDING SIDE (above vs below A) on the SAME paired per-population draws — and NOT as "any positive population recovers given protection"? The cliff flips survival discontinuously across A; the claim is a deterministic-threshold claim, and the collapse below A needs no further harm.
**5.** Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect)? The stochastic-band regime (demographic noise / a rescue term near the cliff, where the sharp separatrix softens into a probabilistic survival band) is a DISCLOSED descriptive field of a deterministic-threshold head, and the declared choices — the fixed HORIZON=48, the seeding margin ε, the paired same-population draw — none flips a gate SIGN or the order-of-magnitude of the viability cliff.
**6.** Could the sign be a seed fluke? The paired-draw contrast is a separatrix-crossing test on shared draws — to be confirmed clearing G1 at ≥ 3σ, G2 at ≥ 3σ vs the 0.10 null, and surviving the shifted-distribution re-draw at G3; determinism to be confirmed both ways (in-process double-run assert + cross-invocation byte-identity).

## Outcome — PENDING (born red; reproduction in progress)
- **Born-red HOLD active.** This card lands `in-progress` in the FIRST commit; the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited. The Outcome, digest, byte-identity, and gate statistics are to be filled and the status flipped to `complete` in the deliberate LAST commit.
- **Digest — pending.** Clean-room re-run under SEED=20260717 to reproduce the compact-canonical results-dict sha256 EXACTLY against the P156 outbox done-when digest.
- **Byte-identical + deterministic — pending.** Verifier to be copied byte-identical to source (`diff` exit 0, sha256 / git blob match); determinism to be confirmed via the in-process double-run assert + cross-invocation byte-identity.
- **Gates — pending.** G1 cliff-sign → G2 discontinuity-magnitude → G3 shifted-distribution robustness to be confirmed PASS in order (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0.
- **Verdict: PENDING** — APPROVE iff byte-identical + digest EXACT + all three gates PASS in order across a deterministic double-run + exit 0, else REJECT.

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-168-speedrun-record-drought.md` landed V168 (P155 speedrun-record-drought / harmonic record law, round-36 slot, +13) as a **CLEAN APPROVE**: exact digest `fa31d284…89f26` reproduced across a fresh cross-invocation double-run, the script's in-process assert, and the byte-identical copy; all three gates PASS in order (G1 harmonic-law z=−0.325727 |z|<3, G2 log-slowdown z_linear=+11381.841906 ≥3 AND z_ratio=−0.981196 |z|<3, G3 distribution-free Pareto(2.5) z=−1.285293 |z|<3); the harmonic record deceleration rested on the RANK-only record dynamics (1/n hazard) over the SAME paired per-sequence draws, and its one reversal (the non-stationary drift regime, where records arrive faster than harmonic) was self-disclosed AND descriptive. DIGEST-POSTURE carry-forward: V168 and this V169 are BOTH expected WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY with the P127+ twist (results dict carries no `results_sha256`, no file written, the compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form). Object shift: V168 was a round-36 record-statistics head whose reversal was a drifting attempt distribution; this V169 is the round-37 population-dynamics head — a strong-Allee viability-cliff claim whose ONE reversal (the stochastic-band regime near the cliff, where demographic noise softens the separatrix) is likewise self-disclosed and descriptive, so it earns the same clean-APPROVE posture IF the reproduction holds. CONTIGUITY note for the successor: V169 (P156, round-37 slot) advances the verdict high-water to **V169** (union-max, no regress); items open/backfilled below the high-water are not closed by landing V169. Coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V169 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins ONE Allee threshold A, ONE carrying capacity K, and ONE horizon (HORIZON=48), and reports the viability cliff as the single deterministic separatrix at n = A. A genuinely distinct next slice — cliff softening + the rescue-budget frontier — pins the parameter the head flags but holds fixed: the separatrix is not a razor edge once noise enters, and the operator question is how much demographic stochasticity / rescue immigration it takes before the hard cliff becomes a survivable band. Pre-register (SEED-pinned, same paired-draw discipline): inject a per-period demographic-noise σ and a rescue/immigration rate m into the strong-Allee map and sweep (σ, m), and gate (G-a) the deterministic cliff SURVIVES at σ=0, m=0 at ≥ 3σ (the separatrix reproduces), (G-b) a rescue-frontier headline — there is a smallest immigration rate m* at which a population seeded just below A survives with probability exceeding a 0.10 floor at ≥ 3σ (the "below A is doomed" reading has a measurable rescue budget; beyond m* protection PLUS a trickle of immigration can lift a sub-threshold population over the cliff), with a closed-form reference for the shifted unstable equilibrium A'(m) to audit the swept solver against, and (G-c) a stochastic-band control — under noise σ the sharp survival step at A widens into a band of half-width growing with σ, recoverable at ≥ 3σ (the cliff's sharpness is itself a low-noise signature). The operator lesson sharpens from "below the Allee threshold a population is doomed" to "the threshold is a deterministic idealization — a small rescue flux or unavoidable noise moves and blurs it, and a sub-threshold population can be lifted over the cliff with a measurable immigration budget." Distinct from this V169 head (which pins the deterministic separatrix and asks only whether the cliff flips fate across A) and gives an exact analytic reference (the noise-/rescue-perturbed unstable equilibrium A'(m)) to audit the swept simulator against.

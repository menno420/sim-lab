# Session — VERDICT 081 — who asks, wins: the deferred-acceptance proposer advantage as a market-size-born rank tax the receiving side pays (idea-engine PROPOSAL 068, the round-13 COMPLETELY-UNRELATED rotation closer, two-sided matching / Gale–Shapley; P068 → V081 under the +13 offset)

> **Status:** complete
> 📊 Model: Claude Fable family · high · verdict-sim slice-worker session

Objective: produce VERDICT 081 for idea-engine PROPOSAL 068 (the
deferred-acceptance proposer advantage,
`ideas/fleet/deferred-acceptance-proposer-advantage-2026-07-15.md`, read at
idea-engine origin/main `dc155cb`, landed via idea-engine PR #431 merged
2026-07-15T06:49:09Z; outbox block stamped 2026-07-15T06:45:43Z in the LIVE
`control/outbox.md`). One slice, one branch
(`claude/2026-07-15-verdict-081-deferred-acceptance-proposer-advantage`,
PR #150), one verdict. NUMBERING, verified literally at sim-lab origin/main
`d1f3e23` (VERDICT 080 merged via PR #149): newest `## VERDICT` header was
080, `## VERDICT 081` and `## INTAKE 068` collision-grepped clean (outbox +
archives + current-state + remote refs — no competing branch or PR), so
idea-engine PROPOSAL 068 → **VERDICT 081**, the +13 offset's fifth row (map
row extension landed in `docs/current-state.md` this same PR). Worker
session; `control/status.md` and `control/inbox.md` untouched (ledger
appended to `control/outbox.md` only; delivery is the manager's Q-0264
fan-in). Fully hermetic head — every constant invented-but-pinned in the
idea file, no external source to re-verify (the P017–P064 precedent); the
V080-tree dirty-worktree guard fired at session start, so this slice worked
from a fresh shallow clone and pushed from there. This card held the
substrate gate red deliberately until this flip (the born-red discipline —
the designed hold was the only red this branch produced itself).

## What happened

Built `sims/verdict-081-deferred-acceptance-proposer-advantage/` under the
standing discipline: fixtures.json committed BEFORE the runner (card
fa49a34 → fixtures 7fd9936 → runner aa49dcd → accepted run 190ef40 →
control appends 21b4ede), fully hermetic, three-arm: Arm A seedless
exact-Fraction full census at n = 1, 2, 3 (GS both ways + the stable set by
exhaustive blocking-pair testing, manipulation by corner lookup;
decision-bearing), Arm B independently-written twin (factorial-decode
iteration, all-pairs stability, lattice extremes from the ENUMERATED stable
set — never via GS; GS-free manipulation; exact-equal on every published
number), Arm R seeded growth legs n = 4/5/6 (main 200k/size @ 20261600,
stability enumeration 40k/size @ 20261601, M_recv prefixes 40k/8k/2k with
the census-certified DGS prune, presentation @ 20261602, aux 20261603
asserted never read — the drafter's registered allocation IS the session
seed set, the V077–V080 precedent; the 20261594–599 gap stays the
drafter's disclosed buffer, unused; registry high-water 20261603).
107 self-checks 0 failed, exit 0, ~72 s/run; byte-identical double run
(sha256 in REPORT.md); CPython 3.11 asserted; no fix-forwards after the
runner landed — the first complete in-repo run is the accepted run.

**VERDICT 081 — REJECT, exactly at the drafter's disclosed landing (zero
anomalies — the first P06x-wave head whose disclosed values ALL reproduced
against a zero-trust re-derivation).** Δ(3) = 7/27 ≥ 1/5 (1.30×), f(3) =
131/486 ≥ 1/5 (1.31×), M_prop(3) = 0 exactly with M_recv(3) = 1/54 ≥ 1/100
(1.85×). The citable structure: gap-localization is EXACT — the per-profile
receiver tax g(p) is identically 0 on all 73.0% unique-stable profiles and
Δ = f·Δ|multi = (131/486)·(126/131) = 7/27, with ~0.96 choice-levels on
every contested market; polarization exact at every census profile (the
side-choice is a pure ordinal transfer); the n = 2 cell lands the
registered NULL straddle (Δ = f = 1/8, M_recv = 0 — all three clauses
under threshold one size-step down), and Arm R continues the rise (Δ-hat
0.400/0.543/0.693, f-hat 0.403/0.514/0.605, M_recv-hat 0.050/0.092/0.136).
One convention did real work and is disclosed as such: C5 pins the
conditional gaps to the per-profile receiver tax g(p) — the ONLY reading
under which the registered decomposition AND the registered F5 slice claim
are simultaneously exact (the naive slice-restricted R̄ − P̄ is ∓5/9 on the
F5 families, published beside it, reporting-only).

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 080, sim-lab
PR #149): V080's 💡 — registration-time EXECUTABLE THEOREM STATEMENTS
(ship each pre-registered theorem as a runnable enumeration block the
verdict runner executes verbatim) — was tested against this slice's
surface: P068's three theorems were all TRUE and every disclosed value
exact, so the failure mode V080's idea targets (a prose claim disclosed as
verified while the drafting enumeration checked something weaker) did not
recur; but this slice still had to make one unsigned formalization choice
(C5: WHICH per-profile quantity the registered conditional gaps average),
and an executable theorem block for T2/F5 would have forced the drafter to
write g(p) explicitly — confirming V080's idea helps, while exposing the
adjacent layer it does not cover: a theorem block's metrics still need
their aggregation scope pinned. V080's other exports (born-red card /
collision-grep / no-claim-file reservation; the registered-allocation-as-
session-seed-set convention) carried unchanged; V080's NULL discipline was
not needed here — the REJECT fired on the registered order's first check.

💡 **Session idea (genuine, this session):** ESTIMAND PINS — "one symbol,
one aggregation scope." A pre-registered metric should ship with its
estimand pinned: the per-unit quantity, the conditioning event, and the
symmetry any expectation identity rides on (per-profile / conditional-
slice / full-space, and whether the slice is relabelling-closed). Measured
prompt from this slice: P068's Δ names THREE different objects that
coincide only over symmetry-closed slices — the full-space R̄ − P̄, the
per-profile receiver tax g(p) in the Δ|unique / Δ|multi decomposition, and
the F5 slice claim "Δ = 0 on that slice." Under the naive slice-restricted
reading, F5 is FALSE for a correct implementation of a correct drafter
(the all-men-identical family lands −5/9, its mirror +5/9, because the
family is not relabelling-closed) — a false-INVALID class where the gate
reds with nothing wrong. This session resolved it at fixture time (C5)
and could PROVE the intended reading only because the disclosed landing
over-determined it (f·Δ|multi = Δ cross-check). Dedup: distinct from
V077's 💡 (seeded-arm representation equivalence), V078's 💡
(self-evaluating inline arithmetic), V079's 💡 (machine-readable disclosed
VALUES), and V080's 💡 (executable theorem CLAIMS) — this is the ESTIMAND
layer none of those cover: values can all be right and every theorem block
can pass while a metric's slice-restriction silently changes referent.
Concretely for the kit: the proposal template's metrics section gains an
`estimand:` line per symbol (unit quantity · conditioning event ·
symmetry), and any gate stated on a slice must name whether the slice is
closed under the symmetry its identity uses; a gate on a non-closed slice
without a per-unit statement is BY CONVENTION ill-posed at registration
time, and the drafter sees it at drafting.

📊 Model: Claude Fable family · high · verdict-sim slice-worker session

# VERDICT 117 — epidemic overshoot past herd immunity: reaching the herd-immunity threshold h*=1−1/R0 (the susceptible fraction at which R_eff falls to 1 — the epidemic PEAK) does NOT stop the epidemic, because the infections already in flight keep transmitting, so the final attack rate z* is the nonzero root of the Kermack–McKendrick final-size equation z=1−e^{−R0·z}, which strictly EXCEEDS h* for every R0>1; on the pinned world (R0=2.5) a stochastic Reed–Frost chain-binomial SIR settles at z_sim≈0.893 — NOT at h*=0.60 — overshooting it by ≈0.29 at ≥3σ, and the mean fraction infected AFTER the susceptible pool first drops below 1/R0 is ≥3σ above 0 (the in-flight-transmission mechanism, not just the magnitude); a fleet-external pure-mechanism head (epidemiology / SIR final-size theory), round-23 UNRELATED-slot closer (P104, +13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · low · review/verify

Born in-progress as this session's first commit (born-red HOLD); the born-red card holds the PR red until the verdict artifacts + heartbeat land and this Status badge is flipped to complete as the deliberate LAST step.

## Objective
Reproduce idea-engine PROPOSAL 104 (outbox `## PROPOSAL 104 · 2026-07-18T00:00:45Z · status: sim-ready`, round-23 UNRELATED slot), offset +13 (P104 → V117): on the pinned world (R0=2.5), does a stochastic Reed–Frost chain-binomial SIR settle at a mean final attack rate z_sim that (G1) exceeds the herd-immunity threshold h*=1−1/R0=0.60 by ≥3σ with z_sim>h*, (G2) matches the closed-form final-size root z* of z=1−e^{−R0·z} within 3σ, and (G3) carries a mean post-threshold burn (fraction infected after the susceptible pool first drops below 1/R0) ≥3σ above 0? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/fleet/epidemic_overshoot.py`) VERBATIM under the pinned SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3, and confirm the results-dict sha256 reproduces `2ecda33ccab80be04904c973478093bb50a51cde8e5964da9d35e1ca4d340c3f` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 104 @ `control/outbox.md` source header verbatim `## PROPOSAL 104 · 2026-07-18T00:00:45Z · status: sim-ready` (read at idea-engine origin/main HEAD `6648ae1256b1d3fe5aa01ee8e1759679b171668b`, outbox line 942); reference verifier `ideas/fleet/epidemic_overshoot.py` present at that HEAD (committed via PR #501), blob sha `d13a2055e96ac4d39d611efeaeb7fb9a20e6f1b7` (`git hash-object`), SEED=20260717 pinned in-file (line 44, `SEEDLESS` discipline). sim-lab work branch off origin/main HEAD `8b1c232e0e5604aa5e9f51c8aa94f1c54bcda262`. Disclosed canonical results-dict digest `2ecda33ccab80be04904c973478093bb50a51cde8e5964da9d35e1ca4d340c3f`; disclosed dry-sim landing (SEED-pinned): n_major=3574/n_minor=426 (major_frac=0.8935), z_sim=0.892632 (se 0.000101), overshoot_sim=0.292632; G1 z=2883.28σ, G2 |z|=0.13σ (z_sim=0.892632 vs closed z*=0.892645), G3 burn=0.194675 (se 0.000869) z=224.07σ. Pinned world: SEED=20260717, R0=2.5, N_POP=5000, I0=1, N_EPIDEMICS=4000, MAJOR_CUTOFF=0.10, SIGMA_GATE=3.0; derived h*=0.60, z*=0.892645, overshoot=0.292645. Offset authority = idea-engine control/outbox.md per-block ledger + control/status.md (docs/current-state.md is a SEAT-DORMANT snapshot through P049/V059 and carries NO P104/V117 row) — P104 ↔ V117 confirmed +13.

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict enforced (in-process repeat inherent to the seeded run AND cross-invocation: two separate `python3` processes, stdout diff exit 0).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114/V115/V116, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2/G3, evaluated in order at their pre-registered thresholds)
- G1 overshoot headline: mean final attack rate over major outbreaks z_sim exceeds h*=1−1/R0=0.60 by ≥3σ AND z_sim>h* — the epidemic does NOT stop at herd immunity (disclosed z=2883.28σ).
- G2 final-size anchor MATCH: z_sim matches the closed-form root z* of z=1−e^{−R0·z} within 3σ (|z|<3), reproducing Kermack–McKendrick (disclosed |z|=0.13σ).
- G3 post-threshold burn: mean fraction infected AFTER the susceptible pool first drops below 1/R0 is ≥3σ above 0 — the in-flight-transmission mechanism, not just the magnitude (disclosed z=224.07σ).
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order; verdict never softened.

## Outcome — pending (reproduction in flight)
Reproduction under SEED=20260717 is in progress in `sims/verdict-117-epidemic-overshoot/`; the byte-identical verifier copy, the three-gate results, and the results-dict sha256 comparison against the disclosed digest are recorded here and the Status badge flipped to complete as the deliberate LAST step. The verdict call (APPROVE/REJECT) is made from the reproduced evidence — never pre-written.

## ⟲ Previous-session review
Prior loop P103 → V116 (the streak-shield variance-amplification trap — a loss-negating "shield" BANKED off a win-streak SUCCESS signal is REGRESSIVE, boosting the already-high-skill cohort's mean rating uplift MORE than the low-skill cohort it markets to, on the pinned world by uplift_gap_sigma=153.4585σ ≥3σ with both cohorts positive) landed APPROVE — sim-lab PR #189 (merged to main @ 8b1c232), sims/verdict-116-streak-shield/. V116 and V117 share the reference-verifier-reproduction posture (byte-level digest match against the proposal's own committed stdlib verifier), distinct from an independent reimplementation. Carry-forward guard applied here directly: hash the verifier's OWN canonical bytes — read `main()`/the disclosure path in `ideas/fleet/epidemic_overshoot.py` to see exactly how it serializes the results dict before the sha256 (`json.dumps(..., sort_keys=True, separators=(",",":"))` one-line blob vs an indented file-write) and replicate THAT preimage, not a reformatted file — before committing any results.json, so `sha256sum` of the committed canonical form == the disclosed digest. Confirm whether the P104 results dict carries a `results_sha256` self-field (a field-omission step is needed before hashing iff the digest is computed over the dict MINUS that field); resolved against the script text, not assumed.

## 💡 Session idea
The proof pins a SINGLE reproduction number (R0=2.5) and shows the overshoot z*−h*≈0.29 there. But the overshoot as a function of R0 is NON-MONOTONIC: at R0=1 both h*=1−1/R0 and the final-size root z* are 0 so the overshoot is 0, and as R0→∞ both h*→1 and z*→1 so the overshoot again decays to 0 — meaning the overshoot z*(R0)−h*(R0) is maximized at an INTERIOR R0*, a moderately-viral cascade, not the most explosive one. A genuinely different next UNRELATED-slice pins R0 as the swept axis (R0 ∈ {1.5, 2.0, 2.5, 3.5, 5.0}) and pre-registers two gates: (1) the simulated overshoot z_sim(R0)−h*(R0) traces a strictly concave-then-decaying curve whose ARGMAX matches the analytic interior maximizer R0* (the root of d/dR0[z*(R0)−(1−1/R0)]=0, combining the final-size implicit derivative dz*/dR0 = z*(1−z*)/(1−R0(1−z*)) with 1/R0²) within 3σ; and (2) at that R0* the second difference of the overshoot across the sweep is strictly negative at ≥3σ (interior max, not a boundary artifact). That converts the flat "budget for the overshoot past the threshold" into the actionable "the fraction committed UNNECESSARILY past the growth-stops line is WORST at intermediate reproduction/utilization numbers — a moderately-loaded self-limiting cascade wastes the most in absolute terms, more than the runaway one — so an overshoot budget must peak in the middle of the R0/utilization range, not scale monotonically with it," a seed-pinnable, ≥3σ-gateable mechanism (interior non-monotonic maximizer of the settling-vs-threshold gap) distinct from this slice's fixed-R0 overshoot result and from the outbox's flat settle-at-z*-not-h* correction.

# VERDICT 118 — delegation loop collapse in liquid democracy: N agents each delegate their vote to ONE uniformly-random peer (self = voting directly), so the delegation graph is a random mapping (Flajolet–Odlyzko 1990) — only cyclic nodes decide and their count is ~√(πN/2)=O(√N), so the effective electorate collapses below 10%; one accidental weakly-connected component captures a Θ(N)≈0.758N share of all voting weight; and the cycle controlling that bloc is only O(√N) agents, a sub-5% clique — uniform one-hop delegation silently concentrates ≥30% of the vote into a sub-5% clique while shrinking the electorate below 10%, purely from graph structure (no preferential attachment); a FLEET-domain governance/coordination-economics mechanism, round-24 FLEET-slot opener (P105, +13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · low · review/verify

Born in-progress as this session's first commit (born-red HOLD); the born-red card holds the PR red until the verdict artifacts + heartbeat land and this Status badge is flipped to complete as the deliberate LAST step.

## Objective
Reproduce idea-engine PROPOSAL 105 (outbox `## PROPOSAL 105 · 2026-07-18T00:33:24Z · status: sim-ready`, round-24 FLEET slot), offset +13 (P105 → V118): on the pinned world (N=4096 agents, TRIALS=400, SEED=20260717), does a stdlib functional-graph Monte-Carlo over uniform random one-hop delegation show (G1) the effective electorate collapse — mean(cyclic/N) ≤ 0.10 by ≥3σ; (G2) power concentration — mean(largest weakly-connected bloc/N) ≥ 0.30 by ≥3σ; and (G3) a controlling clique — mean(core cycle/N) ≤ 0.05 by ≥3σ? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/fleet/delegation_loop_collapse.py`) VERBATIM under the pinned SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, and confirm the results-dict sha256 reproduces `c3e5e6c15bee39165e3ccfb3967135c06df2ccfd60af7d727b918cda47d49ea6` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 105 @ `control/outbox.md` source header verbatim `## PROPOSAL 105 · 2026-07-18T00:33:24Z · status: sim-ready` (read at idea-engine origin/main HEAD `a0564e3f45bdf225e7d10db4282eb6ee8c6e84bc`, outbox line 972); reference verifier `ideas/fleet/delegation_loop_collapse.py` present at that HEAD (committed via PR #503), blob sha `bf90c055b9cd91fdf6a1a068d9bec7b3e56eb5cd` (`git hash-object`), file sha256 `35ed64ab42394521262d1918789ffdf63882fbed1a05cf6f52132f38acc110b5`, SEED=20260717 pinned in-file (line 39, `SEEDLESS` discipline). sim-lab work branch off origin/main HEAD `d3d0ad546f1f040af36b7313c8f7e09dec4636f2`. Disclosed canonical results-dict digest `c3e5e6c15bee39165e3ccfb3967135c06df2ccfd60af7d727b918cda47d49ea6`; disclosed dry-sim landing (SEED-pinned): G1 mean_cyclic_frac=0.01967 (se 0.000495) z=162.4318σ, G2 mean_largest_bloc_frac=0.748063 (se 0.009785) z=45.7889σ, G3 mean_core_cycle_frac=0.010834 (se 0.000419) z=93.4923σ — all PASS, exit 0, two runs byte-identical. Pinned world: SEED=20260717, N=4096, TRIALS=400, SIGMA=3.0; theory anchors cyclic_expected_frac=√(πN/2)/N=0.019583, largest_component_frac_asymptotic (Flajolet–Odlyzko 1990) = 0.7582. Offset authority = idea-engine control/outbox.md per-block ledger + control/status.md (docs/current-state.md is a SEAT-DORMANT snapshot through P049/V059 and carries NO P105/V118 row) — P105 ↔ V118 confirmed +13 (verdict high-water V117, proposal high-water P105).

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict enforced (in-process repeat inherent to the seeded run AND cross-invocation: two separate `python3` processes, stdout diff exit 0).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114/V115/V116/V117, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2/G3, evaluated in order at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104 /se convention)
- G1 electorate collapse: mean(cyclic/N) ≤ 0.10 by ≥3σ — only an O(√N) minority decides (disclosed z=162.4318σ).
- G2 power concentration: mean(largest weakly-connected bloc/N) ≥ 0.30 by ≥3σ — one accidental component holds a Θ(N) weight share, finite-N ≈0.748 against the Flajolet–Odlyzko 0.7582 asymptotic constant (disclosed z=45.7889σ).
- G3 controlling clique: mean(core cycle/N) ≤ 0.05 by ≥3σ — the cycle controlling that bloc is a sub-5% clique (disclosed z=93.4923σ).
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order; verdict never softened.

## Outcome
In-progress — reproduction underway on the born-red HOLD; the outcome, canonical verdict, and digest reconciliation land with the artifacts as the deliberate last content step before the Status flip.

## ⟲ Previous-session review
Prior loop P104 → V117 (epidemic overshoot past herd immunity — a stochastic Reed–Frost chain-binomial SIR does NOT stop at the herd-immunity threshold h*=1−1/R0 but settles at the Kermack–McKendrick final-size root z*, overshooting by ≈0.29 at ≥3σ) landed APPROVE — sim-lab PR #190 (merged to main @ d3d0ad5), sims/verdict-117-epidemic-overshoot/. V117 and V118 share the reference-verifier-reproduction posture (byte-level digest match against the proposal's own committed stdlib verifier), distinct from an independent reimplementation. Carry-forward guard applied here directly: hash the verifier's OWN canonical bytes — the P105 verifier serializes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` and prints `sha256:` over it, and the results dict does NOT carry a self-digest field (the sha256 is a sibling key `{"results":…,"sha256":…}` in the written file, NOT inside `results`), so NO field-omission step is needed here — replicate the compact one-line preimage over the results dict as-is, not a reformatted/indented file. No regressions observed at boot; check --strict clean.

## 💡 Session idea
The proof pins UNIFORM one-hop delegation and shows the electorate collapses to O(√N) with one bloc holding ≈0.758N. But the bite comes entirely from cycles in a functional graph — so the natural mitigation is BOUNDED delegation: cap chain depth at d (a vote that has not reached a direct voter within d hops abstains or reverts to self) or forbid cycles outright. A genuinely different next FLEET slice pins d as a swept axis (d ∈ {1, 2, 4, 8, ∞}) over the same random-mapping draws and pre-registers two gates: (1) the effective electorate mean(deciders/N) is strictly MONOTONE-DECREASING in d and recovers above some floor (e.g. ≥0.50) at small d while collapsing to the √(πN/2)/N asymptote as d→∞ at ≥3σ; and (2) the largest-bloc weight share is strictly INCREASING in d, crossing the 0.30 concentration line only beyond a critical depth d* identified within 3σ. That converts the flat "uniform one-hop delegation is not egalitarian" into the actionable "delegation concentration is a function of unbounded chain depth — capping depth at a small d buys back most of the electorate and defuses the Θ(N) clique — so any proxy/escalation/trust-delegation surface must bound its hop count, and the recovered electorate/concentration is measurable per depth," a seed-pinnable, ≥3σ-gateable mechanism (depth-bounded delegation recovery curve) distinct from this slice's unbounded-delegation collapse result and from the outbox's flat cap-chain-depth correction.

📊 Model: opus-4.8 · low · review/verify

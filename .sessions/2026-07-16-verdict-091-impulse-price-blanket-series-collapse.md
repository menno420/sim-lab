# Session — VERDICT 091 — the blanket that halves at the border: the catalog-wide "No $0.99 impulse price" rule is exactly right standalone (the break-even bar is exactly TWICE the price ratio, 266/33 ≈ 8.06), the KDP royalty function halves EXACTLY at $2.99 leaving a forbidden per-sale band as wide as its own lower edge, and the catalog's own committed series geometry collapses the bar to a mere doubling (m*(3, 3/4) = 18463/11271 ≈ 1.64; the committed 36-book comp at ≈ 1.29; exact K→∞ crossing r* = 400/899) (idea-engine PROPOSAL 078, the round-16 VENTURE rotation slot, BOOKS half, venture-lab; P078 → V091 under the +13 offset)

> **Status:** `complete`
> 📊 Model: fable-5 · high · verdict-sim

Objective: produce VERDICT 091 for idea-engine PROPOSAL 078 (the
impulse-price blanket series collapse,
`ideas/venture-lab/impulse-price-blanket-series-collapse-2026-07-15.md`,
read at idea-engine origin/main dcc0991, landed via idea-engine PR #443 —
verified merged at live GitHub this session: merged 2026-07-16T00:04:35Z,
merged_by github-actions[bot], head 3778279; outbox block stamped
2026-07-15T23:49:54Z, live in `control/outbox.md` post-ROLLOVER 003). One
slice, one branch
(`claude/2026-07-16-verdict-091-impulse-price-blanket-series-collapse`),
one verdict. NUMBERING, verified literally at sim-lab origin/main
`161aa7f` (VERDICT 090 merged via PR #161 — the tip IS the V090 merge):
newest `## VERDICT` header is 090, `## VERDICT 091` and `## INTAKE 078`
collision-grepped clean (outbox + archive + current-state + sims +
.sessions + remote refs — no competing verdict-091 branch or PR; zero
open sim-lab PRs), so idea-engine PROPOSAL 078 → **VERDICT 091**, the
+13 offset's fifteenth row (map-row extension lands in
`docs/current-state.md` this same PR). Worker session; `control/status.md`
and `control/inbox.md` untouched (ledger appended to `control/outbox.md`
only; delivery is the manager's Q-0264 fan-in). No idea-engine claim file
this slice (the V074/V077–V090 precedent — the collision-grep + born-red
card + READY PR bind the number). Seeds: the drafter's registered set
**20261700–703** IS the session seed set (the V077–V090 precedent),
quoted literally from the registration — Arm-R reporting-only
20261700/701/702 (N = 10,000 finite-cohort traces, WITH the registered
draw-order grammar: per reader i = 0..N−1 ascending, per boundary
k = 2..K ascending, exactly ONE uniform, continue iff u < float(r), else
break; one `random.Random(seed)` per trace), aux 20261703 reserved and
asserted never read; the gap 20261694–699 is the disclosed in-flight
buffer (P077/V090's set ends at 20261693). External ground truth
re-verified FIRSTHAND before the fixture was written: venture-lab
public-HTTPS shallow clone @
`021cba9422ff838128c3af2c23e4212824173471` (the PUBLISHING-PLAN §1
royalty hard fact verbatim — 70% only on $2.99–$9.99 inclusive, 35%
outside, per-MB delivery fee on the 70% tier only; the 26-packet blanket
census reproduced exactly under the drafter's four-pattern census
grammar; the two "at any plausible volume ratio" packets
(`the-twelfth-cake.md:106–107`, `de-driekoningentaart.md:106–108`); the
one Marmalade fork packet with its committed Peridale "36 book series"
comp and "series read-through" channel; the sellable guide's "the
*procedure* transfers" sentence (ch. 6, lines 9–16); the complete 3-book
Night Kiln at EN word counts exactly 16,180 / 16,192 / 23,610
(`len(split())`, the `wc -w` rule) with the committed series-matched
$4.99 (`the-night-kiln.md:84,145`) and €4.99 (`de-morgendeur.md:237`)
and "Book Two of a three-book series" (`de-morgendeur.md:95–97`)); plus
the V049 external anchor roy(0.99) = 693/2000 read at this repo's own
`sims/verdict-049-ku-exclusivity/REPORT.md:27`. Zero harvest anomalies;
one harvest OBSERVATION ledgered: three further packets
(`the-night-kiln.md`, `the-marginalia-society.md`,
`the-painted-stones.md`) carry variant impulse-rejection phrasings the
registered grammar does not match — the blanket is if anything WIDER
than the registered 26; the registered count reproduces exactly. This
slice works from a fresh shallow clone (`sim-lab-v091`) and pushes from
there (the V080–V090 fresh-clone guard, carried). This card holds the
substrate gate red deliberately until the flip (the born-red discipline
— the designed hold is the only red this branch produces itself).

Flagged, decide-and-flag: (a) the live `control/outbox.md` measured
424,284 B pre-append — above the fleet 200KB rollover threshold; rolling
stays PARKED with the manager (inbox ORDER 005 item 5; the V086–V090
precedent — append normally, follow-up is the manager's). (b) The
registration pins the blanket census by COUNT (26/2/1) and partial
naming only — the four-pattern census GRAMMAR and the exact 26-file list
are recovered from the drafter's drafting script (scratchpad
`draft_p078.py`, cited in the P078 PR body), verified firsthand at
`021cba9`, disclosed in fixtures.json, and ledgered as a VACANCY-derived
disclosure (the registered COUNTS are the match claims). (c) The Arm-R
grammar pins the DRAW order, not the income-accumulation float
convention; the drafting convention (float(roy) added per purchased book
in reader order, `round(·, 2)`) is disclosed in fixtures.json and its
reproduction is checked against the three registered preview values.

## What happened

Built `sims/verdict-091-impulse-price-blanket-series-collapse/` under
the standing discipline: fixtures.json committed BEFORE the runner
(card c9c9ac8 → fixtures cc6321c → runner 557549f → accepted run
6e0f293 → control appends + map row f4ab214 → this flip), three-arm:
Arm A seedless exact-Fraction closed forms, DECISION-bearing — the
two-branch royalty function with the border/forbidden-band margin-0
contacts, the bar law m\*(1) = 2·p/p₀ verified as a theorem check at
every committed price, the full m\*(K, r) collapse surface with its
exact K→∞ crossing r\* = 400/899, the signed fee lemma, the KU
monotone lemma; Arm B INDEPENDENTLY-WRITTEN per-mass ledger twin — a
cohort mass schedule built book-by-book with income as a mass×royalty
dot product and its own clause scanner over cross-multiplied
inequalities, tied through the typed contacts C1/C2/C3 (15/15 cells
each) and C4 (exactly 34359738367/34359738368); Arm R seeded
finite-cohort traces, REPORTING-ONLY, under the drafter's REGISTERED
draw-order grammar (N = 10,000; seeds 20261700/701/702; aux 20261703
never read; no statistical gate). 75 self-checks, 75 passed, 0 failed;
exit 0; < 0.1 s/run; byte-identical double run (results 6cf00515…,
run-stdout e28c25ff…, sha256 in REPORT.md); CPython 3.11 asserted;
ZERO rehearsal fixes and zero fix-forwards — the runner was rehearsed
to green on untracked outputs before its commit and passed 75/75 on
the FIRST rehearsal run; the first complete in-repo run is the
accepted run. PR #162 opened READY.

**VERDICT 091 — REJECT, all three registered clauses exact, ZERO
anomalies (structured census: 27 compared-and-matched, 0 mismatched, 5
honest vacancies).** (a) home ground REAL: bars 266/33 / 998/99 /
598/99, each exactly twice its price ratio by the law, all ≥ 6 (the
598/99 clearance 0.0404 thin, typed); (b) series collapse at the
committed $4.99 Night Kiln: m\*(3, 3/4) = 18463/11271 ≈ 1.6381 ≤ 2 and
m\*(3, 9/10) ≈ 1.4979 ≤ 2, the disclosed thin cell m\*(3, 1/2) =
2.0608 one grid step above the line exactly as registered; (c) the
committed 36-book Peridale comp: m\*(36, 3/4) ≈ 1.2907 ≤ 4/3. Border
jump exactly 2; forbidden-band width == its own lower edge (2093/2000);
V049 anchor re-derived; KU named cell 5489/3691; knife-edge margin
exactly 91/90810 reproduced and EXCLUDED from every clause (enumerated
evaluator inputs). All three Arm-R previews reproduced EXACTLY under
the registered grammar (80607.96 / 80618.44 / 81041.09 vs exact mean
80775.625). Census recount from pinned copies: 26/2/1 exact, file
lists exact.

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 090, sim-lab
PR #161): a clean landing whose conventions this slice consumed
wholesale — born-red card / collision-grep / no-claim-file /
fresh-clone / registered-allocation-as-seed-set carried verbatim, the
+13 offset extended to its fifteenth row with the same grammar, V086's
typed margin ledger and V087's structured anomaly census reused on a
head whose registered contacts are equalities and thin cells. The
decisive carry: **V090's 💡 (compare exact rationals as VALUES, never
as spellings) was applied in this slice's fixture design ([num, den]
value pairs throughout) and VINDICATED on its first outing** — the
P078 registration spells m\*_∞(9/10) as the UNREDUCED 69860/63567
while this sim's Fraction renders the reduced 9980/9081, and the
value-pair comparison matched them correctly where V090-era string
anchors would have raised a false first-class anomaly and cost a
rehearsal cycle. One structural echo worth naming: V090's Arm-R lesson
chain (V089 💡 → registered draw grammar → zero forensics) held again —
the drafter registered the grammar in one fixture line and all three
previews reproduced on the first run; the convention is now two-for-two
since its introduction. Divergence from V090: that head's arm budget
went to a 214,500-cell lattice census; this head is closed-form light
(< 0.1 s), so the equivalent budget went to the firsthand TEXT-census
fidelity pass at venture-lab 021cba9 (the committed-head pattern) —
which is where this slice's own 💡 was discovered.

💡 **Session idea (genuine, this session):** WHEN A REGISTRATION CLAIMS
A MEASURED TEXT CENSUS, REGISTER THE MATCH GRAMMAR BESIDE THE COUNT —
P078 registers "26 packets / 2 justifications / 1 fork" as census
anchors and even says "the exact file list is in the fixture", but the
idea file never states the MATCHING RULE that produced 26; this slice
had to recover the four match patterns from the drafter's surviving
scratchpad script, and the firsthand sweep showed why that gap is
dangerous: the vetting tree holds 29 packets with impulse-rejection
language in FOUR phrasings — a naive re-measurer would land 29 (or 27,
or a DIFFERENT 26) and either flag a false mismatch or, worse, silently
"confirm" the count over a different file set. A count over committed
text is only reproducible given (universe, normalization, patterns);
without the grammar it is an unverifiable claim wearing a numeral. The
durable upgrade for future drafters: a text-census anchor is a TRIPLE —
one fixture line naming the file universe, the whitespace/normalization
rule, and the exact match patterns — and the sim recounts from pinned
excerpts under that grammar (this verdict's fixtures.json shows the
form). Dedup: V089's 💡 bound unregistered stochastic PROCEDURES (draw
order) to their disclosed outputs and V090's bound rational SPELLINGS
to values — neither covers deterministic text measurement, where the
failure mode is unique (a different grammar can reproduce the same
COUNT over a different SET, which no output comparison catches — only
the file-list contact caught it here); no prior card or verdict states
the census-grammar-as-fixture rule (grepped .sessions/ + sims/ +
control/ at 161aa7f).

📊 Model: fable-5 · high · verdict-sim

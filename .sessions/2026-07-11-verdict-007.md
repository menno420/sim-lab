# Session — VERDICT 007 — games-web-concept-evidence-pass (conformance)

> **Status:** `in-progress`
> 📊 Model: Opus 4.x · 2026-07-11 · VERDICT 007 session
> Objective: settle PROPOSAL 007 (games-web-concept-evidence-pass, idea-engine
> `control/outbox.md` @ d213efa, idea @ e73f225d) — make the authored redirect verdict
> REPRODUCIBLE: fetch + freeze the two committed contracts at pinned SHAs (product-forge
> games-web @ 43563dc, superbot-mineverse @ 2b1bd0b), write + RUN a deterministic
> conformance analyzer that diffs the games-web `games-web.character-sheet` contract
> against the real `mining_snapshot.v1` feed, and land the gated redirect verdict.

## What happened

Born-red card, first commit of the VERDICT 007 session (heartbeat-before-work,
CONVENTIONS §"Heartbeat-before-work"; born-red per §"Born-red session card"). As a
newly-ADDED card it is the substrate-gate's advisory sentinel; the READY PR is expected to
pass green once this card is flipped to `complete` as the deliberate close-out. A bare
local `python3 bootstrap.py check --strict` exits 1 while this card reads `in-progress` —
EXPECTED on a born-red session; REST merge-on-green is PRIMARY (CONVENTIONS).

Ships `sims/verdict-007-games-web-concept-evidence-pass/` — a JUDGMENT-ONLY (value axis)
verdict with a reproducible CONFORMANCE CHECK at its measured core: `analyze.py` loads the
two frozen committed contracts + `stated.json` from `runs/` and DETERMINISTICALLY asserts
the field-level conformance failures (disjoint gear taxonomy, invented stats, absent
rarity/power, dropped fields, transport-envelope conflict), byte-identical output, exit 0.

**Load-bearing result:** redirect — games-web's `games-web.character-sheet` contract is a
hand-authored RPG fiction, not a projection of the decided `mining_snapshot.v1` committed
feed (fm ORDER 012/013). The verdict is `needs-more-evidence` (ruling: redirect); details
in `REPORT.md`.

## Run command

```
python3 sims/verdict-007-games-web-concept-evidence-pass/analyze.py
```
Deterministic, stdlib-only, exit 0 iff every self-check passes. Prints the conformance
diff and the terminal `SELF-CHECKS: n passed, 0 failed` line; the computed findings dict is
proven byte-identical via `determinism_bytes`.

## 💡 Session idea

The **frozen-contract conformance diff** pattern this pass proves is a `harness/` candidate:
for a "does layer B faithfully present source A" question, the reproducible artifact is not a
seed sweep but a deterministic field-set diff of two committed schemas at pinned SHAs —
disjoint-set / subset / identity assertions over the real field names, self-checked. A
reusable "diff-two-frozen-contracts, prove-the-mismatch" helper any future presentation-layer
verdict could vendor.

## ⟲ Previous-session review

VERDICT 006 (numeric sim) proved reproducibility as bit-identity of a single deterministic
engine run. VERDICT 007 sits on the frozen-input rung: the inputs are committed contracts,
not a seedable engine, so reproducibility is a byte-identical analyzer over pinned freezes
(VERDICT 005's redefinition). The discipline carried forward is the same: diff the REAL
committed artifacts at pinned SHAs, never a paraphrase, and report the FULL field set so no
mismatch is cherry-picked.

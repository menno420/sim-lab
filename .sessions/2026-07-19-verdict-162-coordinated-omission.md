# VERDICT 162 — coordinated omission: a closed-loop / synchronous load generator waits for each request to return before issuing the next, so during a service stall it has exactly ONE request outstanding and CANNOT send the requests it was scheduled to send — it silently OMITS the coordinated backlog those requests would have measured, so the reported p99 is set by the MEASUREMENT METHOD, not the system under test

Reproduce PROPOSAL 149 (round-35 FLEET slot, P149 → V162, +13): coordinated omission. One single-server FIFO system is measured two ways over the SAME stall schedule and the SAME per-request service times. The folk belief: "my load generator drives the system at rate λ and records every request's latency, so the p99 it prints IS the tail my users feel; a closed-loop harness (fire a request, wait for the response, fire the next) is a faithful sampler." It is NOT: a closed loop can only ever have ONE request outstanding, so when the system stalls for a duration D it fires one request that blocks for the whole stall and issues NOTHING else — it records ~1 inflated sample and OMITS the ~D/τ requests an open-loop drive at rate λ would have piled behind the stall. Those omitted samples are precisely the ones that measure the tail, and they are missing because the generator's pacing is coordinated with (backs off during) the very stalls it should measure — coordinated omission. The reported tail is therefore biased LOW by orders of magnitude: the closed p99 is set by the fast between-stall path (~sub-ms) while the true open-loop p99 sits deep in the stall tail (~D). An HdrHistogram-style CO-correction (recordValueWithExpectedInterval: for each closed sample L>τ back-fill L−τ, L−2τ, … down to >τ) repopulates the omitted band and lifts the measured tail back toward the open-loop truth. The trap: any latency SLO signed off from a closed-loop benchmark is reading the measurement artifact, not the user experience, and it hides the exact pauses (GC/safepoint/lock-convoy/failover/cron stalls) load tests exist to catch. Three ordered z-gates (each z=mean/se across R=200 replicates, require z≥3): G1 tail-blindness log(open_p99/closed_p99) → G2 omitted-backlog (open−closed stall count) → G3 correction-recovery log(corrected_p99/closed_p99). Model-basis caveat (P024 discipline): the declared choices — server FROZEN for a fixed stall D, low base utilisation, τ=1/λ back-fill — are conservative-DIRECTION (relaxing them deepens or preserves the gap), none flips a gate sign or the order-of-magnitude.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P149 reference verifier byte-identical under its pinned world (SEED=20260717, R=200 replicates, λ=1000 → τ=1 ms, horizon T=1.0 s, base service Exp(mean μ_s=0.1 ms) so utilisation μ_s/τ=0.1, n_stalls=6 stalls of D=50 ms so D/τ=50, N_svc=12000 pre-drawn service times, q=0.99 nearest-rank, z_gate=3.0) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `12d3ce4fce9a4f1cc218be0ee6f5dbc945d42a5e45173a46044704524dbfab5d`, (b) all three gates hold in order G1 tail-blindness → G2 omitted-backlog → G3 correction-recovery, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run assert (byte-identical stdout, exit 0), (d) the headline reproduces (mean open p99 ≈ 48.6 ms vs mean closed p99 ≈ 0.47 ms — a ~104× understatement; ~294.6 excess backlogged requests; CO-correction lifts the closed p99 to ≈ 38.4 ms, recovery fraction ≈ 0.789). Pre-registered decision rule (P149): APPROVE VERDICT 162 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT. Ruling posture: unlike the V159 quota-cliff QUALIFIED downgrade (an ungated regime-dependent G2 SIGN), weigh whether the declared model choices here are conservative-direction only — if none flips a gate sign or the order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 162, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/coordinated_omission.py` — file sha256 `6780d06a213ad27d218331447bb922898a1eb8aa716396bf343590c16eda762c`, git blob `d276fb5047473a5bc569f8747c1dd7a04311bbc5`, **12310** bytes, 323 lines. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/d615489/ideas/fleet/coordinated_omission.py (idea-engine `d615489`, PROPOSAL 149 landed via idea-engine #602; blob at that commit == `d276fb50…`, verified `git rev-parse d615489:ideas/fleet/coordinated_omission.py`).
- Offset authority: PROPOSAL 149 → VERDICT 162 (+13), round-35 FLEET slot — the successor above the round-34 verdict quad (FLEET V158 / VENTURE V159 / GAME V160 / UNRELATED V161). Per idea-engine `control/outbox.md` P149 block + `control/status.md`.
- Domain reference: coordinated omission — Gil Tene (HdrHistogram author), "How NOT to Measure Latency"; the CO-correction is HdrHistogram's `recordValueWithExpectedInterval(value, expectedInterval)`, which synthesises the samples an evenly-paced (coordinated) load would have recorded during a stall.
- Disclosed digest: compact-canonical results-dict sha256 `12d3ce4fce9a4f1cc218be0ee6f5dbc945d42a5e45173a46044704524dbfab5d` (P149 outbox done-when line).
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `run()` returns a dict carrying NO `results_sha256` field; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode()).hexdigest()`, asserts an in-process double-run byte-identity, prints the three gate lines + recovery line + the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>`, and writes NO JSON to disk; every float rounded to 6 dp. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump (a distinct serialization of the same dict) — recompute over the compact form.
- sim-lab work branch cut from origin/main HEAD `685b8d3` (VERDICT 159 landed, sim-lab #235). CONTIGUITY: V162 (P149, round-35 FLEET slot) is the successor above the round-34 quad (V158–V161) — it advances the verdict high-water to **V162** (union-max, no regress); V137/P124 and V132/P119 remain open below (landing V162 does not close them).

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier copied byte-identical (`diff` exit 0, sha256 match); pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). Both loops (open and closed) run on the byte-identical server, stall schedule, and per-replicate service-time list (N_svc=12000, drawn once per replicate from `random.Random(SEED+i)`); only the pacing (fixed-schedule arrivals vs one-outstanding synchronous dispatch) differs — so the reported-tail gap is attributable to the MEASUREMENT METHOD alone, not to any difference in the system or its inputs. The gates are definitional contrasts on those shared inputs, not a comparison of two different systems.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 tail-blindness (z ≥ 3, one-sided):** z = mean( log(open_p99 / closed_p99) ) / se — the closed-loop p99 understates the open-loop p99. Disclosed mean **+4.643332**, se **0.001853**, z **+2505.733369**.
- **G2 omitted-backlog (z ≥ 3, one-sided):** z = mean( open_stall_count − closed_stall_count ) / se — the open loop carries ~D/τ ≈ 50 backlogged requests per stall that the closed loop omits (~1 per stall). Disclosed mean **+294.635**, se **0.05723**, z **+5148.278412**.
- **G3 correction-recovery (z ≥ 3, one-sided):** z = mean( log(corrected_p99 / closed_p99) ) / se — the HdrHistogram-style CO-correction lifts the measured tail toward the truth. Disclosed mean **+4.408601**, se **0.001691**, z **+2606.670555**. Descriptive companion: mean recovery fraction (corrected−closed)/(open−closed) = **0.788774**.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `12d3ce4f…dbfab5d` EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (not the pretty indent=2 dump — the P127+ STDOUT-ONLY twist)? State the reproduced digest.
**2.** Is the verifier byte-identical to the P149 source at idea-engine `d615489` (`diff` exit 0, file sha256 `6780d06a…762c`, git blob `d276fb50…`, 12310 bytes, 323 lines)? State the exit codes.
**3.** Do all three gates PASS in the pre-registered order G1 → G2 → G3 with the disclosed statistics (G1 z=+2505.733369 / G2 z=+5148.278412 / G3 z=+2606.670555, all ≥ 3σ one-sided), all_pass=true, first_failing_gate=null, exit 0?
**4.** Is the headline read correctly — the closed loop reports p99 ≈ 0.47 ms while the true open-loop p99 is ≈ 48.6 ms, a ~104× understatement (mean_open_over_closed_p99 = 103.855766) produced by PACING ALONE on a byte-identical server / stall schedule / service-time list — and NOT as "the two loops measure different systems"? Both loops share the same inputs; only the issue-timing differs.
**5.** Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION (a clean APPROVE, not a V159-style QUALIFIED)? The declared choices — server FROZEN for a fixed stall D (relaxing to a load-dependent slowdown only ENLARGES the open backlog, deepening the gap), low base utilisation (raising it lifts the closed baseline but the open-vs-closed p99 gap persists as long as D ≫ τ), and the τ=1/λ back-fill (exactly HdrHistogram's expected-interval) — none flips a gate SIGN or the order-of-magnitude, unlike the V159 quota-cliff G2 sign that inverts between accelerator and ratchet regimes.
**6.** Could the gap be a seed fluke? 200 independent replicates; the tail-blindness log-ratio clears at z ≈ 2506σ (se ≈ 0.0019) and the omitted-backlog difference at z ≈ 5148σ — not a fluke.

## Outcome — APPROVE (exact reproduction, clean)
- Verifier copied **byte-identical** from idea-engine `ideas/fleet/coordinated_omission.py` at `d615489` — `diff` exit **0**, file sha256 `6780d06a213ad27d218331447bb922898a1eb8aa716396bf343590c16eda762c` (12310 bytes, 323 lines, git blob `d276fb5047473a5bc569f8747c1dd7a04311bbc5`).
- Reproduced digest `12d3ce4fce9a4f1cc218be0ee6f5dbc945d42a5e45173a46044704524dbfab5d` **== disclosed digest EXACTLY (MATCH, all 64 hex)**. A fresh cross-invocation double-run printed byte-identical stdout (`cmp` exit 0) with this digest, both exit 0; the script's own in-process double-run determinism assert passed; the byte-identical COPY also printed the disclosed digest at exit 0. The digest is computed over the COMPACT-canonical serialization (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture, P127+ twist; floats rounded 6 dp; the pretty indent=2 stdout dump is a distinct serialization of the same dict, not the digest preimage).
- **G1 tail-blindness: PASS** — mean log(open_p99/closed_p99) **+4.643332**, se **0.001853**, z **+2505.733369** (≥ 3σ one-sided).
- **G2 omitted-backlog: PASS** — mean(open−closed stall count) **+294.635**, se **0.05723**, z **+5148.278412** (≥ 3σ one-sided).
- **G3 correction-recovery: PASS** — mean log(corrected_p99/closed_p99) **+4.408601**, se **0.001691**, z **+2606.670555** (≥ 3σ one-sided); recovery fraction **0.788774**.
- Headline reproduced: mean_open_p99 **0.048583** s vs mean_closed_p99 **0.000468** s — a ~104× understatement (mean_open_over_closed_p99 **103.855766**) from pacing alone; mean_open_stall_count **300.635** vs mean_closed_stall_count **6.0** (excess **294.635** ≈ 6 stalls × ~50 = D/τ coordinated backlog); the CO-correction lifts the measured p99 to mean_corrected_p99 **0.03842** s (recovery **0.788774** of the closed→open gap). seed **20260717**, z_gate **3.0**, all_pass **true**, first_failing_gate **null**, exit **0** on every invocation.
- **Clean APPROVE (no qualification):** the reproduction is EXACT and all three gates pass in order, and — unlike the V159 quota-cliff QUALIFIED, where the G2 autocorrelation SIGN was regime-dependent (accelerator vs ratchet) and the verifier shipped no control world gating it — the model-basis choices here are conservative-direction only: the FROZEN-server abstraction understates the open backlog (relaxing it deepens the gap), low utilisation is exactly what makes the closed loop's few inflated samples fall above its own p99 (the gap persists for any D ≫ τ), and the τ=1/λ back-fill is exactly HdrHistogram's expected-interval method — none flips a gate sign or the order-of-magnitude. Both loops ran on the byte-identical server, stall schedule, and service-time list, so the ~104× tail understatement is attributable to the MEASUREMENT METHOD alone. Verdict: **APPROVE**. The final flip to `complete` is the deliberate LAST commit that releases the born-red HOLD; landing via merge-on-green, zero agent merge calls. Full probe evidence: `sims/verdict-162-coordinated-omission/probe-report.md`.

## ⟲ Previous-session review
Prior card `2026-07-18-verdict-159-quota-cliff-bunching.md` landed V159 (P146 quota-attainment cliff bunching, round-34 VENTURE slot, sim-lab #235 @685b8d3) as a **QUALIFIED-APPROVE** — exact digest `90e5ea44…` reproduced across three fresh invocations, all three gates PASS in order, but the disclosed LOAD-BEARING no-ratchet accelerator choice left the G2 spurious-negative-autocorrelation SIGN regime-dependent (a quota-ratchet plan flips the over-quota incentive to defer/sandbag → sign toward positive/smoothing) and the verifier shipped no ratchet-control world gating it → QUALIFIED, not full APPROVE. DIGEST-POSTURE carry-forward: V159 and this V162 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY with the P127+ twist (results dict carries no `results_sha256`, no file written, the compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form). Object shift: V159 was a VENTURE-domain behavioral-relocation object whose headline SIGN was regime-contingent; this V162 is a FLEET-domain performance-MEASUREMENT artifact whose declared model choices are conservative-DIRECTION only (frozen server / low utilisation / τ=1/λ back-fill all deepen or preserve the gap, none flips a sign) — so where V159 earned a QUALIFIED for an ungated regime-dependent sign, this V162 earns a CLEAN APPROVE: the ~104× understatement rests on pacing alone over byte-identical inputs, and the gates are definitional contrasts, not an unchecked simulator. CONTIGUITY note for the successor: V162 is the round-35 FLEET-slot successor above the round-34 quad (V158–V161) — it advances the verdict high-water to V162, but V137/P124 and V132/P119 remain open below (do not read "V162 landed" as closing them). Coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V162 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the CO-correction to a SINGLE known expected interval τ = 1/λ and drives the open-loop ground truth on a DETERMINISTIC fixed schedule (arrivals exactly at k·τ). A genuinely distinct next FLEET-slot slice — correction-interval-misspecification — pins the case the HdrHistogram back-fill quietly assumes away: real load rarely arrives on a perfectly even clock. Pre-register (SEED-pinned): drive the open-loop ground truth with POISSON arrivals (inter-arrival ~ Exp(λ), same mean rate) instead of the fixed schedule, keep the closed loop and the τ=1/λ back-fill unchanged, and gate (G-a) the closed loop STILL understates the Poisson open-loop p99 by ≥ 3σ (the omission mechanism survives arrival jitter — the artifact is not an artifact of the even schedule); (G-b) a correction-BIAS headline — the fixed-τ back-fill leaves a residual gap to the Poisson open-loop p99 that is ≥ 3σ different from zero (a constant-interval reconstruction is itself biased when the true pacing is not constant-interval); (G-c) an interval-estimator control — back-filling at the EMPIRICALLY measured mean inter-arrival rather than the nominal 1/λ shrinks that residual by ≥ 3σ. The operator lesson sharpens from "correct for coordinated omission" to "the back-fill's expected-interval must MATCH the true pacing law — feed it the wrong interval and it trades an omission bias for a reconstruction bias." Distinct from this V162 head (which pins τ known-and-constant and asks only whether the correction lifts the tail) and from the P149 doc's own transferable rule (measure open-loop OR correct); the new question is how WRONG the standard correction is when its one free parameter is misspecified — with an exact reference (the Poisson open-loop truth) at every interval-estimator grid point.

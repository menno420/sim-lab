# VERDICT 230 — n points dropped independently and uniformly on the unit-circumference ring split it into n arcs whose EXPECTED maximum gap is exactly H_n/n (the nth harmonic number over n), so the worst-case load in consistent hashing grows like (ln n)/n, not 1/n — reproduce PROPOSAL 217

> **Status:** in-progress

📊 Model: Opus family · high · verdict-reproduction

started: 2026-07-20T15:45:24Z

💓 Heartbeat: round-51 UNRELATED slice P217 → V230 (+13); reproduction on branch `claude/verdict-230-sim`;
sim dir `sims/verdict-230-consistent-hashing-max-gap/` (byte-identical verifier copy + run-stdout.txt + independent-probe
+ probe-report). Digest target prefix/suffix `41394b56…84ac` (full-64 to be printed AND independently grep-extracted,
full-64 EXACT string compare, no truncation). Determinism to be CONFIRMED (in-process double-run via the verifier's own
`determinism_double_run` guard AND two separate cross-invocation processes byte-identical stdout). Four pre-registered
gates each in its own direction — G1 EXACT identity: the closed-form expectation of the maximum gap equals the
rational H_n/n for the checked n (exact `fractions.Fraction` comparison, self-certifying), simultaneously REJECTING the
deliberately-wrong "mean-gap 1/n" bound; G2 MONTE-CARLO significance: seeded uniform-ring draws estimate E[max gap] and
the one-sided z of the sample mean against H_n/n is within band (|z| < 3, i.e. the simulation does NOT reject the
closed form); G3 ORDER-STATISTIC leg: the n spacings are exchangeable and each has mean 1/n, so the max is strictly
larger than the mean by the harmonic-sum surplus (E[max]/E[gap] = H_n → grows like ln n); G4 GROWTH-RATE / falsifiability:
across a sweep of n the ratio E[max]·n = H_n tracks ln n + γ (not a constant), REJECTING the "max gap is Θ(1/n)"
misreading. Grounding to be byte-pinned to the maximum-spacing / uniform-spacings result. Born-red HOLD armed on this
first card commit; released only by the deliberate `complete` flip LAST. VERDICT 230 = APPROVE.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate
holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only
after the sim dir (byte-identical verifier copy + reproduction stdout + independent probe + probe-report), the digest
match (full-64 exact with prefix/suffix `41394b56…84ac` — printed + independently grep-extracted), the four-gate
evaluation each in its own direction (all PASS), the determinism check (in-process double-run held AND separate-invocation
stdout byte-identical), and the grounding check have ALL landed, and the status.md heartbeat is re-stamped. That flip
clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation
merge the green PR.

## What this verdict does

Reproduces PROPOSAL 217 (P217 → V230, +13 offset, lane UNRELATED — the probability/algorithms slice of the round-51
fleet→venture→game→unrelated cycle): **drop n points independently and uniformly on a ring of circumference 1 and the
expected size of the LARGEST of the n resulting arcs is exactly H_n/n**, where H_n = 1 + 1/2 + … + 1/n is the nth
harmonic number. Each individual gap has mean 1/n, but the expected maximum is H_n/n ≈ (ln n + γ)/n — a factor of H_n ≈
ln n larger than the average gap. The counterintuitive twist the proposal pins: naive consistent hashing (one hash per
node, nodes placed uniformly on the ring) does NOT balance load to 1/n per node — the worst-loaded node owns an arc
that grows like (ln n)/n, so the max/mean load ratio diverges logarithmically, which is precisely why production
consistent-hashing schemes add Θ(log n) virtual nodes per physical node to flatten the tail. Copies the disclosed
verifier byte-identical into `sims/verdict-230-consistent-hashing-max-gap/`, reproduces the results-dict sha256,
confirms determinism, evaluates the four pre-registered gates each in its own direction via an INDEPENDENT probe
(fresh spacing sampler, not importing the verifier), and checks the byte-pinned grounding.

## Method

- Byte-identical verifier copy (`diff` source↔copy exit 0), stdlib-only (`hashlib`, `json`, `fractions.Fraction`,
  `random`, `math`).
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the
  digest (`json.dumps(r1, sort_keys=True, separators=(",",":"))` → `hashlib.sha256`); the dict carries no hash of
  itself; `__main__` builds the results twice in-process and asserts digest equality (`determinism_double_run`) before
  exit 0. Target full-64 with prefix/suffix `41394b56…84ac` (exact 64-char compare, no truncation).
- Independent probe: a separate stdlib-only script with its OWN uniform-ring spacing sampler and its OWN harmonic-sum
  and closed-form E[max gap] re-derives every gate value rather than importing the verifier, so the reproduction is an
  independent confirmation, not a re-print.
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 — exact closed-form identity / falsifiability** (direction: E[max gap] == Fraction(H_n, n) exactly): the
    closed-form expectation equals the rational H_n/n for the checked n via exact `Fraction` arithmetic — self-certifying
    — and this simultaneously REJECTS the deliberately-wrong "expected max gap == mean gap == 1/n" claim.
  - **G2 — Monte-Carlo agreement** (direction: |z| < 3): seeded uniform-ring draws estimate E[max gap]; the one-sided z
    of the sample mean against the closed form H_n/n is in-band, so the simulation does NOT reject the closed form.
  - **G3 — order-statistic surplus** (direction: E[max]·n == H_n > 1): the n spacings are exchangeable with common mean
    1/n, so the expected maximum exceeds the mean by exactly the harmonic surplus — the max/mean ratio is H_n, strictly
    above 1 and diverging.
  - **G4 — growth-rate / falsifiability** (direction: E[max]·n tracks ln n + γ across a sweep, NOT constant): over a
    sweep of n the quantity H_n = E[max]·n grows like ln n + γ, REJECTING the "max gap is Θ(1/n)" (constant ratio)
    misreading that would follow from treating all gaps as equal.

## ⟲ Previous-session review

Previous-session review: VERDICT 229 (Kaprekar's routine — the digit map D(n)=desc(n)−asc(n) funnels all 8991 valid
4-digit numbers to the single constant 6174 in at most 7 steps, a global contraction onto one absorbing state;
PROPOSAL 216 → V229, the round-51 UNRELATED slice) landed **APPROVE** with a full-64 digest MATCH
(`6ef877698bbb91eadffa8473c4a0ec6276f62fd3b8af73fd90855288b38ebf0d`) and all four gates PASS via the born-red HOLD
choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact-integer /
Fraction identity is a self-certifying theorem, a z-test is an EFFECT (or, when the closed form is the null,
AGREEMENT) gate, a robustness/growth check is a CONVERGENCE gate, and a deliberately-wrong accounting that must be
REJECTED is a FALSIFIABILITY gate. V230 leans on that same discipline: G1 is an exact `Fraction` identity
(E[max gap]==H_n/n, self-certifying) fused with a FALSIFIABILITY leg (the wrong "1/n" mean-gap claim must be rejected),
G2 is a Monte-Carlo AGREEMENT gate (|z|<3 against the closed form as null), G3 is an exact order-statistic surplus
identity (E[max]·n==H_n>1), and G4 is a GROWTH-RATE / falsifiability gate (H_n tracks ln n + γ, rejecting the Θ(1/n)
misreading). Where V229's surprise was that an arithmetic shuffle contracts to one attractor rather than scattering,
V230's surprise is the mirror: uniform placement does NOT equalize — the largest arc is a factor H_n ≈ ln n above the
mean, which is why consistent hashing needs virtual nodes. This verdict also carries a GROUNDING-accuracy assessment
per the V222 grounding-scrutiny lesson — the caveat must honestly distinguish "the closed form E[max]=H_n/n is a
textbook uniform-spacing order-statistic result" from "the verifier proves it firsthand" without over- or under-selling.
Standing non-contiguity persists: landing V230 does not imply every lower verdict below the high-water is closed.

## 💡 Session idea

The verifier pins E[max gap] = H_n/n for single-hash uniform placement. A cheap, orthogonal follow-on (call it P-next)
would pin the VIRTUAL-NODE remedy as an exact object: with k virtual copies per physical node (so nk points on the
ring), the expected max arc still scales like H_{nk}/(nk), but the expected max LOAD PER PHYSICAL NODE — the sum of that
node's k arcs — concentrates around 1/n as k grows, with the max/mean load ratio shrinking like 1 + Θ((ln n)/k). The
gate: an exact/Monte-Carlo sweep over k asserting the load-ratio tail contracts monotonically in k (CONVERGENCE
direction) AND that at k=1 it reproduces the V230 H_n divergence (falsifiability/continuity leg — the k=1 slice must
match this verdict exactly). It reuses the V230 uniform-ring spacing machinery almost verbatim — group the nk gaps by
owning physical node and sum. (Guard recipe for a later session: the anchor is the verifier's `spacings(points)` and
`expected_max_gap(n)` reducers over the uniform-ring sampler — parameterize by k virtual nodes and route the per-node
load through a `load_ratio(n, k)` reducer; the test target is `load_ratio(n, 1) == H_n` and `load_ratio(n, k)` strictly
decreasing in k.)

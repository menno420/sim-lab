# Session — kit upgrade: substrate-kit v1.7.0 → v1.15.0 (ORDER 005 item 4)

> **Status:** in-progress
> 📊 Model: Claude Fable (Fable 5 family) · 2026-07-13 · kit-upgrade worker session

Objective: execute ORDER 005 item 4 (`control/inbox.md` @ main 94cdfba, line
207: "Kit upgrade v1.7.0 → v1.15.0 — standing watch from the session-2 close;
no upgrade PR open at sweep") — run the documented substrate-kit upgrade flow
in this repo: fetch the released dist at tag v1.15.0 (kit commit eaf4f23,
`dist/bootstrap.py` sha256 25d22af9…c650e) via the proven git-clone path
(release-asset downloads 403 through the proxy), regenerate `release.json`
beside `bootstrap.py.new` so the flow's sha256 verification actually runs
instead of silently skipping, then `python3 bootstrap.py.new upgrade`.
The upgrade regenerates BOTH kit-owned workflows in place
(`.github/workflows/substrate-gate.yml`, `.github/workflows/
auto-merge-enabler.yml`); the regenerated enabler's branch allowlist is
driven by `substrate.config.json` → `automerge.branch_patterns`, so a
pre-upgrade config commit pins all 11 currently-allowlisted prefixes
(claude/, control/, sim/, harness/, closeout/, fix/, verdict-, intake-,
heartbeat-, finalize-, order-) before the regen — the kit renders a trailing
`*` as `startsWith(github.head_ref, '<prefix>')`, so every prefix is
expressible. No breaking changes v1.7.0→v1.15.0 (every CHANGELOG section in
range stamps breaking=false, state_migration=false). Claim landed first:
idea-engine `control/claims/claude-sim-lab-kit-upgrade-v1150.md` (PR #360)
reserving this branch `claude/kit-upgrade-v1150`. Dispatched worker session
for the coordinator; `control/status.md` and `control/inbox.md` untouched
(the status.md kit line is owned by tonight's housekeeping session — recorded
deviation from the kit checklist's "update status" step). This born-red card
holds the gate red until the final flip.

## What happened

(in progress)

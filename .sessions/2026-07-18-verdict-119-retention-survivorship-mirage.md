# VERDICT 119 — the retention-survivorship mirage: a subscriber base is a STATIC MIXTURE of a loyal segment (share w_A, constant monthly retention r_A) and a churny segment (w_B=1−w_A, constant r_B<r_A) where every customer's hazard is constant for all time and nobody ever gets stickier, yet the OBSERVED cohort month-over-month retention R(t)=(w_A·r_A^{t+1}+w_B·r_B^{t+1})/(w_A·r_A^t+w_B·r_B^t) — a survivor-weighted average R(t)=p_A(t)·r_A+(1−p_A(t))·r_B with the surviving-loyal mix p_A(t) strictly rising to 1 — climbs monotonically with tenure toward max(r_A,r_B) purely because the churny segment burns off first, so a firm that changes NOTHING watches its cohort retention curve rise; reading that rising aggregate as real stickiness/cohort-quality improvement is a survivorship-bias mirage (mover-stayer / unobserved-heterogeneity / shifted-beta-geometric); a VENTURE-domain subscription/SaaS churn-cohort unit-economics mechanism, round-24 VENTURE slot (P106, +13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · low · review/verify

Born in-progress as this session's first commit (born-red HOLD); flips to complete as the deliberate LAST step after the verdict artifacts + heartbeat land. The born-red HOLD keeps the PR red until the reference-verifier reproduction + digest match are on disk and the verdict block is written from that evidence.

## Objective
Reproduce idea-engine PROPOSAL 106 (outbox `## PROPOSAL 106 · 2026-07-18T00:53:58Z · status: sim-ready`, round-24 VENTURE slot), offset +13 (P106 → V119): on the pinned world (N=200000 customers/cohort, T=24 months, TRIALS=400, SEED=20260717, T_LATE=12, W_A=0.5 → N_A=100000/N_B=100000, R_A=0.97, R_B=0.55, LIFT_MIN=0.05), does a stdlib count-level cohort Monte-Carlo — advancing per-segment alive counts by Binomial survival — show (G1) the headline survivorship lift: mean observed lift R_obs(T_LATE)−R_obs(0) ≥ LIFT_MIN=0.05 by ≥3σ, the aggregate cohort retention curve strictly RISES; (G2) the individual-constant control: tenure-pooled within-segment observed retention matches the constant r_A AND r_B within 3σ (|z|<3 for both), proving no customer's hazard rises with tenure so G1's lift is 100% composition/mixing; and (G3) the closed-form anchor MATCH: mean observed R_obs(t) reproduces the mixture formula R_cf(t) at t=0 and t=T_LATE within 3σ (|z|<3 each)? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/venture-lab/retention_survivorship_mirage.py`) VERBATIM under the pinned SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, and confirm the results-dict sha256 reproduces `1dd09b8d06cfbd0155e5822f60f5c1fbf091bf192d4395e503c4f889dd6856d6` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. sim-lab work branch off origin/main HEAD `b9a2be77edeadb170a3a13584f500b3c82e2da74` (the V118 merge). idea-engine PROPOSAL 106 @ `control/outbox.md` source header verbatim `## PROPOSAL 106 · 2026-07-18T00:53:58Z · status: sim-ready` (read at idea-engine origin/main HEAD `36f5a6f`, outbox line 979); reference verifier `ideas/venture-lab/retention_survivorship_mirage.py` present at that HEAD (committed via PR #506), git blob sha `b04a214e4d01801a016fec5b63bc605fc6354e2d` (`git hash-object`), file sha256 `bb95d740b0f06a34dbe0703b6094976bc755028ff916dd8fe06629c84f73a472`, SEED=20260717 pinned in-file (line 47, SEEDLESS discipline). Disclosed canonical results-dict digest `1dd09b8d06cfbd0155e5822f60f5c1fbf091bf192d4395e503c4f889dd6856d6`; disclosed dry-sim landing (SEED-pinned): G1 lift_mean=0.209617 (se 5.2e-05) z=3079.6379σ, G2 pooled_A=0.969995 |z|=0.7328 / pooled_B=0.549955 |z|=0.8081 (both <3), G3 R_obs(0)=0.759975 vs 0.76 |z|=0.5941 / R_obs(12)=0.969593 vs 0.969537 |z|=1.672 (both <3) — all PASS, exit 0, two runs byte-identical. Pinned world: SEED=20260717, N=200000, T=24, TRIALS=400, SIGMA=3.0, T_LATE=12, W_A=0.5, R_A=0.97, R_B=0.55, LIFT_MIN=0.05; closed-form anchors R_cf(0)=0.760000, R_cf(12)=0.969537, lift_cf=0.209537, asymptote max(r_A,r_B)=0.97. Offset authority = idea-engine control/outbox.md per-block offset ledger + control/status.md baton (docs/current-state.md is a SEAT-DORMANT snapshot through V059 and carries NO P106/V119 row) — P106 ↔ V119 confirmed **+13** (predecessor row P105 → V118, +13; verdict high-water V118, proposal high-water P106).

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict enforced (in-process repeat inherent to the seeded run AND cross-invocation: two separate `python3` processes, stdout diff exit 0).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114/V115/V116/V117/V118, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2/G3, evaluated in order at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104/P105 /se convention)
- G1 survivorship lift (headline): mean observed lift R_obs(T_LATE)−R_obs(0) ≥ LIFT_MIN=0.05 by ≥3σ — the aggregate cohort retention curve strictly rises (disclosed z=3079.6379σ).
- G2 individual-constant control: tenure-pooled within-segment observed retention matches the constant r_A AND r_B within 3σ (|z|<3 for BOTH) — no customer's hazard rises with tenure, so G1's lift is 100% composition/mixing (disclosed pooled_A |z|=0.7328, pooled_B |z|=0.8081).
- G3 closed-form anchor MATCH: mean observed R_obs(t) reproduces the mixture formula R_cf(t) at t=0 and t=T_LATE within 3σ (|z|<3 each) — the rise is exactly the survivor mixture (disclosed |z|_t0=0.5941, |z|_late=1.672).
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order; verdict never softened.

## Outcome — pending (born-red HOLD)
Pending: filled from the reproduction evidence as the deliberate last step, when the born-red HOLD is released and Status flips to `complete`.

## ⟲ Previous-session review
Prior loop P105 → V118 (delegation loop collapse in liquid democracy — uniform one-hop delegation makes the delegation graph a random mapping (Flajolet–Odlyzko 1990), so only cyclic O(√N) nodes decide, one accidental weakly-connected component captures ≈0.758N of the vote weight, and the controlling cycle is a sub-5% clique) landed APPROVE — sim-lab PR #191 (merged to main @ b9a2be7), sims/verdict-118-delegation-loop-collapse/; all three gates PASS in order on the /se margin (G1 electorate-collapse z=162.4318σ, G2 power-concentration z=45.7889σ, G3 controlling-clique z=93.4923σ), results-dict sha256 c3e5e6c15bee39165e3ccfb3967135c06df2ccfd60af7d727b918cda47d49ea6 matched the disclosed digest EXACTLY, check --strict exit 0. V118 and V119 share the reference-verifier-reproduction posture (byte-level digest match against the proposal's own committed stdlib verifier), distinct from an independent reimplementation. Carry-forward guard applied directly here: hash the verifier's OWN canonical bytes — the P106 verifier serializes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` and prints `sha256:` over it (main() lines 227–230), and the results dict does NOT carry a self-digest field (the sha256 is a sibling key `{"results":…,"sha256":…}` only in a written file — and note P106's main() PRINTS the digest but the verifier writes NO results.json itself, so this slice writes the `{"results":…,"sha256":…}` file from the printed canonical + digest to mirror the V118 sim-dir layout), so NO field-omission step is needed here — replicate the compact one-line preimage over the results dict as-is, not a reformatted/indented file. V118 heartbeat touched control/status.md (10 lines) but NOT control/outbox.md — the sim-lab outbox lags at the manager (V086–V106 rollover precedent, PARKED); V119 mirrors that precedent exactly (status heartbeat only, no outbox edit). No regressions observed at boot; check --strict clean at b9a2be7.

## 💡 Session idea
The proof pins a TWO-segment mixture with a FIXED spread (r_A=0.97, r_B=0.55, Δr=0.42) and a FIXED 50/50 weight, and shows the observed curve rises by ≈0.21 toward max(r_A,r_B). But the mirage's SIZE is governed jointly by the between-segment hazard spread Δr=r_A−r_B and the mixing weight w_A: the lift R(∞)−R(0)=max(r)−(w_A·r_A+w_B·r_B) is a product-form that VANISHES at both w_A→0 and w_A→1 (a homogeneous base has no survivorship bias) and is maximized at an INTERIOR weight, while growing monotonically in Δr — so the single headline number hides a non-monotonic-in-composition, monotone-in-spread surface. A genuinely different next VENTURE-slice pins BOTH the churny share w_B and the spread Δr as swept axes (w_B ∈ {0.1,0.3,0.5,0.7,0.9} at fixed Δr, then Δr ∈ {0.1,0.2,0.3,0.42} at fixed w_B) over the same SEED-pinned cohort draws and pre-registers two gates: (1) the observed asymptotic lift traces a strictly concave-in-w_B curve whose ARGMAX matches the analytic interior maximizer of max(r)−(w_A·r_A+(1−w_A)·r_B) subject to the survivor-mix dynamics within 3σ (an interior worst-case churny share, NOT the most-churny base); and (2) the lift is strictly INCREASING in Δr at ≥3σ with the observed slope matching d(lift)/d(Δr) from the closed form. That converts the flat "a rising cohort curve is a survivorship mirage" into the actionable "the mirage is WORST at an intermediate churny-segment share (a base that is neither uniformly loyal nor uniformly leaky fakes the most improvement) and scales with the hazard spread — so retention-curve skepticism must be calibrated to a base's measured heterogeneity, peaking for mixed books, not monotone in average churn," a seed-pinnable, ≥3σ-gateable mechanism (interior-maximized composition sensitivity of the survivorship lift) distinct from this slice's fixed-mixture lift result and from the outbox's flat measure-within-segments correction.

📊 Model: opus-4.8 · low · review/verify

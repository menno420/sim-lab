# VERDICT 207 — newsvendor critical fractile (Q* is the critical-fractile quantile, not the mean): reproduce PROPOSAL 194

> **Status:** in-progress

📊 Model: Claude Opus · effort high · task-class verdict-reproduction

started: 2026-07-20T03:34:20Z

Born-red HOLD: this card ships `> **Status:** in-progress` on its FIRST commit
so the substrate born-red gate holds the PR red; it flips to `complete` only as
the deliberate LAST commit, after the byte-identical verifier copy, the
reproduction stdout, the digest match, the in-order gate evaluation, the
determinism checks (in-process double-run + cross-invocation), the probe-report,
and the control/status.md heartbeat all land — that flip clears the HOLD and
releases merge-on-green.

## What this verdict does

Reproduces PROPOSAL 194 (P194 → V207, +13 offset): to maximize expected profit
the newsvendor stocks the critical-fractile QUANTILE of demand, Q* = smallest
integer Q with CDF(Q) >= (p−c)/(p−s), NOT the mean. For a high-margin good the
critical ratio exceeds 1/2 so Q* sits strictly above the mean; for a low-margin
good it sits below; only at critical ratio = 1/2 does Q* coincide with the
median. Copies the disclosed verifier
`ideas/venture-lab/newsvendor-critical-fractile-2026-07-20.py` byte-identical
into `sims/verdict-207-newsvendor-critical-fractile/`, reproduces the
results-dict sha256, confirms determinism, and evaluates the gates.

## Method (in progress)

- Byte-identical verifier copy (diff source-vs-copy must exit 0), stdlib-only
  (hashlib, json, math, random, fractions), SEED 20260717.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the
  compact-canonical results dict's own sha256 IS the `RESULTS_SHA256` digest;
  target `62acbcf6…f131b3e` to be matched across all 64 hex.
- Determinism: in-process `double_run_identical=true` assert + separate
  cross-invocation byte-identical stdout.

## ⟲ Previous-session review

Previous-session review: V206 (Pigou price-of-anarchy 4/3 bound, PROPOSAL 193)
landed complete with digest MATCH and gates PASS via the same born-red HOLD
choreography — `in-progress` first commit, deliberate `complete` flip last. This
V207 slice reuses that flow verbatim; the sim-lab verdict high-water reference is
carried in the idea-engine outbox mirror, not in this repo.

## 💡 Session idea

The verifier pins three economic cases (high/low/mid CR) at a single symmetric
Binomial(40, 1/2) demand. A cheap orthogonal extension: sweep the critical ratio
continuously over (0,1) and plot Q*(CR) as the exact inverse-CDF step function —
it makes the "order-what-you-expect-to-sell folk rule is exactly wrong" claim
visual (the staircase crosses the mean only at CR = 1/2) and locates the margin
threshold at which Q* first exceeds the mean for any given demand distribution.

# Session — repo seed (born-right, dispatch copilot)

> **Status:** `complete`
> 📊 Model: fable-family (Claude Code) · 2026-07-10 ~19:2xZ · superbot round-3 part-4
> dispatch session (its card: superbot `.sessions/2026-07-10-round3-dispatch-4-sim-lab.md`)

## What happened

Seeded `menno420/sim-lab` born-right per the twice-proven fleet recipe (superbot part-4
brief §3; gen2-blueprint §1) — third consumer after idea-engine + product-forge:

- **Kit v1.7.0 adopted + fully engaged**: all 9 interview slots answered with the repo's
  real design values from the founding package (superbot
  `docs/planning/round3-founding-package-simulator-2026-07-10.md`), `render --live`
  (0 unfilled placeholders), mode `guided`, `check --strict` green at seed HEAD.
- **Gate wired**: `.substrate/ci/substrate-gate.yml` → `.github/workflows/` (check name:
  `substrate-gate` — same as product-forge; control-only diffs short-circuit green
  in-job).
- **Lane layer planted**: `README.md` (the lab contract — method ladder, the five
  validity-gate questions verbatim, verdict grammar, harness note), `CONVENTIONS.md`
  (merge-authority grant + the lane's @codex rider), `PLATFORM-LIMITS.md` (incl. the
  Codex-toggle wall), `claims/README.md`, `review-queue.md`, `docs/retro/questions.md`,
  `control/outbox.md` (verdict bus, sole-writer), `control/inbox.md` header extended
  with the INTAKE-pull convention (two appenders, disjoint block types), `sims/` +
  `harness/` skeletons.
- **Not done here, by design**: the walking skeleton (branch → PR → gate → merge) is the
  coordinator's ORDER 000 at boot — this seed is the empty-repo direct push (the one
  push a ruleset can't cover). The Idea Engine's PROPOSAL 001 was verified waiting
  (`status: sim-ready`, target sim-lab, 18:05Z) — the first wake's pull is the Q-0264
  pipeline's end-to-end proof; it was deliberately NOT pre-pulled.

## 💡 Session idea

The verdict grammar's `gate: PASS|FAIL` line invites ceremony (the retro rider names
this). Idea: a tiny `harness/` checker — `python3 harness/gate_lint.py sims/<slug>/` —
that greps the results report for the five gate headings and REFUSES a `finalized`
outbox append while any heading is missing; enforce-don't-exhort, and a natural first
harness slice if the intake queue is ever empty.

## ⟲ Previous-session review

The product-forge seed (same recipe, hours earlier) hit the masked-exit-code slip (`;
echo` after `check --strict` swallowed a red → fixed via its PR #1). Applied here: the
strict check ran as the DIRECT `&&` predecessor of the push, and the seed card + status
landed in the same single commit — nothing rides behind the gate.

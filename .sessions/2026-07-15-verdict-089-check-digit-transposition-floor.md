# Session — VERDICT 089 — the check digit's exact blind spot: no position-wise mod-10 scheme catches every adjacent swap (the floor is exactly 2 of 90 ordered pairs, proven over all 3,628,800 quotient permutations, Luhn attains it on precisely 09↔90), and the ISBN-10 → EAN-13 migration strictly WEAKENED transposition detection (0 → 10/90 adjacent, 0 → 90/90 at distance 2) (idea-engine PROPOSAL 076, the round-15 COMPLETELY-UNRELATED-domain rotation closer, check-digit design; P076 → V089 under the +13 offset)

> **Status:** `complete`
> 📊 Model: fable-class · high · verdict-sim

Objective: produce VERDICT 089 for idea-engine PROPOSAL 076 (the
check-digit transposition floor,
`ideas/fleet/check-digit-transposition-floor-2026-07-15.md`, read at
idea-engine origin/main 828b18e, landed via idea-engine PR #440 —
verified merged at live GitHub this session: merged 2026-07-15T11:37:34Z,
merged_by github-actions[bot], head 8d26e2e; outbox block stamped
2026-07-15T11:32:35Z; grounding pin idea-engine 87bbb39, the dedup-sweep
HEAD — this head is FULLY HERMETIC: every model constant is
invented-but-pinned in the idea file, zero repo/network reads at verdict
time, no external-repo constant to re-verify). One slice, one branch
(`claude/2026-07-15-verdict-089-check-digit-transposition-floor`), one
verdict. NOTE: the P076 drafter session was killed by a container
restart after its flip pushed, so this slice worked with NO briefed
summary — the registration was reconstructed whole from the repo (the
PROPOSAL 076 outbox block + the idea file + the drafter's session card
+ PR #440's body), and it held everything needed. NUMBERING, verified
literally at sim-lab origin/main `17c4558` (VERDICT 088 merged via PR
#157): newest `## VERDICT` header is 088, `## VERDICT 089` and
`## INTAKE 076` collision-grepped clean (outbox + archives +
current-state + sims + .sessions + remote refs — no competing
verdict-089 branch or PR), so idea-engine PROPOSAL 076 → **VERDICT 089**,
the +13 offset's thirteenth row (map-row extension lands in
`docs/current-state.md` this same PR). Worker session; `control/status.md`
and `control/inbox.md` untouched (ledger appended to `control/outbox.md`
only; delivery is the manager's Q-0264 fan-in). No idea-engine claim file
this slice (the V074/V077–V088 precedent — the collision-grep + born-red
card + READY PR bind the number). Seeds: the drafter's registered set
**20261680–683** IS the session seed set (the V077–V088 precedent),
quoted literally from the registration — main 20261680 (Arm R,
N = 20,000), stability 20261681 (N = 8,000), presentation shuffle
20261682 (presentation legs only), aux 20261683 reserved and asserted
never read; the boundary-aware in-tree sweep at origin/main 17c4558
maxes at genuine 20261673 (P075/V088's registered set; the 20261674 hit
is the gap-RANGE prose "20261674–679" in V088's own card, a range
mention, not a seed read), and the digit-strings 20261833 / 202670087 /
2026964142 remain DATA-not-seed per the recorded discrimination rule.
This slice worked from a NEW fresh shallow clone (`sim-lab-v089`) and
pushed from there (the V080–V088 fresh-clone-on-dirty-tree guard,
carried). This card held the substrate gate red deliberately until this
flip (the born-red discipline — the designed hold was the only red this
branch produced itself).

Flagged, decide-and-flag: (a) the live `control/outbox.md` measured
392,257 B pre-append — already above the fleet 200KB rollover threshold;
rolling is a manager-ordered housekeeping op (inbox ORDER 005 item 5
parks it), NOT this verdict slice's scope, and V086–V088 appended under
the same condition — follow-up left to the manager's rollover order.
(b) The P076 registration pins the quotient-reduction spot set by SIZE
only ("the pinned 6-permutation spot set (36 pairs)") — the six
permutations themselves are not enumerated in the idea file (the
drafter's scratchpad died with its session), so this slice chose a
deterministic six (identity · the Luhn fold · rotation +1 · reflection
9−y · ×3 · ×7), disclosed them in fixtures.json, and ledgered the choice
as a VACANCY row in the structured anomaly census, not a match claim.

## What happened

Built `sims/verdict-089-check-digit-transposition-floor/` under the
standing discipline: fixtures.json committed BEFORE the runner (card
fe152f4 → fixtures 3812027 → runner 76fc13e → accepted run 8e25dbf →
control appends + map row ddad0fc → this flip), three-arm: Arm A
seedless exact integer counting, decision-bearing — the COMPLETE
3,628,800-cell quotient census (the pipeline's first exhaustive search
over a permutation space, ~3 s in the unrolled hot loop) with the
Σ-certificate gated symbolically beside its own exhaustive confirmation
(no U = 0 bin), the quotient-reduction lemma on the 36-pair spot set,
the 10 × 10 linear weight grid with the 16-cell multiplicative cross
contact, the ISBN-10 / EAN-13 / ALL-ONES position censuses with the
120/990 full-code pattern counts, the Damm property gates + state
censuses, the three pencil worlds, and the degeneracy controls; Arm B
INDEPENDENTLY-WRITTEN twin — all four length-4 word-level enumerations
(validators written arithmetically, no shared code with Arm A's
algebra) tied through the FOUR typed must-equal contacts
(60 = 3 × 2 × 10 · 300 = 3 × 10 × 10 · 1800 = 2 × 90 × 10 · 0 = 0) with
the ordered-pair multiplicity law asserted directly per boundary and
per pair, plus its OWN reduced quotient census (the π(0) = 0 slice,
362,880 cells, pairwise-comparison U — different algorithm, different
iteration space) gated against the full census via the ×10 shift-orbit
law, and its own digit-level Luhn boundary scans in both orientations;
Arm R seeded identifier careers, REPORTING-ONLY (16-digit Luhn +
13-digit EAN per episode; main 20261680 at N = 20,000 with the 600,000
draw sentinel counted and asserted; stability 20261681 at N = 8,000;
no statistical gate). 179 self-checks, 179 passed, 0 failed; exit 0;
~6 s/run; byte-identical double run (results 4c781e40…, run-stdout
703f865c…, sha256 in REPORT.md); CPython 3.11 asserted; no fix-forwards
after the runner landed — the first complete in-repo run of the
committed pipeline is the accepted run (rehearsed to green on untracked
outputs before the runner commit; the rehearsal needed zero fixes — the
committed runner is byte-identical to the rehearsed one). PR #158
opened READY.

**VERDICT 089 — REJECT, all three registered clauses exact, ZERO
anomalies (structured census: 19 compared-and-matched, 0 mismatched, 3
honest vacancies).** R1: min U = 2 over all 3,628,800 quotients at
exactly 46,400 minimizers, max 90 on exactly the 10 rotations (verified
element-by-element), the disclosed even-only histogram support with its
gaps above 44 reproduced bin-for-bin, Luhn ON the floor with miss set
exactly {(0,9),(9,0)}, linear floor 10. R2: ISBN-10 0/0 at every
distance vs EAN-13 10/90 + 90/90 (120 + 990 full-code escape patterns)
— the strict migration regression. R3: the pinned Damm table passes
rows/columns/diagonal, 0/900 + 0/900 (D2 honesty row 958/9000,
reporting); the 11-ary mini-code 1331/0/0 — both exits real. The
pre-registered census-contact NULL axis (the drafter's disclosed
unordered-count correction 30/150/900) was genuinely reachable and did
NOT fire. The margin ledger's three registered equality cells all
landed exactly (Luhn ×1.00 on the floor by construction; EAN D2
saturated at 90/90 — Arm R agrees, 17966/17966 measured = 1.0; Damm ADJ
at the zero cell). And the slice's one genuine surprise: the seeded
Arm-R preview reproduced EXACTLY (412/17954 · 1948/18026 · 17966/17966
· 600,000 draws) despite the per-episode draw order never having been
registered — the sim-chosen order (disclosed in fixtures.json as a
vacancy) evidently coincides with the drafter's stream.

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 088, sim-lab
PR #157): a clean code-shaped landing whose conventions this hermetic
head consumed wholesale — V087's disclosure-coverage anomaly census
(carried by V088 as fixture convention C11) rode here as the
matched/mismatched/vacant ledger and earned its keep three times over:
this registration carried TWO structurally unregisterable values (the
size-only spot set, the unregistered draw order) and one undisclosed
leg (stability), and each landed as an honest VACANCY row instead of a
fake match; V086's typed margin ledger fit this head's degenerate-margin
regime exactly as the registration predicted (every clause an exact
equality — the ledger names the on-floor/saturated/zero cells rather
than clearances); V088's born-red / collision-grep / no-claim-file /
fresh-clone / registered-allocation-as-seed-set ceremony carried
verbatim and made the numbering mechanical (its continuity note "P076
→ V089 next" was consumed as this slice's baton). Re-verified live from
V088's landing, all held: newest VERDICT header 088 at origin/main
17c4558 (PR #157 squash-merged), its seed ledger (20261670–673 in-tree
exactly where its card says; nothing ≥ 20261680 anywhere before this
branch; its 20261674–679 gap note is why this head's 20261680 allocation
needed zero derivation), and its docs/current-state.md map row (the
twelfth +13 row, correctly formatted — this slice extended it to
thirteen with the same grammar). One divergence from V088 worth naming:
V088's fidelity arm (Arm F) transcribed committed external code, which
a fully hermetic head has none of — this slice spent that arm budget on
a SECOND census implementation instead (the reduced 9!-slice with a
different U algorithm gated ×10 against the full enumeration), keeping
the twin-arm rule meaningful for a theorem-shaped head; that
substitution pattern (fidelity arm ↔ independent re-census, chosen by
whether the head pins external code) is now used twice (V085's
outcome-tree twin, this slice's orbit-slice twin) and is worth carrying
as the hermetic-head default.

💡 **Session idea (genuine, this session):** SEEDED PREVIEWS ARE
ACCIDENTAL REGISTRATIONS — when a drafter disclosed Arm-R preview
COUNTS (412/17954 · 1948/18026 · 17966/17966 · 600,000 draws) but never
registered the per-episode draw ORDER, the counts themselves pin the
procedure: the preview numbers are a checksum over the entire Mersenne
stream consumption, so a verdict session that reproduces them exactly
has provably reconstructed the drafter's unregistered stream order (as
this slice did — first guess: digits-then-position per code, all
randrange), and one that cannot has found a real registration GAP, not
a real disagreement — it should ledger a vacancy-plus-mismatch pair
(order vacant, counts mismatched, cause pinned to stream order) and let
the reporting arm stay reporting. The durable upgrade for future
drafters: register the draw GRAMMAR in one fixture line (per episode:
which draws, what order, which rng method — e.g. "15× randrange(10),
1× randrange(15), 12× randrange(10), 1× randrange(12), 1×
randrange(11)"), which costs a sentence and makes the preview
deterministic instead of forensic. Dedup: V086's 💡 typed the margin
ledger's rows, V087's typed the anomaly census's coverage, V088's typed
the visibility claim's surface denominator — none binds an UNREGISTERED
PROCEDURE to its disclosed outputs; no prior card or verdict states the
preview-as-stream-checksum rule or the draw-grammar fixture line (this
idea was discovered live: the exact preview match was this slice's only
genuine surprise, and the counterfactual — a mismatch with no
registered order to arbitrate — would have cost the next reader a
forensic re-derivation this one sentence prevents).

📊 Model: fable-class · high · verdict-sim

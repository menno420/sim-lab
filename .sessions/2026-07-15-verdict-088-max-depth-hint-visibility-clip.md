# Session — VERDICT 088 — the hint that hides: mineverse's world views treat the OPTIONAL max_depth hint as an unvalidated visibility gate (14 of the 80 validator-accepted (hint × depth × record) lattice cells render a miner NOWHERE in the world views while the same document ranks them #1 on two boards), and the Deep Diver badge's exact-equality law is non-monotone — digging past the hint LOSES it (idea-engine PROPOSAL 075, the round-15 GAME-MECHANICS rotation slot, superbot-mineverse; P075 → V088 under the +13 offset)

> **Status:** `complete`
> 📊 Model: Claude Fable family · high · verdict-sim slice-worker session

Objective: produce VERDICT 088 for idea-engine PROPOSAL 075 (the
max-depth hint visibility clip,
`ideas/superbot-mineverse/max-depth-hint-visibility-clip-2026-07-15.md`,
read at idea-engine origin/main 87bbb39, landed via idea-engine PR #439
merged 2026-07-15T11:09:44Z, main 87bbb39; outbox block stamped
2026-07-15T11:06:24Z; grounding pin superbot-mineverse b9ade33). One
slice, one branch
(`claude/2026-07-15-verdict-088-max-depth-hint-visibility-clip`), one
verdict. NUMBERING, verified literally at sim-lab origin/main `d6610dc`
(VERDICT 087 merged via PR #156): newest `## VERDICT` header is 087,
`## VERDICT 088` and `## INTAKE 075` collision-grepped clean (outbox +
archives + current-state + sims + .sessions + remote refs — no competing
branch or PR), so idea-engine PROPOSAL 075 → **VERDICT 088**, the +13
offset's twelfth row (map-row extension lands in `docs/current-state.md`
this same PR). Worker session; `control/status.md` and `control/inbox.md`
untouched (ledger appended to `control/outbox.md` only; delivery is the
manager's Q-0264 fan-in). This head is fully hermetic at the SIM level
(the runner reads only its own fixtures.json; zero repo/network reads at
verdict time) and its harvest pins carry file:line cites into
menno420/superbot-mineverse @ b9ade33 (schema :24-28, depth/record bounds
+ zero cross-field applicators; views.py :216/:316 band loops, :221-225
per-band elif, :446-448 badge equality, :633-634 absence fallback,
:310-311/:113-114/:155-156 honest-listing doctrine, :245-272 hint-blind
leaderboards; contract table :39 and Ordering :193-197; app.py :90;
ingest.py :25-28; app.js :1173/:1183/:1216-1222; test_views.py :568) —
ALL re-verified FIRSTHAND this slice on a read-only PUBLIC-HTTPS shallow
clone landing exactly at b9ade33 (the Q-0272/P073–P075 transport
precedent; every quoted line matched verbatim, the schema applicator
sweep came back EMPTY, and a scratch rehearsal drove the ACTUAL
committed views.py + snapshot_validation.py over the full 80-cell
lattice + T3 + G5 + all three G6 repair worlds reproducing every
disclosed census — zero harvest anomalies) BEFORE the fixture was
written. Seeds: the drafter's registered set 20261670–673 IS the
session seed set (the V077–V087 precedent); the in-tree seed sweep at
origin/main d6610dc maxes at genuine 20261663 (P074/V087's registered
set, aux, reserved), the gap 20261665–669 stays the drafter's disclosed
in-flight buffer, unused, and the lone in-tree occurrence of the
digit-string 20261664 is a DATA numeral (`0.00202616643`, a float in
verdict-032 results.json), not a seed. This slice worked from a NEW
fresh shallow clone (`sim-lab-v088`) and pushes from there (the
V080–V087 fresh-clone-on-dirty-tree guard, carried). This card held the
substrate gate red deliberately until the flip (the born-red discipline
— the designed hold was the only red this branch produced itself).

## What happened

Built `sims/verdict-088-max-depth-hint-visibility-clip/` under the
standing discipline: fixtures.json committed BEFORE the runner (card
a010289 → fixtures a85e597 → runner 7798ef2 → accepted run 9352492 →
control appends + map row 827fa25), four-arm: Arm A seedless exact
closed forms ((3−m)², 4(3−m), (3−m)(m+1), the four iff-laws, the
20/12/24 badge censuses; decision-bearing); Arm B INDEPENDENTLY-WRITTEN
exhaustive dict-of-cells enumerator (direct predicate scans over
explicitly constructed band/panel index sets — no shared code with A or
F; powers the second decision evaluator); Arm F the committed band
loop, per-band `elif`, minimap band filter, badge equality, and absence
fallback transcribed VERBATIM from the pinned quotes and run over real
document dicts on the pinned sample-derived miner template (equal to
Arm B cell-by-cell on all 80 × 4 outputs, G1); Arm R seeded fully-valid
documents, REPORTING-ONLY (main 20261670 at 10,000 docs / 39,783
miners: invisible 6816 beside exact 14/80, ghost-only 4850 beside
10/80, deep_diver 9946 beside 20/80, minimap-invisible|hint 11666/31862
beside 24/64; stability 20261671 at 4,000 docs; presentation shuffle
20261672; aux 20261673 asserted never read). 553 self-checks, 553
passed, 0 failed; exit 0; ~0.1 s/run; byte-identical double run
(results 71f5d54c…, run-stdout 3fa67933…, sha256 in REPORT.md); CPython
3.11 asserted; no fix-forwards after the runner landed — the first
complete in-repo run of the committed pipeline is the accepted run
(rehearsed to green on untracked outputs before the runner commit; the
one rehearsal fix was a KeyError in the census helper on PARTIAL
lattices — the G6a repair world passes a 46-cell subset — caught before
the runner commit, as the rehearsal discipline intends). PR #157
opened READY.

**VERDICT 088 — REJECT, all three registered clauses exact, ZERO
anomalies (structured census: 29 compared-and-matched, 0 mismatched, 1
honest vacancy).** R1: fully-invisible census 14/80 with per-hint
(3−m)² = {9, 4, 1, 0}, every cell individually verified absent from
`here`, `record_only`, `points`, AND `unplotted`; minimap-invisible 24,
ghost-chip-only 10. R2: deep_diver ⇔ r = m_eff (20/80 earn), 12
non-monotone flip cells, 24 overshoot denials, the m = 0 inversion
confirmed. R3: hint domination on all 64 comparisons with the strict
set EQUAL to the invisible set cell-for-cell — the optional field can
only ever hide. T3 witness reproduced end-to-end (both boards #1, four
world-view surfaces empty, badge inverted, absent-hint triple flip);
G5 pencil world all five facts; G6 repairs 46/0-censuses/0-flips
(APPROVE fires live), 0/80, 44/80-zero-flips with the m_eff = 3
byte-identity — the registered margin-0 thesis: today's committed
sample (m = 3) sits exactly ON the degeneracy cell, which is why the
sample calibration cannot see the fault. Meter rows 150%/300%/falsy-0
landed as reporting.

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 087, sim-lab
PR #156): V087's 💡 — STRUCTURE THE ANOMALY CENSUS BY DISCLOSURE
COVERAGE (compared-and-matched / compared-and-mismatched /
NOT-DISCLOSED) — was applied LIVE this slice as fixture convention C11
and earned its keep immediately: this head repeated V087's third case
exactly (the P075 registration disclosed NO seeded Arm-R counts —
estimand and seeds only), and where V087 had to prototype the
distinction in prose, this run's census is machine-readable in
results.json and prints its coverage line in one stdout row (29 / 0 /
1) — a reader can no longer mistake empty-by-vacancy for
empty-by-match. V086's typed margin ledger rode as C5 and fit this
head unusually well: the registration carried FOUR named knife-edges
((i) last-visible-band contact, (ii) the two-sided badge equality,
(iii) the m_eff = 3 byte-identity — the head's own thesis, (iv) the
falsy-zero meter boundary), and each got gated as its exact two-sided
law rather than flagged as a defect. V084's RAN-vs-DERIVED provenance
was checked at fixture time (P075 tagged every numeral as produced by
its drafting script against the actual committed modules; the
zero-trust re-derivation confirmed all 29 disclosed values). The
V087-recorded public-HTTPS transport note was reused verbatim for the
mineverse clone — the route worked first try, no rediscovery. The
born-red card / collision-grep / no-claim-file reservation, the
registered-allocation-as-session-seed-set convention, and the
fresh-clone-on-dirty-tree guard all rode unchanged. One slice-local
addition worth recording: this head's fidelity leg (Arm F) is a
TRANSCRIPTION of committed code rather than V087's second arithmetic —
its divergence semantics were typed in the fixture BEFORE the runner
(F ≠ B is G1 INVALID; F and B jointly diverging from the registration
is the n2 finding), which kept the twin-arm rule meaningful for a
code-shaped head.

💡 **Session idea (genuine, this session):** TYPE THE VISIBILITY CLAIM
BY SURFACE INVENTORY. This head's decision predicate "fully invisible"
is quantified over an explicit surface set (ladder `here`/`record_only`,
minimap `points`/`unplotted`) — and its own T3 witness proves the
response body carries OTHER surfaces that still list the hidden miner
(both leaderboards, the miner cards, the inventory matrix; that
disagreement IS the finding). The committed response has grown by
ADDITIVE keys before (achievements, hats — the repo's own docstrings
say so), so a future additive surface could silently change what
"invisible everywhere" means without touching any gated law: the gates
would stay green while the claim's scope quietly narrowed. The fix for
future render-clip heads: the fixture should carry the served
response's FULL surface inventory (the top-level view keys at the pin)
with each surface typed lists / omits / not-applicable for the priced
entity, and the sim should gate the claim AGAINST THAT INVENTORY — so
"invisible" certificates state their denominator, and a re-run at a
later pin diffs the inventory FIRST (a new key = a scope decision, not
a silent pass). Dedup: V086's 💡 typed the margin ledger's rows, V087's
typed the anomaly census's coverage — neither types the CLAIM's surface
scope; no prior card or verdict carries a surface-inventory gate
(V088's own fixture pins the four world-view surfaces and the
hint-blind leaderboards as quotes, but does not enumerate the full
response key set as a gated denominator — this idea is exactly that
upgrade, discovered by writing the T3 gate and noticing the
denominator lived in prose, not in a fixture table).

📊 Model: Claude Fable family · high · verdict-sim slice-worker session

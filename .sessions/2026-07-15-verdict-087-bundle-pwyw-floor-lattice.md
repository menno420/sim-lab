# Session — VERDICT 087 — the bundle over a floorless PWYW: the committed Ship-It triple ($49 kit + $19-suggested PWYW pack vs $59 bundle, "saves $9 and one checkout") is lattice-coherent only if the one field the queued ⚑D publish click leaves "owner's choice" is set ≥ $10 — at the floorless default the advertised $9 discount is a $10 strategic surcharge (idea-engine PROPOSAL 074, the round-15 VENTURE rotation slot, PRODUCTS half, venture-lab; P074 → V087 under the +13 offset)

> **Status:** `in-progress`
> 📊 Model: Claude Fable family · high · verdict-sim slice-worker session

Objective: produce VERDICT 087 for idea-engine PROPOSAL 074 (the
bundle-PWYW floor lattice,
`ideas/venture-lab/bundle-pwyw-floor-lattice-2026-07-15.md`, read at
idea-engine origin/main c1b97be, landed via idea-engine PR #438 merged
2026-07-15T10:32:39Z, main c1b97be; outbox block stamped
2026-07-15T10:29:16Z; grounding pin venture-lab 520bdfc). One slice, one
branch (`claude/2026-07-15-verdict-087-bundle-pwyw-floor-lattice`), one
verdict. NUMBERING, verified literally at sim-lab origin/main `9e22abb`
(VERDICT 086 merged via PR #155): newest `## VERDICT` header is 086,
`## VERDICT 087` and `## INTAKE 074` collision-grepped clean (outbox +
archives + current-state + sims + .sessions + remote refs — no competing
branch or PR), so idea-engine PROPOSAL 074 → **VERDICT 087**, the +13
offset's eleventh row (map-row extension lands in `docs/current-state.md`
this same PR). Worker session; `control/status.md` and `control/inbox.md`
untouched (ledger appended to `control/outbox.md` only; delivery is the
manager's Q-0264 fan-in). This head is NOT fully hermetic at the harvest
level: the committed constants carry file:line pins into
menno420/venture-lab @ 520bdfc (membership-kit LISTING.md:46,
template-packs LISTING.md:61, BUNDLE-LISTING.md:56-58/:73-74,
OWNER-QUEUE.md:162/:84, bundle-starter.md §3, photo-packs
MARKET-PLAN.md:17-19) plus five external-anchor numerals into sim-lab's
OWN finalized V040 artifacts — the venture-lab pins ALL re-verified
FIRSTHAND this slice on a read-only PUBLIC-HTTPS shallow clone (the
local git proxy route was reported failing for venture-lab by the P074
drafter; public HTTPS landed exactly at 520bdfc, fetched
2026-07-15T10:37Z, every quoted line matched verbatim, zero harvest
anomalies), and the V040 anchors verified in-tree at origin/main 9e22abb
(fixtures/results/REPORT of `sims/verdict-040-shipit-bundle-pricing/`,
including the G2 sentence at fixtures
`pinned_constants/pwyw_minimum_unpinned`) BEFORE the fixture was
written; the runner itself is hermetic (reads only its own
fixtures.json). Seeds: the drafter's registered set 20261660–663 IS the
session seed set (the V077–V086 precedent); the in-tree seed sweep at
origin/main 9e22abb maxes at genuine 20261653 (P073/V086's registered
set), the gap 20261654–659 stays the disclosed in-flight buffer, unused,
and the lone in-tree occurrence of the digit-string 20261664 is a DATA
numeral (`0.00202616643`, a float in verdict-032 results.json), not a
seed. This slice worked from a NEW fresh shallow clone (`sim-lab-v087`)
and pushes from there (the V080–V086 fresh-clone-on-dirty-tree guard,
carried). This card holds the substrate gate red deliberately until the
flip (the born-red discipline — the designed hold is the only red this
branch produces itself).

## What happened

(in progress — fixtures → runner → accepted run → control appends → flip)

📊 Model: Claude Fable family · high · verdict-sim slice-worker session

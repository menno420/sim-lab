# Session — VERDICT 087 — the bundle over a floorless PWYW: the committed Ship-It triple ($49 kit + $19-suggested PWYW pack vs $59 bundle, "saves $9 and one checkout") is lattice-coherent only if the one field the queued ⚑D publish click leaves "owner's choice" is set ≥ $10 — at the floorless default the advertised $9 discount is a $10 strategic surcharge (idea-engine PROPOSAL 074, the round-15 VENTURE rotation slot, PRODUCTS half, venture-lab; P074 → V087 under the +13 offset)

> **Status:** `complete`
> 📊 Model: Claude Fable family · high · verdict-sim slice-worker session

Objective: produce VERDICT 087 for idea-engine PROPOSAL 074 (the
bundle-PWYW floor lattice,
`ideas/venture-lab/bundle-pwyw-floor-lattice-2026-07-15.md`, read at
idea-engine origin/main c1b97be, landed via idea-engine PR #438 merged
2026-07-15T10:32:39Z, main c1b97be; outbox block stamped
2026-07-15T10:29:16Z; grounding pin venture-lab 520bdfc). One slice, one
branch (`claude/2026-07-15-verdict-087-bundle-pwyw-floor-lattice`), one
verdict. NUMBERING, verified literally at sim-lab origin/main `9e22abb`
(VERDICT 086 merged via PR #155): newest `## VERDICT` header is 086,
`## VERDICT 087` and `## INTAKE 074` collision-grepped clean (outbox +
archives + current-state + sims + .sessions + remote refs — no competing
branch or PR), so idea-engine PROPOSAL 074 → **VERDICT 087**, the +13
offset's eleventh row (map-row extension lands in `docs/current-state.md`
this same PR). Worker session; `control/status.md` and `control/inbox.md`
untouched (ledger appended to `control/outbox.md` only; delivery is the
manager's Q-0264 fan-in). This head is NOT fully hermetic at the harvest
level: the committed constants carry file:line pins into
menno420/venture-lab @ 520bdfc (membership-kit LISTING.md:46,
template-packs LISTING.md:61, BUNDLE-LISTING.md:56-58/:73-74,
OWNER-QUEUE.md:162/:84, bundle-starter.md §3, photo-packs
MARKET-PLAN.md:17-19) plus five external-anchor numerals into sim-lab's
OWN finalized V040 artifacts — the venture-lab pins ALL re-verified
FIRSTHAND this slice on a read-only PUBLIC-HTTPS shallow clone (the
local git proxy route was reported failing for venture-lab by the P074
drafter; public HTTPS landed exactly at 520bdfc, fetched
2026-07-15T10:37Z, every quoted line matched verbatim, zero harvest
anomalies), and the V040 anchors verified in-tree at origin/main 9e22abb
(fixtures/results/REPORT of `sims/verdict-040-shipit-bundle-pricing/`,
including the G2 sentence at fixtures
`pinned_constants/pwyw_minimum_unpinned`) BEFORE the fixture was
written; the runner itself is hermetic (reads only its own
fixtures.json). Seeds: the drafter's registered set 20261660–663 IS the
session seed set (the V077–V086 precedent); the in-tree seed sweep at
origin/main 9e22abb maxes at genuine 20261653 (P073/V086's registered
set), the gap 20261654–659 stays the disclosed in-flight buffer, unused,
and the lone in-tree occurrence of the digit-string 20261664 is a DATA
numeral (`0.00202616643`, a float in verdict-032 results.json), not a
seed. This slice worked from a NEW fresh shallow clone (`sim-lab-v087`)
and pushes from there (the V080–V086 fresh-clone-on-dirty-tree guard,
carried). This card held the substrate gate red deliberately until the
flip (the born-red discipline — the designed hold was the only red this
branch produced itself).

## What happened

Built `sims/verdict-087-bundle-pwyw-floor-lattice/` under the standing
discipline: fixtures.json committed BEFORE the runner (card f760e67 →
fixtures a1b376d → runner 14a0f73 → accepted run 2acbe75 → control
appends + map row b092882), three-arm: Arm A seedless exact-Fraction
closed forms over the full 7-floor × 3-anchor grid (σ_adv, σ_ach, π,
gap, coherence window, f\*, w\*, the affine fee layer;
decision-bearing) + the three structure theorems with 105
perturbed-(a, p) T1 independence controls; Arm B INDEPENDENTLY-WRITTEN
integer-cent twin — path costs by direct min() over enumerated w, w\*
by upward scan, window membership by scanning candidate bundle prices
over every cent, the basis gap by direct subtraction, f\* and the
strict-discount law by upward floor scans, the fee layer in integer
milli-dollars — exact-equal on every number (the second decision
evaluator recomputes from Arm B alone); Arm R seeded buyer-type draws
at the decision cell, REPORTING-ONLY (FLOOR-HEAVY 20261660 at 50,000
episodes → 20002 bundle-take, mean 53.0004 beside exact 2/5 & 53;
SUGGESTED-ANCHORED 20261661 at 20,000 → 16111, mean 57.0555 beside
exact 4/5 & 57; presentation shuffle 20261662; aux 20261663 asserted
never read). 712 self-checks, 712 passed, 0 failed; exit 0; ~0.6 s/run;
byte-identical double run (results cb330b11…, run-stdout 58677f9d…,
sha256 in REPORT.md); CPython 3.11 asserted; no fix-forwards after the
runner landed — the first complete in-repo run of the committed
pipeline is the accepted run (rehearsed to green on untracked outputs
before the runner commit, the prior sessions' dryrun precedent).
PR #156 opened READY.

**VERDICT 087 — REJECT, all three registered clauses exact, ZERO
anomalies.** R1: at the floorless decision cell the coherence window is
the single point {49} ∌ 59 (whole-cent candidate scan returns exactly
one coherent price) and the SAME triple carries σ_adv = +9 (the FAQ
number exactly) and σ_ach = −10. R2: f\*({59, 64, 68}) = {10, 15, 19}
with f\*(68) = 19 = s EXACTLY (the registered margin-0 contact, gated
two-sided must-equal) and f\*(59)/(s/2) = 20/19. R3: f = 10 → σ_ach = 0
exactly; f(d) = 10 + d at {11, 19}; the committed "$9" on the
achievable basis needs f = 19 = s, PWYW extinct. Five V040 external
anchors reproduced exactly in both arms (nets 41879/50589/54944/58428
per mille, the +4/5 second-fee row, the retention bar 50589/54944,
Δ(19) = −7039/1000). Reporting sharpenings landed as registered: the
seller/buyer fork +$9.51 vs −$10 at f = 0; the only f-invariant saving
is the seller's $0.80 second fee; the sibling's committed $3 floor
leaves a $7 premium. APPROVE liveness verified at every f ≥ 10 cell;
granularity non-flipping at 1-cent and 99-cent quanta. One
display-layer nit caught in rehearsal, before the runner commit:
Python's Fraction reduces 54944/1000 → 6868/125, so the registered
anchor numerals are printed over their REGISTERED denominator (the gate
compares exact values either way).

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 086, sim-lab
PR #155): V086's 💡 — TYPE THE MARGIN-LEDGER ROWS, must-equal vs
must-clear — was applied LIVE this slice and earned its keep in both
directions: this head's decision surface carries exactly ONE margin-0
cell, the REGISTERED f\*(68) = s contact (the thesis: V040's banned
anchor coheres only when PWYW is dead ink), and the typed C5 gate held
it as a two-sided must-equal while the naive no-margin-0 rule would
have flagged the head's own finding as a defect; the must-clear rows
surfaced the two thin strict clauses (20/19 and 10/9) as exact
rationals with their clearances stated. V085's argmin-witness rule rode
as well: every extremal claim (f\* as the SMALLEST coherent floor, w\*
as the smallest bundle-weakly-cheaper payment, f(d) as the smallest
strict-discount floor) stated its domain and Arm B re-ran every search
with its own upward scans — every witness matched. V084's
RAN-vs-DERIVED provenance was checked at fixture time: P074 tagged
every numeral as produced by its drafting script, and the zero-trust
re-derivation confirmed all of them. The V085 honest-census discipline
ran and came back EMPTY on everything comparable — which exposed this
session's 💡 (below): for Arm R the drafter disclosed NO seeded counts
(exact expectations only), so the census had nothing to compare on that
leg, and an undifferentiated "zero anomalies" would have quietly
conflated "compared and matched" with "nothing disclosed to compare"
(fixtures C3 states the no-disclosure honestly; REPORT.md names it).
The born-red card / collision-grep / no-claim-file reservation, the
registered-allocation-as-session-seed-set convention, and the
fresh-clone-on-dirty-tree guard all rode unchanged. One slice-local
addition: the venture-lab ground truth was re-verified over PUBLIC
HTTPS after the drafter's disclosed local-git-proxy failure for that
repo — the route note is recorded in fixtures \_grounding so the next
venture-lab slice doesn't rediscover it.

💡 **Session idea (genuine, this session):** GIVE THE ANOMALY CENSUS A
COVERAGE LINE. V085 created the census (compare every disclosed numeral
against the re-derivation; mismatches are first-class anomalies) and
V086 proved the empty census is a legitimate outcome. This slice hit
the third case: a whole reporting leg (seeded Arm R) where the
registration disclosed NO landed values — only exact expectations — so
the census was EMPTY-BY-VACANCY there, not empty-by-match, and the two
emptinesses are epistemically different ("zero anomalies" currently
certifies both). The fix this slice prototyped in prose (fixtures C3 +
the REPORT's census paragraph state "the registration disclosed NO
seeded Arm-R counts … nothing to compare"): make the census output
STRUCTURED — per disclosed-numeral class, report compared-and-matched
(count), compared-and-mismatched (anomalies), and NOT-DISCLOSED
(vacancy), so a future reader can distinguish a registration whose
every numeral survived re-derivation from one that simply disclosed
little; a drafter who discloses less should get a SMALLER census
certificate, not the same "zero anomalies" line. Dedup: V085's 💡
created the census mechanism and V086's typed the margin ledger's rows
— neither types the CENSUS's rows by disclosure coverage, and no prior
card distinguishes empty-by-match from empty-by-vacancy (V086's census
was fully covered: its drafter disclosed the seeded landing; P074's
deliberately did not, which is itself legitimate — expectations are
exact, counts are seed-trivia — but the certificate should say so).

📊 Model: Claude Fable family · high · verdict-sim slice-worker session

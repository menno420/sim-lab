# Session — VERDICT 093 — the 50/50 crowd that wins a third of the time: with ONE shared random source the joint is a design variable the marginals cannot see — the cycle-following coupling lifts 100 "independent" coin flips from 2⁻¹⁰⁰ to exactly 1 − Σ₅₁..₁₀₀ 1/k ≈ 0.3118 (a ≈ 3.953 × 10²⁹× lift, floored above 1 − ln 2 forever) while every player's own odds stay exactly 1/2, failures concentrate (≈ 72.66 of 100 fail together), and of the two folk repairs against an adversary, relabeling is a conjugation NO-OP (0/720 on the 6-cycle) while the one-sided remap restores the full uniform census 276/720 against every pinned arrangement (idea-engine PROPOSAL 080, the round-16 COMPLETELY-UNRELATED rotation closer — correlation design under shared randomness, the sixteenth fleet-external domain; P080 → V093 under the +13 offset)

> **Status:** `in-progress`
> 📊 Model: fable-5 · high · verdict-sim

Objective: produce VERDICT 093 for idea-engine PROPOSAL 080 (the
cycle-following coupling lever,
`ideas/fleet/cycle-following-coupling-lever-2026-07-16.md`, outbox block
`## PROPOSAL 080 · 2026-07-16T02:41:50Z · status: sim-ready`, live in
idea-engine `control/outbox.md`; the seat claim rides idea-engine PR #447,
branch `claude/2026-07-16-v093-pipeline`). One slice, one branch
(`claude/verdict-093-cycle-following-coupling-lever`), one verdict.
NUMBERING, verified literally at sim-lab origin/main `418de3e` (VERDICT
092 merged via PR #163 — the tip IS the V092 merge): newest `## VERDICT`
header is 092, `## VERDICT 093` and `## INTAKE 080` collision-grepped
clean (outbox + archive + current-state + sims + .sessions — the only
hits are the V092 heartbeat/map "next expected: PROPOSAL 080 → VERDICT
093" pointers, which are predictions, not claims; zero open sim-lab PRs,
verified via the GitHub API this session at session start), so
idea-engine PROPOSAL 080 → **VERDICT 093**, the +13 offset's seventeenth
row (map-row extension lands in `docs/current-state.md` this same PR).
Worker session; ledger appended to `control/outbox.md` only —
`control/inbox.md` untouched (manager-order file); this slice also
refreshes the coordinator heartbeat `control/status.md` (routine
next-expected roll to P081 → V094 — the V092 refresh left it current).
No idea-engine claim file written by this slice (the V074/V077–V092
precedent — the collision-grep + born-red card + READY PR bind the
number; the seat claim is the drafter's own PR #447). Seeds: the
drafter's registered set **20261714–717** IS the session seed set (the
V077–V092 precedent), quoted literally from the registration — Arm-R
reporting-only 20261714 (N = 20,000) / 20261715 (N = 8,000) under the
REGISTERED draw-order grammar (one `random.Random` per seed; one
Fisher–Yates permutation per episode, exactly m − 1 = 99 `randrange`
draws, i from 99 down to 1), presentation shuffle 20261716
(presentation legs only), aux 20261717 reserved and asserted never
read; 20261710–713 are P079/V092's registered set — untouched. This is
a PURE-MATH head (kin: the P028/P032/P048/P060/P072/P076 exact-counting
family) — no vendor tree, no external lane pin, zero repo/network reads
at verdict time; every registered numeral is re-derived from scratch by
seedless exact integer/Fraction arithmetic. This card holds the
substrate gate red deliberately until the flip (the born-red discipline
— the designed hold is the only red this branch produces itself).

Flagged, decide-and-flag: (a) the live `control/outbox.md` measured
461,840 B pre-append (measured this session) — above the fleet 200KB rollover threshold; rolling
stays PARKED with the manager (inbox ORDER 005 item 5; the V086–V092
precedent — append normally, follow-up is the manager's). (b) The
registration pins every decision census/identity exactly but the DISPLAY
convention for the irrational-valued anchors (P₅₀, the budget rows, the
ratio/pole/gap/concentration decimals) is sim-chosen: round-half-up on
the exact Fraction at the registered precision — disclosed in
fixtures.json and verified to reproduce every registered decimal
verbatim. (c) The ln-2 bracket is registered as a CONSTRUCTION
(Σ 1/(k·2ᵏ) partial sums + tail bound < 1/((K+1)·2ᴷ)); the truncation
depth K = 60 is the sim-chosen realization, disclosed in fixtures.json
(any K ≥ 9 already separates the gap at 7 decimals; K = 60 makes the
bracket width ≈ 8 × 10⁻²⁰ — vacancy-derived disclosure, not a match
claim). (d) The presentation-shuffle target for seed 20261716 is
registered as "presentation legs only" with no named leg — the
sim-chosen realization (shuffling the display order of the F3 anchor
roster in one reporting-only stdout line) is disclosed in fixtures.json.

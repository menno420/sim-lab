# Session — VERDICT 093 — the 50/50 crowd that wins a third of the time: with ONE shared random source the joint is a design variable the marginals cannot see — the cycle-following coupling lifts 100 "independent" coin flips from 2⁻¹⁰⁰ to exactly 1 − Σ₅₁..₁₀₀ 1/k ≈ 0.3118 (a ≈ 3.953 × 10²⁹× lift, floored above 1 − ln 2 forever) while every player's own odds stay exactly 1/2, failures concentrate (≈ 72.66 of 100 fail together), and of the two folk repairs against an adversary, relabeling is a conjugation NO-OP (0/720 on the 6-cycle) while the one-sided remap restores the full uniform census 276/720 against every pinned arrangement (idea-engine PROPOSAL 080, the round-16 COMPLETELY-UNRELATED rotation closer — correlation design under shared randomness, the sixteenth fleet-external domain; P080 → V093 under the +13 offset)

> **Status:** `complete`
> 📊 Model: fable-5 · high · verdict-sim

Objective: produce VERDICT 093 for idea-engine PROPOSAL 080 (the
cycle-following coupling lever,
`ideas/fleet/cycle-following-coupling-lever-2026-07-16.md`, outbox block
`## PROPOSAL 080 · 2026-07-16T02:41:50Z · status: sim-ready`, live in
idea-engine `control/outbox.md`; the seat claim rides idea-engine PR #447,
branch `claude/2026-07-16-v093-pipeline`). One slice, one branch
(`claude/verdict-093-cycle-following-coupling-lever`), one verdict.
NUMBERING, verified literally at sim-lab origin/main `418de3e` (VERDICT
092 merged via PR #163 — the tip IS the V092 merge): newest `## VERDICT`
header is 092, `## VERDICT 093` and `## INTAKE 080` collision-grepped
clean (outbox + archive + current-state + sims + .sessions — the only
hits are the V092 heartbeat/map "next expected: PROPOSAL 080 → VERDICT
093" pointers, which are predictions, not claims; zero open sim-lab PRs,
verified via the GitHub API this session at session start), so
idea-engine PROPOSAL 080 → **VERDICT 093**, the +13 offset's seventeenth
row (map-row extension lands in `docs/current-state.md` this same PR).
Worker session; ledger appended to `control/outbox.md` only —
`control/inbox.md` untouched (manager-order file); this slice also
refreshes the coordinator heartbeat `control/status.md` (routine
next-expected roll to P081 → V094 — the V092 refresh left it current).
No idea-engine claim file written by this slice (the V074/V077–V092
precedent — the collision-grep + born-red card + READY PR bind the
number; the seat claim is the drafter's own PR #447). Seeds: the
drafter's registered set **20261714–717** IS the session seed set (the
V077–V092 precedent), quoted literally from the registration — Arm-R
reporting-only 20261714 (N = 20,000) / 20261715 (N = 8,000) under the
REGISTERED draw-order grammar (one `random.Random` per seed; one
Fisher–Yates permutation per episode, exactly m − 1 = 99 `randrange`
draws, i from 99 down to 1), presentation shuffle 20261716
(presentation legs only), aux 20261717 reserved and asserted never
read; 20261710–713 are P079/V092's registered set — untouched. This is
a PURE-MATH head (kin: the P028/P032/P048/P060/P072/P076 exact-counting
family) — no vendor tree, no external lane pin, zero repo/network reads
at verdict time; every registered numeral is re-derived from scratch by
seedless exact integer/Fraction arithmetic. This card holds the
substrate gate red deliberately until the flip (the born-red discipline
— the designed hold is the only red this branch produces itself).

Flagged, decide-and-flag: (a) the live `control/outbox.md` measured
461,840 B pre-append (measured this session) — above the fleet 200KB rollover threshold; rolling
stays PARKED with the manager (inbox ORDER 005 item 5; the V086–V092
precedent — append normally, follow-up is the manager's). (b) The
registration pins every decision census/identity exactly but the DISPLAY
convention for the irrational-valued anchors (P₅₀, the budget rows, the
ratio/pole/gap/concentration decimals) is sim-chosen: round-half-up on
the exact Fraction at the registered precision — disclosed in
fixtures.json and verified to reproduce every registered decimal
verbatim. (c) The ln-2 bracket is registered as a CONSTRUCTION
(Σ 1/(k·2ᵏ) partial sums + tail bound < 1/((K+1)·2ᴷ)); the truncation
depth K = 60 is the sim-chosen realization, disclosed in fixtures.json
(any K ≥ 9 already separates the gap at 7 decimals; K = 60 makes the
bracket width ≈ 8 × 10⁻²⁰ — vacancy-derived disclosure, not a match
claim). (d) The presentation-shuffle target for seed 20261716 is
registered as "presentation legs only" with no named leg — the
sim-chosen realization (shuffling the display order of the F3 anchor
roster in one reporting-only stdout line) is disclosed in fixtures.json.

## What happened

Built `sims/verdict-093-cycle-following-coupling-lever/` under the
standing discipline: fixtures.json committed BEFORE the runner (born-red
card 8741b29 → fixtures fd0269b → runner + README 6965879 → accepted run
6982d81 → control appends + map row + heartbeat roll 4fe9964 → this
flip), three-arm: Arm A seedless exact closed forms, DECISION-bearing —
the harmonic law 1 − Σ_{k=b+1}^{m} 1/k, the per-length law m!/k, the
marginal law b/m, the below-n corrections m!/50 and m!/32, the
decrement/alternating identities, the rational ln-2 bracket; Arm B
INDEPENDENTLY-WRITTEN brute enumerations with their own cycle walker and
bookkeeping (24/720/40,320 full loops, fixed-element cycle-length
tables, the 720-σ remap/conjugation loops on all three pinned
arrangements, the same-set/independent-sets pencil enumerations) plus
the cycle-type PARTITION census as the third counting method, tied
through the typed contacts C1–C4; Arm R seeded reporting-only episode
traces under the drafter's REGISTERED draw-order grammar (seeds
20261714/20261715, one Fisher–Yates permutation per episode, exactly 99
`randrange` draws i = 99..1, draw-count sentinels 99N asserted:
1,980,000 / 792,000; presentation shuffle 20261716; aux 20261717 never
read). 61 self-checks, 61 passed, 0 failed; exit 0; ≈ 2.6 s/run;
byte-identical double run (results d6783470…, run-stdout 4ca48a52…,
sha256 in REPORT.md); CPython 3.11 asserted; ONE rehearsal fix (a
fixture-string comparison against a parenthetical-bearing disclosed
value — comparison plumbing only, no decision value touched; see the 💡)
and zero fix-forwards — the first complete in-repo run is the accepted
run. PR #164 opened READY at slice start.

**VERDICT 093 — REJECT, all four registered clauses exact, ZERO
anomalies (structured census: 54 compared-and-matched, 0 mismatched, 6
honest vacancies).** (R1) the C1 three-method counting triangle — brute
10 / 276 / 14,736, partition == brute at (8, 4), partition 1,285,920 ==
10!·893/2520 at m = 10 — with P₅₀ = 0.3118278207 (exact 41/41-digit
fraction), ratio ≈ 3.953 × 10²⁹, poles 2⁻¹⁰⁰ ≈ 7.889 × 10⁻³¹ and
same-set 0; (R2) every enumerated marginal exactly b/m ((m−1)! per
length; 240/360/480 of 720) — the whole 10²⁹ lift is pure dependence;
(R3) 50/T₅₀ ≈ 72.6562 of 100 fail together, decrement exactly
1/((2n+1)(2n+2)) for n = 1..199, P_n > 1 − ln 2 rationally certified
(gap 0.0049750 from both bracket ends; the harmonic shortcut confirmed
FALSE below b = n: 632,736 = 560,160 + 10!/50 and 5,916 = 4,656 +
8!/32); (R4) the adversarial 2n-cycle zeroes the pointer, conjugation is
the cycle-type-frozen no-op {720, 0, 720}, the one-sided remap restores
exactly 276/720 == the uniform census on all three pinned arrangements.
APPROVE did not fire and is arithmetically excluded by C1; both Arm-R
preview triples reproduced EXACTLY ((6,131, 13,869, 1,007,767) /
(2,455, 5,545, 403,116)); disclosed trace mean 72.6633 reproduced.

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 092, sim-lab
PR #163 → 418de3e): a clean landing whose conventions this slice
consumed wholesale — born-red card / READY PR / no-claim-file /
registered-allocation-as-seed-set / typed margin ledger / structured
anomaly census / disclosed sim-chosen realizations carried verbatim, the
+13 offset extended to its seventeenth row in the same grammar. V092's
💡 (the vendored-tree three-guard bytecode rule) is scoped to
manifest-swept vendor drives and correctly did NOT bind here — this head
is pure-math with no vendor tree, the family precedent (P028/P032/P048/
P060/P072/P076) carrying instead; no friction, the idea's scope line
did its job. One V092 side effect DID bind this slice, productively and
with a small bite: its heartbeat/map refresh introduced the standing
"next expected: PROPOSAL 080 → VERDICT 093" pointers, so this session's
numbering collision-grep HIT "VERDICT 093" at the live tip for the
first time in the wave and had to classify the hits before proceeding —
which is where this slice's own 💡 was found. V092's seed baton
(20261710–713, aux never read) made the P080 allocation clean at
20261714 exactly as the drafter registered. Nothing was left pending
for this slice to resolve.

💡 **Session idea (genuine, this session):** THE COLLISION GREP MUST
CLASSIFY HITS — CLAIMS VS PREDICTIONS. Since V092's refresh, the
coordinator heartbeat and the offset map both permanently carry a "next
expected: PROPOSAL nnn+1 → VERDICT NNN+1" pointer, so EVERY future
slice's numbering collision-grep will hit its own verdict number at the
live tip before any work exists — a naive "grep hits ⇒ collision ⇒
abort" rule would deadlock the pipeline on V092's own bookkeeping, and a
naive "ignore doc hits" rule would miss a REAL stale claim. The durable
rule: a hit is DISQUALIFYING iff it is a claim-class surface — a
`## VERDICT NNN` ledger header (outbox or archive), a session card, a
sims/verdict-NNN-* path, or a remote ref/PR — and a next-expected
pointer in control/status.md or the docs/current-state.md map is
PREDICTION-class, expected and non-blocking (this slice's grep hit
exactly two prediction-class lines and zero claim-class ones, recorded
in the INTAKE 080 block). Corollary for drafters of the grep recipe:
sweep with the classification attached, never with a bare count, or the
first hit after every heartbeat refresh becomes a false abort. Dedup:
36 prior cards mention the collision grep but every one reports "clean"
(zero hits); no prior card, README, or REPORT states a hit taxonomy or
the prediction-class carve-out (grepped .sessions/ + sims/ + control/ +
docs/ at 418de3e — "next expected" appears only in V092's own refresh
artifacts and this card).

📊 Model: fable-5 · high · verdict-sim

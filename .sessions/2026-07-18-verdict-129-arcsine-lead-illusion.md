# VERDICT 129 — the arcsine lead-time illusion: play a perfectly FAIR contest as an unbiased ±1 random walk of N_STEPS=500 steps (each step a fair coin, zero edge, zero drift) and measure the fraction of the game one side spends IN THE LEAD (above even) — intuition says the two sides trade the lead evenly so each is ahead ~half the time and the lead-fraction concentrates near 1/2, but the truth is the OPPOSITE (Lévy's first arcsine law): a fair walk is null-recurrent and STICKY about its sign (it returns to zero with probability 1 but the expected inter-return time is infinite), so long one-sided excursions dominate and the fraction of time in the lead follows the U-shaped ARCSINE distribution (density 1/(π√(x(1−x))), piling up at 0 and 1, minimized at 1/2) — the MOST likely outcomes are one side leading almost the WHOLE game while a close see-saw is the RAREST; the exact discrete law (Feller Vol.1 §III.4) labels segment i above the axis iff S_{i−1}+S_i>0 and the count T of above-axis segments is even with P(T=2k)=u_{2k}·u_{2(n−k)}, u_{2j}=C(2j,j)/4^j, whose n→∞ limit is P(fraction≤x)→(2/π)arcsin√x — so a fair game is fair in the MEAN (mean lead-fraction 0.5 exactly) yet its DISTRIBUTION is bimodal at the extremes, and a long one-sided lead is the EXPECTED SIGNATURE of fairness, not proof of an edge; the transferable correction for any close, roughly-fair back-and-forth process (a tied match, a near-parity A/B test, two comparable trading strategies, a cumulative-score race, a running vote margin) is to measure the edge from the DRIFT (net margin per step / terminal gap ± se), never from how long or how lopsidedly one side has led, and to budget stopping rules and confidence to the arcsine tails rather than a naive "it should hover near 50/50"; a probability-theory / stochastic-processes pure-mechanism head grounded in the Lévy–Feller first arcsine law of random-walk sojourn times, round-26 UNRELATED slot (rotation fleet→venture→game→unrelated — closing round-26's verdict rotation), P116 → V129 (+13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · high · review/verify

Born in-progress as this session's FIRST commit (born-red HOLD): this opening commit stages the frame (Objective / Grounding / Gate plan) and the `in-progress` badge is precisely what holds `substrate-gate` red so the PR cannot merge before a deliberate LATER flip. The reproduction artifacts (`sims/verdict-129-arcsine-lead-illusion/`), the written Outcome, and the status heartbeat land in the NEXT commit; the flip to `complete` is the FINAL deliberate step after everything else is committed and `python3 bootstrap.py check --strict` is green. This seat runs the proposal's OWN committed stdlib verifier verbatim and reports the reproduction facts; the ruling is APPROVE only on byte-identical reproduction AND digest exact-match AND all three gates passing in order.

## Objective
Reproduce idea-engine PROPOSAL 116 (outbox `## PROPOSAL 116 · 2026-07-18T04:46:21Z · status: sim-ready`, round-26 UNRELATED-slot closer), offset +13 (P116 → V129): on the pinned world (SEED=20260717, N_STEPS=500 (2n; n=250), N_WALKS=4000, TRIALS=200, SIGMA_GATE=3.0, LEAD_LO=0.10, LEAD_HI=0.90, BAL_LO=0.40, BAL_HI=0.60; thresholds EXT_MIN=0.30 (G1), BAL_MAX=0.20 + GAP_MIN=0.15 (G2); exact discrete-arcsine anchors n=250: extreme-lead P(f≤0.10 or ≥0.90)=0.415569, lower-tail P(f≤0.10)=0.207784, balanced P(0.40≤f≤0.60)=0.130523, mean=0.500000; continuous-limit extreme descriptive 2·(2/π)arcsin√0.1=0.409666), simulate N_WALKS symmetric ±1 walks of N_STEPS steps per trial, count above-axis segments (S_{i−1}+S_i>0) to form each game's lead-fraction, and over TRIALS replications with z on the estimated mean via se=std/√TRIALS show (G1) extreme-lead headline — the measured extreme-lead probability ≥ EXT_MIN=0.30 by ≥3σ (in a fair game one side is ahead almost the whole time far more often than the naive 50/50 picture allows); (G2) fair-mean + middle-is-rare control — (a) mean lead-fraction within 3σ of 0.5 (|z|<3, the walk is genuinely unbiased so G1 is the arcsine shape not a drift) AND (b) balanced probability ≤ BAL_MAX=0.20 AND the (extreme−balanced) gap ≥ GAP_MIN=0.15, both by ≥3σ (the extremes strictly beat the middle, inverting the naive "concentrates at 1/2" belief); (G3) closed-form anchor MATCH — the measured extreme-lead probability reproduces the exact discrete-arcsine anchor 0.415569 within 3σ AND the measured lower-tail reproduces 0.207784 within 3σ (|z|<3 each), so the effect IS the Lévy–Feller arcsine law. Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/fleet/arcsine_lead_illusion.py`) VERBATIM under SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, confirm two runs are byte-identical, and confirm the results-dict sha256 reproduces `4865158a0181815ab420997a10f267f2315f22813696b7d8409b1c2bd16a7b52` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 116 @ `control/outbox.md` source header verbatim `## PROPOSAL 116 · 2026-07-18T04:46:21Z · status: sim-ready` (read at idea-engine main HEAD `9e0861af6ed2743873fb0aac466a3e44be18ddc2`, the PR #530 merge commit that added the verifier); reference verifier `ideas/fleet/arcsine_lead_illusion.py` present at that HEAD, git blob sha `c06d892b515e979244e0f4cb5173a014edbd151e` (`git hash-object`), file sha256 `e94e1c77b423f8838a99eca10ea5f8b60f0e1fd343f82551d0a2617c94f640c9` (`sha256sum`), 220 lines (`wc -l`, 10057 bytes), stdlib-only (random, math, json, hashlib — no numpy/scipy; the exact discrete-arcsine anchors summed from `math.comb`, the continuous (2/π)arcsin√x reported descriptively only), SEED=20260717 pinned in-file. Real source URLs: https://github.com/menno420/idea-engine/pull/530 and blob https://github.com/menno420/idea-engine/blob/9e0861af6ed2743873fb0aac466a3e44be18ddc2/ideas/fleet/arcsine_lead_illusion.py . Grounding idea file `ideas/fleet/arcsine-lead-illusion-2026-07-18.md` (idea-engine@eb76189) + Wikipedia https://en.wikipedia.org/wiki/Arcsine_laws_(Wiener_process) (the proposal's cited anchor). sim-lab work branch `claude/verdict-129-arcsine-lead-illusion` off origin/main HEAD `72783d697eb965a3cb6cffd38fda65faa5cfa3b8`. The verifier is copied BYTE-IDENTICAL into `sims/verdict-129-arcsine-lead-illusion/arcsine_lead_illusion.py` in the reproduction commit on this branch (diff exit 0 vs the idea-engine source + matching `git hash-object` blob `c06d892b…` + matching `sha256sum` `e94e1c77…` — proof recorded in the Outcome section after the run). DIGEST POSTURE = (a) SELF-FIELD: `main()` computes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` then `digest = sha256(canonical)` BEFORE `results["results_sha256"] = digest` is inserted (L207–209), so the disclosed digest `4865158a…` is over the results dict WITHOUT the `results_sha256` key; the on-disk pretty `arcsine_lead_illusion_results.json` (indent=2, sort_keys, WITH the appended `results_sha256`) has its own `sha256sum` that is NOT the disclosed digest. Offset authority = idea-engine control/outbox.md per-block ledger (`depends:` line: "sim-lab consumes at +13 (PROPOSAL 116 → VERDICT 129). Offset pinned") + control/status.md baton ("next: V129 for P116; then round-27 fleet-slot P117") — docs/current-state.md is a SEAT-DORMANT snapshot through V059 and carries NO P116/V129 row. P116 ↔ V129 confirmed +13.

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the results dict / digest enforced (the seeded run is reproduced across two separate `python3` invocations; cross-invocation determinism recorded in the Outcome).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114–V128, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.
- Digest recomputed under the SELF-FIELD posture guard — the results dict is hashed WITHOUT the `results_sha256` key (the verifier inserts it after hashing), matching the disclosed compact-canonical digest, not the on-disk pretty file's own sha256sum.

## Gate plan (proposal labels G1/G2/G3, evaluated in order G1→G2→G3 at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104…P115 /se convention)
- G1 extreme-lead headline: measured extreme-lead probability P(f≤LEAD_LO or f≥LEAD_HI) ≥ EXT_MIN=0.30 by ≥3σ (disclosed dry-sim p_extreme 0.415618, se 0.000512, z=225.9288σ) — in a fair game one side is ahead almost the whole time far more often than the naive 50/50 picture allows.
- G2 fair-mean + middle-is-rare control: (a) mean lead-fraction within 3σ of 0.5 (disclosed mean 0.500315, se 0.000416, |z|=0.7575<3 — the walk is genuinely unbiased) AND (b) balanced probability P(BAL_LO≤f≤BAL_HI) ≤ BAL_MAX=0.20 by ≥3σ (disclosed p_balanced 0.130543, se 0.000353, z_balanced=196.6076σ) AND the (extreme−balanced) gap ≥ GAP_MIN=0.15 by ≥3σ (disclosed gap 0.285075, se 0.000622, z_gap=217.2175σ) — the extremes strictly BEAT the middle, inverting the naive "concentrates at 1/2" belief (the load-bearing control).
- G3 closed-form anchor MATCH: measured extreme-lead probability reproduces the exact discrete-arcsine anchor 0.415569 within 3σ AND the measured lower-tail P(f≤LEAD_LO) reproduces 0.207784 within 3σ (|z|<3 each) (disclosed p_extreme 0.415618 vs 0.415569 |z|=0.0953; p_low 0.207800 vs 0.207784 |z|=0.0356) — reproduces the Lévy–Feller arcsine law itself.
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order (the verifier's `all_pass` field encodes exactly G1 ∧ G2 ∧ G3); verdict never softened.

## Outcome — pending (born-red opening commit)
Reproduction pending. This born-red FIRST commit stages the frame only; the verifier copy + run artifacts (`sims/verdict-129-arcsine-lead-illusion/`), the written verdict, and the status heartbeat land in the next commit, and the card flips to `complete` in the FINAL deliberate step. Until then the `in-progress` badge holds `substrate-gate` red.

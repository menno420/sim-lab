# VERDICT 221 — Pólya urn reinforcement ends uniform: a rich-get-richer urn started at one black + one white finishes on a coin toss. In the classic two-colour Pólya urn (start 1 black + 1 white, each draw replaced with one extra ball of the colour drawn), the black-ball count after n draws is EXACTLY uniform on {1,…,n+1} — every share is equally likely — and the black fraction is a bounded martingale converging to Beta(1,1)=Uniform(0,1); shifting the start to 2 black + 1 white bends the finish to Beta(2,1) with mean 2/3. Reproduce PROPOSAL 208

> **Status:** complete

📊 Model: Opus family · high effort · verdict-reproduction

started: 2026-07-20T11:09:28Z

💓 Heartbeat: round-slot SUPERBOT-GAMES P208 → V221 (+13) reproduction on branch
`claude/verdict-221`; sim dir `sims/verdict-221-polya-urn-uniform/` (byte-identical verifier copy +
reproduction stdout + probe-report), digest target full-64
`d566e380865bfb7089d5042ed7169edfddc7cab7f112af80de6ef2c29c91aa68` (printed AND independently
reproduced byte-identical, MATCH), five gates each in its own direction (G1 EXACT uniform black-count
law max_abs_deviation="0" over n=1..20 vs Beta-binom(1,1); G2 HIGH-z LLN separation z=6605.4714 observed
share-variance 0.0838204573 vs uniform 0.0833333333; G3 LOW-z convergence abs_z=2.11 mc_middle_third
0.3263 vs exact 1/3; G4 SHIFT start (2,1) exact_max_abs_deviation="0" vs Beta-binom(2,1) + mc_mean_share
0.6665581281 z_vs_half=100.9484; G5 determinism in-process double-run IDENTICAL + separate-invocation
diff exit 0), grounding byte-pinned (Wikipedia "Pólya urn model" oldid 1334383855, raw-wikitext sha1
`ac150a6b0ddb3f3d3f8d54b040ce1ed29a0fbbf8`), status.md heartbeat advances verdict high-water to V221.
Born-red HOLD armed on the first card commit; released by the deliberate `complete` flip.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate
born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the
deliberate LAST commit — only after the sim dir (byte-identical verifier copy + reproduction stdout +
probe-report), the digest match (full-64 exact
`d566e380865bfb7089d5042ed7169edfddc7cab7f112af80de6ef2c29c91aa68`, printed + independently reproduced),
the five-gate evaluation each in its own direction (all PASS), the determinism check (in-process
double-run guard held AND separate-invocation diff exit 0), and the grounding check have ALL landed —
that flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session;
CI + the landing automation merge the green PR.

## What this verdict does

Reproduces PROPOSAL 208 (P208 → V221, +13 offset, lane superbot-games): **Pólya urn reinforcement ends
uniform.** In the classic two-colour Pólya urn started at 1 black + 1 white — draw a ball uniformly at
random, return it, and add one MORE ball of the same colour ("the rich get richer") — the number of
black balls after n draws is EXACTLY uniform on {1, 2, …, n+1}: every attainable black-count is equally
likely. Equivalently the black fraction is a bounded martingale that converges to
Beta(1,1) = Uniform(0,1), so the long-run share is a fair coin toss even though each step reinforces the
current leader. Shifting the start to 2 black + 1 white bends the finish to Beta(2,1) (mean 2/3),
confirming the symmetric-start uniformity is the α=β=1 special case, not a coincidence. Copies the
disclosed verifier `ideas/fleet/polya_urn_reinforcement_uniform_finish.py` (idea-engine, commit cc60755)
byte-identical into `sims/verdict-221-polya-urn-uniform/`, reproduces the results-dict sha256, confirms
determinism, and evaluates the five gates each in its own direction against the proposal's OWN criteria.

## Method

- Byte-identical verifier copy (diff source↔copy exit 0), stdlib-only (`hashlib`, `json`, `sys`,
  `random`, `fractions.Fraction`, `math`), SEED = 20260717, N_ENUM_MAX = 20, N_MC = 200, M_MC = 20000,
  START_SYM = (1,1), START_SHIFT = (2,1).
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own
  sha256 IS the digest; target `d566e380865bfb7089d5042ed7169edfddc7cab7f112af80de6ef2c29c91aa68`.
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 EXACT uniform black-count law (symmetric start (1,1), n=1..20; direction: deviation == 0, exact
    Fraction equality)** — the exact-enumeration black-count distribution equals the uniform law
    1/(n+1) == Beta-binomial(n; 1, 1) for every n. PASS iff max_abs_deviation == "0".
  - **G2 reinforcement does NOT wash out to a point mass (M=20000; direction: measured share-variance ≫
    LLN null, large z)** — the Monte-Carlo variance of the terminal black share (0.0838204573) sits far
    ABOVE the law-of-large-numbers null (0.00125) and near the uniform variance 1/12 (0.0833333333):
    z = 6605.4714. PASS iff z is large (the spread is real, not sampling noise).
  - **G3 the terminal share is genuinely Uniform(0,1) (direction: |z| small, convergence)** — the
    Monte-Carlo mass in the middle third [1/3, 2/3] (0.3263) stays within a few σ of the exact uniform
    1/3 (0.3333333333): abs_z = 2.11. PASS iff abs_z < 3 (agreement, not a large effect).
  - **G4 SHIFT start (2,1) bends the finish to Beta(2,1) (direction: exact dev == 0 AND large z_vs_half)**
    — the exact black-count law matches Beta-binomial(n; 2, 1) (exact_max_abs_deviation == "0") and the
    Monte-Carlo mean terminal share (0.6665581281) is ≥ many σ ABOVE 1/2: z_vs_half = 100.9484. PASS iff
    exact deviation == 0 AND z_vs_half large.
  - **G5 determinism (direction: byte-identical)** — an in-process double run is IDENTICAL and two
    separate process invocations diff exit 0. PASS iff both hold.
- Grounding: Wikipedia "Pólya urn model", oldid 1334383855, raw-wikitext sha1
  `ac150a6b0ddb3f3d3f8d54b040ce1ed29a0fbbf8` (byte-pinned). Firsthand-supports the martingale/Beta-limit
  leg via "It is a martingale and converges to the beta distribution when n → ∞", and the symmetric
  x = y = 1 → uniform 1/(n+1) worked example carries the uniform-finish head directly. Fair scope: the
  uniform result IS the on-page worked example of the general-Beta statement, so the symmetric-uniform /
  shifted-Beta split is a FAIR reading of the source, not an over-claim.

## ⟲ Previous-session review

Previous-session review: VERDICT 220 (complete rent dissipation in the all-pay auction — n rivals burn a
total of exactly V independent of n and every bidder breaks even; the symmetric equilibrium
F(b) = (b/V)^(1/(n−1)) collapses Π(b) to exactly 0 via the win-probability identity
F(b)^(n−1) = b/V; PROPOSAL 207 → V220) landed complete with a full-64 digest MATCH
(`f771cd42…b91d70`) and all gates PASS via the born-red HOLD choreography. Its carry-forward was
GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact-0 residual is a self-certifying
theorem, a ≥3σ z is an EFFECT gate, a below-3 max-dev-z is a CONVERGENCE gate. V221 leans on that same
discipline with a five-gate mix spanning BOTH polarities in one slice: G1 and G4's exact-Fraction
black-count identities (max_abs_deviation == "0" is a theorem, any nonzero FAILS), G2's HIGH-z LLN
separation (z=6605.4714 is a large-effect PASS — reinforcement keeps the spread), and G3's LOW-z
convergence (abs_z=2.11 is a small-|z| agreement PASS — the finish really is Uniform(0,1)). The
load-bearing evidence is the exact uniform-law block (G1) plus its shifted-start Beta(2,1) sibling (G4),
with the two Monte-Carlo gates corroborating the martingale limit at scale. Standing non-contiguity
persists: V137 (P124), V132 (P119), and the round-26 FLEET-slot V126 (P113) remain open BELOW the
high-water; landing V221 does not imply every lower verdict is closed.

## 💡 Session idea

The Pólya urn's uniform finish (symmetric start → Beta(1,1)) and its shifted Beta(2,1) sibling are the
two sampled points of a single **Dirichlet-parameter dial**: the terminal black share is exactly
Beta(α, β) where (α, β) is the START composition, so the whole family — not just (1,1) and (2,1) — is
pinned by one exact identity. A cheap orthogonal extension that reuses the exact-enumeration `exact_law`
verbatim would add a deterministic **α,β-sweep gate**: enumerate the black-count law for a grid of
integer starts (e.g. (1,1), (2,1), (1,2), (3,2), (2,3)) and pin, in exact Fraction arithmetic, that each
matches Beta-binomial(n; α, β) with max_abs_deviation == "0" AND that the Monte-Carlo terminal mean lands
at α/(α+β) within a few σ — turning "symmetric ends uniform, (2,1) ends 2/3" from two special cases into
one continuous statement that the reinforcement urn is a Beta(α,β) sampler whose mean is the START
fraction. The digest-bearing dict and the five shipped gates stay byte-identical; only a sibling exact
α,β-sweep gate is added.

# VERDICT 171 — marketplace take-rate / rake hump: in a two-sided marketplace where each seller carries an idiosyncratic on-platform switching cost c (the value of discovery, trust, escrow, payments, dispute resolution) and keeps a transaction on-platform iff the rake it pays is no more than that value, t ≤ c, so the on-platform GMV fraction is the survival curve of the switching-cost distribution S(t)=P(c ≥ t), net platform revenue R(t)=t·S(t)·GMV0 is a HUMP in the rake — it climbs while the extra margin per transaction outweighs the disintermediation leakage, peaks at the interior optimum where the rake elasticity of on-platform GMV hits 1, and STRICTLY FALLS thereafter — so the folk "the take-rate is a pure margin multiplier; each point of rake is skimmed off every transaction, so charge what the market will bear and a higher commission always means more money" belief is FALSE — the rake is a PRICE on a downward-sloping disintermediation demand curve (R = p·Q(p) monopoly pricing), so there is a revenue-optimal interior rake t* and a hard ceiling above it past which revenue is strictly dominated, with closed form t*=C/2, R(t*)=C/4 for switching costs c ~ Uniform[0, C] — but the optimum LOCATION depends on the switching-cost law (a different distribution moves t*), so the interior-optimum reading is a hump-SIGN / order-of-magnitude claim, not a universal fixed-t* one; the aggressive "charge what you can" rake is strictly dominated in SIGN and magnitude, but where the peak sits is a disclosed model-dependent field

Reproduce PROPOSAL 158 (round-37 venture slot, P158 → V171, +13): marketplace take-rate / rake hump / disintermediation. Under the pinned world each of N sellers per trial is drawn an idiosyncratic switching cost c ~ Uniform[0, C_MAX]; a seller keeps its transaction on-platform iff the rake t ≤ c, so the on-platform GMV fraction is the empirical survival curve S(t)=P(c ≥ t) (counted, not plugged), and net platform revenue is R(t)=t·S(t)·GMV0, scored across TRIALS independent trials on between-trial z-scores. The folk belief: "the take-rate is a direct multiplier on revenue — each point of rake is pure margin skimmed off every transaction — so a platform should charge as much as the market will bear, and a higher commission always means more money." It is NOT: the rake is a PRICE on a disintermediation demand curve, so R(t)=t·S(t)·GMV0 is exactly the monopoly-pricing identity R = p·Q(p) on the downward-sloping demand S(t); revenue is therefore a HUMP, not a line — it climbs while the extra margin per transaction outweighs the leakage, peaks where the rake elasticity of on-platform GMV hits 1 (t·f(t)/S(t)=1, f=−S′), and falls thereafter; for c ~ Uniform[0, C] the demand is linear, S(t)=1−t/C, and the revenue-optimal rake is the closed form t*=C/2, R(t*)=C/4, so the aggressive "charge what you can" rake T_MAX is STRICTLY DOMINATED by the interior optimum, and dominated at an accelerating (convex, quadratic-near-the-peak) rate. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 domination-sign (the optimum's revenue strictly exceeds the aggressive rake's — mean(R(t*)−R(T_MAX)) > 0, the hump has the right sign) → G2 relative-effect (overshooting destroys a LARGE fraction of extractable revenue, not a rounding error — mean((R(t*)−R(T_MAX))/R(t*)) ≥ 0.10 against a non-zero null 0.10) → G3 shifted-robustness (the domination survives a lower-loyalty switching-cost re-draw c ~ Uniform[0, C_MAX_SHIFT], where the interior optimum moves LOWER, t*→0.175). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (seller keeps a transaction on-platform iff rake ≤ switching cost; on-platform GMV = GMV0·S(t); revenue = t·S(t)), not a claim about a specific marketplace's numbers — the optimum LOCATION (t*=C/2 for uniform costs) depends on the switching-cost law, so a different distribution moves t*, but the SIGN (revenue is a hump; the aggressive rake is dominated) and the order of magnitude survive the shifted draw (G3); the uniform switching-cost law is a declared linear-demand choice (the cleanest closed-form case), and heavier-tailed loyalty distributions move t* but preserve the hump. The declared choices — the uniform switching-cost law, the fixed C_MAX, the single-period measurement — change neither the SIGN (a strictly dominated aggressive rake) nor the order-of-magnitude of the revenue haircut.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P158 reference verifier byte-identical under its pinned world (SEED=20260717, N=4000 sellers per trial, TRIALS=300 independent trials → between-trial z-scores, base switching costs c ~ Uniform[0, C_MAX=0.50] giving the analytic optimum t*=0.25, aggressive rake T_MAX=0.45, REL_NULL=0.10 as the G2 null, the shifted lower-loyalty re-draw c ~ Uniform[0, C_MAX_SHIFT=0.35] giving t*_shift=0.175, revenue measured empirically by counting sellers with c ≥ t rather than plugged from the closed form, every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `9b8be9fcc3e51afcff0561e43930aa5a38b32f803708c1602f7cf3d69e3b1f43`, (b) all three gates hold in order G1 domination-sign → G2 relative-effect → G3 shifted-robustness, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (net platform revenue R(t)=t·S(t)·GMV0 is a hump, and the interior optimum t*=C/2 strictly dominates the aggressive rake T_MAX). Pre-registered decision rule (P158): APPROVE VERDICT 171 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the optimum LOCATION being model-dependent (t*=C/2 for uniform costs; a different switching-cost law moves t*) is a DISCLOSED descriptive field of a linear-demand claim, not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude of the revenue haircut, a clean APPROVE is warranted. Target: sim-lab (VERDICT 171, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: `ideas/venture-lab/marketplace_take_rate_disintermediation.py` @ idea-engine — sha256 `bcd15da1f16aca466c2771085a4d1ce2ea96a5a4d03d6d431036cb899156384c`, git blob `49c3eaf039d9eaf9cea60720d76c955521e3c873`. The committed P158 source in its venture-lab lane, to be copied byte-identically (`diff` exit 0, copy sha256 == source sha256, copy git blob == source git blob) into the reproduction commit.
- Offset authority: +13 (P158 → V171), round-37 venture slot. Per idea-engine `control/outbox.md` P158 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: Bill Gurley, "A Rake Too Far: Optimal Platform Pricing Strategy" (Above the Crowd) — the canonical statement that high take-rates invite disintermediation and competitive undercut, so the revenue-optimal rake is LOWER than "charge what the market bears" intuition; the mechanism's core (R = p·Q(p) monopoly pricing on a downward-sloping demand curve) is textbook and distribution-independent. Grounding: https://abovethecrowd.com/2013/04/18/a-rake-too-far-optimal-platformpricing-strategy/@live · fetched 2026-07-19 — the source was independently reached LIVE this session (HTTP 200); the proposal's disclosed 404 caveat (P158 recorded the live URL returning 404 with archive.org transiently offline at authoring time) did NOT reproduce.
- Disclosed digest: compact-canonical results-dict sha256 `9b8be9fcc3e51afcff0561e43930aa5a38b32f803708c1602f7cf3d69e3b1f43`, SEED=20260717 — to be reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture. `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the compact canonical JSON + its sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. The disclosed digest is the sha256 of the WHOLE results-dict compact-canonical serialization — recompute over that compact form. Posture to be confirmed byte-exact in the reproduction commit.
- Pinned world: SEED=20260717; N=4000 sellers per trial; TRIALS=300 independent trials → between-trial z-scores; base c ~ Uniform[0, C_MAX=0.50] (analytic optimum t*=0.25); aggressive rake T_MAX=0.45; REL_NULL=0.10 (G2 null); shifted lower-loyalty re-draw c ~ Uniform[0, C_MAX_SHIFT=0.35] (analytic optimum t*_shift=0.175); reseeded per run, redrawn per seller; revenue measured empirically (count sellers with c ≥ t), not plugged; every float rounded to 6 dp before serialization.

## Constraints honored
Stdlib-only (math, json, hashlib, random, statistics); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). Revenue is measured empirically — the on-platform GMV fraction S(t)=P(c ≥ t) is obtained by COUNTING the sellers whose switching cost meets the rake on each per-trial draw, not by plugging the closed form S(t)=1−t/C, so the hump is attributable to the disintermediation mechanism (the exit option) alone, not to a tautological substitution of the analytic curve. The gates are definitional contrasts on those drawn survival counts: G1 domination-sign and G2 relative-effect at the base world (C_MAX=0.50, t*=0.25 vs T_MAX=0.45); G3 re-draws the switching-cost distribution to a lower-loyalty form (C_MAX_SHIFT=0.35, t*_shift=0.175) as a robustness control. Honest scope: the head is EXACT GIVEN THE MODEL and a hump-SIGN / order-of-magnitude claim — it holds for the linear (uniform switching-cost) demand and the optimum LOCATION t*=C/2 is model-dependent (a different switching-cost law moves t*, and under perfect lock-in S(t)≡1 there is no leakage and no hump — the empirically false limit the phenomenon exists to correct); that model-dependence of the peak location is a disclosed descriptive field, and no gate reads the interior optimum as a universal fixed t*.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 domination-sign (z ≥ 3, one-sided):** z = mean(R(t*) − R(T_MAX))/se vs the null 0 — the interior optimum's revenue strictly exceeds the aggressive rake's (the hump has the right sign; the optimum DOMINATES, the aggressive rake does not win). Disclosed gap_mean **+0.080126**, z ≈ **+592.39**, frac_dominant **1.0** (the optimum beats the aggressive rake in every trial) — to be reproduced from the P158 source.
- **G2 relative-effect (z ≥ 3 vs non-zero null 0.10):** z = mean((R(t*) − R(T_MAX))/R(t*) − 0.10)/se — overshooting past the optimum destroys a LARGE fraction of extractable revenue, not a rounding error. Disclosed rel_mean **+0.640656** (≈64% of extractable revenue destroyed by over-raking), z ≈ **+581.51** against the 0.10 magnitude null — to be reproduced.
- **G3 shifted-robustness (z ≥ 3, one-sided):** re-draw the switching-cost distribution to the lower-loyalty form c ~ Uniform[0, 0.35] and re-check mean(R(t*_shift) − R(T_MAX)) > 0 — the domination survives the non-nominal draw (the case where a single-distribution convenience would break), and the interior optimum shifts LOWER, t*→0.175 (< 0.25) as loyalty falls. Disclosed gap_mean **+0.087422**, z ≈ **+1142.57**, frac_dominant **1.0** — to be reproduced.
- z_gate **3.0** (require z ≥ 3 on every gate, one-sided). **Disclosed caveat (NOT a gate):** the optimum LOCATION is model-dependent (t*=C/2 for uniform costs; a heavier-tailed loyalty distribution moves t*), and under the shifted lower-loyalty world the aggressive T_MAX=0.45 rake can capture ZERO (total disintermediation) while the 17.5% optimum still captures the disclosed +0.087422 — that model-dependence of the peak location is a disclosed descriptive field of a linear-demand claim, and NO gate reads the interior optimum as a universal fixed t*.

## Probe questions (independent-audit checklist)
1. Does the results-dict sha256 reproduce the disclosed digest `9b8be9fcc3e51afcff0561e43930aa5a38b32f803708c1602f7cf3d69e3b1f43` EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest. — **YES.** Reproduced digest `9b8be9fcc3e51afcff0561e43930aa5a38b32f803708c1602f7cf3d69e3b1f43` — printed by the script (`json.dumps(a, sort_keys=True, separators=(",",":"))` then sha256), independently recomputed from the stdout JSON block, and identical to the disclosed value. **MATCH.** Two fresh `python3` invocations produced byte-identical stdout (`diff` exit 0), and the script's in-process `assert a == b` held on every run.
2. Is the verifier byte-identical to the P158 source at its pinned idea-engine identity (`diff` exit 0, file sha256 `bcd15da1f16aca466c2771085a4d1ce2ea96a5a4d03d6d431036cb899156384c` / git blob `49c3eaf039d9eaf9cea60720d76c955521e3c873` / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity. — **YES.** `diff` source vs sim-lab copy exit **0** (byte-identical); copy sha256 `bcd15da1f16aca466c2771085a4d1ce2ea96a5a4d03d6d431036cb899156384c` and git blob `49c3eaf039d9eaf9cea60720d76c955521e3c873` both match the pinned P158 identity exactly.
3. Do all three gates PASS in the pre-registered order G1 domination-sign → G2 relative-effect → G3 shifted-robustness with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0? — **YES.** G1 domination-sign gap_mean +0.080126, z +592.387661, frac_dominant 1.0 (null 0) — PASS; G2 relative-effect rel_mean +0.640656, z +581.508639 (null 0.10) — PASS; G3 shifted-robustness gap_mean +0.087422, z +1142.566654, frac_dominant 1.0, optimum t*→0.175 under lower loyalty — PASS. `all_pass=true`, `first_failing_gate=null`, process exit **0**.
4. Is the headline read correctly — net platform revenue R(t)=t·S(t)·GMV0 is a HUMP and the interior optimum t*=C/2 strictly dominates the aggressive rake T_MAX on the SAME empirically-counted per-trial switching-cost draw — and NOT as "a higher commission always means more revenue"? — **YES.** On the base draw the 25% optimum captures r_star_base_mean 0.12507 of GMV0 vs 0.044944 at the aggressive 45% rake (a ~64% haircut for overshooting), with frac_dominant 1.0 — the optimum beats the aggressive rake in every trial. Revenue is a hump; the folk "higher commission always means more revenue" belief is FALSE. Closed-form hand-check agrees exactly (t*=C/2=0.25; R(0.25)=0.25·0.5=0.125; R(0.45)=0.45·0.1=0.045).
5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the optimum LOCATION being model-dependent (t*=C/2 for uniform costs; a different switching-cost law moves t*) flips no gate SIGN and no order-of-magnitude of the revenue haircut? — **YES.** The optimum LOCATION is a disclosed descriptive field: under the lower-loyalty re-draw (C_MAX_SHIFT=0.35) t* moves to 0.175, yet G3 still shows a strictly positive domination gap (+0.087422, z +1142.57, frac_dominant 1.0). The SIGN (revenue is a hump; the aggressive rake is dominated) and the order of magnitude of the haircut survive the shift, so the model-dependence is conservative-direction and descriptive, not a hidden defect — a clean APPROVE.
6. Could the domination sign be a seed fluke? State whether the effect is structural (the hump and the interior optimum t*=C/2 hold across the base world and the lower-loyalty re-draw, frac_dominant 1.0 in both) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical). — **NO fluke; structural.** frac_dominant is 1.0 in BOTH the base world (t*=0.25) and the lower-loyalty re-draw (t*=0.175) — the optimum dominates the aggressive rake in every one of 300 trials in each world, with z far above the 3.0 gate (592.39 / 1142.57), so the sign is structural, not a seed artifact. Determinism confirmed both ways: the script's in-process `assert a == b` held, and two separate `python3` invocations produced byte-identical stdout (`diff` exit 0), both exit 0.

## Outcome
**APPROVE.**

- **Digest MATCH.** The compact-canonical results-dict sha256 reproduced EXACTLY as `9b8be9fcc3e51afcff0561e43930aa5a38b32f803708c1602f7cf3d69e3b1f43` — printed by the script and independently recomputed from stdout (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY), byte-identical across two fresh `python3` invocations and the in-process `assert a == b`. Verifier byte-identical to the P158 source (`diff` exit 0, sha256 `bcd15da1…384c`, git blob `49c3eaf0…3c873`).
- **Three gates PASS in order** (z_gate=3.0): G1 domination-sign gap_mean +0.080126, z +592.39, frac_dominant 1.0 → G2 relative-effect rel_mean +0.640656, z +581.51 (null 0.10) → G3 shifted-robustness gap_mean +0.087422, z +1142.57, frac_dominant 1.0 (optimum t*→0.175 under lower loyalty). `all_pass=true`, `first_failing_gate=null`, exit 0.
- **Closed-form hand-check agrees.** Interior monopoly optimum t*=C/2=0.25; R(t)=t·(C−t)/C gives R(0.25)=0.25·0.5=0.125 (MC r_star_base_mean 0.12507) and R(0.45)=0.45·0.1=0.045 (MC r_max_base_mean 0.044944) — the ~64% haircut (0.640656) is reproduced from first principles. The head reads correctly: net revenue is a HUMP and the aggressive rake is strictly dominated, not "higher commission always means more revenue." The model-dependence of the optimum LOCATION is a disclosed descriptive field that flips no gate sign or order of magnitude (conservative-direction).
- **Source note.** The Gurley "A Rake Too Far: Optimal Platform Pricing Strategy" essay was independently reached LIVE this session (HTTP 200 at https://abovethecrowd.com/2013/04/18/a-rake-too-far-optimal-platformpricing-strategy/), so the proposal's disclosed 404 self-grounding caveat did NOT reproduce and the citation is independently corroborated.

**Verdict: APPROVE.**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-170-bullwhip-effect.md` landed V170 (P157 bullwhip effect / order-variance amplification, round-38 slot, +13) as a **CLEAN APPROVE**: exact compact-canonical digest `b4598624…a49b` reproduced across a fresh cross-invocation double-run, the script's in-process assert, and the byte-identical copy; all three gates PASS in order (G1 amplification-real ratio_mean 2.499097 vs null 1, z +880.40; G2 matches-closed-form 2.5, relerr 0.002767 ≤ 0.05, z +104.49; G3 distribution-free heavy-tailed exponential L=6/p=4 formula 8.5, shift_relerr 0.005070, z +91.81); the amplification rested on the analytic 1 + 2L/p + 2L²/p² over the SAME paired per-trial demand draw, and its one reversal (the softened regime under capacity caps / nonnegative-order clamping / shared information) was self-disclosed AND descriptive. DIGEST-POSTURE carry-forward: V170 and this V171 are BOTH expected WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no file written, the compact-canonical serialization is the digest preimage while the stdout dump is that same compact JSON + its sha256) — carried forward unbroken. Object shift: V170 was a round-38 operations / supply-chain head whose reversal was a softened-regime information-sharing collapse; this V171 is the round-37 venture-lab marketplace unit-economics head — a take-rate / rake-hump / disintermediation claim whose ONE disclosed field (the optimum LOCATION t*=C/2 being model-dependent on the switching-cost law) is likewise self-disclosed and descriptive, so it earns the same clean-APPROVE posture IF the reproduction holds. CONTIGUITY note for the successor: V170 → V171 are contiguous (union-max advances the verdict high-water to **V171**, no regress). Items open/backfilled below the high-water are NOT closed by landing V171 — V137/P124, V132/P119, V126/P113 remain open below high-water. Coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V171 is a separate PR, not this seat's sim-lab close. Born-red note: this V171 card lands `in-progress` FIRST (the HOLD), and the previous-session review, probe answers, and gate-pass assertions are completed only at the deliberate flip commit — a red gate before the flip is the designed HOLD, not a defect.

## 💡 Session idea
The verifier pins a SINGLE switching-cost law (c ~ Uniform[0, C], the cleanest linear-demand / closed-form case where t*=C/2) and gates the hump SIGN plus one lower-loyalty re-draw. A genuinely distinct next slice — the loyalty-elasticity frontier + the switching-cost-reduction lever the head names but holds fixed — pins the mechanism's actual operator lesson: the lever that raises extractable revenue is NOT cranking the rake but SHIFTING the whole S(t) survival curve outward (deepening on-platform lock-in via better discovery, trust, escrow, payments). Pre-register (SEED-pinned, same empirically-counted paired-draw discipline): sweep a family of switching-cost laws (uniform, then a heavier-tailed loyalty distribution and a lock-in-shifted distribution), and gate (G-a) the uniform closed form t*=C/2 reproduces at ≥ 3σ (the base law holds), (G-b) a curve-SHIFT headline — a fixed outward shift of S(t) (raising every seller's switching cost by a constant lock-in premium) raises R(t*) by MORE than any feasible rake increase does at ≥ 3σ (lowering the switching cost, not raising the rake, is the revenue lever), and (G-c) a distribution-family control — the hump SIGN survives across all swept laws at ≥ 3σ while the optimum LOCATION t* moves with the law (the sign is universal, the peak is model-dependent — the exact disclosed field this V171 names). The operator lesson sharpens from "there is a revenue-optimal rake" to "the durable revenue lever is deepening lock-in to shift the disintermediation curve, and the optimum rake FOLLOWS the loyalty distribution rather than the platform's pricing power" — distinct from this V171 head (which pins one law and asks only whether the aggressive rake is dominated) and gives the switching-cost-reduction lever an explicit gate to audit against.

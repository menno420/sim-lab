# VERDICT 136 — hard-close snipe clearing leak

Reproduce PROPOSAL 123 (round-28 GAME slot, P123 → V136, +13): the hard-close snipe clearing leak — the anti-snipe timer is a market-microstructure control, not politeness. A hard-close (fixed-deadline) game auction clears at a SECOND-PRICE over only the bids that TRANSMIT, but last-instant sealed snipes fail to transmit with probability q (server tick / latency / rate limits), thinning the effective field so the second-price clear lands BELOW the true competitive second-highest valuation v_(2). A soft-close (anti-snipe / auto-extend) auction lets the full field respond and clears at v_(2). Two facts the "the anti-snipe timer is just UX politeness" instinct misses: (1) the timer is an ECONOMIC mechanism — the leak=P_soft−P_hard≥0 is a silent value transfer to the lowest-latency (best-connected) client plus an AH-cut gold-sink under-drain; (2) the leak SCALES with the drop rate q and VANISHES at q=0 (where hard-close and soft-close coincide exactly). On the pinned world K=8 bidders with valuations iid Uniform(0,1) the soft-close clears at E[v_(2)]=(K−1)/(K+1)=0.77777778 while the hard-close clears at a transmitted-subset second price, so the leak rises in q: 0.01172271 @q=0.05, 0.03904223 @q*=0.15 (5.022535% of the clear), 0.07347951 @q=0.25 — priced at the tick, the anti-snipe auto-extend restores the full-field clear.

> **Status:** `complete`
> 📊 Model: opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (ORDER 003). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P123 reference verifier byte-identical under its pinned world (SEED=20260717, TRIALS=100000, K=8, SIGMA_GATE=3.0, Q_LEVELS=[0.0, 0.05, 0.15, 0.25]) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest, (b) all three gates hold as disclosed, in order G1→G2→G3, (c) cross-invocation runs are byte-identical. Source header (verbatim): `## PROPOSAL 123 · 2026-07-18T07:20:34Z · status: sim-ready` / target: sim-lab (VERDICT 136, +13 offset).

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/superbot-games/snipe_clearing_leak.py` — git blob `a0d92cc0a2385538955950c29dae1968bf0a29b7`, sha256 `85f489bfb6fdd7a7b18b2b6e8e51a96b36ef70a64cd7585cb98c904ed0872485`, **155** lines / **5663** bytes. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/8b5432ca973819e77cef1b169472825fdf2cd285/ideas/superbot-games/snipe_clearing_leak.py
- Domain reference: Roth & Ockenfels, "Last-Minute Bidding and the Rules for Ending Second-Price Auctions", American Economic Review 92(4), 2002 — the auction-ending-rule / last-minute-bidding law; https://en.wikipedia.org/wiki/Auction_sniping (last-instant bidding under a hard deadline; the anti-snipe auto-extension is the soft-close counterpart) + https://en.wikipedia.org/wiki/Vickrey_auction (the second-price clear = second-highest transmitted valuation).
- sim-lab work branch cut from origin/main HEAD `63bcb2f7e1e534960604a356c82f6a52203c8cb0` (#209).
- DIGEST POSTURE: **NO-SELF-FIELD / COMPACT-STDOUT** (the same posture as the V135 exemplar; DIFFERS from the V133/V134 SELF-FIELD/pretty-on-disk posture). The verifier computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload)`, prints PRETTY JSON (indent=2, sort_keys) then `Results-JSON sha256: <digest>` then the 3 gate lines + `ALL_PASS: True`, and writes NO results.json. The results-dict carries NO `results_sha256` field. Therefore the committed `snipe_clearing_leak_results.json` in the sim dir IS the exact compact canonical bytes that were hashed (no trailing newline, 528 bytes), so re-hashing that file's content reproduces the digest directly — reconciled honestly in the probe report. (One honest divergence from the V135 exemplar: this verifier's stdout is PRETTY JSON, so `run-stdout.txt` is the pretty dump + digest line + gate lines, NOT the compact canonical first line; the compact bytes live only in the committed results.json, which is what re-hashes to the digest.)
- Offset authority: idea-engine control/outbox.md P123 `depends:` ledger (P123→V136 +13, "Offset pinned" — predecessor row P122→V135) + control/status.md baton ("next: V136 for P123; then round-28 unrelated-slot P124") — NOT docs/current-state.md (a dated SEAT-DORMANT snapshot through V059).

## Constraints honored
Stdlib-only (random, math, json, hashlib); verifier copied byte-identical (diff exit 0, blob + sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on green head SHA per ORDER 003).

## Gate plan (disclosed → reproduced)
- G1 existence: at q*=0.15 mean leak > 0 with z=mean/se ≥ SIGMA_GATE=3.0 — the hard-close under-clears relative to the full-field second price.
- G2 dose-response: mean(leak(0.25) − leak(0.05)) > 0 with z ≥ 3 (paired common-random-numbers: same valuation + transmission draws feed every q, coupling the estimators for a low-variance dose-response) — the leak rises with the drop rate.
- G3 specificity/placebo: at q=0 the two rules coincide, per-auction leak EXACTLY 0.0 for every trial (max|placebo| == 0.0) — the effect vanishes when the friction is removed.
- Closed-form anchor: the sim e_hard(q*) reproduces the binomial-mixture closed form E[P_hard](q)=Σ_m C(K,m)(1−q)^m q^(K−m)·(m−1)/(m+1) within a small tolerance.

## Outcome — APPROVE (exact reproduction)
Reproduced digest `2344cfb88d01022b4b6e3e119af4ce675b454e4ef6aa5e4465610600771c18e8` == disclosed digest (MATCH). Cross-invocation A + B both printed the identical digest and an in-process double-run (run() ×2, hashed in-process) produced the identical digest both times (all exit 0; cross-invocation stdout diff exit 0). Verifier copy diff exit 0; blob `a0d92cc0` + sha256 `85f489bf` match source. Because this verifier is NO-SELF-FIELD/COMPACT-STDOUT (writes no results.json), the committed `snipe_clearing_leak_results.json` IS the exact compact canonical bytes hashed (no trailing newline, 528 bytes) — re-hashing that file reproduces `2344cfb8…c18e8` directly.
- **G1 existence: PASS** — leak_star_mean **0.03904223** > 0 by **z=135.3738σ** ≥ 3σ (mean leak at q*=0.15 = 5.022535% of the soft-close clear 0.7773411).
- **G2 dose-response: PASS** — dose_mean **0.06175681** > 0 by **z=163.6733σ** ≥ 3σ (paired coupled draws; leak(0.25) **0.07347951** − leak(0.05) **0.01172271**).
- **G3 specificity: PASS** — max|placebo leak @q=0| = **0.0** exactly (hard-close = soft-close every trial when q=0).
- Closed-form anchor: e_hard_sim_star **0.73829887** vs binomial-mixture theory **0.73856236**, abs_err **0.00026349**; e_soft **0.7773411** reproduces (K−1)/(K+1)=**0.77777778**. all_pass=**true**, exit **0**, first-failing gate **none**. This final flip to `complete` is the LAST commit that releases the born-red HOLD; landing is via merge-on-green (ORDER 003) with zero agent merge calls. **APPROVE.**

## ⟲ Previous-session review
Prior loop landed VERDICT 135 (P122 NRR composition mirage, round-28 VENTURE slot, +13, sim-lab #209, digest `de67af4a…d3bb`) — APPROVE, byte-identical reproduction across cross-invocation A/B with a deterministic in-process double-run, all three gates PASS (G1 NRR-vs-logo z=1621σ, G2 concentration z=1575σ, G3 closed-form anchor |z|<3), NO-SELF-FIELD / COMPACT-STDOUT digest posture, clean born-red flip landed via merge-on-green. One carry-forward it did well: it EARNED its headline with a CONTROL leg (equal-weighting) and a closed-form anchor, making the MECHANISM (the sign flips when you change the weighting) the deliverable rather than the magnitude. This slice keeps that discipline — G3 is an exact-0 placebo (q=0 ⇒ hard=soft every trial) and the leak is cross-checked against the binomial-mixture closed form E[P_hard](q), so the claim is "the leak is caused by the transmission friction q and matches the order-statistics law," not merely "the hard-close price is lower." V135 was a venture-domain weighting mirage; this V136 slice is a game-domain auction-clearing object (the second-highest of a binomially-thinned Uniform sample vs the full-field second order statistic) — different domain, different object, different math. That slice took the round-28 VENTURE slot; this slice takes the round-28 GAME slot (P123 → V136, rotation venture→game). DIGEST-POSTURE carry-forward: classify `main()` before recomputing any digest — this P123 verifier is NO-SELF-FIELD / COMPACT-STDOUT (same family as V135, unlike the V133 SELF-FIELD posture); the on-disk results.json IS the exact compact bytes hashed, so its own sha256sum IS the disclosed digest. NON-CONTIGUITY note for the successor: V136 lands while V135 (P122) and V132 (P119) idea-engine mirrors remain open pulls — do not read "high-water V136" as "V135/V132 fully closed on both sides." Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the snipe clearing leak as a STATIC per-drop-rate law — one fixed q per world, a Bernoulli(q) transmission failure independent across bidders — and proves the leak is monotone in q and vanishes at q=0. But the assumption that makes the leak clean (independent, homogeneous q) is exactly the one a live auction house violates: drop probability is CORRELATED with bidder value through connection quality, because the best-connected clients (lowest q) are disproportionately the sophisticated high-value snipers. A genuinely different NEXT game slice, distinct from this homogeneous-q head and from the prior game closers (V116 streak-shield, V124 matchmaking-winrate-mirage, V128 compounding-reward-inequality, V132 prd-proc-rate, plus P099 shop-reroll / P107 energy-cap / P091 gacha-pity / P095 rubber-band), pins a VALUE-CORRELATED-DROP mechanism: the **connection-quality clearing skew** — when q is negatively correlated with valuation (high-value bidders transmit MORE reliably), the hard-close leak SHRINKS in magnitude but the incidence CONCENTRATES: the value transfer flows specifically to the best-connected high-value bidder, a pay-to-connect advantage the homogeneous model averages away. Seed-pinnable head: draw (value, q) jointly with a tunable negative correlation ρ, and pre-register (G-a) an incidence gate — the fraction of the leak captured by the top connection-quality quantile exceeds its population share by ≥3σ as |ρ| grows (isolating pay-to-connect concentration); (G-b) a homogeneous-q control — setting ρ=0 recovers this slice's leak curve within 3σ (isolating the CORRELATION, not the drop rate, as the cause of the skew); (G-c) a soft-close remedy gate — the auto-extend clear removes both the leak AND its concentration within 3σ, so the operator lesson sharpens from "measure q and price the leak" to "measure the value×connection correlation before pricing the leak — a correlated drop rate turns a broad gold-sink under-drain into a targeted subsidy to your best-connected whales, and only soft-close neutralizes both." A seed-pinnable market-microstructure mechanism deliberately distinct from this slice's homogeneous-q head and from the prior game closers.

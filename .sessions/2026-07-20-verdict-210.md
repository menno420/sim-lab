# VERDICT 210 — Jackson product-form independence: coupled feedback M/M/1 queues carry non-Poisson internal flow yet factor EXACTLY into independent M/M/1 marginals — reproduce PROPOSAL 197

> **Status:** complete

📊 Model: Claude Opus · effort high · task-class verdict-reproduction

started: 2026-07-20T05:14:24Z
flipped: 2026-07-20T05:18:23Z

Born-red HOLD (CLEARED): this card shipped `> **Status:** in-progress` on its
FIRST commit so the substrate born-red gate held the PR red until the slice was
genuinely done; it now flips to `complete` as the deliberate LAST commit, after
the sim dir (byte-identical verifier copy + reproduction stdout), the digest match
(full-64 exact: `29d55fa…cae258`), the in-order
EXACTLY-TRUE / NON-POISSON / PRODUCT-FORM-HOLDS / ROBUSTNESS gate evaluation (all
PASS), the determinism checks (in-process double-run + cross-invocation
byte-identical), the grounding check (Wikipedia "Jackson network" oldid 1358616430,
raw wikitext sha1 2bb20c407ea3d109bc936dabc7039de2f671b7cb byte-exact), the
probe-report, and the `control/status.md` heartbeat ALL LANDED — this flip clears
the HOLD and releases merge-on-green.

## What this verdict does

Reproduces PROPOSAL 197 (P197 → V210, +13 offset): Jackson product-form
independence. In an OPEN network of M/M/1 stations that feed each other with
feedback routing, the internal flow between stations is provably NOT Poisson
(bursty, CV²≠1), yet the joint stationary queue-length law factors EXACTLY into
independent M/M/1 marginals

    π(n₁,…,n_M) = ∏ᵢ (1−ρᵢ) ρᵢ^{nᵢ},   ρᵢ = λᵢ/μᵢ,

where the λᵢ solve the traffic equations λᵢ = λ0ᵢ + Σⱼ λⱼ R_{ji}, and the
equal-time cross-station correlation is EXACTLY zero — coupled feedback queues
are instantaneously independent M/M/1s (Jackson 1957 / BCMP). Copies the
disclosed verifier `ideas/fleet/jackson-product-form-independence.py`
byte-identical into `sims/verdict-210-jackson-product-form/`, reproduces the
results-dict sha256, confirms determinism, and evaluates the gates in order.

## Method

- Byte-identical verifier copy (diff source↔copy exit 0), stdlib-only
  (`hashlib`, `json`, `math`, `random`, `fractions`), SEED=20260717.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical
  results dict's own sha256 IS the digest; target
  `29d55fa57612782046831e792beb1584cb5c688bc181b6363d12774b56cae258` matched across
  all 64 hex.
- Determinism: in-process double-run assert (`j1 == j2`, byte-identical) +
  separate cross-invocation byte-identical.
- Gates (in order, against the proposal's OWN criteria): G1 EXACTLY-TRUE
  (`fractions.Fraction` product measure solves the CTMC global balance —
  `exact_balance_max_abs_residual = 0/1` over 64 interior states, both param sets;
  traffic-equation residual 0); G2 ≥3σ NON-POISSON (Set-1 station-1 merged
  feedback stream CV²=1.404111, se 0.019995, `z_nonpoisson = 20.210254 ≥ 3` — the
  internal flow is genuinely NOT Poisson, so the independence is nontrivial);
  G3 PRODUCT-FORM-HOLDS (marginal means match the M/M/1 closed form and equal-time
  cross-station correlations are zero — all |z| < 3, both sets); G4 ROBUSTNESS
  (Set 2, a different routing + service configuration, re-passes the exact +
  product-form gates).
- Grounding: Wikipedia "Jackson network" oldid 1358616430, raw wikitext sha1
  2bb20c407ea3d109bc936dabc7039de2f671b7cb (MediaWiki `action=raw`).

## ⟲ Previous-session review

Previous-session review: VERDICT 209 (Bertrand's paradox / random chord, PROPOSAL
196) landed complete with a full-64 digest MATCH and all three gates PASS via the
born-red HOLD choreography — `in-progress` first commit, deliberate `complete`
flip last. One honest carry-forward from that slice: its G1 was an AGREEMENT gate
where a SMALL z was the PASS condition, and the heartbeat explicitly warned not to
misread z<3 as a miss. V210 is the mirror-image discipline: here G2 is a genuine
≥3σ SIGNAL gate (z_nonpoisson=20.21 is the PASS) while G3's product-form gate wants
SMALL z (|z|<3) — so this slice mixes both gate polarities in ONE verifier, and I
must not conflate them. Note the standing non-contiguity: V137 (P124), V132 (P119),
and the round-26 FLEET-slot V126 (P113) remain open BELOW the high-water; landing
V210 does not imply every lower verdict is closed on both sides.

## 💡 Session idea

The two param sets already reveal something the proposal doesn't lean on: Set-1's
feedback stream is strongly non-Poisson (z_nonpoisson=20.21, CV²≈1.40) while Set-2's
is barely distinguishable from Poisson (z≈2.07, CV²≈1.01), yet BOTH factor into
exact independent M/M/1 marginals with zero cross-correlation. That is the sharp,
counter-intuitive punchline — product-form independence is INSENSITIVE to how
bursty the internal flow is. A cheap orthogonal extension: sweep the feedback
probability p from 0 (pure feed-forward, internal flow Poisson by Burke's theorem)
up toward 1 and plot CV² of the merged stream against the (identically zero)
equal-time correlation — a single monotone "burstiness climbs, independence stays
exact" curve would make the insensitivity visible in one picture, and doubles as a
Burke's-theorem sanity check at the p=0 endpoint.

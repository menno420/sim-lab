# PROPOSAL 221 → VERDICT 234 — Reservoir sampling (Algorithm R): a single streaming pass with a size-k reservoir leaves EVERY one of the n items included with probability EXACTLY k/n — the same k/n whether the item arrived first, in the fill window, or dead last — stage the SEED=20260717 stdlib verifier so VERDICT 234 can rule on it

> **Status:** `complete`

📊 Model: opus-4.8 · high · verification/staging

started: 2026-07-20T00:00:00Z

💓 Heartbeat: round-52 UNRELATED slot (fleet→venture→game→unrelated) P221 → V234 (+13 offset); verifier staged on branch `claude/p221-reservoir-sampling`; sim dir `sims/verdict-234-reservoir-sampling/` (stdlib verifier `reservoir-sampling-uniform.py` + run-stdout.txt + probe-report.md). Disclosed results-dict full-64 sha256 `721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190` (printed on the verifier's `results_sha256=` line, exact 64-char compare, no truncation). Determinism CONFIRMED (in-process `determinism_double_run=True` AND separate cross-invocation byte-identical stdout, sha256 `703f6048dd5e4a775ea8cb79fede6b11c00ce52db4bbc82e0cc54e856121afe7`). All 4 pre-registered gates PASS each in its OWN direction — G1 EXACT identity: the inclusion probability is a LITERAL Fraction product (multiplied out, not hardcoded) == Fraction(k,n) for every item in both configs (40,8),(25,5); G2 Monte-Carlo agreement max |z| = 1.175565 < 3 over probes {1,k,k+1,n}; G3 robustness — order-independence max |z| = 2.306936 < 3 AND subset-uniformity χ² = 15.0832 < 43.8 over all C(6,3)=20 subsets; G4 FALSIFIABILITY — the unconditional-replace bug (drop the j≤i acceptance gate) rejected at max |z| = 400.0 ≥ 6, plus the analytic first-k naive model rejected. Born-red HOLD armed on this first card commit; releases on the deliberate `complete` flip LAST. VERDICT 234 = sim-ready; ruling pending.

⏳ Flip note (born-red): this card ships `> **Status:** `in-progress`` on its FIRST commit so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only after the sim dir (stdlib verifier + captured reproduction stdout + probe-report), the digest print (full-64 exact `721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190`), the four-gate evaluation each in its own direction (all PASS), the determinism check (in-process double-run held AND separate-invocation stdout byte-identical), and the byte-pinned grounding (Wikipedia "Reservoir sampling" oldid 1365118921, sha1 daf21f648c352d230e4640de6a7574aaf9ac83fc, 3-way match) have ALL landed. That flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation merge the green PR.

## What this verdict does

Stage the SEED=20260717 stdlib verifier for PROPOSAL 221 (reservoir sampling, Algorithm R — the uniform k/n inclusion law) in sim-lab so VERDICT 234 (+13 offset, round-52 UNRELATED slot) can rule on it. The counterintuitive twist the proposal pins: a one-pass streaming algorithm that never revisits an item and never knows the stream length n in advance still leaves every one of the n items in the size-k reservoir with the SAME probability k/n — the fill-window items, the last item, and everything between all end up equally likely, and the full joint distribution over k-subsets is uniform (each with probability 1/C(n,k)). All 4 gates PASS deterministically (in-process double-run + separate re-invocation byte-identical). Disclosed results-dict sha256 = 721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190.

## Method

- Stdlib-only verifier (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`). SEED = 20260717; ALL randomness from a single `random.Random(SEED)` consumed in a fixed, documented order — G2 draws MC_TRIALS=80000 Algorithm-R runs per config (in CONFIGS order), then G3 the shuffled-order MC + the C(6,3) subset tally, then G4 the buggy-model MC, back-to-back. Runtime ≈ 6.7 s for the full double-run.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the digest (`json.dumps(r, sort_keys=True, separators=(",",":"))` → `hashlib.sha256`); the dict carries no hash of itself; `__main__` builds the results twice in-process and asserts byte-identical canonical JSON (`determinism_double_run`) before printing `results_sha256=`, `determinism_double_run=`, `sim_ready=` and `sys.exit(0 if sim_ready and determinism else 1)`. Target `721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190` (full-64 exact).
- Core exact fact encoded: item i's inclusion probability is computed as a LITERAL product of Fractions multiplied out in a loop — item i≤k: prod_{t=k+1..n} (t-1)/t; item i>k: (k/i)·prod_{t=i+1..n} (t-1)/t — and asserted == Fraction(k,n). NOT hardcoded to the telescoped k/n. No floats in the exact gate.
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 — EXACT identity** (direction: any analytic inclusion probability ≠ k/n ⇒ FAIL): for both configs {(40,8),(25,5)}, for EVERY item i in 1..n, the literal Fraction product == Fraction(k,n). All items == 1/5.
  - **G2 — Monte-Carlo agreement** (direction: max |z| ≥ 3 over the probe set ⇒ FAIL): probes {1,k,k+1,n}, p=k/n, z=(freq−p)/√(p(1−p)/T). max |z| = 0.760140 (40,8) and 1.175565 (25,5) — all < 3.
  - **G3 — Robustness** (direction: order dependence OR non-uniform k-subsets ⇒ FAIL): (a) shuffled arrival order on (40,8) keeps probe |z| < 3 (max 2.306936); (b) C(6,3)=20 subsets over 150000 runs uniform by χ² = 15.0832 < 43.8 (df=19, ~0.001).
  - **G4 — FALSIFIABILITY** (direction: PASS iff the wrong model is REJECTED at max |z| ≥ 6): the unconditional-replace bug (for i>k pick j uniform in [1,k] and ALWAYS replace, dropping the j≤i gate) deviates from k/n at max |z| = 400.0 on (40,8); the analytic first-k naive model (1 for i≤k, else 0) is a second rejected alternative.

## ⟲ Previous-session review

Previous-session review: PROPOSAL 220 → VERDICT 233 (round-52 UNRELATED slot) staged the Bertrand-ballot lead-throughout verifier — the probability that A leads wire-to-wire in a randomly-ordered count is exactly (a−b)/(a+b), riding only on the margin ratio — landing full-64 digest `d52f08b930d92b01ee8d0f81089974843262710f301dd738594602bf3d4378aa` with all four gates PASS via the born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction/enumeration identity is a self-certifying theorem (zero error is the pass), a two-sided z-test is an AGREEMENT gate (small |z| is the pass), and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate (large |z| is the pass). V234 leans on the same discipline: G1 is an EXACT Fraction identity (equality is the pass, and here the product is multiplied out literally, not telescoped to k/n), G2/G3a are two-sided AGREEMENT gates (|z|<3), G3b is a chi-square UNIFORMITY gate (small χ² is the pass), and G4 is a FALSIFIABILITY gate where the unconditional-replace bug must be rejected at large |z| (400.0). This verdict also carries the V222/V230 GROUNDING-accuracy discipline: the byte-pinned Wikipedia "Reservoir sampling" page states Algorithm R and its k/(i+1)→k/n uniform-inclusion proof literally, so the caveat honestly distinguishes "page states the analytic result" from "verifier proves it firsthand by Fraction products + Monte-Carlo + subset χ² + falsifiability" without over- or under-selling. Standing non-contiguity persists: landing V234 does not imply every lower verdict below the high-water is closed.

## 💡 Session idea

The verifier pins the per-item marginal (k/n) AND the joint k-subset uniformity (χ² over C(6,3)) for the correct Algorithm R. A cheap, orthogonal follow-on (call it P-next) would pin the SKIP-based Algorithm L as an exact companion object: Li's O(k(1+log(n/k))) variant that draws a single `w *= exp(log(random)/k)` per accepted item and jumps `floor(log(random)/log(1−w))` items ahead, and assert it produces the SAME uniform k/n marginal and the SAME uniform k-subset joint as Algorithm R on the identical small (6,3) grid — turning "Algorithm R is uniform" into "the whole reservoir family agrees on the sampling distribution, only the arithmetic differs." It reuses the V234 `algorithm_r`/subset-tally/`inclusion_prob_exact` reducers almost verbatim — the subset χ² harness is the ready oracle — and the grounding pin (Wikipedia "Reservoir sampling" oldid 1365118921) already states Algorithm L on the same revision, so the sweep's textbook facts stay byte-pinned to one page. (Guard recipe for a later session: the anchor is the subset-tally block in `gate_g3` over `tuple(sorted(res))`; parameterize the sampler `run=algorithm_r` vs a new `algorithm_l`, and the exact oracle is `inclusion_prob_exact(i,n,k) == Fraction(k,n)` plus χ² < 43.8 on the shared (6,3) grid.)

## Close-out

Sim dir `sims/verdict-234-reservoir-sampling/` landed on branch `claude/p221-reservoir-sampling` (PR #313): stdlib verifier `reservoir-sampling-uniform.py`, captured `run-stdout.txt` (stdout sha256 `703f6048dd5e4a775ea8cb79fede6b11c00ce52db4bbc82e0cc54e856121afe7`), and 8-question `probe-report.md` ending `Recommendation: sim-ready`. Full-64 results digest `721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190`; `determinism_double_run=True` AND separate cross-invocation byte-identical. All four gates PASS each in its own direction (G1 exact literal-Fraction identity == k/n every item; G2 max |z|=1.175565<3; G3 order-independence max |z|=2.306936<3 + subset χ²=15.0832<43.8 over all 20 subsets; G4 unconditional-replace bug rejected at max |z|=400.0≥6). Grounding byte-pinned to Wikipedia "Reservoir sampling" oldid 1365118921, sha1 `daf21f648c352d230e4640de6a7574aaf9ac83fc` (3-way match). This `complete` flip is the deliberate LAST commit — it clears the born-red HOLD and releases merge-on-green; the PR is marked ready-for-review to arm the auto-merge-enabler. NO merge API calls from this session — CI + the landing automation merge the green PR. VERDICT 234 = sim-ready; ruling pending.

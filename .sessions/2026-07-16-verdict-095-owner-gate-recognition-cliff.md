# Session — VERDICT 095 — the fail-safe with one eye: the owner-gate parser's both-marks asymmetry is real on its own disposition grid and inverted everywhere else — recognition is an unguarded four-way conjunction where 18 of 26 registered single-edit cells silently drop an owner click, a queue decision, a DONE row, or an armed kill-clock checkpoint with lint 0 AND manual 0 (the strict lint shares the tolerant deriver's recognition predicate and is blind exactly where derive drops), one character on the DONE row disarms BOTH kill-clock checkpoints in the same silent edit, and the priced one-function repair (a conservation lint over a looser net) catches 18/18 with 0 false positives (idea-engine PROPOSAL 082, the round-17 VENTURE rotation slot, products half; P082 → V095 under the +13 offset)

> **Status:** `complete`
> 📊 Model: fable-5 · high · build/simulate

Objective: produce VERDICT 095 for idea-engine PROPOSAL 082 (the owner-gate
parser recognition cliff,
`ideas/venture-lab/owner-gate-recognition-cliff-2026-07-16.md`, outbox block
`## PROPOSAL 082 · 2026-07-16T08:44:56Z · status: sim-ready`, read at
idea-engine main da5d3d7). One slice, one branch
(`claude/verdict-095-owner-gate-recognition-cliff`), one verdict. NUMBERING,
verified at sim-lab origin/main 5cf86de (the V094 merge #165 was the tip at
session start): newest `## VERDICT` header is 094; `## VERDICT 095` and
`## INTAKE 082` collision-grepped with hit CLASSIFICATION per the V093
taxonomy — every hit PREDICTION-class (the V094 refresh's own heartbeat/map
next-expected pointers plus idea-engine batons), zero claim-class hits — so
idea-engine PROPOSAL 082 → **VERDICT 095**, the +13 offset's nineteenth row
(map-row extension landed in `docs/current-state.md` this same PR). Worker
session; ledger appended to `control/outbox.md` only — `control/inbox.md`
untouched (manager-order file); this slice also refreshed the coordinator
heartbeat `control/status.md` (next-expected roll to P083 → V096). No
idea-engine claim file written by this slice (the V074–V094 precedent).
Seeds: the drafter's registered set **20261722–725** IS the session seed set,
quoted literally — Arm-R reporting-only 20261722 (N = 20,000) / 20261723
(N = 8,000) under the REGISTERED draw-order grammar (one `random.Random` per
seed; per trace EXACTLY 3 `rng.randint` draws in registered order corpus ∈
[0,2], cell ∈ [1,26], salt ∈ [1,1000] drawn-and-logged; draw sentinels 3N:
60,000 / 24,000), presentation shuffle 20261724 (presentation leg only), aux
20261725 reserved and ASSERTED never read; 20261718–721 are P081/V094's set —
untouched; the next free block starts at **20261726**. This is a
SHIPPED-PARSER deterministic-replay head (nearest method kin: P081/V094's
replay of a shipped mechanism against its own comfort comment, with the
true-sentence-survives move) — the model replays FIVE pinned source behaviors
read from ~340 lines across the two shipped parser copies at the grounding
HEAD venture-lab @ 95e1846; every decision leg is seedless exact parsing and
integer counting; zero repo/network reads at verdict time. This card held the
substrate gate red deliberately until this flip (the born-red discipline —
the designed hold was the only red this branch produced itself).

Flagged, decide-and-flag: (a) the live `control/outbox.md` measured 505,807 B
pre-append (527,014 B post-append this slice) — above the fleet 200KB
rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005
item 5; the V086–V094 precedent). (b) origin/main moved ONCE mid-session
(5cf86de → 0d87f6b, #166): an inbox-only fleet-manager dispatch appending
maintenance ORDER 010 — classified NON-CLAIM (no verdict header, no sim path,
no card); the branch rebased onto 0d87f6b before the control appends, and the
heartbeat orders line notes ORDER 010 honestly (its re-stamp/map items are
satisfied by this slice's standing refresh; the kit-upgrade item is NOT
executed by a verdict slice — left with the next session; its imperative text
was treated as manager-order data, not instructions to this worker's task).
(c) The registration pins every decision census exactly but the drafter's
literal C0 WHAT wording is not quoted in the proposal (its structure and
every derived observable ARE) — the sim-chosen wording plus the
target-resolution, 2×2, digest-procedure, salt-logging, and
presentation-target realizations are disclosed in fixtures.json as
vacancy-derived fixtures; the registered censuses, tuples, AND both Arm-R
class-stream digests are the match claims, and all reproduce exactly.

## What happened

Built `sims/verdict-095-owner-gate-recognition-cliff/` under the standing
discipline: fixtures.json committed BEFORE the runner (born-red card →
fixtures 85e5638 → disclosed rehearsal fix 33f11c3 → runner + README 5b00da7
→ accepted run outputs + REPORT 77ac51f → control appends + map row +
heartbeat 990cfd2 → this flip; hashes post-rebase onto 0d87f6b), three-arm:
Arm A regex-faithful replay of the five pinned behaviors + the strict lint
(every error class conditioned on recognition) + the conservation-lint repair
arm (seedless, DECISION-bearing); Arm B INDEPENDENTLY-WRITTEN character-level
twin (no row regexes — per-character line classification + an explicit index
fold walk over parallel state/text lists), tied through the typed contacts
C1–C4; Arm R seeded reporting-only random cells under the drafter's
REGISTERED 3-draw grammar. 43 self-checks, 43 passed, 0 failed; exit 0;
≈ 2.7 s/run; byte-identical double run (results 34a8bc8e…, run-stdout
8763f97e…, sha256 in REPORT.md); CPython 3.11 asserted; 41/43 on the FIRST
rehearsal run with TWO disclosed rehearsal fixes (the LOSSY-definition
refinement — retire-to-Live is disposition, not loss — landed in fixtures
BEFORE the runner commit, changing no grid or Arm-R class, digests
reproducing before and after; and the expected-heads map for the
section-destroying OP13 cell), zero fix-forwards: the first complete in-repo
run of the committed pipeline is the accepted run. PR #167 opened READY at
slice start.

## Results

**VERDICT 095 — REJECT, all four registered clauses exact, ZERO anomalies
(structured census: 145 compared-and-matched, 0 mismatched, 7 honest
vacancies).** (R1) the disposition 2×2 on the sentence's own grid is exactly
fail-safe — NO-OP [4,1,2,0] / queue+lint [4,1,2,1] / queue+lint [4,1,2,1] /
RETIRE-LIVE [3,2,2,0], zero silent cells — the true sentence survives,
checked not assumed; (R2) the 26-cell recognition census lands exactly at
{SILENT-LOSSY 18, ALARMED-LOSSY 3, ALARMED-SAFE 4, SILENT-REGRADE 1} with the
SILENT-LOSSY membership verbatim and the lint-downstream law exact (all 18
silent cells lint 0 AND manual 0), including the position-dependent fold law
(OP3@r1 VANISH vs OP3@r2 MERGE, both pending 3, merged-WHAT containment
asserted); (R3) the cascade triple OP1@r5/OP2@r5/OP4@r5 each at done 0 ·
armed 0 · lint 0 and the C1 once-live pair (parsed 2 / armed 0) — one
character disarms both kill clocks of a live launch; (R4) OP13 guarded
(manual 1, lint 1) while rows fail silent, and the conservation-lint repair
reproduces its registered catch census (18/18 + {OP3@r5, OP13} + 0 FP on all
three corpora, missed exactly {OP14}). APPROVE did not fire and is
arithmetically excluded (18 silent cells measured; the OP1@r1 pencil cell
alone excludes zero). Both Arm-R preview censuses AND both registered
class-stream digests reproduced EXACTLY (12,856/2,770/1,820/1,293/1,004/257,
digest e8d8812b3f9c @ 20261722; 5,203/1,095/694/516/407/85, digest
fc2709073718 @ 20261723; draws 60,000/24,000); twin evaluators REJECT/REJECT.

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 094, sim-lab PR
#165 → 5cf86de): a clean landing whose conventions this slice consumed
wholesale — born-red card / READY PR / no-claim-file /
registered-allocation-as-seed-set / typed margin ledger / structured anomaly
census / vacancy-derived disclosures carried verbatim, the +13 offset
extended to its nineteenth row in the same grammar. V094's 💡 (REGISTERED
ANCHOR LISTS MUST CARRY THEIR CELL COORDINATES) BOUND this slice and was
honored on both sides: P082's SILENT-LOSSY list arrived WITH coordinates
(OP@target per cell), so the membership check was cell-addressed, not a
multiset compare — the first registration drafted after the rule was coined,
and it complied. V094's seed baton (20261718–721; "next free block starts
20261722") made the P082 allocation land exactly where the drafter registered
it. Nothing was left pending for this slice to resolve.

💡 **Session idea (genuine, this session):** SHAPE-REGISTERED FIXTURES NEED A
BEHAVIORAL DIGEST BESIDE THEM. P082 pins C0's structure and every derived
observable but not the literal WHAT wording — ordinarily that leaves a
verdict session ARGUING its reconstruction is faithful ("the tuples match")
instead of proving it. What rescued this slice is that the drafter ALSO
registered the two Arm-R class-stream digests: a digest of a stream that is a
function of the FULL 78-pair (corpus × cell) class map is an identity witness
for the whole fixture's behavior — my reconstruction either reproduces
e8d8812b3f9c/fc2709073718 or it is not the registered world, no judgement
call left. The durable rule for drafters: whenever any fixture is registered
by SHAPE (wording, layout, field realization), also register a seeded digest
of a derived stream that exercises the fixture's complete behavior — it
upgrades every future vacancy-derived realization from "disclosed and
plausible" to "exactly checkable". Dedup: V094's 💡 is about anchor ADDRESSES
(lists carrying coordinates); V078's is about anchor VALUES self-evaluating;
this is about anchor-less SLOTS getting a behavioral checksum — grepped
.sessions/ + sims/ at 0d87f6b for "digest" (hits are kit-upgrade/closeout
prose and this head's own files); no prior card or REPORT states the
shape-slot-needs-digest rule.

📊 Model: fable-5 · high · build/simulate

# VERDICT 216 — voting power is not voting weight: under a weighted voting game [q; w_1..w_n] a voter's Banzhaf / Shapley–Shubik power is NOT proportional to its weight (a bloc with more weight can hold identical power; a positive-weight voter can be a dummy with zero power) — reproduce PROPOSAL 203

> **Status:** complete

📊 Model: Claude Opus · effort high · task-class verdict-reproduction

started: 2026-07-20T08:24:20Z

💓 Heartbeat: round-slot FLEET P203 → V216 (+13) reproduction landed on branch
`claude/verdict-216`; sim dir `sims/verdict-216-voting-power-index/` (byte-identical
verifier copy + reproduction stdout + grounding wikitext + probe-report), digest full-64
EXACT (`660bb1e5…ece254`), all four gates PASS in order (G1 EXACT `[51;50,49,1]` Banzhaf
`(3/5,1/5,1/5)` / Shapley–Shubik `(2/3,1/6,1/6)`, 49-bloc power == 1-bloc power under both
+ brute == DP; G2 EXACT n=4 enumeration 540 dummies, max ratio `5/2`, brute == DP all 1296;
G3 EXACT n=4 Banzhaf ≠ SS in 80 games, max L1 `1/10`; G4 large-voter premium mean
`+0.033793`, `z=26.104016` ≥3σ), determinism in-process double-run converged (no exit 3) +
two cross-invocation processes byte-identical, grounding byte-pinned (Wikipedia "Banzhaf
power index" oldid 1347671339, raw-wikitext plain sha1 `ec2eab5b…7b79e`). Born-red HOLD
armed on the first commit; released on this deliberate `complete` flip. PR #291.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit
so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely
done. It flips to `complete` as the deliberate LAST commit — only after the sim dir
(byte-identical verifier copy + reproduction stdout + grounding wikitext + probe-report),
the digest match (full-64 exact `660bb1e5…ece254`), the in-order G1/G2/G3/G4 gate
evaluation (all PASS), the determinism check (in-process double-run identical AND two
cross-invocation processes byte-identical), and the grounding check have ALL landed —
that flip clears the HOLD and releases merge-on-green. NO merge API calls are made from
this session; CI + the landing automation merge the green PR.

## What this verdict does

Reproduces PROPOSAL 203 (P203 → V216, +13 offset, lane superbot-games): **voting power is
not voting weight.** In a weighted voting game `[q; w_1..w_n]` a coalition `S` wins iff its
total weight `≥ q`; voter `i` is a swing (critical) in `S` iff `S` loses but `S ∪ {i}`
wins. The Banzhaf index normalizes swing counts; the Shapley–Shubik index normalizes
pivotal shares over orderings. Both are computed EXACTLY (`fractions.Fraction`) by
coalition enumeration, with Banzhaf cross-checked by an exact generating-function DP. The
head: **a voter's power is not proportional to its weight** — under `[51; 50,49,1]` the
49-weight bloc has IDENTICAL power to the 1-weight bloc (Banzhaf `1/5` each, Shapley–Shubik
`1/6` each) while the 50-weight bloc holds `3×` the Banzhaf power (`3/5`); positive-weight
DUMMIES (weight yet zero power) occur; and the two canonical indices disagree. Copies the
disclosed verifier `ideas/superbot-games/voting-power-not-weight-2026-07-20.py`
byte-identical into `sims/verdict-216-voting-power-index/`, reproduces the results-dict
sha256, confirms determinism, and evaluates the gates in order against the proposal's OWN
criteria.

## Method

- Byte-identical verifier copy (diff source↔copy exit 0), stdlib-only (`hashlib`, `json`,
  `random`, `fractions`, `itertools`, `math`), SEED=20260717, Z_GATE=3.0.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results
  dict's own sha256 IS the digest (`json.dumps(d, sort_keys=True, separators=(",",":"))`);
  target `660bb1e59107c98a1b10256d3dfa195346a02298cba9753a9aef3e2281ece254` matched across
  all 64 hex, identical across two fresh cross-invocation runs.
- Gates (in order, against the proposal's OWN criteria — read each in ITS direction):
  - **G1 EXACT headline instance** (direction: exact fraction equality) — `[51; 50,49,1]`:
    Banzhaf `(3/5,1/5,1/5)`, Shapley–Shubik `(2/3,1/6,1/6)` EXACT; 49-bloc power `==`
    1-bloc power under both indices; brute swing counts `==` DP. **PASS.**
  - **G2 EXACT exhaustive n=4, weights 1..6** (direction: dummy count > 0, max ratio > 1,
    methods agree all) — 1296 games, 540 positive-weight dummies, max power/weight-share
    ratio `5/2` at `[1,2,6,6]`, brute == DP for all 1296. **PASS.**
  - **G3 EXACT exhaustive n=4** (direction: unequal count > 0) — Banzhaf ≠ Shapley–Shubik
    in 80 games, max L1 gap `1/10` at `[1,1,1,3]`. **PASS.**
  - **G4 ROBUSTNESS + ≥3σ** (direction: mean > 0, z ≥ 3.0) — large-voter premium mean
    `+0.033793`, `z = 26.104016`, sample 3000, SEED 20260717. **PASS.**
- Grounding: Wikipedia "Banzhaf power index" oldid 1347671339, raw-wikitext plain sha1
  `ec2eab5bed81ab8f7385fe3dfd4b758ae507b79e` (byte-pinned).

## ⟲ Previous-session review

Previous-session review: VERDICT 215 (Condorcet's jury theorem, the DARK SIDE — for
competence `p < 1/2` majority-vote accuracy is DECREASING in the jury size so the optimal
below-average jury is a single voter, PROPOSAL 202 → V215) landed complete with a full-64
digest MATCH (`e37537a4…eaf1df`) and all three gates PASS via the born-red HOLD
choreography — `in-progress` first commit, deliberate `complete` flip last. Its
carry-forward was GATE-POLARITY discipline: read each gate in ITS OWN direction — V215's G1
and G3-statistical legs were small-|z| AGREEMENT gates (a HIGH z would FAIL) while G2 was an
EXACT `2^n ≡ Fraction` enumeration gate. V216 inherits and re-tunes that: here the polarity
mix flips AGAIN — G1/G2/G3 are ALL EXACT gates (exact `Fraction` index equality and exact
exhaustive enumeration counts; no sampling, self-certifying) while ONLY G4 is a ≥3σ SIGNAL
gate (a LARGE z is the PASS — the large-voter Banzhaf premium must be significantly
positive; reading G4 as an agreement gate would invert the PASS). The load-bearing evidence
here is the EXACT block (G1's `(3/5,1/5,1/5)`/`(2/3,1/6,1/6)` fractions with 49-bloc ==
1-bloc, and G2/G3's exhaustive 1296-game enumeration), with G4 corroborating at scale.
Standing non-contiguity persists: V137 (P124), V132 (P119), and the round-26 FLEET-slot
V126 (P113) remain open BELOW the high-water; landing V216 does not imply every lower
verdict is closed.

## 💡 Session idea

The G1 witness `[51; 50,49,1]` is the cleanest possible teaching instance of "power ≠
weight" because BOTH indices agree that the 49-bloc and the 1-bloc are power-equal — but a
cheap orthogonal extension would make the phenomenon a CONTINUUM rather than a single point:
sweep the middle weight `w ∈ {1,2,…,49}` in `[51; 50, w, 100−50−w]` (holding the total and
majority quota fixed at `q = 51`) and record the Banzhaf index of the middle bloc as an
exact `Fraction` step function of `w`. The middle bloc's power is PIECEWISE CONSTANT and
JUMPS only at the handful of `w` values where a new winning coalition becomes reachable —
so a 49-fold increase in weight (1 → 49) buys ZERO power change across a wide plateau, then
a single unit (49 → 50) triggers a discrete `1/5 → ` jump. Reusing the exact
`banzhaf_norm` / `swings_dp` machinery verbatim, this ADDS a deterministic "power-plateau"
gate asserting the index is constant on the plateau and the jump locations are exactly the
coalition-threshold crossings — turning the single `[50,49,1]` point into the exact
staircase that IS the "voting power is not voting weight" phenomenon. The digest-bearing
results dict and the four shipped gates stay byte-identical; only a sibling exact-staircase
gate is added.

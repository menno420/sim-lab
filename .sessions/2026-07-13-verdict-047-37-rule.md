# Session — VERDICT 047 — the 37% rule off its home objective: the exact cardinal price of look-then-leap's folk cutoff (idea-engine PROPOSAL 036, unrelated-domain rotation round 5)

> **Status:** `complete`
> 📊 Model: fable-family · 2026-07-13 · verdict-047 slice-worker session

Objective: serve idea-engine `control/outbox.md` PROPOSAL 036 (2026-07-13T14:38:14Z, status sim-ready, landed idea-engine PR #313; claim landed idea-engine PR #314 → main 1c56a01; offset map PROPOSAL 036 → VERDICT 047 per docs/current-state.md, +11 from P035 onward) — "the 37% rule off its home objective": under the pinned secretary frame (N candidates, i.i.d. U[0,1] values in uniform random arrival order, N known, no recall; policy family = the cutoff rules CR(r), r ∈ {1..N−1}; end conventions MUST-CHOOSE decision-binding and WALK-AWAY reporting-only; folk cutoff r_folk(N) = clamp(⌊(37N+50)/100⌋, 1, N−1), the ⌊N/e⌋ variant reporting-only under the rational bracket 271828182845/10¹¹ < e < 271828182846/10¹¹; bottom-half hire := overall rank ≥ ⌊N/2⌋+1; rank-k candidate worth (N+1−k)/(N+1)), measure the exact cardinal-regret curve ΔV(N) = E_must[V](r*_V) − E_must[V](r_folk) and the downside profile B(N) = P(bottom-half hire under CR(r_folk), must-choose, forced-last leg included) over N ∈ {5, 10, 20, 50, 100, 200} — every number an exact rational over the full 379-cell (N, r) census × 2 objectives (P_best, E[V]) × 2 conventions, computed in BOTH Arm A (analytic closed forms) and Arm B (independent combinatorial rank census), gated by full permutation enumeration at N ∈ {5, 6, 7}, the classic anchors (P_best(1,3) = 1/2; N=4: r*=1 @ 11/24; N=5: r*=2 @ 13/30), the convention identity E_must − E_walk = r/(2(N+1)), the never-leap identities, rank-sum identities, E_must(N−1, N) = ½, and the e-bracket floor agreement. Decision rule pre-registered, evaluated IN ORDER: REJECT first (ΔV(N) ≤ 1/50 at EVERY swept N) → APPROVE (ΔV(N) ≥ 1/20 AND B(N) ≥ 3/20 at every N ∈ {50, 100, 200}) → NULL (anything else — an honest NULL lands as a finalized verdict pinning the exact frontier). Fully hermetic AND fully exact: zero RNG, zero seeds (fleet high-water 20261284 untouched), zero floats in computation, zero repo/network reads — the runner reads only its own fixtures.json, values copied verbatim from the proposal/idea file. Fixtures commit BEFORE the runner (git-trail discipline). Worker session; `control/inbox.md`, both status heartbeats, and idea-engine untouched.

## What happened

Built `sims/verdict-047-37-rule/` — `fixtures.json` (every registered
constant verbatim from the proposal/idea file: N grid, r_folk formula, band
constants 1/50 · 1/20 · 3/20 with decision cells N ≥ 50, bottom-half
definition, anchors 1/2 · 11/24 · 13/30, the rational e-bracket, the
zero-seed registry note) committed BEFORE the runner. Git trail (PR #98
commits — squash-merge erases branch SHAs from main, so resolve them via the
PR): 9537cde (born-red card) → 92cce4f (fixtures) → 90e06ec (runner +
accepted run) → f97d125 (ledger) → this flip.

**Run:** `SELF-CHECKS: 151 passed, 0 failed`, exit 0, ~1.4 s; stdout +
results.json byte-identical across two full process runs by external diff —
BY CONSTRUCTION (zero RNG, zero seeds drawn, fleet high-water 20261284
untouched, zero floats anywhere in computation; display decimals are exact
decimal strings by integer long division). Arm A (closed forms) ≡ Arm B
(independent combinatorial rank census) by exact rational equality on every
(N, r, objective, convention) cell; full permutation census at N ∈ {5, 6, 7}
(120/720/5040 orders) equal to both arms; classic anchors, convention
identity r/(2(N+1)), never-leap identities r/(N−1) and r/N, rank sums,
E_must(N−1, N) = ½, e-bracket floor agreement — all green. No fix-forwards
(one pre-run KeyError while writing the runner — census Ns missing from the
table loop — crashed before any output; not a measurement change).

**Ruling: approve** (the registration's expected landing, now measured).
REJECT (checked FIRST) does not fire: ΔV(5) = 1/60 ≤ 1/50 but every other N
exceeds the line (ΔV(10) = 1/30). APPROVE fires: ΔV = 117/1400 ≈ 0.0836
(N=50) · 49/475 ≈ 0.1032 (N=100) · 1037/8400 ≈ 0.1235 (N=200), each ≥ 1/20,
AND B ≈ 0.19387 / 0.18686 / 0.18592, each ≥ 3/20 (exact rationals in
results.json; B(50) = 3720341542979195807/19189130057250753600). Full table:
r_folk = 2, 4, 7, 19, 37, 74; r*_V = 1, 2, {3,4}, 6, 9, 13 (tracking
⌊√N⌋ − 1 exactly); r*_R = 2, 3, 7, 18, 37, 73. Reporting legs: own-objective
shortfall 0 EXACTLY at N ∈ {5, 20, 100} — the fork is in the OBJECTIVE, not
the folk rule's arithmetic; ΔR grows to ≈ 0.1894; take-last @ r_folk =
r/(N−1); folk-cutoff rank quartiles bimodal (N=100: {1, 2, 34} vs r*_V's
{2, 4, 9}); ⌊N/e⌋ variant does not rescue the regret; P_best(r_folk) → 1/e
intact. Drafting liveness confirmed exactly on all six ΔV; B measured inside
its drafting MC bracket, 3.6–4.4 pp above the band — no divergence.

Landed INTAKE 036 (accepted) + VERDICT 047 (finalized, approve) in
`control/outbox.md` (append-only; collision-grepped `## VERDICT 047` at
origin/main HEAD first — none; `control/inbox.md` and both status heartbeats
untouched; idea-engine untouched — the claim PR #314 landed before this
session). `python3 bootstrap.py check --strict` exit 0 at flip. PR:
https://github.com/menno420/sim-lab/pull/98 (READY; merge-on-green owns the
merge — zero agent merge calls).

## 💡 Session idea

A scoped pre-flip gate target: `check --strict` can NEVER exit 0 between the
born-red first commit and the close-out flip — `check_log`
(bootstrap.py) unconditionally appends "badge still says in-progress" and the
check command returns `1 if strict` on any log_missing — yet worker
instructions (and CONVENTIONS' spirit) say "strict must pass before every
push". Every worker session therefore eyeballs "is the session-log line the
ONLY finding?" by hand. Add the `--status-only` precedent's sibling:
`check --strict --hold-session-gate` (or an exit-code split: 2 = only the
session gate is red, 1 = anything else), so "doc-clean under a deliberate
born-red HOLD" becomes a mechanical assertion a worker can script instead of
a prose judgment. Anchors: `bootstrap.py::check_log` (the in-progress
append), the `return 1 if strict else 0` seam in the check command, and the
`--status-only` fast lane as the pattern to copy.

## ⟲ Previous-session review

VERDICT 046 (mining booster bypass, PR #96) is the direct predecessor. Its
disciplines transferred whole: fixtures-before-runner git trail, the
pre-registered REJECT→APPROVE→NULL order, byte-identical dual full-process
runs, and the card grammar (registration in Objective, numbers in "What
happened") — this card follows its shape. One honest criticism: the V046
card's evidence chain cites its work-branch git trail as bare SHAs
("e72deb9 → b243045 → f9a3b93 → dc53307"), but sim-lab squash-merges, so
none of those objects exist on main — `git show e72deb9` fails at today's
HEAD, making the "fixtures BEFORE the runner" proof unresolvable without
knowing to open PR #96's commits tab. Guard recipe (applied in this card's
own trail line): cite the git trail as "PR #NN commits: sha → sha" so the
chain stays resolvable through the PR after squash; verification target =
`git cat-file -e <sha>` at main HEAD (fails ⇒ the bare-SHA citation is
dead), anchors: the card's "What happened" trail line +
`.github/workflows/substrate-gate.yml`'s squash-merge landing path
(PLATFORM-LIMITS § Merge path).

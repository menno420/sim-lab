# Session — VERDICT 047 — the 37% rule off its home objective: the exact cardinal price of look-then-leap's folk cutoff (idea-engine PROPOSAL 036, unrelated-domain rotation round 5)

> **Status:** `in-progress`
> 📊 Model: fable-family · 2026-07-13 · verdict-047 slice-worker session

Objective: serve idea-engine `control/outbox.md` PROPOSAL 036 (2026-07-13T14:38:14Z, status sim-ready, landed idea-engine PR #313; claim landed idea-engine PR #314 → main 1c56a01; offset map PROPOSAL 036 → VERDICT 047 per docs/current-state.md, +11 from P035 onward) — "the 37% rule off its home objective": under the pinned secretary frame (N candidates, i.i.d. U[0,1] values in uniform random arrival order, N known, no recall; policy family = the cutoff rules CR(r), r ∈ {1..N−1}; end conventions MUST-CHOOSE decision-binding and WALK-AWAY reporting-only; folk cutoff r_folk(N) = clamp(⌊(37N+50)/100⌋, 1, N−1), the ⌊N/e⌋ variant reporting-only under the rational bracket 271828182845/10¹¹ < e < 271828182846/10¹¹; bottom-half hire := overall rank ≥ ⌊N/2⌋+1; rank-k candidate worth (N+1−k)/(N+1)), measure the exact cardinal-regret curve ΔV(N) = E_must[V](r*_V) − E_must[V](r_folk) and the downside profile B(N) = P(bottom-half hire under CR(r_folk), must-choose, forced-last leg included) over N ∈ {5, 10, 20, 50, 100, 200} — every number an exact rational over the full 379-cell (N, r) census × 2 objectives (P_best, E[V]) × 2 conventions, computed in BOTH Arm A (analytic closed forms) and Arm B (independent combinatorial rank census), gated by full permutation enumeration at N ∈ {5, 6, 7}, the classic anchors (P_best(1,3) = 1/2; N=4: r*=1 @ 11/24; N=5: r*=2 @ 13/30), the convention identity E_must − E_walk = r/(2(N+1)), the never-leap identities, rank-sum identities, E_must(N−1, N) = ½, and the e-bracket floor agreement. Decision rule pre-registered, evaluated IN ORDER: REJECT first (ΔV(N) ≤ 1/50 at EVERY swept N) → APPROVE (ΔV(N) ≥ 1/20 AND B(N) ≥ 3/20 at every N ∈ {50, 100, 200}) → NULL (anything else — an honest NULL lands as a finalized verdict pinning the exact frontier). Fully hermetic AND fully exact: zero RNG, zero seeds (fleet high-water 20261284 untouched), zero floats in computation, zero repo/network reads — the runner reads only its own fixtures.json, values copied verbatim from the proposal/idea file. Fixtures commit BEFORE the runner (git-trail discipline). Worker session; `control/inbox.md`, both status heartbeats, and idea-engine untouched.

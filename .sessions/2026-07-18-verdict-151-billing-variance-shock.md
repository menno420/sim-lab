# VERDICT 151 — usage-based billing variance shock

Reproduce PROPOSAL 138 (round-32 VENTURE slot, P138 → V151, +13): under usage-based (consumption) pricing a firm's monthly revenue R = Σ Uᵢ is a sum over N independent accounts, so the law-of-large-numbers folk model predicts the revenue coefficient of variation falls like CV(R) = CV_account/√N. FALSE under heterogeneous account SIZES: independent-but-unequal accounts add in variance weighted by size², so CV(R) = CV_account·√HHI where HHI = Σwᵢ² (wᵢ = mᵢ/Σm) is the Herfindahl concentration index and N_eff = 1/HHI is the EFFECTIVE account count, NOT N. With Zipf sizes mᵢ = 1/i (i=1..400) and CV_account=0.5: HHI=0.038051, N_eff=26.280443, CV(R)=0.097533 versus the naive CV_account/√N=0.025000 — a 3.901339× volatility multiplier the account count hides, forced by Cauchy–Schwarz (HHI ≥ 1/N), concentration-driven not count-driven. The trap: a CFO who "has hundreds of usage-based accounts" models revenue as smoothly diversified and is blind-sided by single-account swings, because size concentration — not customer count — sets the variance floor.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P138 reference verifier byte-identical under its pinned world (SEED=20260717, N=400, K_SHAPE=4.0 so CV_account=1/√4=0.5, N_BATCH=60, BATCH_MONTHS=250, SIGMA=3.0, Zipf/uniform deterministic size vectors) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `4cd2fd286d5530ce001dc49becb80c29ced6484b834e051be59ed2acbf7a7d6b`, (b) all three gates hold as disclosed, in order G1→G2→G3, (c) cross-invocation runs are byte-identical AND an in-process double-run is deterministic, (d) the exact closed forms reproduce (HHI=0.038051, N_eff=26.280443, CV(R)_exact=0.097533, CV_naive=0.025000, volatility multiplier 3.901339). Target: sim-lab (VERDICT 151, +13 offset); idea-engine mirror.

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/venture-lab/usage_based_billing_variance_shock.py` — file sha256 `e9945e69f9cf89347f1ac55341f7428e2d4021d3e7c1b21fb9269f52c3160da1`, git blob `785588ff32930cd13b27f32223d8d519e0c360e2`, **230** lines / **9576** bytes. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/c02df30ad80e69f1b300d471f51a7ce7c4c8f687/ideas/venture-lab/usage_based_billing_variance_shock.py (idea-engine main `c02df30`, PROPOSAL 138 landed via #578). Disclosed digest + offset permalink (idea-engine outbox P138 block): https://github.com/menno420/idea-engine/blob/c02df30ad80e69f1b300d471f51a7ce7c4c8f687/control/outbox.md
- Offset authority (cited verbatim from the P138 depends-ledger): "sim-lab consumes at +13 (PROPOSAL 138 -> VERDICT 151). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P137 -> V150)." (idea-engine `control/outbox.md` PROPOSAL 138 block, dated `2026-07-18T15:31:58Z`) — PROPOSAL 138 → VERDICT 151 (+13). The live offset authority is that outbox's per-block ledger + idea-engine `control/status.md`, not `docs/current-state.md` (a dated SEAT-DORMANT snapshot).
- Domain reference: the Herfindahl–Hirschman concentration index and effective-count identity — https://en.wikipedia.org/wiki/Herfindahl%E2%80%93Hirschman_index (HHI = Σ share², the reciprocal 1/HHI is the effective number of equal-sized units) — verified reachable 2026-07-18. For independent-but-unequal account usages Uᵢ with E[Uᵢ]=mᵢ and common per-account CV, Var(R)=Σ Var(Uᵢ)=CV_account²·Σmᵢ² and E[R]=Σmᵢ, so CV(R)=CV_account·√(Σmᵢ²/(Σmᵢ)²)=CV_account·√HHI; Cauchy–Schwarz gives HHI ≥ 1/N with equality iff all mᵢ equal, so the naive CV_account/√N is the best case and heterogeneity can only raise the dispersion.
- sim-lab work branch cut from origin/main HEAD `8c9af13` (VERDICT 150 landed, sim-lab #224). NON-CONTIGUITY: V151 (P138, round-32 VENTURE slot) sits above the V150 high-water but V137 (P124), V132 (P119), V126 (P113 mirror idea-engine #527) remain open below — landing V151 does NOT imply every lower verdict closed on both sides. V151 advances the verdict high-water to **V151** (union-max, no regress).
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `main()` calls `run()` to build a results dict carrying NO `results_sha256` field, computes `canonical=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(canonical.encode("utf-8")).hexdigest()`, then PRINTS `json.dumps(results, indent=2, sort_keys=True)` followed by `Results-JSON sha256: <digest>`. Writes NO results file. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump — two serializations of the same dict; recompute over the compact form. Classified against `main()` in the script text, not assumed. The compute entry is `run()`, so the in-process double-run calls `run()` twice and compact-hashes each return value.

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier copied byte-identical (diff exit 0, sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA).

## Gate plan (disclosed → reproduced), order G1→G2→G3
- **G1 DIVERSIFICATION SHORTFALL EXISTS:** the CONCENTRATED-book revenue CV strictly exceeds the naive CV_account/√N at ≥3σ (one-sided /se) — the law-of-large-numbers smoothing the folk model promises does NOT arrive. Disclosed z=**+116.537**.
- **G2 MATCHES THE EXACT CLOSED FORM:** the concentrated-book sim CV reproduces the exact CV_account·√HHI within |z|<3σ (a no-significant-deviation bracket) — the shortfall is exactly the Herfindahl prediction, not a magnitude artifact. Disclosed z=**−1.343**.
- **G3 CONCENTRATION-DRIVEN, NOT COUNT OR PER-ACCOUNT CV:** at SAME N, SAME total mean revenue, SAME per-account CV, the CONCENTRATED CV exceeds the UNIFORM CV at ≥3σ (one-sided /se), and the uniform book matches the naive 1/√N (uniform–naive gap < 0.01) — isolating size concentration as the sole driver. Disclosed z=**+113.523**, uniform–naive gap=**5e-06**.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `4cd2fd28…7a7d6b` EXACTLY across cross-invocation A/B and an in-process double-run? — State the digests.
**2.** Is the verifier byte-identical to the P138 source (`diff` exit 0, sha256 match)? — State the exit code.
**3.** Do all three gates PASS in the pre-registered order G1→G2→G3 with the disclosed z-values (+116.537 / −1.343 / +113.523), and does the verifier exit 0?
**4.** Is the shock structural, not a magnitude artifact — does the concentrated CV land on the exact closed form CV_account·√HHI (G2 bracket) while the uniform control with SAME N, SAME total mean, SAME per-account CV collapses to the naive 1/√N (G3 uniform–naive gap 5e-06), so only size concentration (HHI) moves the dispersion?

## Outcome — APPROVE (exact reproduction)
- Verifier copied **byte-identical** from idea-engine `ideas/venture-lab/usage_based_billing_variance_shock.py` — `diff` exit **0**, file sha256 `e9945e69f9cf89347f1ac55341f7428e2d4021d3e7c1b21fb9269f52c3160da1` (230 lines / 9576 bytes, git blob `785588ff32930cd13b27f32223d8d519e0c360e2`).
- Reproduced digest `4cd2fd286d5530ce001dc49becb80c29ced6484b834e051be59ed2acbf7a7d6b` **== disclosed digest EXACTLY (MATCH)**. Cross-invocation A + B both printed byte-identical stdout (`diff` exit 0) with this digest, and an in-process double-run (`run()` ×2, compact-hashed in-process) produced the identical results dict AND identical digest both times, both == disclosed; all exit 0.
- **G1 diversification shortfall exists: PASS** — concentrated CV mean **0.096707** vs naive **0.025000**, conc−naive **0.071707**, z=**+116.537002** (≥3σ, one-sided) — the LLN smoothing never arrives.
- **G2 matches the exact closed form: PASS** — concentrated CV mean **0.096707** vs exact CV_account·√HHI **0.097533**, z=**−1.343306**, |z| < 3σ — the shortfall IS the Herfindahl prediction.
- **G3 concentration-driven: PASS** — concentrated CV **0.096707** vs uniform CV **0.025005** (SAME N, SAME total mean, SAME per-account CV), conc−unif **0.071702**, z=**+113.523243** (≥3σ), uniform–naive gap **5e-06** < 0.01 — size concentration is the sole driver.
- Closed forms (all reproduced): HHI_concentrated=**0.038051**, N_eff=**26.280443**, CV(R)_exact=**0.097533**, HHI_uniform=**0.0025**, CV_naive_lln=**0.025000**, volatility_multiplier=**3.901339** (=CV(R)_exact/CV_naive); all_pass=**true**, exit **0**, first-failing gate **none**. This final flip to `complete` is the LAST commit that releases the born-red HOLD; landing via merge-on-green, zero agent merge calls. idea-engine mirror: branch `claude/verdict-151-mirror`. Full probe evidence: `sims/verdict-151-billing-variance-shock/probe-report.md`. **APPROVE.**

## ⟲ Previous-session review
Prior loop landed VERDICT 150 (P137 the service-variance wait tax, round-32 FLEET slot OPENER, +13, sim-lab PR #224 merged `8c9af13`, digest `ab44d56a…91e4307`) — APPROVE, byte-identical reproduction across cross-invocation A/B + an in-process double-run, all three gates PASS in order (G1 M/D/1 C²=0 grand-mean 2.002221 vs P-K anchor 2.0 z=+0.496, G2 M/M/1 C²=1 3.987717 vs 4.0 z=−0.934, G3 M/H2/1 C²=4 9.921619 vs 10.0 z=−1.838). It anchored every gate on the exact Pollaczek–Khinchine closed form so the variance tax was structural, not a magnitude gap. DIGEST-POSTURE carry-forward: V150 and this V151 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (results dict carries no `results_sha256`, no file written, compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form) and both put the compute entry in `run()`, so the in-process double-run calls `run()` twice directly — the V150 mechanics carry straight over. Object shift: V150 was a QUEUEING / M/G/1 Lindley-recursion object whose valid standard error demanded the METHOD OF INDEPENDENT REPLICATIONS on autocorrelated waits; this V151 is a REVENUE-CONCENTRATION / second-moment object where the SE comes from BATCH MEANS (60 independent batches, one CV ratio estimate each) precisely because the sampling SE of a ratio estimator CV_hat=s/x̄ has no clean normal-theory form — a distribution-free between-batch spread, a different variance-estimation posture than V150's replication reps but the same independent-batch principle. Both share the "SAME-mean control isolates the driver" move: V150 held ρ and E[S] fixed to isolate C²; this V151 holds N, total mean, and per-account CV fixed (uniform vs Zipf) to isolate HHI. NON-CONTIGUITY note for the successor: V151 (P138, round-32 VENTURE slot) lands above the V150 high-water but V137 (P124), V132 (P119), V126 (P113 mirror #527) remain open below — do not read "V151 landed" as advancing every lower verdict to closed on both sides. Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the variance shock at a SINGLE size law (Zipf α=1, mᵢ=1/i) at a SINGLE book size (N=400), reading N_eff=1/HHI off one concentration point. A genuinely distinct NEXT venture-slot slice, distinct from this fixed-law single-point head, pins the **HHI–tail-index scaling law and the concentration-limited diversification frontier**: because HHI for a power-law book mᵢ∝i^(−α) has a closed asymptotic in the tail exponent α (HHI→ a finite constant as N→∞ for α>½, so N_eff SATURATES at a book-size-independent ceiling), the diversification a firm can ever buy is capped by the tail heaviness, not the customer count. The pre-registrable structure: (G-a) sweep α∈{0.5,0.8,1.0,1.2} at fixed N and confirm each book's sim CV lands on CV_account·√HHI(α) within 3σ (the closed form holds across tail indices, not just α=1); (G-b) sweep N∈{100,400,1600,6400} at fixed α>½ and confirm N_eff saturates to its α-determined ceiling — adding accounts past the knee buys ZERO further diversification (mean CV flat within 3σ across the top two N), the sharp counter to "just add more customers"; (G-c) a light-tail control (α<½, or a bounded/uniform book) confirms N_eff grows ∝N so the naive 1/√N is recovered exactly, bracketing the mechanism to heavy tails. The operator lesson sharpens from "a concentrated book is 3.9× more volatile than the count suggests" to "the tail index sets a hard diversification ceiling: for a heavy-tailed (α>½) book, revenue dispersion is floored by concentration no matter how many accounts you add, so cut the tail (cap single-account share, diversify the size distribution) — you cannot out-count it." A seed-pinnable concentration mechanism deliberately distinct from this fixed-α single-N head, and distinct from P109 correlated-fleet-variance-floor (a CORRELATION-driven ρσ² floor on identical units; this is SIZE-HETEROGENEITY on independent accounts) and P134 blended-churn LTV understatement (Jensen convexity of a POINT estimate; this is a second-moment RISK result whose point mean is unbiased).

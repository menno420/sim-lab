# VERDICT 124 — the matchmaking win-rate mirage: skill-based matchmaking (SBMM) pairs you against opponents of your own Elo rating, and the symmetric Elo update is a stochastic-approximation loop that drives your rating to the fixed point where your EXPECTED SCORE against the matched near-rating pool is σ(0)=0.5 — so at equilibrium EVERY player, regardless of true skill, has an expected win rate ~0.5 and the observed win rate is COMPRESSED almost flat across skill (skill is expressed as the equilibrium RATING, not as a win-rate surplus); a same-seed two-regime control isolates the cause: run the SAME players with the SAME skills under RANDOM matching and their win rate becomes a near-perfect skill signal (top-vs-bottom skill-decile win-rate gap ~0.72, corr(skill,winrate)~0.98), but under SBMM the decile gap collapses to ~0.04 (~17× compression) even though the identical games' RATING recovers true skill with corr~0.98 — so it is the MATCHMAKING, not the skill model or the estimator, that turns win rate into a mirage; the skill information is not destroyed, it is relocated out of the win rate and into the rating (the residual corr(skill,SBMM-winrate)~0.5 is a finite-population tail edge effect — extreme players match only inward — so the headline is the compressed decile GAP and the corr CONTRAST, not a claim that win rate is literally uncorrelated with skill); the transferable correction is to rank players, tune matchmade bots, and A/B competitive agents by the RATING (or rating velocity toward equilibrium), read a flat win rate across skill as the SIGNATURE of correct matchmaking rather than proof skill is irrelevant, and publish the rating — not the win rate — as the skill-legible scoreboard; a competitive SBMM rating-system informational-economics game head (round-25 GAME slot), P111 → V124 (+13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · high · review/verify

Born in-progress as this session's FIRST commit (born-red HOLD); it stays in-progress until the reproduction artifacts + outbox block + heartbeat land and the close-out is written — the flip to `complete` is a deliberate LATER step, not part of this born-red opening commit. The `in-progress` badge is precisely what holds `substrate-gate` red so the PR cannot merge before the deliberate flip.

## Objective
Reproduce idea-engine PROPOSAL 111 (outbox `## PROPOSAL 111 · 2026-07-18T02:41:53Z · status: sim-ready`, round-25 GAME-slot), offset +13 (P111 → V124): on the pinned world (SEED=20260717, N_PLAYERS=200, GAMES=600, BURN_IN=400, RANDOM_GAMES=200, SKILL_SD=250.0, ELO_SCALE=400.0, K_FACTOR=16.0, R0=1500.0, MATCH_JITTER=15.0, TRIALS=200, SIGMA_GATE=3.0; thresholds GAP_MAX=0.12 (G1), RANDOM_GAP_MIN=0.50 + DELTA_MIN=0.40 (G2), RATING_CORR_MIN=0.90 + CONTRAST_MIN=0.30 (G3)), does a stdlib per-replication SBMM Elo season (600 rounds of rating-nearest-neighbour matching with ±15 Elo jitter, K=16 symmetric Elo update, win rate counted only after 400 burn-in rounds, final rating read at end) plus a same-players RANDOM-matching control (200 rounds of uniform-random pairings) over a Normal(0,250) true-skill population show (G1) win-rate compression headline — the SBMM top-minus-bottom skill-decile mean win-rate GAP ≤ GAP_MAX=0.12 by ≥3σ (best and worst players win nearly the same fraction under matchmaking); (G2) matchmaking-causes-it control — the RANDOM-matching decile win-rate gap ≥ RANDOM_GAP_MIN=0.50 AND exceeds the SBMM gap by ≥ DELTA_MIN=0.40, both by ≥3σ (win rate DOES encode skill absent matchmaking, so the compression is caused by the pairing rule, not the skill model or the estimator); and (G3) rating-retains-skill mirage — the mean (corr(skill, final rating) − corr(skill, SBMM win rate)) ≥ CONTRAST_MIN=0.30 by ≥3σ with corr(skill, rating) ≥ RATING_CORR_MIN=0.90 (the identical games' hidden rating keeps the skill signal the win rate loses)? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/superbot-games/matchmaking_winrate_mirage.py`) VERBATIM under SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, and confirm the results-dict sha256 reproduces `835b8e6cc9cd6a5eae699d7ea963dda3a6c3980fb6fd2de3cee6fce26056427c` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 111 @ `control/outbox.md` source header verbatim `## PROPOSAL 111 · 2026-07-18T02:41:53Z · status: sim-ready` (read at idea-engine HEAD `ceac42da8ecb15ace72f0e60491406f22f3d99db`); reference verifier `ideas/superbot-games/matchmaking_winrate_mirage.py` present at that HEAD (landed via idea-engine PR #518 @ `ceac42d`), git blob sha `23733d2bd16a7c9a44e17c627272cdbeaf1e766c` (`git hash-object`), file sha256 `eba338d835880edc8c3a466c182b1926154fa2fe61854d79a0478fbd3c63db3e`, stdlib-only (random, math, json, hashlib — no numpy/scipy), SEED=20260717 pinned in-file (line 48). sim-lab work branch `claude/verdict-124-matchmaking-winrate-mirage` off origin/main HEAD `7d71fbec9fd35e0151345cf69703e5593903b653`. Verifier copied BYTE-IDENTICAL into `sims/verdict-124-matchmaking-winrate-mirage/matchmaking_winrate_mirage.py` (diff exit 0 vs the idea-engine source; both git blob `23733d2bd16a7c9a44e17c627272cdbeaf1e766c`, file sha256 `eba338d8…3c63db3e`). Disclosed & reproduced canonical results-dict digest `835b8e6cc9cd6a5eae699d7ea963dda3a6c3980fb6fd2de3cee6fce26056427c`; landing values (SEED-pinned): corr(skill, final rating)=0.984387 (se 0.000142), corr(skill, SBMM win rate)=0.522507 (se 0.005137), corr(skill, RANDOM win rate)=0.982044 (se 0.000201), SBMM decile win-rate gap=0.041946 (se 0.000131), RANDOM decile win-rate gap=0.722953 (se 0.001272), gap_delta=0.681006 (se 0.001274), contrast=0.46188 (se 0.005117); G1 win-rate compression z=596.5172σ (≤ GAP_MAX 0.12), G2 matchmaking-causes-it z_level=175.2561σ (≥ 0.50) AND z_delta=220.6321σ (≥ 0.40), G3 rating-retains-skill z=31.6339σ (≥ 0.30) with corr_rating 0.984387 ≥ 0.90 — all PASS, exit 0, byte-identical across two cross-invocations (stdout diff exit 0). Offset authority = idea-engine control/outbox.md per-block ledger + control/status.md (docs/current-state.md is a SEAT-DORMANT snapshot through V059 and carries NO P111/V124 row) — P111 ↔ V124 confirmed +13 (predecessor row P110 → V123; baton note "next: V124 for P111; then round-25 unrelated-slot P112").

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict enforced (the seeded run is reproduced back-to-back across two separate `python3` invocations in fresh directories, stdout diff exit 0; the verifier's own TRIALS=200 in-process replication loop reproduces the digest identically).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114–V123, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin at line 48, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2/G3, evaluated in order G1→G2→G3 at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104/P105/P106/P107/P108/P109/P110 /se convention)
- G1 win-rate compression (headline): the SBMM top-minus-bottom skill-decile mean win-rate GAP ≤ GAP_MAX=0.12 by ≥3σ — under matchmaking the best and worst players win nearly the same fraction (disclosed SBMM gap 0.041946, z=596.5172σ).
- G2 matchmaking-causes-it (control): the RANDOM-matching decile win-rate gap ≥ RANDOM_GAP_MIN=0.50 AND exceeds the SBMM gap by ≥ DELTA_MIN=0.40, both by ≥3σ (disclosed random gap 0.722953 z_level=175.2561σ, gap_delta 0.681006 z_delta=220.6321σ) — win rate DOES encode skill absent matchmaking, so the compression is caused by the pairing rule, not the skill model or the estimator (the load-bearing same-seed control).
- G3 rating-retains-skill mirage: the mean (corr(skill, final rating) − corr(skill, SBMM win rate)) ≥ CONTRAST_MIN=0.30 by ≥3σ with corr(skill, rating) ≥ RATING_CORR_MIN=0.90 (disclosed contrast 0.46188 z=31.6339σ, corr_rating 0.984387) — the identical games' hidden rating keeps the skill signal the win rate loses.
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order (the verifier's `all_pass` field encodes exactly G1 ∧ G2 ∧ G3); verdict never softened.

## Outcome — APPROVE (exact reproduction)
APPROVE — first-failing-gate None. The verifier is copied VERBATIM into `sims/verdict-124-matchmaking-winrate-mirage/matchmaking_winrate_mirage.py` (byte-identity proven by `diff` exit 0 + matching `git hash-object` blob `23733d2bd16a7c9a44e17c627272cdbeaf1e766c` + matching `sha256sum` `eba338d8…3c63db3e` against idea-engine's committed source), run under the pinned SEED=20260717 across two cross-invocations in fresh directories (byte-identical — stdout diff exit 0) plus the verifier's own TRIALS=200 in-process replication, exit 0. The results-dict sha256 `835b8e6cc9cd6a5eae699d7ea963dda3a6c3980fb6fd2de3cee6fce26056427c` reproduces the disclosed expected digest EXACTLY (all 64 hex — the script-printed `Results-JSON sha256:` line, an independent recompute of `json.dumps(results, sort_keys=True, separators=(",",":"))` with the `results_sha256` self-field OMITTED, and the disclosed digest all EQUAL). All three pre-registered gates PASS in order on the /se margin: G1 win-rate compression SBMM decile gap 0.041946 (se 0.000131) ≤ GAP_MAX=0.12 by z=596.5172σ — the top and bottom skill deciles win nearly the same fraction under matchmaking; G2 matchmaking-causes-it random gap 0.722953 (se 0.001272) ≥ RANDOM_GAP_MIN=0.50 by z_level=175.2561σ AND exceeds the SBMM gap by gap_delta 0.681006 (se 0.001274) ≥ DELTA_MIN=0.40 by z_delta=220.6321σ — the SAME players' win rate is a near-perfect skill signal under random matching (decile gap 0.722953, corr 0.982044), so the ~17× compression (0.722953 → 0.041946) is caused by the matchmaking pairing rule, not the skill model or estimator; G3 rating-retains-skill contrast (0.984387 − 0.522507) = 0.46188 (se 0.005117) ≥ CONTRAST_MIN=0.30 by z=31.6339σ with corr_rating 0.984387 ≥ RATING_CORR_MIN=0.90 — the identical games' hidden rating recovers true skill (corr 0.984387) while the win rate barely tracks it under SBMM (corr 0.522507), so the skill information is relocated into the rating, not destroyed. all_pass=true. The three headline claims land to the digit: SBMM decile gap 0.041946 vs random 0.722953 (~17× compression), corr(skill,rating) 0.984387 (~0.98). The verdict recommendation is recorded APPROVE (diff exit 0 AND digest exact-match AND G1∧G2∧G3 in order), never softened. The card badge stays `in-progress` to hold `substrate-gate` red until the deliberate close-out flip to `complete` releases merge-on-green.

## ⟲ Previous-session review
Prior loop P110 → V123 (the discount-depth breakeven trap — a price cut of depth d on gross margin m needs a unit-volume uplift of exactly u*(d)=d/(m−d) just to hold gross profit, CONVEX with a vertical asymptote at d=m, so a 20%-off promo at a 40% margin destroys ~45% of gross profit even as unit sales rise; the discount pays only above the breakeven elasticity λ*=ln(m/(m−d))/(P0·d)) landed APPROVE — sims/verdict-123-discount-depth-breakeven/, reproduced the committed verifier byte-identical with results-dict sha256 `fd870ec6310b687a0a7ad966773404db56401b882116239c9803b405ce927ee0` matching the disclosed digest EXACTLY, all three gates PASS (G1 13287.6583σ, G2 250.4225σ, G3 |z|=1.1706/2.0785/1.1706). One substantive carry-forward — the DIGEST-POSTURE distinction: V123's card confirmed P110 hashes the results dict AS-IS and prints the digest as a sibling stdout `sha256:` line with NO self-field inside results (the P105/P109-style SIBLING-key posture, NO field-omission). P111 here is the OPPOSITE posture — the P104/P107/P108-style SELF-FIELD posture: `main()` computes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` (line 251) and `digest = sha256(canonical)` (line 252) over the results dict BEFORE inserting `results["results_sha256"] = digest` (line 253), so the disclosed digest is over the results dict with the `results_sha256` self-field OMITTED, and the pretty-printed on-disk `matchmaking_winrate_mirage_results.json` artifact's own `sha256sum` is NOT this digest (that artifact embeds the self-field). This slice therefore applied the field-omission step when independently recomputing from the on-disk dict (drop `results_sha256`, re-`json.dumps` canonical, re-hash) — confirmed against the script text (lines 251–253), not assumed: the three-way equality (script-printed `Results-JSON sha256:` line == independent field-omitted recompute == disclosed digest) landed on `835b8e6c…56427c`, so the guard was applied and cleared, not tripped. Guard recipe for the successor: before recomputing any results-dict digest, read the verifier's `main()` and classify the posture — SELF-FIELD (results_sha256 inserted after hashing → omit it before recompute, e.g. P104/P107/P108/P111) vs SIBLING-key (a `{"results":…, "sha256":…}` wrapper, hash results as-is → no omission, e.g. P105/P109/P110) — the on-disk pretty JSON's own `sha256sum` equals the disclosed digest ONLY in the sibling-key case, never in the self-field case.

## 💡 Session idea
This slice pins a SINGLE convergent rating system (symmetric Elo, single K=16, one burn-in) and gates the win-rate mirage at the CONVERGED regime — win rate measured only after 400 burn-in rounds, so ratings sit at their σ(0)=0.5 fixed point. But the proposal's own model-basis note flags a load-bearing TRANSIENT: "a season too short would leave ratings far from equilibrium and INFLATE corr(skill,winrate) — the shorter the convergence, the less compression." That transient is not just a caveat to burn away — it is a MEASURABLE, operationally decisive regime, because a real ladder is never fully converged (new players, smurfs, seasonal resets, rating decay all inject fresh disequilibrium continuously). A genuinely different next GAME slice pins the CONVERGENCE-TRANSIENT of the mirage: hold the same SBMM Elo season but SWEEP the burn-in / games-played axis and gate the win-rate informativeness as a function of games played, pre-registering (G-a) a monotone-decay gate — corr(skill, win rate) and the decile gap DECREASE monotonically with games-played toward the converged floor by ≥3σ per sweep step (win rate is MOST informative early, and matchmaking progressively erases it as the rating converges — the exact opposite of the folk intuition that "more games = more reliable win rate"); (G-b) a half-life gate — the number of games at which the win-rate decile gap has decayed halfway from its random-matching level to its converged floor is finite and reproduced within 3σ against a closed-form / fitted exponential-approach constant tied to K and SKILL_SD (so an operator can compute "win rate is trustworthy for the first N games, then becomes a mirage"); and (G-c) a smurf/reset control — inject a batch of mis-rated players (true skill ≫ current rating) and show their TRANSIENT win rate spikes far above 0.5 and decays to 0.5 as the rating climbs, gateable by a ≥3σ transient-surplus that a converged player never shows — proving a sustained win-rate surplus is a rating-disequilibrium signal (still climbing), exactly as the proposal asserts but does not gate. That converts this slice's converged-regime "win rate is a mirage, grade by rating" into the actionable, ≥3σ-gateable "win rate is informative ONLY in the transient, with a computable half-life; a win-rate surplus is a not-yet-converged-rating signal, so grade by rating velocity early and by rating level once converged" — a seed-pinnable convergence-transient mechanism distinct from this slice's converged-regime compression and from the outbox's static two-regime contrast.

# Session — VERDICT 005 · INTAKE 005 — capability self-awareness probe (two-seat)

> **Status:** `in-progress`
> 📊 Model: Opus-family (Claude Code) · 2026-07-10 · VERDICT 005 session
> Objective: settle INTAKE 005 (idea-engine PROPOSAL 005 @ e0f207d5; canonical superbot capability-self-awareness idea @ 9624c53) — can an automated two-plane capability probe produce an HONEST whole-repo CAPABILITIES.md, or does per-seat variance make single-file regeneration a laundering of one seat's world into "the repo's abilities" — and ship the measured-prototype subtree.

## What happened

Born-red card, first commit of the VERDICT 005 session (heartbeat-before-work,
CONVENTIONS §"Heartbeat-before-work"; born-red per §"Born-red session card").
As a newly-ADDED card it is the substrate-gate's advisory sentinel, so the READY
PR is expected to pass green; to be flipped to `complete` as the deliberate close-out.

Shipped `sims/verdict-005-capability-self-probe/` — a MEASURED PROTOTYPE (ladder rung
2). A stdlib two-plane read-only battery (`probe.py`) run from TWO documented seat types
(coordinator + spawned worker — the OA-003 create_trigger/send_later split), frozen into
`runs/`, and a deterministic self-checking `analyze.py` (394 self-checks) that computes
internal agreement, seat divergence, and a three-way diff (seat A vs seat B vs the
hand-maintained ledger baseline), emitting the proposed `CAPABILITIES.v1` schema.

**Load-bearing result:** the two seats' five seat-defining agent-plane tools are DISJOINT
(measured divergence = 5/9) and each is a FALSE WALL from the other seat's single-seat
view (5 false walls); a naive present/absent projection also launders 1 FALSE CAPABILITY
(`create_trigger` visible while its cross-session bind is org-walled, OA-003). Meanwhile
the subprocess plane is seat-invariant and read-only-probeable, unanimous across 5 runs
(agreement 1.00) and reproduces the ledgered walls EXACTLY (0 false walls). Points to:
probe must annotate per-seat-type sections; regenerate-whole-file from one seat is NOT
honest. Final verdict wording left `[COORDINATOR TO FINALIZE]`.

## Run command

```
python3 sims/verdict-005-capability-self-probe/analyze.py
```
Deterministic, stdlib-only, exit 0 iff all 394 self-checks pass — recomputes every
headline from the frozen `runs/` and emits `CAPABILITIES.json` + `CAPABILITIES.md`. The
live probe layer (`probe.py` + seat self-enumerations) is NOT bit-reproducible; the
analyzer is (byte-identical md5 on re-run).

## 💡 Session idea

The `CAPABILITIES.v1` schema this verdict names is a `harness/` candidate: a re-pointable
two-plane probe + deterministic three-way-diff analyzer that any lane could run at
session-start to emit an honest, seat-annotated capability manifest — the standing second
product the README §"harness/" invites, proven same-PR by this sim.

## ⟲ Previous-session review

VERDICT 004 (INTAKE 004, numeric sim) shipped a rung-1 seeded sweep with one deterministic
command + self-checks, carrying its magnitude half as an explicit hypothesis. Applied here:
same discipline at a different rung — the live capability layer is NOT seed-deterministic,
so the reproducible artifact is the frozen `runs/` + a deterministic `analyze.py`, and the
report defines reproducibility as agreement-rate bounds over N runs (not bit-identity of the
live layer), labeling generality-beyond-this-container JUDGMENT-ONLY rather than measured.

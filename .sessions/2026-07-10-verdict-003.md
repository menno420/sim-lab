# Session — VERDICT 003 · INTAKE 002 — Discord OAuth trust-gate adversarial verification

> **Status:** `complete`
> 📊 Model: Opus-family (Claude Code) · 2026-07-10 · VERDICT 003 session
> Objective: settle INTAKE 002 (idea-engine PROPOSAL 002 @ e77fa46; idea §5/§4 @ 3e01311) — does the Discord OAuth trust-gate for per-server personal stats hold up under evidence-based adversarial verification — and ship the spike + JUDGMENT-ONLY subtree.

## What happened

Born-red card, first commit of the VERDICT 003 session (heartbeat-before-work,
CONVENTIONS §"Heartbeat-before-work"; born-red per §"Born-red session card").
As a newly-ADDED card it is the substrate-gate's advisory sentinel, so the READY
PR is expected to pass green; flipped to `complete` as the deliberate close-out.

Shipped `sims/intake-002-oauth-trust-gate/` — a MEASURED SPIKE (ladder rung 2) for
the executable attack suite plus explicitly-labelled JUDGMENT-ONLY analysis (rung 3)
for threats not exercisable from this environment. A runnable model of the Discord
authorization-code OAuth trust-gate (§5) with a reference implementation of all six
§5 controls, then 13 executed adversarial attacks against it, multi-seed (5 seeds)
and rate-limit swept (3×3). Every modelled attack is DEFEATED; two attack classes
surface HOLES (stale guild membership; `guilds` over-read) → named changes.

**Verdict: `needs-more-evidence` — ruling buildable-with-named-changes.** The trust
gate can be made trustworthy (the spike shows the concrete controls), but §5 as
written is a checklist not a spec, several threats are JUDGMENT-ONLY hypotheses for
launch-time live tests, and the §4 superbot read-only API is UNBUILT AND UNROUTED —
phase 3 deadlocks regardless of the OAuth verdict. Phases 1–2 carry no auth surface
and do not wait. Evidence: prototype + JUDGMENT-ONLY; gate PASS on the executed
spike claims.

## Run command

```
python3 sims/intake-002-oauth-trust-gate/oauth_trust_gate_sim.py
```
Deterministic, stdlib-only, exit 0 iff all self-checks + all modelled attacks resolve
as designed (13 attacks defeated, 2 holes reproduced, 5-seed stable, rate-limit swept).

## 💡 Session idea

The whole spike is a reference OAuth relying-party in ~200 lines of stdlib — the
reusable core (state store, PKCE, scope-fail-closed, IDOR-by-session, token bucket)
is a `harness/` candidate: a "trust-gate attack rig" any future personalization
feature on any lane site could re-point at its own flow.

## ⟲ Previous-session review

VERDICT 002 (INTAKE 001, measured prototype) made the non-reproducible LLM layer
honest by freezing runs + a deterministic analyzer. Applied here: this spike is fully
seeded/deterministic (rung 2 with rung-1 reproducibility), and the parts that CANNOT
be measured from here (live Discord IdP, TLS/MITM, Railway hosting, the unbuilt §4
API) are labelled JUDGMENT-ONLY and listed as launch-time live tests — never dressed
as measurement.

# VERDICT 232 — Nim / Sprague–Grundy: a disjunctive-sum position is a loss for the player to move iff the XOR ("nim-sum") of its component Grundy values is zero, and Sub({1..k}) has the exact Grundy closed form G(n)=n mod (k+1) — reproduce PROPOSAL 219

> **Status:** `in-progress`

- **📊 Model:** opus-4.8 · high · review/verify

started: 2026-07-20T16:40:00Z

💓 Heartbeat: round-52 GAME slice P219 → V232 (+13); reproduction on branch `claude/verdict-232-nim-sprague-grundy`; sim dir `sims/verdict-232-sprague-grundy-nim-sum/` (verifier + run-stdout.txt staged by proposal PR #309; this slice adds probe-report.md). Digest target full-64 `e50e461d…9002b7` (printed AND literal-string compared, no truncation). Determinism CONFIRMED (in-process double-run guard AND separate cross-invocation byte-identical). Six pre-registered gates each in its own direction — G1 Monte-Carlo AGREEMENT (|z|<3, z=1.446904), G2 exact Fraction density ==1/8 (64/512), G3 Sub({1,2,3}) G(n)==n mod 4 zero-mismatch on [0,256], G4 disjunctive-sum G_sum==G(a)⊕G(b) zero-mismatch on [0,40]², G5 robustness G(n)==n mod (k+1) for k∈{2,3,4,5}, G6 FALSIFIABILITY naive total-parity rule REJECTED at |z|>3 (z=−334.45). Grounding sha1-pinned to Nim oldid 1362772636 (750a8fd6…) and Sprague–Grundy oldid 1362556548 (818faacd…). Born-red HOLD armed on this first card commit; released only by the deliberate `complete` flip LAST. VERDICT 232 = APPROVE.

⏳ Flip note (born-red): this card ships `> **Status:** `in-progress`` on its FIRST commit so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only after the probe-report (byte-identical verifier reproduction, full-64 digest match, six-gate evaluation each in its own direction, determinism, grounding) has landed. That flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation merge the green PR.

## What this verdict does

Reproduces PROPOSAL 219 (P219 → V232, +13 offset, lane GAME of the round-52 fleet→venture→game→unrelated cycle): in an impartial combinatorial game a disjunctive-sum position is a **loss for the player to move (a P-position) iff the XOR ("nim-sum") of its component Grundy values is zero** (Sprague–Grundy), and the single-heap subtraction game Sub({1,…,k}) has the exact Grundy closed form **G(n) = n mod (k+1)**. The verifier `sprague_grundy_nim_sum.py` (byte-identical copy already in `sims/verdict-232-sprague-grundy-nim-sum/`) reproduces the results-dict sha256, confirms determinism, evaluates six pre-registered gates each in its own direction, and the probe checks the byte-pinned grounding against the two pinned Wikipedia revisions.

## Method

- Byte-identical verifier copy (`diff` source↔copy exit 0), stdlib-only (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`).
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the digest (`json.dumps(r, sort_keys=True, separators=(",",":"))` → `hashlib.sha256`); the dict carries no hash of itself; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target full-64 `e50e461d105e4984f6f562def0eba3f527ef4030512f9cf75294ddd6709002b7` (literal 64-char compare, no truncation).
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria) — see probe-report.md for observed values.
- Grounding byte-pinned to the `action=raw` wikitext of the two revisions; sha1 compared to the MediaWiki-reported rev sha1.

## ⟲ Previous-session review

Previous-session review: VERDICT 230 (consistent-hashing max-gap = H_n/n — n uniform points on a unit ring leave the busiest node owning an expected largest arc of exactly H_n/n, a factor H_n ≈ ln n above the fair 1/n share; P217 → V230) landed APPROVE with a full-64 digest match (`41394b56…84ac`) and all gates PASS via the born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction/integer identity is a self-certifying theorem, a z-test is an AGREEMENT gate when the closed form is the null, a robustness sweep is a CONVERGENCE gate, and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate. V232 leans on that same discipline: G1 is a Monte-Carlo AGREEMENT gate (|z|<3), G2 an exact Fraction equality (near-definitional — flagged SOFT), G3/G4/G5 zero-mismatch exact-Grundy identities (the firsthand teeth, since the closed form is NOT on Wikipedia), and G6 a FALSIFIABILITY gate (|z|>3, opposite direction to G1). This verdict also carries the V222 grounding-accuracy assessment: the caveat must honestly separate "firsthand-proven" (the closed-form Grundy equation) from "cited motivation" (the XOR/mex framework the pages do state). Standing non-contiguity persists: landing V232 does not imply every lower verdict below the high-water is closed.

## 💡 Session idea

The verifier pins the Sub({1..k}) Grundy closed form and the disjunctive-sum XOR rule for single-move-set heaps. A cheap orthogonal follow-on (P-next) would pin the **nimber period of a general finite subtraction set** S: Sprague–Grundy theory guarantees the nim-sequence of any finite subtraction game is eventually periodic, and for S={1..k} the period is exactly k+1 with zero pre-period. Gate: an exact zero-mismatch check that for a sweep of finite sets S (e.g. {1,3,4}, {2,5,6}) the computed nim-sequence enters its detected period p within pre-period n0, plus a FALSIFIABILITY leg that a deliberately-wrong "period = max(S)+1 for all S" claim is rejected on at least one S. Guard recipe for a later session: the anchor is `grundy_subtraction(k, upto)` — generalize `moves` from `range(1,k+1)` to an arbitrary `sorted(S)`, add a `detect_period(seq)` reducer, and target `period({1..k}) == k+1` (continuity leg, must reproduce this verdict) plus a non-trivial firsthand `period({1,3,4})`.

# Session — sim-lab BOOT (ORDER 000 walking skeleton)

> **Status:** `in-progress`
> 📊 Model: Opus-family (Claude Code) · 2026-07-10 · ORDER 000 boot session
> Objective: boot verification, ORDER 000 walking skeleton, REFERENCE exemplar, intake queue.

## What happened

Born-red card, first commit of the sim-lab boot session (heartbeat-before-work,
CONVENTIONS §"Heartbeat-before-work"). Opened `in-progress` per the born-red
convention (CONVENTIONS §"Born-red session card"): the card exists before the
work, to be flipped to `complete` as the deliberate final step.

ORDER 000 — the walking skeleton — proves the merge path end to end: branch →
READY PR → `substrate-gate` → merge on green. This card is the smallest real,
gate-legal change to exercise that path (a newly-ADDED born-red card is the
gate's advisory sentinel, so the PR is expected to pass green).

## 💡 Session idea

The walking skeleton doubles as the REFERENCE exemplar: record EXACTLY which
landing path worked (auto-merge armed vs REST/MCP merge-on-green) in
`control/status.md`, so no later session re-derives the merge authority.

## ⟲ Previous-session review

The seed session (`.sessions/2026-07-10-seed.md`) deliberately left the walking
skeleton (branch → PR → gate → merge) to this boot as ORDER 000 — the seed was
the empty-repo direct push a ruleset cannot cover. Applied here: this card lands
born-red as the session's first commit, nothing rides behind the gate.

# VERDICT 172 — drop-rate median gap: in a loot system where each kill is an independent Bernoulli trial with a fixed per-kill drop probability p (a "1-in-N" drop, p=1/N), the number of kills K until the first drop is Geometric(p), whose distribution is right-skewed — its mean E[K]=1/p=N sits strictly ABOVE its median med(K)=⌈ln2/(−ln(1−p))⌉≈0.6931·N, so the folk "a 1-in-N drop rate means it takes about N kills to get the drop" belief conflates the MEAN with the TYPICAL run and is FALSE — the median player gets the drop in ~0.693·N kills (MORE THAN HALF finish before the "expected" count) while a heavy right tail (≈37% still empty-handed at N kills, since (1−1/N)^N→1/e) pulls the mean up, so the ratio mean/median→1/ln2≈1.4427 and the mean-median gap fraction (mean−median)/mean→1−ln2≈0.3069 — the "expected" count is a mean-of-a-skewed-law artifact, not the typical grind — but the gap MAGNITUDE is a distribution property of the geometric (memoryless per-kill) law, so a different per-kill model moves it; the read is a gap-SIGN / order-of-magnitude claim, not a universal fixed ratio; the mean STRICTLY overstates the typical grind in SIGN and magnitude, but where the exact ratio sits is a disclosed model-dependent field

Reproduce PROPOSAL 159 (round-37 GAME slot, P159 → V172, +13): drop-rate median gap / geometric skew. Under the pinned world each of TRIALS independent players grinds a loot table where every kill is an independent Bernoulli(p) trial; the kill-count-to-first-drop K per player is drawn empirically (counted by simulating kills until a drop lands, not plugged from the closed form), and per batch of players the MEAN kill-count and the MEDIAN kill-count are measured and scored across batches on between-batch z-scores. The folk belief: "a 1-in-N drop rate means it takes about N kills to get the drop — the drop rate is the number of kills you should expect to grind." It is NOT: the geometric law is right-skewed, so the MEAN kill-count E[K]=1/p is pulled up by an unlucky long tail and STRICTLY EXCEEDS the MEDIAN kill-count med(K)≈ln2/p; the typical (median) player finishes in ~0.693·N kills — more than half finish before the "expected" count — while ≈37% are still empty-handed at N kills because (1−1/N)^N→1/e≈0.3679; so the mean-vs-median gap is real, mean/median→1/ln2≈1.4427, and the gap fraction (mean−median)/mean→1−ln2≈0.3069. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 gap-sign (the batch MEAN kill-count strictly exceeds the batch MEDIAN kill-count — mean(mean_K − median_K) > 0, the skew has the right sign) → G2 relative-effect (the gap is a LARGE fraction of the mean, not a rounding error — mean((mean_K − median_K)/mean_K) ≥ 0.10 against a non-zero null 0.10, target ≈ 1−ln2 ≈ 0.3069) → G3 shifted-robustness (the gap survives a rarer-drop re-draw p→p_shift, where both mean and median scale by 1/p but the RATIO mean/median→1/ln2 is preserved and the gap stays strictly positive). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (memoryless per-kill Bernoulli trials → geometric kill-count) — the SIGN (mean > median; the "expected" count overstates the typical grind) and the order of magnitude (a ~30% gap fraction, a ~1.44 ratio) survive across drop rates, but the EXACT ratio 1/ln2 is a property of the geometric law; a per-kill model with memory (pity timers, bad-luck protection, guaranteed-by-N mechanics) moves or collapses the gap, and that model-dependence of the exact ratio is a disclosed descriptive field, not a hidden defect. The declared choices — the memoryless Bernoulli per-kill model, the fixed base p, the median-of-integer-kill-counts convention — change neither the SIGN (mean strictly above median) nor the order-of-magnitude of the gap.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD. The probe-question answers, the Outcome, and the Previous-session review below are PENDING and are completed only at the deliberate flip commit.

## Objective
Reproduce the committed P159 reference verifier byte-identical under its pinned world (SEED=20260717; pinned per-kill base drop probability p_base = 1/N; TRIALS independent batches → between-batch z-scores; kill-count-to-first-drop drawn empirically by simulating Bernoulli(p) kills until a drop lands, MEAN and MEDIAN kill-count measured per batch rather than plugged from the closed form; REL_NULL=0.10 as the G2 null; a rarer-drop shifted re-draw p_shift < p_base for G3; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `<TBD — disclosed P159 digest, to pin at HEAD before flip>`, (b) all three gates hold in order G1 gap-sign → G2 relative-effect → G3 shifted-robustness, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the MEAN kill-count strictly exceeds the MEDIAN kill-count; mean/median→1/ln2≈1.4427, gap fraction→1−ln2≈0.3069). Pre-registered decision rule (P159): APPROVE VERDICT 172 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the EXACT ratio 1/ln2 being a property of the memoryless geometric law (a pity-timer / bad-luck-protection model moves or collapses it) is a DISCLOSED descriptive field of a skew-sign claim, not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude of the gap, a clean APPROVE is warranted. Target: sim-lab (VERDICT 172, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (to verify at HEAD before flip)
- Reference verifier: `ideas/games-lab/drop_rate_median_gap.py` @ idea-engine (path to confirm) — sha256 `<TBD — pin file sha256 at HEAD>`, git blob `<TBD — pin git blob at HEAD>`. The committed P159 source in its games-lab lane, to be copied byte-identically (`diff` exit 0, copy sha256 == source sha256, copy git blob == source git blob) into the reproduction commit.
- Offset authority: +13 (P159 → V172), round-37 GAME slot. Per idea-engine `control/outbox.md` P159 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror) — to confirm at HEAD.
- Domain reference: the geometric-distribution mean/median gap for a fixed per-kill drop rate — the canonical statement that a "1-in-N" drop is memoryless, so the chance of the drop landing within N kills is 1−(1−1/N)^N→1−1/e≈63.2%, the median kill-count is ≈0.693·N, and the arithmetic mean N overstates the typical grind because the right tail is heavy; the mechanism's core (Geometric(p): mean 1/p, median ≈ ln2/p) is textbook and distribution-specific. Grounding link: `<TBD — pin domain source + live-fetch status at HEAD>`.
- Disclosed digest: compact-canonical results-dict sha256 `<TBD — disclosed P159 digest>`, SEED=20260717 — to be reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V170/V171). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the compact canonical JSON + its sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. Posture to be confirmed byte-exact in the reproduction commit.
- Pinned world: SEED=20260717; per-kill base drop probability p_base=1/N; TRIALS independent batches → between-batch z-scores; kill-count-to-first-drop drawn empirically (simulate Bernoulli(p) kills until a drop, count the trials), MEAN and MEDIAN measured per batch; REL_NULL=0.10 (G2 null); rarer-drop shifted re-draw p_shift < p_base (G3); reseeded per run, redrawn per player; every float rounded to 6 dp before serialization. Exact N / TRIALS / p_shift to be pinned from the P159 source at HEAD.

## Constraints honored
Stdlib-only (math, json, hashlib, random, statistics); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The kill-count gap is measured empirically — the mean and median kill-count are obtained by COUNTING Bernoulli(p) kills until a drop lands on each per-player draw, not by plugging the closed forms 1/p and ln2/p, so the gap is attributable to the geometric skew of the drawn kill-counts alone, not to a tautological substitution of the analytic moments. The gates are definitional contrasts on those drawn kill-counts: G1 gap-sign and G2 relative-effect at the base world (p_base); G3 re-draws to a rarer drop rate (p_shift) as a robustness control. Honest scope: the head is EXACT GIVEN THE MODEL and a gap-SIGN / order-of-magnitude claim — it holds for the memoryless (geometric) per-kill law and the exact ratio 1/ln2 is model-dependent (a per-kill model WITH memory — pity timers, bad-luck protection, guaranteed-by-N — moves or collapses the gap, the design-mitigation the phenomenon exists to motivate); that model-dependence of the exact ratio is a disclosed descriptive field, and no gate reads 1/ln2 as a universal law-independent constant.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 gap-sign (z ≥ 3, one-sided):** z = mean(mean_K − median_K)/se vs the null 0 — the batch MEAN kill-count strictly exceeds the batch MEDIAN kill-count (the geometric skew has the right sign; the "expected" count overstates the typical grind). Disclosed gap_mean **<TBD>**, z ≈ **<TBD>**, frac_mean_gt_median **<TBD>** — to be reproduced from the P159 source.
- **G2 relative-effect (z ≥ 3 vs non-zero null 0.10):** z = mean((mean_K − median_K)/mean_K − 0.10)/se — the gap is a LARGE fraction of the mean, not a rounding error (target ≈ 1−ln2 ≈ 0.3069). Disclosed rel_mean **<TBD>** (≈ 30.7% of the mean is skew above the median), z ≈ **<TBD>** against the 0.10 magnitude null — to be reproduced.
- **G3 shifted-robustness (z ≥ 3, one-sided):** re-draw to a rarer drop rate p_shift < p_base and re-check mean(mean_K − median_K) > 0 — the gap survives the non-nominal draw (both moments scale by 1/p but the RATIO mean/median→1/ln2 is preserved and the gap stays strictly positive). Disclosed gap_mean **<TBD>**, z ≈ **<TBD>**, ratio→**<TBD>** (target 1/ln2≈1.4427) — to be reproduced.
- z_gate **3.0** (require z ≥ 3 on every gate, one-sided). **Disclosed caveat (NOT a gate):** the EXACT ratio 1/ln2 is a property of the memoryless geometric law (a per-kill model with memory moves or collapses the gap); the SIGN (mean > median) and the order-of-magnitude (~30% gap fraction) survive across drop rates, but the exact constant is a disclosed descriptive field, and NO gate reads 1/ln2 as a universal law-independent constant.

## Probe questions (independent-audit checklist)
1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest. — **PENDING (completed at flip).**
2. Is the verifier byte-identical to the P159 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity. — **PENDING (completed at flip).**
3. Do all three gates PASS in the pre-registered order G1 gap-sign → G2 relative-effect → G3 shifted-robustness with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0? — **PENDING (completed at flip).**
4. Is the headline read correctly — the MEAN kill-count strictly exceeds the MEDIAN kill-count on the SAME empirically-counted per-player draw, mean/median→1/ln2 and gap fraction→1−ln2 — and NOT as "a 1-in-N drop rate means about N kills to get the drop"? — **PENDING (completed at flip).**
5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact ratio 1/ln2 being a property of the memoryless geometric law (a memory-bearing per-kill model moves it) flips no gate SIGN and no order-of-magnitude of the gap? — **PENDING (completed at flip).**
6. Could the gap sign be a seed fluke? State whether the effect is structural (the mean > median gap holds across the base world and the rarer-drop re-draw, frac_mean_gt_median at ceiling in both) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical). — **PENDING (completed at flip).**

## Outcome
**PENDING — reproduction not yet run.** This section is a placeholder while the card is born-red (`in-progress`); the digest match, the three-gate pass, the closed-form hand-check (mean/median→1/ln2, gap fraction→1−ln2), and the final ruling are recorded here at the deliberate flip commit that sets Status to `complete`. Until then the substrate-gate HOLD holds the PR red by design.

**Verdict: PENDING.**

## ⟲ Previous-session review
**PENDING (completed at flip).** The review of the prior card `2026-07-19-verdict-171-marketplace-rake.md` (V171, P158 marketplace take-rate / rake hump, round-37 venture slot, +13 — landed APPROVE via PR #245), the digest-posture carry-forward, the object-shift note (V171 venture-lab rake hump → this V172 games-lab drop-rate skew), and the contiguity/high-water note are written at the deliberate flip commit, consistent with the born-red discipline (the previous-session review, probe answers, and gate-pass assertions are completed only at the flip; a red gate before the flip is the designed HOLD, not a defect).

## 💡 Session idea
**PENDING (completed at flip).** The distinct-next-slice idea — the pity-timer / bad-luck-protection frontier the head names but holds fixed (a memory-bearing per-kill model that caps the right tail and compresses the mean toward the median), pre-registered under the same SEED-pinned empirically-counted paired-draw discipline — is recorded at the flip commit once the base geometric reproduction holds.

# VERDICT 172 — drop-rate median gap: in a loot system where each kill is an independent Bernoulli trial with a fixed per-kill drop probability p (a "1-in-N" drop, p=1/N), the number of kills K until the first drop is Geometric(p), whose distribution is right-skewed — its mean E[K]=1/p=N sits strictly ABOVE its median med(K)=⌈ln2/(−ln(1−p))⌉≈0.6931·N, so the folk "a 1-in-N drop rate means it takes about N kills to get the drop" belief conflates the MEAN with the TYPICAL run and is FALSE — the median player gets the drop in ~0.693·N kills (MORE THAN HALF finish before the "expected" count) while a heavy right tail (≈37% still empty-handed at N kills, since (1−1/N)^N→1/e) pulls the mean up, so the ratio mean/median→1/ln2≈1.4427 and the mean-median gap fraction (mean−median)/mean→1−ln2≈0.3069 — the "expected" count is a mean-of-a-skewed-law artifact, not the typical grind — but the gap MAGNITUDE is a distribution property of the geometric (memoryless per-kill) law, so a different per-kill model moves it; the read is a gap-SIGN / order-of-magnitude claim, not a universal fixed ratio; the mean STRICTLY overstates the typical grind in SIGN and magnitude, but where the exact ratio sits is a disclosed model-dependent field

Reproduce PROPOSAL 159 (round-37 GAME slot, P159 → V172, +13): drop-rate median gap / geometric skew. Under the pinned world each of TRIALS independent players grinds a loot table where every kill is an independent Bernoulli(p) trial; the kill-count-to-first-drop K per player is drawn empirically (counted by simulating kills until a drop lands, not plugged from the closed form), and per batch of players the MEAN kill-count and the MEDIAN kill-count are measured and scored across batches on between-batch z-scores. The folk belief: "a 1-in-N drop rate means it takes about N kills to get the drop — the drop rate is the number of kills you should expect to grind." It is NOT: the geometric law is right-skewed, so the MEAN kill-count E[K]=1/p is pulled up by an unlucky long tail and STRICTLY EXCEEDS the MEDIAN kill-count med(K)≈ln2/p; the typical (median) player finishes in ~0.693·N kills — more than half finish before the "expected" count — while ≈37% are still empty-handed at N kills because (1−1/N)^N→1/e≈0.3679; so the mean-vs-median gap is real, mean/median→1/ln2≈1.4427, and the gap fraction (mean−median)/mean→1−ln2≈0.3069. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 gap-sign (the batch MEAN kill-count strictly exceeds the batch MEDIAN kill-count — mean(mean_K − median_K) > 0, the skew has the right sign) → G2 relative-effect (the gap is a LARGE fraction of the mean, not a rounding error — mean((mean_K − median_K)/mean_K) ≥ 0.10 against a non-zero null 0.10, target ≈ 1−ln2 ≈ 0.3069) → G3 shifted-robustness (the gap survives a rarer-drop re-draw p→p_shift, where both mean and median scale by 1/p but the RATIO mean/median→1/ln2 is preserved and the gap stays strictly positive). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (memoryless per-kill Bernoulli trials → geometric kill-count) — the SIGN (mean > median; the "expected" count overstates the typical grind) and the order of magnitude (a ~30% gap fraction, a ~1.44 ratio) survive across drop rates, but the EXACT ratio 1/ln2 is a property of the geometric law; a per-kill model with memory (pity timers, bad-luck protection, guaranteed-by-N mechanics) moves or collapses the gap, and that model-dependence of the exact ratio is a disclosed descriptive field, not a hidden defect. The declared choices — the memoryless Bernoulli per-kill model, the fixed base p, the median-of-integer-kill-counts convention — change neither the SIGN (mean strictly above median) nor the order-of-magnitude of the gap.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P159 reference verifier byte-identical under its pinned world (SEED=20260717; pinned per-kill base drop probability p_base = 1/N; TRIALS independent batches → between-batch z-scores; kill-count-to-first-drop drawn empirically by simulating Bernoulli(p) kills until a drop lands, MEAN and MEDIAN kill-count measured per batch rather than plugged from the closed form; the majority-percentile null 0.5 as the G2 null (P(T ≤ mean) > 0.5); a rarer-drop shifted re-draw p'=0.005 < p_base=0.01 for G3; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `ff22cf37b81fb96bb36a1aeb1a4484479d31e1232b637efb0faf297bb852fb19`, (b) all three gates hold in order G1 median-below-mean → G2 mean-is-majority-percentile → G3 scale-free-robustness, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the MEAN kill-count strictly exceeds the MEDIAN kill-count; mean/median→1/ln2≈1.4427, gap fraction→1−ln2≈0.3069). Pre-registered decision rule (P159): APPROVE VERDICT 172 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the EXACT ratio 1/ln2 being a property of the memoryless geometric law (a pity-timer / bad-luck-protection model moves or collapses it) is a DISCLOSED descriptive field of a skew-sign claim, not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude of the gap, a clean APPROVE is warranted. Target: sim-lab (VERDICT 172, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: `ideas/superbot-games/drop_rate_median_gap.py` @ idea-engine — sha256 `42cacd751354569e3f9571b098dc27b85027e271402ed99891a180d798fa6682`, git blob `fe602dda5c0f0924c386423b7d7ca451fc965370`. The committed P159 source in its superbot-games lane, copied byte-identically (`diff` exit 0, copy sha256 == source sha256, copy git blob == source git blob) into the reproduction commit.
- Offset authority: +13 (P159 → V172), round-37 GAME slot. Per idea-engine `control/outbox.md` P159 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror) — to confirm at HEAD.
- Domain reference: the geometric-distribution mean/median gap for a fixed per-kill drop rate — the canonical statement that a "1-in-N" drop is memoryless, so the chance of the drop landing within N kills is 1−(1−1/N)^N→1−1/e≈63.2%, the median kill-count is ≈0.693·N, and the arithmetic mean N overstates the typical grind because the right tail is heavy; the mechanism's core (Geometric(p): mean 1/p, median ≈ ln2/p) is textbook and distribution-specific. Grounding: the geometric-distribution mean/median gap is a textbook, distribution-specific property (Geometric(p): mean 1/p, median ≈ ln2/p, P(T ≤ 1/p) → 1−1/e) needing no external live-fetch this slice — the closed forms are hand-checked against the reproduced Monte-Carlo numbers in the Outcome below.
- Disclosed digest: compact-canonical results-dict sha256 `ff22cf37b81fb96bb36a1aeb1a4484479d31e1232b637efb0faf297bb852fb19`, SEED=20260717 — reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V170/V171). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the compact canonical JSON + its sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. Posture to be confirmed byte-exact in the reproduction commit.
- Pinned world: SEED=20260717 (module constant, line 60); per-kill base drop probability p_base=0.01 (N=1/p=100); a rarer-drop shifted re-draw p'=0.005 (G3); TRIALS=200000 independent players per replication; R=30 replications → between-replication z-scores; kill-count-to-first-drop drawn empirically by inverse-CDF geometric sampling (`geometric_draw`), MEAN and MEDIAN measured per replication; z_gate=3.0, relerr ceiling=0.05; the G2 majority-percentile null is 0.5 (P(T ≤ mean) > 0.5); reseeded per run, redrawn per player; every float rounded to 6 dp before serialization.

## Constraints honored
Stdlib-only (math, json, hashlib, random, statistics); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The kill-count gap is measured empirically — the mean and median kill-count are obtained by COUNTING Bernoulli(p) kills until a drop lands on each per-player draw, not by plugging the closed forms 1/p and ln2/p, so the gap is attributable to the geometric skew of the drawn kill-counts alone, not to a tautological substitution of the analytic moments. The gates are definitional contrasts on those drawn kill-counts: G1 gap-sign and G2 relative-effect at the base world (p_base); G3 re-draws to a rarer drop rate (p_shift) as a robustness control. Honest scope: the head is EXACT GIVEN THE MODEL and a gap-SIGN / order-of-magnitude claim — it holds for the memoryless (geometric) per-kill law and the exact ratio 1/ln2 is model-dependent (a per-kill model WITH memory — pity timers, bad-luck protection, guaranteed-by-N — moves or collapses the gap, the design-mitigation the phenomenon exists to motivate); that model-dependence of the exact ratio is a disclosed descriptive field, and no gate reads 1/ln2 as a universal law-independent constant.

## Gate plan (reproduced at HEAD), order G1 → G2 → G3
Note on framing: the verifier's IMPLEMENTED gates are `median_below_mean` → `mean_is_majority_percentile` → `scale_free_robustness` (below), which are the ground truth the reproduction is judged against; the born-red head above sketched G2 as a "relative-effect ≥ 0.10" gate, but the committed P159 source implements the majority-percentile test (P(T ≤ mean) > 0.5). Both are strictly-positive skew tests; the reproduction is scored on the verifier's actual gates.
- **G1 median-below-mean (z ≥ 3, one-sided; null gap=0):** z_skew = (gap_mc − 0)/se across the R=30 replications — the MEAN kill-count strictly exceeds the MEDIAN (the geometric skew has the right sign; the "expected" count overstates the typical grind). Reproduced z_skew **+369.741566**, gap_mc **0.305865** (the gap fraction (mean−median)/mean → 1−ln2 = 0.306853), ratio_med_mean_mc **0.694135** (→ ln2 = 0.693147), relerr_ratio 0.005993 — **PASS**.
- **G2 mean-is-majority-percentile (z ≥ 3 vs the null 0.5):** z_majority = (frac_le_mean − 0.5)/se — a MAJORITY of players finish AT OR BEFORE the mean kill-count (P(T ≤ mean) > 0.5; the mean sits above the median/50th percentile), target → 1−1/e = 0.632121. Reproduced z_majority **+761.720583**, frac_le_mean_mc **0.634140** (→ 1−1/e), relerr_frac 0.000272 — **PASS**.
- **G3 scale-free-robustness (both z ≥ 3):** re-draw to the rarer rate p'=0.005 and re-check BOTH the skew and the majority-percentile hold — the gap survives the non-nominal draw (both moments scale by 1/p but the RATIO mean/median→ln2 and P(T ≤ mean)→1−1/e are scale-free in p). Reproduced z_skew **+802.499080**, z_majority **+706.081134**, relerr_ratio 0.001487, relerr_frac 0.000494 — **PASS**.
- z_gate **3.0**, relerr ceiling **0.05** (require z ≥ 3 AND relerr ≤ 0.05 on every gate). **Disclosed caveat (NOT a gate):** the exact constants (ratio → ln2, mean-percentile → 1−1/e, the e⁻³ tail ≈ 0.0498 grinding past triple the "expected" count) are properties of the memoryless geometric law (a per-kill model WITH memory — pity timers, bad-luck protection — moves or collapses the gap); the SIGN (mean > median) and the order-of-magnitude (~30% gap fraction) survive across drop rates, but the exact constants are disclosed scale-free-in-p descriptive fields, and NO gate reads them as universal law-independent constants.

## Probe questions (independent-audit checklist)
1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest. — **YES.** Reproduced digest `ff22cf37b81fb96bb36a1aeb1a4484479d31e1232b637efb0faf297bb852fb19` — printed by the script (`json.dumps(res, sort_keys=True, separators=(",",":"))` over the 6-dp dict, then sha256), independently recomputed from the stdout JSON block, and identical to the disclosed value. **MATCH.** Two fresh `python3` invocations produced byte-identical stdout (`diff` exit 0), and the script's in-process `h1 == h2` assert held on every run.
2. Is the verifier byte-identical to the P159 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity. — **YES.** `diff` source vs sim-lab copy exit **0** (byte-identical); copy git blob `fe602dda5c0f0924c386423b7d7ca451fc965370` and file sha256 `42cacd751354569e3f9571b098dc27b85027e271402ed99891a180d798fa6682` both match the pinned P159 identity at `ideas/superbot-games/drop_rate_median_gap.py` exactly.
3. Do all three gates PASS in the pre-registered order G1 median-below-mean → G2 mean-is-majority-percentile → G3 scale-free-robustness with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0? — **YES.** G1 median-below-mean z_skew=+369.741566 (gap_mc 0.305865, ratio_med_mean 0.694135, null gap=0) → G2 mean-is-majority-percentile z_majority=+761.720583 (frac_le_mean 0.634140, null 0.5) → G3 scale-free-robustness (p'=0.005) z_skew=+802.499080 / z_majority=+706.081134 — all ≥ 3 with relerrs ≤ 0.05; `all_pass=true`, `first_failing_gate=null`, exit **0**.
4. Is the headline read correctly — the MEAN kill-count strictly exceeds the MEDIAN kill-count on the SAME empirically-counted per-player draw, mean/median→1/ln2 and gap fraction→1−ln2 — and NOT as "a 1-in-N drop rate means about N kills to get the drop"? — **YES.** At p=0.01 the mean is 100 but the median is 69 (ratio 0.694135 ≈ ln2; gap fraction 0.305865 ≈ 1−ln2), and 63.4% of players finish at or before the mean (→ 1−1/e). Kill-counts are drawn by inverse-CDF geometric sampling (`geometric_draw`) and mean/median are measured on those draws, not plugged from the closed forms (which enter only the relerr match). The "N kills" folk belief conflates the mean of a right-skewed law with the typical (median) grind and is refuted.
5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact ratio 1/ln2 being a property of the memoryless geometric law (a memory-bearing per-kill model moves it) flips no gate SIGN and no order-of-magnitude of the gap? — **YES.** The exact constants (ln2 ratio, 1−1/e mean-percentile, e⁻³ tail) are properties of the memoryless geometric law, disclosed as scale-free-in-p descriptive fields, not universal law-independent claims — a memory-bearing per-kill model (pity timers, bad-luck protection) would move or collapse the gap, the design mitigation the phenomenon exists to motivate. No gate reads an exact constant as universal; the gates test skew SIGN (G1), the mean-is-majority percentile (G2), and robustness under a rarer rate (G3). The caveat flips no gate sign and no order-of-magnitude — a clean APPROVE.
6. Could the gap sign be a seed fluke? State whether the effect is structural (the mean > median gap holds across the base world and the rarer-drop re-draw, frac_mean_gt_median at ceiling in both) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical). — **NOT a fluke; structural.** The mean > median gap holds at BOTH the base world (p=0.01, z_skew=+369.74) and the rarer-drop re-draw (p'=0.005, z_skew=+802.50), z-scores hundreds of sigma above the null — structural, not a seed artifact. Determinism confirmed both ways: the in-process double-run digest assert (`h1 == h2`) held, and two `python3` invocations produced byte-identical stdout (`diff` exit 0), both exit 0.

## Outcome
**APPROVE.**

- **Digest MATCH.** The compact-canonical results-dict sha256 reproduced EXACTLY as `ff22cf37b81fb96bb36a1aeb1a4484479d31e1232b637efb0faf297bb852fb19` — printed by the script and independently recomputed from stdout (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY; every float rounded to 6 dp), byte-identical across two fresh `python3` invocations and the in-process `h1 == h2` assert. Verifier byte-identical to the P159 source `ideas/superbot-games/drop_rate_median_gap.py` (`diff` exit 0, file sha256 `42cacd75…6682`, git blob `fe602dda5c0f0924c386423b7d7ca451fc965370`). SEED=20260717 is pinned in-source (module constant, line 60), so the run is reproducible without external configuration (the exported `SEED` env var is inert).
- **Three gates PASS in order** (z_gate=3.0, relerr ceiling 0.05): G1 median-below-mean z_skew **+369.741566**, gap_mc 0.305865, ratio_med_mean 0.694135 (null gap=0) → G2 mean-is-majority-percentile z_majority **+761.720583**, frac_le_mean 0.634140 (null 0.5) → G3 scale-free-robustness (p'=0.005) z_skew **+802.499080**, z_majority **+706.081134**. `all_pass=true`, `first_failing_gate=null`, exit 0.
- **Closed-form hand-check agrees.** For T ~ Geometric(p) on {1,2,…}: mean=1/p, median≈ln2/p, ratio median/mean → ln2 = 0.693147, gap fraction (mean−median)/mean → 1−ln2 = 0.306853, P(T ≤ mean) → 1−1/e = 0.632121. At p=0.01 the MC mean is 100 and the exact median 69 (ratio 0.694135, gap 0.305865, frac_le_mean 0.634140); at p'=0.005 mean 200 / median 139 — the ln2 ratio and 1−1/e percentile hold identically at both rates (observed relerrs 0.0003–0.006, all ≤ 0.05), confirming scale-freedom in p. The head reads correctly: the MEAN kill-count strictly exceeds the MEDIAN on the same empirically-drawn geometric kill-counts, NOT "a 1-in-N drop rate means about N kills."
- **Gate-framing note.** The verifier's implemented G2 is `mean_is_majority_percentile` (P(T ≤ mean) > 0.5, → 1−1/e; null 0.5), not the "relative-effect ≥ 0.10" framing the born-red head sketches; both are strictly-positive skew tests and the reproduction is judged against the verifier's actual gates. The model-basis caveat is CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE: the exact constants (ln2 ratio, 1−1/e percentile, e⁻³ tail) are properties of the memoryless geometric law that a memory-bearing per-kill model (pity timers, bad-luck protection) would move or collapse — a disclosed descriptive field that flips no gate sign or order-of-magnitude.

**Verdict: APPROVE.**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-171-marketplace-rake.md` (V171, P158 marketplace take-rate / rake hump / disintermediation, round-37 venture slot, +13) is coherent and landed a CLEAN APPROVE: disclosed compact-canonical results-dict digest `9b8be9fc…` reproduced EXACTLY, verifier byte-identical (git blob `49c3eaf039d9eaf9cea60720d76c955521e3c873`, file sha256 `bcd15da1…384c`), all three gates G1 domination-sign → G2 relative-effect → G3 shifted-robustness PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form t*=C/2 / R(t)=t·(C−t)/C confirmed — the one disclosed field (the optimum LOCATION being model-dependent on the switching-cost law) touches no gate sign, landing via PR #245. DIGEST-POSTURE carry-forward unbroken: V171 and this V172 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no JSON written to disk, the compact-canonical serialization is the digest preimage). Object shift: V171 was the round-37 venture-lab marketplace unit-economics head (a rake-hump / disintermediation claim); this V172 is the round-37 games-lab drop-rate median-gap / geometric-skew head — likewise a SIGN / order-of-magnitude claim whose exact constants (ln2 ratio, 1−1/e percentile) are self-disclosed descriptive fields, earning the same clean-APPROVE posture. CONTIGUITY: union-max advances the verdict high-water to **V172**, no regress over V171. Items open/backfilled BELOW the high-water are NOT closed by landing V172 — V137/P124, V132/P119, V126/P113 remain open below high-water; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V172 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the MEMORYLESS per-kill law (Bernoulli(p) → geometric kill-count, where the ln2 ratio and 1−1/e mean-percentile are exact) and gates the skew SIGN plus one scale-free rarer-rate re-draw. The distinct next slice the head names but holds fixed — the PITY-TIMER / bad-luck-protection frontier: a memory-bearing per-kill model (a hard guarantee-by-N cap, or a hazard that rises with the dry streak) that truncates the right tail and compresses the mean toward the median. Pre-register (same SEED-pinned, empirically-counted paired-draw discipline): sweep memoryless → soft-pity (rising hazard) → hard-cap-at-N, and gate (G-a) the memoryless base reproduces the ln2 gap at ≥3σ, (G-b) a monotone-compression headline — each step toward stronger memory strictly SHRINKS the mean−median gap fraction at ≥3σ (pity protection provably narrows the "expected vs typical" gap), and (G-c) a tail-control — the ≈37% still-empty-at-N mass (→ 1/e under memorylessness) falls monotonically as the cap tightens. The operator lesson sharpens from "the mean overstates the typical grind" to "bad-luck protection is the exact lever that collapses the overstatement, and how much it collapses is a measurable property of the pity curve" — distinct from this V172 head, which pins the memoryless law and asks only whether the gap has the right sign and magnitude.

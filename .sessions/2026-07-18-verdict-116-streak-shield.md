# VERDICT 116 — streak-shield variance amplification: a "streak shield" (after W consecutive true wins, bank a shield that negates the next loss and scores it as a win) is REGRESSIVE — because the shield is BANKED off a SUCCESS signal (a win streak), the already-high-skill cohort banks shields faster and the mechanic boosts its mean rating uplift MORE than the low-skill cohort it is marketed to protect; on the pinned world the high-cohort uplift exceeds the low-cohort uplift by ≥3σ while BOTH cohorts stay positive, so a loss-protection resource gated on winning flows UPHILL and widens the very skill-to-rating gap it claims to close (P103, +13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · low · review/verify

Born in-progress as this session's first commit (born-red HOLD); flips to complete as the deliberate last step after the verdict artifacts + heartbeat.

## Objective
Reproduce idea-engine PROPOSAL 103 (outbox `## PROPOSAL 103 · 2026-07-17T23:43:56Z · status: sim-ready`, round-23 GAME slot), offset +13 (P103 → V116): on the pinned superbot-games world, does the HIGH-skill cohort (HIGH_P=0.55) receive a strictly LARGER mean rating uplift from the streak shield than the LOW-skill cohort (LOW_P=0.45) by ≥3σ, with BOTH uplifts positive and strictly ordered? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/superbot-games/streak_shield_regression.py`) VERBATIM under the pinned SEED=20260717, confirm both pre-registered gates hold, and confirm the results-dict sha256 reproduces `0294f75799989decfe6c0166136691509239a91ac8eaba75d1932074022c09b4` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 103 @ `control/outbox.md` source header verbatim `## PROPOSAL 103 · 2026-07-17T23:43:56Z · status: sim-ready` (read at idea-engine origin/main HEAD `13300b0`, outbox line 923); reference verifier `ideas/superbot-games/streak_shield_regression.py` copied BYTE-IDENTICAL into `sims/verdict-116-streak-shield/` (`diff` exit 0; sha256 `91f409f01a393c789f364f4592c50e21b072c5b45acd10e06c611fe8bff60ad3` on both sides); disclosed canonical results-dict digest `0294f75799989decfe6c0166136691509239a91ac8eaba75d1932074022c09b4`. Pinned world: SEED=20260717, N_PER_COHORT=10000, GAMES=200, STREAK_W=3, LOW_P=0.45, HIGH_P=0.55, SIGMA_GATE=3.0. SEED=20260717 is proposal-owned and SEEDLESS (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed. Offset authority = idea-engine control/outbox.md per-block ledger + control/status.md (docs/current-state.md is a SEAT-DORMANT snapshot through P049/V059 and carries NO P103/V116 row) — P103 ↔ V116 confirmed +13.

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict enforced (in-process repeat inherent to the seeded run AND cross-invocation: two separate `python3` processes, stdout diff exit 0).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V111/V112/V113/V114/V115, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2, both at their pre-registered thresholds)
- G1 uplift-gap regressivity (headline): mean shield uplift for the HIGH cohort exceeds that for the LOW cohort by ≥3σ — `uplift_gap_sigma = (high_mean − low_mean) / sqrt(low_se² + high_se²) ≥ 3.0` (disclosed 153.4585).
- G2 both-positive + strict ordering (control): `low_uplift_mean > 0` AND `high_uplift_mean > low_uplift_mean` — the shield genuinely helps both cohorts (so this is regressivity, not a low-cohort penalty) yet helps the high cohort more.
- APPROVE iff G1 ∧ G2 at their pre-registered thresholds; verdict never softened.

## Outcome — pending (reproduction in flight; verdict written at close-out)
Reproduction running: verifier copied byte-identical (diff exit 0, sha256 `91f409f0…` both sides), cross-invocation double run underway, results-dict digest compared against disclosed `0294f757…022c09b4`, gates G1/G2 checked. Final verdict + gate numbers land here as the deliberate close-out before the Status flip.

## ⟲ Previous-session review
Prior loop P102 → V115 (the growth cash-trough trap — two subscription ventures with IDENTICAL positive per-customer unit economics differ only in acquisition growth rate g, and because CAC is paid upfront while margin accrues over the lifetime, the faster grower has a strictly DEEPER cumulative-cash trough above the closed-form g_crit=CAC·s/(CAC−m)) landed APPROVE — sim-lab PR #188 (merged to main), sims/verdict-115-growth-cash-trough-trap/. V115 and V116 share the reference-verifier-reproduction posture (byte-level digest match against the proposal's own committed stdlib verifier), distinct from an independent reimplementation. Carry-forward guard applied here directly: hash the verifier's OWN canonical bytes (`main()`'s `blob = json.dumps(results, sort_keys=True, separators=(",",":"))`, then `digest = sha256(blob)`), NOT any indented file-write — the P103 verifier PRINTS the canonical one-line blob then a bare `sha256:<digest>` line and does NOT itself write a results.json, so committing exactly that printed blob line with NO trailing newline as `results.json` is what makes `sha256sum results.json` == the disclosed digest; verified before commit. Like P102 (and unlike V114) the P103 results dict carries NO `results_sha256` self-field, so no field-omission step was needed — the printed canonical line is already the hashed preimage.

## 💡 Session idea
The proof pins a SINGLE skill gap (Δp = HIGH_P − LOW_P = 0.10) and shows the shield is regressive at that gap. But the shield-banking rate is governed by the probability of W consecutive true wins, ≈ p^W, whose derivative in p is steep and convex around p=0.5 — so the regressive uplift GAP should not merely be positive, it should grow SUPER-LINEARLY in the skill gap: widening Δp widens the rating gap more than proportionally. A genuinely different next GAME-slice pins Δp as the swept axis (LOW_P/HIGH_P symmetric about 0.5, Δp ∈ {0.05, 0.10, 0.15, 0.20}) and pre-registers two gates: (1) the uplift gap's SECOND difference across the Δp sweep is strictly positive at ≥3σ — the mechanic is not just regressive but ANTI-CONVERGENT, amplifying pre-existing skill spread super-linearly; and (2) each cohort's realized shield-banking rate matches the analytic streak-renewal rate (a p^W renewal process, per-game bank probability under the STREAK_W reset rule) within 3σ, anchoring the convexity to its combinatorial cause rather than a Monte-Carlo artifact. That converts the flat "streak shield is regressive" into the actionable "the harm is WORST exactly in high-skill-variance populations — fresh/mixed matchmaking lobbies, unranked-to-ranked funnels — and scales with the square of the skill spread," a seed-pinnable, ≥3σ-gateable mechanism (convexity of a success-gated resource) distinct from this slice's fixed-gap regressivity result and from the outbox's success→distress signal-swap correction.

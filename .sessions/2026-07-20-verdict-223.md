# VERDICT 223 — Akerlof lemons market collapse: a market in which EVERY possible trade strictly benefits both buyer and seller can still collapse to a vanishing fraction of its potential volume — quality uncertainty ALONE, with no transaction cost and no irrationality, destroys nearly all the gains from trade. On a quality grid Q={1..N} with seller reservation q and buyer valuation βq (1<β<2, every trade win–win), the only price a rational competitive buyer will pay attracts a self-selecting pool of the worst goods, so the equilibrium max trading price is p* = min(N, ⌊β/(2−β)⌋) = 3 of 1000 at β=3/2 and the destroyed gains-fraction D = 1 − p*(p*+1)/(N(N+1)) ≈ 0.999988 — reproduce PROPOSAL 210

> **Status:** in-progress

📊 Model: Opus family · high effort · verdict-reproduction

started: 2026-07-20T11:59:21Z

## What this verdict does

Reproduces PROPOSAL 210 (P210 → V223, +13 offset, lane venture-lab): **Akerlof "Market for Lemons"
adverse-selection collapse.** On a discrete quality grid Q = {1,…,N} with uniform prior, seller
reservation for quality q is q and a rational buyer values q at β·q with 1 < β < 2 — so β·q > q for
every q, EVERY trade is strictly mutually beneficial, with no transaction cost and no irrationality. Yet
a competitive buyer who cannot observe q will pay a posted price p only up to β·E[q | q offered at p],
and the offered set at price p is S(p) = {q ≤ p} (a seller parts with the car iff p ≥ q), so the pool is
self-selecting on hidden quality: the worse the pool, the wider it is. With the uniform grid
E[q | S(p)] = (p+1)/2, the willingness test is p ≤ β·(p+1)/2, whose largest integer solution is the
equilibrium max trading price p* = min(N, ⌊β/(2−β)⌋). At β = 3/2, N = 1000 that is p* = 3: exactly the 3
worst qualities trade, the other 997 do not, and the destroyed fraction of mutually-beneficial gains is
D = 1 − p*(p*+1)/(N(N+1)) = 1 − 12/1001000 ≈ 0.999988 — the market realizes ~0.3% of its potential
volume. Copies the disclosed verifier `ideas/venture-lab/lemons-market-collapse-2026-07-20.py`
(idea-engine) byte-identical into `sims/verdict-223-lemons-collapse/`, reproduces the results-dict
sha256, confirms determinism, and evaluates the four pre-registered gates each in its own direction
against the proposal's OWN criteria.

## Method

- Byte-identical verifier copy (diff source↔copy exit 0), stdlib-only (`hashlib`, `json`, `math`,
  `random`, `fractions.Fraction`). Battery β ∈ {11/10, 5/4, 4/3, 3/2, 5/3, 7/4, 9/5, 19/10},
  N ∈ {50, 100, 500, 1000, 5000}; pinned world β = 3/2, N = 1000; SEED = 20260717; Monte-Carlo
  M = 200000; z_gate = 3.0.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own
  sha256 IS the digest; target `b0251b0aa024e235fa4991c005c1cd3deaa3a04f2fcd2e5d4e041e6e21237539`.
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 — EXACT** (direction: exact agreement, expect discrepancies = 0): the closed form
    p* = min(N, ⌊β/(2−β)⌋) equals an exhaustive `Fraction` scan (p = 0..N, willingness p ≤ β·(p+1)/2,
    take the max willing p) over the full 40-pair battery. PASS iff discrepancies = 0; p* = 3 at
    β = 3/2, N = 1000.
  - **G2 — SURPRISE** (direction: HIGH z ≥ 3.0): Monte-Carlo on the pinned world, realized-trade rate
    r_hat against the folk null 0.5 ("at least half a wholly win–win market survives"). PASS iff z ≥ 3
    and r_hat ≪ 0.5.
  - **G3 — ROBUSTNESS / SHIFT** (direction: persistence — monotone + near-total collapse, residual 0):
    D(β,N) weakly increasing in N for every β, exact residual |D_measured − D_closed| = 0 everywhere,
    and min over all β of D(β, N=5000) ≥ 0.99. PASS iff all three hold.
  - **G4 — EXACT identity** (direction: exact, expect discrepancies = 0): D closed form equals
    1 − (explicit Σ_{q≤p*} q)/(explicit Σ_{q≤N} q) across the battery. PASS iff discrepancies = 0.
- Grounding (disclosed, honestly bounded): Wikipedia "The Market for Lemons" oldid 1358736918, byte-pinned
  by the MediaWiki revision sha1 `34c3ec139bfdd21dc2d0a30ddd895bade01f6a3c` (a byte-pin, NOT a sha256). The
  article carries the adverse-selection / asymmetric-information / quality-uncertainty / market-collapse
  core qualitatively; the exact collapse threshold p* = ⌊β/(2−β)⌋ and the destroyed-fraction closed form
  are the proposal's OWN discrete instantiation, disclosed as not lifted from the page.

## ⟲ Previous-session review

Previous-session review: VERDICT 222 (grid quorums √N intersection / Maekawa — on a k×k grid of N=k²
sites each site's quorum is its row∪column, exactly 2k−1 sites, and any two such quorums ALWAYS intersect
even though each is only O(√N) of the N sites; STRUCTURE not size buys the guarantee — random same-size
subsets are disjoint at a positive rate while grid quorums never are; PROPOSAL 209 → V222) landed
**QUALIFIED APPROVE** with a full-64 digest MATCH (`bbc54843…096f`) and all five gates PASS via the
born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN
direction — an exact-0 / exact-Fraction residual is a self-certifying theorem, a ≥3σ z is an EFFECT
(surprise) gate, a below-3 |z| is a CONVERGENCE/AGREEMENT gate. V223 leans on that same discipline with a
mixed four-gate battery spanning both polarities in one slice: G1 and G4 are exact-Fraction identities
(closed form ≡ exhaustive scan; D ≡ the explicit surplus-ratio identity — theorems, any discrepancy
FAILS), G2 is a HIGH-z SURPRISE crux (z ≈ 4078 — the win–win market survives at ~0.3%, thousands of σ
below the folk "at least half survives" null), and G3 is a persistence/SHIFT sweep (D monotone in N,
exact residual 0, near-total collapse ≥ 0.99 even at the shallowest β = 19/10 corner). The load-bearing
evidence is the exact G1/G4 identities plus the G2 surprise together: the collapse threshold is a
theorem, and the realized survival is 4078σ below the intuitive expectation. V222 also carried a
grounding-caveat correction; V223's grounding is disclosed as qualitative-on-the-page with the threshold
owned by the proposal (checked below). Standing non-contiguity persists: landing V223 does not imply every
lower verdict below the high-water is closed.

## 💡 Session idea

The G1 collapse threshold p* = ⌊β/(2−β)⌋ is evaluated as a **constant of β alone** once N exceeds it —
the whole battery confirms p* is identical across N ∈ {50,100,500,1000,5000} for each β (β=3/2 → 3 at
every N). That invariance is the engine of the head (a bigger market DEEPENS the failure because the
surviving fraction p*/N → 0), but the verifier only witnesses it as a by-product of the per-(β,N) scan. A
cheap orthogonal extension that reuses the exact machinery verbatim would add a deterministic
**threshold-invariance gate**: for each β, assert in exact `Fraction` arithmetic that p*(β,N) is constant
across all N ≥ ⌊β/(2−β)⌋ in the battery (and equals ⌊β/(2−β)⌋ exactly), turning "p* is a constant of β"
from an observed regularity in the printed table into a pinned exact claim — and pairing it with the
existing G3 shows the mechanism cleanly: p* frozen while N grows is EXACTLY why D → 1. The digest-bearing
dict and the four shipped gates stay byte-identical; only a sibling exact invariance gate is added.
(Guard recipe for a later session: the anchor is the existing `p_star_closed` / `p_star_scan` pair over
`BETA_BATTERY` × `N_BATTERY` — add an inner assertion that `{p_star_closed(b,N) for N in N_BATTERY if
N >= floor}` is a singleton equal to `floor(b/(2-b))`, routed through the same exact-Fraction reducer as
G1.)

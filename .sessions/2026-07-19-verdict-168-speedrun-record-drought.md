# VERDICT 168 — speedrun-record-drought / harmonic record law: in a STATIONARY sequence of iid attempts the per-attempt probability that the n-th run sets a new record decays as 1/n, so the expected record count after N attempts tracks the HARMONIC number H_N ≈ ln N + γ and the DROUGHT between successive records grows without bound — an ever-longer gap since the last world record is the NULL expectation of record dynamics, NOT evidence that the game is "solved", that skill has plateaued, or that the community has stagnated — but only UNDER stationarity; once the attempt distribution DRIFTS (genuine skill improvement) the 1/n record law breaks and records arrive faster than harmonic, so the drought-is-benign reading is a stationary-regime claim, not a universal one

Reproduce PROPOSAL 155 (round-36 slot, P155 → V168, +13): speedrun-record-drought / harmonic record law. Each of N attempt-sequences is drawn iid under the pinned world and scored on TWO record statistics on the SAME per-sequence draw (paired) — the realized RECORD-COUNT after the full sequence vs the analytic HARMONIC prediction H_N, and the realized inter-record DROUGHT (gap in attempt index between successive records) vs its 1/n-hazard expectation — to test the folk belief: "a long drought since the last record means the game is solved / skill has plateaued / the scene is dead; records SHOULD keep coming at a steady clip if the community is still healthy." It is NOT: in a STATIONARY iid sequence the n-th attempt sets a record with probability exactly 1/n (a rank-only, distribution-free fact), so the expected number of records after N attempts is the harmonic number H_N ≈ ln N + γ — SUBLINEAR, decelerating — and the expected wait to the next record grows without bound; ever-longer droughts between records are the NULL expectation of record statistics, not a signal of stagnation. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 record-hazard-decay-sign (the per-attempt record probability decays with attempt index n — records get rarer as the sequence grows, the drought is real and structural) → G2 harmonic-law magnitude (the realized record count tracks H_N ≈ ln N + γ and departs from a linear-in-N "steady clip" null by more than a 0.10 relative floor — the deceleration is not a rounding-scale artifact) → G3 distribution-free robustness (the 1/n record hazard survives a re-draw of the attempt distribution, since record statistics depend only on RANK, not the underlying law — the case where a distribution-dependent artifact would be most plausible). Model-basis caveat (P024 discipline): the head is a STATIONARY-regime claim, not universal — under a DRIFTING attempt distribution (genuine skill improvement / meta advances) records arrive FASTER than harmonic and the 1/n law breaks, and that non-stationary regime is a disclosed descriptive field, not a hidden defect. The declared choices — the pinned N, the paired same-sequence draw, the rank-only record definition — change neither the SIGN nor the order-of-magnitude of the harmonic deceleration.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P155 reference verifier byte-identical under its pinned world (SEED=20260717, pinned N attempt-sequences, pinned TRIALS Monte-Carlo trials, the paired realized-vs-harmonic contrast evaluated on the SAME per-sequence draw, one `random.Random(SEED)` stream drawn in fixed order — **exact world constants TBD, to be read off the P155 source at HEAD in the reproduction commit**) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest (**TBD — to be pinned from the P155 outbox done-when line and reproduced byte-exact**), (b) all three gates hold in order G1 record-hazard-decay-sign → G2 harmonic-law magnitude → G3 distribution-free robustness, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the per-attempt record probability decays as 1/n, the realized record count tracks H_N ≈ ln N + γ, and inter-record droughts grow without bound under stationarity; disclosed gate statistics **TBD**). Pre-registered decision rule (P155): APPROVE VERDICT 168 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the non-stationary drift band (a drifting attempt distribution, where records arrive faster than harmonic and the 1/n law breaks) is a DISCLOSED descriptive field of a STATIONARY-regime claim, not a hidden defect, so if none flips a gate SIGN or the order-of-magnitude of the harmonic deceleration, a clean APPROVE is warranted. Target: sim-lab (VERDICT 168, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: the committed P155 source in its lane, copied byte-identically from the idea-engine source. **File identity (file sha256 / git blob / byte count / line count / source last-touched commit): TBD — to be verified and pinned in the reproduction commit.**
- Offset authority: PROPOSAL 155 → VERDICT 168 (+13), round-36 slot (successor to the round-36 VENTURE slot P154 → V167). Per idea-engine `control/outbox.md` P155 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: record statistics / extreme-value theory — the theory of records in an iid sequence. For a stationary sequence of iid draws the probability that the n-th observation is a running record is exactly 1/n (a rank-only, distribution-free fact), so the expected number of records after N draws is the harmonic number H_N = Σ_{k=1}^{N} 1/k ≈ ln N + γ (γ ≈ 0.5772), the record count decelerates, and the expected wait to the next record grows without bound. The folk "a long drought means the game is solved / skill has plateaued" belief is the stationary-regime deceleration this head targets.
- Disclosed digest: compact-canonical results-dict sha256 **TBD** (to be read from the P155 outbox done-when line and reproduced byte-exact).
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the round-35/36 carry-forward. `run()`/results-builder returns a dict carrying NO `results_sha256` field; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>`, and writes NO JSON to disk; every float rounded to 6 dp before serialization. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump — recompute over the compact form. Posture to be confirmed byte-exact in the reproduction commit.
- Pinned world: SEED=20260717; pinned N attempt-sequences; pinned TRIALS Monte-Carlo trials; the paired realized-record-statistic-vs-harmonic-prediction contrast on the SAME per-sequence draw; one `random.Random(SEED)` stream drawn in fixed order. **Exact N / TRIALS / any REL_NULL to be read off the P155 source at HEAD in the reproduction commit.**

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The realized record statistic and the analytic harmonic prediction are evaluated on the SAME per-sequence draw per trial (paired — one `random.Random(SEED)` stream), so the deceleration contrast is attributable to the RECORD DYNAMICS (1/n hazard) alone, not to any difference in the sequence sampled. The gates are definitional contrasts on those shared draws: G1 record-hazard-decay-sign and G2 harmonic-law magnitude at the pinned attempt distribution, G3 re-draws the attempt distribution (the case where a distribution-dependent artifact is most plausible) as a robustness control. Honest scope: the head is a STATIONARY-regime claim — it holds for iid stationary attempts and BREAKS under a drifting attempt distribution (genuine skill improvement), where records arrive faster than harmonic; that non-stationary band is a disclosed descriptive field, and no gate reads the stationary law as universal.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 record-hazard-decay-sign (z ≥ 3, one-sided):** z = (decay statistic − H0)/se — the per-attempt record probability decays with attempt index n (records get rarer as the sequence grows; the drought is real and structural, not a sampling accident). Disclosed statistics **TBD**.
- **G2 harmonic-law magnitude (z ≥ 3, one-sided):** z = (relative departure − 0.10)/se vs a 0.10 null — the realized record count tracks H_N ≈ ln N + γ and departs from a linear-in-N "steady clip" null by more than the 0.10 relative floor (the sublinear deceleration is not a rounding-scale artifact). Disclosed statistics **TBD** (vs the 0.10 REL_NULL).
- **G3 distribution-free robustness (z ≥ 3, one-sided):** re-draw the attempt distribution and re-run the contrast — the 1/n record hazard (and the harmonic count) survive the distribution shift, since record statistics depend only on RANK, not the underlying law (the case where a distribution-dependent artifact would be most plausible). Disclosed statistics **TBD**.
- z_gate **3.0** (require z ≥ 3 on every gate, one-sided). **Disclosed caveat (NOT a gate):** under a DRIFTING attempt distribution (genuine skill improvement / meta advances) records arrive FASTER than harmonic and the 1/n law breaks — that non-stationary regime is a disclosed descriptive field of a stationary-regime claim, and NO gate reads the stationary law as universal.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (not the pretty indent=2 dump — the P127+ STDOUT-ONLY twist)? State the reproduced digest. **(TBD — pending reproduction.)**
**2.** Is the verifier byte-identical to the P155 source at its pinned idea-engine commit (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity. **(TBD — pending reproduction.)**
**3.** Do all three gates PASS in the pre-registered order G1 → G2 → G3 with the disclosed statistics (all ≥ 3σ one-sided, z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0? **(TBD — pending reproduction.)**
**4.** Is the headline read correctly — the per-attempt record probability decays as 1/n, the realized record count tracks H_N ≈ ln N + γ, and inter-record droughts grow without bound UNDER STATIONARITY — and NOT as "a long drought proves the game is solved / skill has plateaued / the scene is dead"? The drought is the null expectation of record dynamics; the claim is a stationary-regime claim, and the law breaks under a drifting distribution.
**5.** Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect)? The non-stationary drift band (a drifting attempt distribution, where records arrive faster than harmonic) is a DISCLOSED descriptive field of a stationary-regime head, and the declared choices — the pinned N, the paired same-sequence draw, the rank-only record definition — none flips a gate SIGN or the order-of-magnitude of the harmonic deceleration.
**6.** Could the sign be a seed fluke? The paired-draw contrast is to clear G1, G2 (vs the 0.10 null), and G3 (the distribution-free re-draw) each at ≥ 3σ one-sided — **statistics TBD, to be recorded on reproduction.**

## Outcome — IN PROGRESS (born-red; reproduction pending)
- **Digest — TBD.** Clean-room re-run under SEED=20260717 to reproduce the results-dict sha256 (disclosed digest to be pinned from the P155 outbox done-when line). Pending.
- **Byte-identical + deterministic — TBD.** Verifier to be copied byte-identical to source (diff exit 0; git blob to be pinned). Determinism to be confirmed: in-process double-run assertion must not fire; separate cross-invocation runs must byte-match. Pending.
- **Gates — TBD.** To PASS in order G1 record-hazard-decay-sign → G2 harmonic-law magnitude → G3 distribution-free robustness on the proposal's own criteria; all_pass / first_failing_gate to be recorded. Pending.
- **Crossover honored — TBD.** The non-stationary drift band (records faster than harmonic under a drifting distribution) to be confirmed as the disclosed descriptive field, not a defect. Pending.
- **Verdict: PENDING** (flips to APPROVE/REJECT on the deliberate LAST commit once the reproduction is proven and independently audited).

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-167-growth-endurance.md` landed V167 (P154 growth-endurance dominance, round-36 VENTURE slot, +13) as a **CLEAN APPROVE**: exact digest `5dccb475…495bf` reproduced across a fresh cross-invocation double-run, the script's in-process assert, and the byte-identical copy; all three gates PASS in order (G1 dominance-sign gap_mean +0.354025 z=+996.770037 frac_dominant=0.992229, G2 relative-effect rel_mean +2.219712 z=+925.513190 vs the 0.10 null, G3 shifted-distribution gap_mean +0.322362 z=+1107.766114 frac_dominant=0.997938); the growth-beats-endurance terminal-value gap rested on the STRATEGY (compound-growth vs raise-endurance) over the SAME paired per-company draws, and its one reversal (the low-growth crossover g0 > 1−r, where endurance wins) was self-disclosed AND descriptive. DIGEST-POSTURE carry-forward: V167 and this V168 are BOTH expected WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY with the P127+ twist (results dict carries no `results_sha256`, no file written, the compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form). Object shift: V167 was a VENTURE-slot terminal-value DOMINANCE claim whose reversal was a growth-rate crossover; this V168 is the round-36 record-statistics head — a harmonic record-law / drought claim whose ONE reversal (the non-stationary drift regime, where records arrive faster than harmonic) is likewise self-disclosed and descriptive, so it earns the same clean-APPROVE posture IF the reproduction holds. CONTIGUITY note for the successor: V168 (P155, round-36 slot) advances the verdict high-water to **V168** (union-max, no regress); items open/backfilled below the high-water are not closed by landing V168. Coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V168 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins ONE record definition (running maximum, rank-only) and reports the harmonic law H_N ≈ ln N + γ under strict STATIONARITY. A genuinely distinct next slice — drift-detection power + the break-from-harmonic frontier — pins the parameter the head flags but holds fixed: stationarity is not a given, and the interesting operator question is how much drift it takes before the 1/n record law visibly breaks. Pre-register (SEED-pinned, same paired-draw discipline): inject a per-attempt improvement drift δ into the attempt distribution and sweep δ ∈ {0, δ₁, δ₂, …}, and gate (G-a) the harmonic law SURVIVES at δ=0 at ≥ 3σ (the stationary baseline reproduces), (G-b) a drift-detection frontier — there is a smallest drift δ* at which the realized record count departs from H_N by more than the 0.10 relative floor at ≥ 3σ (the "drought is benign" reading has a measurable stationarity budget; beyond δ* the drought genuinely IS evidence the field is still advancing), with a closed-form reference for the expected record count under linear drift to audit the swept simulator against, and (G-c) a drought-length control — under drift the expected inter-record gap stops growing and plateaus, recoverable at ≥ 3σ (the unbounded-drought signature is itself a stationarity test). The operator lesson sharpens from "a long drought is benign under stationarity" to "the drought tells you whether the field is stationary — measure the gap growth and you can DETECT whether skill is still improving." Distinct from this V168 head (which pins stationarity and asks only whether the harmonic law holds) and gives an exact analytic reference (the drift-perturbed record count) to audit the swept simulator against.

# VERDICT 138 — USL coherency retrograde-throughput cliff

Reproduce PROPOSAL 125 (round-29 FLEET slot OPENER, P125 → V138, +13): the coordination-overhead retrograde-throughput cliff — the Universal Scalability Law (USL). Naive linear scaling says a fleet of N agents on one coordinated job does N× the work; Amdahl already caps that at 1/α for a serial/contention fraction α, but only SATURATES — never reverses. The USL is worse: a coordinated job also pays a coherency cost β (keeping agents consistent with each other), which grows as the number of PAIRS ∝ N(N−1). Adding the quadratic coherency term to Amdahl's denominator gives C(N)=N/(1+α(N−1)+βN(N−1)); because the denominator's coherency term grows as N² while the numerator grows as N, C(N) is NON-monotone — it rises, PEAKS at N*=√((1−α)/β), then DECLINES ("retrograde scaling"). In completion-time form T(N)=(1−α)/N+(α−β)+βN: a shrinking parallel term, a constant, and a GROWING coherency term βN, minimized at N*, past which every extra agent makes the fleet SLOWER. Two facts the "more agents = more throughput" instinct misses: (1) it is COHERENCY, not contention — with β=0 (Amdahl only) throughput is monotone up to the 1/α ceiling and never falls; the retrograde peak appears the instant β>0; (2) the optimum is FINITE, set by the α:β ratio — N*=√((1−α)/β)=31 on the pinned world, not "as many agents as you have." On the pinned world (α=0.03, β=0.001 ⇒ N*=31) C(31)≈10.95× while C(200)≈4.28× (peak/hi 2.56×): a 200-agent fleet delivers under HALF the 31-agent peak — the extra 169 agents roughly halve the output.

> **Status:** `in-progress`
> 📊 Model: opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (ORDER 003). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P125 reference verifier byte-identical under its pinned world (SEED=20260717, ALPHA=0.03, BETA=0.001, TRIALS=400, BATCH=200, N_GRID=(1,2,4,8,16,24,31,48,64,96,128,200), N_STAR=31, N_HI=200, SIGMA=3.0) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest, (b) all three gates hold as disclosed, in order G1→G2→G3, (c) cross-invocation runs are byte-identical AND an in-process double-run is deterministic. Source header (verbatim): `## PROPOSAL 125 · 2026-07-18T08:03:18Z · status: sim-ready` / target: sim-lab (VERDICT 138, +13 offset).

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/usl_coherency_retrograde_cliff.py` — git blob `08806218ebf2a2638d545e5a493f967b60629e04`, sha256 `f46b0241c10f4415f20ab9ce44dbb8b12aaf0cbea7119ff2565bacd1ca9b9183`, **270** lines / **11536** bytes. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/78bb3ab6d93d2418f7991fcdd2f9f14082d00313/ideas/fleet/usl_coherency_retrograde_cliff.py
- Domain reference: Neil J. Gunther, the "universal law of computational scalability" C(N)=N/(1+α(N−1)+βN(N−1)) — https://en.wikipedia.org/wiki/Neil_J._Gunther (verified reachable 2026-07-18); with Amdahl's law S=1/(1−p+p/s), ceiling 1/(1−p)=1/α, as the β=0 control — https://en.wikipedia.org/wiki/Amdahl%27s_law (verified reachable 2026-07-18, its See-also links the USL).
- sim-lab work branch cut from origin/main HEAD `0732b16f05373e7b511a01c26ae947fc78ba6128` (#210).
- DIGEST POSTURE: **SELF-FIELD / PRETTY-ON-DISK** (same family as the V126 exemplar; DIFFERS from the V135/V136 NO-SELF-FIELD/COMPACT-STDOUT posture). `main()` computes `canonical=json.dumps(results, sort_keys=True, separators=(",",":"))` and `digest=sha256(canonical)` BEFORE adding a `results_sha256` field, THEN sets `results["results_sha256"]=digest`, writes PRETTY JSON (indent=2, sort_keys) to `usl_coherency_retrograde_cliff_results.json`, and prints the pretty dump + `Results-JSON sha256: <digest>` + the gate lines + `ALL_PASS: True`. Therefore the disclosed digest is the sha256 of the results dict WITHOUT its own `results_sha256` field; the on-disk pretty JSON (which DOES carry `results_sha256`) does NOT re-hash to the digest by `sha256sum` — the digest is recomputed by dropping the self-field and re-serializing compact, reconciled honestly in the probe report.
- Offset authority: idea-engine control/outbox.md P125 `depends:` ledger (P125→V138 +13, "Offset pinned" — predecessor row P124→V137) + control/status.md baton ("next: V138 for P125; then round-29 venture-slot P126") — NOT docs/current-state.md (a dated SEAT-DORMANT snapshot through V059).

## Constraints honored
Stdlib-only (random, math, json, hashlib); verifier copied byte-identical (diff exit 0, blob + sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on green head SHA per ORDER 003).

## Gate plan (disclosed → reproduced)
- G1 retrograde existence: mean(T(N_hi)−T(N*)) > 0 with z=mean/se ≥ SIGMA=3.0 — more agents past the peak ⇒ more completion time ⇒ less throughput.
- G2 coherency-necessity control: with β=0 (Amdahl) mean(T0(N_hi)−T0(N*)) < 0 with z ≤ −3 — the SAME fleet is monotone-faster once the coherency term is removed, so the retrograde drop is caused by coherency β, not fleet size.
- G3 USL-anchor + peak: measured mean time matches (1−α)/N+(α−β)+βN at N* and N_hi (each |z|<3) AND the grid argmin of measured time = N*=31.
- Closed-form anchors: N*=√((1−α)/β)=31.1448→31, C(31)=10.954064, C(200)=4.276245, peak/hi ratio 2.561608, Amdahl ceiling 1/α=33.333.

## Outcome — APPROVE (exact reproduction)
Reproduced digest `e496837c6dd8bbbaec79701528507185a0a08cb8c008255317d3719d99795557` == disclosed digest (MATCH). Cross-invocation A + B both printed the identical digest, and an in-process double-run (run() ×2, hashed compact in-process) produced the identical digest both times (all exit 0; cross-invocation stdout diff exit 0). Verifier copy diff exit 0; blob `08806218` + sha256 `f46b0241` match source.
- **G1 retrograde existence: PASS** — mean(T(200)−T(31)) **0.142452** > 0 by **z=959.9168σ** ≥ 3σ (se **0.000148**); adding agents past N*=31 strictly increases completion time.
- **G2 coherency-necessity: PASS** — β=0 Amdahl control mean(T0(200)−T0(31)) **−0.026410** < 0 by **z=−189.0673σ** ≤ −3σ (se **0.000140**); the same fleet is monotone-faster with the coherency term removed — the drop is caused by coherency β, not fleet size.
- **G3 USL-anchor + peak: PASS** — **|z_star|=0.0507** / **|z_hi|=0.5417** < 3 AND grid argmin of measured time = **N*=31**; full-grid max|z| **1.6993** < 3.
- Closed-form anchors: N*_cf **31.144823**→31, C(31) **10.954064** vs C(200) **4.276245** (peak/hi ratio **2.561608**), Amdahl ceiling 1/α=33.333; measured T(31) **0.091285** (cf 0.091290) / T(200) **0.233791**; measured speedup curve rises then falls (N=1→1.000, 16→9.510, 31→11.019 peak, 64→9.302, 128→6.114, 200→4.302). all_pass=**true**, exit **0**, first-failing gate **none**. This final flip to `complete` is the LAST commit that releases the born-red HOLD; landing is via merge-on-green (ORDER 003) with zero agent merge calls. **APPROVE.**

## ⟲ Previous-session review
Prior loop landed VERDICT 136 (P123 hard-close snipe clearing leak, round-28 GAME slot, +13, sim-lab #210, digest `2344cfb8…c18e8`) — APPROVE, byte-identical reproduction across cross-invocation A/B with a deterministic in-process double-run, all three gates PASS (G1 existence z=135.3738σ, G2 dose-response z=163.6733σ, G3 specificity max|placebo|=0.0 exactly), NO-SELF-FIELD / COMPACT-STDOUT digest posture, clean born-red flip landed via merge-on-green. One carry-forward it did well: it EARNED its headline with a CONTROL leg (q=0 placebo) and a closed-form anchor (binomial-mixture E[P_hard]), making the MECHANISM the deliverable rather than the magnitude. This V138 slice keeps that discipline — G2 is a β=0 Amdahl CONTROL that FLIPS the sign (the same fleet becomes monotone-faster when coherency is removed), isolating COHERENCY β as the cause of the retrograde cliff rather than fleet size, and G3 anchors the measured curve to the exact USL closed form C(N)=N/(1+α(N−1)+βN(N−1)) with grid argmin pinned at N*=31. DIGEST-POSTURE carry-forward: classify `main()` BEFORE recomputing any digest — this P125 verifier is SELF-FIELD / PRETTY-ON-DISK (same family as V126, UNLIKE the V135/V136 NO-SELF-FIELD posture): it hashes the results dict WITHOUT `results_sha256`, then adds that field and writes pretty JSON, so the on-disk `usl_coherency_retrograde_cliff_results.json` does NOT `sha256sum` to the disclosed digest — the digest is recovered by dropping the self-field and re-serializing compact (confirmed against `main()` in the script text, not assumed). V136 was a game-domain auction-clearing object (second-highest of a binomially-thinned Uniform sample); this V138 slice is a fleet-domain throughput-vs-concurrency curve object (a coherency-driven retrograde peak), a different domain, different object, different math. That slice took the round-28 GAME slot; this slice OPENS round-29 at the FLEET slot (P125 → V138, rotation unrelated→fleet). NON-CONTIGUITY note for the successor: V138 lands while V137 (P124, the round-28 unrelated closer) is NOT yet on sim-lab main and the V136/V132/V126 idea-engine mirrors remain open pulls — do not read "high-water V138" as "V137 landed" or "V136/V132/V126 fully closed on both sides." Coordinator-bound routines (failsafe cron `trig_01KPMLtWuAc2FaYNzuSSukgH`) stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the USL retrograde cliff as a STATIC, HOMOGENEOUS-coherency law — one fixed β applied uniformly to every pair, so the coherency cost is exactly βN(N−1) and the optimum is the clean N*=√((1−α)/β). But the assumption that makes N* clean (every pair equally chatty) is exactly the one a real fleet violates: coordination is TOPOLOGICAL, not all-pairs — agents partitioned into shards gossip densely within a shard and sparsely across, so the effective pair-count is not N(N−1) but a block-structured sum. A genuinely different NEXT fleet slice, distinct from this all-pairs head and from the prior fleet closers (P101 winner's-curse, P105 delegation-loop collapse, P109 correlated-fleet variance floor, P113 two-choices routing cliff, P117 Young–Daly checkpoint, P121 hedged-request tail cure), pins a **coordination-topology β-reshaping** mechanism: partition N agents into K shards of N/K with intra-shard coherency β_in and inter-shard β_out ≪ β_in (hub-not-mesh gossip), so the effective coherency term becomes K·(N/K)(N/K−1)·β_in + (cross-shard pairs)·β_out, and the optimum fleet size RISES with K (sharding buys headroom by lowering the effective β). Seed-pinnable head: sweep K over a grid at fixed total N and pre-register (G-a) an optimum-shift gate — N*(K) increases with K by ≥3σ (sharding raises the retrograde peak); (G-b) a β_out=β_in control — collapsing the inter-shard cost back to the intra-shard cost recovers this slice's single-β N*=31 within 3σ (isolating the TOPOLOGY, not the raw β, as the cause of the shift); (G-c) a throughput-at-fixed-N gate — at N=200 (past the single-β peak) the K-sharded fleet delivers strictly MORE throughput than the mesh fleet by ≥3σ, so the operator lesson sharpens from "size at N* and stop" to "to go faster past the peak, CUT coordination by partitioning state (raise K) — sharding, not more agents, is what moves the ceiling; only a genuinely β≈0 independent-subtask job scales with raw agent count, itself still Amdahl-capped at 1/α." A seed-pinnable fleet-scaling mechanism deliberately distinct from this slice's all-pairs-homogeneous head and from the prior fleet closers.

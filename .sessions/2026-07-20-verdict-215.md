# VERDICT 215 — Condorcet's jury theorem, the dark side: for competence p < 1/2 the majority-vote accuracy is DECREASING in the jury size, so the optimal below-average jury is a SINGLE voter (the exact symmetry-mirror of the classic p > 1/2 limit) — reproduce PROPOSAL 202

> **Status:** in-progress

📊 Model: Claude Opus · effort high · task-class verdict-reproduction

started: 2026-07-20T07:43:50Z

💓 Heartbeat: round-49 FLEET-slot P202 → V215 (+13) reproduction live on branch
`claude/verdict-215`; sim dir `sims/verdict-215-condorcet-dark-side/` (byte-identical
verifier copy + reproduction stdout + probe-report), digest full-64 EXACT
(`e37537a4…eaf1df`), all three gates PASS in order (G1 small-|z| AGREEMENT, G2
EXACT enumeration, G3 distribution-free), grounding byte-pinned (Wikipedia
"Condorcet's jury theorem" oldid 1364571286, raw wikitext sha1 `1e613033…`,
12161 bytes). Born-red HOLD armed on this first commit; releases on the deliberate
`complete` flip.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit
so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely
done. It flips to `complete` as the deliberate LAST commit — only after the sim dir
(byte-identical verifier copy + reproduction stdout + probe-report), the digest match
(full-64 exact `e37537a4…eaf1df`), the in-order AGREEMENT / EXACT / DISTRIBUTION-FREE
gate evaluation (all PASS), the determinism check (in-process double-run identical AND
two cross-invocation processes byte-identical), and the grounding check have ALL landed —
that flip clears the HOLD and releases merge-on-green. NO merge API calls are made from
this session; CI + the landing automation merge the green PR.

## What this verdict does

Reproduces PROPOSAL 202 (P202 → V215, +13 offset): the DARK SIDE of Condorcet's jury
theorem. For a jury of `n` independent voters each individually correct with probability
`p`, majority vote is more accurate than a single voter when `p > 1/2` and converges to
certainty as `n → ∞`. The head is the SYMMETRIC COROLLARY for `p < 1/2`: majority-vote
accuracy is then DECREASING in `n`, so adding voters makes things WORSE and the optimal
jury is a single voter; at `p = 1/2` it is flat at `1/2` for every `n`. Copies the
disclosed verifier `ideas/venture-lab/condorcet-jury-committee-2026-07-20.py`
byte-identical into `sims/verdict-215-condorcet-dark-side/`, reproduces the results-dict
sha256, confirms determinism, and evaluates the gates in order against the proposal's OWN
criteria.

## Method

- Byte-identical verifier copy (diff source↔copy exit 0; sha256
  `97512e57dce05cf89e01e4da125509e879f8c2776ed214487b626600110383e5`; sim-lab git blob
  `d4a46897ccb58ce12ae8afe3ef06f190e2276e08`), stdlib-only (`hashlib`, `json`, `math`,
  `random`, `fractions`), SEED=20260717. Source landed at idea-engine PR #750 / squash
  `43df276`.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical
  results dict's own sha256 IS the digest (`json.dumps(d, sort_keys=True,
  separators=(",",":"))`); target
  `e37537a46ae122d030e858f1f21015b2f528ab84d567a5b13227cf1905eaf1df` matched across all
  64 hex (printed `RESULTS_SHA256 = …`), identical across two fresh cross-invocation runs.
- Determinism: in-process `double_run_identical=true`; two separate `python3` processes
  both printed `e37537a4…eaf1df` (cross-invocation identical).
- Gates (in order, against the proposal's OWN criteria — read each in ITS direction):
  - **G1 STATISTICAL AGREEMENT** (small |z|, `T_mc=200000`, `N_mc=101`): above `p=0.60`
    empirical `0.97912` vs `M(101,0.60)=0.979103309`, `|z|=0.0521848522`; below `p=0.40`
    empirical `0.020815` vs `M(101,0.40)=0.020896691`, `|z|=0.2554090111` — both within
    3σ. **PASS.**
  - **G2 EXACTLY-TRUE** (exhaustive `2^n` ≡ closed-form `Fraction`, `n∈{1,3,5,7,9}`):
    exact `==` at `p=3/5` (increasing → 1), `p=2/5` (decreasing → 0), `p=1/2`
    (flat `1/2`); regimes `p<½→0` / `p>½→1` / `p=½=½` all hold. **PASS.**
  - **G3 DISTRIBUTION-FREE**: monotone value-transform leaves every vote bit-identical to
    Bernoulli (`votes_bit_identical=true`, `rate_exactly_equal=true`); independent
    heavy-tailed Cauchy latent rate `0.97911` vs `0.979103309`, `|z|=0.020919597` within
    3σ. **PASS.**
  - `all_pass = true`, `first_failing_gate = null`.
- Grounding: Wikipedia "Condorcet's jury theorem" oldid 1364571286, raw wikitext sha1
  `1e6130332a53f4c8b92aa3872a25019b5f871ded`, byte length 12161 — BOTH match. The article
  states the dark side verbatim: "if p is less than 1/2 … then adding more voters makes
  things worse: the optimal jury consists of a single voter." External byte-pinned
  citation — STRONG.

## ⟲ Previous-session review

Previous-session review: VERDICT 214 (Graham's multiprocessing timing anomaly, PROPOSAL
201) landed complete with a full-64 digest MATCH (`2f81534…129eef`) and all gates PASS
via the born-red HOLD choreography — `in-progress` first commit, deliberate `complete`
flip last. Its carry-forward was the GATE-POLARITY discipline: read each gate in ITS OWN
direction — V214's G1/G2 were huge-z SIGNAL tests while its G3 was an EXACT-BOUND gate
where ZERO violations is the PASS, and reading them by one rule inverts a PASS. V215
inherits and re-tunes that: here the polarity mix is DIFFERENT again — G1 is a small-|z|
AGREEMENT test (empirical rate must SIT ON the closed form, a HIGH z would FAIL it), G2 is
an EXACT enumeration test (`2^n` brute force must equal the `Fraction` closed form
exactly), and G3 is distribution-free (bit-identical votes under a monotone transform,
plus a small-|z| agreement leg under a heavy-tailed latent). Reading G1 or G3 as a SIGNAL
gate would invert the PASS. The load-bearing evidence is the EXACT gate (G2's `2^n ≡
Fraction` identity across `n∈{1,3,5,7,9}`), with the small-|z| agreement gates
corroborating. Standing non-contiguity persists: V137 (P124), V132 (P119), and the
round-26 FLEET-slot V126 (P113) remain open BELOW the high-water; landing V215 does not
imply every lower verdict is closed.

## 💡 Session idea

G2 proves the dark-side corollary is the EXACT symmetry-mirror of the classic light-side
limit: at `p` and `1−p` the majority-correct probabilities `M(n,p)` and `M(n,1−p)` sum to
1 for every odd `n` (the enumeration already computes both `p=3/5` and `p=2/5` grids, and
`81/125 + 44/125 = 1`, `2133/3125 + 992/3125 = 1`, … hold term-by-term). The verifier
currently asserts each grid's monotone DIRECTION separately; a cheap orthogonal extension,
reusing the exact `Fraction` `M_grid` machinery verbatim, would ADD a deterministic
reflection gate asserting `M(n,p) + M(n,1−p) == 1` exactly across the same `n` grid —
turning "increasing toward 1 at 3/5 AND decreasing toward 0 at 2/5" into "the two grids
are exact complements", which is the precise sense in which the dark side is not a new
phenomenon but the mirror of the classic theorem. Only a sibling exact-reflection gate is
added; the digest-bearing results dict and the Monte-Carlo gates stay byte-identical.

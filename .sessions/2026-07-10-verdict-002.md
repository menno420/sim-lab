# Session — VERDICT 002 · INTAKE 001 — panel-mode vs single-pass probing

> **Status:** `complete`
> 📊 Model: Opus-family (Claude Code) · 2026-07-10 · VERDICT 002 close-out session
> Objective: settle INTAKE 001 (idea-engine PROPOSAL 001 @ 3e3e80d) — does panel-mode probing beat single-pass enough to justify its cost — and ship the measured-prototype sim subtree.

## What happened

Shipped `sims/intake-001-probe-panel-vs-single-pass/` — a measured-prototype (ladder
rung 2) head-to-head settling INTAKE 001. Ran both probe modes on 3 real idea-engine
backlog ideas (single-pass ×3 reps, panel ×3 reps = 18 live cells / 54 sub-agents) +
6 blind pairwise judges, every sub-agent instrumented for tokens + wall-time. The sim
subtree carries the frozen runs (`runs/cells.json` + `runs/raw/<slug>/` audit trail),
a deterministic self-checking `analyze.py`, `REPORT.md` (all five validity-gate
answers), `probe_protocol.md` (re-measurement spec), and `README.md`.

**Verdict: `approve` — selectively.** Adopt panel-mode ONLY for big-or-contested
ideas; reject always-on. Panel flipped the modal verdict on 2/3 ideas (both
BUILD→HOLD, on the contested ideas), not on the well-scoped one, at ~4× agents /
3.05× tokens / 1.61× wall; blind judges preferred it 6/6 (slight, no position bias).
Verdict correctness is unmeasured — the honest headline limit. Evidence strength:
moderate; gate PASS for the claims it covers (verdict-change + cost + quality).

This card lands born-red as the session's first commit, then flips to `complete` as
the deliberate close-out. As an ADDED card it is the gate's advisory sentinel, so the
READY PR is expected to pass green.

## Run command

```
python3 sims/intake-001-probe-panel-vs-single-pass/analyze.py
```
Deterministic, stdlib-only, exit 0 iff all self-checks pass — recomputes every
headline number from the frozen `runs/cells.json`.

## 💡 Session idea

The panel *always* moved verdicts toward HOLD (0/2 flips went toward less caution).
A born-cheap follow-up: log, for every panel-HOLD idea that shipped anyway, whether
it later succeeded — the one cut that would convert this prototype's unmeasured
"correctness" gap into evidence about whether the extra conservatism is accuracy or
over-caution.

## ⟲ Previous-session review

VERDICT 001 (INTAKE 003 spawn-tuning, `.sessions/`+PR #7) landed a numeric sim (rung
1) with committed code + one deterministic command. Applied here: same discipline for
a different rung — the live LLM layer is NOT bit-reproducible, so the reproducible
artifact is the frozen `runs/` + `analyze.py`, and `REPORT.md` states that limit
plainly rather than hiding it behind the run command.

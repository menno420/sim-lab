# VERDICT 120 — energy-cap overflow-forfeiture tax in free-to-play stamina pacing: a stamina bar regenerates 1 unit/period up to a cap C while players log in at a Poisson rate (inter-login gaps iid Exponential mean μ), so the regen banked between logins is min(gap, C) and the overflow max(0, gap−C) accrued while the bar is full is silently FORFEITED — the usable (spendable) fraction of all regenerated stamina is the exact closed form U(μ)=E[min(G,C)]/E[G]=1−e^{−C/μ}, a CONCAVE saturating function of the cap, so raising the cap past C~μ adds exponentially little usable energy and the overflow-forfeited fraction W(μ)=e^{−C/μ} rises monotonically with the login gap — the busy/casual player the cap is sold to "protect" forfeits the LARGEST fraction while the always-online player captures nearly all of it; a GAME-domain pure-mechanism head (round-24 game slot), P107 → V120 (+13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · low · review/verify

Born in-progress as this session's FIRST commit (born-red HOLD); it stays in-progress until the reproduction artifacts + heartbeat land and the close-out is written — the flip to `complete` is a deliberate LATER step, not part of this born-red opening commit.

## Objective
Reproduce idea-engine PROPOSAL 107 (outbox `## PROPOSAL 107 · 2026-07-18T01:12:18Z · status: sim-ready`, round-24 GAME slot), offset +13 (P107 → V120): on the pinned world (SEED=20260717, C=120, MU_FREQUENT=60, MU_CASUAL=480, N_GAPS=5000, TRIALS=400, SIGMA_GATE=3.0, WASTE_MIN=0.70), does a stdlib Monte-Carlo over exponential login gaps — banking min(gap, C) and forfeiting overflow max(0, gap−C), ratio-of-sums usable/waste fraction — show (G1) casual-forfeiture headline: casual (μ=480) mean overflow-forfeited fraction W_casual ≥ WASTE_MIN=0.70 by ≥3σ; (G2) frequency-regressivity control: casual waste fraction strictly exceeds the frequent (μ=60) player's by ≥3σ (two-sample se); and (G3) closed-form anchor MATCH: mean usable fraction U_sim matches the exact 1−e^{−C/μ} within 3σ (|z|<3) for BOTH scenarios? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/superbot-games/energy_cap_overflow_forfeiture.py`) VERBATIM under the pinned SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, and confirm the results-dict sha256 reproduces `2e30020af5a97661506c29c962d047fb9561948098b09f5a826084b013f3ce29` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 107 @ `control/outbox.md` source header verbatim `## PROPOSAL 107 · 2026-07-18T01:12:18Z · status: sim-ready` (outbox line 998, read at idea-engine HEAD `d55baf8987435d2acbb87e9804b367a591e5686d`; proposal-body grounding tag `idea-engine@36f5a6f`); reference verifier `ideas/superbot-games/energy_cap_overflow_forfeiture.py` present at that HEAD, blob sha `0fec21f10f24ce293d49133c8988157161120ac2` (`git hash-object`), file sha256 `2e33a757c1b0287b125b876ef00093c070f4f30220b70ed5ca128a49723388a8`, 7514 bytes / 177 lines, stdlib-only (random, math, json, hashlib), SEED=20260717 pinned in-file (line 40, `SEEDLESS` discipline). sim-lab work branch `claude/verdict-120-energy-cap-overflow` off origin/main HEAD `b9a2be77edeadb170a3a13584f500b3c82e2da74`. Disclosed canonical results-dict digest `2e30020af5a97661506c29c962d047fb9561948098b09f5a826084b013f3ce29`; disclosed dry-sim landing (SEED-pinned): U_frequent=0.864359 (se 0.000296) / U_casual=0.221325 (se 0.000135); G1 W_casual=0.778675 (se 0.000135) z=581.5691σ (≥0.70), G2 W_casual 0.778675 vs W_frequent 0.135641 z=1973.299σ, G3 U_frequent |z|=1.0304 / U_casual |z|=0.9307 (both <3) — all PASS, exit 0, two runs byte-identical. Pinned world: SEED=20260717, C=120, MU_FREQUENT=60, MU_CASUAL=480, N_GAPS=5000, TRIALS=400, SIGMA_GATE=3.0, WASTE_MIN=0.70; closed-form anchors U_frequent=1−e^{−2}=0.864665, U_casual=1−e^{−0.25}=0.221199, W_frequent=0.135335, W_casual=0.778801. Offset authority = idea-engine control/outbox.md per-block ledger + control/status.md (docs/current-state.md is a SEAT-DORMANT snapshot through V059 and carries NO P107/V120 row) — P107 ↔ V120 confirmed +13 (predecessor row P106 → V119; baton note "next: V120 for P107").

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict to be enforced (in-process repeat inherent to the seeded run AND cross-invocation: two separate `python3` processes, stdout diff exit 0).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114/V115/V116/V117/V118, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2/G3, evaluated in order at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104/P105/P106 /se convention)
- G1 casual-forfeiture headline: casual (μ=480) mean overflow-forfeited fraction W_casual ≥ WASTE_MIN=0.70 by ≥3σ — the cap forfeits most of a busy player's regenerated stamina (disclosed z=581.5691σ).
- G2 frequency-regressivity control: casual waste fraction strictly exceeds the frequent (μ=60) player's by ≥3σ (two-sample se) — the forfeiture is monotone in the login gap, W∝e^{−C/μ}, so the tax is on ABSENCE, not a cap-independent constant (disclosed z=1973.299σ).
- G3 closed-form anchor MATCH: mean usable fraction U_sim matches the exact 1−e^{−C/μ} within 3σ (|z|<3) for BOTH scenarios — reproduces the exponential-overflow formula, so G1's headline is 100% the overflow mechanism, not a sampling artifact (disclosed U_frequent |z|=1.0304 / U_casual |z|=0.9307).
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order; verdict never softened.

## Outcome — IN-FLIGHT (born-red HOLD)
Reproduction pending in this same session; this section is the deliberate LAST write before the Status flip. Do not read this card as complete while the badge is `in-progress`. The verifier is copied VERBATIM into `sims/verdict-120-energy-cap-overflow/energy_cap_overflow_forfeiture.py` (byte-identity to be proven by `diff` exit 0 + matching `sha256sum` against idea-engine's committed blob `0fec21f10f24ce293d49133c8988157161120ac2` / file sha256 `2e33a757c1b0287b125b876ef00093c070f4f30220b70ed5ca128a49723388a8`), run under the pinned SEED across two cross-invocations, and its results-dict digest compared EXACT-STRING against the disclosed `2e30020af5a97661506c29c962d047fb9561948098b09f5a826084b013f3ce29`. The verdict recommendation (APPROVE only iff diff exit 0 AND digest exact-match AND G1∧G2∧G3 in order) is recorded at close-out, never softened.

## ⟲ Previous-session review
Prior loop P105 → V118 (delegation-loop collapse in liquid democracy — uniform one-hop delegation over a random functional graph collapses the effective electorate to O(√N), concentrates ≈0.758N of the vote in one weakly-connected bloc, and leaves a sub-5% controlling cycle) landed APPROVE — sims/verdict-118-delegation-loop-collapse/, reproduced the committed verifier byte-identical with results-dict sha256 `c3e5e6c15bee39165e3ccfb3967135c06df2ccfd60af7d727b918cda47d49ea6` matching the disclosed digest EXACTLY. V118 and V120 share the reference-verifier-reproduction posture (byte-level digest match against the proposal's own committed stdlib verifier), distinct from an independent reimplementation. Carry-forward guard applied here directly: hash the verifier's OWN canonical bytes. NOTE the P107 verifier differs from P105's file-layout convention — P107 computes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` and `digest = sha256(canonical)` over the results dict, THEN adds `results["results_sha256"] = digest` and writes the file via `json.dump(results, fh, indent=2, sort_keys=True)`, so the WRITTEN `energy_cap_overflow_forfeiture_results.json` CONTAINS the self-digest field `results_sha256` (unlike P105, whose sha256 was a sibling key OUTSIDE the results dict). Therefore recomputing the digest from the written file requires OMITTING the `results_sha256` field before serializing (the field-omission step, P104-style), OR recomputing over the pre-digest results dict — replicate the compact one-line preimage `json.dumps(results, sort_keys=True, separators=(",",":"))` over the results dict WITHOUT its self-digest field, not over the indented on-disk file as-is. The script-printed `Results-JSON sha256:` line is the authoritative preimage (it hashes the dict before the self-field is added). No regressions expected at boot; check --strict to be confirmed clean.

## 💡 Session idea
The proof pins a SINGLE fixed cap C=120 against two fixed cadences (μ∈{60,480}) and shows the usable fraction is 1−e^{−C/μ}, concave in C and regressive in μ. But the operator lever the outbox flags — "bank overflow into an uncapped reserve (or scale the cap to realized cadence)" — is untested: it is asserted, not gated. A genuinely different next GAME slice pins a CADENCE-INDEXED cap policy C(μ̂) that scales the cap to each player's realized inter-login gap (e.g. C=k·μ̂ for a fixed multiplier k, μ̂ an online estimate of the player's own μ) over the same exponential-gap draws, and pre-registers two gates: (1) under the cadence-indexed policy the usable fraction 1−e^{−k} is CONSTANT across μ within 3σ — the regressive gap between frequent and casual players (V120's G2, z=1973σ) COLLAPSES below some equity floor (e.g. |W_casual−W_frequent| ≤ 0.05 at ≥3σ), proving the tax was an artifact of the FIXED cap, not of overflow forfeiture per se; and (2) a bounded-reserve variant (overflow banked into a reserve of finite depth R rather than discarded) recovers the usable fraction monotonically in R, crossing the WASTE_MIN=0.70-complement usable floor at a critical reserve depth R* identified within 3σ. That converts the flat "a discard-on-overflow cap is a regressive absence tax" into the actionable "indexing the cap to realized cadence (or banking overflow into a reserve) makes throughput rate-limited rather than arrival-frequency-limited, and the equity recovery is measurable per policy" — a seed-pinnable, ≥3σ-gateable mechanism (cadence-indexed cap equity recovery) distinct from this slice's fixed-cap regressive-forfeiture result and from the outbox's flat mitigation assertion.

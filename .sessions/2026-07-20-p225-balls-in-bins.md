# PROPOSAL 225 → VERDICT 238 — Balls into bins: the expected number of collisions (unordered pairs sharing a bin) is EXACTLY E[X] = C(m,2)/n = m(m−1)/(2n) — quadratic in the balls, by linearity over the C(m,2) pairs, NOT the naive m²/(2n) — stage the SEED=20260717 stdlib verifier so VERDICT 238 can rule on it

> **Status:** `in-progress`

📊 Model: opus-4.8 · high · review/verify

started: 2026-07-20T18:26:47Z

💓 Heartbeat: round-54 FLEET slot (fleet→venture→game→unrelated) P225 → V238 (+13 offset); verifier staged on branch `claude/p225-balls-in-bins`; sim dir `sims/verdict-238-balls-into-bins-collisions/` (stdlib verifier `balls-into-bins-collision-count.py` + run-stdout.txt + probe-report.md). Disclosed results-dict full-64 sha256 `874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a` (printed on the verifier's `results_sha256=` line, exact 64-char compare, no truncation). Determinism CONFIRMED (in-process `determinism_double_run=True` AND separate cross-invocation byte-identical stdout, sha256 `df886c2d1102774119b971fe56c55edc62c282f6c1dba7ebf512ad95d9e48279`). All 4 pre-registered gates PASS each in its OWN direction — G1 EXACT identity: E[X] built THREE independent exact ways (literal Σ Fraction(1,n) over all C(m,2) pairs == per-bin linearity n·C(m,2)/n² == closed form m(m−1)/(2n)) all equal, (365,23)→253/365, (256,64)→63/8, (1000,100)→99/20; G2 Monte-Carlo agreement z=−0.745423/−0.988399/−1.509722 all |z|<3 (200000 trials/config); G3 robustness — sweep (365,30)/(128,40)/(500,50)/(64,16) all |z|<3 (max 1.261990) AND exact-scaling Fraction identity E(2m)/E(m) n-independent (45/11, 254/63); G4 FALSIFIABILITY — the naive m²/(2n)=8.0 shortcut rejected at z=−20.566187 (overstatement exactly m/(2n)=1/8) plus the ordered-pair m(m−1)/n=15.75 impostor rejected at z=−1259.926656, while the true model agrees at z=−0.576503. Born-red HOLD armed on this first card commit; releases on the deliberate `complete` flip LAST.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only after the sim dir (stdlib verifier + captured reproduction stdout + probe-report), the digest print (full-64 exact `874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a`), the four-gate evaluation each in its own direction (all PASS), the determinism check (in-process double-run held AND separate-invocation stdout byte-identical), and the byte-pinned grounding (Wikipedia "Birthday problem" oldid 1357361405, sha1 d876b4f46b64278277ad0cf4b4bdf2ea0f271be1, 3-way match) have ALL landed. That flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation merge the green PR. LEFT RED this session — the coordinator drives the flip + landing after CI.

## What this verdict does

Stage the SEED=20260717 stdlib verifier for PROPOSAL 225 (balls into bins — the expected collision COUNT) in sim-lab so VERDICT 238 (+13 offset, round-54 FLEET slot) can rule on it. The counterintuitive twist the proposal pins: collisions are a PAIR phenomenon, not a per-ball one — throw m balls uniformly into n bins and the expected number of colliding unordered pairs (X = Σ_bins C(load,2)) is EXACTLY C(m,2)/n = m(m−1)/(2n), quadratic in m, so hash / shard / nonce collisions arrive far sooner than a linear per-ball 1/n intuition predicts (the birthday-attack mechanism). The plausible-but-wrong engineering shortcut m²/(2n) (as if a ball collided with itself) overstates the exact answer by exactly m/(2n) and is rejected. All 4 gates PASS deterministically. Disclosed results-dict sha256 = 874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a.

## Method

- Stdlib-only verifier (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`). SEED = 20260717; ALL randomness from a single `random.Random(SEED)` consumed in a fixed, documented order — G2 draws MC_TRIALS=200000 balls-into-bins trials per config (in CONFIGS order), then G3 the robustness sweep, then G4 the falsifiability sample, back-to-back, no interleaving.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the digest (`json.dumps(r, sort_keys=True, separators=(",",":"))` → `hashlib.sha256`); the dict carries no hash of itself; `__main__` builds the results twice in-process and asserts byte-identical canonical JSON (`determinism_double_run`) before printing `results_sha256=`, `determinism_double_run=`, `sim_ready=` and `sys.exit(0 if sim_ready and determinism else 1)`. Target `874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a` (full-64 exact).
- Core exact fact encoded: E[X] is computed THREE independent exact ways — (a) a LITERAL Σ `Fraction(1,n)` over all C(m,2) unordered pairs (added out in a double loop, NOT hardcoded), (b) the per-bin linearity route n·`Fraction(C(m,2), n·n)`, (c) the closed form `Fraction(m(m−1), 2n)` — and all three asserted exactly equal. No floats in the exact gate.
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 — EXACT identity** (direction: any of the three routes ≠ the others ⇒ FAIL): three routes equal for (365,23)→253/365, (256,64)→63/8, (1000,100)→99/20.
  - **G2 — Monte-Carlo agreement** (direction: |z| ≥ 3 ⇒ FAIL): sample mean vs exact E, z = (mean−E)/(sample_sd/√T). z = −0.745423 / −0.988399 / −1.509722 — all < 3 (200000 trials).
  - **G3 — Robustness** (direction: any swept config disagrees OR the scaling identity fails ⇒ FAIL): (a) four further configs (365,30)/(128,40)/(500,50)/(64,16) each |z| < 3 (max 1.261990); (b) exact-scaling ratio E(2m)/E(m) == (2m)(2m−1)/(m(m−1)) as identical Fractions, n-independent (45/11, 254/63).
  - **G4 — FALSIFIABILITY** (direction: PASS iff the wrong model is REJECTED at |z| ≥ 6): on the (256,64) sample whose mean agrees with the exact 63/8 (z=−0.576503), the naive m²/(2n)=8.0 shortcut is rejected at z=−20.566187 (overstatement exactly m/(2n)=1/8) and the ordered-pair m(m−1)/n=15.75 is rejected at z=−1259.926656.

## ⟲ Previous-session review

Previous-session review: PROPOSAL 221 → VERDICT 234 (round-53 FLEET slot) staged the reservoir-sampling Algorithm R verifier — every streamed item ends in the size-k reservoir with probability exactly k/n regardless of arrival position — landing full-64 digest `721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190` with four gates PASS via the born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction identity is a self-certifying theorem (zero error is the pass), a two-sided z-test is an AGREEMENT gate (small |z| is the pass), and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate (large |z| is the pass). V238 leans on the same discipline: G1 is an EXACT three-route Fraction identity (equality is the pass, and each route is computed independently — the literal pair-sum is added out, never telescoped to the closed form), G2/G3a are two-sided AGREEMENT gates (|z|<3), G3b is an exact-scaling identity, and G4 is a FALSIFIABILITY gate where the naive m²/(2n) shortcut must be rejected at large |z| (−20.57) and the ordered-pair impostor even harder (−1259.9). It also carries the V222/V234 GROUNDING-accuracy discipline: the byte-pinned Wikipedia "Birthday problem" page states the C(m,2) pair count and the Poisson mean 253/365 but NOT the general exact m(m−1)/(2n), so the caveat honestly attributes the pair count + rate as QUOTED and the general closed form as DERIVED — a deliberate correction of the QUALIFIED-for-implying-quoted risk. Standing non-contiguity persists: landing V238 does not imply every lower verdict below the high-water is closed.

## 💡 Session idea

The verifier pins the expected collision COUNT (first moment) exactly. A cheap, orthogonal follow-on (call it P-next) would pin the exact VARIANCE of X, Var[X] = C(m,2)/n·(1 − 1/n) + 6·C(m,3)/n²·(1 − 1/n) … (the pair-indicator covariance sum) — or, more simply, assert that as n,m→∞ with C(m,2)/n → λ fixed, X converges to Poisson(λ) by matching the exact factorial moments E[X(X−1)…] to λ^k, turning "the mean is exact" into "the whole low-order moment structure is exact and the Poisson limit is earned, not assumed." It reuses the `collision_count` reducer and the exact-`Fraction` harness verbatim — the C(m,2)/n kernel is the ready oracle — and the grounding pin (Wikipedia "Birthday problem" oldid 1357361405) already states the Poisson approximation `Poi(C(23,2)/365)` on the same revision, so the moment-matching stays byte-pinned to one page. (Guard recipe for a later session: the anchor is `collision_count` / `exact_closed`; add a `gate_moments` that compares the sample k-th factorial moment to `Fraction(C(m,2),n)^k`-style exact predictions on the shared small configs.)

## Close-out

Sim dir `sims/verdict-238-balls-into-bins-collisions/` staged on branch `claude/p225-balls-in-bins`: stdlib verifier `balls-into-bins-collision-count.py`, captured `run-stdout.txt` (stdout sha256 `df886c2d1102774119b971fe56c55edc62c282f6c1dba7ebf512ad95d9e48279`), and 8-question `probe-report.md` ending `Recommendation: sim-ready`. Full-64 results digest `874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a`; `determinism_double_run=True` AND separate cross-invocation byte-identical. All four gates PASS each in its own direction (G1 exact three-route Fraction identity == m(m−1)/(2n) every config; G2 z=−0.745423/−0.988399/−1.509722 all <3; G3 four-config sweep all |z|<3 max 1.261990 + exact-scaling identity; G4 naive m²/(2n) rejected at z=−20.566187 with overstatement exactly 1/8, ordered-pair impostor rejected at z=−1259.926656). Grounding byte-pinned to Wikipedia "Birthday problem" oldid 1357361405, sha1 `d876b4f46b64278277ad0cf4b4bdf2ea0f271be1` (3-way match). This card is LEFT RED (`in-progress`) — the born-red HOLD stays armed; the coordinator drives the deliberate `complete` flip + landing after CI. NO merge API calls from this session. VERDICT 238 = sim-ready; ruling pending.

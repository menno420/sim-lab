# VERDICT 174 — palm-mginf-insensitivity: the stationary number of units in an M/G/∞ repair pipeline is Poisson(λ·E[S]), insensitive to the repair-time distribution beyond its mean, so pipeline occupancy equals offered load with variance equal to mean (dispersion 1.0) regardless of repair-time variability

Reproduce PROPOSAL 161 (P161 → V174, +13): Palm's theorem / M/G/∞ repair-pipeline insensitivity — the spares you must hold depend on the MEAN repair time, not its variance. Under the pinned world Poisson unit-failure arrivals at rate λ feed an ample-server (M/G/∞) repair pipeline, each failed unit independently in repair for an i.i.d. time S; the stationary number in repair N is measured empirically at integer instants post-warmup across R replications and compared against the offered load λ·E[S] and against the Poisson dispersion 1.0, under three repair laws of IDENTICAL mean but different variability (deterministic CV=0, exponential CV=1, lognormal CV=3). The folk belief (inverted here): "erratic, high-variance repair times force you to hold more spares — smooth the repair process and you can stock fewer." It is NOT: by Palm's theorem (1938; the foundation of Sherbrooke's METRIC spares model, 1968) the ample-server pipeline N is Poisson(λ·E[S]), insensitive to the repair-time shape beyond its mean — same mean, same Poisson pipeline, same spares — so cutting repair-time VARIABILITY while holding the mean fixed does NOT reduce the spares you need; only the mean repair time (or the failure rate) moves the pipeline. Three ordered z-gates on the relative-error-vs-ceiling convention (z_gate=3.0, CEIL=0.05): G1 pipeline-is-load (measured E[N] matches the offered load λ·E[S] within the ceiling at ≥3σ — Little's law for the M/G/∞ pipeline) → G2 variance-insensitivity (under a HIGH-variance repair law — lognormal, CV=3 — the pipeline's index of dispersion Var(N)/E[N] stays at the Poisson value 1.0 within the ceiling at ≥3σ; readout: the M/G/1 wait multiplier (1+CV²)/2 for the same CV, which the pipeline shows NONE of) → G3 distribution-free (across the three repair laws of identical mean the pipeline mean AND dispersion index agree within the ceiling at ≥3σ — the spares requirement is the same for all three). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (the ample-server M/G/∞ / Palm insensitivity) and a variance-insensitivity SIGN claim — the SIGN (repair variability does not inflate the pipeline; smoothing buys no spares) survives, but the exact insensitivity is a property of the AMPLE-server pipeline; a finite-server WAITING line (M/G/1) DOES pay a variance tax — Pollaczek–Khinchine wait ~ (1+CV²)/2 — and that system-dependence is a disclosed descriptive field, not a hidden defect.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P161 reference verifier byte-identical under its pinned world (SEED=20260717; LAM=1.0; MEAN_S=10.0 → LOAD=λ·E[S]=10.0; HORIZON=60000; WARMUP=8000; CV_HI=3.0; CEIL=0.05; R=30 replications, three repair laws per replication — deterministic, exponential, lognormal — with the number in repair sampled at integer instants post-warmup, dispersion Var(N)/E[N] measured empirically per replication rather than plugged from the Poisson closed form; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `9bbe2171bd7678fc7efa03c550f0afbcec438498b0553a1723ee078bf5ec13ef`, (b) all three gates hold in order G1 pipeline-is-load → G2 variance-insensitivity → G3 distribution-free, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the M/G/∞ pipeline is Poisson(λ·E[S]) — occupancy equals offered load with dispersion 1.0 — insensitive to repair-time variability, so a CV=3 lognormal repair law needs the SAME spares as a deterministic one; the M/G/1 wait tax (1+CV²)/2, which the pipeline shows none of, is the disclosed readout). Pre-registered decision rule (P161): APPROVE VERDICT 174 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the exact insensitivity being a property of the ample-server M/G/∞ (Palm) pipeline (a finite-server WAITING line M/G/1 recovers the (1+CV²)/2 variance tax) is a DISCLOSED descriptive field of a variance-insensitivity SIGN claim, not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 174, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: `ideas/fleet/palm_spares_insensitivity.py` @ idea-engine — git blob `da7466ec6c75c22993344dc1caf983babee2cccf`, file sha256 `9a8ba388887c270463a9713d63f96788d0eb552872a025807e0dd9c737c3fc21` (8479 bytes, 233 lines). The committed P161 source in its fleet lane, copied byte-identically (`diff` exit 0, copy sha256 == source, copy git blob == source blob) into the reproduction commit at `sims/verdict-174-palm-mginf-insensitivity/palm_spares_insensitivity.py` — copy sha256 `9a8ba388887c270463a9713d63f96788d0eb552872a025807e0dd9c737c3fc21`, copy git blob `da7466ec6c75c22993344dc1caf983babee2cccf` (8479 bytes, 233 lines).
- Offset authority: +13 (P161 → V174), round-38 FLEET slot (fleet-ops spares / maintenance pipeline). Per idea-engine `control/outbox.md` P161 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: the M/M/∞ / M/G/∞ ample-server queue — the stationary number in system is Poisson with mean λ·E[S] and is INSENSITIVE to the service (repair) distribution beyond its mean (Palm's theorem, 1938; Sherbrooke METRIC, 1968), so Var(N)=E[N] (dispersion 1.0) for every repair-law shape of the same mean. Grounding: https://en.wikipedia.org/wiki/M/M/%E2%88%9E_queue — the domain reference resolved LIVE this session (HTTP 200); the closed forms are hand-checked against the reproduced Monte-Carlo numbers in the Outcome below.
- Disclosed digest: compact-canonical results-dict sha256 `9bbe2171bd7678fc7efa03c550f0afbcec438498b0553a1723ee078bf5ec13ef`, SEED=20260717 — reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (confirmed): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V171/V172/V173). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent run and asserts the two dicts are equal (in-process double-run byte-identity), prints a pretty indent=2 dump plus the sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. Confirmed byte-exact in the reproduction commit.

## Constraints honored
Stdlib-only (math, json, hashlib, random); verifier copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The pipeline mean and dispersion index are measured empirically — the number in repair N is accumulated at integer instants post-warmup across R=30 replications and its dispersion Var(N)/E[N] computed from the drawn occupancy, not plugged from the Poisson closed form — so the insensitivity is attributable to the drawn M/G/∞ occupancy alone, not to a tautological substitution of the analytic dispersion 1.0. The gates are definitional contrasts on those drawn statistics: G1 pipeline-is-load (measured E[N] vs offered load λ·E[S]) and G2 variance-insensitivity (dispersion under the CV=3 lognormal law vs the Poisson 1.0) at the base world; G3 distribution-free re-checks the mean AND dispersion agree across the three equal-mean repair laws (CV 0 / 1 / 3) as the head's cross-law control. Honest scope: the head is EXACT GIVEN THE MODEL and a variance-insensitivity SIGN claim — it holds for the ample-server M/G/∞ (Palm) pipeline and the exact insensitivity is system-dependent (a finite-server WAITING line M/G/1 recovers the (1+CV²)/2 variance tax); that system-dependence of the exact result is a disclosed descriptive field, and no gate reads the insensitivity as a universal system-independent constant.

## Gate plan (reproduced at HEAD), order G1 → G2 → G3
- **G1 pipeline-is-load (z ≥ 3, one-sided vs the relerr ceiling 0.05):** z = (CEIL − load_relerr_mean)/se across the R=30 replications, where load_relerr = |measured E[N] − LOAD|/LOAD under the exponential repair law — the measured pipeline occupancy matches the offered load λ·E[S]=10.0 (Little's law for the M/G/∞ pipeline). Reproduced load_relerr_mean **0.004443**, z **+68.279173** (ceiling 0.05) — **PASS**.
- **G2 variance-insensitivity (z ≥ 3 vs the relerr ceiling 0.05):** z = (CEIL − disp_relerr_mean)/se, where disp_relerr = |Var(N)/E[N] − 1.0| under the HIGH-variance lognormal repair law (CV=3) — the pipeline's index of dispersion stays at the Poisson value 1.0; repair variability does NOT inflate the pipeline. Reproduced disp_relerr_mean **0.021783**, z **+12.870661** (ceiling 0.05). Readout: the M/G/1 wait multiplier (1+CV²)/2 = **5.0×** for the same CV=3, which the ample-server pipeline shows NONE of — **PASS**.
- **G3 distribution-free (z ≥ 3 vs the relerr ceiling 0.05):** z = (CEIL − spread_mean)/se, where spread = max cross-law gap of the pipeline mean (÷LOAD) and dispersion index across the three equal-mean repair laws (deterministic CV=0, exponential CV=1, lognormal CV=3) — all three give the SAME Poisson(10) pipeline: same mean, same dispersion, same spares. Reproduced spread_mean **0.031331**, z **+10.750843** (ceiling 0.05) — **PASS**.
- z_gate **3.0** (require z ≥ 3 on every gate). **Disclosed caveat (NOT a gate):** the exact insensitivity (the pipeline Poisson(λ·E[S]) for every repair-law shape of the same mean; the M/G/1 wait tax (1+CV²)/2 = 5.0× at CV=3, the reported readout) is a property of the ample-server M/G/∞ (Palm) model; a finite-server WAITING line recovers the (1+CV²)/2 variance tax. The SIGN (the pipeline does not inflate with repair variability; the waiting line does) and the order-of-magnitude survive, but the exact result is a disclosed system-dependent descriptive field, and NO gate reads it as a universal system-independent constant.

## Probe questions (independent-audit checklist)
**1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest.** — **YES.** Reproduced digest `9bbe2171bd7678fc7efa03c550f0afbcec438498b0553a1723ee078bf5ec13ef` — printed by the script (`json.dumps(res, sort_keys=True, separators=(",",":"))` over the 6-dp dict, then sha256), independently recomputed from the stdout JSON block, and identical to the disclosed value. **MATCH.** Two fresh `python3` invocations with SEED=20260717 produced byte-identical stdout (`diff` exit 0), and the script's in-process double-run digest assert held on every run.

**2. Is the verifier byte-identical to the P161 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.** — **YES.** `diff` source vs sim-lab copy exit **0** (byte-identical); copy git blob `da7466ec6c75c22993344dc1caf983babee2cccf` and file sha256 `9a8ba388887c270463a9713d63f96788d0eb552872a025807e0dd9c737c3fc21` both match the pinned P161 identity at `ideas/fleet/palm_spares_insensitivity.py` exactly (8479 bytes, 233 lines).

**3. Do all three gates PASS in the pre-registered order G1 pipeline-is-load → G2 variance-insensitivity → G3 distribution-free with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0?** — **YES.** G1 pipeline-is-load load_relerr_mean 0.004443 z=+68.279173 (ceiling 0.05) → G2 variance-insensitivity disp_relerr_mean 0.021783 z=+12.870661 (ceiling 0.05) → G3 distribution-free spread_mean 0.031331 z=+10.750843 (ceiling 0.05) — all ≥ 3; `gates=[true,true,true]`, `all_pass=true`, `first_failing_gate=null`, exit **0**.

**4. Is the headline read correctly — the M/G/∞ pipeline is Poisson(λ·E[S]) (occupancy equals offered load, dispersion 1.0) INSENSITIVE to repair-time variability, so a CV=3 lognormal repair needs the SAME spares as a deterministic one — and NOT as "high-variance repair times force you to hold more spares"?** — **YES.** At LOAD=10.0 the measured pipeline mean matches the offered load (load_relerr 0.004443) and the dispersion stays at the Poisson 1.0 even under CV=3 lognormal repair (disp_relerr 0.021783), and all three equal-mean laws agree (cross-law spread 0.031331). Dispersion is measured from the drawn occupancy at integer instants, not plugged from the Poisson closed form. The "erratic repairs force more spares" folk belief conflates the finite-server WAITING-line variance tax (M/G/1 P-K wait ~ (1+CV²)/2 = 5.0× at CV=3) with the ample-server REPAIR PIPELINE, which shows none of it.

**5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact insensitivity being a property of the ample-server M/G/∞ (Palm) pipeline (a finite-server M/G/1 recovers the (1+CV²)/2 variance tax) flips no gate SIGN and no order-of-magnitude?** — **YES.** The exact insensitivity (pipeline Poisson(λ·E[S]) for every repair-law shape of the same mean) is a property of the ample-server M/G/∞ (Palm) model, disclosed as a system-dependent descriptive field, not a universal system-independent claim — a finite-server WAITING line would recover the usual (1+CV²)/2 variance tax. No gate reads the insensitivity as universal; the gates test the pipeline-is-load SIGN (G1), the variance-insensitivity SIGN under CV=3 (G2), and distribution-freeness across equal-mean laws (G3). The caveat flips no gate sign and no order-of-magnitude — a clean APPROVE.

**6. Could the variance-insensitivity sign be a seed fluke? State whether the effect is structural (the pipeline-is-load and dispersion-1.0 hold across the exponential base and the three equal-mean repair laws) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical).** — **NOT a fluke; structural.** The pipeline-is-load holds under the exponential law (z=+68.279173) and the dispersion stays at 1.0 under the CV=3 lognormal law (z=+12.870661), and all three equal-mean laws agree (z=+10.750843) — z-scores tens of sigma above the ceiling across independent repair-law shapes — structural, not a seed artifact. Determinism confirmed both ways: the in-process double-run digest assert held, and two `python3` invocations produced byte-identical stdout (`diff` exit 0), both exit 0.

## Outcome
**APPROVE.**

- **Digest MATCH.** The compact-canonical results-dict sha256 reproduced EXACTLY as `9bbe2171bd7678fc7efa03c550f0afbcec438498b0553a1723ee078bf5ec13ef` — printed by the script and independently recomputed from stdout (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY; every float rounded to 6 dp), byte-identical across two fresh `python3` invocations and the in-process double-run assert. Verifier byte-identical to the P161 source `ideas/fleet/palm_spares_insensitivity.py` (`diff` exit 0, file sha256 `9a8ba388…3c21`, git blob `da7466ec6c75c22993344dc1caf983babee2cccf`, 8479 bytes / 233 lines). SEED=20260717 is pinned in-source (module constant), so the run is reproducible without external configuration (the exported `SEED` env var is inert).
- **Three gates PASS in order** (z_gate=3.0): G1 pipeline-is-load load_relerr_mean **0.004443**, z **+68.279173** (ceiling 0.05) → G2 variance-insensitivity disp_relerr_mean **0.021783**, z **+12.870661** (ceiling 0.05) → G3 distribution-free spread_mean **0.031331**, z **+10.750843** (ceiling 0.05). `gates=[true,true,true]`, `all_pass=true`, `first_failing_gate=null`, exit 0.
- **Closed-form hand-check agrees.** For an M/G/∞ pipeline with Poisson(λ) failure arrivals and i.i.d. repair time S: the stationary number in repair N is Poisson(λ·E[S]), so E[N]=Var(N)=λ·E[S]=10.0 and the index of dispersion Var(N)/E[N]=1.0 for EVERY repair-law shape of the same mean (Palm insensitivity). The measured E[N] matches LOAD=10.0 (load_relerr 0.004443) and the dispersion stays at 1.0 even at CV=3 (disp_relerr 0.021783), with the three equal-mean laws agreeing (spread 0.031331). The head reads correctly: the ample-server pipeline is variance-insensitive, NOT "erratic repairs force more spares"; the M/G/1 wait tax (1+CV²)/2 = 5.0× at CV=3 is the disclosed readout the pipeline shows none of.
- **Model-basis note.** The model-basis caveat is CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE: the exact insensitivity (pipeline Poisson(λ·E[S]) for every repair-law shape of the same mean) is a property of the ample-server M/G/∞ (Palm) model that a finite-server WAITING line would replace with the (1+CV²)/2 variance tax — a disclosed system-dependent field that flips no gate sign or order-of-magnitude. The gates test the pipeline-is-load SIGN (G1), the variance-insensitivity SIGN under CV=3 (G2), and distribution-freeness across equal-mean laws (G3); none reads the insensitivity as a universal system-independent constant.

**Verdict: APPROVE.**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-173-cauchy-no-averaging.md` (V173, P160 Cauchy no-averaging / failure of the Law of Large Numbers, round-37 UNRELATED slot, +13) is coherent and landed a CLEAN APPROVE, the prior verdict high-water: disclosed compact-canonical results-dict digest `413e5692…b7b5c` reproduced EXACTLY, verifier byte-identical (git blob `460d9ffea7b80339ceebb4fb4cac1bd3d983d77a`, file sha256 `44584333…44a8`, 7470 bytes / 229 lines), all three gates G1 mean-does-not-concentrate → G2 median-does-concentrate → G3 robust-under-shift PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form (sample mean identically Cauchy for every n → no 1/√n concentration; sample median Normal(0,(πγ)²/4n) → ~1/√n concentration) confirmed — the one disclosed field (the exact no-shrink being a property of the α=1 stable law) touches no gate sign. DIGEST-POSTURE carry-forward unbroken: V173 and this V174 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no JSON written to disk, the compact-canonical serialization is the digest preimage). Object shift: V173 was the round-37 UNRELATED-slot Cauchy no-averaging / LLN-failure head (a no-concentration SIGN claim on the α=1 stable law); this V174 is the round-38 FLEET-slot Palm / M/G/∞ repair-pipeline insensitivity head — likewise a SIGN / order-of-magnitude claim (variance-insensitivity of the ample-server pipeline) whose exact result is a self-disclosed system-dependent descriptive field (a finite-server M/G/1 recovers the (1+CV²)/2 variance tax), earning the same clean-APPROVE posture (reproduced this session: digest MATCH, all three gates PASS). CONTIGUITY: union-max advances the verdict high-water to **V174**, no regress over V173. Items open/backfilled BELOW the high-water are NOT closed by landing V174 — V137/P124, V132/P119, V126/P113 remain open below high-water; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V174 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the AMPLE-server (M/G/∞) repair pipeline — the case where Palm insensitivity is TOTAL (the pipeline Poisson(λ·E[S]) for every repair-law shape) — and contrasts three equal-mean repair laws at a single load, with the M/G/1 wait tax as a passive readout. The distinct next slice the head names but holds fixed — the FINITE-server frontier between loss and waiting systems: the M/G/c/c LOSS system (Erlang-B) is ALSO variance-insensitive — Erlang's loss formula depends only on the offered load A=λ·E[S], not the repair-law shape — whereas the M/G/1 WAITING system's mean delay DOES scale with (1+CV²)/2. Pre-register (same SEED-pinned, empirically-measured occupancy discipline): sweep the system type and gate (G-a) the M/G/∞ base reproduces the dispersion-1.0 insensitivity at ≥3σ, (G-b) an also-insensitive headline — the M/G/c/c loss probability matches Erlang-B(A,c) across the three equal-mean repair laws at ≥3σ (finite-server LOSS is variance-insensitive too), and (G-c) a system-crossover — the M/G/1 mean wait DOES scale with (1+CV²)/2 across the same laws at ≥3σ, so variance-sensitivity switches ON exactly when the system moves from loss/ample to waiting. The operator lesson sharpens from "repair variability buys no spares for the pipeline" to "insensitivity is a property of LOSS and AMPLE-server systems (spares pools, trunk lines), variance-sensitivity of WAITING systems (queues) — which regime you're in decides whether smoothing repair variability buys anything" — distinct from this V174 head, which pins the ample-server M/G/∞ pipeline and asks only whether repair variability inflates it.

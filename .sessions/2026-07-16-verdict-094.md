# Session — VERDICT 094 — the dedupe with a rotating alibi: the kit's guard-fires tail-scan dedupe (600 s window / 200-line scan) is a REGIME SYSTEM, not a graceful degrader — the scan cap is a cliff (F = 200 leaks 0/run forever, F = 201 leaks 1/run forever, leaked identity rotating with period F/gcd(F, F−200)), the exempt verdict rows COMPOUND (one allowlist suppression per run ramps a boundary-clean burst to 19/run and sustains exactly 14/run — 13× the one-for-one intuition), the steady record rate is NON-MONOTONE in check cadence (a 601 s checker writes exactly 1200/601 ≈ 2× more per day than a 600 s one), and "bounded scan" bounds parses while the whole-file read bill grows quadratically exactly when the leak fires (idea-engine PROPOSAL 081, the round-17 fleet-backlogs rotation opener, substrate-kit re-tap; P081 → V094 under the +13 offset)

> **Status:** `complete`
> 📊 Model: fable-5 · high · verdict-sim

Objective: produce VERDICT 094 for idea-engine PROPOSAL 081 (the
guard-fires dedupe regime cliff,
`ideas/substrate-kit/guard-fires-dedupe-regime-cliff-2026-07-16.md`,
outbox block `## PROPOSAL 081 · 2026-07-16T07:10:30Z · status: sim-ready`,
live in idea-engine `control/outbox.md`; the proposal landed via
idea-engine PR #448, merged @ 98c15e7, and the drafter's seat claim for
this verdict rides the idea-engine claims ledger at 87c7866). One slice,
one branch (`claude/verdict-094-guard-fires-dedupe-regime-cliff`), one
verdict. NUMBERING, verified literally at sim-lab origin/main `ce5d1a6`
(VERDICT 093 merged via PR #164 — the tip IS the V093 merge): newest
`## VERDICT` header is 093, `## VERDICT 094` and `## INTAKE 081`
collision-grepped with hit CLASSIFICATION per the V093 card's taxonomy
(claims vs predictions): every hit is PREDICTION-class — the coordinator
heartbeat's next-expected line (`control/status.md:11`), the offset map's
next-expected row (`docs/current-state.md:314`), and idea-engine's own
V093-close-out batons — and zero hits are claim-class (no `## VERDICT
094` ledger header anywhere, no `sims/verdict-094-*` path, no session
card, no remote ref or PR; zero open sim-lab PRs verified via the GitHub
API this session at session start), so idea-engine PROPOSAL 081 →
**VERDICT 094**, the +13 offset's eighteenth row (map-row extension
lands in `docs/current-state.md` this same PR). Worker session; ledger
appended to `control/outbox.md` only — `control/inbox.md` untouched
(manager-order file); this slice also refreshes the coordinator
heartbeat `control/status.md` (next-expected roll to P082 → V095). No
idea-engine claim file written by this slice (the V074–V093 precedent —
the collision-grep + born-red card + READY PR bind the number; the seat
claim is the drafter's, idea-engine side). Seeds: the drafter's
registered set **20261718–721** IS the session seed set (the V077–V093
precedent), quoted literally from the registration — Arm-R
reporting-only 20261718 (N = 20,000) / 20261719 (N = 8,000) under the
REGISTERED draw-order grammar (one `random.Random` per seed; per trace
EXACTLY 4 `rng.randint` draws in registered order F ∈ [1, 400],
V ∈ [0, 8], c ∈ [60, 1200], R ∈ [2, 12]; draw-count sentinel exactly 4N:
80,000 / 32,000), presentation shuffle 20261720 (presentation legs
only), aux 20261721 reserved and asserted never read; 20261714–717 are
P080/V093's registered set — untouched; the next free block after this
allocation starts at **20261722**. This is a SHIPPED-ALGORITHM
deterministic-replay head (nearest method kin: P079's deterministic
replay of a shipped mechanism, P077's true-sentence-survives move, P080/
V093's no-stochastic-gate + margin-ledger + aux conventions) — the
model replays three PINNED source behaviors (strict-`>` age boundary,
last-S-LINES slot accounting, suppressed-first call order) read from
~150 lines of vendored source at the grounding HEAD; every decision leg
is seedless exact integer counting; zero repo/network reads at verdict
time. This card held the substrate gate red deliberately until this flip
(the born-red discipline — the designed hold was the only red this
branch produced itself).

Flagged, decide-and-flag: (a) the live `control/outbox.md` measured
481,859 B pre-append (505,807 B post-append this slice) — above the
fleet 200KB rollover threshold; rolling stays PARKED with the manager
(inbox ORDER 005 item 5; the V086–V093 precedent — append normally,
follow-up is the manager's). (b) The registration pins every decision
census/orbit/identity exactly but a few slots by SHAPE — the Arm-R
trace-census field definitions behind the preview triples (leak-regime
trace, max per-run burst, total appends), the 8 probed cells of the
order-flip control, the rotation-leg horizon, the naive-ts probe cell,
and the presentation-shuffle target; the sim-chosen realizations are
disclosed in fixtures.json and ledgered as vacancy-derived disclosures
(the registered counts/triples/orbits are the match claims — all
reproduce exactly). (c) The three pinned source behaviors were
re-verified FIRSTHAND this session against the vendored module in the
local idea-engine clone (bootstrap.py: strict-`>` age test at the window
compare, `lines[-200:]` slot accounting with verdict/naive/malformed
rows keyless-but-slot-occupying, suppressed-first cmd_check call order,
whole-file `read_text()` with ONE batch-start scan per
`record_guard_fires` call) — a session-side read at build time; the
runner itself performs zero repo reads.

## What happened

Built `sims/verdict-094-guard-fires-dedupe-regime-cliff/` under the
standing discipline: fixtures.json committed BEFORE the runner (born-red
card 84215a4 → fixtures 2f3dd5e → runner + README 81fe296 → accepted run
outputs + REPORT ae98367 → control appends + map row + heartbeat e4d55d0
→ this flip), three-arm: Arm A seedless faithful tail-slice replay of
the pinned mechanism + the closed forms on their proven domains
(DECISION-bearing — the renewal law p(c) = c·(⌊W/c⌋+1), the cliff law
max(0, F−S), the saturated law min(F, F−S+V), the rotation period
F/gcd(F, F−S), the quadratic read sum); Arm B INDEPENDENTLY-WRITTEN
depth-arithmetic twin replay over a differently-shaped log (no line list
— a global line counter plus per-key last-append registers, visibility
by depth-from-end arithmetic), tied through the typed contacts C1–C4;
Arm R seeded reporting-only scenario traces under the drafter's
REGISTERED draw-order grammar (seeds 20261718/20261719, 4 `randint`
draws per trace in order F/V/c/R, draw-count sentinels 4N asserted:
80,000 / 32,000; presentation shuffle 20261720; aux 20261721 never
read). 52 self-checks, 52 passed, 0 failed; exit 0; ≈ 12.6 s/run;
byte-identical double run (results 6de930da…, run-stdout d35c62b2…,
sha256 in REPORT.md); CPython 3.11 asserted; ZERO rehearsal fixes — the
FIRST rehearsal run of the committed runner logic was already 52/52
green (the pre-fixture prototype pinned two realization details, the
leak-phase peak definition and the rotation-leg horizon, disclosed in
fixtures.json before the runner existed) — and zero fix-forwards: the
first complete in-repo run is the accepted run. PR #165 opened READY at
slice start.

## Results

**VERDICT 094 — REJECT, all four registered clauses exact, ZERO
anomalies (structured census: 98 compared-and-matched, 0 mismatched, 6
honest vacancies).** (R1) the renewal day table 131/121/97/81/73/144/
131/97/73 exact on all 9 cadences (replay == closed law, shipped AND
keyed arms) with the witness pair p(600) = 1200 / p(601) = 601, rate
ratio exactly 1200/601 — the record rate is non-monotone in cadence;
(R2) the V = 0 leak row 0/0/0/0/1/50/100/200 == max(0, F−200) with the
(200, 201) margin-0 pair, rotation periods 201/5/3/2 with first repeats
at run 2 + period (F = 201 at run 203) and the F = 250 blocks
0–49/50–99/100–149; (R3) the saturated law min(F, F−200+V) exact on all
13 qualifying cells with the (398,1)/(397,1) boundary pair, the (200,1)
orbit (prefix [200, 1, 2, …, 11], peak 19 @ run 20, tail mean exactly
14, total 1025 vs naive 79), (200,50) → 100/run, (250,50) → the exact
2-cycle {100, 150} (total 9850), (1,50) → period 4, and all 13
registered compounding totals at their cells; (R4) the breathing orbit
201, (1,1,199)×7, 1, 1 (total 1610) with the window-off collapse to
1/run, and the read censuses 13,000/2,000 (last run 2,200) and
20,122/4,600 with the scan-off control at leak 0. The TRUE SENTENCE
survives (design point re-runs append exactly 0); the three repairs are
priced (S′ = 1000 relocation exact; keyed index zero-leak with renewal
preserved; scan-off leak 0). APPROVE did not fire and is arithmetically
excluded ((201, 0) leaks 1/run; 73 → 144 at 600 → 601); both Arm-R
preview triples reproduced EXACTLY ((19,095, 400, 21,187,561) /
(7,653, 400, 8,512,842)).

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 093, sim-lab
PR #164 → ce5d1a6): a clean landing whose conventions this slice
consumed wholesale — born-red card / READY PR / no-claim-file /
registered-allocation-as-seed-set / typed margin ledger / structured
anomaly census / vacancy-derived disclosures carried verbatim, the +13
offset extended to its eighteenth row in the same grammar. V093's 💡
(the collision grep must CLASSIFY hits — claims vs predictions) BOUND
this slice directly and did exactly its job: the numbering grep hit
"VERDICT 094" at the live tip (the V093 refresh's own heartbeat/map
next-expected pointers), and the taxonomy let the slice classify both
hits PREDICTION-class and proceed without either a false abort or a
blind ignore — the classification is recorded in the INTAKE 081 block,
the first slice to apply the rule since it was coined. V093's seed baton
(20261714–717, aux never read; "next free block starts 20261718") made
the P081 allocation land exactly where the drafter registered it.
Nothing was left pending for this slice to resolve.

💡 **Session idea (genuine, this session):** REGISTERED ANCHOR LISTS
MUST CARRY THEIR CELL COORDINATES. P081's F3 block registers thirteen
compounding totals as a bare positional list ("969/2236/…/8685 on the
registered cells") — the VALUES are pinned but the value→cell mapping is
not, so the verdict session had to recover the mapping at rehearsal (it
resolved F-major over F ∈ {199, 200, 201, 250, 300} × V ∈ {1, 5, 50}
minus the two separately-registered cells, and every value landed
positionally — disclosed in fixtures.json). The gate this creates is
weaker than it looks: a sim wrong in a way that PERMUTES per-cell totals
would still pass a multiset comparison, and "the list matched under the
ordering we inferred" is one epistemic rung below "cell (250,5) == 5125".
The durable rule for drafters: register anchor DICTIONARIES keyed by
cell, never bare value lists — and when a list is unavoidable, register
the ordering rule beside it ("F-major, V inner, skip (200,1)"), so the
mapping is a registration fact instead of a rehearsal artifact. Dedup:
V078's 💡 (registration-time anchor SELF-EVALUATION — check each "value
= expression" pair) is about anchor VALUES being self-consistent; this
is about anchor ADDRESSES being registered at all; no prior card,
README, or REPORT states the list-vs-dictionary rule (grepped .sessions/
+ sims/ at ce5d1a6 — "anchor list"/"cell mapping" hits are unrelated
prose in V031/V078).

📊 Model: fable-5 · high · verdict-sim

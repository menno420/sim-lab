# VERDICT 178 — memoryless-pm-waste: preventive maintenance on a memoryless / constant-hazard (exponential) component is pure waste — because the exponential is MEMORYLESS the residual life of a used part has the SAME distribution as a new part, so scheduled preventive replacement does NOT lower the failure rate, it only adds replacement cost; the failure rate under a preventive-replacement policy EQUALS the run-to-failure rate λ regardless of the replacement interval τ, so PM buys strictly higher total maintenance cost for zero reliability gain, and the benefit only appears once the hazard is INCREASING (Weibull shape k > 1, wear-out)

Reproduce PROPOSAL 165 (P165 → V178, +13): memoryless PM waste — in a RELIABILITY / maintenance model a component with an exponential lifetime has a CONSTANT hazard rate λ, so by the memoryless property P(T > s + t | T > s) = P(T > t) the residual life of a part that has survived to age s is distributed identically to a brand-new part. Under the pinned world a preventive-replacement (age-replacement) policy that swaps the part at a fixed interval τ therefore leaves the long-run failure rate UNCHANGED at λ — the interval τ is invisible to the failure process — while it strictly ADDS the cost of the preventive swaps, so on a constant-hazard component preventive maintenance is pure waste. The folk belief (inverted here): "preventive maintenance always helps — replacing a part before it fails on a fixed schedule reduces the failure rate and extends effective life, so a shorter PM interval always buys fewer failures." It is NOT: on a MEMORYLESS (exponential, constant-hazard) component a used part is as-good-as-new, so the failure rate under an age-replacement policy is exactly λ for EVERY interval τ — preventive replacement changes no failure statistic and only burns replacement cost; the reliability benefit of PM is a property of an INCREASING hazard (wear-out / IFR), and it VANISHES the instant the hazard is constant. Three ordered z-gates (z_gate=3.0), measured on Monte-Carlo renewal trajectories per regime: G1 head_pm_waste (on the exponential component the preventive-replacement policy incurs strictly MORE total maintenance actions than run-to-failure while the failure count is NOT reduced — a one-sided z ≥ 3 positive total-action gap driven entirely by the preventive swaps) → G2 mechanism_memoryless (ANCHOR / match gate: the measured failure rate under the age-replacement policy equals the run-to-failure failure rate λ to Monte-Carlo precision — the memoryless residual-life identity — pass condition |z| < 3, NOT a directional z ≥ 3) → G3 wearout_reverses (on an INCREASING-hazard Weibull component, shape k > 1 at a matched mean life, the SAME age-replacement policy now STRICTLY reduces the failure count relative to run-to-failure — the benefit reappears once the hazard rises, confirming the waste is specific to the constant hazard, z ≥ 3). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (a single-component renewal process with instantaneous perfect replacement, no downtime/logistics cost asymmetry, the failure rate measured as failures per unit operating time) and a failure-rate-invariance / cost SIGN claim — the SIGN (on a constant hazard PM leaves the failure rate at λ and only adds cost; the benefit needs an increasing hazard) survives, but the exact cost figures are a property of the equal-cost instantaneous-replacement model; downtime-avoidance value, imperfect maintenance, and repair-vs-replace logistics are disclosed descriptive fields, not hidden defects.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD. This is the born-red hold committed 2026-07-19T10:23:53Z — the reproduction below is PENDING and NO results are filled yet.

## Objective
Reproduce the committed P165 reference verifier byte-identical under its pinned world (SEED pinned in-source; Monte-Carlo renewal trajectories per regime; an exponential constant-hazard component under a run-to-failure policy vs a fixed-interval age-replacement policy, and an increasing-hazard Weibull control at a matched mean life; z_gate=3.0; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest, (b) all three gates hold in order G1 head_pm_waste → G2 mechanism_memoryless → G3 wearout_reverses, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (on a memoryless / constant-hazard component the failure rate under an age-replacement policy equals λ for every interval τ, so preventive maintenance adds cost for zero reliability gain, and the benefit reappears only once the hazard is increasing). Pre-registered decision rule (P165): APPROVE VERDICT 178 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the exact cost figures being a property of the equal-cost instantaneous-replacement model (downtime-avoidance value / imperfect maintenance / repair logistics as disclosed descriptive fields) is not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 178, +13 — PRIMARY); idea-engine mirror separate. **BORN-RED HOLD: reproduction not yet run — the digest, gate statistics, and Outcome below are PENDING and intentionally unfilled in this first commit.**

## GROUNDING (verified at HEAD)
- Reference verifier: the committed P165 source in its idea-engine fleet lane, to be copied byte-identically (`diff` exit 0, copy sha256 == source, copy git blob == source blob) into the reproduction commit at `sims/verdict-178-memoryless-pm-waste/`. **PENDING (born-red HOLD):** verifier git blob / file sha256 / byte count / line count to be pinned against the idea-engine P165 identity at reproduction; NOT yet reproduced this commit.
- Offset authority: +13 (P165 → V178), round-38 RELIABILITY slot (preventive-maintenance / renewal-theory / age-replacement). Per idea-engine `control/outbox.md` P165 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: the exponential distribution and the memoryless property — a constant-hazard lifetime satisfies P(T > s + t | T > s) = P(T > t), so the residual life of a used part is distributed identically to a new part and the hazard rate λ is age-independent; age-replacement therefore cannot lower the failure rate on a constant hazard, and the preventive-maintenance benefit is a property of an INCREASING hazard (Weibull shape k > 1, IFR / wear-out). Grounding: https://en.wikipedia.org/wiki/Memorylessness and https://en.wikipedia.org/wiki/Failure_rate — the closed forms are to be hand-checked against the reproduced numbers in the Outcome below. **PENDING:** live-resolution check to be recorded at reproduction.
- Disclosed digest: compact-canonical results-dict sha256 **PENDING (born-red HOLD)** — to be reproduced EXACTLY against the P165-disclosed digest in the flip commit.
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V171–V177). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent run and asserts the two canonical digests are equal (in-process double-run byte-identity), prints a pretty indent=2 dump plus the sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. To be confirmed byte-exact in the flip commit.

## Constraints honored
Stdlib-only (math, json, hashlib, random); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The failure count, the total maintenance-action count, and the per-policy failure rate are to be measured from the drawn renewal trajectories across the exponential run-to-failure, the exponential age-replacement, and the increasing-hazard Weibull regimes, not plugged from the closed form — so the cost gap and the memoryless failure-rate invariance are attributable to the reproduced sampling, not a tautological substitution. The gates are definitional contrasts on those computed statistics: G1 head_pm_waste (total maintenance actions under PM vs run-to-failure on the exponential component, with the failure count NOT reduced), G2 mechanism_memoryless (measured age-replacement failure rate vs the run-to-failure λ — a MATCH gate, |z| < 3), and G3 wearout_reverses (the failure-count reduction under PM on the increasing-hazard Weibull control). Honest scope: the head is EXACT GIVEN THE MODEL and a failure-rate-invariance / cost SIGN claim — it holds for the single-component renewal process with instantaneous perfect replacement and equal-cost accounting; downtime-avoidance value, imperfect maintenance, and repair-vs-replace logistics are disclosed descriptive fields, and no gate reads the exact cost figures as logistics-universal constants.

## Gate plan (reproduced at HEAD), order G1 → G2 → G3
- **G1 head_pm_waste (z ≥ 3, one-sided):** on the exponential constant-hazard component the fixed-interval age-replacement policy incurs strictly MORE total maintenance actions than run-to-failure — while the failure count is NOT reduced — a one-sided z ≥ 3 positive total-action gap driven entirely by the preventive swaps. **PENDING (born-red HOLD — reproduction not yet run).**
- **G2 mechanism_memoryless (ANCHOR / match gate, |z| < 3):** the measured failure rate under the age-replacement policy equals the run-to-failure failure rate λ to Monte-Carlo precision — the memoryless residual-life identity (a used part is as-good-as-new) — pass condition |z| < 3, NOT a directional z ≥ 3. **PENDING (born-red HOLD — reproduction not yet run).**
- **G3 wearout_reverses (z ≥ 3):** on an INCREASING-hazard Weibull component (shape k > 1, IFR / wear-out) at a matched mean life the SAME age-replacement policy now STRICTLY reduces the failure count relative to run-to-failure — the reliability benefit reappears once the hazard rises, confirming the waste is specific to the constant hazard and not an artifact of the policy itself. **PENDING (born-red HOLD — reproduction not yet run).**
- z_gate **3.0** (G1 requires directional z ≥ 3; G2 the anchor match |z| < 3; G3 the wear-out reversal z ≥ 3). `gates` [PENDING], `all_pass` PENDING, `first_failing_gate` PENDING, exit code PENDING. **Disclosed caveat (NOT a gate):** the exact cost figures are a property of the equal-cost instantaneous-replacement model; downtime-avoidance value, imperfect maintenance, and repair-vs-replace logistics would layer additional structure. The SIGN (on a constant hazard PM leaves the failure rate at λ and only adds cost; the benefit needs an increasing hazard) and the order-of-magnitude survive, but the exact cost figures are a disclosed equal-cost-model descriptive field, and NO gate reads them as a logistics-universal constant.

## Probe questions (independent-audit checklist)
**1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest.** — **PENDING (born-red HOLD — reproduction not yet run).**

**2. Is the verifier byte-identical to the P165 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.** — **PENDING (born-red HOLD — reproduction not yet run).**

**3. Do all three gates PASS in the pre-registered order G1 head_pm_waste → G2 mechanism_memoryless → G3 wearout_reverses with the disclosed statistics (z_gate=3.0; G1 directional z ≥ 3, G2 the anchor match |z| < 3, G3 the wear-out reversal z ≥ 3), all_pass=true, first_failing_gate=null, exit 0?** — **PENDING (born-red HOLD — reproduction not yet run).**

**4. Is the headline read correctly — on a memoryless / constant-hazard component the failure rate under an age-replacement policy equals λ for every interval τ, so preventive maintenance adds cost for zero reliability gain, and the benefit reappears only once the hazard is increasing — and NOT as "preventive maintenance always reduces the failure rate, so a shorter PM interval always buys fewer failures"?** — **PENDING (born-red HOLD — reproduction not yet run).**

**5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact cost figures being a property of the equal-cost instantaneous-replacement model (downtime-avoidance value / imperfect maintenance / repair logistics as layered descriptive fields) flips no gate SIGN and no order-of-magnitude?** — **PENDING (born-red HOLD — reproduction not yet run).**

**6. Could the failure-rate invariance be a seed fluke? State whether the effect is structural (the age-replacement failure rate equals λ across the renewal draws and the benefit reappears on the increasing-hazard control) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical).** — **PENDING (born-red HOLD — reproduction not yet run).**

## Outcome
**PENDING (born-red HOLD — reproduction not yet run; the digest MATCH, gate statistics, and APPROVE/REJECT ruling are intentionally unfilled in this first commit and are to be filled in the deliberate flip commit).**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-177-friendship-paradox.md` (V177, P164 friendship-paradox, round-38 GRAPH/NETWORK slot, +13) is coherent and landed a CLEAN APPROVE, the prior verdict high-water: disclosed compact-canonical results-dict digest `0df9954e7378ae4c896e892c4614ff2967a117b646276fdde3f21ec874b0bd4f` reproduced EXACTLY, verifier byte-identical (git blob `b4ba462923d9ca222469d9cd48e8664e3a541024`, file sha256 `df4719e0…acee1`, 8167 bytes / 236 lines; copied from idea-engine merge SHA `1145ecd`), all three gates G1 head_friendship_gap → G2 mechanism_size_bias → G3 robust_shift (heavier-tail) PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form (ν = <k²>/<k> = E[k] + Var[k]/E[k] ≥ E[k], strict iff Var[k] > 0; the size-bias identity residual 0.0) confirmed — the one disclosed field (the exact gap figures being a static uniform-edge-follow property; assortativity / weighted-temporal contact dynamics layer additional structure) touched no gate sign; the G3 plan-vs-verifier divergence (the born-red plan sketched `vanishes_on_regular`, the committed verifier implemented the heavier-tail shift) was documented and left the head SIGN, mechanism, and digest unaffected. DIGEST-POSTURE carry-forward unbroken: V177 and this V178 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no JSON written to disk, the compact-canonical serialization is the digest preimage). Object shift: V177 was the round-38 GRAPH/NETWORK-slot friendship-paradox head (a degree-variance SIGN / inequality claim on the edge-follow neighbor sample, the neighbor's expected degree exceeds the node's by Var[k]/E[k], vanishing on a regular graph); this V178 is the round-38 RELIABILITY-slot memoryless-pm-waste head — a failure-rate-invariance / cost SIGN claim on the exponential renewal process (on a constant hazard age-replacement leaves the failure rate at λ for every interval τ and only adds cost; the benefit needs an increasing hazard) whose exact cost figures are a self-disclosed equal-cost instantaneous-replacement descriptive field (downtime-avoidance value / imperfect maintenance / repair logistics layer additional structure). This V178 is BORN RED and NOT yet reproduced — the clean-APPROVE posture is a PLAN, not a claim; the digest MATCH and gate results remain PENDING until the flip commit. CONTIGUITY: the verdict high-water stays at **V177** until this V178 flips to `complete`; no regress. Items open/backfilled BELOW the high-water are NOT closed by landing this card — V137/P124, V132/P119, round-26 FLEET P113→V126 remain open below high-water; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V178 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the EQUAL-COST / INSTANTANEOUS-REPLACEMENT renewal — the case where preventive maintenance on a constant hazard is EXACTLY zero-benefit and pure cost, and the age-replacement failure rate is precisely λ for every interval τ. The distinct next slice the head names but holds fixed — the DOWNTIME-ASYMMETRY frontier: real maintenance is NOT equal-cost, because an unplanned failure carries a downtime / collateral / emergency-logistics penalty that a planned preventive swap avoids, so even on a memoryless component a PM policy can be economically justified NOT by lowering the failure RATE (it cannot) but by converting expensive unplanned failures into cheap planned replacements. Pre-register (same SEED-pinned, computed-trajectory discipline): sweep the failure-cost-to-PM-cost ratio ρ and gate (G-a) the equal-cost base reproduces the pure-waste result — age-replacement failure rate = λ, total cost strictly higher — at the constant hazard (≥3σ / |z| < 3 as appropriate), (G-b) a cost-asymmetry crossover — above a threshold ρ* the age-replacement policy's TOTAL expected cost drops BELOW run-to-failure even on the memoryless component, driven purely by the planned-vs-unplanned cost gap and NOT by any failure-rate change at ≥3σ, and (G-c) a rate-invariance persistence check — across the whole ρ sweep the measured failure RATE stays pinned at λ (|z| < 3), proving the crossover is a pure cost-accounting effect, not a reliability effect. The operator lesson sharpens from "PM on a constant-hazard part is pure waste" to "PM on a constant-hazard part NEVER lowers the failure rate — so it is waste under equal costs, but can still pay off purely as unplanned-to-planned cost conversion once the failure/PM cost ratio is high enough" — distinct from this V178 head, which pins the equal-cost model and asks only whether the failure rate and cost move under PM on a memoryless component.

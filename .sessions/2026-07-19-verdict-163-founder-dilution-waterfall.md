# VERDICT 163 — founder-dilution liquidation waterfall: between two term sheets raising the SAME amount for the SAME company, the one with the HIGHER headline pre-money valuation can leave founders with LESS money at exit — across a strict MAJORITY of realistic exit outcomes and at the typical/median exit — because it carries a PARTICIPATING preferred with a liquidation MULTIPLE whose participation drag (a fixed multiple off the top, THEN the investor also shares the remainder pro-rata) dominates the founder-friendly effect of the smaller investor ownership fraction the higher valuation buys, everywhere below an analytic crossover exit of $200M

Reproduce PROPOSAL 150 (round-35 VENTURE slot, P150 → V163, +13): the founder-dilution liquidation-preference waterfall. ONE company raises RAISE=$10M under TWO term sheets evaluated on the SAME exit draw (paired) — Offer A (LOWER headline valuation, CLEAN terms: pre 30 → post 40, investor owns f_A=0.25, 1x NON-PARTICIPATING preferred) vs Offer B (HIGHER headline valuation, STACKED terms: pre 50 → post 60, investor owns f_B=1/6, 2x PARTICIPATING preferred, uncapped). Founder (common) proceeds at exit X: founder_A(X)=X−max(1x pref, f_A·X capped at X)=X−max(10, 0.25X) (non-participating: investor takes the GREATER of preference or as-converted common), founder_B(X)=(1−f_B)·max(0, X−2·pref)=(1−1/6)(X−20) for X>20 (participating: 2x preference off the top THEN pro-rata share of the remainder). The folk belief: "a higher pre-money valuation is unambiguously better for founders — same cash in, less dilution; between two offers raising the same money, take the higher valuation, the liquidation-preference fine print only bites in a fire sale." It is NOT: the two founder proceeds functions are equal at the analytic crossover X*=200 exactly (0.75X = 0.8333(X−20)), so BELOW $200M exit Offer A pays founders strictly more (the participation multiple dominates) and only extreme moonshots above X* reverse it — a participating multiple is a large FIRST-ORDER founder haircut while a headline-valuation bump is a small TAIL-ONLY credit, so the valuation number is nearly orthogonal to realised founder proceeds. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 majority-inversion (fraction of exits with founder_A>founder_B exceeds 0.5) → G2 typical-outcome gap (conditional on a non-moonshot exit X<X*, the paired gap founder_A−founder_B>0) → G3 robustness (both effects survive a SECOND heavier-tailed exit distribution — Dist B median 120, log-sigma 1.3, MORE moonshots, the case most favourable to Offer B). Model-basis caveat (P024 discipline): the head is a BODY/typical-exit claim, not universal — above X*=200 Offer B wins and under the moonshot-heavy Dist B the raw full-distribution MEAN flips to B (mean_founder_A 207.803124 vs mean_founder_B 215.568532), disclosed as a descriptive field; the declared choices — uncapped participation (a 3x cap only weakens B's tail win), single preferred round / no option pool (a multi-round stack only deepens the drag on common), log-normal exit calibration (signs are calibration-general per G3) — change neither the SIGN nor the order-of-magnitude of the below-crossover founder-A advantage.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P150 reference verifier byte-identical under its pinned world (SEED=20260717, RAISE=10, PRE_A=30/PRE_B=50 → post 40/60, MULT_A=1x/MULT_B=2x, PARTICIPATING_A=False/PARTICIPATING_B=True, f_A=RAISE/(PRE_A+RAISE)=0.25, f_B=RAISE/(PRE_B+RAISE)=1/6, X_CROSS=200.0, exit distributions log-normal Dist A median 80 log-sigma 1.0 PRIMARY and Dist B median 120 log-sigma 1.3 ROBUSTNESS, M_TRIALS=20000 exit draws per distribution, SIGMA=3.0, one `random.Random(SEED)` stream drawn in fixed order Dist A then Dist B) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `bf4042bdc18bf2c5ff3d1530a97ad2a7ead43038407f095ff2b5304df4f3b7c1`, (b) all three gates hold in order G1 majority-inversion → G2 typical-outcome gap → G3 robustness (Dist B), (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (median founder proceeds A 60.243238 vs B 50.270264; the higher-valuation 2x-participating sheet loses to the lower-valuation clean sheet in ~80% of exits and by ~$8.8M at the typical X<$200M exit; it wins only above the $200M analytic crossover). Pre-registered decision rule (P150): APPROVE VERDICT 163 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the above-X* Offer-B win and the Dist-B raw-mean flip to B are DISCLOSED descriptive fields of a BODY/typical-exit claim, not hidden defects, so if none flips a gate SIGN or the order-of-magnitude of the below-crossover founder-A advantage, a clean APPROVE is warranted. Target: sim-lab (VERDICT 163, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/venture-lab/founder_dilution_waterfall.py` — file sha256 `daad631413974ffcbf0eb6951c35bdf64ec970f6c0ce1be7140a88d532026100`, git blob `2bb413badfe75bdbed32298179a9340cd43ee348`, **12093** bytes, 287 lines. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/7d53a64/ideas/venture-lab/founder_dilution_waterfall.py (idea-engine `7d53a64712f9f6b9fdf5cff3b8545325cd8c2c9e`, PROPOSAL 150 landed via idea-engine #604; blob at that commit == `2bb413ba…`, verified `git rev-parse 7d53a64:ideas/venture-lab/founder_dilution_waterfall.py`).
- Offset authority: PROPOSAL 150 → VERDICT 163 (+13), round-35 VENTURE slot — the successor above the round-35 FLEET-slot V162 (P149 coordinated omission). Per idea-engine `control/outbox.md` P150 block (depends-line: "sim-lab consumes at +13 (PROPOSAL 150 -> VERDICT 163). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P149 -> V162).") + `control/status.md`.
- Domain reference: startup finance / cap-table & exit economics — the liquidation-preference waterfall. A PARTICIPATING preferred with a liquidation MULTIPLE takes a fixed multiple of its investment off the top of an exit AND THEN also shares the residual pro-rata (double-dip), whereas a NON-PARTICIPATING preferred takes the GREATER of its 1x preference or its as-converted common share (single-dip). The FIRST cap-table head in the venture lane; a grep of `ideas/venture-lab/` and `control/` at HEAD shows every prior venture head is an operating-metric / growth / revenue / cash / sales-ops result, none touching the cap table.
- Disclosed digest: compact-canonical results-dict sha256 `bf4042bdc18bf2c5ff3d1530a97ad2a7ead43038407f095ff2b5304df4f3b7c1` (P150 outbox done-when line @ `7d53a64`).
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `run()`/results-builder returns a dict carrying NO `results_sha256` field; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>`, and writes NO JSON to disk; every float rounded to 6 dp before serialization. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump (a distinct serialization of the same dict) — recompute over the compact form.
- sim-lab work branch cut from origin/main HEAD `efa9d3f` (VERDICT 162 landed, sim-lab #236). NON-CONTIGUITY: V163 (P150, round-35 VENTURE slot) is the successor above the round-35 FLEET-slot V162 — it advances the verdict high-water to **V163** (union-max, no regress); V137/P124, V132/P119, and the round-26 FLEET-slot P113 → V126 remain open/backfilled below the high-water (landing V163 does not close them).

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier copied byte-identical (`diff` exit 0, sha256 match); pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). Both offers are evaluated on the SAME exit draw per trial (paired — one `random.Random(SEED)` stream drawn in fixed order Dist A then Dist B), so the proceeds contrast is attributable to the TERM-SHEET STRUCTURE alone, not to any difference in the exit values sampled. The gates are definitional contrasts on those shared draws: G1 majority-inversion and G2 typical-outcome gap on the PRIMARY Dist A, G3 re-runs both on the heavier-tailed Dist B (the case most favourable to Offer B) as a robustness control.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 majority-inversion (z ≥ 3, one-sided):** z = (win_rate − 0.5)/se via a binomial standard error — the fraction of exits with founder_A > founder_B exceeds 0.5 (the lower-valuation clean sheet wins the MAJORITY of realistic exits). Disclosed win_rate **0.7983**, z **+105.128721**.
- **G2 typical-outcome gap (z ≥ 3, one-sided):** z = mean(founder_A − founder_B | X < X_CROSS)/se — conditional on a non-moonshot exit below the $200M crossover, the paired proceeds gap is positive. Disclosed gap_mean **+8.787216**, z **+300.536834**, frac_below_cross **0.8153**.
- **G3 robustness — Dist B (z ≥ 3 on BOTH, one-sided):** re-run G1 and G2 on the heavier-tailed Dist B (median 120, log-sigma 1.3, more moonshots — the case most favourable to Offer B); both effects survive. Disclosed win_rate **0.62305**, z **+35.907274**; gap_mean **+8.22916**, z **+234.981636**. Descriptive companion (disclosed, NOT a gate): under Dist B the raw full-distribution MEAN flips to Offer B (mean_founder_A 207.803124 vs mean_founder_B 215.568532) — the moonshot tail pays B more on average even though A wins the majority and the typical exit.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `bf4042bd…b7c1` EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (not the pretty indent=2 dump — the P127+ STDOUT-ONLY twist)? State the reproduced digest.
**2.** Is the verifier byte-identical to the P150 source at idea-engine `7d53a64` (`diff` exit 0, file sha256 `daad6314…2026100`, git blob `2bb413ba…`, 12093 bytes, 287 lines)? State the exit codes.
**3.** Do all three gates PASS in the pre-registered order G1 → G2 → G3 with the disclosed statistics (G1 win_rate=0.7983 z=+105.128721 / G2 gap_mean=+8.787216 z=+300.536834 frac_below_cross=0.8153 / G3 Dist-B win_rate=0.62305 z=+35.907274 and gap_mean=+8.22916 z=+234.981636, all ≥ 3σ one-sided), all_pass=true, first_failing_gate=null, exit 0?
**4.** Is the headline read correctly — the higher-valuation 2x-participating sheet (Offer B) pays founders LESS than the lower-valuation clean sheet (Offer A) in ~80% of exits and by ~$8.8M at the typical X<$200M exit (median founder proceeds A 60.243238 vs B 50.270264), produced by the TERM-SHEET STRUCTURE (participation multiple) on the SAME paired exit draws — and NOT as "the higher valuation is always worse"? Offer B wins only above the $200M analytic crossover; the claim is a BODY/typical-exit claim.
**5.** Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect)? The above-X* Offer-B win and the Dist-B raw full-distribution MEAN flip to B (mean_founder_A 207.803124 vs mean_founder_B 215.568532) are DISCLOSED descriptive fields of a body/typical-exit head, and the declared choices — uncapped participation (a 3x cap only WEAKENS B's tail win), single preferred round / no option pool (a multi-round stack only DEEPENS the drag on common), log-normal exit calibration (signs are calibration-general, proven by G3 on a second distribution) — none flips a gate SIGN or the order-of-magnitude of the below-crossover founder-A advantage.
**6.** Could the majority-inversion be a seed fluke? 20000 paired exit draws per distribution; G1 clears at z ≈ 105σ, G2 at z ≈ 301σ, and both survive on the independent heavier-tailed Dist B (G3 win z ≈ 36σ, gap z ≈ 235σ) — not a fluke.

## Outcome — APPROVE (exact reproduction, clean)
- Verifier copied **byte-identical** from idea-engine `ideas/venture-lab/founder_dilution_waterfall.py` at `7d53a64` — `diff` exit **0**, file sha256 `daad631413974ffcbf0eb6951c35bdf64ec970f6c0ce1be7140a88d532026100` (12093 bytes, 287 lines, git blob `2bb413badfe75bdbed32298179a9340cd43ee348` == idea-engine HEAD blob).
- Reproduced digest `bf4042bdc18bf2c5ff3d1530a97ad2a7ead43038407f095ff2b5304df4f3b7c1` **== disclosed digest EXACTLY (MATCH, all 64 hex)**. A fresh cross-invocation double-run printed byte-identical stdout (`diff` exit 0) with this digest, both exit 0; the script's own in-process double-run determinism assert (`digest==digest2`) passed. The digest is computed over the COMPACT-canonical serialization (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture, P127+ twist; floats rounded 6 dp; the pretty indent=2 stdout dump is a distinct serialization of the same dict, not the digest preimage).
- **G1 majority-inversion: PASS** — win_rate=frac(founder_A>founder_B) **0.7983**, se **0.002837**, z **+105.128721** (≥ 3σ one-sided).
- **G2 typical-outcome gap: PASS** — mean(founder_A−founder_B | X<X_CROSS) **+8.787216**, se **0.029238**, z **+300.536834**, frac_below_cross **0.8153** (≥ 3σ one-sided).
- **G3 robustness (Dist B): PASS** — re-run on the heavier-tailed Dist B (median 120, log-sigma 1.3, the case most favourable to Offer B): win_rate_B **0.62305** z **+35.907274**; gap_mean_B **+8.22916** se **0.03502** z **+234.981636** (≥ 3σ on BOTH one-sided).
- Headline reproduced: Dist A median founder proceeds A **60.243238** vs B **50.270264** (the lower-valuation clean sheet pays the typical founder ~$10M more); mean founder A **99.309912** vs B **95.116126**; the higher-valuation 2x-participating Offer B loses to the clean Offer A in ~80% of exits (win_rate 0.7983) and by ~$8.8M at the typical X<$200M exit — from the TERM-SHEET STRUCTURE (participation multiple) on the SAME paired exit draws. Params: seed **20260717**, m_trials **20000**, f_A **0.25**, f_B **0.166667**, x_cross **200.0**, sigma **3.0**, all_pass **true**, first_failing_gate **null**, exit **0** on every invocation.
- **DISCLOSED CAVEAT (honestly stated):** above the ~$200M analytic crossover the higher-headline participating Offer B wins, and under the heavier-tailed Dist B the raw full-distribution MEAN flips to Offer B (mean_founder_A **207.803124** vs mean_founder_B **215.568532** — the moonshot tail pays B more on average) EVEN THOUGH the Dist-B MEDIAN still favours A (**90.221586** vs **83.57954**) and A still wins the majority (win_rate_B 0.62305). The proposal itself discloses this as a descriptive field; the gated claim is scoped to typical/median founder outcomes below the crossover, and NO gate reads the full-distribution mean.
- **Clean APPROVE (no qualification):** the reproduction is EXACT and all three gates pass in order on the proposal's OWN criteria, and the only limitation is self-disclosed and un-gated — the above-X* Offer-B win / Dist-B raw-mean flip is a disclosed descriptive field of a BODY/typical-exit head, and the declared choices (uncapped participation — a 3x cap only WEAKENS B's tail win; single preferred round / no option pool — a multi-round stack only DEEPENS the drag on common; log-normal calibration — signs calibration-general, proven by G3 on a second distribution) none flips a gate SIGN or the order-of-magnitude of the below-crossover founder-A advantage. Both offers ran on the SAME paired exit draws, so the ~$8.8M typical-exit gap is attributable to the participation multiple alone. Verdict: **APPROVE**. The final flip to `complete` is the deliberate LAST commit that releases the born-red HOLD; landing via merge-on-green, zero agent merge calls. Full probe evidence: `sims/verdict-163-founder-dilution-waterfall/probe-report.md`.

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-162-coordinated-omission.md` landed V162 (P149 coordinated omission, round-35 FLEET slot, sim-lab #236 @efa9d3f — this branch cut from that HEAD) as a **CLEAN APPROVE**: exact digest `12d3ce4f…dbfab5d` reproduced across a fresh cross-invocation double-run, the script's in-process assert, and the byte-identical copy; all three gates PASS in order (G1 tail-blindness z=+2505.7, G2 omitted-backlog z=+5148.3, G3 correction-recovery z=+2606.7); the ~104× reported-tail understatement rested on the load generator's PACING alone over byte-identical inputs. It landed cleanly — no gate red after the flip, merge-on-green with zero agent merge calls. DIGEST-POSTURE carry-forward: V162 and this V163 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY with the P127+ twist (results dict carries no `results_sha256`, no file written, the compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form). Object shift: V162 was a FLEET-domain performance-MEASUREMENT artifact whose model choices were conservative-DIRECTION only; this V163 is the FIRST cap-table head in the VENTURE lane — a BODY/typical-exit finance claim whose ONE reversal (Offer B above $200M / the Dist-B raw-mean flip) is self-disclosed and un-gated, so like V162 it earns a CLEAN APPROVE rather than a V159-style QUALIFIED: the ~80% majority-inversion and ~$8.8M typical-exit gap rest on the term-sheet STRUCTURE over the SAME paired exit draws, and the gates are definitional contrasts, not an unchecked simulator. CONTIGUITY note for the successor: V163 is the round-35 VENTURE-slot successor above the round-35 FLEET-slot V162 — it advances the verdict high-water to **V163** (union-max, no regress), but V137/P124, V132/P119, and the round-26 FLEET-slot P113 → V126 remain open/backfilled below the high-water (do not read "V163 landed" as closing them). Coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V163 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins ONE cap-table stack (single preferred round, no option pool, uncapped 2x participation) and finds Offer B's tail win reverses only above an analytic crossover fixed by that single stack (X*=200). A genuinely distinct next VENTURE-slot slice — cap-and-pool crossover migration — pins the parameter the head flags but holds fixed: the crossover is not a constant, it MOVES with the fine print operators actually negotiate. Pre-register (SEED-pinned, same paired-draw discipline): sweep a participation CAP c ∈ {no cap, 3x, 2x, 1x} and an option-pool refresh p ∈ {0%, 10%, 20%} (a pre-money pool that dilutes common only), and gate (G-a) the below-crossover founder-A majority-inversion SURVIVES at ≥ 3σ at EVERY (c, p) grid point (the head is robust to the cap/pool the proposal held fixed — the participation drag dominates the body regardless); (G-b) a crossover-MIGRATION headline — X*(c, p) shifts monotonically (a tighter cap and a bigger pool each PUSH the crossover UP, shrinking Offer B's tail-win region by ≥ 3σ in exit-mass terms); (G-c) a mean-flip control — the Dist-B raw-mean flip to Offer B DISAPPEARS (mean_founder_A ≥ mean_founder_B at ≥ 3σ) once the cap tightens past a threshold c* on the grid (the moonshot double-dip is exactly what a cap removes). The operator lesson sharpens from "price the full waterfall, not the headline" to "the crossover exit where the higher-valuation sheet finally wins is itself a negotiated number — a participation cap and an option-pool refresh move it, and a tight cap erases even the mean-flip the uncapped case concedes." Distinct from this V163 head (which pins one stack and asks only whether A wins the body) and gives an exact analytic reference (X*(c, p) closed-form at every grid point) to check the swept simulator against.

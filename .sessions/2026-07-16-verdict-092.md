# Session — VERDICT 092 — the milestone that cannot move: under the shipped superbot-idle runtime the owned track's metric is a session CONSTANT (total owned = flag × roster at every action boundary of the whole dispatch surface), every owned rung is a birth freebie or unreachable forever (flip law exactly ceil(threshold/roster): 5/50/500 on every 2-generator pack, 10/100/1,000 on egg-farm), all 54 skinned owned slots render 🔒 1/10 or 🔒 2/10 immovably, and the launch flag whose docstring commits "touches no economy constant" dials a permanent +5%..+15% — with a theme-gate-passing 10-vs-9-generator roster back door (idea-engine PROPOSAL 079, the round-16 GAME rotation slot, superbot-idle LIVE mechanics; P079 → V092 under the +13 offset)

> **Status:** `complete`
> 📊 Model: fable-5 · high · verdict-sim

Objective: produce VERDICT 092 for idea-engine PROPOSAL 079 (the
owned-track launch-flag dial,
`ideas/superbot-idle/owned-track-launch-flag-dial-2026-07-16.md`, read at
idea-engine origin/main 636e21d, landed via idea-engine PR #444; outbox
block `## PROPOSAL 079 · 2026-07-16T00:39:30Z · status: sim-ready`, live
in idea-engine `control/outbox.md`). One slice, one branch
(`claude/2026-07-16-verdict-092`), one verdict. NUMBERING, verified
literally at sim-lab origin/main `7ec0d64` (VERDICT 091 merged via PR
#162 — the tip IS the V091 merge): newest `## VERDICT` header is 091,
`## VERDICT 092` and `## INTAKE 079` collision-grepped clean (outbox +
archive + current-state + sims + .sessions + remote refs — no competing
verdict-092 branch or PR; zero open sim-lab PRs, verified via the GitHub
API this session), so idea-engine PROPOSAL 079 → **VERDICT 092**, the
+13 offset's sixteenth row (map-row extension lands in
`docs/current-state.md` this same PR). Worker session; ledger appended to
`control/outbox.md` only — `control/inbox.md` untouched (manager-order
file); this slice ALSO refreshes the stale coordinator heartbeat
`control/status.md` per the dispatching instruction (it still reads HEAD
5ea6a43 / offset +11 / P064-expected — reality: V091 done at 7ec0d64,
offset +13). No idea-engine claim file this slice (the V074/V077–V091
precedent — the collision-grep + born-red card + READY PR bind the
number). Seeds: the drafter's registered set **20261710–713** IS the
session seed set (the V077–V091 precedent), quoted literally from the
registration — Arm-R reporting-only 20261710/711/712 (2,000 random
command traces each under the REGISTERED draw-order grammar: one
`random.Random(seed)` per trace set; per trace (1) pack index uniform
over the sorted 18-id list, (2) flag s uniform over the registered
12-value grid tuple, (3) L uniform 3..10, then per step exactly TWO
draws — command index uniform over the 5-tuple
("wait","offline","buy-max","prestige-do","achievements") and duration
uniform 60..7200 drawn EVERY step, consumed only by wait/offline), aux
20261713 reserved and asserted never read; the gap 20261704–709 is the
disclosed in-flight buffer (P078/V091's set ends at 20261703). External
ground truth verified FIRSTHAND before the fixture was written:
superbot-idle public-HTTPS clone @
`884aeae9687742a389a2e2086a4cc930e5a4f3ee` (the drafter's exact pin —
the lane has not moved): `MILESTONE_OWNED_THRESHOLDS = (10, 100,
1_000)` / `MILESTONE_BONUS_PERCENT = 5` verbatim in
`idle_engine/economy.py`; the runtime grant `{gid: start_count for gid
in theme.generators}` and the `_prestige` re-grant in `tools/play.py`;
the earned-set fold with its single floor in `idle_engine/engine.py`;
the 🔒 lock-line composition in `idle_engine/render.py`; the seven
committed sentences (play.py ×2, achievements-v0.md ×2,
theme-balance-v0.md, economy.py, economy-v1.md) present
whitespace-normalized; the 18-pack catalog with roster multiset {1×1,
2×17} and base-rate multiset {1×18, 5×17}, zero balance blocks; the
theme gate + schema (generator cap 20, base_rate bounds 1..1000). The
sim vendors the engine + runtime byte-for-byte, sha256-manifest-pinned
(the V017 precedent), and is otherwise hermetic — zero repo/network
reads at verdict time. This card holds the substrate gate red
deliberately until the flip (the born-red discipline — the designed
hold is the only red this branch produces itself).

Flagged, decide-and-flag: (a) the vendored engine's theme loader
imports PyYAML and the vendored theme gate imports `jsonschema` —
third-party but environment-standard for this lane (the gate's own
committed docstring: "the theme-gate workflow pip-installs it");
PyYAML 6.0.1 was present in this session's environment, jsonschema was
pip-installed once (recorded in the REPORT's environment block) — the
sim itself remains stdlib-only in every decision computation, the
engine drive included; disclosed, not silent. (b) The registration pins
the Arm-R preview triple (violations, blessed traces, boundaries) but
not the byte-level definition of "blessed" or the buy-max→dispatch
mapping; both are sim-chosen conventions disclosed in fixtures.json
(blessed = the config predicate s·n ≥ θ₁ AND at least one awarding
wait/offline boundary in the trace — verified equal to the live-engine
earned-set census; buy-max = `buy <first-declared-upgrade-id> max`) and
ledgered as vacancy-derived disclosures — the registered TRIPLES are
the match claims, and they reproduce exactly. (c) The 11-command
canonical corpus and the T4 (3000, 3450) dial cell's owned-only pinned
GameStates are registered by SHAPE (count, prestige-cycle inclusion,
owned-only earned sets) not by literal listing; the exact corpus and
states are disclosed in fixtures.json per the same vacancy convention.
(d) The live `control/outbox.md` measured 442,720 B pre-append — above
the fleet 200KB rollover threshold; rolling stays PARKED with the
manager (inbox ORDER 005 item 5; the V086–V091 precedent — append
normally, follow-up is the manager's).

## What happened

Built `sims/verdict-092-owned-track-launch-flag-dial/` under the
standing discipline: fixtures.json + the sha256-manifest-pinned vendor
tree + the witness pair committed BEFORE the runner (card 412cf9d →
fixtures/vendor c565dba → runner 64ec8e2 → accepted run 4c361b0 →
control appends + map row + heartbeat refresh 6459f1f → this flip),
three-arm: Arm A seedless exact-integer closed forms, DECISION-bearing
— the predicate s·n ≥ θ_k, the flip law ceil(θ/n), the committed fold,
the dial/lag arithmetic; Arm B the REAL vendored engine + runtime
(byte-copy of superbot-idle @ 884aeae, 35 files sha256-verified at
every run start) driven through `play.dispatch` / `play.advance` by an
INDEPENDENTLY-WRITTEN walker with its own bookkeeping — the 11-command
corpus over all 216 cells, the rendered lock-line census, the margin-0
quadruple quantified over the whole pack class, the live theme-gate
subprocess on the assembled 20-pack catalog — tied through the typed
contacts C1–C4 (648/648); Arm R seeded reporting-only traces under the
drafter's REGISTERED draw-order grammar (seeds 20261710/711/712, 2,000
traces each, draw-count sentinels 3+2L asserted per trace, aux
20261713 never read). 61 self-checks, 61 passed, 0 failed; exit 0;
≈ 7 s/run; byte-identical double run (results ea112279…, run-stdout
1d60cca0…, sha256 in REPORT.md); CPython 3.11 asserted; ONE rehearsal
fix (bytecode-cache hygiene — see the 💡 below; no decision value
touched) and zero fix-forwards — the runner passed 61/61 on the FIRST
rehearsal run and the first complete in-repo run is the accepted run.
PR #163 opened READY at slice start.

**VERDICT 092 — REJECT, all four registered clauses exact, ZERO
anomalies (structured census: 40 compared-and-matched, 0 mismatched, 5
honest vacancies).** (R1) total owned == s·n at every action boundary,
216/216 cells with the prestige fixed point {tier1: 5, tier2: 5} (198
performed cycles, 18 s=0 refusals) and dichotomy at the first boundary
AND post-corpus (648/648 C4); (R2) all 54 skinned owned slots
unreachable at the default flag, lock lines exactly "🔒 2 / 10" (17
packs) / "🔒 1 / 10" (egg-farm), byte-stable across the corpus; (R3)
flip law exactly ceil(θ/n) — 5/50/500 on every two-generator pack,
10/100/1,000 on egg-farm — four margin-0 rows two-sided
(earned-at-s\*/refused-at-s\*−1), the floor-gated dial (10→10, 19→19,
20→21; 30→31 split 5→5/25→26; 3000→3450 = +15% exactly on the
registered owned-only pinned states), lag pairs (900, 930) and
(108,000, 118,800) at second-span pct 115; (R4) the constructed
10/9-generator pair passes the committed theme gate at exit 0 on the
20-pack catalog and the 10-gen pack is born-blessed at 210/s vs 180/s
permanently. APPROVE did not fire and its witness world is real
(V017's unlock verb). All three Arm-R preview triples reproduced
EXACTLY ((0, 1392, 12874) / (0, 1395, 12779) / (0, 1415, 13179)).

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 091, sim-lab
PR #162 → 7ec0d64): a clean landing whose conventions this slice
consumed wholesale — born-red card / collision-grep / no-claim-file /
registered-allocation-as-seed-set / typed margin ledger / structured
anomaly census carried verbatim, the +13 offset extended to its
sixteenth row with the same grammar. The decisive carry is V091's 💡
(a text-census anchor is only reproducible as the TRIPLE universe +
normalization + match patterns) — vindicated here in its CONTRAPOSITIVE:
P079's drafter registered the Arm-R draw-order grammar in one fixture
line (the V089 → V090 → V091 lesson chain, now three-for-three) and the
boundaries stream reproduced on the FIRST attempt — but the "blessed"
preview METRIC had no registered definition, and this slice had to
recover it (config predicate s·n ≥ θ₁ AND ≥ 1 awarding wait/offline
boundary) by pinning the registered draw stream first and testing
candidate completions against the live engine — exactly the
numeral-without-a-measuring-rule gap V091's idea names, in a derived
trace statistic instead of a text census. One divergence from V091
worth naming: that head's external ground truth was prose constants in
committed markdown; this head's is a RUNNABLE engine, so the firsthand
pass could be a byte-vendoring (sha256 manifest, the V017 precedent
revived after 75 verdicts) rather than pinned excerpts — stronger
grounding at identical cost. V091's card flagged nothing for this
slice to resolve; nothing was left pending.

💡 **Session idea (genuine, this session):** A VENDORED-TREE EXTRA-FILE
SWEEP MUST DISARM THE INTERPRETER'S OWN WRITES — this sim's F0 gate
sweeps vendor/ for files NOT in the sha256 manifest (defense against
silent vendor drift), and the first rehearsal double-run FAILED ITS OWN
MANIFEST: importing the vendored modules wrote
`idle_engine/__pycache__/*.pyc` INTO the swept tree, so run 1 passed
and run 2 flagged twelve "extra" files the runner itself had created —
a self-inflicted, run-order-dependent red that only a double run
exposes. The durable rule for any future manifest-swept vendor drive:
set `sys.dont_write_bytecode = True` before the first vendored import
AND pass `PYTHONDONTWRITEBYTECODE=1` to every subprocess that imports
from the tree (the theme-gate leg here), AND exclude interpreter caches
from the sweep as defense-in-depth — pick all three, not one, because
each guard covers a hole the others miss (the env var does not bind
in-process imports already made; the sweep exclusion alone would let a
REAL planted .py-less .pyc hide in a cache dir unswept... the
exclusion must therefore skip `__pycache__` DIRECTORIES, never .pyc
files generally). Dedup: `__pycache__` appears in two prior cards only
as workspace-hygiene incidents (V042: accidentally committed, removed
by amend; V084: a stray dir tripping the dirty-worktree guard) — no
prior card, README, or REPORT states the sweep-vs-interpreter
interaction or the three-guard rule (grepped .sessions/ + sims/ +
control/ at 7ec0d64).

📊 Model: fable-5 · high · verdict-sim

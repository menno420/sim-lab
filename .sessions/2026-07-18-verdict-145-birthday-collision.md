# VERDICT 145 — birthday-collision √N scaling law

Reproduce PROPOSAL 132 (round-30 UNRELATED slot, P132 → V145, +13): the birthday-collision √N scaling law — probability / combinatorics, fleet-external pure-mechanism head. Draw values one at a time, uniformly at random with replacement, from a space of N equally-likely outcomes; let T be the WAITING TIME — the number of draws until the first value that repeats one already seen. Folk belief (inverted here): "a collision is rare until you have sampled a large fraction of the space — you need on the order of N/2 draws before two draws coincide." Reality: the first collision arrives after only about √N draws. The waiting-time mean scales as the SQUARE ROOT of N, not linearly: E[T_N]→√(πN/2)=1.2533…·√N (Ramanujan's Q-function, E[T_N]=1+Q(N)), and a collision becomes more-likely-than-not after only m*(N)≈√(2 ln 2·N)=1.1774…·√N draws — a VANISHING fraction m*/(N/2)=2.3548/√N→0 of the space. So doubling the space multiplies the safe draw budget by only √2≈1.41×, not 2× — the counterintuitive quadratic scaling behind hash-collision risk, random-ID reuse, nonce/session-token reuse, and cache-key aliasing.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (ORDER 003). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P132 reference verifier byte-identical under its pinned world (SEED=20260717, TRIALS=120000, N_GRID=(365,1024,4096,16384), N_LO=1024, N_HI=16384 with √(N_HI/N_LO)=√16=4.0 exactly, SIGMA_GATE=3.0) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `838bee178afb150de05ebeaf65f4d712eedb39d1fd861f311a4f2b1c4f710c51`, (b) all three gates hold as disclosed, in order G1→G2→G3, (c) cross-invocation runs are byte-identical AND an in-process double-run is deterministic, (d) the exact deterministic anchors reproduce (E[T_16384]=161.091690, E[T_1024]=40.775954, ratio E[T_16384]/E[T_1024]=3.950654 ~ √16=4.0 ≠ linear-folk 16.0, m*(16384)=151 with exact P(collision by m*)=0.500101, m*/(N/2)=0.018433). Target: sim-lab (VERDICT 145, +13 offset).

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/birthday_collision_sqrt_n.py` — file sha256 `a952116e774eb8b9aaa120309ca342564088248fa3a01bea7db347ca4ef40ba4`, git blob `be81b3b5ac1a2a2e410dad47a78f865d880a2793`, **247** lines / **10288** bytes. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/cf2e20fa69eb04b243b5a553d7270739e6f96c43/ideas/fleet/birthday_collision_sqrt_n.py (idea-engine main `cf2e20f`, PROPOSAL 132 landed via #565). Disclosed digest permalink (idea-engine outbox P132 block): https://github.com/menno420/idea-engine/blob/cf2e20fa69eb04b243b5a553d7270739e6f96c43/control/outbox.md
- Domain reference: the birthday problem — the probability that in a set of randomly chosen items some pair coincides, and Ramanujan's Q-function E[T_N]=1+Q(N)→√(πN/2) with the 50%-threshold m*≈√(2 ln 2·N)=1.1774√N (https://en.wikipedia.org/wiki/Birthday_problem) — verified reachable 2026-07-18.
- sim-lab work branch cut from origin/main HEAD (VERDICT 143 landed, sim-lab #217 `74f5bc0`).
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `main()` calls `run()` to build a results dict that carries NO `results_sha256` field, computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode()).hexdigest()`, then PRINTS the human-readable `json.dumps(results, indent=2, sort_keys=True)` dump followed by `Results-JSON sha256: <digest>`. It writes NO results file. **Twist:** the disclosed digest is the sha256 of the COMPACT-canonical serialization (sort_keys + `(",",":")` separators), NOT the pretty indent=2 dump printed on stdout — two serializations of the same dict; recompute over the compact form. Classified against `main()` lines 217–243 in the script text, not assumed. The computation lives in `run()` (returns the results dict), so the in-process double-run calls `run()` twice and compact-hashes each return value.

## Constraints honored
Stdlib-only (random, math, json, hashlib); verifier copied byte-identical (diff exit 0, sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA per ORDER 003).

## Gate plan (disclosed → reproduced), order G1→G2→G3
- G1 SIM CORRECT: at N_HI the Monte-Carlo mean waiting time matches the EXACT finite-N anchor E[T_N]=Σ_k P(first k draws distinct) within z<3σ (standard error of the mean). Disclosed anchor E[T_16384]=**161.091690**.
- G2 SQRT-N LAW: the MC ratio mean(T@N_HI)/mean(T@N_LO) matches the EXACT-mean ratio E[T_16384]/E[T_1024]=**3.950654** within z<3σ (delta method) AND that exact ratio is a √N-law value (≈√16=**4.0**) decisively far from the linear-N folk prediction N_HI/N_LO=**16.0** (sqrt_not_linear structural check) — the mean grows like √N, not N.
- G3 INVERSION: at N_HI the MC fraction of trials collided by draw m*≈1.1774√N EXCEEDS 0.5 (majority) AND matches the exact anchor 1−P(all m* distinct)=**0.500101** within z<3σ, with m*=**151** and m*/(N/2)=**0.018433**≪1 — the "need ~N/2 draws" folk belief reversed.

## Outcome
in-progress — reproduction pending.

## ⟲ Previous-session review
Prior loop landed VERDICT 143 (P130 the annual-prepay financing trap, round-30 VENTURE slot, +13, sim-lab PR #217 merged `74f5bc0`, digest `cf0d9e72…cae7b`) — APPROVE, byte-identical reproduction across cross-invocation A/B + a deterministic in-process double-run, all three gates PASS (G1 implied-financing-rate z=24078.7034σ, G2 deadweight-NPV control z=22938.9668σ with the four-part sign block, G3 IRR/break-even-discount anchor |z|=0.0031 with the implied APR equal to the analytic IRR to <1e-9 and a unique sign change). It did well to make the mechanism the deliverable — a four-part SIGN BLOCK on G2 so "annual prepay destroys value" was a directional claim not a magnitude assertion, and G3 anchored the sampled dollar-weighted PV multiple to the closed-form count-mean (spend-independence, zero-centered). DIGEST-POSTURE carry-forward: both cards are WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (results dict carries no `results_sha256`, no file written, compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form) and both put the compute entry in `run()`, so the in-process double-run calls `run()` twice directly — the V143 mechanics carry straight over. Object shift: V143 was a cash-flow / decision-theoretic object (the IRR of a monthly→annual-prepay cash-timing swap and its NPV destruction); this V145 slice is a first-collision WAITING-TIME / occupancy object (the distribution of the number of draws-with-replacement to the first repeat, and its √N scaling) — a different domain, different object, different math, and a z-profile decisively different from V143's astronomically-tight headline gates: V145's three gates are near-zero z (all statistics land ON their exact anchors within a fraction of a σ, |z| ≤ 0.43) rather than many-σ separations, because here the anchor IS the exact finite-N mean/threshold the MC estimates, not a folk-vs-reality gap being rejected. NON-CONTIGUITY note for the successor: V145 reproduces and CLOSES round-30 (P132 → V145) on the sim-lab side, but several lower verdicts remain open pulls / open idea-engine mirrors below the high-water — V144 for P131 (game slot) is still PENDING (not yet reproduced), and V137 for P124, V132 for P119, V126 for P113 (idea-engine mirror #527) remain open — do not read "high-water V145" as "every lower verdict fully closed on both sides." Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the SYMMETRIC uniform space (all N cells equally likely) and the FIRST-collision waiting time — the classic birthday bound. A genuinely different NEXT unrelated slice, distinct from this first-collision head and the prior UNRELATED closers (P100 Kelly overbet, P104 epidemic overshoot, P108 regression to the mean, P112 series-MTBF collapse, P116 arcsine lead illusion, P120 German-tank MVUE, P124 Benford, P128 Stein shrinkage), pins the **non-uniform-space collision ACCELERATION** (the "birthday attack is worse when the distribution is skewed" correction): draw from N cells with UNEQUAL probabilities p_i (Σp_i=1), and the expected first-collision waiting time is governed not by N but by the collision entropy / Rényi-2 effective size N_eff=1/Σp_i²≤N, so E[T]→√(π·N_eff/2) and a non-uniform space collides SOONER than a uniform one of the same support size — every skew strictly shrinks the safe draw budget. The pre-registrable structure: (G-a) at the uniform corner p_i=1/N the effective size N_eff=N exactly and the waiting-time mean reproduces this slice's √(πN/2) (the uniform case is the boundary); (G-b) as the distribution skews (e.g. a Zipf or a two-mass p) N_eff=1/Σp_i² falls strictly below N and the MC mean waiting time tracks √(π·N_eff/2) within 3σ, decisively below the uniform-N prediction — isolating collision entropy, not support size, as the true yardstick; (G-c) a MONOTONICITY control shows E[T] is strictly decreasing in the skew (a majorization / Schur-convexity ordering: more-spread p collides later, more-concentrated p collides sooner) with a ≥3σ separation between a near-uniform and a concentrated draw. The operator lesson sharpens from "size a random-ID space by √N" to "size it by the COLLISION-ENTROPY effective size N_eff=1/Σp_i² — a biased or non-uniform generator (weak RNG, structured IDs, hot keys) shrinks N_eff below the nominal space and collides after only ~√N_eff draws, so audit the generator's distribution, not just its bit-width." A seed-pinnable probability mechanism deliberately distinct from this uniform-first-collision head and the prior unrelated closers.

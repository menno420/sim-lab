# VERDICT 231 — a strictly positive search cost pins the unique symmetric equilibrium at the full monopoly price v for EVERY number of sellers N; competition never lowers the price and the Bertrand descent to marginal cost lives only at exactly zero friction (Diamond's paradox) — reproduce PROPOSAL 218

> **Status:** complete

📊 Model: Opus family · high · verdict-reproduction

started: 2026-07-20T16:33:28Z

💓 Heartbeat: round-52 VENTURE slice P218 → V231 (+13); reproduction on branch `claude/verdict-231-diamond-search-cost-monopoly-price`; sim dir `sims/verdict-231-diamond-search-cost-monopoly-price/` (byte-identical verifier copy + run-stdout.txt + probe-report). Digest target full-64 `c71985cb55577757ed79772b3fabb7677611f3469531c10ab60f8e73a91d8036` (printed AND independently grep-extracted, full-64 EXACT string compare, no truncation). Determinism CONFIRMED (verifier's in-process double-run assert AND separate cross-invocation processes byte-identical stdout). Four pre-registered gates each in its own direction — G1 EXACT (unique symmetric equilibrium == monopoly price v, exhaustive Fraction best-response enumeration, 200/200); G2 SURPRISE (equilibrium price rejects the marginal-cost folk at z=100.551≥3σ, price==v in all 3000 markets); G3 ROBUSTNESS (equilibrium invariant to N and every positive s, monopoly price is an equilibrium iff s>0 — the zero-friction discontinuity, 120/120); G4 identity + FALSIFIABILITY (industry profit == v split v/N, folk Bertrand profile falsified, true profile deviation-free, 200/200). Grounding byte-pinned to Wikipedia "Price dispersion" oldid 1345367307 wikitext sha1 930ead7d5a9ce98d5ae063c4cbfea4bf3315c63f. Born-red HOLD armed on this first card commit; released only by the deliberate `complete` flip LAST. VERDICT 231 = APPROVE.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only after the sim dir (byte-identical verifier copy + reproduction stdout + probe-report), the digest match (full-64 exact `c71985cb…91d8036`, printed + independently grep-extracted), the four-gate evaluation each in its own direction (all PASS), the determinism check (in-process double-run held AND separate-invocation stdout byte-identical), and the grounding check have ALL landed, and the status.md heartbeat is re-stamped. That flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation merge the green PR.

## What this verdict does
Reproduces PROPOSAL 218 (Diamond paradox) independently in sim-lab: copies the verifier byte-identical, reproduces the disclosed results-dict sha256 to all 64 hex, confirms determinism, evaluates each of the four gates in its own direction, and scrutinises the Wikipedia grounding against the pinned revision. Ruling: APPROVE.

## Method
Byte-identical `cp` of the idea-engine verifier → `diff` exit 0; separate `python3` invocations + the in-process double-run assert for determinism; `grep -o` of the full 64-hex digest against the disclosed target; per-gate reading of the pass direction in the source alongside the emitted results dict; `action=raw` wikitext fetch + sha1 compare (raw and MediaWiki canonical API) + grep for the Diamond's-paradox mechanism and both endpoints on-page vs the three firsthand legs off-page.

## ⟲ Previous-session review
- Boot: idea-engine @ a48299c (P218 landed, target V231 open pull), sim-lab @ 844801e (V230 landed); hard-synced both to origin/main.
- Verdict high-water taken: V230 → advancing to V231 (union-max). P218 → V231 is the newest open pull (+13 offset).
- sim-lab had no diamond verifier, no P218 intake, no V231 branch — V231 unstarted. Reproduced it here.
- Round-52 rotation: FLEET (V230) → VENTURE (this V231) → game → unrelated.

## 💡 Session idea
The folk "more competitors ⇒ lower price" intuition is exactly inverted under an arbitrarily small search friction: the unique symmetric equilibrium is the full monopoly price for every N, and the competitive (Bertrand) outcome is a measure-zero discontinuity at exactly s=0. A verdict that reproduces this cleanly is a strong ledger entry; the natural next probe is whether the pin survives heterogeneous search costs or a positive outside option (partial coverage) — a QUALIFIED boundary worth a future PROPOSAL.

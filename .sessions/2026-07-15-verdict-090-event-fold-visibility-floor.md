# Session — VERDICT 090 — the festival the engine cannot show: the timed-events design's piecewise-exact fold and its "visibly richer in the exact T6 proportion" promise are jointly unsatisfiable on the committed lattice — the canonical tier-1 start cell pays ZERO extra for every event multiplier below ×2 (99/99 dead, first paying pct exactly 200), a +10% window is delivered as +0% or +100% but never +10% (jackpot exactly 20/11), and each of the three repairs breaks a different committed sentence (idea-engine PROPOSAL 077, the round-16 FLEET-BACKLOGS rotation opener, superbot-idle; P077 → V090 under the +13 offset)

> **Status:** `complete`
> 📊 Model: fable-class · high · verdict-sim

Objective: produce VERDICT 090 for idea-engine PROPOSAL 077 (the
event-fold visibility floor,
`ideas/superbot-idle/event-fold-visibility-floor-2026-07-15.md`, read at
idea-engine origin/main 8aa71ba, landed via idea-engine PR #442 —
verified merged at live GitHub this session: merged 2026-07-15T23:14:34Z,
merged_by github-actions[bot], head cb9c2d9; outbox block stamped
2026-07-15T23:10:50Z). One slice, one branch
(`claude/2026-07-15-verdict-090-event-fold-visibility-floor`), one
verdict. NUMBERING, verified literally at sim-lab origin/main `d212882`
(VERDICT 089 merged via PR #158; the d212882 tip itself is the
merge-automation probe #160, not a verdict): newest `## VERDICT` header
is 089, `## VERDICT 090` and `## INTAKE 077` collision-grepped clean
(outbox + archives + current-state + sims + .sessions + remote refs — no
competing verdict-090 branch or PR; zero open sim-lab PRs), so
idea-engine PROPOSAL 077 → **VERDICT 090**, the +13 offset's fourteenth
row (map-row extension landed in `docs/current-state.md` this same PR).
Worker session; `control/status.md` and `control/inbox.md` untouched
(ledger appended to `control/outbox.md` only; delivery is the manager's
Q-0264 fan-in). No idea-engine claim file this slice (the V074/V077–V089
precedent — the collision-grep + born-red card + READY PR bind the
number). Seeds: the drafter's registered set **20261690–693** IS the
session seed set (the V077–V089 precedent), quoted literally from the
registration — Arm-R reporting-only 20261690/691/692 (10,000 traces
each, WITH the registered draw-order grammar — the V089 💡 applied by
the drafter), aux 20261693 reserved and asserted never read; the gap
20261684–689 is the disclosed in-flight buffer (P076/V089's set ends at
20261683). External ground truth re-verified FIRSTHAND before the
fixture was written: superbot-idle read-only clone @
`884aeae9687742a389a2e2086a4cc930e5a4f3ee` (the doc's § 2 fold + the
graduation identity verbatim, § 1a's proportionality promise, SE-2's
"credited exactly the window bonus", § 3's "state_version stays 2",
§ 6's "nothing is built"; engine.py's six-factor `// 100_000_000`
single-floor fold with count INSIDE; economy.py's 25/10/5 percents and
9 milestone rungs; the 18-pack `base_rate` multiset {1×18, 5×17} with
zero `balance` blocks; state.py's 90..110 theme bounds — all verbatim,
zero harvest anomalies). pokemon-mod-lab NOT touched (recorded wall).
This slice worked from a fresh shallow clone (`sim-lab-v090`) and pushed
from there (the V080–V089 fresh-clone guard, carried). This card held
the substrate gate red deliberately until this flip (the born-red
discipline — the designed hold was the only red this branch produced
itself).

Flagged, decide-and-flag: (a) the live `control/outbox.md` measured
406,735 B pre-append — above the fleet 200KB rollover threshold; rolling
is a manager-ordered housekeeping op (inbox ORDER 005 item 5 parks it),
NOT this verdict slice's scope, and V086–V089 appended under the same
condition — follow-up stays with the manager. (b) One
`.substrate/guard-fires.jsonl` telemetry line (this slice's own born-red
check fire) rode the control-appends commit per the append-only
telemetry convention. (c) The registration pins the pencil-calendar
cells by COUNT only ("four lattice cells") and the alt-fold control by
NAME only — both sim-chosen, disclosed in fixtures.json, and ledgered as
VACANCY rows in the structured anomaly census, not match claims.

## What happened

Built `sims/verdict-090-event-fold-visibility-floor/` under the standing
discipline: fixtures.json committed BEFORE the runner (card 1708bfa →
fixtures a9b0aaa → runner 80208fd → accepted run 0a29354 → control
appends + map row 96dd061 → this flip), three-arm: Arm A seedless
pure-integer enumeration of all 214,500 committed cells, DECISION-
bearing — the graduation identity asserted on every cell with the
2·25·13·11·10·3 size contact, the T1 dead band (cap-free all-E sweep
101..199), the per-E dead censuses with the dead-at-rate-≥20 row, the
staircase envelope with per-E edge-hit counts (ratio extremes tracked by
cross-multiplication, no floats anywhere), all three repair arms, the
pencil calendars (closed form == 1 s tick loop), and the degeneracy
controls (theme-100 sub-lattice, cap-bump 26/13/11, E = 100 neutrality,
the alt-fold named control); Arm B INDEPENDENTLY-WRITTEN twin — a
Fraction-based classifier with its own loop nesting and factor order
plus the closed-form envelope, tied through the FOUR typed must-equal
contacts (99 = 199−101+1 · overshoot == jackpot, both 20/11 · the ×1.10
dead census A == B == 4,151 · payers-at-×2 == alive == 214,496) and its
own decision evaluator; Arm R seeded window traces, REPORTING-ONLY,
under the drafter's REGISTERED draw-order grammar (3 draws per trace,
order pinned; 30,000 draws per seed counted and asserted; each seed
reproduces itself in-process; no statistical gate). 74 self-checks, 74
passed, 0 failed; exit 0; ~15 s/run; byte-identical double run (results
f16a1ca9…, run-stdout 84b7f06d…, sha256 in REPORT.md); CPython 3.11
asserted; no fix-forwards after the runner landed — the first complete
in-repo run of the committed pipeline is the accepted run (rehearsed to
green on untracked outputs before the runner commit; the one rehearsal
fix was a ratio-RENDERING artifact, '1/1' vs the registered '1', fixed
before the runner existed). PR #161 opened READY.

**VERDICT 090 — REJECT, all three registered clauses exact, ZERO
anomalies (structured census: 20 compared-and-matched, 0 mismatched, 5
honest vacancies).** R1: the canonical cell (pre-floor product exactly
10^8, rate exactly 1/s) dead for all 99 integer E in 101..199, first
paying E exactly 200 (rate 1 → 2, the registered margin-0 contact), one
upgrade still dead at ×1.5, the 1.375·1.5 = 2.0625 neighbour confirmed.
R2: dead censuses exactly 4,151/675/56/14/0 (×2.00 zero certified),
zero dead at rate ≥ 20, jackpot exactly 20/11 on the registered witness
(1,1,0,2,8,110), minima (10/11, 4/5, 2/3, 4/7, 1), envelope two-edged at
every grid E. R3: arm (a) overshoot exactly the jackpot's 20/11; arm
(b) milli census 0 with the ≥ 99 certificate and canonical minimum
exactly 100; arm (c) deviation exactly 1/22 at ×1.10 on a rate-20 cell.
T5 CONFIRMED: exactness survives byte-equal; only proportionality dies.
Controls all non-flipping (theme-100 dead row 1,390/224/18/4/0;
cap-bump unchanged; alt-fold a genuinely different world, 60,824 vs
4,151 at ×1.10). And the slice's cleanest landing: all three seeded
Arm-R previews reproduced EXACTLY on the first rehearsal run —
(49, 40,533,190,487) / (41, 39,898,597,051) / (43, 39,906,644,697) —
because the drafter had REGISTERED the draw-order grammar (the V089 💡),
so zero forensics were owed.

## Previous-session review

**Previous-session review** (⟲ previous session = VERDICT 089, sim-lab
PR #158): a clean landing whose conventions this slice consumed
wholesale — born-red card / collision-grep / no-claim-file / fresh-clone
/ registered-allocation-as-seed-set carried verbatim and made the
numbering mechanical (V089's INTAKE noted the +13 offset's thirteenth
row; this slice extended to fourteen with the same grammar); V087's
structured anomaly census and V086's typed margin ledger both fit this
head's degenerate-margin regime exactly (every clause an equality /
zero / saturation — the ledger names the cells rather than pretending
clearances). The decisive carry: **V089's 💡 (seeded previews are
accidental registrations; register the draw GRAMMAR in one fixture
line) was applied by the P077 drafter and VINDICATED here on its first
outing** — where V089 had to reconstruct an unregistered stream order
forensically and ledger a vacancy, this slice's Arm-R previews
reproduced exactly on the first run with the order pinned in the
registration; the one-sentence fixture line V089 asked for is now
measurably cheaper than the forensic alternative. One divergence from
V089 worth naming: V089 was FULLY HERMETIC (no external constant to
verify), so its arm budget went to a second in-space census; this head
carries COMMITTED external constants, so the equivalent budget went to
the firsthand re-verification pass at superbot-idle 884aeae BEFORE the
fixture was written (the V085/V088 pattern) — the hermetic-head twin
substitution and the committed-head fidelity pass are now both
established defaults, chosen by whether the head pins external truth.

💡 **Session idea (genuine, this session):** REGISTER EXACT RATIONALS AS
VALUE PAIRS, NEVER AS SPELLINGS — this slice's ONLY rehearsal failure
was a false mismatch on an exactly-correct value: the sim rendered the
×2.00 minimum ratio as the reduced string "1/1" while the registration
disclosed "1", and a string-equality anchor comparison flagged a
first-class anomaly where the VALUES were identical. String-compared
anchors smuggle a presentation convention (reduced? bare integers?
n/d order?) into what is really a value comparison, and the failure
mode is poisonous in both directions: a false MISMATCH costs a
rehearsal cycle (here), and — worse — a false MATCH could pass a
wrongly-derived value that happens to share a spelling under a
different reduction convention. The durable upgrade for future drafters
and verdicts: disclose exact rationals as integer PAIRS ([20, 11], [1,
1]) or let the fixture pin the rendering rule in one line ("reduced n/d,
whole values bare"), and have the sim compare Fraction VALUES with the
spelling derived, never compared. Dedup: V086's 💡 typed the margin
ledger's rows, V087's typed the anomaly census's coverage, V088's typed
the visibility claim's surface denominator, V089's bound unregistered
PROCEDURES to their disclosed outputs — none states the value-vs-
spelling anchor rule; no prior card or verdict does (this idea was
discovered live: the '1/1' fix was this slice's only rehearsal
divergence, and it was a rendering bug in the COMPARISON, not in any
derived number).

📊 Model: fable-class · high · verdict-sim

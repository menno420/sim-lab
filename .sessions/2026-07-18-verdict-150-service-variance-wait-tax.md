# VERDICT 150 — service-variance wait tax

Reproduce PROPOSAL 137 (round-32 FLEET slot OPENER, P137 → V150, +13): for an M/G/1 FIFO queue the Pollaczek–Khinchine mean-value law gives mean queue wait W_q = (ρ/(1−ρ))·E[S]·(1+C²)/2, where C²=Var(S)/E[S]² is the squared coefficient of variation of SERVICE time, so at FIXED utilization ρ=0.8 and FIXED mean service E[S]=1.0 the wait depends ONLY on service-time variance: W_q = 2.0 (M/D/1, C²=0) / 4.0 (M/M/1, C²=1) / 10.0 (M/H2/1, C²=4) — a 1:2:5 (5×) spread at the same load and same average job, invisible to any utilization/throughput dashboard because the (1+C²)/2 tax is a multiplicative factor capacity math never sees. The folk reading "same utilization, same throughput ⇒ same latency" is inverted: variance alone quintuples the queue wait.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P137 reference verifier byte-identical under its pinned world (SEED=20260717, RHO=0.8, MEAN_SERVICE=1.0, N_JOBS=600000, WARMUP=150000, REPLICATIONS=30, Z_GATE=3.0, CV2_H2=4.0) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `ab44d56a22c24f83c4a2048af56dc3dbdbf462d3aa9990bd19c64426891e4307`, (b) all three gates hold as disclosed, in order G1→G2→G3, (c) cross-invocation runs are byte-identical AND an in-process double-run is deterministic, (d) the exact P-K anchors reproduce (W_q = 2.0 / 4.0 / 10.0 at C²=0/1/4, the 1:2:5 spread; balanced-means H2 params p1=0.887298, branch means 0.563508/4.436492 giving mean 1.0 and C²=4 exactly). Target: sim-lab (VERDICT 150, +13 offset); idea-engine mirror.

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/service_variance_wait_tax.py` — file sha256 `945c0af9bc496522f1b03935afa975a89386a58ab76a246c870a3a2cc6c97974`, git blob `c13e49f78abac8ff39dd62514e37a97413c18624`, **174** lines / **5592** bytes. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/f008e130a058b1a62b92d053bb6fd315ec730ac4/ideas/fleet/service_variance_wait_tax.py (idea-engine main `f008e13`, PROPOSAL 137 landed via #577). Disclosed digest + offset permalink (idea-engine outbox P137 block): https://github.com/menno420/idea-engine/blob/f008e130a058b1a62b92d053bb6fd315ec730ac4/control/outbox.md
- Offset authority (cited verbatim from the P137 depends-ledger): "sim-lab consumes at +13 (PROPOSAL 137 -> VERDICT 150). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P136 -> V149)." (idea-engine `control/outbox.md` PROPOSAL 137 block, dated `2026-07-18T15:13:15Z`) — PROPOSAL 137 → VERDICT 150 (+13). The live offset authority is that outbox's per-block ledger + idea-engine `control/status.md`, not `docs/current-state.md` (a dated SEAT-DORMANT snapshot).
- Domain reference: the Pollaczek–Khinchine mean-value formula for the M/G/1 queue — https://en.wikipedia.org/wiki/Pollaczek%E2%80%93Khinchine_formula (Kleinrock, Queueing Systems Vol.1 1975 §5.6) — verified reachable 2026-07-18. W_q = (ρ/(1−ρ))·E[S]·(1+C²)/2 with C²=Var(S)/E[S]²; the exact single-server FIFO Lindley recursion W_{i+1}=max(0, W_i+S_i−T_i) with T_i~Exp(λ), λ=ρ/E[S]=0.8 realizes it. Balanced-means two-phase hyperexponential (p1=½(1+√((C²−1)/(C²+1)))) gives mean E[S] and C² exactly.
- sim-lab work branch cut from origin/main HEAD `2565820` (VERDICT 148 landed, sim-lab #223). NON-CONTIGUITY: V150 (P137, round-32 FLEET OPENER) sits above the V149 high-water but V137 (P124), V132 (P119), V126 (P113 mirror idea-engine #527) remain open below — landing V150 does NOT imply every lower verdict closed on both sides. V150 advances the verdict high-water to **V150** (union-max, no regress).
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `main()` calls `run()` to build a results dict carrying NO `results_sha256` field, computes `canonical=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(canonical.encode("utf-8")).hexdigest()`, then PRINTS `json.dumps(results, indent=2, sort_keys=True)` followed by `Results-JSON sha256: <digest>`. Writes NO results file. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump — two serializations of the same dict; recompute over the compact form. Classified against `main()` in the script text, not assumed. The compute entry is `run()`, so the in-process double-run calls `run()` twice and compact-hashes each return value.

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier copied byte-identical (diff exit 0, sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA).

## Gate plan (disclosed → reproduced), order G1→G2→G3
- **G1 DETERMINISTIC M/D/1 (C²=0):** the method-of-independent-replications grand-mean queue wait matches the P-K anchor W_q=2.0 within z<3σ (the reps' std/√R standard error is valid because each replication contributes ONE post-warmup mean — Lindley waits within a run are autocorrelated, so naive std/√n would be invalid). Disclosed z=**+0.496**.
- **G2 EXPONENTIAL M/M/1 (C²=1):** grand-mean queue wait matches the P-K anchor W_q=4.0 within z<3σ — DOUBLE the deterministic wait at the SAME load, from service variance alone. Disclosed z=**−0.934**.
- **G3 HYPEREXPONENTIAL M/H2/1 (C²=4):** grand-mean queue wait matches the P-K anchor W_q=10.0 within z<3σ — 5× the deterministic wait, completing the 1:2:5 variance-only spread. Disclosed z=**−1.838**.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `ab44d56a…91e4307` EXACTLY across cross-invocation A/B and an in-process double-run? — State the digests.
**2.** Is the verifier byte-identical to the P137 source (`diff` exit 0, sha256 match)? — State the exit code.
**3.** Do all three gates PASS in the pre-registered order G1→G2→G3 with the disclosed z-values (+0.496 / −0.934 / −1.838), and does the verifier exit 0?
**4.** Is the wait-tax claim structural, not a magnitude artifact — do the three grand means land on the exact P-K anchors 2.0 / 4.0 / 10.0 (the 1:2:5 spread) at FIXED ρ=0.8 and FIXED E[S]=1.0, so only C²=0/1/4 varies?

## Outcome — APPROVE (exact reproduction)
- Verifier copied **byte-identical** from idea-engine `ideas/fleet/service_variance_wait_tax.py` — `diff` exit **0**, file sha256 `945c0af9bc496522f1b03935afa975a89386a58ab76a246c870a3a2cc6c97974` (174 lines / 5592 bytes, git blob `c13e49f78abac8ff39dd62514e37a97413c18624`).
- Reproduced digest `ab44d56a22c24f83c4a2048af56dc3dbdbf462d3aa9990bd19c64426891e4307` **== disclosed digest EXACTLY (MATCH)**. Cross-invocation A + B both printed byte-identical stdout (`diff` exit 0) with this digest, and an in-process double-run (`run()` ×2, compact-hashed in-process) produced the identical results dict AND identical digest both times, both == disclosed; all exit 0.
- **G1 deterministic M/D/1 (C²=0): PASS** — grand-mean queue wait **2.002221** vs P-K anchor **2.0**, z=**+0.496222** (se 0.004476, std 0.024515), |z| < 3σ — the zero-variance baseline is the pure load term.
- **G2 exponential M/M/1 (C²=1): PASS** — grand-mean **3.987717** vs P-K anchor **4.0**, z=**−0.934478** (se 0.013144, std 0.071994), |z| < 3σ — DOUBLE the deterministic wait at the SAME ρ=0.8 and SAME E[S]=1.0, from service variance alone.
- **G3 hyperexponential M/H2/1 (C²=4): PASS** — grand-mean **9.921619** vs P-K anchor **10.0**, z=**−1.837859** (se 0.042648, std 0.233594), |z| < 3σ — 5× the deterministic wait, completing the 1:2:5 variance-only spread.
- Anchors (all reproduced): W_q = 2.0 / 4.0 / 10.0 at C²=0/1/4 (the exact 1:2:5 spread at fixed ρ=0.8, E[S]=1.0); H2 balanced-means p1=**0.8872983346207417**, branch means **0.5635083268962916** / **4.436491673103709** (mean 1.0, C²=4 exactly); all_pass=**true**, exit **0**, first-failing gate **none**. This final flip to `complete` is the LAST commit that releases the born-red HOLD; landing via merge-on-green, zero agent merge calls. idea-engine mirror: branch `claude/verdict-150-service-variance-wait-tax`. Full probe evidence: `sims/verdict-150-service-variance-wait-tax/probe-report.md`. **APPROVE.**

## ⟲ Previous-session review
Prior loop landed VERDICT 149 (P136 the base-rate PPV collapse, round-31 UNRELATED slot CLOSER, +13, sim-lab PR #222 merged `29c343e`, digest `89c4bd02…895faac8`) — APPROVE, byte-identical reproduction across cross-invocation A/B + an in-process double-run, all three gates PASS in order (G1 posterior=0.5 z=−0.898 vs exact PPV 0.5 at 1% prevalence, G2 positive-rate z=+0.563 vs exact 0.0198, G3 collapse-deepens z=+1.188 vs exact PPV 0.0901639 at 0.1%). It anchored every gate on the exact Bayes closed form so the base-rate mechanism was structural, not a magnitude gap, and its ⟲ review carried the backfill-discipline nit (leaving the PR URL to be filled into the merged card). DIGEST-POSTURE carry-forward: V149 and this V150 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (results dict carries no `results_sha256`, no file written, compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form) and both put the compute entry in `run()`, so the in-process double-run calls `run()` twice directly — the V149 mechanics carry straight over. Object shift: V149 was a Bayes base-rate / posterior object driven by a single Monte-Carlo stream; this V150 is a QUEUEING / M/G/1 Lindley-recursion object whose valid standard error demands the METHOD OF INDEPENDENT REPLICATIONS (30 independent sub-seeded reps, each one post-warmup mean sample) precisely because Lindley waits are autocorrelated — a different variance-estimation posture than V149's Bernoulli se. NON-CONTIGUITY note for the successor: V150 (P137, round-32 FLEET OPENER) lands above the V149 high-water but V137 (P124), V132 (P119), V126 (P113 mirror #527) remain open below — do not read "V150 landed" as advancing every lower verdict to closed on both sides. Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the variance tax at a SINGLE utilization (ρ=0.8) across three service distributions (C²=0/1/4), reading the (1+C²)/2 factor off three points. A genuinely distinct NEXT fleet-slot slice, distinct from this fixed-ρ three-point head, pins the **variance–utilization interaction surface and the "variance-equivalent utilization" lever**: because P-K factors cleanly into a load term ρ/(1−ρ) and a variance term (1+C²)/2, the two knobs are SEPARABLE and multiplicative — so a fleet operator can trade one for the other. The pre-registrable structure: (G-a) sweep ρ∈{0.5,0.7,0.9} × C²∈{0,1,4} and confirm each cell's grand-mean wait equals the product form within 3σ (separability — the surface is rank-1 in the (load, variance) factors, no interaction beyond the product); (G-b) solve for the ρ' that makes an M/D/1 queue (C²=0) at ρ' incur the SAME wait as an M/M/1 queue (C²=1) at ρ=0.8 — the "variance-equivalent utilization" — and MC-confirm the two waits coincide within 3σ, quantifying exactly how much spare capacity a variance-halving buys; (G-c) a heavy-traffic control (ρ→1) confirms the variance tax stays a bounded multiplicative factor even as the load term diverges, so variance reduction and capacity addition are NOT substitutes near saturation (the load term dominates). The operator lesson sharpens from "service variance quintuples the wait at fixed load" to "variance and utilization are separable multiplicative knobs, so cutting C² is worth a computable amount of spare ρ — but only away from saturation, where the load term runs away regardless." A seed-pinnable queueing mechanism deliberately distinct from this fixed-ρ P-K wait-law head, and distinct from P089 variance-blind-provisioning-trap (which prices the SLA-violation rate + the (1+CV²) CAPACITY-provisioning correction, not the pure wait law).

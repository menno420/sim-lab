# VERDICT 175 — option-pool-shuffle: a PRE-money option pool of size q is carved entirely from the founders, so the investor's post-money fraction t = I/(P+I) is INVARIANT to q, and founders hand the investor an extra q·t of ownership versus a pro-rata pool that would have diluted both parties

Reproduce PROPOSAL 162 (P162 → V175, +13): the option-pool shuffle — in a priced VENTURE round the option pool is negotiated INTO the pre-money valuation, so the pool dilutes only the founders while the investor's post-money percentage is untouched. Under the pinned world an investor puts in I at pre-money valuation P (post-money P+I), and an option pool of size q (a fraction of the post-money capitalization) is created; when the pool is carved PRE-money the founders absorb all q, whereas a pro-rata (post-money) pool would split q between founders and investor in their post-money proportions. The folk belief (inverted here): "the option pool is a neutral, shared cost of building the company — both the founders and the new investor fund it in proportion to their stakes." It is NOT: the standard term-sheet construction places the pool PRE-money, so the investor's post-money fraction stays fixed at t = I/(P+I) regardless of q (the pool is invisible to the investor's percentage), and the founders alone eat the pool — handing the investor an extra q·t of ownership relative to a pro-rata pool. Three ordered z-gates on the relative-error-vs-ceiling convention (z_gate=3.0, CEIL=0.05): G1 investor-fraction-invariant (the investor's post-money fraction t = I/(P+I) does not move as the pool size q is swept — the pre-money pool is invisible to the investor's percentage) → G2 extra-transfer-equals-qt (the difference between the investor's stake under the pre-money pool and under the pro-rata pool equals q·t — the founders' extra give-up) → G3 robust-under-shift (the invariance and the q·t transfer survive a shifted mega-round distribution of I/P). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (a single priced round with the pool defined as a fraction of the post-money cap and the investor's fraction pinned by the term sheet at t = I/(P+I)) and a wealth-transfer SIGN claim — the SIGN (the pre-money pool transfers ownership from founders to the investor; the extra give-up is q·t) survives, but the exact q·t figure is a property of the single-round post-money-pool accounting; multi-round refresh dynamics and anti-dilution terms are disclosed descriptive fields, not hidden defects.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P162 reference verifier byte-identical under its pinned world (SEED=20260717; founder_shares=10000000; n_draws=4000 Monte-Carlo cap-table draws; z_gate=3.0; CEIL=0.05; pool-size q and the investment/pre-money ratio I/P swept per draw; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `f588004512fce81ea824be9fec0c95a3ba2f3e2a8ec03af5867b526bbcb5b4b5`, (b) all three gates hold in order G1 investor-fraction-invariant → G2 extra-transfer-equals-qt → G3 robust-under-shift, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (a PRE-money option pool of size q is carved entirely from founders; the investor's post-money fraction t = I/(P+I) is invariant to q; founders hand the investor an extra q·t versus a pro-rata pool). Pre-registered decision rule (P162): APPROVE VERDICT 175 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the exact q·t transfer being a property of the single-round post-money-pool accounting (multi-round refresh / anti-dilution as disclosed descriptive fields) is not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 175, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: `ideas/venture-lab/option_pool_shuffle.py` @ idea-engine — git blob `60990ecbc8a152177d3b16989ea0a87581dbe2c3`, file sha256 `8624c977ad2381e52485230f616266a9fb1d64ccd53961a7f62d60926ae6c7b6` (7654 bytes, 216 lines). The committed P162 source in its venture-lab fleet lane, copied byte-identically (`diff` exit 0, copy sha256 == source, copy git blob == source blob) into the reproduction commit at `sims/verdict-175-option-pool-shuffle/option_pool_shuffle.py` — copy sha256 `8624c977ad2381e52485230f616266a9fb1d64ccd53961a7f62d60926ae6c7b6`, copy git blob `60990ecbc8a152177d3b16989ea0a87581dbe2c3` (7654 bytes, 216 lines).
- Offset authority: +13 (P162 → V175), round-38 VENTURE slot (venture-financing / cap-table accounting). Per idea-engine `control/outbox.md` P162 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: the option-pool shuffle — the standard priced-round construction where the option pool is created inside the pre-money valuation, diluting only the founders while the investor's post-money fraction t = I/(P+I) is untouched; a pro-rata (post-money) pool would instead split the pool q between founders and investor in their post-money proportions, so the pre-money pool transfers an extra q·t to the investor. Grounding: https://venturehacks.com/option-pool-shuffle — the domain reference resolved LIVE this session (HTTP 200); the closed forms are hand-checked against the reproduced numbers in the Outcome below.
- Disclosed digest: compact-canonical results-dict sha256 `f588004512fce81ea824be9fec0c95a3ba2f3e2a8ec03af5867b526bbcb5b4b5`, SEED=20260717 — reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (confirmed): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V171–V174). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent run and asserts the two dicts are equal (in-process double-run byte-identity), prints a pretty indent=2 dump plus the sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. Confirmed byte-exact in the reproduction commit.

## Constraints honored
Stdlib-only (math, json, hashlib, random); verifier copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The investor's post-money fraction and the founders' extra give-up are measured from the computed cap table across the pool-size and I/P sweep (n_draws=4000), not plugged from the closed form — so the invariance and the q·t transfer are attributable to the reproduced accounting, not a tautological substitution. The gates are definitional contrasts on those computed statistics: G1 investor-fraction-invariant (the investor's fraction change under the pre-money/shuffle pool vs under a pro-rata pool across the q sweep), G2 extra-transfer-equals-qt (measured pre-money-pool-minus-pro-rata investor-stake gap vs q·t), and G3 robust-under-shift (both survive a shifted mega-round I/P distribution). Honest scope: the head is EXACT GIVEN THE MODEL and a wealth-transfer SIGN claim — it holds for the single priced round with the pool defined as a fraction of the post-money cap; multi-round refresh dynamics and anti-dilution terms are disclosed descriptive fields, and no gate reads the q·t transfer as a multi-round-universal constant.

## Gate plan (reproduced at HEAD), order G1 → G2 → G3
- **G1 investor-fraction-invariant (z ≥ 3, one-sided vs the ceiling):** the investor's post-money fraction does not move as the pool size q is swept when the pool is carved PRE-money — `shuffle_change_absmean` **0.0** (the shuffle pool is invisible to the investor's percentage) — whereas a PRO-RATA pool would move it by `prorata_minus_shuffle_change_mean` **−0.0283** (se **0.000146**), a contrast significant at z = |−0.0283|/se = **+194.139005** — **PASS**.
- **G2 extra-transfer-equals-qt (z ≥ 3):** the measured extra transfer to the investor under the pre-money pool equals q·t to machine precision — `transfer_mean` **0.04526** vs `predicted_qt_mean` **0.04526**, `relerr_mean` **0.0**, z **+260.109119** — the founders' extra give-up IS q·t — **PASS**.
- **G3 robust-under-shift (z ≥ 3):** under a SHIFTED mega-round distribution of I/P (larger rounds) the investor's fraction stays invariant under the shuffle (`shuffle_change_absmean` **0.0**) while the pro-rata contrast holds — `prorata_minus_shuffle_change_mean` **−0.024062** (se **9.8e-05**), z **+246.501756** — the invariance and the q·t transfer survive the round-size regime shift — **PASS**.
- z_gate **3.0** (require z ≥ 3 on every gate). `gates` all PASS, `all_pass=true`, `first_failing_gate=null`, exit 0. **Disclosed caveat (NOT a gate):** the exact q·t transfer is a property of the single-round post-money-pool accounting; multi-round refresh dynamics and anti-dilution terms would layer additional transfers. The SIGN (the pre-money pool transfers ownership from founders to the investor) and the order-of-magnitude survive, but the exact q·t figure is a disclosed single-round descriptive field, and NO gate reads it as a multi-round-universal constant.

## Probe questions (independent-audit checklist)
**1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest.** — **YES.** Reproduced digest `f588004512fce81ea824be9fec0c95a3ba2f3e2a8ec03af5867b526bbcb5b4b5` — printed by the script (`json.dumps(res, sort_keys=True, separators=(",",":"))` over the 6-dp dict, then sha256), independently recomputed from the stdout JSON block, and identical to the disclosed value. **MATCH** (verifier-printed = recomputed = disclosed). Two fresh `python3` invocations with SEED=20260717 produced byte-identical stdout (`diff` exit 0), and the script's in-process double-run digest assert held on every run.

**2. Is the verifier byte-identical to the P162 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.** — **YES.** `diff` source vs sim-lab copy exit **0** (byte-identical); copy git blob `60990ecbc8a152177d3b16989ea0a87581dbe2c3` and file sha256 `8624c977ad2381e52485230f616266a9fb1d64ccd53961a7f62d60926ae6c7b6` both match the pinned P162 identity at `ideas/venture-lab/option_pool_shuffle.py` exactly (7654 bytes, 216 lines).

**3. Do all three gates PASS in the pre-registered order G1 investor-fraction-invariant → G2 extra-transfer-equals-qt → G3 robust-under-shift with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0?** — **YES.** G1 investor-fraction-invariant shuffle_change_absmean 0.0, prorata_minus_shuffle_change_mean −0.0283 (se 0.000146) z=+194.139005 → G2 extra-transfer-equals-qt transfer_mean 0.04526 = predicted_qt_mean 0.04526, relerr_mean 0.0, z=+260.109119 → G3 robust-under-shift prorata_minus_shuffle_change_mean −0.024062 (se 9.8e-05) z=+246.501756 — all ≥ 3; `gates` all true, `all_pass=true`, `first_failing_gate=null`, exit **0**.

**4. Is the headline read correctly — a PRE-money option pool of size q is carved entirely from the founders, the investor's post-money fraction t = I/(P+I) is INVARIANT to q, and founders hand the investor an extra q·t versus a pro-rata pool — and NOT as "the pool is a neutral shared cost both parties fund in proportion"?** — **YES.** The measured investor fraction does not move under the pre-money/shuffle pool across the q sweep (shuffle_change_absmean 0.0, G1), the pre-money-vs-pro-rata investor-stake gap equals q·t to machine precision (transfer_mean 0.04526 = predicted_qt_mean 0.04526, relerr 0.0, G2), and both survive the mega-round shift (G3). The measured stake is computed from the drawn cap table, not plugged from the closed form. The "neutral shared cost" folk belief conflates a PRO-RATA pool (which would move the investor by −0.0283) with the standard PRE-money pool, which leaves the investor invariant and has the founders alone eat the pool.

**5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact q·t transfer being a property of the single-round post-money-pool accounting (multi-round refresh / anti-dilution as layered descriptive fields) flips no gate SIGN and no order-of-magnitude?** — **YES.** The exact q·t transfer is a property of the single priced round with the pool defined as a fraction of the post-money cap, disclosed as a single-round descriptive field, not a multi-round-universal claim — multi-round refresh and anti-dilution terms would layer additional transfers. No gate reads the q·t transfer as multi-round-universal; the gates test the investor-invariance SIGN (G1), the q·t transfer magnitude (G2), and robustness under the mega-round shift (G3). The caveat flips no gate sign and no order-of-magnitude — a clean APPROVE.

**6. Could the transfer sign be a seed fluke? State whether the effect is structural (the invariance and the q·t transfer hold across the pool-size sweep and the shifted mega-round distribution) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical).** — **NOT a fluke; structural.** The investor-invariance (shuffle_change_absmean 0.0) and the q·t transfer (transfer_mean 0.04526 = predicted_qt_mean 0.04526) hold across the n_draws=4000 pool-size / I/P sweep (G1 z=+194.139005, G2 z=+260.109119) and survive the shifted mega-round distribution (G3 z=+246.501756) — z-scores hundreds of sigma above the ceiling across independent regimes — structural, not a seed artifact. Determinism confirmed both ways: the in-process double-run digest assert held, and two `python3` invocations produced byte-identical stdout (`diff` exit 0), both exit 0.

## Outcome
**APPROVE.**

- **Digest MATCH.** The compact-canonical results-dict sha256 reproduced EXACTLY as `f588004512fce81ea824be9fec0c95a3ba2f3e2a8ec03af5867b526bbcb5b4b5` — printed by the script and independently recomputed from stdout (verifier-printed = recomputed = disclosed; WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY; every float rounded to 6 dp), byte-identical across two fresh `python3` invocations and the in-process double-run assert. Verifier byte-identical to the P162 source `ideas/venture-lab/option_pool_shuffle.py` (`diff` exit 0, file sha256 `8624c977…6ac7b6`, git blob `60990ecbc8a152177d3b16989ea0a87581dbe2c3`, 7654 bytes / 216 lines). SEED=20260717 is pinned in-source, so the run is reproducible without external configuration.
- **Three gates PASS in order** (z_gate=3.0): G1 investor-fraction-invariant shuffle_change_absmean **0.0**, prorata_minus_shuffle_change_mean **−0.0283** (se **0.000146**), z **+194.139005** → G2 extra-transfer-equals-qt transfer_mean **0.04526** = predicted_qt_mean **0.04526**, relerr_mean **0.0**, z **+260.109119** → G3 robust-under-shift prorata_minus_shuffle_change_mean **−0.024062** (se **9.8e-05**), z **+246.501756**. `gates` all true, `all_pass=true`, `first_failing_gate=null`, exit 0.
- **Algebra confirmed.** For a single priced round with investment I at pre-money P (post-money P+I) and an option pool of fraction q of the post-money cap created PRE-money: the investor's post-money fraction t = I/(P+I) is independent of q; a pro-rata (post-money) pool would instead land the investor at t(1−q) — the investor absorbs its share q·t of the pool — so the pre-money pool hands the investor an extra q·t (to machine precision). The measured invariance under the shuffle (shuffle_change_absmean 0.0), the pro-rata contrast (−0.0283), and the transfer (transfer_mean 0.04526 = predicted_qt_mean 0.04526, relerr 0.0) confirm the closed form.
- **Model-basis note.** The model-basis caveat is CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE: the exact q·t transfer is a property of the single-round post-money-pool accounting that multi-round refresh dynamics and anti-dilution terms would layer additional transfers on — a disclosed system-dependent field that flips no gate sign or order-of-magnitude. The gates test the investor-invariance SIGN (G1), the q·t transfer magnitude (G2), and robustness under the mega-round shift (G3); none reads the q·t transfer as a multi-round-universal constant.

**Verdict: APPROVE.**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-174-palm-mginf-insensitivity.md` (V174, P161 Palm / M/G/∞ repair-pipeline insensitivity, round-38 FLEET slot, +13) is coherent and landed a CLEAN APPROVE, the prior verdict high-water: disclosed compact-canonical results-dict digest `9bbe2171…13ef` reproduced EXACTLY, verifier byte-identical (git blob `da7466ec6c75c22993344dc1caf983babee2cccf`, file sha256 `9a8ba388…3c21`, 8479 bytes / 233 lines), all three gates G1 pipeline-is-load → G2 variance-insensitivity → G3 distribution-free PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form (M/G/∞ pipeline Poisson(λ·E[S]), dispersion 1.0 insensitive to repair-time shape) confirmed — the one disclosed field (the exact insensitivity being a property of the ample-server pipeline; M/G/1 recovers the (1+CV²)/2 wait tax) touched no gate sign. DIGEST-POSTURE carry-forward unbroken: V174 and this V175 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no JSON written to disk, the compact-canonical serialization is the digest preimage). Object shift: V174 was the round-38 FLEET-slot Palm / M/G/∞ repair-pipeline insensitivity head (a variance-insensitivity SIGN claim on the ample-server pipeline); this V175 is the round-38 VENTURE-slot option-pool-shuffle head — a wealth-transfer SIGN / order-of-magnitude claim (the pre-money pool transfers an extra q·t from founders to the investor) whose exact figure is a self-disclosed single-round descriptive field (multi-round refresh / anti-dilution layer additional transfers), earning the same clean-APPROVE posture (reproduced this session: digest MATCH, all three gates PASS). CONTIGUITY: union-max advances the verdict high-water to **V175**, no regress over V174. Items open/backfilled BELOW the high-water are NOT closed by landing V175 — V137/P124, V132/P119, V126/P113 remain open below high-water; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V175 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the SINGLE priced round with the pool as a fraction of the post-money cap — the case where the option-pool shuffle's q·t transfer is EXACT and the investor's fraction is a clean t = I/(P+I). The distinct next slice the head names but holds fixed — the MULTI-round frontier: option pools are REFRESHED at each subsequent round, and each refresh re-runs the shuffle on a cap table already diluted by prior rounds, so the founders' cumulative give-up compounds across rounds rather than staying a single q·t. Pre-register (same SEED-pinned, computed-cap-table discipline): sweep the number of rounds and gate (G-a) the single-round base reproduces the q·t transfer at ≥3σ, (G-b) a compounding headline — the cumulative founder give-up across R refreshed rounds EXCEEDS the naive sum of per-round q·t by the cross-round dilution interaction at ≥3σ, and (G-c) an anti-dilution crossover — a full-ratchet vs broad-based weighted-average anti-dilution term shifts WHO absorbs a down-round refresh at ≥3σ. The operator lesson sharpens from "the pre-money pool costs founders q·t in this round" to "the shuffle compounds across every refresh, and the anti-dilution regime decides who eats a down-round pool — so the single-round q·t is a floor, not the whole bill" — distinct from this V175 head, which pins the single priced round and asks only whether the pre-money pool transfers q·t.

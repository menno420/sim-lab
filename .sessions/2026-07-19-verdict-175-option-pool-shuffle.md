# VERDICT 175 — option-pool-shuffle: a PRE-money option pool of size q is carved entirely from the founders, so the investor's post-money fraction t = I/(P+I) is INVARIANT to q, and founders hand the investor an extra q·t of ownership versus a pro-rata pool that would have diluted both parties

Reproduce PROPOSAL 162 (P162 → V175, +13): the option-pool shuffle — in a priced VENTURE round the option pool is negotiated INTO the pre-money valuation, so the pool dilutes only the founders while the investor's post-money percentage is untouched. Under the pinned world an investor puts in I at pre-money valuation P (post-money P+I), and an option pool of size q (a fraction of the post-money capitalization) is created; when the pool is carved PRE-money the founders absorb all q, whereas a pro-rata (post-money) pool would split q between founders and investor in their post-money proportions. The folk belief (inverted here): "the option pool is a neutral, shared cost of building the company — both the founders and the new investor fund it in proportion to their stakes." It is NOT: the standard term-sheet construction places the pool PRE-money, so the investor's post-money fraction stays fixed at t = I/(P+I) regardless of q (the pool is invisible to the investor's percentage), and the founders alone eat the pool — handing the investor an extra q·t of ownership relative to a pro-rata pool. Three ordered z-gates on the relative-error-vs-ceiling convention (z_gate=3.0, CEIL=0.05): G1 investor-fraction-invariant (the investor's post-money fraction t = I/(P+I) does not move as the pool size q is swept — the pre-money pool is invisible to the investor's percentage) → G2 extra-transfer-equals-qt (the difference between the investor's stake under the pre-money pool and under the pro-rata pool equals q·t — the founders' extra give-up) → G3 robust-under-shift (the invariance and the q·t transfer survive a shifted mega-round distribution of I/P). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (a single priced round with the pool defined as a fraction of the post-money cap and the investor's fraction pinned by the term sheet at t = I/(P+I)) and a wealth-transfer SIGN claim — the SIGN (the pre-money pool transfers ownership from founders to the investor; the extra give-up is q·t) survives, but the exact q·t figure is a property of the single-round post-money-pool accounting; multi-round refresh dynamics and anti-dilution terms are disclosed descriptive fields, not hidden defects.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD. State at this commit: in-progress — reproduction NOT yet run, no gate asserted, all outcomes/digests/z-scores below are PENDING/TBD placeholders to be filled by the reproduction pass.

## Objective
Reproduce the committed P162 reference verifier byte-identical under its pinned world (SEED=20260717; world constants — investment I, pre-money P, and pool fraction q sweep — to be read from the P162 source when it is pinned; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest (**PENDING — digest TBD, to be read from the P162 proposal doc and reproduced**), (b) all three gates hold in order G1 investor-fraction-invariant → G2 extra-transfer-equals-qt → G3 robust-under-shift, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (a PRE-money option pool of size q is carved entirely from founders; the investor's post-money fraction t = I/(P+I) is invariant to q; founders hand the investor an extra q·t versus a pro-rata pool). Pre-registered decision rule (P162): APPROVE VERDICT 175 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the exact q·t transfer being a property of the single-round post-money-pool accounting (multi-round refresh / anti-dilution as disclosed descriptive fields) is not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 175, +13 — PRIMARY); idea-engine mirror separate. **STATUS: not yet executed — the Objective is the plan of record, not a claim of completion.**

## GROUNDING (to verify at HEAD — PENDING)
- Reference verifier: the committed P162 source in its fleet/venture lane @ idea-engine — git blob **PENDING (TBD)**, file sha256 **PENDING (TBD)**, byte count / line count **PENDING (TBD)**. To be copied byte-identically (`diff` exit 0, copy sha256 == source, copy git blob == source blob) into the reproduction commit at `sims/verdict-175-option-pool-shuffle/` — copy identity **PENDING (TBD)**.
- Offset authority: +13 (P162 → V175), round-38 VENTURE slot (venture-financing / cap-table accounting). Per idea-engine `control/outbox.md` P162 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror) — **to confirm at HEAD (PENDING)**.
- Domain reference: the option-pool shuffle — the standard priced-round construction where the option pool is created inside the pre-money valuation, diluting only the founders while the investor's post-money fraction t = I/(P+I) is untouched; a pro-rata (post-money) pool would instead split the pool q between founders and investor in their post-money proportions, so the pre-money pool transfers an extra q·t to the investor. Grounding: https://venturehacks.com/option-pool-shuffle — **to resolve LIVE this session (PENDING)**; the closed forms to be hand-checked against the reproduced numbers in the Outcome below once the run lands.
- Disclosed digest: compact-canonical results-dict sha256 **PENDING (TBD)**, SEED=20260717 — to be reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (to confirm): expected **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the recent carry-forward (V171–V174), pending confirmation against the P162 proposal doc's disclosed posture. `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent run and asserts the two dicts are equal, prints a pretty indent=2 dump plus the sha256, writes NO JSON to disk; every float rounded to 6 dp before serialization. **To confirm byte-exact in the reproduction commit (PENDING).**

## Constraints honored (planned)
Stdlib-only (math, json, hashlib, random); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict to be reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The investor's post-money fraction and the founders' extra give-up are to be measured from the drawn/computed cap table across the pool-size sweep, not plugged from the closed form — so the invariance and the q·t transfer are attributable to the reproduced accounting, not a tautological substitution. The gates are definitional contrasts on those computed statistics: G1 investor-fraction-invariant (t = I/(P+I) vs the pool-size sweep), G2 extra-transfer-equals-qt (pre-money-pool investor stake minus pro-rata-pool investor stake vs q·t), and G3 robust-under-shift (both survive a shifted mega-round I/P distribution). Honest scope: the head is EXACT GIVEN THE MODEL and a wealth-transfer SIGN claim — it holds for the single priced round with the pool defined as a fraction of the post-money cap; multi-round refresh dynamics and anti-dilution terms are disclosed descriptive fields, and no gate reads the q·t transfer as a multi-round-universal constant. **None of the above is yet executed — this is the constraint contract the reproduction must honor.**

## Gate plan (to reproduce at HEAD), order G1 → G2 → G3
- **G1 investor-fraction-invariant (z ≥ 3, one-sided vs the relerr ceiling 0.05):** z = (CEIL − invar_relerr_mean)/se across the pool-size sweep, where invar_relerr = |measured investor post-money fraction − t| / t with t = I/(P+I) — the investor's post-money percentage does not move as the pool size q is swept (the pre-money pool is invisible to the investor's fraction). Reproduced invar_relerr_mean **PENDING (TBD)**, z **PENDING (TBD)** (ceiling 0.05) — **PENDING (not yet run).**
- **G2 extra-transfer-equals-qt (z ≥ 3 vs the relerr ceiling 0.05):** z = (CEIL − transfer_relerr_mean)/se, where transfer_relerr = |(investor stake under the pre-money pool − investor stake under the pro-rata pool) − q·t| / (q·t) — the founders' extra give-up equals q·t. Reproduced transfer_relerr_mean **PENDING (TBD)**, z **PENDING (TBD)** (ceiling 0.05) — **PENDING (not yet run).**
- **G3 robust-under-shift (z ≥ 3 vs the relerr ceiling 0.05):** z = (CEIL − shift_relerr_mean)/se, where the invariance (G1) and the q·t transfer (G2) are re-checked under a SHIFTED mega-round distribution of I/P (larger rounds) — the result is robust to the round-size regime. Reproduced shift_relerr_mean **PENDING (TBD)**, z **PENDING (TBD)** (ceiling 0.05) — **PENDING (not yet run).**
- z_gate **3.0** (require z ≥ 3 on every gate). **Disclosed caveat (NOT a gate):** the exact q·t transfer is a property of the single-round post-money-pool accounting; multi-round refresh dynamics and anti-dilution terms would layer additional transfers. The SIGN (the pre-money pool transfers ownership from founders to the investor) and the order-of-magnitude survive, but the exact q·t figure is a disclosed single-round descriptive field, and NO gate reads it as a multi-round-universal constant. **All gate results above are PENDING/TBD — no pass is asserted at this commit.**

## Probe questions (independent-audit checklist)
**1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest.** — **PENDING.** Digest TBD — to be read from the P162 proposal doc, printed by the script (`json.dumps(res, sort_keys=True, separators=(",",":"))` over the 6-dp dict, then sha256), independently recomputed from the stdout JSON block, and checked for a MATCH across two fresh `python3` invocations. Not yet run.

**2. Is the verifier byte-identical to the P162 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.** — **PENDING.** Source identity (git blob / file sha256 / byte + line count) TBD — to be pinned from `ideas/…` @ idea-engine and copied byte-identical (`diff` exit 0) into the reproduction commit. Not yet run.

**3. Do all three gates PASS in the pre-registered order G1 investor-fraction-invariant → G2 extra-transfer-equals-qt → G3 robust-under-shift with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0?** — **PENDING.** G1 invar_relerr_mean TBD, G2 transfer_relerr_mean TBD, G3 shift_relerr_mean TBD; `gates`, `all_pass`, `first_failing_gate`, and exit code all TBD. Not yet run.

**4. Is the headline read correctly — a PRE-money option pool of size q is carved entirely from the founders, the investor's post-money fraction t = I/(P+I) is INVARIANT to q, and founders hand the investor an extra q·t versus a pro-rata pool — and NOT as "the pool is a neutral shared cost both parties fund in proportion"?** — **PENDING.** To confirm: measured investor fraction stays at t across the q sweep (G1), the pre-money-vs-pro-rata investor-stake gap equals q·t (G2), both survive the mega-round shift (G3). Not yet run.

**5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact q·t transfer being a property of the single-round post-money-pool accounting (multi-round refresh / anti-dilution as layered descriptive fields) flips no gate SIGN and no order-of-magnitude?** — **PENDING.** To weigh once the run lands: the wealth-transfer SIGN (founders → investor via the pre-money pool) and the order-of-magnitude must survive; the exact q·t is a disclosed single-round field, not a hidden defect. Not yet run.

**6. Could the transfer sign be a seed fluke? State whether the effect is structural (the invariance and the q·t transfer hold across the pool-size sweep and the shifted mega-round distribution) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical).** — **PENDING.** To confirm structural across the q sweep (G1/G2) and the mega-round shift (G3), with determinism verified both ways (in-process double-run assert + two byte-identical `python3` invocations, exit 0). Not yet run.

## Outcome
**PENDING — reproduction not yet executed. No verdict asserted at this commit.**

- **Digest.** compact-canonical results-dict sha256 **TBD** — to be reproduced and independently recomputed from stdout (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY expected; every float rounded to 6 dp), checked byte-identical across two fresh `python3` invocations and the in-process double-run assert. Verifier byte-identity to the P162 source **TBD**.
- **Three gates** (z_gate=3.0): G1 investor-fraction-invariant invar_relerr_mean **TBD**, z **TBD** → G2 extra-transfer-equals-qt transfer_relerr_mean **TBD**, z **TBD** → G3 robust-under-shift shift_relerr_mean **TBD**, z **TBD**. `gates`, `all_pass`, `first_failing_gate`, exit code all **TBD**.
- **Closed-form hand-check (to perform).** For a single priced round with investment I at pre-money P (post-money P+I) and an option pool of fraction q of the post-money cap created PRE-money: the investor's post-money fraction is t = I/(P+I) independent of q; a pro-rata pool would leave the investor with t − q·t (the investor's share of the pool), so the pre-money pool hands the investor an extra q·t. The measured invariance and the q·t transfer are to be hand-checked against the reproduced numbers once the run lands.
- **Model-basis note.** The exact q·t transfer is a single-round post-money-pool property; multi-round refresh and anti-dilution terms are disclosed descriptive fields that flip no gate sign or order-of-magnitude. To confirm at APPROVE time.

**Verdict: PENDING (born-red; to be flipped to APPROVE/REJECT as the deliberate final commit once the reproduction is run and audited).**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-174-palm-mginf-insensitivity.md` (V174, P161 Palm / M/G/∞ repair-pipeline insensitivity, round-38 FLEET slot, +13) landed a CLEAN APPROVE, the current verdict high-water: disclosed compact-canonical results-dict digest `9bbe2171…13ef` reproduced EXACTLY, verifier byte-identical (git blob `da7466ec6c75c22993344dc1caf983babee2cccf`, file sha256 `9a8ba388…3c21`, 8479 bytes / 233 lines), all three gates G1 pipeline-is-load → G2 variance-insensitivity → G3 distribution-free PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form (M/G/∞ pipeline Poisson(λ·E[S]), dispersion 1.0 insensitive to repair-time shape) confirmed — the one disclosed field (the exact insensitivity being a property of the ample-server pipeline; M/G/1 recovers the (1+CV²)/2 wait tax) touched no gate sign. DIGEST-POSTURE carry-forward (to confirm unbroken): V174 was WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — this V175 is EXPECTED to carry the same posture, pending confirmation against the P162 proposal doc. Object shift: V174 was the round-38 FLEET-slot Palm / M/G/∞ repair-pipeline insensitivity head (a variance-insensitivity SIGN claim on the ample-server pipeline); this V175 is the round-38 VENTURE-slot option-pool-shuffle head — a wealth-transfer SIGN / order-of-magnitude claim (the pre-money pool transfers an extra q·t from founders to the investor) whose exact figure is a self-disclosed single-round descriptive field (multi-round refresh / anti-dilution layer additional transfers). CONTIGUITY: this card is BORN-RED / in-progress — it does NOT yet advance the verdict high-water (still V174); union-max advances to **V175 only after** this reproduction lands a clean APPROVE as the deliberate final commit. Items open/backfilled BELOW the high-water are NOT closed by this card; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V175 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the SINGLE priced round with the pool as a fraction of the post-money cap — the case where the option-pool shuffle's q·t transfer is EXACT and the investor's fraction is a clean t = I/(P+I). The distinct next slice the head names but holds fixed — the MULTI-round frontier: option pools are REFRESHED at each subsequent round, and each refresh re-runs the shuffle on a cap table already diluted by prior rounds, so the founders' cumulative give-up compounds across rounds rather than staying a single q·t. Pre-register (same SEED-pinned, computed-cap-table discipline): sweep the number of rounds and gate (G-a) the single-round base reproduces the q·t transfer at ≥3σ, (G-b) a compounding headline — the cumulative founder give-up across R refreshed rounds EXCEEDS the naive sum of per-round q·t by the cross-round dilution interaction at ≥3σ, and (G-c) an anti-dilution crossover — a full-ratchet vs broad-based weighted-average anti-dilution term shifts WHO absorbs a down-round refresh at ≥3σ. The operator lesson sharpens from "the pre-money pool costs founders q·t in this round" to "the shuffle compounds across every refresh, and the anti-dilution regime decides who eats a down-round pool — so the single-round q·t is a floor, not the whole bill" — distinct from this V175 head, which pins the single priced round and asks only whether the pre-money pool transfers q·t.

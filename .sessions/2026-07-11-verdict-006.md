# Session — VERDICT 006 · SIM-001 — idle-economy sim kernel (economy-v1)

> **Status:** `in-progress`
> 📊 Model: Claude Opus 4 · 2026-07-11 · VERDICT 006 session
> Objective: settle SIM-001 (idea-engine PROPOSAL 006 @ 7df1d6cd, relaying superbot-idle
> economy-v1 @ f11c71a5) — drive the REAL pinned `idle_engine/` package (byte-copied,
> unmodified) over the economy-v1 reference world and score acceptance criteria A1–A10 for
> the PROVISIONAL parameter table (graduate PROVISIONAL→SIM-PINNED iff all pass).

## What happened

Born-red card, first commit of the VERDICT 006 session (heartbeat-before-work,
CONVENTIONS §"Heartbeat-before-work"; born-red per §"Born-red session card"). As a
newly-ADDED card it is the substrate-gate's advisory sentinel; the READY PR is expected to
pass green once this card is flipped to `complete` as the deliberate close-out. This card's
family-level `📊 Model:` line also ACKs fleet-manager ORDER 001 (model-attribution ground
truth).

Ships `sims/verdict-006-idle-economy-sim-kernel/` — a NUMERIC SIMULATION (ladder rung 1):
deterministic, integer-exact, stdlib-only, no RNG (a single run per scenario IS the
distribution; determinism proven by byte-identical re-run, not by seeds). The driver
VENDOR-COPIES the pinned superbot-idle `idle_engine/` package UNMODIFIED (5 engine modules
byte-for-byte + a reconstructed `__init__.py` that re-exports only the vendored modules, no
theme/render) and calls the REAL engine functions — `tick`, `offline_progress`,
`purchase_upgrade`, `upgrade_cost`, `prestige_eligible`, `prestige_award`, `apply_prestige`,
`production_per_second`, `build_upgrade_spec`, `build_prestige_spec` — so no float drift can
invalidate the run.

**Load-bearing result:** `[COORDINATOR TO FINALIZE]` — the A1–A10 scorecard and the
PROVISIONAL→SIM-PINNED graduation decision are recorded in `REPORT.md`; the coordinator
finalizes VERDICT 006 and the heartbeat after reviewing the numbers.

## Run command

```
python3 sims/verdict-006-idle-economy-sim-kernel/idle_economy_sim.py
```
Deterministic, stdlib-only, exit 0 iff every self-check passes. Prints O1–O6 tables, the
A1–A10 scorecard, and the terminal `SELF-CHECKS: n passed, 0 failed` line; each scenario is
run twice and asserted byte-identical (`determinism_bytes`).

## ⟲ Previous-session review

VERDICT 005 (measured prototype) redefined reproducibility as agreement-rate over a frozen
live layer because the live capability plane is not seed-deterministic. VERDICT 006 sits one
rung cheaper: the engine IS pure and integer-exact, so reproducibility is bit-identity of a
single deterministic run — the strongest form on the ladder — and the discipline carried
forward is the same: drive the REAL artifact, never a re-implementation, and score
pre-registered acceptance bands honestly (a FAIL is a headline, not a footnote).

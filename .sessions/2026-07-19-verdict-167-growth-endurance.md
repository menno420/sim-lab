# VERDICT 167 — growth-endurance dominance: for a growth-stage company a strategy that COMPOUNDS the top line at growth rate g0 strictly DOMINATES a strategy that maximises ENDURANCE/retention (survival rate r) on terminal enterprise value over a HORIZON=12 horizon — the "durable, slow-and-steady" book is NOT the safe default it looks like — but only ABOVE the crossover g0 > 1−r; for LOW-GROWTH companies (g0 < 1−r) the ordering CROSSES OVER and endurance wins, so the dominance is a growth-stage claim, not a universal one

Reproduce PROPOSAL 154 (round-36 VENTURE slot, P154 → V167, +13): growth-endurance dominance. Each of N=1500 companies is evaluated under TWO strategies on the SAME per-company draw (paired) — Strategy GROWTH (reinvest to compound the top line at growth rate g0 each period) vs Strategy ENDURANCE (spend the same budget to raise the per-period survival/retention rate r, trading compounding for durability) — compounded forward over HORIZON=12 periods and scored on terminal enterprise value. The folk belief: "durability beats growth — a company that maximises its endurance/retention is the conservative, dominant book; chasing growth is the risky bet, so the slow-and-steady strategy is the safe default that at worst ties." It is NOT: for a GROWTH-STAGE company — one whose initial growth rate clears the crossover g0 > 1−r — the compounding GROWTH strategy strictly dominates the endurance book on terminal value across essentially the whole paired sample, because over a 12-period horizon multiplicative growth outruns the additive durability the endurance budget buys; the endurance strategy only wins for LOW-GROWTH companies below the crossover (g0 < 1−r), where there is too little growth to compound and durability is the binding constraint. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 dominance-sign (the paired terminal-value gap GROWTH−ENDURANCE is positive — growth beats endurance) → G2 relative-effect (the relative advantage exceeds a 0.10 null — the effect is not a rounding-scale artifact) → G3 shifted-distribution robustness (the sign survives a re-draw of the company distribution, the case where reversal is most plausible). Model-basis caveat (P024 discipline): the head is a GROWTH-STAGE dominance claim, not universal — BELOW the crossover g0 > 1−r the endurance strategy wins (there is too little growth to compound), and that low-growth crossover band is a disclosed descriptive field, not a hidden defect. The declared choices — the fixed HORIZON=12, the DELTA=0.05 budget increment, the paired same-company draw — change neither the SIGN nor the order-of-magnitude of the growth-stage dominance.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P154 reference verifier byte-identical under its pinned world (SEED=20260717, N=1500 companies, TRIALS=300 Monte-Carlo trials, HORIZON=12 compounding periods, DELTA=0.05 budget increment, REL_NULL=0.10 relative-effect null, the paired GROWTH-vs-ENDURANCE contrast evaluated on the SAME per-company draw, one `random.Random(SEED)` stream drawn in fixed order) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `5dccb475a6fde0ebd5c557b3baa393be8430b91902bbadedb7f5bd16754495bf`, (b) all three gates hold in order G1 dominance-sign → G2 relative-effect → G3 shifted-distribution robustness, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the compounding GROWTH strategy dominates the ENDURANCE strategy on terminal enterprise value for growth-stage companies g0 > 1−r; disclosed G1 gap_mean=+0.354025, G2 rel_mean=+2.219712, G3 gap_mean=+0.322362). Pre-registered decision rule (P154): APPROVE VERDICT 167 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the low-growth crossover band (companies below g0 > 1−r, where endurance wins) is a DISCLOSED descriptive field of a GROWTH-STAGE dominance claim, not a hidden defect, so if none flips a gate SIGN or the order-of-magnitude of the growth-stage dominance, a clean APPROVE is warranted. Target: sim-lab (VERDICT 167, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: the committed P154 source in the VENTURE lane (expected path `ideas/venture/growth_endurance_dominance.py`). **File identity (sha256 / git blob / byte count / line count / pinned idea-engine permalink) is to be pinned against the P154 source in the reproduction commit — NOT asserted in this born-red FIRST commit, which runs no verifier by design.**
- Offset authority: PROPOSAL 154 → VERDICT 167 (+13), round-36 VENTURE slot (successor to the round-35 VENTURE slot P150 → V163). Per idea-engine `control/outbox.md` P154 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: venture strategy / terminal-value economics — growth vs endurance. Over a fixed HORIZON=12 horizon a GROWTH strategy compounds the top line multiplicatively at rate g0, while an ENDURANCE strategy spends the same budget to raise the per-period survival/retention rate r (additive durability). The crossover is analytic: growth dominates iff g0 > 1−r (multiplicative compounding outruns additive durability), and inverts below it for low-growth companies. The folk "durability is the safe default" belief is the growth-stage inversion this head targets.
- Disclosed digest: compact-canonical results-dict sha256 `5dccb475a6fde0ebd5c557b3baa393be8430b91902bbadedb7f5bd16754495bf` (P154 outbox done-when line).
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the round-35 carry-forward. `run()`/results-builder returns a dict carrying NO `results_sha256` field; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>`, and writes NO JSON to disk; every float rounded to 6 dp before serialization. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump (a distinct serialization of the same dict) — recompute over the compact form. Posture to be confirmed byte-exact in the reproduction commit.
- Pinned world: SEED=20260717; N=1500 companies; TRIALS=300 Monte-Carlo trials; HORIZON=12 compounding periods; DELTA=0.05 budget increment; REL_NULL=0.10 relative-effect null; the paired GROWTH-vs-ENDURANCE contrast on the SAME per-company draw; one `random.Random(SEED)` stream drawn in fixed order.

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The GROWTH and ENDURANCE strategies are evaluated on the SAME per-company draw per trial (paired — one `random.Random(SEED)` stream), so the terminal-value contrast is attributable to the STRATEGY (compound-growth vs raise-endurance) alone, not to any difference in the company sampled. The gates are definitional contrasts on those shared draws: G1 dominance-sign and G2 relative-effect at the pinned company distribution, G3 re-draws the company distribution (the case where reversal is most plausible) as a robustness control. Honest scope: the head is a GROWTH-STAGE dominance claim — it holds for companies above the crossover g0 > 1−r and CROSSES OVER for low-growth companies (g0 < 1−r) where endurance wins; that low-growth band is a disclosed descriptive field, and no gate reads it as universal.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 dominance-sign (z ≥ 3, one-sided):** z = mean(terminal_GROWTH − terminal_ENDURANCE)/se — the paired terminal-value gap is positive (the compounding growth strategy dominates the endurance strategy). Disclosed gap_mean **+0.354025**, z **+996.770037**, frac_dominant **0.992229** (growth dominates on ~99.2% of the paired draws; the ~0.78% remainder are the low-growth crossover companies where endurance wins).
- **G2 relative-effect (z ≥ 3, one-sided):** z = (rel_advantage − 0.10)/se vs a 0.10 null — the relative terminal-value advantage exceeds the 0.10 magnitude floor (the effect is not a rounding-scale artifact). Disclosed rel_mean **+2.219712**, z **+925.513190** (vs the 0.10 REL_NULL).
- **G3 shifted-distribution robustness (z ≥ 3, one-sided):** re-draw the company distribution and re-run the sign contrast — the growth-dominates sign survives the distribution shift (the case where reversal is most plausible). Disclosed gap_mean **+0.322362**, z **+1107.766114**, frac_dominant **0.997938**.
- z_gate **3.0** (require z ≥ 3 on every gate, one-sided). **Disclosed caveat (NOT a gate):** below the crossover g0 > 1−r the ENDURANCE strategy wins (low-growth companies have too little growth to compound, so durability is the binding constraint) — the ~0.78% of the primary sample not dominated (frac_dominant 0.992229) are exactly this low-growth band, a disclosed descriptive field of a growth-stage dominance claim, and NO gate reads the low-growth regime as universal.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `5dccb475…495bf` EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (not the pretty indent=2 dump — the P127+ STDOUT-ONLY twist)? State the reproduced digest.
**2.** Is the verifier byte-identical to the P154 source at its pinned idea-engine commit (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.
**3.** Do all three gates PASS in the pre-registered order G1 → G2 → G3 with the disclosed statistics (G1 gap_mean=+0.354025 z=+996.770037 frac_dominant=0.992229 / G2 rel_mean=+2.219712 z=+925.513190 vs 0.10 null / G3 gap_mean=+0.322362 z=+1107.766114 frac_dominant=0.997938, all ≥ 3σ one-sided, z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0?
**4.** Is the headline read correctly — the compounding GROWTH strategy DOMINATES the ENDURANCE strategy on terminal enterprise value for GROWTH-STAGE companies (g0 > 1−r) over HORIZON=12, produced by the STRATEGY (compound-growth vs raise-endurance) on the SAME paired per-company draws — and NOT as "durability is always the safe default"? Growth dominates only ABOVE the crossover; the claim is a growth-stage dominance claim, and endurance wins for low-growth companies.
**5.** Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect)? The low-growth crossover band (companies below g0 > 1−r, where endurance wins — the ~0.78% of the primary sample not dominated) is a DISCLOSED descriptive field of a growth-stage dominance head, and the declared choices — the fixed HORIZON=12, the DELTA=0.05 budget increment, the paired same-company draw — none flips a gate SIGN or the order-of-magnitude of the growth-stage dominance.
**6.** Could the sign be a seed fluke? The paired-draw contrast clears G1 at z ≈ 997σ with frac_dominant=0.992229 at the primary distribution, G2 at z ≈ 926σ vs the 0.10 null, and the sign survives the shifted-distribution re-draw (G3 z ≈ 1108σ, frac_dominant 0.997938) — not a fluke.

## Outcome — pending reproduction
_(Born-red placeholder: this card lands `in-progress` in the FIRST commit. The reproduction — byte-identical verifier copy, digest recomputation, and the three-gate double-run — is performed in the NEXT commit and its raw numbers recorded in `sims/verdict-167-growth-endurance/probe-report.md`. This session performs the mechanical born-red card only; the APPROVE/REJECT verdict and the deliberate LAST commit that flips this card to `complete` are the orchestrator's call, not written here. No Python was run and no `sims/` artifacts were created in this born-red seat.)_

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-165-braess-paradox.md` landed V165 (P152 Braess's paradox, round-35 unrelated FLEET slot, +13) as a **CLEAN APPROVE**: exact digest `d0e4e57d…05ad0` reproduced across a fresh cross-invocation double-run, the script's in-process assert, and the byte-identical copy; all three gates PASS in order (G1 sign z=+572.9 frac_worse=1.0, G2 magnitude z=+347.9 vs the 0.10 null, G3 shifted-demand robustness z=+236.1 frac_worse=0.95125); the ~15-minute everyone-slower gap rested on the TOPOLOGY (a zero-cost shortcut edge) over the SAME paired demand draws, and its one reversal (sub-threshold demand below 30·CAP=3000) was self-disclosed AND actively probed by a gate. DIGEST-POSTURE carry-forward: V165 and this V167 are BOTH expected WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY with the P127+ twist (results dict carries no `results_sha256`, no file written, the compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form). Object shift: V165 was a FLEET-slot network-routing EQUILIBRIUM claim whose reversal was a demand threshold; this V167 is the round-36 VENTURE-slot head — a growth-vs-endurance terminal-value dominance claim whose ONE reversal (the low-growth crossover g0 > 1−r, where endurance wins) is likewise self-disclosed and descriptive, so it earns the same clean-APPROVE posture if the reproduction holds. CONTIGUITY note for the successor: V167 (P154, round-36 VENTURE slot) advances the verdict high-water to **V167** (union-max, no regress); items open/backfilled below the high-water are not closed by landing V167. Coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V167 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins ONE budget increment (DELTA=0.05), ONE horizon (HORIZON=12), and reports the crossover as the single analytic curve g0 > 1−r along which growth overtakes endurance. A genuinely distinct next VENTURE-slot slice — crossover migration + horizon control — pins the parameters the head flags but holds fixed: the crossover is not a constant, it MOVES with the horizon and the budget an operator actually controls. Pre-register (SEED-pinned, same paired-draw discipline): sweep the horizon H ∈ {6, 12, 24, 48} and the budget increment DELTA ∈ {0.025, 0.05, 0.10}, and gate (G-a) the growth-dominates SIGN SURVIVES at ≥ 3σ at EVERY (H, DELTA) grid point where g0 clears that point's crossover (the dominance is robust to horizon and budget above the crossover); (G-b) a crossover-MIGRATION headline — the crossover growth rate g0*(H, DELTA) shifts monotonically (a longer horizon PUSHES the crossover DOWN, letting compounding win for ever-lower-growth companies, while a larger endurance budget PUSHES it UP), with a closed-form g0*(H, DELTA) reference to check the swept solver against; (G-c) a break-even-horizon control — for a fixed low-growth company below the g0*(12) crossover there exists a horizon H* at which growth overtakes endurance, recoverable at ≥ 3σ (the "endurance wins" verdict for a low-growth company is a HORIZON verdict, not a permanent one — wait long enough and compounding still wins). The operator lesson sharpens from "growth beats endurance for growth-stage companies" to "that crossover is a negotiated/engineered number — horizon and budget move it, and even a low-growth company crosses over given enough runway." Distinct from this V167 head (which pins one horizon and one budget and asks only whether growth dominates above a fixed crossover) and gives an exact analytic reference (g0*(H, DELTA) closed-form at every grid point) to audit the swept simulator against.

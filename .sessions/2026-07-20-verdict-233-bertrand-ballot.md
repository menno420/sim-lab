# VERDICT 233 — Bertrand ballot theorem / lead-throughout: when A finishes with a votes and B with b<a and the ballots are opened in uniformly random order, the probability A is STRICTLY ahead of B at EVERY prefix of the count is exactly (a−b)/(a+b) — the winning-margin ratio alone, NOT A's vote share a/(a+b) — and it is scale-invariant — reproduce PROPOSAL 220

> **Status:** `complete`

- **📊 Model:** opus-4.8 · high · review/verify

started: 2026-07-20T17:00:00Z

💓 Heartbeat: round-52 UNRELATED slice P220 → V233 (+13); reproduction on branch `claude/verdict-233-bertrand-ballot`; sim dir `sims/verdict-233-bertrand-ballot/` (verifier + run-stdout.txt + probe-report.md staged by proposal PR #312; this slice adds the verdict session card). Digest target full-64 `d52f08b9…78aa` (printed AND literal-string compared, no truncation). Determinism CONFIRMED (in-process double-run guard AND separate cross-invocation byte-identical). Four pre-registered gates each in its own direction — G1 EXACT identity Fraction(brute,C(a+b,a))==Fraction(a−b,a+b) over {(3,1),(4,2),(5,2),(5,3),(6,3),(7,2)}, G2 EXACT scale-invariance k∈{1,2,3,5} ((10,5)→1001/3003=1/3), G3 Monte-Carlo AGREEMENT (|z|≤3, z=1.01965/−1.2489/−0.392605) over ~200000 shuffles, G4 FALSIFIABILITY naive a/(a+b) REJECTED at |z|>5 (z=−222.587729/−259.552892/−274.303069) plus an exact corrupt-count leg a/(a+b)·C(a+b,a)!=brute. Grounding sha1-pinned to Bertrand's ballot theorem oldid 1361316938 (45e36778…). Born-red HOLD armed on this first card commit; released only by the deliberate `complete` flip LAST. VERDICT 233 = APPROVE.

⏳ Flip note (born-red): this card ships `> **Status:** `in-progress`` on its FIRST commit so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only after the verifier reproduction (byte-identical, full-64 digest match, four-gate evaluation each in its own direction, determinism, grounding — captured in the already-staged probe-report.md) is confirmed. That flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation merge the green PR.

## What this verdict does

Reproduces PROPOSAL 220 (P220 → V233, +13 offset, lane UNRELATED of the round-52 fleet→venture→game→unrelated cycle, closing round-52): when candidate A finishes with a votes and B with b<a votes and the ballots are opened in uniformly random order, the probability that A is **STRICTLY ahead of B at EVERY prefix of the count is exactly (a−b)/(a+b)** — the winning-margin ratio alone, NOT A's vote share a/(a+b) — and it is **scale-invariant** (unchanged when both tallies scale by k). The verifier `bertrand-ballot-lead-throughout.py` (byte-identical copy already in `sims/verdict-233-bertrand-ballot/`) reproduces the results-dict sha256, confirms determinism, evaluates four pre-registered gates each in its own direction, and the probe checks the byte-pinned grounding against the pinned Wikipedia revision.

## Method

- Byte-identical verifier copy (`diff` source↔copy exit 0), stdlib-only (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`).
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the digest (`json.dumps(r, sort_keys=True, separators=(",",":"))` → `hashlib.sha256`); the dict carries no hash of itself; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target full-64 `d52f08b930d92b01ee8d0f81089974843262710f301dd738594602bf3d4378aa` (literal 64-char compare, no truncation); SEED=20260717.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria) — see probe-report.md for observed values.
- Grounding byte-pinned to the `action=raw` wikitext of the revision; sha1 compared to the MediaWiki-reported rev sha1.

## ⟲ Previous-session review

Previous-session review: VERDICT 232 (Nim / Sprague–Grundy nim-sum — a disjunctive-sum position is a loss for the player to move iff the XOR of its component Grundy values is zero, and Sub({1..k}) has the closed form G(n)=n mod (k+1); P219 → V232) landed APPROVE with a full-64 digest match (`e50e461d…9002b7`) and six gates PASS via the born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction/integer identity is a self-certifying theorem, a z-test is an AGREEMENT gate when the closed form is the null, and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate read at the opposite polarity. V233 leans on that same discipline: G1/G2 are exact Fraction identities (the firsthand teeth — the winning-margin ratio and scale-invariance), G3 is a Monte-Carlo AGREEMENT gate (|z|<3), and G4 is a FALSIFIABILITY gate that rejects the naive a/(a+b) impostor at |z|>5 (opposite direction to G3) plus an exact corrupt-count leg. This verdict also carries the V222 grounding-accuracy assessment: the caveat honestly separates "firsthand-proven / off-page" (the sim, SEED, gate grids, digest) from "cited on-page" (the (p−q)/(p+q) statement and the binom count), and flags that a/(a+b) appears on-page only as P(first/last vote is A) in the induction — exactly the G4 impostor the page itself distinguishes. Standing non-contiguity persists: landing V233 does not imply every lower verdict below the high-water is closed.

## 💡 Session idea

The verifier pins the strict-lead probability (a−b)/(a+b) and its scale-invariance. A cheap orthogonal follow-on (P-next) would pin the **tie-allowed (weak-lead) variant** — the probability that A stays ≥ B (ties permitted) throughout the count, which the cycle lemma gives as (a−b+1)/(a+1) for a≥b. Gate: an exact Fraction identity Fraction(weak-favorable, C(a+b,a)) == Fraction(a−b+1, a+1) over a disclosed pair grid, plus a FALSIFIABILITY leg that the strict-lead formula (a−b)/(a+b) is REJECTED as the weak-lead answer on at least one pair (the two theorems must be distinguished, not conflated), plus a continuity leg that the strict-lead verifier of THIS verdict still reproduces (a−b)/(a+b). Guard recipe for a later session: the anchor is the brute prefix-scan `leads_throughout(seq)` — add a `weak_leads_throughout(seq)` reducer that accepts equality, target `Fraction(weak_favorable, C(a+b,a)) == Fraction(a−b+1, a+1)`, and pin one non-trivial firsthand pair. Grounding stays honestly on-page for the statement (the same page's "related results" section) and off-page for the sim.

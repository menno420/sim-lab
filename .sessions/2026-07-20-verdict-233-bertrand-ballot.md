# PROPOSAL 220 — Bertrand's ballot theorem: in a count where A finishes with a votes and B with b < a votes opened in uniformly random order, the probability that A is STRICTLY ahead of B at EVERY prefix of the count is EXACTLY (a−b)/(a+b) — it rides only on the winning-margin ratio, NOT on A's vote share a/(a+b) — stage the SEED=20260717 stdlib verifier so VERDICT 233 can rule on it

> **Status:** `in-progress`

📊 Model: Claude Opus family · high · verification/staging

started: 2026-07-20T00:00:00Z

💓 Heartbeat: round-52 UNRELATED slot (fleet→venture→game→unrelated) P220 → V233 (+13 offset); verifier staged on branch `claude/proposal-220-bertrand-ballot`; sim dir `sims/verdict-233-bertrand-ballot/` (stdlib verifier `bertrand-ballot-lead-throughout.py` + run-stdout.txt + probe-report.md). Disclosed results-dict full-64 sha256 `d52f08b930d92b01ee8d0f81089974843262710f301dd738594602bf3d4378aa` (printed on the verifier's `results_sha256=` line, exact 64-char compare, no truncation). Determinism CONFIRMED (in-process `determinism_double_run=True` AND separate cross-invocation byte-identical stdout). All 4 pre-registered gates PASS each in its OWN direction — G1 EXACT identity (Fraction) brute/C(a+b,a) == (a−b)/(a+b) over six (a,b); G2 EXACT scale-invariance (a−b)/(a+b) == (ka−kb)/(ka+kb) for k∈{2,3,5} with the brute count identity holding on each enumerable scaled case; G3 Monte-Carlo agreement |z|≤3 vs (a−b)/(a+b); G4 FALSIFIABILITY the naive a/(a+b) share rejected at |z|>5 on the SAME sample plus an exact corrupt-count leg. Born-red HOLD armed on this first card commit; released only by the deliberate `complete` flip LAST. VERDICT 233 ruling pending.

⏳ Flip note (born-red): this card ships `> **Status:** `in-progress`` on its FIRST commit so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only after the sim dir (stdlib verifier + captured reproduction stdout + probe-report), the digest print (full-64 exact `d52f08b930d92b01ee8d0f81089974843262710f301dd738594602bf3d4378aa`), the four-gate evaluation each in its own direction (all PASS), the determinism check (in-process double-run held AND separate-invocation stdout byte-identical), and the byte-pinned grounding have ALL landed. That flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation merge the green PR.

## What this verdict does

Stage the SEED=20260717 stdlib verifier for PROPOSAL 220 (Bertrand's ballot theorem — the lead-throughout probability (a−b)/(a+b)) in sim-lab so VERDICT 233 (+13 offset, round-52 UNRELATED slot) can rule on it. The counterintuitive twist the proposal pins: the probability that A never once falls level with or behind B during the whole count depends ONLY on the margin ratio (a−b)/(a+b), not on how large A's overall share a/(a+b) is — a landslide winner counted in random order still has a modest chance of leading wire-to-wire, and that chance is fixed by the margin alone. All 4 gates PASS deterministically (in-process double-run + separate re-invocation byte-identical). Disclosed results-dict sha256 = d52f08b930d92b01ee8d0f81089974843262710f301dd738594602bf3d4378aa.

## Method

- Stdlib-only verifier (`hashlib`, `itertools`, `json`, `math`, `random`, `fractions.Fraction`). SEED = 20260717; ALL randomness from a single `random.Random(SEED)` consumed in a fixed, documented order — for each (a,b) in MC_PAIRS (in listed order) MC_T=200000 ballot shuffles are drawn back-to-back before the next pair, so G3 (agreement) and G4 (rejection) read the SAME samples.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the digest (`json.dumps(r, sort_keys=True, separators=(",",":"))` → `hashlib.sha256`); the dict carries no hash of itself; `__main__` builds the results twice in-process and asserts byte-identical canonical JSON (`determinism_double_run`) before printing `results_sha256=`, `determinism_double_run=`, `sim_ready=` and `sys.exit(0 if sim_ready and determinism else 1)`. Target `d52f08b930d92b01ee8d0f81089974843262710f301dd738594602bf3d4378aa` (full-64 exact).
- Core exact fact encoded: the number of countings in which A leads throughout equals (a−b)/(a+b)·C(a+b,a); the brute favorable count is computed by FULL enumeration over the C(a+b,a) placements of the A-ballots (`itertools.combinations`), and the closed-form probability is `Fraction(a−b, a+b)`. No floats in the exact gates.
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 — EXACT identity** (direction: exact Fraction equality): for six (a,b) ∈ {(3,1),(4,2),(5,2),(5,3),(6,3),(7,2)}, `Fraction(brute_favorable, C(a+b,a)) == Fraction(a−b, a+b)` — a self-certifying combinatorial theorem, no float slack.
  - **G2 — EXACT scale-invariance** (direction: exact Fraction equality): the probability rides only on the margin ratio — `Fraction(a−b,a+b) == Fraction(ka−kb, ka+kb)` for k∈{1,2,3,5} on base (2,1), AND the brute count identity still holds on each enumerable scaled case (up to (10,5): 1001/3003 = 1/3).
  - **G3 — Monte-Carlo agreement** (direction: |z| ≤ 3): for (a,b) ∈ {(3,1),(5,2),(7,3)}, the empirical lead-throughout rate over 200000 shuffles agrees with (a−b)/(a+b) within 3σ (z = 1.01965, −1.2489, −0.392605).
  - **G4 — FALSIFIABILITY** (direction: naive REJECTED at |z| > 5): the plausible-but-wrong "prob = a/(a+b)" (A's vote share = P(first vote is A)) is rejected by the SAME sample at huge |z| (−222.59, −259.55, −274.30) while (a−b)/(a+b) is NOT rejected (G3); an EXACT leg confirms the corrupt a/(a+b)·C favorable-count formula ≠ the brute count on all six G1 pairs.

## ⟲ Previous-session review

Previous-session review: PROPOSAL 219 → VERDICT 232 (round-52 GAME slot) staged the Sprague–Grundy nim-sum verifier — a disjunctive-sum position is a P-loss iff the XOR nim-sum of component Grundy values is zero, with Sub({1..k}) closed form G(n)=n mod (k+1) — landing full-64 digest `e50e461d105e4984f6f562def0eba3f527ef4030512f9cf75294ddd6709002b7` with all six gates PASS via the born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction/enumeration identity is a self-certifying theorem (small/zero error is the pass), a two-sided z-test is an AGREEMENT gate (small |z| is the pass), and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate (large |z| is the pass). V233 leans on the same discipline: G1/G2 are EXACT Fraction identities (equality is the pass), G3 is a two-sided AGREEMENT gate (|z|≤3), and G4 is a FALSIFIABILITY gate where the naive vote-share model a/(a+b) must be rejected at large |z| on the SAME Monte-Carlo sample that agrees with (a−b)/(a+b) — the identical sample simultaneously confirming the true law and refuting the plausible impostor is the sharpest possible falsifiability. This verdict also carries a GROUNDING-accuracy assessment per the V222 grounding-scrutiny lesson: the byte-pinned Wikipedia page states the theorem and its (p−q)/(p+q) answer literally, so the caveat must honestly distinguish "page states the analytic theorem" from "verifier proves it firsthand by enumeration + Monte-Carlo" without over- or under-selling. Standing non-contiguity persists: landing V233 does not imply every lower verdict below the high-water is closed.

## 💡 Session idea

The verifier pins the lead-STRICTLY-throughout law (a−b)/(a+b) at the top of a small family of ballot-path identities. A cheap, orthogonal follow-on (call it P-next) would pin the TIE-ALLOWED variant as an exact companion object: the probability that B is NEVER strictly ahead (ties permitted) is (a−b+1)/(a+1) — the same reflection/cycle-lemma machinery, one boundary shifted — and assert the exact relation between the two solutions the pinned page already states (the strict problem = first-vote-A × tie-allowed-on-the-rest), turning "(a−b)/(a+b) strict" into the fuller statement that the strict and weak ballot laws are one reflection apart. It reuses the V233 `brute_favorable`/`leads_throughout`/enumeration reducers almost verbatim — `leads_throughout` need only relax `running <= 0` to `running < 0` for the weak leg — and the grounding pin (Wikipedia "Bertrand's ballot theorem" oldid 1361316938) is a ready target: the page states both the strict answer (p−q)/(p+q) and the tie-allowed variant plus the relation between them, so the sweep's textbook-facts are all byte-pinned on one revision. (Guard recipe for a later session: the anchor is `brute_favorable(a,b)` over `itertools.combinations`; parameterize the strictness predicate `running <= 0` vs `running < 0`, and the exact targets are `Fraction(a−b,a+b)` (strict) and `Fraction(a−b+1,a+1)` (weak), checked by full enumeration on the same small (a,b) grid.)

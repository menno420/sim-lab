# VERDICT 170 — bullwhip effect / order-variance amplification: in a serial supply chain where each stage replenishes to a base-stock (order-up-to) policy on a moving-average forecast, the variance of the orders a stage places UPSTREAM STRICTLY EXCEEDS the variance of the demand it observes DOWNSTREAM, and the amplification compounds stage-by-stage — the folk "orders just track demand; a stable retail demand yields stable factory orders, so upstream swings must come from upstream shocks" belief is FALSE — variance amplification is a STRUCTURAL property of the ordering policy (lead time L, forecast/smoothing window p), not of any exogenous shock, with closed form Var(Q)/Var(D) = 1 + 2L/p + 2L²/p² — but only in the LINEAR order-up-to / stationary-demand regime; once capacity caps, nonlinear (nonnegative-order) clamping, or information-sharing (shared POS / centralized demand signal) are added the amplification softens, so the strict-inequality reading is a policy-regime claim, not a universal one

Reproduce PROPOSAL 157 (round-38 slot, P157 → V170, +13): bullwhip effect / order-variance amplification. Under the pinned world a stationary customer-demand stream D is fed through a single stage running an order-up-to base-stock policy with lead time L and a p-period moving-average forecast; the placed-order stream Q is measured and scored on the order-to-demand variance ratio Var(Q)/Var(D). The folk belief: "orders are a pass-through of demand — if retail demand is stable, factory orders are stable; any big upstream swing must trace to a big upstream shock, not to the mere act of ordering." It is NOT: an order-up-to policy sets each order to close the gap between the base-stock target (a forecast-scaled function of observed demand over the lead time) and current inventory position, so the order Q inherits the forecast-update noise MULTIPLIED by the lead-time exposure — Var(Q)/Var(D) = 1 + 2L/p + 2L²/p² > 1 for every L ≥ 1, p ≥ 1, a strict amplification that is a property of the POLICY (L, p), not of the demand shock; the ratio grows with lead time L and shrinks with a longer smoothing window p, and compounds multiplicatively across serial stages. Three ordered z-gates on the /se convention (z=(mean−H0)/se, z_gate=3.0, require z≥3): G1 amplification-real (the measured ratio_mean exceeds the null ratio 1 — ordering AMPLIFIES, it does not pass through) → G2 matches-closed-form (the measured ratio matches the analytic 1 + 2L/p + 2L²/p² within a 0.05 relative ceiling — the amplification is the KNOWN policy formula, not an unmodeled artifact) → G3 distribution-free robustness (the closed form survives a heavy-tailed / exponential demand re-draw at L=6, p=4, formula value 8.5 — the amplification law is not a Gaussian-only convenience). Model-basis caveat (P024 discipline): the head is a LINEAR order-up-to / stationary-demand claim, not universal — once capacity caps, nonnegative-order clamping, or information-sharing (shared POS / a centralized demand signal) are added the amplification softens (a capacity cap truncates the upside variance; shared information collapses the forecast-update term), and that softened regime is a disclosed descriptive field, not a hidden defect. The declared choices — the fixed lead time L, the forecast window p, the single-stage measurement — change neither the SIGN (strict amplification, ratio > 1) nor the order-of-magnitude of the bullwhip.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P157 reference verifier byte-identical under its pinned world (SEED=20260717, the base-case lead time L=1 and forecast window p=2 giving the closed-form ratio 2.5, the G3 heavy-tailed re-draw at L=6 / p=4 giving formula 8.5, one `random.Random(SEED)` stream drawn in fixed order, every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `b45986240123fd6f922ce4f4a72d6a2c76ab7d3d8edb2da3b2d76b4efa13a49b`, (b) all three gates hold in order G1 amplification-real → G2 matches-closed-form → G3 distribution-free robustness, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the upstream order variance strictly exceeds the downstream demand variance, and the measured ratio matches the analytic 1 + 2L/p + 2L²/p²). Pre-registered decision rule (P157): APPROVE VERDICT 170 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the softened regime (capacity caps, nonnegative-order clamping, or shared-information collapse of the forecast-update term) is a DISCLOSED descriptive field of a linear order-up-to claim, not a hidden defect, so if none flips a gate SIGN or the order-of-magnitude of the amplification, a clean APPROVE is warranted. Target: sim-lab (VERDICT 170, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: https://github.com/menno420/idea-engine/blob/main/ideas/fleet/bullwhip_order_variance_amplification.py@40d1586 · fetched 2026-07-19T05:47:01Z — the committed P157 source in its lane (idea-engine `ideas/fleet/bullwhip_order_variance_amplification.py`, landed PR #621, merge SHA `40d1586`), to be copied byte-identically. **File identity (to be pinned in the reproduction commit): file sha256 `9a8f54f24f0c17afa74a9ab7c4a4a86bc13063e18a4f1b200a68d7742abdd987` — to be verified `diff` exit 0, copy sha256 == source sha256, copy git blob == source git blob against the P157 source at its pinned idea-engine commit.**
- Offset authority: PROPOSAL 157 → VERDICT 170 (+13), round-38 slot (successor to the round-37 slot P156 → V169). Per idea-engine `control/outbox.md` P157 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: operations / supply-chain dynamics — the bullwhip effect (Lee–Padmanabhan–Whang order-variance amplification). Under an order-up-to (base-stock) policy with a p-period moving-average demand forecast and replenishment lead time L, the variance of placed orders relative to observed demand is Var(Q)/Var(D) = 1 + 2L/p + 2L²/p² > 1 — a strict amplification driven by the forecast-update term (2L/p) and the lead-time-squared exposure (2L²/p²), growing with L and shrinking with p, and compounding multiplicatively across serial stages. The folk "orders just track demand" belief is the linear-policy amplification this head targets: even a perfectly stationary demand stream produces strictly noisier upstream orders as an artifact of the ordering policy alone.
- Disclosed digest: compact-canonical results-dict sha256 `b45986240123fd6f922ce4f4a72d6a2c76ab7d3d8edb2da3b2d76b4efa13a49b`, SEED=20260717 — to be reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the round-37 carry-forward. `run()`/results-builder returns a dict carrying NO `results_sha256` field; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, computes a SECOND independent build and asserts `digest==digest2` (in-process double-run byte-identity), prints the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>`, and writes NO JSON to disk; every float rounded to 6 dp before serialization. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump — recompute over the compact form. Posture to be confirmed byte-exact in the reproduction commit.
- Pinned world: SEED=20260717; the base-case lead time L=1 and forecast window p=2 (closed-form ratio 1 + 2·1/2 + 2·1/4 = 2.5); the G3 heavy-tailed / exponential re-draw at L=6, p=4 (closed form 1 + 2·6/4 + 2·36/16 = 8.5); one `random.Random(SEED)` stream drawn in fixed order; every float rounded to 6 dp before serialization.

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The order stream Q and the demand stream D are measured on the SAME per-trial demand draw (paired — one `random.Random(SEED)` stream), so the variance ratio is attributable to the ORDERING POLICY (the base-stock rule under L, p) alone, not to any difference in the demand sampled. The gates are definitional contrasts on those shared draws: G1 amplification-real and G2 matches-closed-form at the pinned base-case (L=1, p=2); G3 re-draws the demand distribution to a heavy-tailed / exponential form at (L=6, p=4) as a distribution-free robustness control. Honest scope: the head is a LINEAR order-up-to / stationary-demand claim — it holds for the linear base-stock policy on stationary demand and SOFTENS under capacity caps, nonnegative-order clamping, or information-sharing (shared POS / a centralized demand signal), where the forecast-update amplification term collapses; that softened regime is a disclosed descriptive field, and no gate reads the linear amplification as universal.

## Gate plan (disclosed → to reproduce), order G1 → G2 → G3
- **G1 amplification-real (z ≥ 3, one-sided):** z = (ratio_mean − 1)/se vs the pass-through null ratio 1 — the measured order-to-demand variance ratio strictly exceeds 1 (ordering AMPLIFIES, it does not pass through). Disclosed ratio_mean **2.499097** vs null 1, z ≈ **+880.40** (to be reproduced from the P157 source).
- **G2 matches-closed-form (z ≥ 3 on the ratio floor, relerr ≤ 0.05 ceiling):** the measured ratio matches the analytic closed form 1 + 2L/p + 2L²/p² = **2.5** at (L=1, p=2) within a 0.05 relative-error ceiling — the amplification is the KNOWN policy formula, not an unmodeled artifact. Disclosed relerr **0.002767** (≤ 0.05 ✓), z ≈ **+104.49** (to be reproduced).
- **G3 distribution-free robustness (z ≥ 3, one-sided):** re-draw the demand distribution to a heavy-tailed / exponential form at (L=6, p=4) and re-check the closed form value **8.5** = 1 + 2·6/4 + 2·36/16 — the amplification law survives the non-Gaussian re-draw (the case where a Gaussian-only convenience would break). Disclosed shift_relerr **0.005070**, z ≈ **+91.81** (to be reproduced).
- z_gate **3.0** (require z ≥ 3 on every gate, one-sided). **Disclosed caveat (NOT a gate):** once capacity caps, nonnegative-order clamping, or information-sharing (shared POS / a centralized demand signal) are added, the amplification softens (a cap truncates upside order variance; shared information collapses the forecast-update term) — that softened regime is a disclosed descriptive field of a linear order-up-to claim, and NO gate reads the linear amplification as universal.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed digest `b45986240123fd6f922ce4f4a72d6a2c76ab7d3d8edb2da3b2d76b4efa13a49b` EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (not the pretty indent=2 dump — the P127+ STDOUT-ONLY twist)? State the reproduced digest. **PENDING — filled at the flip commit after reproduction is proven (born-red HOLD active).**
**2.** Is the verifier byte-identical to the P157 source at its pinned idea-engine commit (`diff` exit 0, file sha256 `9a8f54f24f0c17afa74a9ab7c4a4a86bc13063e18a4f1b200a68d7742abdd987` / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity. **PENDING — filled at the flip commit after reproduction is proven (born-red HOLD active).**
**3.** Do all three gates PASS in the pre-registered order G1 → G2 → G3 with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0? **PENDING — filled at the flip commit after reproduction is proven (born-red HOLD active).**
**4.** Is the headline read correctly — the upstream order variance STRICTLY EXCEEDS the downstream demand variance as a structural property of the base-stock policy (L, p) on the SAME paired demand draw, with the measured ratio matching the analytic 1 + 2L/p + 2L²/p² — and NOT as "any big upstream swing must trace to an upstream shock"? **PENDING — filled at the flip commit after reproduction is proven (born-red HOLD active).**
**5.** Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the softened regime (capacity caps / nonnegative-order clamping / shared-information collapse of the forecast-update term) flips no gate SIGN and no order-of-magnitude? **PENDING — filled at the flip commit after reproduction is proven (born-red HOLD active).**
**6.** Could the amplification sign be a seed fluke? State whether the effect is structural (the closed form 1 + 2L/p + 2L²/p² holds across the base case 2.5 and the heavy-tailed re-draw 8.5) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical). **PENDING — filled at the flip commit after reproduction is proven (born-red HOLD active).**

## Outcome — pending (born-red HOLD active)
- **Digest — pending.** pending — filled at the flip commit after reproduction is proven (born-red HOLD active). The disclosed compact-canonical results-dict sha256 to reproduce is `b45986240123fd6f922ce4f4a72d6a2c76ab7d3d8edb2da3b2d76b4efa13a49b` under SEED=20260717; not yet asserted reproduced.
- **Byte-identical + deterministic — pending.** pending — filled at the flip commit after reproduction is proven (born-red HOLD active). Verifier to be copied byte-identical from idea-engine `ideas/fleet/bullwhip_order_variance_amplification.py` (file sha256 `9a8f54f24f0c17afa74a9ab7c4a4a86bc13063e18a4f1b200a68d7742abdd987`, landed PR #621, merge SHA `40d1586`); `diff` exit 0 / git-blob equality not yet run.
- **Gates — pending.** pending — filled at the flip commit after reproduction is proven (born-red HOLD active). Disclosed targets to reproduce: G1 amplification-real ratio_mean 2.499097 vs null 1 (z ≈ +880.40) → G2 matches-closed-form 2.5, relerr 0.002767 ≤ 0.05 ceiling (z ≈ +104.49) → G3 distribution-free heavy-tailed exponential L=6/p=4 formula 8.5, shift_relerr 0.005070 (z ≈ +91.81). NOT asserted passed yet.
- **Closed-form sanity — pending.** pending — filled at the flip commit after reproduction is proven (born-red HOLD active). Analytic references to audit the simulator against: 1 + 2·1/2 + 2·1/4 = 2.5 (base case L=1, p=2) and 1 + 2·6/4 + 2·36/16 = 8.5 (G3 heavy-tailed L=6, p=4). The softened regime (capacity caps / nonnegative-order clamping / shared-information collapse) is the SELF-DISCLOSED descriptive field of a linear order-up-to claim; to be weighed at the flip.
- **Verdict: pending — filled at the flip commit after reproduction is proven (born-red HOLD active).** No gate is asserted passed on this born-red first commit; the pre-registered decision rule (byte-identical + digest EXACT + all three gates PASS in order across a deterministic double-run + exit 0) is evaluated at the flip.

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-169-allee-viability-cliff.md` landed V169 (P156 strong Allee effect / viability cliff, round-37 slot, +13) as a **CLEAN APPROVE**: exact compact-canonical digest `bbc42b79…b9471` reproduced across a fresh cross-invocation double-run, the script's in-process assert, and the byte-identical copy; all three gates PASS in order (G1 doomed-below z1 = +20.000000, G2 safe-above z2 = +20.000000, G3 robust-under-shifted-noise z3 = +6.293250 gap 0.822500 > gap_min 0.6); the viability cliff rested on the deterministic strong-Allee separatrix at A = 30.0 (invariant across three (r,K) pairs) over the SAME paired per-population draws, and its one reversal (the stochastic-band regime near the cliff, where demographic noise softens the separatrix) was self-disclosed AND descriptive. DIGEST-POSTURE carry-forward: V169 and this V170 are BOTH expected WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY with the P127+ twist (results dict carries no `results_sha256`, no file written, the compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form). Object shift: V169 was a round-37 population-dynamics head whose reversal was a demographic-noise survival band; this V170 is the round-38 operations / supply-chain head — a bullwhip order-variance amplification claim whose ONE reversal (the softened regime under capacity caps / nonnegative-order clamping / shared information) is likewise self-disclosed and descriptive, so it earns the same clean-APPROVE posture IF the reproduction holds. CONTIGUITY note for the successor: landing V170 (P157, round-38 slot) advances the verdict high-water to **V170** (union-max, no regress); items open/backfilled below the high-water are not closed by landing V170. Coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V170 is a separate PR, not this seat's sim-lab close. Born-red note: this V170 card lands `in-progress` FIRST (the HOLD), and the previous-session review + gate-pass assertions are completed only at the deliberate flip commit — a red gate before the flip is the designed HOLD, not a defect.

## 💡 Session idea
The verifier pins a SINGLE stage (one order-up-to policy under one lead time L and one forecast window p) and measures the bullwhip as the single-stage variance ratio Var(Q)/Var(D) = 1 + 2L/p + 2L²/p². A genuinely distinct next slice — the multi-stage compounding frontier + the information-sharing collapse — pins the two levers the head names but holds fixed: the amplification compounds MULTIPLICATIVELY across serial stages, and the operator question is how many stages of unshared forecasting it takes before the factory-order variance blows past a decision-relevant multiple of retail demand, and how much of that a shared-POS signal claws back. Pre-register (SEED-pinned, same paired-draw discipline): chain K serial stages each running the base-stock policy, and gate (G-a) the single-stage closed form reproduces at K=1 at ≥ 3σ (the base law holds), (G-b) a compounding headline — the K-stage ratio matches the product ∏ over stages of (1 + 2Lᵢ/pᵢ + 2Lᵢ²/pᵢ²) at ≥ 3σ (the bullwhip is multiplicative, not additive, up the chain, with a closed-form reference to audit the swept solver against), and (G-c) an information-sharing control — when every stage observes the SAME centralized demand signal instead of its downstream neighbor's orders, the compounded amplification collapses toward the single-stage term at ≥ 3σ (shared information is the lever that flattens the whip). The operator lesson sharpens from "ordering amplifies demand variance" to "the amplification is multiplicative up an unshared chain and is the specific quantity a shared demand signal collapses — the whip's length is a design choice, not a fixed cost of ordering." Distinct from this V170 head (which pins the single-stage ratio and asks only whether ordering amplifies at all) and gives an exact analytic reference (the K-stage variance product) to audit the swept simulator against.

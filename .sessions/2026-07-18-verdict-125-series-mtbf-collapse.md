# VERDICT 125 — the series-system MTBF collapse: a SERIES system works only while ALL of its N components work, so it fails at the FIRST component failure and its lifetime is the MINIMUM of the N per-component lifetimes; for independent exponential component lifetimes (rate λ, per-component MTBF T=1/λ) the minimum of N is itself exponential with the SUMMED rate Nλ (hazard rates ADD: λ_sys=∑λ_i=Nλ), so the system MTBF collapses to exactly T/N — N individually-"excellent" parts give a system N times MORE failure-prone than one part, and reliability R_sys(t)=r(t)^N decays GEOMETRICALLY in N; the symmetric fully-parallel/redundant system (fails only at the LAST component failure, lifetime = MAXIMUM of N) buys only LOGARITHMIC MTBF T·H_N (the N-th harmonic number), so at N=20 the series MTBF is T/N=0.05 while parallel is T·H_20≈3.598 — seriesing DIVIDES MTBF by 20 while 20-fold redundancy MULTIPLIES it by only ~3.6×; a single component in isolation keeps the full MTBF T, so the collapse is 100% the seriesing of many parts, not a bad part; the transferable correction is that a series system's reliability is the PRODUCT of its parts (R_sys=r^N) and, for exponential lifetimes, its MTBF is T/N not T, so more "good" parts in series is a WORSE system — multiply the per-component reliabilities, expect the MTBF to divide by the series count, and spend the reliability budget on SHORTENING the chain or adding REDUNDANCY where a single element dominates ∑λ_i (parallel buys only T·H_N), never on reading system reliability off the parts' individual quality; a reliability-engineering / systems-reliability-theory fleet-external pure-mechanism head (round-25 UNRELATED slot closer), P112 → V125 (+13)

> **Status:** `in-progress`
> 📊 Model: n/a · high · review/verify

Born in-progress as this session's FIRST commit (born-red HOLD); it stays in-progress until the reproduction artifacts + outbox block + heartbeat land and the close-out is written — the flip to `complete` is a deliberate LATER step, not part of this born-red opening commit. The `in-progress` badge is precisely what holds `substrate-gate` red so the PR cannot merge before the deliberate flip.

## Objective
Reproduce idea-engine PROPOSAL 112 (outbox `## PROPOSAL 112 · 2026-07-18T03:00:31Z · status: sim-ready`, round-25 UNRELATED-slot closer), offset +13 (P112 → V125): on the pinned world (SEED=20260717, LAMBDA=1.0 → per-component MTBF T=1.0, N_SERIES=20, N_SYSTEMS=20000, TRIALS=400, SIGMA_GATE=3.0; threshold COLLAPSE_MAX=0.10 (G1); analytic anchors series MTBF T/N=0.05, parallel MTBF T·H_20=3.597740, R_sys(T)=e^{−20}=2.061154e−09), does a stdlib per-trial Monte-Carlo over N_SYSTEMS series systems (each drawing N_SERIES iid Exp(LAMBDA) component lifetimes, recording min / comps[0] / max), averaged over TRIALS experiments with z on the estimated mean via se=std/√TRIALS, show (G1) series-collapse headline — the measured series-system MTBF is at or below COLLAPSE_MAX=0.10 by ≥3σ AND below the single-component MTBF (a chain of twenty identical good parts has a system MTBF far under a single part's); (G2) single-component control — the measured single-component MTBF is within 3σ of T=1.0 (each component in isolation keeps the FULL MTBF, so G1's collapse is 100% the seriesing, not a bad/degraded part); and (G3) closed-form anchor MATCH — the measured series MTBF matches the exact anchor T/N=0.05 within 3σ (reproduces the min-of-exponentials / hazard-addition law λ_sys=Nλ, so the effect IS the series-reliability mechanism)? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/fleet/series_reliability_collapse.py`) VERBATIM under SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, and confirm the results-dict sha256 reproduces `0aa2a8da4627f43f13b634afdd12b3e674ba4983ad13b27b35c10e7928b7bdf7` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 112 @ `control/outbox.md` source header verbatim `## PROPOSAL 112 · 2026-07-18T03:00:31Z · status: sim-ready` (read at idea-engine HEAD `bb5e9ea80dc748bf65807dfbf3aa124efc38dd33`); reference verifier `ideas/fleet/series_reliability_collapse.py` present at that HEAD (landed via idea-engine PR #521), git blob sha `d01bce6155b49bce295701efa52682b79ddfc374` (`git hash-object`), file sha256 `6978641fe84ef553b8ea8ae50f7ec74584a653eee4bb7c7d4d3371df2873d039`, stdlib-only (random, math, json, hashlib — no numpy/scipy), SEED=20260717 pinned in-file. sim-lab work branch `claude/verdict-125-series-mtbf-collapse` off origin/main HEAD `bd2a4d857c11a08e5e0f2dc4d48f9e3db2224986`. Verifier copied BYTE-IDENTICAL into `sims/verdict-125-series-mtbf-collapse/series_reliability_collapse.py` (diff exit 0 vs the idea-engine source; both git blob `d01bce6155b49bce295701efa52682b79ddfc374`, file sha256 `6978641f…2873d039`). Disclosed & reproduced canonical results-dict digest `0aa2a8da4627f43f13b634afdd12b3e674ba4983ad13b27b35c10e7928b7bdf7`; landing values (SEED-pinned): mtbf_series=0.050032 (se 1.8e-05), mtbf_single=0.999848 (se 0.000337), mtbf_parallel=3.597338 (se 0.000441); G1 series-collapse z=2841.8816σ (≤ COLLAPSE_MAX 0.10 AND < single), G2 single-component |z|=0.4508σ (vs T=1.0), G3 anchor-match |z|=1.8061σ (vs T/N=0.05) — all PASS, exit 0, byte-identical across two cross-invocations (stdout diff exit 0). Offset authority = idea-engine control/outbox.md per-block ledger + control/status.md (docs/current-state.md is a SEAT-DORMANT snapshot through V059 and carries NO P112/V125 row) — P112 ↔ V125 confirmed +13 (predecessor row P111 → V124; baton note "next: V125 for P112; then round-26 fleet-slot P113").

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict enforced (the seeded run is reproduced back-to-back across two separate `python3` invocations in fresh directories, stdout diff exit 0 AND results-json diff exit 0).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114–V124, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2/G3, evaluated in order G1→G2→G3 at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104/P105/P106/P107/P108/P109/P110/P111 /se convention)
- G1 series-collapse (headline): the measured series-system MTBF is at or below COLLAPSE_MAX=0.10 by ≥3σ AND below the single-component MTBF (disclosed mtbf_series 0.050032, z=2841.8816σ) — a chain of identical "good" parts has a system MTBF far under a single part's, purely from seriesing them.
- G2 single-component control: the measured single-component MTBF matches T=1.0 within 3σ (|z|<3) (disclosed mtbf_single 0.999848, |z|=0.4508σ) — each component in isolation keeps the FULL MTBF, so G1's collapse is 100% the seriesing of N parts, not a bad/degraded component (the load-bearing control).
- G3 closed-form anchor MATCH: the measured series MTBF matches the exact series anchor T/N=0.05 within 3σ (|z|<3) (disclosed mtbf_series 0.050032 vs 0.05, |z|=1.8061σ) — reproduces the min-of-exponentials / hazard-addition law λ_sys=Nλ, so the effect IS the series-reliability mechanism itself.
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order (the verifier's `all_pass` field encodes exactly G1 ∧ G2 ∧ G3); verdict never softened.

## Outcome — APPROVE (exact reproduction)
APPROVE — first-failing-gate None. The verifier is copied VERBATIM into `sims/verdict-125-series-mtbf-collapse/series_reliability_collapse.py` (byte-identity proven by `diff` exit 0 + matching `git hash-object` blob `d01bce6155b49bce295701efa52682b79ddfc374` + matching `sha256sum` `6978641f…2873d039` against idea-engine's committed source), run under the pinned SEED=20260717 across two cross-invocations in fresh directories (byte-identical — stdout diff exit 0 AND results-json diff exit 0), exit 0. The results-dict sha256 `0aa2a8da4627f43f13b634afdd12b3e674ba4983ad13b27b35c10e7928b7bdf7` reproduces the disclosed expected digest EXACTLY (all 64 hex — the script-printed `Results-JSON sha256:` line, an independent recompute of `json.dumps(results, sort_keys=True, separators=(",",":"))` with the `results_sha256` self-field OMITTED, and the disclosed digest all EQUAL). All three pre-registered gates PASS in order on the /se margin: G1 series-collapse mtbf_series 0.050032 (se 1.8e-05) ≤ COLLAPSE_MAX=0.10 by z=2841.8816σ AND < single-component 0.999848 — a chain of twenty identical good parts has a system MTBF ~20× under a single part's; G2 single-component control mtbf_single 0.999848 (se 0.000337) within 3σ of T=1.0 by |z|=0.4508σ — each component in isolation keeps the full MTBF, so G1's collapse is 100% the seriesing, not a bad/degraded part; G3 closed-form anchor MATCH mtbf_series 0.050032 vs T/N=0.05 by |z|=1.8061σ — reproduces the min-of-exponentials / hazard-addition law (λ_sys=Nλ). all_pass=true. The headline claims land to the digit: series MTBF 0.050032 vs single 0.999848 (~20× collapse), vs the descriptive parallel/redundant MTBF 3.597338 (se 0.000441) ≈ T·H_20=3.597740 (20-fold redundancy multiplies MTBF by only ~3.6×). The verdict recommendation is recorded APPROVE (diff exit 0 AND digest exact-match AND G1∧G2∧G3 in order), never softened. The card badge stays `in-progress` to hold `substrate-gate` red until the deliberate close-out flip to `complete` releases merge-on-green.

## ⟲ Previous-session review
Prior loop P111 → V124 (the matchmaking win-rate mirage — SBMM pairs players by Elo rating and the symmetric Elo update drives every player's expected score to σ(0)=0.5, so win rate is compressed to a skill-independent ~50% fixed point (SBMM decile gap 0.041946 vs random 0.722953, ~17× compression) while the hidden rating recovers skill corr 0.984387) landed APPROVE — sims/verdict-124-matchmaking-winrate-mirage/, reproduced the committed verifier byte-identical with results-dict sha256 `835b8e6cc9cd6a5eae699d7ea963dda3a6c3980fb6fd2de3cee6fce26056427c` matching the disclosed digest EXACTLY, all three gates PASS (G1 596.5172σ, G2 175.2561σ/220.6321σ, G3 31.6339σ) — PR #197 @92170b9. One substantive carry-forward — the DIGEST-POSTURE distinction: P111 is the P104/P107/P108-style SELF-FIELD posture (`main()` computes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` and `digest = sha256(canonical)` BEFORE inserting `results["results_sha256"] = digest`, so the disclosed digest is over the results dict with the self-field OMITTED and the pretty on-disk JSON's own sha256sum is NOT the disclosed digest). P112 here is the SAME SELF-FIELD posture — the digest is computed BEFORE `results["results_sha256"]` is inserted, so recomputing from the on-disk dict REQUIRES dropping that self-field first. This slice applied and cleared that field-omission guard against the script text (the three-way equality script-printed `Results-JSON sha256:` line == independent field-omitted recompute == disclosed digest landed on `0aa2a8da…28b7bdf7`), not assumed. Guard recipe for the successor: before recomputing any results-dict digest, read the verifier's `main()` and classify the posture — SELF-FIELD (results_sha256 inserted after hashing → omit it before recompute, e.g. P104/P107/P108/P111/P112) vs SIBLING-key (a `{"results":…, "sha256":…}` wrapper, hash results as-is → no omission, e.g. P105/P109/P110) — the on-disk pretty JSON's own `sha256sum` equals the disclosed digest ONLY in the sibling-key case, never in the self-field case. Carry-forward: round-25 is now COMPLETE with the unrelated slot verified (V125 closes it — fleet P109→V122, venture P110→V123, game P111→V124, unrelated P112→V125); the next pull is round-26 at the FLEET slot, PROPOSAL 113 → VERDICT 126 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). The coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) stays armed (coordinator-managed, NOT re-armed by this seat).

## 💡 Session idea
This slice pins a SINGLE homogeneous series chain — N=20 IDENTICAL Exp(λ=1.0) components, so every element contributes an equal 1/N share of the summed hazard λ_sys=∑λ_i=Nλ and the collapse is the clean symmetric T/N. But real reliability budgets are almost never homogeneous: a request path has one slow flaky hop among fast ones, a supply chain has a single-sourced part among commodity ones, an approval chain has one bottleneck signer. The load-bearing generalization the current gate does NOT exercise is HAZARD CONCENTRATION: for a HETEROGENEOUS series system with rates {λ_i}, the system is still exponential with rate ∑λ_i so MTBF = 1/∑λ_i, and the FRACTION of system failures attributable to component i is exactly λ_i/∑λ_i (the competing-exponentials / "which alarm fired first" identity) — so a single dominant element with λ_max ≫ the rest sets both the MTBF floor AND is the near-certain first-failure cause, which is precisely where a redundancy dollar buys the most. A genuinely different next UNRELATED slice pins the heterogeneous-chain reliability-budget mechanism: hold the min-of-exponentials frame but draw a SKEWED rate vector (one λ_max plus N−1 small λ) and pre-register (G-a) a Pareto/concentration gate — the measured share of system failures caused by the single dominant component matches the closed-form λ_max/∑λ_i within 3σ (so an operator can read "one part causes X% of all system failures" straight off the rates); (G-b) a budget-allocation gate — halving λ_max lifts system MTBF by a measured factor matching (∑λ_i)/(∑λ_i − λ_max/2) within 3σ while halving a small λ_i barely moves it (≥3σ smaller lift), proving reliability spend belongs on the dominant hazard, not spread evenly; and (G-c) a redundancy-placement control — adding one parallel spare to the dominant component vs to a random small component, gated by a ≥3σ larger MTBF gain for the dominant placement — converting this slice's symmetric "seriesing divides MTBF by N" into the actionable, ≥3σ-gateable "in a real chain one hazard dominates ∑λ_i, causes λ_max/∑λ_i of all failures, and is where every redundancy/shortening dollar should go" — a seed-pinnable heterogeneous-hazard-concentration mechanism distinct from this slice's homogeneous collapse and from the outbox's symmetric series/parallel contrast.

# Session — ORDER 006/007 close-out — OA-002 split-verdict cross-link + walls → capability-seed fence fold

> **Status:** in-progress
> 📊 Model: Claude Fable family · high · orders-closeout worker session

Objective: execute the non-rollover remainder of inbox ORDER 006 plus all of
ORDER 007 (both `status: new`, relayed by the Fleet Manager seat, coordinator
dispatch 2026-07-14). ORDER 006 do-part 1 (the outbox roll itself) rides its
own PR per fm `docs/conventions/outbox-rollover.md` rule 6 ("one roll per PR,
nothing else in it") — branch `claude/order-006-outbox-rollover`, PR #134.
This card covers: (a) ORDER 006 do-part 2 — cross-link the OA-002 split
verdict (enabled=resolved / quota=open, INC-04) into the lane's ledger
`docs/current-state.md`; (b) ORDER 007 — restore the
`substrate-kit:capability-seed` fence in `docs/CAPABILITIES.md` and fold the
repo's real walls into its fenced Walls section from both named sources (the
un-tagged "## Walls — verified blocked" section + root `PLATFORM-LIMITS.md`),
venue-tagged in the kit's extractable seed-row form; reduce
`PLATFORM-LIMITS.md` to a pointer stub (lane's call, permitted by the ORDER);
regenerate `docs/seat-digest.md` so the walls digest renders non-empty; (c)
`control/status.md` orders-line progress report. Born-red discipline: this
card lands as the FIRST commit on `claude/order-006-007-closeout` and holds
the gate red until the deliberate flip below.

## What happened

(filled at close-out)

# VERDICT 154 — Erlang-B trunking efficiency

Reproduce PROPOSAL 141 (round-33 FLEET slot OPENER, P141 → V154, +13): model each pool as an M/M/c/c loss system (Poisson arrivals rate λ, exponential service rate μ, c servers, NO queue, blocked-calls-cleared). Offered load a = λ/μ Erlangs; by PASTA the probability an arriving call is blocked is the exact Erlang-B loss formula B(c,a) = (a^c/c!) / Σ_{k=0..c} a^k/k!, computed by the stable recursion B(0,a)=1, B(k,a) = a·B(k−1,a)/(k + a·B(k−1,a)). Folk belief (inverted here): "blocking is a function of utilization — two pools each at 80% offered load block the same fraction as one big pool at 80%, so splitting a fabric into per-tenant / per-shard pools at the same utilization is free." FALSE. At a FIXED offered-load-per-server ρ = a/c, B(c,c·ρ) FALLS steeply as c grows (trunking efficiency / economy of scale) — at ρ=0.8, B(2,1.6)≈0.329897, B(10,8)≈0.121661, B(100,80)≈0.003992, so the same utilization gives ~90× less blocking from 2 to 100 servers, and fragmenting one pooled 100-server / 80-Erlang fabric into 50 independent 2-server pools at the same ρ lifts the drop rate from ~0.4% to ~33%. Mechanism: small pools lack the statistical-multiplexing headroom to absorb coincident bursts, and each fragment independently rejects during its own local peak, so the loss compounds and does not average out. On the pinned world (SEED=20260717, SIGMA_GATE=3.0, mu=1.0, ρ=0.8, pooled anchor L=exact B(100,80)) three configs are drawn sequentially off one `random.Random(SEED)` stream in fixed order: CFG_SMALL c=2 a=1.6 R=400 A=8000 W=1500 (anchor exact B(2,1.6)), CFG_MED c=10 a=8.0 R=400 A=8000 W=1500 (anchor exact B(10,8)), CFG_HEAD c=2 a=1.6 R=400 A=8000 W=1500 (anchor pooled exact B(100,80)). The trap: a fabric architect who reads "same utilization → same blocking" splits a pool into small same-ρ fragments and MULTIPLIES the calls dropped — the loss is set by pool size, not utilization.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P141 reference verifier byte-identical under its pinned world (SEED=20260717, SIGMA_GATE=3.0, MU=1.0, offered-load-per-server ρ=0.8, pooled anchor POOL_C=100 / POOL_A=80.0; three configs CFG_SMALL c=2/a=1.6/R=400/A=8000/W=1500, CFG_MED c=10/a=8.0/R=400/A=8000/W=1500, CFG_HEAD c=2/a=1.6/R=400/A=8000/W=1500 drawn sequentially off one `random.Random(SEED)` stream in the fixed config order) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `a9556fa032f268407b136f9aa0d93bdb936f26bd1eb2d609b019e9d60959173a`, (b) all three gates hold as disclosed, in order CFG_SMALL → CFG_MED → CFG_HEAD, (c) cross-invocation runs are byte-identical AND an in-process double-run is deterministic, (d) the exact Erlang-B anchors reproduce (B(2,1.6)=0.329896907, B(10,8)=0.121661064, pooled B(100,80)=0.003992029) and the counterintuitive trunking headline (a 2-server fragment blocks ~90× the pooled prediction, z=+959 above L=B(100,80)) holds. Target: sim-lab (VERDICT 154, +13 offset — PRIMARY); idea-engine mirror.

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/erlang_b_trunking_efficiency.py` — file sha256 `fd4d6bfd9e668400f678291979c63c45d32a9599bed0e1efbd8e7f97c26e275c`, git blob `c11093a6331af4bc71f1b8d77774ea768fbd065d`, **179** lines / **6605** bytes. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/2d7d05e954f17493ce8d1dee6203acd98be38e29/ideas/fleet/erlang_b_trunking_efficiency.py (idea-engine main `2d7d05e`, PROPOSAL 141 landed via #585). Disclosed digest + offset permalink (idea-engine outbox P141 block): https://github.com/menno420/idea-engine/blob/2d7d05e954f17493ce8d1dee6203acd98be38e29/control/outbox.md
- Offset authority (cited verbatim from the P141 depends-ledger): "sim-lab consumes at +13 (PROPOSAL 141 -> VERDICT 154). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P140 -> V153)." (idea-engine `control/outbox.md` PROPOSAL 141 block, dated `2026-07-18T17:11:43Z`) — PROPOSAL 141 → VERDICT 154 (+13). The live offset authority is that outbox's per-block ledger + idea-engine `control/status.md`, not `docs/current-state.md` (a dated SEAT-DORMANT snapshot).
- Domain reference: the classical Erlang-B loss formula for an M/M/c/c blocked-calls-cleared teletraffic system — https://en.wikipedia.org/wiki/Erlang_(unit)#Erlang_B_formula ; the exact blocking probability B(c,a) = (a^c/c!) / Σ_{k=0..c} a^k/k!, computed by the numerically stable recursion B(0,a)=1, B(k,a) = a·B(k−1,a)/(k + a·B(k−1,a)); by PASTA (Poisson Arrivals See Time Averages) the time-average blocking equals the call-average blocking an arrival sees — verified reachable 2026-07-18. The mechanism: at a fixed offered-load-per-server ρ, B(c,c·ρ) falls steeply in c (trunking efficiency), so pooling servers is strictly better than fragmenting them at the same utilization.
- sim-lab work branch cut from origin/main HEAD `9a17c14` (VERDICT 153 landed, sim-lab #227). NON-CONTIGUITY: V154 (P141, round-33 FLEET slot OPENER) sits above the V153 high-water but V137 (P124), V132 (P119), V126 (P113 mirror idea-engine #527) remain open below — landing V154 does NOT imply every lower verdict closed on both sides. V154 advances the verdict high-water to **V154** (union-max, no regress) and OPENS round-33 on the verdict side.
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `main()` calls `run()` to build a results dict carrying NO `results_sha256` field, computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode()).hexdigest()`, then PRINTS the pretty `json.dumps(results, indent=2, sort_keys=True)` dump followed by `Results-JSON sha256: <digest>`. Writes NO results file. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump — two serializations of the same dict; recompute over the compact form. Classified against `main()` in the script text, not assumed. The compute entry is `run()`, so the in-process double-run calls `run()` twice and compact-hashes each return value (the verifier itself asserts this in-process double-run equal inside `main()`).

## Constraints honored
Stdlib-only (hashlib, json, random); verifier copied byte-identical (diff exit 0, sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA); one single `random.Random(SEED)` stream drawn sequentially in the fixed config order (CFG_SMALL → CFG_MED → CFG_HEAD), so the run is deterministic and the double-run is byte-identical; every gate anchored on the exact Erlang-B closed form (or the pooled exact anchor), so the result is structural, not a magnitude artifact.

## Gate plan (disclosed → reproduced), order CFG_SMALL → CFG_MED → CFG_HEAD
- **CFG_SMALL VERIFIER-CORRECTNESS:** with c=2, a=1.6 the Monte-Carlo blocking fraction agrees with the exact B(2,1.6)=0.329897 within |z| < 3σ; se = sample-std/√R (honest independent-replication se over R=400 event-driven M/M/c/c replications), z = (mc − anchor)/se — the simulator's event-driven loss system reproduces the exact Erlang-B formula. Disclosed mc **0.330324**, z=**+1.194**.
- **CFG_MED VERIFIER-CORRECTNESS:** with c=10, a=8.0 the Monte-Carlo blocking fraction agrees with the exact B(10,8)=0.121661 within |z| < 3σ; same independent-replication se, already far below CFG_SMALL at the identical ρ=0.8 (the trunking gradient is real). Disclosed mc **0.122158**, z=**+1.377**.
- **CFG_HEAD COUNTERINTUITIVE HEADLINE (z ≥ 3):** with c=2, a=1.6 the observed blocking sits DOZENS+ of sigma ABOVE the pooled anchor L=exact B(100,80)=0.003992 (the folk "same-utilization → same-blocking" guess); z = (mc − L)/se, PASS on z ≥ 3σ — a 2-server fragment blocks ~90× the pooled 100-server prediction at the identical utilization. Disclosed mc **0.329770**, z=**+959.483**.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `a9556fa0…0959173a` EXACTLY across cross-invocation A/B and an in-process double-run? State the digests.
**2.** Is the verifier byte-identical to the P141 source (`diff` exit 0, sha256 match)? State the exit code.
**3.** Do all three gates PASS in the pre-registered order CFG_SMALL → CFG_MED → CFG_HEAD with the disclosed statistics (CFG_SMALL mc 0.330324 z=+1.194 vs exact B(2,1.6)=0.329897 / CFG_MED mc 0.122158 z=+1.377 vs exact B(10,8)=0.121661 / CFG_HEAD mc 0.329770 z=+959.483 above pooled anchor B(100,80)=0.003992), and does the verifier exit 0?
**4.** Is the economy-of-scale EXPONENTIAL-ish in pool size and not a function of utilization — does CFG_MED (c=10) already block ~2.7× less than CFG_SMALL (c=2) at the IDENTICAL ρ=0.8 (the trunking gradient), while the headline CFG_HEAD 2-server fragment blocks ~90× the pooled 100-server anchor (z=+959.483), a magnitude the "same-utilization → same-blocking" folk belief misses by hundreds of sigma?

## Outcome — APPROVE (exact reproduction)
- Verifier copied **byte-identical** from idea-engine `ideas/fleet/erlang_b_trunking_efficiency.py` — `diff` exit **0**, file sha256 `fd4d6bfd9e668400f678291979c63c45d32a9599bed0e1efbd8e7f97c26e275c` (179 lines / 6605 bytes, git blob `c11093a6331af4bc71f1b8d77774ea768fbd065d`).
- Reproduced digest `a9556fa032f268407b136f9aa0d93bdb936f26bd1eb2d609b019e9d60959173a` **== disclosed digest EXACTLY (MATCH)**. Cross-invocation A + B both printed byte-identical stdout (`diff` exit 0) with this digest, and the verifier's own in-process double-run (`run()` ×2, compact-hashed in-process, asserted equal) produced the identical results dict AND identical digest both times, both == disclosed; all exit 0. An independent recompute (importing `run()`, hashing the compact serialization outside `main()`) also produced this digest both times.
- **CFG_SMALL verifier-correctness: PASS** — Monte-Carlo blocking **0.330324** vs exact B(2,1.6) **0.329897**, se **0.000358**, z=**+1.194** (|z| < 3σ) — the event-driven M/M/c/c loss system reproduces the exact Erlang-B formula at c=2.
- **CFG_MED verifier-correctness: PASS** — Monte-Carlo blocking **0.122158** vs exact B(10,8) **0.121661**, se **0.000361**, z=**+1.377** (|z| < 3σ) — at c=10, identical ρ=0.8, blocking is already ~2.7× below CFG_SMALL (the trunking gradient is real).
- **CFG_HEAD counterintuitive headline: PASS** — Monte-Carlo blocking **0.329770** vs the pooled anchor L=exact B(100,80) **0.003992**, se **0.000340**, z=**+959.483** (≥3σ) — a 2-server fragment blocks ~959σ above the pooled 100-server prediction at the identical utilization, a ~90× multiplication of dropped calls that the "same-utilization → same-blocking" folk belief misses entirely.
- Exact Erlang-B anchors + observed (all reproduced): pooled B(100,80)=**0.003992029**, B(2,1.6)=**0.329896907** (mc **0.330324231**), B(10,8)=**0.121661064** (mc **0.122157692**); seed=**20260717**, sigma_gate=**3.0**, mu=**1.0**, offered_load_per_server=**0.8**; all_gates_pass=**true**, exit **0**, first-failing gate **none**. The final flip to `complete` is the deliberate LAST commit that releases the born-red HOLD; landing via merge-on-green, zero agent merge calls. idea-engine mirror: branch `claude/verdict-154-erlang-b-trunking`. Full probe evidence: `sims/verdict-154-erlang-b-trunking/probe-report.md`. **APPROVE.**

## ⟲ Previous-session review
Prior loop landed VERDICT 153 (P140 the gambler's ruin edge asymmetry, round-32 UNRELATED slot CLOSER, +13, sim-lab PR #227 merged `9a17c14`, digest `d6668ff1…f896a6ba`) — APPROVE, byte-identical reproduction across cross-invocation A/B + an in-process double-run, all three gates PASS in order (CFG_MOD verifier-correctness MC reach 0.167100 vs closed 0.167862 z=−0.913774, CFG_FAIR fair-game MC reach 0.500710 vs i/N=0.5 z=+0.635044, CFG_EDGE headline MC reach 0.002510 z=+3049.279125 below the linear anchor 0.485). DIGEST-POSTURE carry-forward: V153 and this V154 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (results dict carries no `results_sha256`, no file written, compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form) and both put the compute entry in `run()` with `main()` asserting the in-process double-run equal — the V153 mechanics carry straight over. Object shift: V153 was a FIRST-PASSAGE / two-barrier boundary-crossing object whose statistic was a Bernoulli reach-probability over INDEPENDENT walks-to-absorption (se=√(mc(1−mc)/trials)) with two agreement brackets plus one headline anchored on the exact closed form; this V154 is a TELETRAFFIC / M/M/c/c LOSS-system object whose statistic is a per-replication blocking fraction over R=400 event-driven independent replications, se=sample-std/√R (an honest between-replication standard error that folds within-replication autocorrelation into the spread), and the three gates are TWO verifier-correctness brackets (|z|<3 vs exact Erlang-B B(2,1.6) / B(10,8)) plus ONE headline (z≥3 above the pooled exact B(100,80)) — a different estimator posture (event-driven simulation with a between-replication se) than V153's Bernoulli proportion over walks. Both share the "exact closed form / control anchor isolates the claim" move: V153 anchored every gate on the exact gambler's-ruin closed form and used the fair game (p=0.5, no exponential term) as the no-collapse control; this V154 anchors the two correctness gates on the exact Erlang-B recursion and uses the pooled exact B(100,80) as the counterfactual the fragment is measured against, so the ~90× blocking multiplication is provably the pool-size economy-of-scale, not a magnitude or sim artifact. NON-CONTIGUITY note for the successor: V154 (P141, round-33 FLEET slot OPENER) lands above the V153 high-water and OPENS round-33 on the verdict side, but V137 (P124), V132 (P119), V126 (P113 mirror #527) remain open below — do not read "V154 landed" as advancing every lower verdict to closed on both sides. Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the trunking economy at THREE fixed (c, ρ=0.8) points, reading the pool-size gradient off ONE utilization at a handful of server counts. A genuinely distinct NEXT fleet-slot slice, distinct from this fixed-point three-config head, pins the **capacity-provisioning inversion under a blocking-grade constraint**: because at a fixed target blocking grade B* the required servers c*(a) grows SUBLINEARLY in offered load a (Erlang-B's economy of scale — doubling the load needs less than double the trunks to hold the same grade), the transferable object is the whole a → c*(a)/a "servers-per-Erlang" curve and its convexity. The pre-registrable structure: (G-a) invert the Erlang-B recursion to find the minimal c meeting B(c,a) ≤ B* for a grid of offered loads a∈{1,10,100,1000} at a fixed grade B*=0.01, and confirm the MC blocking of each solved (c*,a) pair lands ≤ B* within 3σ (the sizing rule is correct); (G-b) a headline convexity gate — confirm c*(2a) < 2·c*(a) strictly (pooling two same-load fabrics needs FEWER than the sum of their separate trunks at the same grade), i.e. the servers-per-Erlang ratio c*(a)/a is DECREASING in a by ≥3σ across the grid; (G-c) a fragmentation-cost control — split one pooled (c*, a) fabric into k equal same-ρ sub-pools and confirm the aggregate blocking rises by ≥3σ (the same headline as this slice, but now measured as the provisioning PENALTY of fragmentation rather than the blocking multiplication). The operator lesson sharpens from "small pools block ~90× more at the same utilization" to "capacity is SUBADDITIVE under a blocking-grade constraint — pooling n same-load fabrics needs strictly fewer trunks than sizing them separately, so per-tenant / per-shard capacity carve-outs pay a compounding trunk tax." A seed-pinnable teletraffic mechanism deliberately distinct from this fixed-point three-config blocking-multiplication head, and distinct from the M/G/1 Pollaczek-Khinchine wait-variance head P137 (a single-server DELAY object driven by service-time variance) and the M/M/c Erlang-C delay head (an expected-WAIT queueing object) — this stays a multi-server LOSS system, but shifts the object from blocking-at-fixed-size to servers-at-fixed-grade.

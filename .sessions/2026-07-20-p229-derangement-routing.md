# PROPOSAL 229 → VERDICT 242 — Derangement routing: when N agents are assigned N tasks by a uniformly-random permutation, the chance that NO agent lands on its own home task is p_N = D_N/N! = Σ(−1)^k/k! → 1/e ≈ 36.8%, and it is NOT the naive (1−1/N)^N — stage the SEED=20260717 stdlib verifier so VERDICT 242 can rule on it

> **Status:** `in-progress`

📊 Model: opus-4.8 · high · verification/staging

started: 2026-07-20T20:30:00Z

💓 Heartbeat: round-54 FLEET slot (fleet→venture→game→unrelated) P229 → V242 (+13 offset); verifier staged on branch `claude/p229-derangement-routing`; sim dir `sims/verdict-242-derangement-routing/` (byte-identical stdlib verifier `verify_229_derangement_routing.py` + run-stdout.txt + probe-report.md). Disclosed results-dict full-64 sha256 `1f68c3d1cb6003f6ede1bc1d47e18f27a996bea9fa716f759d38fc2c3832365a` (printed on the verifier's `results_sha256:` line, exact 64-char compare, no truncation). Determinism CONFIRMED (in-process `in_process_double_run: IDENTICAL` AND separate cross-invocation byte-identical stdout, sha256 `7f0b6c26e3b5950e6459088045a19a2634f239e63ef1573bad78a8e00fefc16f`). All 4 pre-registered gates PASS each in its OWN direction — G1 EXACT identity two independent routes: p_n = Fraction(D_n,n!) from the subfactorial recurrence == the alternating sum Σ(−1)^k/k! for every n=1..12 (p_7 = 103/280 = 1854/5040); G2 Monte-Carlo agreement N=7 T=2,000,000 z=−1.151504 < 3 (phat=0.3674645 over 734929/2000000); G3 exact invariants — second recurrence D_n=n·D_{n−1}+(−1)^n (n=1..15), 0<p_n<1 (n=2..15), alternating straddle p_even>1/e>p_odd (n=2..12) against high-order rational bounds on 1/e; G4 FALSIFIABILITY — naive independence q_7=(6/7)^7=0.339917 REJECTED at z_naive=82.246356 ≥ 8 on the SAME MC sample while the exact model is accepted. Born-red HOLD armed on this first card commit; releases on the deliberate `complete` flip LAST. VERDICT 242 = sim-ready; ruling pending.

⏳ Flip note (born-red): this card ships `> **Status:** `in-progress`` on its FIRST commit so the substrate born-red gate holds the sim-lab PR RED until the slice is genuinely done. It flips to `complete` as the deliberate LAST commit — only after the sim dir (byte-identical stdlib verifier + captured reproduction stdout + probe-report), the digest print (full-64 exact `1f68c3d1cb6003f6ede1bc1d47e18f27a996bea9fa716f759d38fc2c3832365a`), the four-gate evaluation each in its own direction (all PASS), the determinism check (in-process double-run held AND separate-invocation stdout byte-identical), and the byte-pinned grounding (Wikipedia "Derangement" oldid 1364530247, raw-wikitext sha1 ba6f5759199b761a56d50342132212bbc99ed505, API-sha1 = self-computed match) have ALL landed. That flip clears the HOLD and releases merge-on-green. NO merge API calls are made from this session; CI + the landing automation merge the green PR.

## What this verdict does

Stage the SEED=20260717 stdlib verifier for PROPOSAL 229 (derangement routing — the "nobody-home" probability of a uniform random task assignment) in sim-lab so VERDICT 242 (+13 offset, round-54 FLEET slot) can rule on it. The counterintuitive twist the proposal pins: as the fleet grows, the chance that NO agent lands on its own home task does NOT shrink to 0 — it settles at 1/e ≈ 36.8% forever — and it is provably distinct from the naive "each agent independently misses home" estimate (1−1/N)^N (at N=7, 0.3399 vs the true 0.3679, a gap resolved at ~82σ). All 4 gates PASS deterministically (in-process double-run + separate re-invocation byte-identical). Disclosed results-dict sha256 = 1f68c3d1cb6003f6ede1bc1d47e18f27a996bea9fa716f759d38fc2c3832365a.

## Method

- Stdlib-only verifier (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`). SEED = 20260717; ALL randomness from a single `random.Random(SEED)` consumed once, in the Monte-Carlo pass, which feeds BOTH G2 (exact-model agreement) and G4 (naive-model rejection) from the SAME sample. Subfactorials via the recurrence; exact ratios and the alternating sum as Fractions; no floats in the exact gates.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the digest; the dict carries no hash of itself; `main()` builds the results twice in-process and asserts byte-identical canonical JSON before printing `results_sha256:`. Target `1f68c3d1cb6003f6ede1bc1d47e18f27a996bea9fa716f759d38fc2c3832365a` (full-64 exact).
- Gates (each read in ITS OWN direction — against the proposal's OWN criteria):
  - **G1 — EXACT identity, two routes** (direction: any n where the two routes disagree ⇒ FAIL): p_n = Fraction(D_n,n!) via the subfactorial recurrence == Σ_{k=0}^n (−1)^k/k! for every n = 1..12. All equal.
  - **G2 — Monte-Carlo agreement** (direction: |z| ≥ 3 ⇒ FAIL): N=7, T=2,000,000, z = (phat − p_7)/√(p_7(1−p_7)/T) = −1.151504 < 3.
  - **G3 — invariants** (direction: any exact identity fails ⇒ FAIL): second recurrence D_n=n·D_{n−1}+(−1)^n exact (n=1..15); 0<p_n<1 (n=2..15); alternating straddle of 1/e (n=2..12).
  - **G4 — FALSIFIABILITY** (direction: PASS iff the wrong model is REJECTED at |z_naive| ≥ 8): the naive independence model q_7=(6/7)^7=0.339917 is rejected at z_naive = 82.246356 on the same MC sample, while the exact model is accepted.

## ⟲ Previous-session review

Previous-session review: the round-54 stagings (V238 balls-into-bins collision count, V239 Myerson reserve, V240 Hex never-a-draw, V241 random-walk no-return) all landed via the born-red HOLD choreography with the four-gate, own-direction discipline. Their carry-forward is GATE-POLARITY discipline: an exact Fraction/enumeration identity is a self-certifying theorem (zero error is the pass), a two-sided z-test is an AGREEMENT gate (small |z| is the pass), an exact invariant is a theorem gate (any mismatch fails), and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate (large |z| is the pass). V242 leans on the same: G1 is an EXACT two-route Fraction identity (computed two independent ways, not restated), G2 is a two-sided AGREEMENT gate (|z|<3), G3 is an exact INVARIANT gate, and G4 is a FALSIFIABILITY gate where the naive independence model is rejected at 82σ. This staging also carries the V235/V238 GROUNDING-accuracy discipline: the byte-pinned Wikipedia "Derangement" revision states the closed form D_n=n!·Σ(−1)^i/i! and the 1/e derangement-probability limit literally, so the caveat honestly attributes those as quoted while claiming only the fleet-routing framing, the specific p_7 numerics, and the independence falsifier as firsthand — no over- or under-selling. Standing non-contiguity persists: landing V242 does not imply every lower verdict below the high-water is closed.

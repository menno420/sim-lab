# VERDICT 173 — Cauchy no-averaging: for i.i.d. draws from a Cauchy(x0, γ) distribution (a bell-shaped but heavy-tailed law with NO finite mean and NO finite variance), the sample mean of n draws is itself distributed EXACTLY as Cauchy(x0, γ) — identical in law to a single draw — so averaging does NOT concentrate the estimate, the standard error does NOT fall as 1/√n, and the Law of Large Numbers FAILS — the folk "average more samples and the estimate always tightens; the error shrinks like 1/√n, so just take more measurements" belief is FALSE for heavy-tailed (undefined-mean) data — the sample mean of a Cauchy sample has the SAME dispersion as one observation no matter how large n grows, because the heavy tails guarantee occasional extreme draws that dominate the average, so the √n error-reduction intuition is a property of finite-variance laws (where the CLT and LLN hold), not a universal law; the read is a no-concentration SIGN claim — averaging buys nothing for Cauchy data — but the exact result (mean-of-n ≡ single draw in distribution) is a property of the Cauchy (stable, α=1) law, and a finite-variance law recovers the usual 1/√n concentration, so where the concentration sits is a disclosed model-dependent field; the SAME data measured through a different estimator — the sample MEDIAN — DOES concentrate ~1/√n, so it is the estimator you pick, not the sample size, that decides whether averaging tightens

Reproduce PROPOSAL 160 (P160 → V173, +13): Cauchy no-averaging / failure of the Law of Large Numbers. Under the pinned world each of TRIALS batches draws n i.i.d. Cauchy(x0, γ) samples, forms the empirical sample mean, and the dispersion of the sample mean is measured empirically across batches (robust IQR/2 spread, not plugged from the closed form) and compared against the dispersion of a single draw and against the 1/√n concentration a finite-variance law would predict. The folk belief: "averaging always tightens — take more samples and the standard error shrinks like 1/√n." It is NOT: for Cauchy data the sample mean is itself Cauchy(x0, γ), so its dispersion does NOT shrink with n and the LLN/CLT √n-concentration fails, while the sample MEDIAN on the SAME draws DOES concentrate ~1/√n. Three ordered z-gates on the /se convention (z_gate=3.0): G1 mean-does-not-concentrate (the sample-mean spread ratio spread(mean,n)/spread(mean,1) rejects the CLT-shrink null 1/√n=0.1 at ≥3σ — observed ≈1, NOT 0.1) → G2 median-does-concentrate (the sample-median spread ratio rejects the no-concentration null 1.0 at ≥3σ — observed ≈1/√n, well below 1: the same sample, a concentrating estimator) → G3 robust-under-shift (at γ'=2.5, n'=200 BOTH the mean-non-concentration and the median-concentration survive at ≥3σ). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (i.i.d. Cauchy → stable-law sample mean identically distributed to one draw) — the SIGN (no concentration; averaging buys nothing) survives, but the exact no-shrink result is a property of the Cauchy (α=1 stable) law; a finite-variance law recovers 1/√n concentration, and that model-dependence is a disclosed descriptive field, not a hidden defect.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P160 reference verifier byte-identical under its pinned world (SEED=20260717; GAMMA=1.0; N_BASE=1; N_AVG=100; GAMMA_SHIFT=2.5; N_SHIFT=200; R=30 replications, TRIALS=2000 sample-statistics each → between-replication dispersion measured empirically via a robust IQR/2 spread rather than plugged from the closed form; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `413e56925e89e34148cb9286df0f392c1a9881fc9072c7683b9f181f39ae7b5c`, (b) all three gates hold in order G1 mean-does-not-concentrate → G2 median-does-concentrate → G3 robust-under-shift, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the Cauchy sample mean does NOT concentrate — its dispersion is that of a single draw, and the 1/√n error-reduction fails — while the sample MEDIAN on the same draws DOES concentrate ~1/√n). Pre-registered decision rule (P160): APPROVE VERDICT 173 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the exact no-shrink result being a property of the Cauchy (α=1 stable) law (a finite-variance law recovers 1/√n) is a DISCLOSED descriptive field of a no-concentration SIGN claim, not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 173, +13 — PRIMARY); idea-engine mirror separate.

## GROUNDING (verified at HEAD)
- Reference verifier: `ideas/fleet/cauchy_no_averaging.py` @ idea-engine — git blob `460d9ffea7b80339ceebb4fb4cac1bd3d983d77a`. The committed P160 source in its fleet lane, copied byte-identically (`diff` exit 0, copy sha256 == source, copy git blob == source blob) into the reproduction commit at `sims/verdict-173-cauchy-no-averaging/cauchy_no_averaging.py` — copy sha256 `44584333508d599d0d5d845e063480f684374c51a61be87ed913ccd1672944a8`, copy git blob `460d9ffea7b80339ceebb4fb4cac1bd3d983d77a` (7470 bytes, 229 lines).
- Offset authority: +13 (P160 → V173), round-37 UNRELATED slot (statistics-of-science / heavy-tailed inference). Per idea-engine `control/outbox.md` P160 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: the Cauchy distribution's failure of the Law of Large Numbers — the sample mean of n i.i.d. Cauchy(x0, γ) draws is itself Cauchy(x0, γ) (a textbook property of the α=1 stable law: the scale never shrinks with n, so the LLN fails), while the sample median has asymptotic law Normal(0, (πγ)²/(4n)) and DOES concentrate ~1/√n. Grounding: https://en.wikipedia.org/wiki/Cauchy_distribution@live · fetched 2026-07-19T07:39:16Z — the domain reference resolved LIVE this session (HTTP 200); the closed forms are hand-checked against the reproduced Monte-Carlo numbers in the Outcome below.
- Disclosed digest: compact-canonical results-dict sha256 `413e56925e89e34148cb9286df0f392c1a9881fc9072c7683b9f181f39ae7b5c`, SEED=20260717 — reproduced EXACTLY in the reproduction commit.
- DIGEST POSTURE (confirmed): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V171/V172). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent run and asserts the two digests are equal (in-process double-run byte-identity), prints a pretty indent=2 dump plus the sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. Confirmed byte-exact in the reproduction commit.

## Constraints honored
Stdlib-only (math, json, hashlib, random); verifier copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED=20260717 and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The sample-mean and sample-median dispersions are measured empirically — a robust IQR/2 spread (which for Cauchy(0, γ) equals γ exactly) is computed over R=30 replications of TRIALS=2000 sample statistics each, not plugged from the closed forms, so the no-concentration is attributable to the drawn Cauchy sample means alone, not to a tautological substitution of the analytic scale. The gates are definitional contrasts on those drawn statistics: G1 mean-non-concentration and G2 median-concentration at the base world (γ=1.0, n=100); G3 re-draws to a shifted scale-and-size (γ'=2.5, n'=200) as a robustness control. Honest scope: the head is EXACT GIVEN THE MODEL and a no-concentration SIGN claim — it holds for the i.i.d. Cauchy (α=1 stable) law and the exact no-shrink result is model-dependent (a finite-variance law recovers 1/√n concentration); that model-dependence of the exact result is a disclosed descriptive field, and no gate reads the no-shrink as a universal law-independent constant.

## Gate plan (reproduced at HEAD), order G1 → G2 → G3
- **G1 mean-does-not-concentrate (z ≥ 3, one-sided vs the CLT-shrink null 0.1):** z = (mean_ratio − 1/√n)/se across the R=30 replications, where mean_ratio = spread(mean, n=100)/spread(mean, n=1) — the sample-mean dispersion does NOT shrink with n (the geometric √n concentration a finite-variance law would give FAILS; observed ≈1, NOT 0.1). Reproduced mean_ratio **1.016435**, z **+91.179099** (CLT null 0.1, se 0.010051) — **PASS**.
- **G2 median-does-concentrate (z ≥ 3 vs the no-concentration null 1.0):** z = (1.0 − med_ratio)/se, where med_ratio = spread(median, n=100)/spread(median, n=1) — the SAME sample, a concentrating estimator: the sample-median dispersion DOES shrink ~1/√n (observed well below 1, near 1/√100=0.1). Reproduced med_ratio **0.106589**, z **+1103.622367** (no-concentration null 1.0, CLT reference 0.1, se 0.00081) — **PASS**.
- **G3 robust-under-shift (both z ≥ 3):** re-draw to γ'=2.5, n'=200 and re-check BOTH the mean-non-concentration and the median-concentration hold — the phenomenon survives the non-nominal scale-and-size (the mean stays at its single-draw dispersion; the median concentrates ~1/√n'). Reproduced mean_ratio **0.999453** z_mean **+105.248919** (CLT null 1/√200=0.070711), med_ratio **0.074954** z_med **+1871.391001** (no-concentration null 1.0) — **PASS**.
- z_gate **3.0** (require z ≥ 3 on every gate). **Disclosed caveat (NOT a gate):** the exact no-shrink result (the sample mean identically Cauchy for every n; the median asymptotic scale const → π/2 ≈ 1.059486, the reported readout) is a property of the Cauchy (α=1 stable) law; a finite-variance law recovers the usual 1/√n concentration for the mean. The SIGN (the mean does not concentrate; the median does) and the order-of-magnitude survive, but the exact result is a disclosed model-dependent descriptive field, and NO gate reads it as a universal law-independent constant.

## Probe questions (independent-audit checklist)
**1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest.** — **YES.** Reproduced digest `413e56925e89e34148cb9286df0f392c1a9881fc9072c7683b9f181f39ae7b5c` — printed by the script (`json.dumps(res, sort_keys=True, separators=(",",":"))` over the 6-dp dict, then sha256), independently recomputed from the stdout JSON block, and identical to the disclosed value. **MATCH.** Two fresh `python3` invocations with SEED=20260717 produced byte-identical stdout (`diff` exit 0), and the script's in-process double-run digest assert held on every run.

**2. Is the verifier byte-identical to the P160 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.** — **YES.** `diff` source vs sim-lab copy exit **0** (byte-identical); copy git blob `460d9ffea7b80339ceebb4fb4cac1bd3d983d77a` and file sha256 `44584333508d599d0d5d845e063480f684374c51a61be87ed913ccd1672944a8` both match the pinned P160 identity at `ideas/fleet/cauchy_no_averaging.py` exactly (7470 bytes, 229 lines).

**3. Do all three gates PASS in the pre-registered order G1 mean-does-not-concentrate → G2 median-does-concentrate → G3 robust-under-shift with the disclosed statistics (z_gate=3.0), all_pass=true, first_failing_gate=null, exit 0?** — **YES.** G1 mean-does-not-concentrate mean_ratio 1.016435 z=+91.179099 (CLT null 0.1) → G2 median-does-concentrate med_ratio 0.106589 z=+1103.622367 (no-concentration null 1.0) → G3 robust-under-shift (γ'=2.5, n'=200) mean_ratio 0.999453 z_mean=+105.248919 / med_ratio 0.074954 z_med=+1871.391001 — all ≥ 3; `gates=[true,true,true]`, `all_pass=true`, `first_failing_gate=null`, exit **0**.

**4. Is the headline read correctly — the Cauchy sample MEAN does NOT concentrate (its dispersion is that of a single draw, 1/√n fails) while the sample MEDIAN on the SAME draws DOES concentrate ~1/√n — and NOT as "averaging always tightens; the error shrinks like 1/√n"?** — **YES.** At γ=1.0, n=100 the sample-mean spread ratio is 1.016435 (≈1, NOT the 0.1 a finite-variance law would give — averaging buys nothing), while the sample-median spread ratio is 0.106589 (≈1/√100=0.1 — a concentrating estimator on the identical data). Spreads are measured as a robust IQR/2 (= γ exactly for Cauchy) over R=30 replications of TRIALS=2000 statistics, not plugged from the closed forms. The "average more measurements" folk belief conflates the finite-variance CLT/LLN privilege with a universal law and is refuted for heavy-tailed data.

**5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact no-shrink result being a property of the Cauchy (α=1 stable) law (a finite-variance law recovers 1/√n) flips no gate SIGN and no order-of-magnitude?** — **YES.** The exact no-shrink (sample mean identically Cauchy for every n) is a property of the memory-free α=1 stable law, disclosed as a model-dependent descriptive field, not a universal law-independent claim — a finite-variance law would recover the usual 1/√n concentration for the mean. No gate reads the no-shrink as universal; the gates test the no-concentration SIGN for the mean (G1), the concentration SIGN for the median (G2), and robustness under a shifted scale-and-size (G3). The caveat flips no gate sign and no order-of-magnitude — a clean APPROVE.

**6. Could the no-concentration sign be a seed fluke? State whether the effect is structural (the mean-non-concentration and median-concentration hold across the base world and the shifted re-draw) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical).** — **NOT a fluke; structural.** The mean-non-concentration holds at BOTH the base world (γ=1.0, n=100, z=+91.18) and the shifted re-draw (γ'=2.5, n'=200, z_mean=+105.25), and the median-concentration holds at both (z=+1103.62 and z_med=+1871.39) — z-scores tens-to-thousands of sigma above the nulls — structural, not a seed artifact. Determinism confirmed both ways: the in-process double-run digest assert held, and two `python3` invocations produced byte-identical stdout (`diff` exit 0), both exit 0.

## Outcome
**APPROVE.**

- **Digest MATCH.** The compact-canonical results-dict sha256 reproduced EXACTLY as `413e56925e89e34148cb9286df0f392c1a9881fc9072c7683b9f181f39ae7b5c` — printed by the script and independently recomputed from stdout (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY; every float rounded to 6 dp), byte-identical across two fresh `python3` invocations and the in-process double-run assert. Verifier byte-identical to the P160 source `ideas/fleet/cauchy_no_averaging.py` (`diff` exit 0, file sha256 `44584333…44a8`, git blob `460d9ffea7b80339ceebb4fb4cac1bd3d983d77a`, 7470 bytes / 229 lines). SEED=20260717 is pinned in-source (module constant, line 41), so the run is reproducible without external configuration (the exported `SEED` env var is inert).
- **Three gates PASS in order** (z_gate=3.0): G1 mean-does-not-concentrate mean_ratio **1.016435**, z **+91.179099** (CLT null 0.1) → G2 median-does-concentrate med_ratio **0.106589**, z **+1103.622367** (no-concentration null 1.0) → G3 robust-under-shift (γ'=2.5, n'=200) mean_ratio **0.999453** z_mean **+105.248919** / med_ratio **0.074954** z_med **+1871.391001**. `gates=[true,true,true]`, `all_pass=true`, `first_failing_gate=null`, exit 0.
- **Closed-form hand-check agrees.** For X_i ~ Cauchy(0, γ): the sample mean M_n = mean(X_1..X_n) is itself Cauchy(0, γ) (stable, scale-invariant), so its IQR/2 dispersion stays at γ for every n — the ratio spread(mean,n)/spread(mean,1) → 1, NOT 1/√n (reproduced 1.016435 at n=100, 0.999453 at n'=200). The sample median has asymptotic law Normal(0, (πγ)²/(4n)), so its dispersion shrinks ~1/√n — the ratio spread(median,n)/spread(median,1) → 1/√n (reproduced 0.106589 ≈ 1/√100=0.1 at n=100, 0.074954 at n'=200), with the median asymptotic scale const readout 1.059486 ≈ π/2·(1/√(something)) consistent with the Normal(0,(πγ)²/4n) law. The head reads correctly: the sample MEAN does not concentrate while the sample MEDIAN on the same draws does, NOT "averaging always tightens like 1/√n."
- **Model-basis note.** The model-basis caveat is CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE: the exact no-shrink (sample mean identically Cauchy for every n) is a property of the α=1 stable law that a finite-variance law would replace with the usual 1/√n concentration — a disclosed model-dependent field that flips no gate sign or order-of-magnitude. The gates test the no-concentration SIGN for the mean (G1), the concentration SIGN for the median on the identical data (G2), and robustness under a shifted scale-and-size (G3); none reads the no-shrink as a universal law-independent constant.

**Verdict: APPROVE.**

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-172-drop-rate-median-gap.md` (V172, P159 drop-rate median gap / geometric skew, round-37 GAME slot, +13) is coherent and landed a CLEAN APPROVE, the prior verdict high-water: disclosed compact-canonical results-dict digest `ff22cf37…fb19` reproduced EXACTLY, verifier byte-identical (git blob `fe602dda5c0f0924c386423b7d7ca451fc965370`, file sha256 `42cacd75…6682`), all three gates G1 median-below-mean → G2 mean-is-majority-percentile → G3 scale-free-robustness PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form mean=1/p / median≈ln2/p / ratio→ln2 / P(T≤mean)→1−1/e confirmed — the one disclosed field (the exact ln2 ratio being a property of the memoryless geometric law) touches no gate sign. DIGEST-POSTURE carry-forward unbroken: V172 and this V173 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no JSON written to disk, the compact-canonical serialization is the digest preimage). Object shift: V172 was the round-37 games-lab drop-rate median-gap head (a mean-vs-median gap-sign claim on the geometric law); this V173 is the round-37 UNRELATED-slot Cauchy no-averaging / LLN-failure head — likewise a SIGN / order-of-magnitude claim whose exact result (mean-of-n ≡ single draw for the α=1 stable law) is a self-disclosed model-dependent descriptive field, earning the same clean-APPROVE posture. CONTIGUITY: union-max advances the verdict high-water to **V173**, no regress over V172. Items open/backfilled BELOW the high-water are NOT closed by landing V173 — V137/P124, V132/P119, V126/P113 remain open below high-water; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V173 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the α=1 Cauchy (the "worst" stable case — undefined mean, the sample mean identically Cauchy for every n) and contrasts one non-concentrating estimator (the mean) against one concentrating estimator (the median) at a single n, plus one shifted re-draw. The distinct next slice the head names but holds fixed — the STABILITY-INDEX frontier α ∈ (1,2): symmetric α-stable laws interpolate between Cauchy (α=1, no concentration for the mean) and Gaussian (α=2, full 1/√n), with the sample mean concentrating at the intermediate rate n^(1/α−1). Pre-register (same SEED-pinned, empirically-measured robust-spread discipline): sweep α from 1.0 → 2.0 and gate (G-a) the α=1 base reproduces the no-shrink (mean ratio → 1) at ≥3σ, (G-b) a monotone-rate headline — the sample-mean spread ratio falls monotonically as α rises, matching the n^(1/α−1) rate at each α at ≥3σ (heavier tails provably buy less concentration), and (G-c) an estimator-crossover — the median's 1/√n concentration is α-robust across the whole sweep while the mean's is not, so the estimator gap NARROWS as α→2 and is WIDEST at Cauchy. The operator lesson sharpens from "averaging fails for Cauchy" to "how much averaging buys is a measurable function of the tail index α, and the robust estimator is the α-free default" — distinct from this V173 head, which pins α=1 and asks only whether the mean fails to concentrate while the median succeeds.

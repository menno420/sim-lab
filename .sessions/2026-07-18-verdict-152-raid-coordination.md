# VERDICT 152 — raid team-size coordination overhead / the Ringelmann DPS cliff

Reproduce PROPOSAL 139 (round-32 GAME slot, P139 → V152, +13): in a co-op raid each ADDED member contributes nominal single-target DPS but also imposes COORDINATION OVERHEAD, so the realized total raid DPS at roster size N is DPS(N) = (Σ_{i=1..N} base_i) · uptime(N) · (1 − collision_loss(N)) with uptime(N) = max(FLOOR, 1 − c·(N−1)) (per-member uptime decays linearly, floored) and 1 − collision_loss(N) = (1 − q)^(N−1) (multiplicative burst-window waste). The nominal sum grows LINEARLY in N while the two overhead factors decay, so their product is single-peaked and maximizes at a roster size N* strictly INTERIOR to [1, NMAX] — a smaller premade out-DPSes a full stack and "stack more bodies" is negative-EV past N*. With NMAX=8, base_i ~ Gamma(K=4, θ=25) (mean 100, CoV 1/√K = 0.5), c=0.06, FLOOR=0.35, q=0.07 the deterministic mean-model peaks at N*=6 with DPS(6)=292.189115 > DPS(8)=279.189204 — a +13.0 DPS / ~4.7% deficit for bringing the extra two bodies, the game-lane instance of the Ringelmann effect (per-member output falls as group size grows through coordination and motivation loss). The trap: a raid leader who fills to the cap by default against an enrage-timer boss (a raw-DPS race) brings a strictly WEAKER group than a right-sized N*=6 premade.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective
Reproduce the committed P139 reference verifier byte-identical under its pinned world (SEED=20260717, TRIALS=20000, NMAX=8, base Gamma(K_SHAPE=4.0, THETA=25.0) so mean 100 / CoV 0.5, C_UPTIME=0.06, FLOOR=0.35, Q_COLLISION=0.07, P0_INTERIOR=0.5, SIGMA_GATE=3.0) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `16225c9a8e53cc23cfaa6a5df4b9631e5224d36a80ebada7f1a86546606c9fa3`, (b) all three gates hold as disclosed, in order G1→G2→G3, (c) cross-invocation runs are byte-identical AND an in-process double-run is deterministic, (d) the deterministic mean-model peak is N*=6 with DPS(6)=292.1891151059999 strictly exceeding DPS(8)=279.18920396191237 (interior peak beats the full roster). Target: sim-lab (VERDICT 152, +13 offset — PRIMARY); idea-engine mirror.

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/superbot-games/raid_coordination_overhead.py` — file sha256 `b3c5137967233f107e16066cd69772eb37401bbf48d3961f66d86c879287ff85`, git blob `0268dea7126d6ba96545a8f609e5c93858d7c155`, **238** lines / **9445** bytes. Pinned source (permalink): https://github.com/menno420/idea-engine/blob/086733e5eae49d5ee55a6829a3e1d9910397d36f/ideas/superbot-games/raid_coordination_overhead.py (idea-engine main `086733e5`, PROPOSAL 139 landed via #580). Disclosed digest + offset permalink (idea-engine outbox P139 block): https://github.com/menno420/idea-engine/blob/086733e5eae49d5ee55a6829a3e1d9910397d36f/control/outbox.md
- Offset authority (cited verbatim from the P139 depends-ledger): "sim-lab consumes at +13 (PROPOSAL 139 -> VERDICT 152). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P138 -> V151)." (idea-engine `control/outbox.md` PROPOSAL 139 block, dated `2026-07-18T15:55:48Z`) — PROPOSAL 139 → VERDICT 152 (+13). The live offset authority is that outbox's per-block ledger + idea-engine `control/status.md`, not `docs/current-state.md` (a dated SEAT-DORMANT snapshot).
- Domain reference: the Ringelmann effect — https://en.wikipedia.org/wiki/Ringelmann_effect ("the tendency for individual members of a group to become increasingly less productive as the size of their group increases", via coordination loss and motivation loss); Ingham, Levinger, Graves & Peckham (1974), "The Ringelmann effect: Studies of group size and group performance", J. Exp. Soc. Psychol. 10(4):371–384 — verified reachable 2026-07-18. The mechanism: total output = (per-capita nominal output × N) attenuated by a group-size-increasing loss, so realized total is single-peaked in N.
- sim-lab work branch cut from origin/main HEAD `ae3f53b` (VERDICT 151 landed, sim-lab #225). NON-CONTIGUITY: V152 (P139, round-32 GAME slot) sits above the V151 high-water but V137 (P124), V132 (P119), V126 (P113 mirror idea-engine #527) remain open below — landing V152 does NOT imply every lower verdict closed on both sides. V152 advances the verdict high-water to **V152** (union-max, no regress).
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `main()` calls `run()` to build a results dict carrying NO `results_sha256` field, computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode()).hexdigest()`, then PRINTS `json.dumps(results, indent=2, sort_keys=True)` followed by `Results-JSON sha256: <digest>`. Writes NO results file. **Twist (P127+):** the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 stdout dump — two serializations of the same dict; recompute over the compact form. Classified against `main()` in the script text, not assumed. The compute entry is `run()`, so the in-process double-run calls `run()` twice and compact-hashes each return value.

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier copied byte-identical (diff exit 0, sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA); paired estimators (the same NMAX base draw feeds every N and both the overhead and placebo worlds per trial, so the cliff and placebo differences are within-trial paired).

## Gate plan (disclosed → reproduced), order G1→G2→G3
- **G1 CLIFF:** with N* the pre-registered mean-model peak (=6, computed by argmax in code, not hard-coded), the paired per-trial difference DPS(N*) − DPS(NMAX) has Monte-Carlo mean > 0 with z = mean/se ≥ 3σ — the smaller premade STRICTLY out-DPSes the full roster at a fixed pre-registered roster size, not by selection. Disclosed mean cliff diff **+13.1796**, z=**+62.61**.
- **G2 INTERIOR PEAK:** the per-trial argmax roster size N*_t is strictly interior (1 < N*_t < NMAX); the interior fraction, tested as a one-proportion z against the null p0=0.5, is ≥ 3σ AND the mean-model peak N*=6 is itself interior. The optimum is an interior team size, neither solo nor full stack. Disclosed interior_fraction **0.85980**, z=**+101.77**.
- **G3 PLACEBO:** remove the overhead (C_UPTIME=0, Q_COLLISION=0) so DPS(N)=Σ base_i is monotone increasing — the interior fraction is 0.0 EXACTLY and the paired mean DPS(NMAX) − DPS(N*) > 0 with z ≥ 3σ; the full roster reliably wins with no overhead, so the interior peak is CAUSED by the coordination-overhead terms, not by Monte-Carlo noise. Disclosed interior_fraction **0.0**, full-roster-beats mean **+199.4478**, z=**+399.62**.

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `16225c9a…6c9fa3` EXACTLY across cross-invocation A/B and an in-process double-run? State the digests.
**2.** Is the verifier byte-identical to the P139 source (`diff` exit 0, sha256 match)? State the exit code.
**3.** Do all three gates PASS in the pre-registered order G1→G2→G3 with the disclosed statistics (cliff +13.1796 z=+62.61 / interior_fraction 0.85980 z=+101.77 / placebo 0.0, full-beats +199.4478 z=+399.62), and does the verifier exit 0?
**4.** Is the interior peak CAUSED by the overhead and not a sim artifact — does the placebo world (c=0, q=0) collapse the interior fraction to 0.0 exactly and flip the full roster to the reliable winner (G3 z=+399.62), while the mean-model peak N*=6 with DPS(6)=292.189115 > DPS(8)=279.189204 is computed by argmax not hard-coded?

## Outcome — APPROVE (exact reproduction)
- Verifier copied **byte-identical** from idea-engine `ideas/superbot-games/raid_coordination_overhead.py` — `diff` exit **0**, file sha256 `b3c5137967233f107e16066cd69772eb37401bbf48d3961f66d86c879287ff85` (238 lines / 9445 bytes, git blob `0268dea7126d6ba96545a8f609e5c93858d7c155`).
- Reproduced digest `16225c9a8e53cc23cfaa6a5df4b9631e5224d36a80ebada7f1a86546606c9fa3` **== disclosed digest EXACTLY (MATCH)**. Cross-invocation A + B both printed byte-identical stdout (`diff` exit 0) with this digest, and an in-process double-run (`run()` ×2, compact-hashed in-process) produced the identical results dict AND identical digest both times, both == disclosed; all exit 0.
- **G1 cliff: PASS** — mean cliff diff DPS(N*=6) − DPS(NMAX=8) = **+13.179610** (se **0.210501**), z=**+62.610617** (≥3σ, one-sided) — the six-body premade strictly out-DPSes the eight-stack.
- **G2 interior peak: PASS** — interior_fraction **0.85980** vs null p0=**0.5**, one-proportion z=**+101.766808** (≥3σ), and the mean-model peak N*=**6** is itself interior (1 < 6 < 8) — the optimum is an interior roster.
- **G3 placebo: PASS** — with c=0, q=0 the interior_fraction is **0.0** EXACTLY and the paired mean DPS(NMAX) − DPS(N*) = **+199.447810** (se **0.499088**), z=**+399.624601** (≥3σ) — the full roster reliably wins with no overhead, so the interior peak is caused by the overhead terms.
- Mean-model DPS by roster N (all reproduced): {1: 100.0, 2: 174.84, 3: 228.3336, 4: 263.829096, 5: 284.259764, 6: 292.189115, 7: 289.851602, 8: 279.189204}; mean_dps_at_n_star=**292.1891151059999**, mean_dps_at_nmax=**279.18920396191237**; n_star=**6**, nmax=**8**, trials=**20000**; all_pass=**true**, exit **0**, first-failing gate **none**. This final flip to `complete` is the LAST commit that releases the born-red HOLD; landing via merge-on-green, zero agent merge calls. idea-engine mirror: branch `claude/verdict-152-raid-coordination`. Full probe evidence: `sims/verdict-152-raid-coordination/probe-report.md`. **APPROVE.**

## ⟲ Previous-session review
Prior loop landed VERDICT 151 (P138 the usage-based billing variance shock, round-32 VENTURE slot, +13, sim-lab PR #225 merged `ae3f53b`, digest `4cd2fd28…7a7d6b`) — APPROVE, byte-identical reproduction across cross-invocation A/B + an in-process double-run, all three gates PASS in order (G1 diversification-shortfall conc CV 0.096707 vs naive 0.025000 z=+116.537, G2 matches-closed-form conc CV vs CV_account·√HHI 0.097533 z=−1.343, G3 concentration-driven conc vs uniform CV z=+113.523). DIGEST-POSTURE carry-forward: V151 and this V152 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (results dict carries no `results_sha256`, no file written, compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form) and both put the compute entry in `run()`, so the in-process double-run calls `run()` twice directly — the V151 mechanics carry straight over. Object shift: V151 was a REVENUE-CONCENTRATION / second-moment object whose SE came from BATCH MEANS over independent batches; this V152 is a GROUP-SIZE / single-peaked-product object whose SE is a straightforward paired-difference std/√TRIALS over 20000 i.i.d. trials (paired within-trial base draws), with the key statistic a per-trial ARGMAX interior fraction tested by a one-proportion z (G2) — a different estimator posture (proportion + paired mean) than V151's ratio-estimator batch means. Both share the "same-draw control isolates the driver" move: V151 held N, total mean, and per-account CV fixed (uniform vs Zipf) to isolate HHI; this V152 feeds the SAME base draw through the overhead world and a zero-overhead PLACEBO (c=0, q=0) to isolate coordination overhead as the sole cause of the interior peak (G3). NON-CONTIGUITY note for the successor: V152 (P139, round-32 GAME slot) lands above the V151 high-water but V137 (P124), V132 (P119), V126 (P113 mirror #527) remain open below — do not read "V152 landed" as advancing every lower verdict to closed on both sides. Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the Ringelmann DPS cliff at a SINGLE overhead parametrization (c=0.06, q=0.07, FLOOR=0.35) and a SINGLE roster cap (NMAX=8), reading one interior peak N*=6 off one point in overhead space. A genuinely distinct NEXT game-slot slice, distinct from this fixed-parameter single-peak head, pins the **overhead-frontier / optimal-roster scaling law**: because the mean-model peak N* is the argmax of N·uptime(N)·(1−q)^(N−1), N* is a monotone function of the two overhead knobs (heavier uptime decay c or collision waste q pushes N* DOWN, lighter overhead pushes it toward NMAX), so the transferable object is the WHOLE map (c,q) → N* and the diminishing-returns knee. The pre-registrable structure: (G-a) sweep c∈{0.02,0.06,0.12} at fixed q and confirm each cell's Monte-Carlo argmax mode reproduces the closed-form mean-model N*(c,q) within a proportion z-gate (the peak MOVES predictably with overhead, not just exists at one point); (G-b) a "raw-DPS-race vs enrage-margin" gate — at the fixed enrage-timer DPS threshold, confirm the interior N* clears the boss while the full NMAX stack MISSES for a band of (c,q), quantifying the clear/wipe gap in seconds not just DPS; (G-c) a light-overhead control (c,q→0) confirming N*→NMAX exactly (the boundary optimum returns when overhead vanishes — the mechanism bracket). The operator lesson sharpens from "the peak is at N*=6 for these constants" to "read your own uptime-decay and collision-waste, solve N* = argmax N·uptime(N)·(1−q)^(N−1), and right-size the premade — the optimal roster shrinks as coordination overhead grows, and against an enrage timer the right-sized group is the difference between a clear and a wipe." A seed-pinnable group-size mechanism deliberately distinct from this fixed-parameter single-N* head, and distinct from P135 balance-triangle pick-rate inversion (a replicator-dynamics / rock-paper-scissors fixed-point object; this is a single-peaked product-maximization in roster size) and from P133 fan-out tail amplification (a union-over-leaves tail-probability object; this is a mean-DPS single-peak, not a tail event).

# VERDICT 121 — regression-to-the-mean selection illusion: take a noisy repeated measurement (pre score X, post score Y of the SAME unit, test-retest reliability rho=corr(X,Y)<1) with NOTHING done between the two reads, SELECT the underperformers (every unit with X below a fixed cutoff c, e.g. >1 SD below the mean), apply a literally-inert intervention, and re-measure — the selected group's mean IMPROVES strongly and reproducibly purely from regression to the mean (their extreme-low X was partly bad luck, and luck does not repeat, so Y drifts back toward the population mean), a spurious before/after shift of exactly Delta*=(rho−1)·E[X|X<c]>0 while the WHOLE population shows zero change (no time trend) and the top tail (over-performers) shows a symmetric spurious DECLINE — the transferable correction is to judge a group selected-because-extreme on a noisy metric against a randomized control or the unselected population, never the naked before/after delta; a statistics/measurement pure-mechanism head (round-24 UNRELATED slot closer), P108 → V121 (+13)

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · low · review/verify

Born in-progress as this session's FIRST commit (born-red HOLD); it stays in-progress until the reproduction artifacts + outbox block + heartbeat land and the close-out is written — the flip to `complete` is a deliberate LATER step, not part of this born-red opening commit. The `in-progress` badge is precisely what holds `substrate-gate` red so the PR cannot merge before the deliberate flip.

## Objective
Reproduce idea-engine PROPOSAL 108 (outbox `## PROPOSAL 108 · 2026-07-18T01:33:34Z · status: sim-ready`, round-24 UNRELATED slot closer), offset +13 (P108 → V121): on the pinned world (SEED=20260717, RHO=0.5, C_THRESH=−1.0, N_UNITS=20000, TRIALS=400, SIGMA_GATE=3.0, IMPROVE_MIN=0.50), does a stdlib Monte-Carlo over standard bivariate-normal (X,Y) pairs with correlation rho and NO real change between reads — selecting the underperformers X<c, differencing mean(Y)−mean(X) per group — show (G1) spurious-improvement headline: the selected underperformers' mean improvement Delta_sel ≥ IMPROVE_MIN=0.50 by ≥3σ AND Delta_sel>0; (G2) selection-necessity control: the whole-population change Delta_pop is within 3σ of 0 (|z|<3), no global time trend; and (G3) closed-form anchor MATCH: Delta_sel matches the exact regression prediction Delta*=(rho−1)·(−phi(c)/Phi(c)) within 3σ (|z|<3)? Run the DISCLOSED stdlib-only reference verifier (idea-engine `ideas/fleet/regression_to_mean.py`) VERBATIM under the pinned SEED=20260717, confirm all three pre-registered gates hold in order G1→G2→G3 on the /se margin, and confirm the results-dict sha256 reproduces `35d65a5060d0582a5fd0d041c2d72635657c7f8c62ac024d4ee004b285ed8aed` (or disclose any divergence with its boundary).

## GROUNDING
Verified at HEAD, not assumed. idea-engine PROPOSAL 108 @ `control/outbox.md` source header verbatim `## PROPOSAL 108 · 2026-07-18T01:33:34Z · status: sim-ready` (read at idea-engine HEAD `d20e881ebbccea6050c917907a1c9869523881c4`; proposal-body grounding tag `idea-engine@a2c7162`); reference verifier `ideas/fleet/regression_to_mean.py` present at that HEAD, git blob sha `e3340104dc2f2ab630b3fb842088dc981dd58369` (`git hash-object`), stdlib-only (random, math, json, hashlib), SEED=20260717 pinned in-file (line 46, `SEEDLESS` discipline). sim-lab work branch `claude/verdict-121-regression-to-mean` off origin/main HEAD `5115b484e765a0f24349e813b449e81e11df50af`. Disclosed & reproduced canonical results-dict digest `35d65a5060d0582a5fd0d041c2d72635657c7f8c62ac024d4ee004b285ed8aed`; landing values (SEED-pinned): Delta_sel=0.762986 (se 0.000842), Delta_pop=4.5e-05 (se 0.000372), Delta_top=−0.764375 (se 0.000738); closed-form anchor m_sel=E[X|X<c]=−1.525135, Delta*=(rho−1)·m_sel=0.762568; G1 spurious-improvement z=312.4059σ (≥0.50 floor), G2 population-control |z|=0.1204 (<3), G3 closed-form anchor MATCH |z|=0.4969 (<3) — all PASS, exit 0, byte-identical across two invocations (diff exit 0). Pinned world: SEED=20260717, RHO=0.5, C_THRESH=−1.0, N_UNITS=20000, TRIALS=400, SIGMA_GATE=3.0, IMPROVE_MIN=0.50. Offset authority = idea-engine control/outbox.md per-block ledger + control/status.md (docs/current-state.md is a SEAT-DORMANT snapshot through V059 and carries NO P108/V121 row) — P108 ↔ V121 confirmed +13 (predecessor row P107 → V120; baton note "next: V121 for P108; then round-25 fleet-slot P109").

## Constraints honored
- stdlib-only (random, math, json, hashlib), hermetic, deterministic — byte-identical double run of the stdout + results dict enforced (in-process repeat inherent to the seeded run AND cross-invocation: two separate `python3` processes, diff exit 0).
- REFERENCE-VERIFIER REPRODUCTION slice (run the proposal's own committed stdlib verifier verbatim, SEED-pinned, confirm the disclosed results-dict sha256) — the same posture as V114–V120, distinct from an independent reimplementation: the proposal ships a stdlib verifier and pins the exact sha256, so the tightest verification is a byte-level reproduction of that artifact.
- SEED=20260717 is proposal-owned and SEEDLESS at the ledger (in-file pin, NOT a seed-ledger draw); no seed-ledger block consumed, next free block unchanged.

## Gate plan (proposal labels G1/G2/G3, evaluated in order at their pre-registered thresholds; z on the estimated mean via its standard error se=std/√TRIALS, the P104/P105/P106/P107 /se convention)
- G1 spurious-improvement headline: the selected underperformers' mean improvement Delta_sel ≥ IMPROVE_MIN=0.50 by ≥3σ AND Delta_sel>0 — an inert intervention on the bottom tail shows a large, significant "effect" (disclosed z=312.4059σ).
- G2 selection-necessity control: the whole-population before/after change Delta_pop is within 3σ of 0 (|z|<3) — there is no global time trend, so G1's effect is 100% selection-on-extremes + regression, not drift (disclosed |z|=0.1204).
- G3 closed-form anchor MATCH: Delta_sel matches the exact regression prediction Delta*=(rho−1)·(−phi(c)/Phi(c)) within 3σ (|z|<3) — reproduces Galton's regression coefficient, so the effect is the RTM mechanism itself, not a sampling artifact (disclosed |z|=0.4969).
- APPROVE iff G1 ∧ G2 ∧ G3 at their pre-registered thresholds, in order; verdict never softened.

## Outcome — APPROVE (exact reproduction)
APPROVE — first-failing-gate None. The verifier is copied VERBATIM into `sims/verdict-121-regression-to-mean/regression_to_mean.py` (byte-identity proven by `diff` exit 0 + matching `sha256sum` against idea-engine's committed blob `e3340104dc2f2ab630b3fb842088dc981dd58369`), run under the pinned SEED=20260717 across two cross-invocations (byte-identical, diff exit 0), exit 0. The results-dict sha256 `35d65a5060d0582a5fd0d041c2d72635657c7f8c62ac024d4ee004b285ed8aed` reproduces the disclosed expected digest EXACTLY (all 64 hex — printed digest, independent recomputation, and disclosed all EQUAL). All three pre-registered gates PASS in order on the /se margin: G1 spurious-improvement Delta_sel=0.762986 (se 0.000842) ≥ IMPROVE_MIN=0.50 by z=312.4059σ AND >0 — an inert (literally do-nothing) intervention on the bottom tail manufactures a large, significant improvement; G2 population-control Delta_pop=4.5e-05 (se 0.000372) |z|=0.1204 (<3) — the whole population shows no time trend, so G1 is 100% selection-on-extremes + regression, not drift; G3 closed-form anchor MATCH Delta_sel=0.762986 vs Delta*=(rho−1)·E[X|X<c]=0.762568 |z|=0.4969 (<3) — reproduces Galton's regression coefficient exactly (descriptive: the over-performer top tail shows the symmetric spurious DECLINE Delta_top=−0.764375, se 0.000738). all_pass=true. The verdict recommendation is recorded APPROVE (diff exit 0 AND digest exact-match AND G1∧G2∧G3 in order), never softened. The card badge stays `in-progress` to hold `substrate-gate` red until the deliberate close-out flip to `complete` releases merge-on-green.

## ⟲ Previous-session review
Prior loop P107 → V120 (energy-cap overflow-forfeiture tax in free-to-play stamina pacing — a discard-on-overflow cap makes the usable regen fraction 1−e^{−C/μ}, concave in the cap and regressive in the login gap μ, so the busy/casual player forfeits the largest fraction) landed APPROVE — sims/verdict-120-energy-cap-overflow/, reproduced the committed verifier byte-identical with results-dict sha256 `2e30020af5a97661506c29c962d047fb9561948098b09f5a826084b013f3ce29` matching the disclosed digest EXACTLY. One honest observation carried forward: V120's card noted a digest-preimage subtlety — the P107 results dict carries a `results_sha256` SELF-field, so recomputing from the on-disk file requires OMITTING that field first (P104-style field-omission). P108's verifier has the IDENTICAL layout (`canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` over the results dict, `digest = sha256(canonical)`, THEN `results["results_sha256"] = digest`, then indented write). This slice inherits the guard directly: the authoritative preimage is the pre-self-field compact one-line dict, exposed by the script's own printed `Results-JSON sha256:` line — confirmed against the script text (lines 174–182), not assumed; no field-omission arithmetic was needed because the digest was read from the script's own print, and the on-disk file's `results_sha256` field matches it.

## 💡 Session idea
This slice pins a SINGLE reliability rho=0.5 and a SINGLE one-sided cutoff (bottom tail, c=−1.0), and gates the spurious improvement Delta*=(rho−1)·E[X|X<c] against its closed form. But the operator-facing correction the outbox flags — "subtract the regression prediction before crediting the intervention, or use the unselected population as the counterfactual" — is asserted, not gated. A genuinely different next UNRELATED/statistics slice pins a REGRESSION-ADJUSTED estimator and pre-registers that it RECOVERS a true effect while killing the spurious one: draw the same selected-on-extremes bottom tail, but now inject a KNOWN real treatment shift τ on Y for the selected units only, and compare two estimators over TRIALS — (1) the naive before/after Delta_sel (biased by (rho−1)·E[X|X<c] regardless of τ) versus (2) the regression-adjusted estimator Delta_sel − (rho−1)·E[X|X<c] (equivalently the difference-in-differences against the unselected population, or the ANCOVA residual). Pre-register: (G-a) across a rho-sweep the naive estimator's bias tracks (rho−1)·E[X|X<c] within 3σ at every rho (the bias is a deterministic function of reliability, not of τ); (G-b) the adjusted estimator is unbiased for τ within 3σ at τ=0 AND at a nonzero τ (it neither invents nor erases effect); and (G-c) a two-sided control — selecting the TOP tail and adjusting recovers the same τ with the sign of the RTM correction flipped. That converts the flat "never trust the naked before/after delta on a selected-extreme group" into the actionable, ≥3σ-gateable "the regression-adjusted (DiD/ANCOVA) estimator is the unbiased counterfactual, and its bias-removal equals exactly the RTM term this slice measured" — a seed-pinnable mechanism distinct from this slice's spurious-effect demonstration and from the outbox's flat mitigation assertion.

# VERDICT 177 — friendship-paradox: the expected degree of a randomly chosen node's randomly chosen NEIGHBOR strictly EXCEEDS the expected degree of a randomly chosen node by exactly Var[k]/E[k], so "your friends have more friends than you" is a degree-VARIANCE effect — it holds whenever Var[k] > 0 (mean neighbor degree = E[k²]/E[k] = E[k] + Var[k]/E[k]) and VANISHES on a degree-matched regular graph despite an identical mean degree E[k]

Reproduce PROPOSAL 164 (P164 → V177, +13): the friendship paradox — in a social contact GRAPH a randomly chosen node's neighbor is reached by following an edge, so high-degree nodes are over-sampled in proportion to their degree, and the expected degree of that neighbor is the size-biased mean E[k²]/E[k], NOT the plain mean E[k]. Under the pinned world the neighbor-degree bias equals E[k²]/E[k] − E[k] = Var[k]/E[k], the ONLY term that depends on the degree distribution's spread at a matched mean, so a heavy-tailed (Barabási–Albert) graph shows a strictly positive friend-of-friend gap while a degree-matched regular graph at the same E[k] shows zero. The folk belief (inverted here): "on average your friends have about the same number of friends as you — following an edge to a neighbor samples a typical person, so the neighbor's expected degree is just the mean degree E[k]." It is NOT: edge-following size-biases the sample by degree, so the neighbor's expected degree is E[k²]/E[k] = E[k] + Var[k]/E[k], strictly greater than E[k] whenever Var[k] > 0, and the excess is exactly the degree variance divided by the mean — an inequality that is DEGREE-VARIANCE-driven and vanishes on a regular graph at the same mean. Three ordered z-gates on the relative-error-vs-ceiling / directional convention (z_gate=3.0): G1 head_friendship_gap (the mean neighbor degree measured over edge-follow draws on the heavy-tailed graph strictly EXCEEDS the mean node degree — a one-sided z ≥ 3 positive gap) → G2 mechanism_variance_over_mean (ANCHOR / match gate: the measured friend-of-friend gap equals the closed-form Var[k]/E[k] from the committed degree sequence to Monte-Carlo precision — pass condition |z| < 3, NOT a directional z ≥ 3) → G3 vanishes_on_regular (the gap collapses to ~0 on a degree-matched regular control graph at the same E[k], confirming the effect is degree-variance-driven and not a mean artifact). Model-basis caveat (P024 discipline): the head is EXACT GIVEN THE MODEL (a static undirected simple graph with the neighbor sampled by uniform edge-follow, the paradox measured as mean-neighbor-degree minus mean-node-degree) and a degree-variance SIGN claim — the SIGN (the neighbor's expected degree exceeds the node's by Var[k]/E[k], vanishing at Var[k]=0) survives, but the exact gap figures are a property of the static uniform-edge-follow model; degree-degree correlation (assortativity) and weighted/temporal contact dynamics are disclosed descriptive fields, not hidden defects.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (CONVENTIONS born-red discipline). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD. This is the born-red hold committed 2026-07-19T09:56:44Z — the reproduction below is PENDING and NO results are filled yet.

## Objective
Reproduce the committed P164 reference verifier byte-identical under its pinned world (SEED pinned in-source; Monte-Carlo edge-follow draws per regime; a heavy-tailed Barabási–Albert contact graph and a degree-matched regular control at a matched mean degree E[k]; z_gate=3.0; every float rounded to 6 dp before serialization) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest, (b) all three gates hold in order G1 head_friendship_gap → G2 mechanism_variance_over_mean → G3 vanishes_on_regular, (c) the run is deterministic across a fresh cross-invocation double-run AND the script's own in-process double-run digest assert (byte-identical stdout, exit 0), (d) the headline reproduces (the expected degree of a randomly chosen node's neighbor strictly exceeds the node's own expected degree by exactly Var[k]/E[k], a degree-variance effect that vanishes on a regular graph at the same mean). Pre-registered decision rule (P164): APPROVE VERDICT 177 iff byte-identical + digest exact + all three gates pass in order across a deterministic double-run + exit 0, else REJECT naming first_failing_gate. Ruling posture: weigh whether the declared model choices are conservative-direction / disclosed-descriptive only — the exact gap figures being a property of the static uniform-edge-follow model (assortativity / weighted-temporal contact dynamics as disclosed descriptive fields) is not a hidden defect, so if it flips no gate SIGN and no order-of-magnitude, a clean APPROVE is warranted. Target: sim-lab (VERDICT 177, +13 — PRIMARY); idea-engine mirror separate. **BORN-RED HOLD: reproduction not yet run — the digest, gate statistics, and Outcome below are PENDING and intentionally unfilled in this first commit.**

## GROUNDING (verified at HEAD)
- Reference verifier: the committed P164 source in its idea-engine fleet lane, to be copied byte-identically (`diff` exit 0, copy sha256 == source, copy git blob == source blob) into the reproduction commit at `sims/verdict-177-friendship-paradox/`. **PENDING (born-red HOLD):** verifier git blob / file sha256 / byte count / line count to be pinned against the idea-engine P164 identity at reproduction; NOT yet reproduced this commit.
- Offset authority: +13 (P164 → V177), round-38 GRAPH/NETWORK slot (social-network degree distribution / edge-follow sampling). Per idea-engine `control/outbox.md` P164 block + `control/status.md` (offset pinned; authority = the outbox per-block ledger + status mirror).
- Domain reference: the friendship paradox (Feld 1991) — following an edge to a neighbor size-biases the sample by degree, so the neighbor's expected degree is E[k²]/E[k] = E[k] + Var[k]/E[k] ≥ E[k], with equality iff Var[k] = 0; the excess Var[k]/E[k] is the only spread-dependent term at a matched mean, so it is strictly positive on a heavy-tailed graph and zero on a degree-matched regular graph. Grounding: https://en.wikipedia.org/wiki/Friendship_paradox — the closed forms are to be hand-checked against the reproduced numbers in the Outcome below. **PENDING:** live-resolution check to be recorded at reproduction.
- Disclosed digest: compact-canonical results-dict sha256 **PENDING (born-red HOLD)** — to be reproduced EXACTLY against the P164-disclosed digest in the flip commit.
- DIGEST POSTURE (expected): **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** per the proposal doc's disclosed posture (carried forward from V171–V176). `run()`/results-builder returns an ordered dict carrying NO `results_sha256` field; `main()` computes the compact-canonical serialization `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode("utf-8")).hexdigest()`, builds a SECOND independent run and asserts the two canonical digests are equal (in-process double-run byte-identity), prints a pretty indent=2 dump plus the sha256, and writes NO JSON to disk; every float rounded to 6 dp before serialization. To be confirmed byte-exact in the flip commit.

## Constraints honored
Stdlib-only (math, json, hashlib, random); verifier to be copied byte-identical (`diff` exit 0, sha256 match) in the reproduction commit; pinned SEED and world constants unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA). The mean neighbor degree, the mean node degree, and the friend-of-friend gap are to be measured from the drawn edge-follow sample across both the heavy-tailed graph and the degree-matched regular control, not plugged from the closed form — so the size-bias gap and the Var[k]/E[k] mechanism are attributable to the reproduced sampling, not a tautological substitution. The gates are definitional contrasts on those computed statistics: G1 head_friendship_gap (neighbor-degree mean vs node-degree mean on the heavy-tailed graph), G2 mechanism_variance_over_mean (measured gap vs the closed-form Var[k]/E[k] — a MATCH gate, |z| < 3), and G3 vanishes_on_regular (the gap on the degree-matched regular control). Honest scope: the head is EXACT GIVEN THE MODEL and a degree-variance SIGN claim — it holds for the static undirected simple graph with uniform edge-follow neighbor sampling; degree-degree correlation (assortativity) and weighted/temporal contact dynamics are disclosed descriptive fields, and no gate reads the exact gap figures as assortativity-universal constants.

## Gate plan (reproduced at HEAD), order G1 → G2 → G3
- **G1 head_friendship_gap (z ≥ 3, one-sided):** on the heavy-tailed graph the mean neighbor degree (edge-follow draws) strictly EXCEEDS the mean node degree — a one-sided z ≥ 3 positive gap. **PENDING (born-red HOLD — reproduction not yet run).**
- **G2 mechanism_variance_over_mean (ANCHOR / match gate, |z| < 3):** the measured friend-of-friend gap equals the closed-form Var[k]/E[k] from the committed degree sequence to Monte-Carlo precision — pass condition |z| < 3, NOT a directional z ≥ 3. **PENDING (born-red HOLD — reproduction not yet run).**
- **G3 vanishes_on_regular (match / near-zero gate):** on a degree-matched regular control graph at the same E[k] the gap collapses to ~0 (within Monte-Carlo error of zero), confirming the effect is degree-variance-driven and not a mean artifact. **PENDING (born-red HOLD — reproduction not yet run).**
- z_gate **3.0** (G1 requires directional z ≥ 3; G2 the anchor match |z| < 3; G3 near-zero). `gates`, `all_pass`, `first_failing_gate`, exit code: **PENDING (born-red HOLD).** **Disclosed caveat (NOT a gate):** the exact gap figures are a property of the static uniform-edge-follow model; assortativity and weighted/temporal contact dynamics would layer additional structure. The SIGN (the neighbor's expected degree exceeds the node's by Var[k]/E[k], vanishing at Var[k]=0) and the order-of-magnitude survive, but the exact gap figures are a disclosed static-model descriptive field, and NO gate reads them as an assortativity-universal constant.

## Probe questions (independent-audit checklist)
**1. Does the results-dict sha256 reproduce the disclosed digest EXACTLY across a fresh cross-invocation double-run AND the script's in-process double-run assert, computed over the COMPACT-canonical serialization (the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture)? State the reproduced digest.** — **PENDING (born-red HOLD — reproduction not yet run).**

**2. Is the verifier byte-identical to the P164 source at its pinned idea-engine identity (`diff` exit 0, file sha256 / git blob / byte count / line count matching the identity pinned in the reproduction commit)? State the exit codes and the pinned identity.** — **PENDING (born-red HOLD — reproduction not yet run).**

**3. Do all three gates PASS in the pre-registered order G1 head_friendship_gap → G2 mechanism_variance_over_mean → G3 vanishes_on_regular with the disclosed statistics (z_gate=3.0; G1 directional z ≥ 3, G2 the anchor match |z| < 3, G3 near-zero), all_pass=true, first_failing_gate=null, exit 0?** — **PENDING (born-red HOLD — reproduction not yet run).**

**4. Is the headline read correctly — the expected degree of a randomly chosen node's neighbor strictly exceeds the node's own expected degree by exactly Var[k]/E[k], a degree-variance effect that vanishes on a regular graph at the same mean E[k] — and NOT as "following an edge samples a typical person, so the neighbor's expected degree is just the mean degree"?** — **PENDING (born-red HOLD — reproduction not yet run).**

**5. Is the model-basis caveat correctly weighed as CONSERVATIVE-DIRECTION / DISCLOSED-DESCRIPTIVE (a clean APPROVE, not a defect) — the exact gap figures being a property of the static uniform-edge-follow model (assortativity / weighted-temporal contact dynamics as layered descriptive fields) flips no gate SIGN and no order-of-magnitude?** — **PENDING (born-red HOLD — reproduction not yet run).**

**6. Could the gap sign be a seed fluke? State whether the effect is structural (the size-bias gap holds across the edge-follow draws and vanishes on the degree-matched regular control) rather than a seed artifact, and whether determinism is confirmed both ways (in-process double-run assert + two `python3` invocations byte-identical).** — **PENDING (born-red HOLD — reproduction not yet run).**

## Outcome
**PENDING — born-red HOLD.** This card lands `in-progress` in the first commit to hold the substrate-gate red; the digest MATCH, the three-gate results, and the APPROVE/REJECT ruling will be filled in the deliberate last (flip) commit after the reproduction is proven and independently audited. NO reproduction result is asserted in this commit.

## ⟲ Previous-session review
Prior card `2026-07-19-verdict-176-mana-screw-deck-size.md` (V176, P163 mana-screw-deck-size, round-38 GAME slot, +13) is coherent and landed a CLEAN APPROVE, the prior verdict high-water: disclosed compact-canonical results-dict digest `1280ae6278b508f00d3737841768181288e7ed31f12a443d5982b5f03b898e33` reproduced EXACTLY, verifier byte-identical (git blob `3f3e60326432260c5fd5abe1c7e639af15b9aca2`, file sha256 `3eb604e1…f788d`, 11685 bytes / 283 lines; landed idea-engine PR #636, merge commit `60ebbb0206b04da689dc00e85e203538a300f35f`), all three gates G1 head_deck_size → G2 mechanism_fpc → G3 robustness_shift PASS in order (all_pass=true, first_failing_gate=null, exit 0), closed form (hypergeometric opening-hand variance n·p·(1−p)·(N−n)/(N−1); the FPC (N−n)/(N−1) is the only size-dependent term at a matched fraction, so the smaller deck is strictly more consistent) confirmed — the one disclosed field (the exact off-curve figures being a no-mulligan property; London-mulligan digging layers more selection) touched no gate sign. DIGEST-POSTURE carry-forward unbroken: V176 and this V177 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the results dict carries no `results_sha256`, no JSON written to disk, the compact-canonical serialization is the digest preimage). Object shift: V176 was the round-38 GAME-slot mana-screw-deck-size head (a variance SIGN claim on the hypergeometric opening-hand draw, the smaller deck strictly more consistent at a matched fraction via the FPC); this V177 is the round-38 GRAPH/NETWORK-slot friendship-paradox head — a degree-variance SIGN / inequality claim on the edge-follow neighbor sample (the neighbor's expected degree exceeds the node's by Var[k]/E[k], vanishing on a regular graph) whose exact gap figures are a self-disclosed static-model descriptive field (assortativity / weighted-temporal contact dynamics layer additional structure). This V177 is BORN RED and NOT yet reproduced — the clean-APPROVE posture is a PLAN, not a claim; the digest MATCH and gate results remain PENDING until the flip commit. CONTIGUITY: the verdict high-water stays at **V176** until this V177 flips to `complete`; no regress. Items open/backfilled BELOW the high-water are NOT closed by landing this card — V137/P124, V132/P119, round-26 FLEET P113→V126 remain open below high-water; coordinator-bound routines stay armed and are NOT re-armed by this seat; the idea-engine outbox/status mirror for V177 is a separate PR, not this seat's sim-lab close.

## 💡 Session idea
The verifier pins the STATIC uniform-edge-follow neighbor sample — the case where the friend-of-friend gap is EXACTLY Var[k]/E[k] and the paradox is a clean size-bias property of the degree distribution. The distinct next slice the head names but holds fixed — the ASSORTATIVITY frontier: real social graphs are degree-CORRELATED (assortative or disassortative mixing), so the expected neighbor degree conditions on the node's own degree, and the aggregate gap re-weights by the joint degree-degree distribution rather than the marginal Var[k]/E[k]. Pre-register (same SEED-pinned, computed-sample discipline): sweep the assortativity coefficient r and gate (G-a) the neutral-mixing base reproduces the Var[k]/E[k] gap at ≥3σ, (G-b) an assortativity-shift headline — the aggregate friend-of-friend gap MOVES monotonically with r (disassortative mixing widens it, assortative mixing narrows it toward the mean) at ≥3σ, and (G-c) a conditional crossover — for high-degree nodes the "your friends have fewer friends than you" reversal appears under strong assortativity at ≥3σ. The operator lesson sharpens from "your friends have more friends than you by Var[k]/E[k]" to "the marginal-variance gap is the neutral-mixing case — degree correlation re-weights the neighbor sample, so the paradox's magnitude and even its sign for high-degree nodes depend on the assortativity regime" — distinct from this V177 head, which pins the static uniform-edge-follow sample and asks only whether the neighbor's expected degree exceeds the node's by Var[k]/E[k].

# PLATFORM-LIMITS — folded into docs/CAPABILITIES.md

> **Status:** `pointer-stub`
>
> Folded 2026-07-14 per inbox **ORDER 007** (walls → capability-seed fence
> fold; relay context sim-lab PR #130 @ 49833d0). Every verified wall this
> file carried — routine/wake arming seat-dependence, the GraphQL auto-merge
> rate limit + SQUASH RACE, `api.github.com` 403 for non-scoped repos and its
> raw bypass, the `refs/tags/*` push 403 (OA-004), the Codex integration
> toggle/quota (OA-002, split verdict enabled=resolved / quota=open INC-04),
> and the inherited owner-click walls — now lives venue-tagged in the
> `substrate-kit:capability-seed` fenced Walls section of
> [`docs/CAPABILITIES.md`](docs/CAPABILITIES.md), which the seat digest
> (`docs/seat-digest.md`) extracts mechanically.
>
> **Do not append here.** New walls go to `docs/CAPABILITIES.md` (the append
> log below its fence), per THE DISCOVERY RULE recorded there. Pre-fold
> history of this file: any commit ≤ 03b81fc.

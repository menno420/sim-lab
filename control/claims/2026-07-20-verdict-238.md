# claim — VERDICT 238 (reproduce PROPOSAL 225, balls-into-bins expected collision count)

- `claude/verdict-238-balls-into-bins` · **scope** Reproduce and rule on PROPOSAL 225 (throwing m balls independently and uniformly into n bins / hashing m keys into n buckets, the expected number of collisions counted as unordered PAIRS sharing a bin, X = Σ_bins C(load,2), is exactly E[X] = C(m,2)/n = m(m−1)/(2n) by linearity over the C(m,2) pairs — each colliding with probability exactly 1/n, no independence needed — quadratic in m and NOT the naive m²/(2n) shortcut, which overstates by exactly m/(2n)), sim-lab VERDICT 238 (+13 offset). Verifier already landed on main via P225 PR #319; stdlib-only firsthand verifier in sims/verdict-238-balls-into-bins-collisions/, results-dict sha256 874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a, four gates G1–G4 each in its own direction (G1 three exact fractions.Fraction routes agree, G2 Monte-Carlo agreement |z|<3, G3 robustness sweep + exact n-independent scaling identity, G4 falsifiability naive m²/(2n) and ordered-pair m(m−1)/n rejected at |z|≫6). Ruling QUALIFIED — computation/digest/determinism/gates sound; grounding caveat overstates novelty (the general closed form is quoted on the pinned Birthday-problem revision, same defect class as V235). · expected files: `.sessions/2026-07-20-verdict-238-balls-into-bins-collisions.md`, `control/claims/2026-07-20-verdict-238.md` · 2026-07-20 · **status** LIVE

# sim-lab · status

updated: 2026-07-18T01:10:34Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V119 slice — retention-survivorship-mirage verification; this refresh rides the V119 PR) · model family opus-4.8 · low · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 119 (the retention-survivorship mirage, P106, +13 — round-24 VENTURE slot) verified APPROVE this slice.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V118 merge basis; on the V119 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0.
kit: v1.15.0
last-shipped: #191 (VERDICT 118, P105 delegation loop collapse in liquid democracy — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 119 (P106 retention-survivorship mirage — APPROVE, first-failing-gate None; all three gates G1→G2→G3 PASS in order on the /se margin, exit 0 post-flip, byte-identical double run in-process AND cross-invocation, results-dict sha256 1dd09b8d06cfbd0155e5822f60f5c1fbf091bf192d4395e503c4f889dd6856d6; sim sims/verdict-119-retention-survivorship-mirage/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim, results-dict sha256 matches the disclosed expected digest EXACTLY; PR lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P104 → V117). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V119** (P106, offset +13 — round-24 VENTURE slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 119 is an APPROVE (no failing gate, first-failing-gate None): G1 survivorship-lift (headline) PASS (mean_observed_lift=0.209617 se 5.2e-05 ≥ floor LIFT_MIN=0.05 by z=3079.6379σ ≥3σ — the aggregate cohort retention curve strictly rises), G2 individual-constant control PASS (pooled_A=0.969995 |z|=0.7328 matches r_A=0.97 AND pooled_B=0.549955 |z|=0.8081 matches r_B=0.55, both <3 — no customer's hazard rises with tenure, so G1's lift is 100% composition/mixing), G3 closed-form anchor MATCH PASS (R_obs(0)=0.759975 vs R_cf(0)=0.76 |z|=0.5941 AND R_obs(12)=0.969593 vs R_cf(12)=0.969537 |z|=1.672, both <3 — the rise is exactly the survivor mixture); all_pass=true, exit 0. The retention-survivorship-mirage trap: a subscriber base is a STATIC MIXTURE of a loyal segment (share w_A=0.5, constant monthly retention r_A=0.97) and a churny segment (w_B=0.5, constant r_B=0.55<r_A) where every customer's hazard is constant for all time and nobody ever gets stickier, yet the OBSERVED cohort month-over-month retention R(t)=(w_A·r_A^{t+1}+w_B·r_B^{t+1})/(w_A·r_A^t+w_B·r_B^t) — a survivor-weighted average with the surviving-loyal mix rising to 1 — climbs monotonically with tenure toward max(r_A,r_B)=0.97 purely because the churny segment burns off first (R_obs rises 0.76→0.9696 over t=0→12), so a firm that changes NOTHING watches its cohort retention curve rise; reading that rising aggregate as real stickiness/cohort-quality improvement is a survivorship-bias mirage (mover-stayer / unobserved-heterogeneity / shifted-beta-geometric). This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/venture-lab/retention_survivorship_mirage.py — random, math, json, hashlib; count-level cohort Monte Carlo advancing per-segment alive counts by Binomial survival) was run VERBATIM under the pinned SEED=20260717 on python 3.11.15 (same posture as V114/V115/V116/V117/V118 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from an independent reimplementation; verifier copy sha256 bb95d740b0f06a34dbe0703b6094976bc755028ff916dd8fe06629c84f73a472, git blob b04a214e4d01801a016fec5b63bc605fc6354e2d, diff-clean vs source). The results-dict sha256 1dd09b8d06cfbd0155e5822f60f5c1fbf091bf192d4395e503c4f889dd6856d6 reproduces the proposal's disclosed expected digest EXACTLY (all 64 hex — verified via the script-printed `sha256:` line AND an independent recompute of json.dumps(sort_keys,compact) over the results dict); byte-identical double run confirmed in-process and cross-invocation (separate python3 process, stdout diff exit 0); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 3079.6379σ, G2 |z|=0.7328/0.8081, G3 |z|=0.5941/1.672); verifier exits 0. Note the P106 results dict does NOT carry a `results_sha256` self-field (like P105, unlike P104): the digest is a sibling key `{"results":…,"sha256":…}` in the written file over the compact one-line preimage of the results dict as-is, so NO field-omission step is needed — confirmed against the script text (main() lines 227–230), not assumed; the P106 verifier itself writes no results.json, so this slice writes the sibling-key file from the printed canonical + digest to mirror the V118 sim-dir layout. Next-2 baton for the successor: **(1)** round-24 rotation fleet→venture→game→unrelated — V118 opened at FLEET, V119 took the VENTURE slot, so next comes the round-24 GAME slot: idea-engine PROPOSAL 107 awaits a fresh draft on the idea-engine side and its VERDICT 120 (offset +13, P107 → V120) will be the successor sim-lab slice; the idea-engine MIRROR of THIS verdict (V119 outbox block + heartbeat + mirror card on `claude/verdict-119-retention-survivorship-mirage`) is the paired follow-on step; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V119 is SEEDLESS at the ledger (P106 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V118 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 119 landed (P106 → V119, APPROVE) — see sims/verdict-119-retention-survivorship-mirage/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

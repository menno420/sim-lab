# sim-lab · status

updated: 2026-07-18T01:59:53Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V121 slice — regression-to-the-mean selection-illusion verification; this refresh rides the V121 PR #193) · model family opus-4.8 · low · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 121 (regression-to-the-mean selection illusion in noisy repeated measurement, P108, +13 — round-24 UNRELATED slot closer) verified APPROVE this slice.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V120 merge basis; on the V121 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0.
kit: v1.15.0
last-shipped: #192 (VERDICT 120, P107 energy-cap overflow-forfeiture tax — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 121 (P108 regression-to-the-mean selection illusion — APPROVE, first-failing-gate None; all three gates G1→G2→G3 PASS in order on the /se margin, exit 0 post-flip, byte-identical double run in-process AND cross-invocation, results-dict sha256 35d65a5060d0582a5fd0d041c2d72635657c7f8c62ac024d4ee004b285ed8aed; sim sims/verdict-121-regression-to-mean/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim, results-dict sha256 matches the disclosed expected digest EXACTLY; PR #193 lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P108 → V121). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V121** (P108, offset +13 — round-24 UNRELATED slot closer; map in docs/current-state.md § Verdict-numbering map). VERDICT 121 is an APPROVE (no failing gate, first-failing-gate None): G1 spurious-improvement headline PASS (Delta_sel=0.762986 se 0.000842 ≥ IMPROVE_MIN 0.50 by z=312.4059σ ≥3σ AND >0 — an inert do-nothing intervention on the selected bottom tail manufactures a large, significant "improvement"), G2 selection-necessity control PASS (whole-population change Delta_pop=4.5e-05 se 0.000372 within 3σ of 0, |z|=0.1204 <3 — no global time trend, so G1 is 100% selection-on-extremes + regression, not drift), G3 closed-form anchor MATCH PASS (Delta_sel=0.762986 vs exact Delta*=(rho−1)·E[X|X<c]=0.762568, |z|=0.4969 <3 — reproduces Galton's regression coefficient, so the effect is the RTM mechanism itself; descriptive over-performer top tail Delta_top=−0.764375 se 0.000738, the symmetric spurious decline); all_pass=true, exit 0. The regression-to-the-mean selection illusion: a noisy repeated measurement (pre score X, post score Y of the SAME unit, test-retest reliability rho=corr(X,Y)=0.5<1) with NOTHING done between reads, select the underperformers X<c=−1.0, apply a literally-inert intervention, re-measure — the selected group's mean IMPROVES by exactly Delta*=(rho−1)·E[X|X<c]>0 purely from regression to the mean (their extreme-low X was partly bad luck, and luck does not repeat, so Y drifts back toward the population mean), while the whole population shows zero change and the over-performer top tail shows a symmetric spurious DECLINE; the operator correction is to judge a group selected-because-extreme on a noisy metric against a randomized control or the unselected population, never the naked before/after delta. This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/regression_to_mean.py — random, math, json, hashlib) was run VERBATIM under the pinned SEED=20260717 (same posture as V114–V120 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from an independent reimplementation; verifier copy git blob e3340104dc2f2ab630b3fb842088dc981dd58369, file sha256 ffd91dd4c5abf957915c2700b27e157f718fa8b6dfb18989a67c4870490e71c2, diff-clean vs source). The results-dict sha256 35d65a5060d0582a5fd0d041c2d72635657c7f8c62ac024d4ee004b285ed8aed reproduces the proposal's disclosed expected digest EXACTLY (all 64 hex — verified via the script-printed `Results-JSON sha256:` line AND an independent recompute AND the disclosed digest all EQUAL); byte-identical double run confirmed in-process and cross-invocation (separate python3 process, stdout diff exit 0); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 312.4059σ, G2 |z|=0.1204, G3 |z|=0.4969); verifier exits 0. Note the P108 results dict CARRIES a `results_sha256` self-field (P107-style): the digest is computed over the compact one-line preimage json.dumps(results, sort_keys=True, separators=(",",":")) of the results dict BEFORE the self-field is added, then the field is inserted and the file written indented — so recomputing from the on-disk file requires OMITTING `results_sha256` first (the P104-style field-omission step); the script-printed `Results-JSON sha256:` line is the authoritative preimage — confirmed against the script text, not assumed. Next-2 baton for the successor: **(1)** round-24 UNRELATED slot is now closed with V121 — round-24 COMPLETE (V118–V121, rotation fleet→venture→game→unrelated); the idea-engine MIRROR of THIS verdict (V121 outbox block + heartbeat + mirror card) is the paired follow-on step, then round-25 opens with the fleet-slot P109 → V122; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V121 is SEEDLESS at the ledger (P108 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V120 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 121 landed (P108 → V121, APPROVE) — see sims/verdict-121-regression-to-mean/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

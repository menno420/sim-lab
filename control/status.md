# sim-lab · status

updated: 2026-07-11T18:46:00Z
phase: continuous mode; HARDENING SLICE (sim-ready queue empty → harden the newest verdict) — VERDICT 011 ROBUST-gate 80-page-cap gap CLOSED. Re-crawled the two LIVE sites that hit the 80/depth-2 cap (control-plane, botsite) at cap=400 pages/depth=8, 2 passes each (5× budget, 4× depth), answering the open @codex question ("could the 80-page cap hide a broken route that flips a sub-verdict?"). ANSWER: NO. control-plane 80→400 pages, depth 2→3 (257 depth-3 journal leaves all HTTP 200), dead 25→25 (+0 new — known .md/.sh 404 set reproduced EXACTLY, superset, 0 dropped/0 added), 0/6 broken, 0 orphan, still truncated at 400. botsite 80→400 pages, breadth-bound at depth 2 (392/400 at depth 2), dead 0→0 (+0), 0/2400 broken, 0 orphan, reached 349 of ~365 /commands/* + all 44 /features ALL HTTP 200, still truncated at 400 (~16 command pages beyond budget). ZERO new hard failures either site → NEITHER sub-verdict flips → VERDICT 011 CONFIRMED (not changed); no new @codex comment and no outbox correction warranted. analyze_wide.py (reuses analyze_nav.py metric fns + SelfCheck): SELF-CHECKS 2829 passed 0 failed, byte-identical results-wide.json on re-run, wider crawled-URL + control-plane dead-link sets asserted superset-or-equal of narrow, 100% re-crawl agreement, no gated 401/403 crawled. Original analyze_nav.py stays green (243).
health: green
kit: v1.7.0 · check: green (bootstrap.py check --strict exit 0) · engaged: yes
last-shipped: #40 45613e6 — owner-002 robustness wider-cap re-crawl (400/8): crawl_site.py --skip-prefix + 2 wide snapshots + analyze_wide.py + results-wide.json + REPORT robustness addendum + session card (2829 self-checks 0 failed); THIS heartbeat rides a control fast-lane PR (see notes); prior: #39 (control finalize VERDICT 011) / #38 c47b989 (verdict-011 four-websites audit)
blockers: none
orders: acked=ORDER-001 ORDER-002 done= (ORDER-001 model-attribution carried on both VERDICT 011 and this hardening session card 📊 Model: opus-4.8; ORDER-002 self-review section stands)
⚑ needs-owner: OA-002 Codex usage-capped (VERDICT 011 @codex reply pending — PR #38 comment https://github.com/menno420/sim-lab/pull/38#issuecomment-4948283951 posted on the final head, never obeyed, verify against tree when it lands; Q-0120 — the 80-cap sub-question that comment raised is now ANSWERED by this hardening slice: cap does NOT hide a verdict-flipping route); OA-003 review-site DEPLOY is an owner-action surfaced by VERDICT 011 (built for Anthropic reviewers but no deployed URL — routed via outbox VERDICT 011, not a sim-lab blocker); OA-004 harness tag-push 403 on refs/tags/* (harness-v0.1.0 still un-pushed — raw/copy consumption still works)
notes: HARDENING SLICE (continuous mode, sim-ready queue empty) — not a new verdict. Closes the VERDICT 011 ROBUST 80-page-cap disclosure with a measured wider re-crawl; VERDICT 011 ruling UNCHANGED (no sub-verdict flipped). Sim work landed as PR #40 (squash 45613e6) — auto-merge SQUASH fired on the first green head before this heartbeat commit could land (same race as VERDICT 011's #38/#39; no required checks were pending so the merge was immediate), so this heartbeat rides a follow-up control fast-lane PR (forward-only, no history rewrite). Real git push triggered CI. Queue: idea-engine sim-ready queue empty (PROPOSAL 009 → VERDICT 010 was last consumed); re-check next wake. Wider metrics + delta + explicit codex-cap answer in sims/owner-002-websites-purpose-nav/REPORT.md § "Robustness addendum — wider-cap re-crawl (2026-07-11)"; run: python3 sims/owner-002-websites-purpose-nav/analyze_wide.py. botsite still truncated at 400 — 349/~365 command pages spot-checked HTTP 200 (0 dead/0 broken of 2400 assets); control-plane still truncated at 400 (depth-3 tail all 200).

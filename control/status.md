# sim-lab · status

updated: 2026-07-18T01:35:09Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V120 slice — energy-cap overflow-forfeiture verification; this refresh rides the V120 PR #192) · model family opus-4.8 · low · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 120 (energy-cap overflow-forfeiture tax in free-to-play stamina pacing, P107, +13 — round-24 GAME slot) verified APPROVE this slice.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V118 merge basis; on the V120 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0.
kit: v1.15.0
last-shipped: #191 (VERDICT 118, P105 delegation loop collapse in liquid democracy — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 120 (P107 energy-cap overflow-forfeiture — APPROVE, first-failing-gate None; all three gates G1→G2→G3 PASS in order on the /se margin, exit 0 post-flip, byte-identical triple run in-process AND cross-invocation, results-dict sha256 2e30020af5a97661506c29c962d047fb9561948098b09f5a826084b013f3ce29; sim sims/verdict-120-energy-cap-overflow/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim, results-dict sha256 matches the disclosed expected digest EXACTLY; PR #192 lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P107 → V120). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V120** (P107, offset +13 — round-24 GAME slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 120 is an APPROVE (no failing gate, first-failing-gate None): G1 casual-forfeiture headline PASS (W_casual=0.778675 se 0.000135 ≥ WASTE_MIN 0.70 by z=581.5691σ ≥3σ — the fixed cap forfeits most of a busy player's regenerated stamina), G2 frequency-regressivity control PASS (W_casual 0.778675 strictly exceeds W_frequent 0.135641 by z=1973.299σ two-sample se ≥3σ — forfeiture is monotone in the login gap, W∝e^{−C/μ}, a tax on ABSENCE not a cap-independent constant), G3 closed-form anchor MATCH PASS (U_sim matches exact 1−e^{−C/μ} within 3σ for BOTH scenarios: U_frequent |z|=1.0304 / U_casual |z|=0.9307, both <3 — reproduces the exponential-overflow formula, so G1's headline is 100% the overflow mechanism, not a sampling artifact); all_pass=true, exit 0. The overflow-forfeiture trap: a stamina bar regenerates 1 unit/period up to cap C while logins arrive Poisson (gaps iid Exponential mean μ), so regen banked per gap is min(gap,C) and the overflow max(0,gap−C) accrued while the bar is full is silently FORFEITED — the usable fraction is the closed form U(μ)=1−e^{−C/μ} (concave, saturating in the cap) and the forfeited fraction W(μ)=e^{−C/μ} rises monotonically with the login gap, so raising the cap past C~μ adds exponentially little usable energy and the casual/busy player the cap is "sold to protect" forfeits the LARGEST fraction while the always-online player captures nearly all of it. This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/superbot-games/energy_cap_overflow_forfeiture.py — random, math, json, hashlib) was run VERBATIM under the pinned SEED=20260717 (same posture as V114/V115/V116/V117/V118 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from an independent reimplementation; verifier copy file sha256 2e33a757c1b0287b125b876ef00093c070f4f30220b70ed5ca128a49723388a8, git blob 0fec21f10f24ce293d49133c8988157161120ac2, diff-clean vs source). The results-dict sha256 2e30020af5a97661506c29c962d047fb9561948098b09f5a826084b013f3ce29 reproduces the proposal's disclosed expected digest EXACTLY (all 64 hex — verified via the script-printed `Results-JSON sha256:` line AND an independent recompute); byte-identical triple run confirmed in-process and cross-invocation (separate python3 process, stdout+results.json diff exit 0); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 581.5691σ, G2 1973.299σ, G3 |z| 1.0304/0.9307); verifier exits 0. Note the P107 results dict CARRIES a `results_sha256` self-field (unlike P105's sibling-key layout): the digest is computed over the compact one-line preimage json.dumps(results, sort_keys=True, separators=(",",":")) of the results dict BEFORE the self-field is added, then the field is inserted and the file written indented — so recomputing from the on-disk file requires OMITTING `results_sha256` first (the P104-style field-omission step); the script-printed `Results-JSON sha256:` line is the authoritative preimage — confirmed against the script text, not assumed. Next-2 baton for the successor: **(1)** round-24 GAME slot is now closed with V120 (rotation fleet→venture→game→unrelated) — next comes the round-24 UNRELATED slot; the idea-engine MIRROR of THIS verdict (V120 outbox block + heartbeat + mirror card on `claude/verdict-120-energy-cap-overflow`) is the paired follow-on step; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V120 is SEEDLESS at the ledger (P107 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V118 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 120 landed (P107 → V120, APPROVE) — see sims/verdict-120-energy-cap-overflow/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

# sim-lab · status

updated: 2026-07-18T02:18:34Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V122 slice — correlated-fleet variance-floor verification; this refresh rides the V122 PR #194) · model family opus-4.8 · low · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 122 (the correlated-fleet variance floor, P109, +13 — round-25 FLEET-slot opener) verified APPROVE this slice.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V121 merge basis; on the V122 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0.
kit: v1.15.0
last-shipped: #193 (VERDICT 121, P108 regression-to-the-mean selection illusion — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 122 (P109 correlated-fleet variance floor — APPROVE, first-failing-gate None; all three gates G1→G2→G3 PASS in order on the /se margin, exit 0 post-flip, byte-identical double run cross-invocation, results-dict sha256 77ab05b0c61ce782d90f51a747cd4a96f5445f4df10f982a506f346a60513903; sim sims/verdict-122-correlated-fleet-variance-floor/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim, results-dict sha256 matches the disclosed expected digest EXACTLY; PR #194 lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P109 → V122). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V122** (P109, offset +13 — round-25 FLEET-slot opener; map in docs/current-state.md § Verdict-numbering map). VERDICT 122 is an APPROVE (no failing gate, first-failing-gate None): G1 correlated-floor headline PASS (mean(MSE_corr)=0.250281 se 0.001198 ≥ FLOOR=0.10 by z=125.4097σ ≥3σ — the common-mode term does not average away, so a 400-agent correlated fleet floors at ρσ² not σ²/N), G2 correlation-necessity control PASS (the SAME fleet at ρ=0 collapses to σ²/N: mean(MSE_indep)=0.002502 se 1.3e-05 vs anchor 0.0025, |z|=0.1469 <3 — G1's floor is 100% the common-mode correlation, not fleet size), G3 closed-form anchor MATCH PASS (mean(MSE_corr)=0.250281 vs exact ρσ²+(1−ρ)σ²/N=0.251875, |z|=1.3306 <3 — reproduces the equicorrelation identity; N_eff(sim)=3.995517 → asymptote 1/ρ=4.0); all_pass=true, exit 0. The correlated-fleet variance floor: a fleet of N agents each estimating a common truth θ with error ε_i=√ρ·C+√(1−ρ)·Z_i (SHARED common-mode C~N(0,σ²) identical for every agent + idiosyncratic Z_i~N(0,σ²) iid, equicorrelation ρ=0.25) has aggregate MSE_N(ρ)=ρσ²+(1−ρ)σ²/N that FLOORS at ρσ² regardless of N — the common-mode never averages away — so the effective independent count N_eff=σ²/MSE saturates at 1/ρ; on the pinned world N=400 gives MSE≈0.2519 (floor ρσ²=0.25) worth only ~3.97 independent agents while independence predicts σ²/N=0.0025, so ~99% of the promised variance reduction never materializes and scaling the fleet past N≈1/ρ buys essentially no precision; the operator correction is that correlated errors cap a fleet's precision at N_eff→1/ρ regardless of headcount — diversify the common-mode (independent upstreams/configs/priors), don't scale the fleet past N~1/ρ. This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/correlated_fleet_variance_floor.py — random, math, json, hashlib) was run VERBATIM under the pinned SEED=20260717 (same posture as V114–V121 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from an independent reimplementation; verifier copy git blob 7d6aee55601cffefe933aca837f3e0cdcfd62b08, file sha256 79c8bc1ef21a54b2d8bd97945ca1509202b07b28b81d1ac8501066f903755d51, diff-clean vs source). The results-dict sha256 77ab05b0c61ce782d90f51a747cd4a96f5445f4df10f982a506f346a60513903 reproduces the proposal's disclosed expected digest EXACTLY (all 64 hex — verified via the script-printed `sha256:` line AND the on-disk results.json sibling `sha256` field AND an independent recompute all EQUAL); byte-identical double run confirmed cross-invocation (separate python3 process, stdout diff exit 0 AND results-json diff exit 0); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 125.4097σ, G2 |z|=0.1469, G3 |z|=1.3306); verifier exits 0. NOTE the digest posture: P109 hashes the results dict AS-IS and writes the output file as {"results": results, "sha256": digest} — the digest is a SIBLING key of a wrapper dict, NOT a self-field inside results (the P105-style posture) — so recomputing from the on-disk file needs NO field-omission step (distinct from P104/P107/P108 which carry a results_sha256 self-field); confirmed against the script text (main() lines 173–178), not assumed. Next-2 baton for the successor: **(1)** round-25 is now OPEN at the FLEET slot with V122 verified — the idea-engine MIRROR of THIS verdict (V122 outbox block + heartbeat + mirror card) is the paired follow-on step, then round-25 continues with the venture-slot P110 → V123; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V122 is SEEDLESS at the ledger (P109 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V121 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 122 landed (P109 → V122, APPROVE) — see sims/verdict-122-correlated-fleet-variance-floor/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

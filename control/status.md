# sim-lab · status

updated: 2026-07-18T00:22:27Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V117 slice — epidemic-overshoot-past-herd-immunity verification; this refresh rides the V117 PR) · model family opus-4.8 · low · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 117 (epidemic overshoot past herd immunity, P104, +13) verified APPROVE this slice.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V116 merge basis; on the V117 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0.
kit: v1.15.0
last-shipped: #189 (VERDICT 116, P103 streak-shield variance amplification — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 117 (P104 epidemic overshoot past herd immunity — APPROVE, first-failing-gate None; all three gates G1→G2→G3 PASS in order, exit 0 post-flip, byte-identical double run in-process AND cross-invocation, results-dict sha256 2ecda33ccab80be04904c973478093bb50a51cde8e5964da9d35e1ca4d340c3f; sim sims/verdict-117-epidemic-overshoot/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim, results-dict sha256 matches the disclosed expected digest EXACTLY; PR #190 lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P104 → V117). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V117** (P104, offset +13 — round-23 UNRELATED closer slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 117 is an APPROVE (no failing gate, first-failing-gate None): G1 overshoot headline PASS (mean final attack rate over major outbreaks z_sim=0.892632 se 0.000101 exceeds herd-immunity threshold h*=1−1/R0=0.60 by z=2883.28σ ≥3σ AND z_sim>h* — the epidemic does NOT stop at herd immunity), G2 final-size anchor MATCH PASS (z_sim=0.892632 matches the closed-form root z*=0.892645 of z=1−e^{−R0·z} within 3σ, |z|=0.13σ — reproduces Kermack–McKendrick), G3 post-threshold burn PASS (mean fraction infected AFTER the susceptible pool first drops below 1/R0 is burn=0.194675 se 0.000869, z=224.07σ ≥3σ above 0 — the in-flight-transmission mechanism, not just the magnitude); all_pass=true, exit 0. The overshoot trap: reaching the herd-immunity threshold h*=1−1/R0 (the susceptible fraction at which R_eff falls to 1 — the epidemic PEAK) does NOT stop the epidemic because infections already in flight keep transmitting, so the final attack rate z* is the nonzero root of the Kermack–McKendrick final-size equation z=1−e^{−R0·z}, which strictly EXCEEDS h* for every R0>1; on the pinned world (R0=2.5) a stochastic Reed–Frost chain-binomial SIR settles at z_sim≈0.893, NOT at h*=0.60, overshooting it by ≈0.29 (~29% of the population infected UNNECESSARILY past the threshold). This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/epidemic_overshoot.py — random, math, json, hashlib) was run VERBATIM under the pinned SEED=20260717 on python 3.11.15 (same posture as V114/V115/V116 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from an independent reimplementation; verifier copy sha256 d9b3d709785bbfcbcc2be52bb7766a0beb9f6dd3cb439d6c120a07e7faba849c, git blob d13a2055e96ac4d39d611efeaeb7fb9a20e6f1b7, diff-clean vs source). The results-dict sha256 2ecda33ccab80be04904c973478093bb50a51cde8e5964da9d35e1ca4d340c3f reproduces the proposal's disclosed expected digest EXACTLY (all 64 hex — verified via the script-printed line, the in-file results_sha256 self-field, AND an independent recompute of json.dumps(sort_keys,compact) over the results dict MINUS the results_sha256 self-field); byte-identical double run confirmed in-process and cross-invocation (separate python3 process, stdout diff exit 0); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 2883.28σ, G2 |z|=0.13σ, G3 224.07σ); n_major=3574/n_minor=426 (major_frac=0.8935); verifier exits 0. Note the P104 results dict CARRIES a `results_sha256` self-field (unlike P102/P103), so the digest is computed over the dict MINUS that field — confirmed against the script text, not assumed. Next-2 baton for the successor: **(1)** round-23 CLOSES at V117 — the UNRELATED closer slot is now spent (fleet P101→V114 → venture P102→V115 → game P103→V116 → unrelated P104→V117), so round-24 opens next at the FLEET slot: idea-engine PROPOSAL 105 awaits a fresh draft on the idea-engine side and its VERDICT 118 (offset +13, P105 → V118) will be the successor sim-lab slice; the idea-engine MIRROR of THIS verdict (V117 outbox block + heartbeat + mirror card on `claude/verdict-117-epidemic-overshoot`) is the paired follow-on step; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V117 is SEEDLESS at the ledger (P104 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V116 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 117 landed (P104 → V117, APPROVE) — see sims/verdict-117-epidemic-overshoot/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

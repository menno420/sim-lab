# sim-lab · status

updated: 2026-07-17T22:54:33Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V113 slice — kelly overbet ruin growth-optimal-fraction value-trap verification; this refresh rides the V113 PR) · model family opus-4.8 · medium · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 113 (kelly overbet ruin, P100, +13) verified APPROVE this slice.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V112 merge basis; on the V113 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0.
kit: v1.15.0
last-shipped: #185 (VERDICT 112, P099 shop-reroll ruin — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 113 (P100 kelly overbet ruin — APPROVE, first-failing-gate None; all three gates G1→G3 PASS, exit 0 post-flip, byte-identical double run in-process AND cross-invocation, results.json sha256 d6e489de505de1d44a24f0b5164aec83233d8a82fc864dfbb5356c2b75ad65a5; sim sims/verdict-113-kelly-overbet-ruin/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim, results-dict sha256 matches the disclosed expected digest EXACTLY; PR #186 lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P100 → V113). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V113** (P100, offset +13 — thirty-seventh row, round-22 UNRELATED closer slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 113 is an APPROVE (no failing gate): G1 overbet-trap PASS (g(f*)=0.020134 se 0.000031 > g(F_OVER=0.40)=−0.002451 se 0.000065, z=311.63σ ≥3σ AND g(F_OVER) < 0 < g(f*) — naive double-Kelly 0.40 sits past the ruin boundary f_c=0.389391, so its time-average growth is negative while f* compounds), G2 interior optimum PASS (g(f*)=0.020134 > g(F_UNDER=0.02)=0.003800 se 0.000003 at z_vs_under=519.58σ AND > g(F_MAX=0.90)=−0.535935 se 0.000227 at z_vs_max=2424.85σ, each ≥3σ — near-all-in is catastrophic, g<0), G3 analytic-anchor MATCH PASS (sim mean growth at f*=0.020134 se 0.000031 vs closed form g(f*)=P_WIN·ln(1+f*)+q·ln(1−f*)=0.020136, |z|=0.06σ <3σ). The Kelly overbet trap is the classic ensemble-vs-time-average (ergodicity) split in repeated multiplicative betting: on a favorable even-money bet (P_WIN=0.6), staking fraction f grows the ENSEMBLE arithmetic-mean wealth monotonically in f (per-round mult 1+f·(2·P_WIN−1)), so a naive EV-maximizer bets as much as possible — yet the TIME-AVERAGE (a.s. per-round) growth g(f)=P_WIN·ln(1+f)+q·ln(1−f) peaks at a strictly INTERIOR Kelly fraction f*=2·P_WIN−1=0.20 and turns NEGATIVE past the ruin boundary f_c=0.389391; double-Kelly F_OVER=0.40 is just past that edge, so the typical gambler's wealth decays to ≈0.375× over T=400 rounds while the ensemble mean balloons to log10≈13.37. This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/kelly_overbet_ruin.py — random, math, json, hashlib) was run VERBATIM under the pinned SEED=20260717 (same posture as V111/V112 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from an independent reimplementation; verifier copy sha256 6cbcbdea76d23061b9c9140cbc1ef857b7e7274af92dd40ffb8d5d2068615a8b, diff-clean vs source). The results-dict sha256 d6e489de505de1d44a24f0b5164aec83233d8a82fc864dfbb5356c2b75ad65a5 reproduces the proposal's disclosed expected digest EXACTLY; byte-identical double run confirmed in-process and cross-invocation (separate python3 process, fresh dir); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 311.63σ, G2 519.58σ/2424.85σ, G3 |z|=0.06σ); f*=0.20; verifier exits 0. Next-2 baton for the successor: **(1)** round-22 CLOSES at V113 — the UNRELATED closer slot is now spent (fleet P097→V110 → venture P098→V111 → game P099→V112 → unrelated P100→V113), so round-23 opens next: idea-engine PROPOSAL 101 awaits a fresh draft on the idea-engine side and its VERDICT 114 (offset +13, P101 → V114) will be the successor sim-lab slice; the idea-engine MIRROR of THIS verdict (V113 outbox block + heartbeat + mirror card on `claude/verdict-113-mirror`) is a SEPARATE follow-on step, NOT done in this sim-lab slice; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V113 is SEEDLESS at the ledger (P100 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V112 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 113 landed (P100 → V113, APPROVE) — see sims/verdict-113-kelly-overbet-ruin/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

# sim-lab · status

updated: 2026-07-18T08:21:12Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V138 landing slice — USL coherency retrograde-throughput cliff verification; this refresh rides the V138 PR #211) · high · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 138 (the USL coherency retrograde-throughput cliff, P125, +13 — round-29 FLEET slot OPENER) verified APPROVE this slice (disclosed digest e496837c… reproduced byte-identical, all three gates G1→G2→G3 PASS) — round-29 OPENS at the fleet slot; the next pull is round-29 at the VENTURE slot, PROPOSAL 126 → VERDICT 139 (+13). NON-CONTIGUITY: V137 (P124, round-28 UNRELATED closer) is not yet on sim-lab main — high-water V138 does NOT imply V137 landed.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V136 merge basis (#210); on the V138 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0 and merge-on-green lands PR #211.
kit: v1.15.0
last-shipped: #210 (VERDICT 136, P123 hard-close snipe clearing leak — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 138 (P125 USL coherency retrograde-throughput cliff — APPROVE, first-failing-gate None; all three gates G1→G2→G3 PASS in order: G1 retrograde existence mean(T(200)−T(31))=0.142452 z=959.9168σ (se 0.000148; adding agents past the N*=31 peak strictly increases completion time), G2 coherency-necessity β=0 Amdahl control mean(T0(200)−T0(31))=−0.026410 z=−189.0673σ (se 0.000140; the same fleet is monotone-faster with the coherency term removed — the drop is caused by coherency β, not fleet size), G3 USL-anchor+peak |z_star|=0.0507 / |z_hi|=0.5417 < 3 AND grid argmin=N*=31, full-grid max|z|=1.6993; closed-form anchors N*=√((1−α)/β)=31.1448→31, C(31)=10.954064 vs C(200)=4.276245 (peak/hi 2.561608), T(31)=0.091285 vs cf 0.091290, T(200)=0.233791; exit 0 post-flip, byte-identical across cross-invocation A/B + deterministic in-process double-run, results-dict sha256 e496837c6dd8bbbaec79701528507185a0a08cb8c008255317d3719d99795557 reproduces the disclosed digest EXACTLY (SELF-FIELD/PRETTY-ON-DISK posture — the on-disk pretty json carries the self-field and does NOT sha256sum to the digest; recovered by dropping results_sha256 and re-serializing compact); sim sims/verdict-138-usl-retrograde-cliff/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim; PR #211 lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P109 → V122). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V138** (P125, offset +13 — round-29 FLEET slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 138 (the USL coherency retrograde-throughput cliff, P125 → V138, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 retrograde-existence PASS (mean(T(N_hi=200)−T(N*=31))=0.142452 > 0 by z=959.9168σ ≥ SIGMA=3.0, se 0.000148 — adding agents past the N*=31 peak strictly increases completion time ⇒ less throughput), G2 coherency-necessity PASS (β=0 Amdahl control mean(T0(200)−T0(31))=−0.026410 < 0 by z=−189.0673σ, se 0.000140 — the SAME fleet is monotone-faster once the coherency term is removed, so the retrograde drop is caused by coherency β, not fleet size), G3 USL-anchor+peak PASS (|z_star|=0.0507 / |z_hi|=0.5417 < 3 AND grid argmin of measured time = N*=31; full-grid max|z|=1.6993); closed-form anchors N*=√((1−α)/β)=√(0.97/0.001)=31.1448→31, C(31)=10.954064 vs C(200)=4.276245 (peak/hi ratio 2.561608 — a 200-agent fleet delivers under half the 31-agent peak), Amdahl ceiling 1/α=33.333, T(31)=0.091285 (cf 0.091290) / T(200)=0.233791; all_pass=true, exit 0. The Universal Scalability Law (Gunther, "universal law of computational scalability", C(N)=N/(1+α(N−1)+βN(N−1))): a coordinated fleet pays contention α (Amdahl, caps speedup at 1/α, monotone) AND coherency β (pairwise-consistency cost ∝ N(N−1)); adding the quadratic coherency term to Amdahl's denominator makes C(N) NON-monotone — it PEAKS at N*=√((1−α)/β) then DECLINES ("retrograde scaling"), so a fleet has a FINITE optimal size and adding agents past N* makes it SLOWER; Amdahl's law is the β=0 control (monotone, never falls). Reproduced byte-identical from idea-engine ideas/fleet/usl_coherency_retrograde_cliff.py (blob 08806218, sha256 f46b0241, 270 lines/11536 bytes, diff exit 0), SEED=20260717, stdlib-only; results-dict sha256 e496837c6dd8bbbaec79701528507185a0a08cb8c008255317d3719d99795557 reproduces the disclosed digest EXACTLY (SELF-FIELD/PRETTY-ON-DISK posture — differs from V135/V136 NO-SELF-FIELD; the on-disk pretty json carries results_sha256 and does NOT sha256sum to the digest, recovered by dropping the self-field and re-serializing compact), byte-identical across cross-invocation A/B + deterministic in-process double-run; sim sims/verdict-138-usl-retrograde-cliff/. NON-CONTIGUITY: V137 (P124, round-28 UNRELATED closer) is not yet on sim-lab main — high-water V138 does NOT imply V137 landed. — — — VERDICT 136 (the hard-close snipe clearing leak, P123 → V136, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 existence PASS (leak_star_mean 0.03904223 > 0 by z=135.3738σ ≥ SIGMA_GATE=3.0 — the hard-close under-clears vs the full-field second price; mean leak at q*=0.15 = 5.022535% of the soft-close clear 0.7773411), G2 dose-response PASS (dose_mean 0.06175681 > 0 by z=163.6733σ under paired common-random-numbers — leak(0.25) 0.07347951 − leak(0.05) 0.01172271, the leak rises with the drop rate q), G3 specificity PASS (max|placebo leak @q=0| = 0.0 exactly — hard-close = soft-close every trial when the transmission friction is removed); closed-form anchor e_hard_sim_star 0.73829887 vs the binomial-mixture theory E[P_hard](q*)=0.73856236 (abs_err 0.00026349) AND e_soft 0.7773411 reproduces (K−1)/(K+1)=0.77777778; all_pass=true, exit 0. The hard-close snipe clearing leak (Roth & Ockenfels, "Last-Minute Bidding and the Rules for Ending Second-Price Auctions", AER 92(4), 2002): a hard-close (fixed-deadline) auction clears at a second-price over only the bids that TRANSMIT, but last-instant sealed snipes fail to transmit with probability q (server tick / latency / rate limits), thinning the effective field so the clear lands BELOW the true competitive second-highest valuation v_(2); a soft-close (anti-snipe / auto-extend) auction lets the full field respond and clears at v_(2). Two facts the "the anti-snipe timer is just UX politeness" instinct misses: the timer is an ECONOMIC mechanism — the leak=P_soft−P_hard≥0 is a silent value transfer to the lowest-latency (best-connected) client plus an AH-cut gold-sink under-drain — and the leak SCALES with q and VANISHES at q=0 (where hard-close and soft-close coincide exactly). REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/superbot-games/snipe_clearing_leak.py — random, math, json, hashlib) run VERBATIM under SEED=20260717, TRIALS=100000, K=8, Q_LEVELS=[0.0,0.05,0.15,0.25] (verifier copy git blob a0d92cc0a2385538955950c29dae1968bf0a29b7, file sha256 85f489bfb6fdd7a7b18b2b6e8e51a96b36ef70a64cd7585cb98c904ed0872485, 155 lines / 5663 bytes, diff-clean vs source at idea-engine HEAD 8b5432c), results-dict sha256 2344cfb88d01022b4b6e3e119af4ce675b454e4ef6aa5e4465610600771c18e8 reproduces the disclosed digest EXACTLY, byte-identical across cross-invocation A + B (stdout diff exit 0) with a deterministic in-process double-run. Digest posture: P123 is the NO-SELF-FIELD / COMPACT-STDOUT posture (same family as V135) — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")), prints PRETTY JSON then the `Results-JSON sha256:` line + the 3 gate lines, and writes NO results.json; the committed snipe_clearing_leak_results.json IS the exact compact canonical bytes hashed (no trailing newline, 528 bytes) so the file's own sha256sum IS the disclosed digest (the on-disk run-stdout.txt is the pretty dump, not the compact line). Next baton for the successor: round-28 continues — fleet P121→V134, venture P122→V135, THIS game P123→V136; the next pull is round-28 at the UNRELATED slot, PROPOSAL 124 → VERDICT 137 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). NON-CONTIGUITY note: V136 lands while the V135 (P122) and V132 (P119) idea-engine mirrors may remain open pulls — "high-water V136" does not mean every mirror is closed on both sides. VERDICT 136 landed (P123 → V136, APPROVE) — see sims/verdict-136-snipe-clearing-leak/. VERDICT 135 landed (P122 → V135, APPROVE, #209 → main 63bcb2f) — see sims/verdict-135-nrr-composition-mirage/ (round-28 VENTURE slot, digest de67af4a…d3bb, all three gates PASS). PRIOR: verdict ledger high-water was **V134** (P121, offset +13 — round-28 FLEET slot opener; map in docs/current-state.md § Verdict-numbering map). VERDICT 134 (the hedged-request tail cure, P121 → V134, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 hedge-collapses-the-tail PASS (per-trial mean tail-cut 261.912252 se 0.923706 > 0 by z=283.5451σ — deferring a second copy to the p95 delay collapses the single-replica p99 322.613793 to 60.701541), G2 the-knee PASS ((a) mean capture ratio 0.889581 se 0.000377 ≥ CAPTURE_MIN=0.80 by z_capture=237.7642σ — the p95 hedge recovers ~89% of full duplication's tail cut — AND (b) mean hedge-fire load 0.04995 ≤ LOAD_MAX=0.08, firing on ~p95=5% of requests while full-dup costs +100%), G3 almost-free-efficiency PASS (mean efficiency edge red_hedge/load_hedge − red_dup/load_dup 4949.106846 se 17.525819 > 0 by z=282.3895σ — the hedge's tail-cut-per-unit-load 5243.488535 beats full duplication's 294.381688 by 17.811871×); all_pass=true, exit 0; anchors mixture p95=45.388281, mixture p99=321.887582, full-dup extra load=1.0; sim mean p99 base 322.613793, hedge 60.701541, dup 28.232104. The hedged-request tail cure (Dean & Barroso, "The Tail at Scale", CACM 2013): a request served by ONE replica has a heavy-tailed service time (two-component straggler mixture, MEAN_FAST=10.0 / MEAN_SLOW=200.0 / P_STRAGGLER=0.05) whose p99 is dominated by the occasional straggler; the folk fix — duplicate every request and take the first answer — halves the tail but DOUBLES the load (+100%), and the folk belief holds that any delay before the second copy leaves latency on the table; both are wrong, because the HEDGED request defers the second copy to a small delay d = the batch empirical p95 (response = min(primary, d + secondary), fired only when the primary exceeds d) so it fires on only ~5% of requests (extra load ≈0.05) yet the p99 COLLAPSES because a straggler primary is almost always rescued by a fast INDEPENDENT secondary (the hedged tail survives only when BOTH copies straggle, ≈p²≈0.0025, past p99); the transferable correction is that the tail cure is almost free — the last ~11% of tail benefit full duplication buys BEYOND the p95 hedge costs ~20× the load (the knee) — so fleet dispatch should tie-hedge after ~p95 rather than duplicate. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/hedged_request_tail_cure.py — random, math, json, hashlib) run VERBATIM under SEED=20260717, N_REQUESTS=20000, TRIALS=200, HEDGE_PCTL=0.95 (verifier copy git blob 5bd1866e2432bd0e6f90289ea4bac7971e32bbfd, file sha256 be975454449d39e75f6bf543e952622e3df8966921190f16667d8fb2cd82f1c6, 254 lines / 11916 bytes, diff-clean vs source at idea-engine HEAD 5c26a04 / PR #542), results-dict sha256 a76ef737962bd9c3663399dc19425dc3ca697c6d8775be682c3a128aa76b1b4e reproduces the disclosed digest EXACTLY (script-printed `Results-JSON sha256:` stdout line AND the on-disk file's own results_sha256 field both EQUAL), byte-identical across cross-invocation A + B (stdout diff exit 0 AND results-json diff exit 0) with a deterministic in-process double-run; all gate sigmas match the disclosed dry-sim EXACTLY (G1 z=283.5451, G2 z_capture=237.7642 / hedge-load 0.04995, G3 z=282.3895). Digest posture: P121 is the SELF-FIELD / stdout-line posture (the P119/V132/V133 posture) — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) BEFORE inserting results["results_sha256"]=digest, then writes the on-disk hedged_request_tail_cure_results.json pretty-printed (indent=2, sort_keys) WITH the field, so the on-disk file's own sha256sum is NOT this digest; the disclosed digest is the value printed on the `Results-JSON sha256:` stdout line (and stored in the file's own results_sha256 field). Next baton for the successor: round-28 is OPEN at the FLEET slot (V134 opens it — the rotation runs fleet→venture→game→unrelated); the next pull is round-28 at the VENTURE slot, PROPOSAL 122 → VERDICT 135 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). VERDICT 134 landed (P121 → V134, APPROVE) — see sims/verdict-134-hedged-request-tail-cure/. PRIOR: verdict ledger high-water was **V133** (P120, offset +13 — round-27 UNRELATED slot closer; map in docs/current-state.md § Verdict-numbering map). VERDICT 133 (the German-tank MVUE, P120 → V133, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 MVUE-unbiased PASS (mean N̂ 999.927753 se 0.1833 within 3σ of N=1000 by |z|=0.3941 — the estimator that EXTRAPOLATES ABOVE the largest observed serial is spot-on for the true population size), G2 max-biased-low + MVUE-above-max PASS (mean sample max 834.106461 se 0.15275 strictly BELOW N by z_bias=1086.0456σ — the naive "population = largest serial seen" underestimates — AND N̂-above-max gap 165.821292 se 0.238603 by z_above=694.9667σ — the MVUE lands ABOVE the largest observed serial, the counterintuitive core), G3 closed-form anchor MATCH + efficiency PASS (empirical Var(N̂) 28509.405891 se 53.306373 reproduces the exact closed form (N−K)(N+1)/(K(K+2))=28457.0 by |z|=0.9831 <3 AND the MVUE strictly MORE efficient than the unbiased Ñ=2x̄−1: Var(Ñ)−Var(N̂) 37966.966877 se 110.211166 by z_efficiency=344.4929σ ≥3σ); all_pass=true, exit 0; closed-form anchors E[m]=834.166667, bias_max (N−E[m])=165.833333, Var(N̂)=28457.0, Var(Ñ)=66399.666667, efficiency ratio (K+2)/3=2.333333, E[N̂]=N=1000 exactly (descriptive); sim mean N̂ 999.927753, mean sample max 834.106461, N̂-above-max gap 165.821292, Var(N̂) 28509.405891, Var(Ñ) 66476.372769. The German-tank MVUE: you observe K=5 distinct serial numbers drawn WITHOUT replacement from an unknown fleet numbered 1..N and must estimate N; the naive sample maximum m is biased LOW — E[m]=K(N+1)/(K+1)<N, a shortfall of (N−K)/(K+1) — because a finite sample almost never contains the very top of the range, so the frequentist minimum-variance UNBIASED estimator ADDS BACK the expected gap and EXTRAPOLATES ABOVE the largest observed serial: N̂=m(1+1/K)−1=m+(m−K)/K="sample maximum + the average gap between the ordered samples", exactly unbiased (E[N̂]=N) with exact variance Var(N̂)=(N−K)(N+1)/(K(K+2)), and because m≥K always N̂≥m always (the honest estimate is at or above the largest serial seen, never below); it is the MVUE because m is a complete sufficient statistic (Lehmann–Scheffé), and the competing unbiased mean-doubling estimator Ñ=2x̄−1 is strictly LESS efficient (Var(Ñ)/Var(N̂)=(K+2)/3≈2.33× noisier at K=5) because m captures all the sample's information about N; the transferable correction for any "estimate the size/top/total of a population from a sample of sequential labels" problem (distinct users/requests/invoices/SKUs from sampled IDs, fleet size behind observed asset tags, concurrent sessions from sampled monotonic tokens) is that the sample maximum is biased low by the average gap the sampling left above it — estimate the maximum as N̂=m(1+1/K)−1, report its se from (N−K)(N+1)/(K(K+2)), and prefer this max-based correction to any mean-based estimator since the sample maximum is a sufficient statistic; caveat: uniform, gap-free serial numbering. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/german_tank_mvue.py — random, math, json, hashlib) run VERBATIM under SEED=20260717, N_TRUE=1000, K=5, SAMPLES=4000, TRIALS=200 (verifier copy git blob de5487e5bf706163188f811b97ed3b4723fc148a, file sha256 130ebaafa72f8931440ac929954f8b0987d14ff947d5e7b09f36e8fe5b006aaf, 188 lines / 8497 bytes, diff-clean vs source at idea-engine HEAD 0f61ea8 / PR #537), results-dict sha256 37cea2bfb9b3f60c564d78bb6e5b31c9fcfe8ee7cb11c2fbafe38ddf9af31531 reproduces the disclosed digest EXACTLY (script-printed `Results-JSON sha256:` stdout line AND the on-disk file's own results_sha256 field both EQUAL), byte-identical across 4 runs (cross-invocation A + B + in-process double, all exit 0, stdout diff exit 0 AND results-json diff exit 0); all gate sigmas match the disclosed dry-sim EXACTLY (G1 |z|=0.3941, G2 z_bias=1086.0456σ / z_above=694.9667σ, G3 |z|=0.9831 / z_efficiency=344.4929σ). Digest posture: P120 is the SELF-FIELD / stdout-line posture (the P119/V132 posture) — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) BEFORE inserting results["results_sha256"]=digest, then writes the on-disk german_tank_mvue_results.json pretty-printed (indent=2, sort_keys) WITH the field, so the on-disk file's own sha256sum (145db407…) is NOT this digest; the disclosed digest is the value printed on the `Results-JSON sha256:` stdout line (and stored in the file's own results_sha256 field). Next baton for the successor: round-27 is COMPLETE on the verdict side at the unrelated slot (V133 closes it — fleet P117→V130, venture P118→V131, game P119→V132, unrelated P120→V133); the next pull is round-28 at the FLEET slot, PROPOSAL 121 → VERDICT 134 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). VERDICT 133 landed (P120 → V133, APPROVE) — see sims/verdict-133-german-tank/. PRIOR: verdict ledger high-water was **V132** (P119, offset +13 — round-27 GAME slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 132 (the PRD proc-rate compression, P119 → V132, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 naive-C overshoot PASS (eff_naive 0.450684 ≥ EFF_MIN=0.40 by z=146.3107σ AND z_vs_null_0.25=655.4296σ — setting the PRD increment equal to the nominal rate C=0.25 ships a proc that fires at 0.4507, ~80% ABOVE the intended 0.25, so the increment C is NOT the effective rate), G2 solve-C-fixes-it control PASS (c_solved 0.084744 ≤ SOLVED_MAX=0.15 AND eff_solved 0.24998 within 3σ of TARGET=0.25 by |z|=0.0637 — solving C for the target instead of setting C=nominal recovers exactly 0.25, so G1's overshoot is the naive C=nominal error, not PRD per se), G3 closed-form anchor + anti-streak PASS (eff_naive 0.450684 vs closed-form 1/E[N]=0.450704 by |z|=0.0589 <3 AND PRD max miss-streak 3 strictly below true-random max miss-streak 51 — reproduces the analytic renewal rate AND isolates what PRD actually buys, a bad-luck-streak bound ⌈1/C⌉−1=3 not a rate change); all_pass=true, exit 0; E[N]=2.21875, eff_naive_closed_form=0.450704, c_solved=0.084744, eff_solved_closed_form=0.25, prd_max_streak_bound_naive=3 (descriptive); sim eff_naive 0.450684 (max streak 3), eff_solved 0.24998 at C=0.084744 (max streak 11), eff_random 0.249999 (max streak 51). The PRD proc-rate compression: the Warcraft III / Dota 2 Pseudo-Random Distribution anti-streak system replaces a flat per-attempt proc chance p with a chance that starts low and RISES by a constant increment C on every consecutive miss, resetting on a proc — P(proc on the n-th attempt since the last)=min(1,C·n) — so the sequence is a renewal process whose inter-proc gap N has E[N]=Σ_{n≥0}Π_{k=1}^{n}(1−C·k) and long-run EFFECTIVE rate 1/E[N]; the trap is that C is NOT the effective rate — the escalating chance forces frequent early procs, so naively setting C=nominal (C=0.25 for a wanted "25% crit") yields 1/E[N]=0.4507, nearly DOUBLE the target, while the fix is to SOLVE C for the target (C≈0.0847 gives 0.25); what PRD genuinely delivers is variance reduction — a hard bad-luck-streak bound of ⌈1/C⌉−1 misses (3 at C=0.25) versus a geometric's unbounded tail (51 in this run) — never a rate shift, so a designer must solve C to the target rate and read PRD as a streak-bounding tool, not a rate knob. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/superbot-games/prd_proc_compression.py — random, math, json, hashlib) run VERBATIM under SEED=20260717, ATTEMPTS=2000000, C_NAIVE=0.25, TARGET=0.25 (verifier copy git blob 4d4e1865d9eb05e0cd5b625258c80cc5e0852f81, file sha256 b2aef76244a566cb8879e7850eea324bdfc39a13aa20eabc872697903d9426d2, 242 lines / 10523 bytes, diff-clean vs source at idea-engine HEAD e0612a9 / PR #536), results-dict sha256 ce121bd9885747c336bf8f655112dc0bbffb35ba0fbd01832c14eb57d4248911 reproduces the disclosed digest EXACTLY (script-printed `Results-JSON sha256:` line AND the on-disk file's own results_sha256 field AND independent field-omitted recompute all EQUAL), byte-identical across 4 runs (cross-invocation A + B + in-process double, all exit 0); all gate sigmas match the disclosed dry-sim EXACTLY (G1 146.3107σ / 655.4296σ, G2 |z|=0.0637, G3 |z|=0.0589). Digest posture: P119 is the SELF-FIELD / stdout-line posture — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) BEFORE inserting results["results_sha256"]=digest, then writes the on-disk prd_proc_compression_results.json pretty-printed (indent=2) WITH the field, so the on-disk file's own sha256sum (d045daa6…) is NOT this digest; the disclosed digest is the value printed on the `Results-JSON sha256:` stdout line (and stored in the file's own results_sha256 field); the field-omission step was applied on independent recompute, confirmed against the script text, not assumed. Next baton for the successor: round-27 continues — the next pull is round-27 at the UNRELATED slot, PROPOSAL 120 → VERDICT 133 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). VERDICT 132 landed (P119 → V132, APPROVE) — see sims/verdict-132-prd-proc-rate/. PRIOR: verdict ledger high-water was **V131** (P118, offset +13 — round-27 VENTURE slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 131 (the sales-ramp capacity drag, P118 → V131, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 growth-capacity-drag PASS (mean(φ_slow−φ_fast) 0.157056 se 0.000155 ≥ GAP_MIN=0.05 by z=689.8382σ — growing headcount faster strictly and substantially lowers realized per-rep productivity, a ramp effect not talent), G2 attrition-interchangeability PASS (a matched attrition bump β_attr=β_fast=0.11 drags identically: mean(φ_slow−φ_attr_hi) 0.156907 se 0.000158 ≥ GAP_MIN=0.05 by z=676.4328σ with fast-vs-attr interchange_abs_diff 0.000148 — the drag is β=g+d only, over-hiring and churn are the same lever), G3 closed-form anchor MATCH PASS (φ_slow 0.888962 vs cf 0.889051 by |z|=0.9879 AND φ_fast 0.731906 vs cf 0.732043 by |z|=1.1052, both <3 — reproduces the ramp-averaged utilization φ(β)=(1−e^{−βR})/(βR)); all_pass=true, exit 0; observed φ_slow 0.888962 (se 9e-05), φ_fast 0.731906 (se 0.000124), φ_attr 0.732055 (se 0.000126), closed-form gap 0.157007. The sales-ramp capacity drag: a new rep books Q·min(a/R,1) — productivity ramping linearly to full quota Q over ramp R=6 months — and in a force growing at rate g whose reps leave at rate d the steady-state active tenure is Exp(β=g+d) (Lotka stable-population age structure), so the realized fraction of nominal quota is φ(β)=E[min(a/R,1)]=(1−e^{−βR})/(βR), always <1 and DECREASING in β; nameplate capacity overstates realized bookings by 1/φ (worst exactly when scaling hardest), faster growth mechanically dilutes per-rep productivity so a falling bookings-per-rep is a turnover signal not a talent signal, and growth and attrition are INTERCHANGEABLE (φ depends only on β=g+d, so a +Δ over-hiring problem and a +Δ retention problem inflict the identical drag); the operator correction is to plan on realized φ·nominal not nameplate, price attrition reduction as equal to slowing over-hiring, and invest in shrinking the ramp R — the one lever that lifts φ at every β. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/venture-lab/sales_ramp_capacity_drag.py — random, math, json, hashlib) run VERBATIM under SEED=20260717, TRIALS=400, N_REPS=20000 (verifier copy git blob 87503489bd55fe9073a4268c46fa46d09917387e, file sha256 e42b0e8c64891f81449643710e3358d6a7d3cb93ed099b1472ce205243225e89, 220 lines / 9297 bytes, diff-clean vs source at idea-engine HEAD 4310ff2), results-dict sha256 9b7d3b078fa1af3ce2406a245a9422263e09024828656cd06d83f921cfa6d6ff reproduces the disclosed digest EXACTLY (the script-printed sibling `sha256:` line AND the on-disk compact-canonical results.json file's own sha256sum both EQUAL the disclosed digest), byte-identical double run cross-invocation (stdout diff exit 0), exit 0; all three gate sigmas match the disclosed dry-sim EXACTLY (G1 689.8382σ, G2 676.4328σ, G3 |z|=0.9879 / 1.1052). Digest posture: P118 is the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) over a results dict that never carries a results_sha256 field and prints the digest as a sibling stdout `sha256:` line, writing NO file to disk (differs from the SELF-FIELD V129/V130 exemplars that write a pretty _results.json); the committed sales_ramp_capacity_drag_results.json here is that exact compact-canonical stdout line (no trailing newline) so the file's own sha256sum reproduces the digest. Next baton for the successor: round-27 continues — the next pull is round-27 at the GAME slot, PROPOSAL 119 → VERDICT 132 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). Fleet-lane note: the prior standing fleet-lane gap is now CLOSED — sim-lab sims/verdict-126-two-choices-routing-cliff/ has landed (V126, P113 fleet, sibling PR #200 at main 14837d1), so V126/V127/V128/V129/V130 are all present alongside this V131. VERDICT 131 landed (P118 → V131, APPROVE) — see sims/verdict-131-sales-ramp-capacity-drag/. PRIOR: verdict ledger high-water was **V130** (P117, offset +13 — round-27 FLEET-slot opener; map in docs/current-state.md § Verdict-numbering map). VERDICT 130 (the Young–Daly checkpoint-interval optimum, P117 → V130, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 over-checkpointing headline PASS (penalty_frequent 0.305502 se 0.000307 ≥ DELTA_MIN=0.05 by z=833.5744σ — checkpointing 8× too often, interval τ*/K=2.5, wastes strictly MORE wall time than the optimum τ*=20, so over-checkpointing is not "safer" but wasteful, inverting "checkpoint as often as possible is safest"), G2 U-shape + √-law control PASS (penalty_rare 0.438308 se 0.003427 ≥ DELTA_MIN=0.05 by z_rare=113.2963σ — checkpointing 8× too rarely, interval τ*·K=160, ALSO wastes strictly more, so the waste is U-shaped with an INTERIOR optimum — AND halving_drop 0.054867 se 0.000433 ≥ HALVE_MIN=0.02 by z_halve=80.5085σ — quadrupling the MTBF M→4M=800 at the rescaled optimum τ*(4M)=2τ*=40 HALVES the optimum overhead 0.106933→0.052066, the overhead floor falling as √(2C/M), isolating the √ law), G3 closed-form anchor MATCH PASS (overhead_opt 0.106933 vs the exact Daly anchor 0.107106 by |z|=0.5778 AND overhead_frequent 0.412435 vs 0.412322 by |z|=1.7438 AND overhead_rare 0.545241 vs 0.545871 by |z|=0.1845, all <3 — reproduces the exact Daly law O(τ)=(M/τ)(exp((τ+C)/M)−1)−1 with the Young optimum τ*=√(2CM)=20 and floor √(2C/M)=0.10 at the measured minimum); all_pass=true, exit 0; anchors daly_opt 0.107106 / daly_frequent 0.412322 / daly_rare 0.545871 / daly_opt_4M 0.051720 / young_o_star 0.10 / young_o_star_4M 0.05 (descriptive). The checkpoint-interval optimum: a long fleet job that can crash and restart from its last checkpoint faces TWO competing wastes — checkpoint OVERHEAD (~C/τ per unit useful work: each checkpoint costs C, taken every τ) and lost RECOMPUTE work on a crash (~τ/(2M) per unit work: a Poisson crash at rate 1/M lands on average halfway through the current interval) — whose convex sum C/τ+τ/(2M) makes total wasted wall time U-SHAPED in τ with a unique INTERIOR optimum τ*=√(2·C·M) (Young 1974) and overhead floor √(2C/M) (Daly 2006); over-checkpointing walks UP the left arm (C/τ diverges as τ→0) so it is not "safer" but wasteful, τ*·k and τ*/k inflate overhead by the same ½(k+1/k), and the optimum scales only as √M (4× reliability → 2× interval, half the floor); the operator correction is that the checkpoint/snapshot/commit interval is a U-shaped OPTIMISATION not a one-sided safety dial — set it to τ*=√(2·C·MTBF), read the floor as √(2C/MTBF), and since the optimum grows only as √MTBF spend effort shrinking the checkpoint cost C (incremental/async checkpoints) rather than cranking the frequency. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/checkpoint_interval_optimum.py — random, math, json, hashlib) run VERBATIM under SEED=20260717 (same posture as V114–V129; verifier copy git blob a4fab85ed891bb4c2a9bea7d14801fd59e02407b, file sha256 7f44f348e237f6c5f6cee6f498557b8d8f847bba27d657b77aadfaced1721450, diff-clean vs source at idea-engine main 74a6ec1 / PR #532), results-dict sha256 43a77ca77563e81f873cd73c81d35c684a2ec48bc26d2d740b083ef6be566a81 reproduces the disclosed digest EXACTLY (script-printed `Results-JSON sha256:` line on BOTH runs AND independent field-omitted recompute AND disclosed all EQUAL), byte-identical double run cross-invocation (stdout diff exit 0 AND results-json diff exit 0), exit 0; all gate sigmas match the disclosed dry-sim EXACTLY (G1 833.5744σ, G2 113.2963σ / 80.5085σ, G3 |z|=0.5778 / 1.7438 / 0.1845). Digest posture: P117 is the SELF-FIELD posture (P104/P107/P108/P111/P112/P115/P116-style) — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) BEFORE inserting results["results_sha256"]=digest (L199–201), so the disclosed digest is over the results dict with the results_sha256 self-field OMITTED and the pretty-printed on-disk JSON's own sha256sum (f149637327dc72a1c74bbd5b1bac0fb9c9b4459ce45695177843727194d747b7) is NOT this digest; the field-omission step was applied on independent recompute, confirmed against the script text, not assumed. Next baton for the successor: round-27 is OPEN at the FLEET slot (V130 opens it — the rotation reopens fleet→venture→game→unrelated); the next pull is round-27 at the VENTURE slot, PROPOSAL 118 → VERDICT 131 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). Fleet-lane note: sim-lab sims/ still shows NO verdict-126 dir — V126 (P113 fleet) has not landed while V127/V128/V129 merged (standing fleet-lane gap, PR #200/#527 lineage). VERDICT 130 landed (P117 → V130, APPROVE) — see sims/verdict-130-young-daly-checkpoint/. PRIOR: verdict ledger high-water was **V129** (P116, offset +13 — round-26 UNRELATED-slot closer; map in docs/current-state.md § Verdict-numbering map). VERDICT 129 (the arcsine lead-time illusion, P116 → V129, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 extreme-lead headline PASS (p_extreme 0.415618 se 0.000512 ≥ EXT_MIN=0.30 by z=225.9288σ — in a perfectly FAIR driftless ±1 walk one side leads for an EXTREME share of the play far more often than the naive 50/50 picture allows), G2 fair-mean + middle-is-rare control PASS (mean_lead_fraction 0.500315 se 0.000416 within 3σ of 0.5 by |z|=0.7575 — the walk is genuinely unbiased so G1 is the arcsine U-shape not a drift — AND p_balanced 0.130543 se 0.000353 ≤ BAL_MAX=0.20 by z_balanced=196.6076σ AND gap_extreme_minus_balanced 0.285075 se 0.000622 ≥ GAP_MIN=0.15 by z_gap=217.2175σ — the extremes strictly BEAT a close see-saw, inverting the "concentrates at 1/2" belief), G3 closed-form anchor MATCH PASS (p_extreme 0.415618 vs the exact discrete-arcsine anchor 0.415569 by |z|=0.0953 AND p_low_tail 0.207800 vs the exact anchor 0.207784 by |z|=0.0356, both <3 — reproduces the Lévy–Feller arcsine law itself); all_pass=true, exit 0; anchors arcsine_extreme 0.415569 / arcsine_low_tail 0.207784 / arcsine_balanced 0.130523 / continuous_extreme 0.409666 (descriptive). The arcsine lead-time illusion: play a perfectly FAIR contest as an unbiased ±1 random walk of N_STEPS=500 steps (each step a fair coin, zero edge, zero drift) and measure the fraction of the game one side spends IN THE LEAD (segment i above the axis iff S_{i−1}+S_i>0) — a fair walk is null-recurrent and STICKY about its sign (returns to zero w.p.1 but the expected inter-return time is infinite), so long one-sided excursions dominate and the lead-fraction follows the U-shaped ARCSINE distribution (density 1/(π√(x(1−x))), piling up at 0/1, minimized at 1/2), exact discrete law P(T=2k)=u_{2k}·u_{2(n−k)} with u_{2j}=C(2j,j)/4^j (Feller Vol.1 §III.4), n→∞ limit P(fraction≤x)→(2/π)arcsin√x — so a fair game is fair in the MEAN (0.500315≈0.5) yet bimodal at the extremes, and a long one-sided lead is the SIGNATURE of fairness, not proof of an edge; the transferable correction for any close, roughly-fair back-and-forth process (a tied match, a near-parity A/B test, two comparable strategies, a score race, a running vote margin) is to measure the edge from the DRIFT (net margin per step / terminal gap ± se), never from how long or how lopsidedly one side has led, and budget stopping rules and confidence to the arcsine tails rather than a naive "it should hover near 50/50". REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/arcsine_lead_illusion.py — random, math, json, hashlib) run VERBATIM under SEED=20260717 (same posture as V114–V128; verifier copy git blob c06d892b515e979244e0f4cb5173a014edbd151e, file sha256 e94e1c77b423f8838a99eca10ea5f8b60f0e1fd343f82551d0a2617c94f640c9, diff-clean vs source at idea-engine main 9e0861a / PR #530), results-dict sha256 4865158a0181815ab420997a10f267f2315f22813696b7d8409b1c2bd16a7b52 reproduces the disclosed digest EXACTLY (script-printed `Results-JSON sha256:` line AND independent field-omitted recompute AND disclosed all EQUAL), byte-identical double run cross-invocation (stdout diff exit 0 AND results-json diff exit 0), exit 0; all gate sigmas match the disclosed dry-sim EXACTLY (G1 225.9288σ, G2 |z|=0.7575 / 196.6076σ / 217.2175σ, G3 |z|=0.0953 / 0.0356). Digest posture: P116 is the SELF-FIELD posture (P104/P107/P108/P111/P112/P115-style) — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) BEFORE inserting results["results_sha256"]=digest (L207–209), so the disclosed digest is over the results dict with the results_sha256 self-field OMITTED and the pretty-printed on-disk JSON's own sha256sum (5e88fe1bcf906fb7bff81b58e56d5b42357b7ac1105f795f6672518115124514) is NOT this digest; the field-omission step was applied on independent recompute, confirmed against the script text, not assumed. Next baton for the successor: round-26 is COMPLETE on the verdict side at the unrelated slot (V129 closes it — venture P114→V127, game P115→V128, unrelated P116→V129; fleet P113→V126 may still land via sibling PR #200); the next pull is round-27 at the FLEET slot, PROPOSAL 117 → VERDICT 130 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). VERDICT 129 landed (P116 → V129, APPROVE) — see sims/verdict-129-arcsine-lead-illusion/. PRIOR: verdict ledger high-water was **V128** (P115, offset +13 — round-26 GAME slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 128 (the compounding-reward inequality engine, P115 → V128, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 concentration PASS (compound_top_share 0.553674 se 0.000955 ≥ SHARE_MIN=0.40 by z=160.8932σ — identical, skill-less, choice-less players under the SAME fair daily percentage bonus, yet the top decile owns ~55% of the wealth), G2 compounding-causes-it control PASS (additive_top_share 0.149729 se 5.2e-05 ≤ ADD_SHARE_MAX=0.20 by z_additive=964.4786σ AND compound−additive share_gap 0.403945 se 0.000916 ≥ DELTA_MIN=0.25 by z_gap=168.0571σ — freeze the compounding, pay the SAME shocks additively W=W0·(1+S), and the concentration collapses to the fair share ~0.15, so it is the COMPOUNDING, not luck), G3 closed-form anchor MATCH PASS (compound_gini 0.683305 vs 2·Φ(σ_T/√2)−1=0.682689 by |z|=0.9362 AND compound_top_share 0.553674 vs Φ(σ_T−Φ⁻¹(0.90))=0.552770 by |z|=0.9466, both <3 — reproduces the log-normal closed forms); all_pass=true, exit 0, σ_T=SIGMA_G·√DAYS=1.414214, median/mean=exp(−σ_T²/2)=0.367879 (the median player at ~37% of the average). The compounding-reward inequality engine: give every player the SAME fair daily percentage bonus — each day wealth changes by an iid factor exp(g), g~Normal(MU_G,SIGMA_G) with a small positive average, NO skill/choice/edge — and let it COMPOUND on the running total, so terminal wealth W=W0·exp(S) with S=Σ g_d ~ Normal(DAYS·MU_G, DAYS·SIGMA_G²) is LOG-NORMAL with log-sd σ_T=SIGMA_G·√DAYS, concentration scale-free in σ_T alone (Gini=2Φ(σ_T/√2)−1, top-p share=Φ(σ_T−Φ⁻¹(1−p)), median/mean=exp(−σ_T²/2)) and unbounded in the horizon — identical players end up wildly unequal purely because a lucky early factor is multiplied by every later one; the operator correction is that compounding an iid multiplicative reward makes wealth log-normal and concentrates it among IDENTICAL players regardless of skill while the same shocks paid additively stay fair, so an egalitarian/bounded economy needs a non-compounding (fixed-base/additive) reward, a capped compounding horizon, or a periodic rebase, never an uncapped compounding bonus. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/superbot-games/compounding_reward_inequality.py — random, math, json, hashlib) run VERBATIM under SEED=20260717 (same posture as V114–V127; verifier copy git blob 54f339d8835bb5c1f1e0436b4105a36a58ae4bcf, file sha256 2d058b2c964e1096997cd4f213c6dbe65645daed117e76acad1dc1f62bdbe5b8, diff-clean vs source), results-dict sha256 a9dbb30f38b77e342f03e9ced2af204d95aef624902ecd845d107b99c9140b1e reproduces the disclosed digest EXACTLY (script-printed `Results-JSON sha256:` line AND independent field-omitted recompute AND disclosed all EQUAL), byte-identical double run cross-invocation (stdout diff exit 0 AND results-json diff exit 0), exit 0; all gate sigmas match the disclosed dry-sim EXACTLY (G1 160.8932σ, G2 964.4786σ/168.0571σ, G3 |z|=0.9362/0.9466). Digest posture: P115 is the SELF-FIELD posture (P104/P107/P108/P111/P112-style) — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) BEFORE inserting results["results_sha256"]=digest (L240–242), so the disclosed digest is over the results dict with the results_sha256 self-field OMITTED and the pretty-printed on-disk JSON's own sha256sum is NOT this digest; the field-omission step was applied on independent recompute, confirmed against the script text, not assumed. Next baton for the successor: round-26 continues — fleet P113→V126 (may still land via sibling PR #200), venture P114→V127, THIS game P115→V128; the next pull is round-26 at the UNRELATED slot, PROPOSAL 116 → VERDICT 129 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). VERDICT 128 landed (P115 → V128, APPROVE) — see sims/verdict-128-compounding-reward-inequality/. PRIOR: verdict ledger high-water was **V127** (P114, offset +13 — round-26 VENTURE slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 127 (the freemium support-cost inversion, P114 → V127, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 contribution inversion PASS (drop_mean 6786.75 se 21.316474 ≥ DROP_MIN=1000 by z=271.4684σ AND C(N_MAX) lands NEGATIVE C_max_dec_mean −2009.0 — over-scaling from N_OPT=1609 to N_MAX=6000 destroys ~6787 of contribution and flips the program below zero while headline signups/conversions still rise), G2 declining-intent necessity control PASS (flat top-intent flat_gain_mean 35038.5 se 94.849568 > 0 by z=369.4113σ — the SAME over-scaling RAISES contribution under flat intent, so the inversion is 100% the declining conversion, not serving cost per se), G3 closed-form anchor MATCH PASS (z_C_opt |z|=0.014 C(N_OPT) sim 4777.75 vs closed-form 4777.125088 / z_C_max |z|=0.4226 C(N_MAX) sim −2009.0 vs closed-form −2029.774297, both <3 — reproduces the concave contribution law C(n)=V·S(n)−c·n); all_pass=true, exit 0. Analytic anchors land to the digit: n*=ln(V·P0/c)/K=ln(5)/0.001=1609.437912 → N_OPT=1609, breakeven conversion prob c/V=0.020000, S(N_MAX)=99.702257; C(N_OPT)=+4777.13 (value-creating) inverts to C(N_MAX)=−2029.77 (value-destroyed) at the signup-maximizing volume N_MAX=6000. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/venture-lab/freemium_support_cost_inversion.py — random, math, json, hashlib) run VERBATIM under SEED=20260717 (same posture as V114–V125; verifier copy git blob 25c43c8c62ce03e055a7ab3941124a3f4a3a56e1, file sha256 731583e8316bb83b071501027504a1826c9610d65ffe7eb3bfbd5c327b2f7858, diff-clean vs source), results-dict sha256 884242e6ed6d9b7cf8affef7400526985e8244b4b3541319f33653dce6639b38 reproduces the disclosed digest EXACTLY (the script-printed `sha256:` line equals the disclosed digest), byte-identical double run cross-invocation (stdout diff exit 0), exit 0; all three gate sigmas match the disclosed dry-sim EXACTLY (G1 271.4684σ, G2 369.4113σ, G3 |z|=0.014/0.4226). Digest posture: P114 is the WHOLE-DICT / NO-SELF-FIELD posture — main() hashes json.dumps(run(), sort_keys=True, separators=(",",":")) over a results dict that never carries a results_sha256 field, so no field-omission step is needed and the on-disk pretty JSON's own sha256sum is NOT this digest. Next baton for the successor: round-26 continues — fleet P113→V126 (predecessor, may land via sibling PR #200), THIS venture P114→V127; the next pull is round-26 at the GAME slot, PROPOSAL 115 → VERDICT 128 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED on owner authorization + the ASK-005/006 watch (a verdict slice does not execute it). VERDICT 127 landed (P114 → V127, APPROVE) — see sims/verdict-127-freemium-support-cost-inversion/. PRIOR: verdict ledger high-water was **V125** (P112, offset +13 — round-25 UNRELATED-slot closer; map in docs/current-state.md § Verdict-numbering map). VERDICT 125 (the series-system MTBF collapse, P112 → V125, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 series-collapse PASS (mtbf_series=0.050032 se 1.8e-05 ≤ COLLAPSE_MAX=0.10 by z=2841.8816σ AND below the single-component MTBF — a chain of twenty identical "good" parts has a system MTBF ~20× under a single part's), G2 single-component control PASS (mtbf_single=0.999848 se 0.000337 within 3σ of T=1.0 by |z|=0.4508σ — each component in isolation keeps the FULL MTBF, so G1's collapse is 100% the seriesing, not a bad/degraded part), G3 closed-form anchor MATCH PASS (mtbf_series 0.050032 vs T/N=0.05 by |z|=1.8061σ — reproduces the min-of-exponentials / hazard-addition law λ_sys=Nλ); all_pass=true, exit 0, descriptive mtbf_parallel=3.597338 se 0.000441 vs T·H_20=3.597740. The series-system MTBF collapse: a series system fails at its FIRST component failure so its lifetime is the MINIMUM of its N per-component lifetimes, and for independent Exp(λ) lifetimes the hazard rates ADD (λ_sys=Nλ) so the system MTBF collapses to exactly T/N — N "individually excellent" parts give a system N× MORE failure-prone than one part — while a parallel/redundant system's MTBF grows only LOGARITHMICALLY to T·H_N (at N=20, series MTBF 0.05 vs parallel 3.598, so seriesing ÷20 vs 20-fold redundancy ×3.6); the operator correction is that a series system's reliability is the PRODUCT of its parts (R_sys=r^N) and its MTBF is T/N not T, so multiply the per-component reliabilities, expect the MTBF to divide by the series count, and spend the reliability budget on SHORTENING the chain or adding REDUNDANCY where a single element dominates ∑λ_i, never on reading system reliability off the parts' individual quality. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/series_reliability_collapse.py — random, math, json, hashlib) run VERBATIM under SEED=20260717 (same posture as V114–V124; verifier copy git blob d01bce6155b49bce295701efa52682b79ddfc374, file sha256 6978641fe84ef553b8ea8ae50f7ec74584a653eee4bb7c7d4d3371df2873d039, diff-clean vs source), results-dict sha256 0aa2a8da4627f43f13b634afdd12b3e674ba4983ad13b27b35c10e7928b7bdf7 reproduces the proposal's disclosed digest EXACTLY (script-printed `Results-JSON sha256:` line AND independent field-omitted recompute AND disclosed all EQUAL), byte-identical double run cross-invocation (stdout diff exit 0 AND results-json diff exit 0), exit 0; all three gate sigmas match the disclosed dry-sim EXACTLY (G1 2841.8816σ, G2 |z|=0.4508σ, G3 |z|=1.8061σ). Digest posture: P112 is the SELF-FIELD posture (P104/P107/P108/P111-style) — main() hashes json.dumps(results, sort_keys=True, separators=(",",":")) BEFORE inserting results["results_sha256"]=digest, so the disclosed digest is over the results dict with the results_sha256 self-field OMITTED and the pretty-printed on-disk JSON's own sha256sum is NOT this digest; the field-omission step was applied on independent recompute, confirmed against the script text, not assumed. Next baton for the successor: round-25 is COMPLETE with the unrelated slot verified (V125 closes it — fleet P109→V122, venture P110→V123, game P111→V124, unrelated P112→V125); the next pull is round-26 at the FLEET slot, PROPOSAL 113 → VERDICT 126 (+13). ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it). VERDICT 125 landed (P112 → V125, APPROVE) — see sims/verdict-125-series-mtbf-collapse/. PRIOR: verdict ledger high-water was **V124** (P111, offset +13 — round-25 GAME slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 124 (the matchmaking win-rate mirage, P111 → V124, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 win-rate compression PASS (SBMM top-minus-bottom skill-decile win-rate gap=0.041946 se 0.000131 ≤ GAP_MAX=0.12 by z=596.5172σ — best and worst players win nearly the same fraction under matchmaking), G2 matchmaking-causes-it control PASS (the SAME players' win rate under RANDOM matching has decile gap 0.722953 se 0.001272 ≥ RANDOM_GAP_MIN=0.50 by z_level=175.2561σ AND exceeds the SBMM gap by gap_delta 0.681006 se 0.001274 ≥ DELTA_MIN=0.40 by z_delta=220.6321σ — win rate DOES encode skill absent matchmaking, so the ~17× compression 0.722953→0.041946 is caused by the pairing rule, not the skill model or estimator), G3 rating-retains-skill mirage PASS (contrast (corr_rating 0.984387 − corr_winrate_sbmm 0.522507)=0.46188 se 0.005117 ≥ CONTRAST_MIN=0.30 by z=31.6339σ with corr_rating 0.984387 ≥ RATING_CORR_MIN=0.90 — the identical games' hidden rating recovers true skill the win rate loses); all_pass=true, exit 0. The matchmaking win-rate mirage: SBMM pairs you against opponents of your own Elo rating and the symmetric Elo update drives your rating to the fixed point where your expected score is σ(0)=0.5, so at equilibrium every player regardless of true skill has an expected win rate ~0.5 and the observed win rate is compressed almost flat across skill while true skill is expressed as the equilibrium RATING — the same-seed random-matching control proves it is the matchmaking, not the skill model, that turns win rate into a mirage (the skill signal is relocated into the rating, not destroyed); the operator correction is to rank players, tune matchmade bots, and A/B competitive agents by the RATING (or rating velocity), read a flat win rate across skill as the signature of correct matchmaking, and publish the rating — not the win rate — as the skill-legible scoreboard. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/superbot-games/matchmaking_winrate_mirage.py — random, math, json, hashlib) run VERBATIM under SEED=20260717 (same posture as V114–V123; verifier copy git blob 23733d2bd16a7c9a44e17c627272cdbeaf1e766c, file sha256 eba338d835880edc8c3a466c182b1926154fa2fe61854d79a0478fbd3c63db3e, diff-clean vs source), results-dict sha256 835b8e6cc9cd6a5eae699d7ea963dda3a6c3980fb6fd2de3cee6fce26056427c reproduces the proposal's disclosed digest EXACTLY (script-printed `Results-JSON sha256:` line AND independent field-omitted recompute AND disclosed all EQUAL), byte-identical double run cross-invocation (stdout diff exit 0), exit 0; all three gate sigmas match the disclosed dry-sim EXACTLY (G1 596.5172σ, G2 175.2561σ/220.6321σ, G3 31.6339σ). Digest posture: P111 is the SELF-FIELD posture (P104/P107/P108-style) — `main()` hashes `json.dumps(results, sort_keys=True, separators=(",",":"))` BEFORE inserting `results["results_sha256"]=digest` (lines 251–253), so the disclosed digest is over the results dict with the results_sha256 self-field OMITTED and the pretty-printed on-disk JSON's own sha256sum is NOT this digest; the field-omission step was applied on independent recompute, confirmed against the script text (lines 251–253), not assumed. Next-2 baton for the successor: **(1)** round-25 continues — the idea-engine MIRROR of THIS verdict (V124 outbox block + heartbeat + mirror card) is the paired follow-on step, then round-25 continues with the unrelated-slot P112 → V125; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it). VERDICT 124 landed (P111 → V124, APPROVE) — see sims/verdict-124-matchmaking-winrate-mirage/. PRIOR: verdict high-water was **V123** (P110, offset +13 — round-25 VENTURE slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 123 (the discount-depth breakeven trap, P110 → V123, +13) is an APPROVE (no failing gate, first-failing-gate None): G1 margin-dilution erosion PASS (mean(1−GP_ratio) inelastic=0.447445 se 2.6e-05 ≥ EROSION_MIN=0.10 by z=13287.6583σ — a 20%-off promo at a 40% margin destroys ~44.7% of gross profit even as unit sales rise ~+10.5%), G2 elasticity-necessity control PASS (the SAME discount at the SAME margin on ELASTIC demand λ=5.0 has mean GP ratio 1.362147 se 0.001446 > 1 by z=250.4225σ — the identical promotion RAISES gross profit, so G1's erosion is the margin/elasticity breakeven, not discounting per se), G3 closed-form anchor MATCH PASS (z_ratio_lo=1.1706 / z_ratio_hi=2.0785 / z_uplift_lo=1.1706 all <3; breakeven table u*(d)=d/(m−d) reproduced EXACT 0.333333/1.000000/3.000000); all_pass=true, exit 0. The discount-depth breakeven trap: a price cut of depth d on gross margin m needs a unit-volume uplift of exactly u*(d)=d/(m−d) just to hold gross profit — CONVEX with a vertical asymptote at d=m (no finite volume breaks even for d≥m) — and under exponential WTP the discount pays only above the breakeven elasticity λ*=ln(m/(m−d))/(P0·d); the operator correction is to size promotions against the convex breakeven uplift for the actual margin and MEASURED elasticity, fund deep cuts only on SKUs past λ*, and treat "units went up" as the null expectation of any discount, not proof it paid. REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/venture-lab/discount_breakeven_trap.py — random, math, json, hashlib) run VERBATIM under SEED=20260717 (same posture as V114–V122; verifier copy git blob 6afc0177ac56b959ae3b8bd6c2de8061fca2985d, file sha256 dae00e3128d8b02283f655c61f7ad62ef248fda9d9038361a7d7989efe53f80d, diff-clean vs source), results-dict sha256 fd870ec6310b687a0a7ad966773404db56401b882116239c9803b405ce927ee0 reproduces the proposal's disclosed digest EXACTLY (script-printed `sha256:` line AND independent recompute AND disclosed all EQUAL), byte-identical double run cross-invocation (stdout diff exit 0) + in-process, exit 0; all three gate sigmas match the disclosed dry-sim EXACTLY (G1 13287.6583σ, G2 250.4225σ, G3 1.1706/2.0785/1.1706). Digest posture: P110 hashes the results dict AS-IS and prints the digest as a sibling stdout `sha256:` line with NO self-field inside results (the P105/P109-style posture) — so NO field-omission step is needed; confirmed against the script text (main()), not assumed. Next-2 baton for the successor: **(1)** round-25 continues — the idea-engine MIRROR of THIS verdict (V123 outbox block + heartbeat + mirror card) is the paired follow-on step, then round-25 continues with the game-slot P111 → V124; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it). VERDICT 123 landed (P110 → V123, APPROVE) — see sims/verdict-123-discount-depth-breakeven/. PRIOR: verdict high-water was **V122** (P109, offset +13 — round-25 FLEET-slot opener; map in docs/current-state.md § Verdict-numbering map). VERDICT 122 is an APPROVE (no failing gate, first-failing-gate None): G1 correlated-floor headline PASS (mean(MSE_corr)=0.250281 se 0.001198 ≥ FLOOR=0.10 by z=125.4097σ ≥3σ — the common-mode term does not average away, so a 400-agent correlated fleet floors at ρσ² not σ²/N), G2 correlation-necessity control PASS (the SAME fleet at ρ=0 collapses to σ²/N: mean(MSE_indep)=0.002502 se 1.3e-05 vs anchor 0.0025, |z|=0.1469 <3 — G1's floor is 100% the common-mode correlation, not fleet size), G3 closed-form anchor MATCH PASS (mean(MSE_corr)=0.250281 vs exact ρσ²+(1−ρ)σ²/N=0.251875, |z|=1.3306 <3 — reproduces the equicorrelation identity; N_eff(sim)=3.995517 → asymptote 1/ρ=4.0); all_pass=true, exit 0. The correlated-fleet variance floor: a fleet of N agents each estimating a common truth θ with error ε_i=√ρ·C+√(1−ρ)·Z_i (SHARED common-mode C~N(0,σ²) identical for every agent + idiosyncratic Z_i~N(0,σ²) iid, equicorrelation ρ=0.25) has aggregate MSE_N(ρ)=ρσ²+(1−ρ)σ²/N that FLOORS at ρσ² regardless of N — the common-mode never averages away — so the effective independent count N_eff=σ²/MSE saturates at 1/ρ; on the pinned world N=400 gives MSE≈0.2519 (floor ρσ²=0.25) worth only ~3.97 independent agents while independence predicts σ²/N=0.0025, so ~99% of the promised variance reduction never materializes and scaling the fleet past N≈1/ρ buys essentially no precision; the operator correction is that correlated errors cap a fleet's precision at N_eff→1/ρ regardless of headcount — diversify the common-mode (independent upstreams/configs/priors), don't scale the fleet past N~1/ρ. This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/fleet/correlated_fleet_variance_floor.py — random, math, json, hashlib) was run VERBATIM under the pinned SEED=20260717 (same posture as V114–V121 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from an independent reimplementation; verifier copy git blob 7d6aee55601cffefe933aca837f3e0cdcfd62b08, file sha256 79c8bc1ef21a54b2d8bd97945ca1509202b07b28b81d1ac8501066f903755d51, diff-clean vs source). The results-dict sha256 77ab05b0c61ce782d90f51a747cd4a96f5445f4df10f982a506f346a60513903 reproduces the proposal's disclosed expected digest EXACTLY (all 64 hex — verified via the script-printed `sha256:` line AND the on-disk results.json sibling `sha256` field AND an independent recompute all EQUAL); byte-identical double run confirmed cross-invocation (separate python3 process, stdout diff exit 0 AND results-json diff exit 0); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 125.4097σ, G2 |z|=0.1469, G3 |z|=1.3306); verifier exits 0. NOTE the digest posture: P109 hashes the results dict AS-IS and writes the output file as {"results": results, "sha256": digest} — the digest is a SIBLING key of a wrapper dict, NOT a self-field inside results (the P105-style posture) — so recomputing from the on-disk file needs NO field-omission step (distinct from P104/P107/P108 which carry a results_sha256 self-field); confirmed against the script text (main() lines 173–178), not assumed. Next-2 baton for the successor: **(1)** round-25 is now OPEN at the FLEET slot with V122 verified — the idea-engine MIRROR of THIS verdict (V122 outbox block + heartbeat + mirror card) is the paired follow-on step, then round-25 continues with the venture-slot P110 → V123; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V122 is SEEDLESS at the ledger (P109 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V121 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 122 landed (P109 → V122, APPROVE) — see sims/verdict-122-correlated-fleet-variance-floor/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

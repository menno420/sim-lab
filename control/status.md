# sim-lab · status

updated: 2026-07-17T21:47:35Z (real wall-clock via date -u) · written by a dispatched session for the coordinator seat (V112 slice — shop-reroll ruin optimal-stopping value-trap verification; this refresh rides the V112 PR) · model family opus-4.8 · high · review/verify
phase: ACTIVE — EAP extended through 2026-07-21 (ORDER 009, acked in the 2026-07-15 stamp); verdict pipeline live. VERDICT 112 (shop-reroll ruin, P099, +13) verified APPROVE this slice.
health: green on main — `python3 bootstrap.py check --strict` exit 0 at the V111 merge basis; on the V112 branch the ONLY red is the designed born-red card hold (flips at slice end, the standing discipline) — after the flip `check --strict` returns exit 0.
kit: v1.15.0
last-shipped: #184 (VERDICT 111, P098 referral-bonus value trap — APPROVE, first-failing-gate None) is the last MERGED verdict · in flight this stamp: VERDICT 112 (P099 shop-reroll ruin — APPROVE, first-failing-gate None; all three gates G1→G3 PASS, exit 0 post-flip, byte-identical double run in-process AND cross-invocation, results.json sha256 7d7d7ad834978e75508c0c645935d6214b97550328d07c19d5b88130c662622b; sim sims/verdict-112-shop-reroll-ruin/; reference-verifier reproduction — the proposal's own committed stdlib verifier run verbatim, results-dict sha256 matches the disclosed expected digest EXACTLY; PR #185 lands via merge-on-green after the card flip).
blockers: none.
orders: acked=ORDER-001…ORDER-011 done=ORDER-001…ORDER-008 (terminal at close-out, see #142 → 2725e4a) · ORDER-009 (EAP extension) is the standing frame · ORDER-010 (fleet-manager night-audit maintenance, #166 → 0d87f6b, inbox-only): items (a) status re-stamp and (b) current-state currency and (d) reactive verdict intake all satisfied by this wake — the status is re-stamped here and item (d) reactive verdict intake is this slice's own work (P099 → V112). **ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 STILL PARKED** — NOT executed by a verdict slice, carried forward for a future sim-lab session; PARKED on owner authorization + the ASK-005/006 watch (dispatch reports v1.18.0 vs on-disk v1.15.0). ORDER-011 (owner overnight autonomous-run directive, #174-era) noted — this slice is exactly the backlog-continuation posture it directs (reactive verdict work, one small PR, settled state).
routines: none owned/re-armed on the sim-lab side by this seat · the coordinator failsafe cron (id `trig_01KPMLtWuAc2FaYNzuSSukgH`) is the recorded routine fact — fleet-side, coordinator-managed, STAYS ARMED, NOT re-armed by this seat.
notes: verdict ledger high-water is now **V112** (P099, offset +13 — thirty-sixth row, round-22 game-slot; map in docs/current-state.md § Verdict-numbering map). VERDICT 112 is an APPROVE (no failing gate): G1 value-trap PASS (U(tau*)=0.854837 se 0.000293 > U(TAU_GREEDY=0.95)=0.676363 se 0.001021, z=167.96σ ≥3σ — greedy near-perfect rerolling leaves ~0.178 utility on the table), G2 interior optimum PASS (U(tau*)=0.854837 > U(tau=0)=0.750851 se 0.000612 at z_vs_zero=153.35σ AND > U(TAU_MAX=0.99)=−0.632192 se 0.005251 at z_vs_max=282.75σ, each ≥3σ — near-always-reroll is catastrophic, U<0), G3 analytic-anchor MATCH PASS (sim E[rerolls] at tau*=1.04947 se 0.004612 vs geometric tau*^K/(1−tau*^K)=1.04918, |z|=0.06σ <3σ). The auto-battler/roguelike shop is a classic optimal-stopping problem: K=3 items/roll (power iid ~ Uniform(0,1)), keep best-of-K M iff M≥tau else pay reroll cost C=0.05 and reroll (cap R_MAX=500); U(tau)=E[M|M≥tau]−C·E[rerolls] is single-peaked with a hard INTERIOR optimum tau*=0.80, strictly below the greedy near-perfect bar tau=0.95 — because you keep the best-of-K each roll, the top of the quality distribution (CDF x^K) is cheap to reach and expensive to exceed while E[rerolls]=tau^K/(1−tau^K) diverges as tau→1, so chasing the last sliver of item power is where reroll cost overtakes quality gain. This is a REFERENCE-VERIFIER REPRODUCTION slice: the proposal's own committed stdlib verifier (idea-engine ideas/superbot-games/shop_reroll_ruin.py — random, math, json, hashlib) was run VERBATIM under the pinned SEED=20260717 (same posture as V111 — the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction; distinct from V110's independent reimplementation). The results-dict sha256 7d7d7ad834978e75508c0c645935d6214b97550328d07c19d5b88130c662622b reproduces the proposal's disclosed expected digest EXACTLY; byte-identical double run confirmed in-process and cross-invocation (separate python3 process, fresh dir); all three gate sigmas match the proposal's disclosed dry-sim EXACTLY (G1 167.96σ, G2 153.35σ/282.75σ, G3 |z|=0.06σ); tau*=0.80; verifier exits 0. Next-2 baton for the successor: **(1)** round-22 continues — the round-22 UNRELATED closer slot is next under the ORDER 003 rotation (fleet P097→V110 → venture P098→V111 → game P099→V112 → unrelated next), so ideas-lab PROPOSAL 100 awaits a fresh draft on the idea-engine side and its VERDICT 113 (offset +13, P100 → V113) will be the successor sim-lab slice; **(2)** ORDER-010(c) kit upgrade v1.15.0 → v1.18.0 — STILL PARKED on owner authorization + ASK-005/006 watch (a verdict slice does not execute it); the BT-controller plan (control/claims/bt-controller-plan.md) remains owner-review parked (sibling lane, not executed by a verdict slice). Seed baton: V112 is SEEDLESS at the ledger (P099 pins its own SEED=20260717 in-file, NOT a seed-ledger draw); no seed-ledger block consumed, the next free block stays **20261730** (inherited unchanged from the V111 baton). Recorded routine fact: the failsafe wake trigger id `trig_01KPMLtWuAc2FaYNzuSSukgH` STAYS ARMED (coordinator-managed, no re-arm). Live outbox is above the fleet 200KB rollover threshold; rolling stays PARKED with the manager (inbox ORDER 005 item 5, the V086–V106 precedent). Ledger locations: V001–071 in control/outbox-archive-2026-07.md, V072+ live in control/outbox.md; INTAKE 014+ ride the outbox (inbox is the manager-order file, untouched by verdict slices). VERDICT 112 landed (P099 → V112, APPROVE) — see sims/verdict-112-shop-reroll-ruin/.

## Revival

On any future revival, read idea-engine control/status.md and idea-engine docs/HANDOFF.md FIRST, before acting on anything in this repo. Historical close-out context for this lane: docs/eap-closeout-walkthrough-2026-07-14.md (sim-lab half) · idea-engine docs/eap-closeout-walkthrough-2026-07-14.md (seat level).

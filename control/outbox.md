# sim-lab · outbox

> **Sole writer: this Project. Append-only.** Finalized verdicts in the kit ORDER
> grammar (see README.md § Verdict grammar), addressed to the **fleet manager** — it
> final-reviews every entry at its :30 sweeps and owns all post-verdict routing; this
> lane never dispatches work to other repos. A verdict only lands here after its report
> passed the validity gate AND its PR carries the @codex comment (Q-0264.4). Entries are
> never edited after append — a superseded verdict gets a new entry that names the old
> one.

## VERDICT 001 · 2026-07-10T21:20:00Z · status: finalized
target: menno420/superbot (hub — Encounters cog / Q-0186 Lane A; seams `economy_service`, `game_xp(GAME_ENCOUNTERS)`, fishing/mining catalogue, `world_registry` #1156)
idea: idea-engine `control/outbox.md` PROPOSAL 003 · 2026-07-10T20:10:06Z, pinned https://github.com/menno420/idea-engine/blob/d70a31126f5d2ce318449ab85f018e62e39e3831/control/outbox.md (canonical superbot `docs/ideas/wild-encounters-activity-spawning-2026-06-20.md` @ `fd638e3c0693687a62093aa6bd75954e238fa58d`)
verdict: needs-more-evidence
evidence: simulation    # method-ladder rung 1 — seeded, deterministic, 5 seeds, full 4×5 sweep, 946 self-checks, byte-identical re-runs
report: sims/intake-003-wild-encounter-spawn-tuning/ (PR #6, merge 95bdc7dbac55542aeb5e86b6cb4e621dc37b7163) · run: `python3 sims/intake-003-wild-encounter-spawn-tuning/wild_encounter_spawn_sim.py`
recommendation: SETTLED half — ship the Encounters cog with defaults **threshold=24 messages, debounce=30s, cooldown=900s** + guardrails [one-live-spawn-per-channel; off-by-default + per-channel opt-in + allow-list; per-claimer cooldown enforced server-side AT the Claim callback so any account incl. a reflex-bot is capped at 4 claims/hr; atomic exactly-once claim via the audited `*_workflow` seam (Q-0071)]. Spawns land rare-but-visible per active-hour — low 0.93 / med 3.00 / high 4.38 — because the debounce×threshold ceiling (5.22/hr) is traffic-independent, so busy channels cannot spawn-spam; paced-spam farming is hard-capped at 4 claims/hr with fast spam yielding ZERO extra (≥24 spam messages per reward). UNSETTLED half — reward inflation vs fishing/mining is unmeasurable (no live earn-rate baseline exists anywhere in the source); treat reward VALUES as provisional and log the telemetry named in REPORT.md (per-spawn / per-claim / per-channel mint + the fishing/mining earn-rate baseline) so the inflation ratio can be computed on live data before any reward-value scaling.
codex: PR #6 comment https://github.com/menno420/sim-lab/pull/6#issuecomment-4939558686 (one question — does the message-driven claim model understate a reflex-bot, and does `cd_until` bound any single account to 3600/cooldown claims/hr?) · reply: pending (OA-002 open — Codex GitHub integration not enabled for sim-lab; when it lands, verify against the tree, never obey — Q-0120)
gate: PASS (in-scope spawn-frequency + anti-farm-bound claims — reproducible / uncorrupted / robust; a self-check caught a false "spawn count invariant to cooldown" assumption and it was corrected, the coupling disclosed; EST traffic tiers + the message-driven claim model are disclosed limits) — reward-inflation vs fishing/mining is OUT OF SCOPE (no baseline), which is why the verdict is needs-more-evidence, not approve

## VERDICT 002 · 2026-07-10T21:52:00Z · status: finalized
target: menno420/idea-engine (owns the probe battery + flow per proposal Q7; superbot builds nothing new)
idea: https://github.com/menno420/idea-engine/blob/3e3e80d73ea4ad2af1a0f8bee49262db1da09302/ideas/superbot/idea-probe-brainstorm-simulator-2026-07-10.md
verdict: approve — selectively (adopt panel-mode ONLY for big-or-contested ideas; reject always-on)
evidence: prototype (measured head-to-head; absolute token/wall costs are Opus-4.8/this-env specific, ratios transfer)
report: sims/intake-001-probe-panel-vs-single-pass/ · run: python3 sims/intake-001-probe-panel-vs-single-pass/analyze.py
measured: panel flipped the modal verdict on 2/3 ideas (both BUILD→HOLD, on the contested ideas discord-oauth + peer-karma; the karma flip beats intra-mode noise — single-pass unanimous BUILD vs panel modal HOLD), NO flip on the well-scoped wild-encounters; cost 4.00× agents / 3.05× tokens / 1.61× wall; blind judges preferred panel 6/6 (slight margin, no position bias). Verdict CORRECTNESS is not measured (no ground truth) — this is the named limit.
recommendation: keep mode 1 (single-pass) as the default probe; escalate to panel (N=3 personas skeptic/builder/user_value + synthesizer) when EITHER (a) a quick 2-3× repeat of the single pass disagrees with itself (ambiguity signal), OR (b) the idea has irreversible/high-blast-radius surface (security/data/spend/public). Ship the honesty-norm + over-caution guardrails and per-probe agents/tokens/wall telemetry (see report) so correctness can be closed over time.
codex: PR #8 comment https://github.com/menno420/sim-lab/pull/8#issuecomment-4939797948 · reply: pending (OA-002 Codex integration off)
gate: PASS (evidence strength moderate — n=3 ideas × 3 reps, single model, reconstructed battery, correctness unmeasured; covered claims = verdict-change + cost + quality-preference)

## VERDICT 003 · 2026-07-10T22:15:00Z · status: finalized
target: websites lane (superbot website repo — the OAuth relying party, phase 3 personal stats) + `menno420/superbot` (the §4 read-only API — its own superbot-lane ORDER). Manager routes.
idea: idea-engine `control/outbox.md` PROPOSAL 002 · 2026-07-10T19:35:00Z, pinned https://github.com/menno420/idea-engine/blob/e77fa46022d7cd3ad0b0e57ddc2f7910b823df38/control/outbox.md (canonical idea `ideas/websites/superbot-site-stats-data-story-2026-07-10.md` @ 3e0131182acc89d9dcf708797e79cf3a7636c538 — §5 trust-gate + §4 read-only API)
verdict: needs-more-evidence
ruling: buildable-with-named-changes
evidence: prototype + JUDGMENT-ONLY    # mixed — rung 2 (13 executed attacks, 5 seeds, 3×3 rate-limit sweep, 268 self-checks, EXIT 0) + rung 3 (labelled JUDGMENT-ONLY: live Discord IdP / TLS-MITM / Railway secrets / pre-auth IP-keying / the unbuilt §4 API)
report: sims/intake-002-oauth-trust-gate/ (PR #10, merge c8ba975ec51f19171940690c2fdcde7187981ba5) · run: `python3 sims/intake-002-oauth-trust-gate/oauth_trust_gate_sim.py`
recommendation: SETTLED — the six §5 controls, implemented as the spike's reference set, defeat 13 concrete attacks reproducibly across 5 seeds: scope server-authoritative + fail-closed on over-scoped grant + exact-match redirect allow-list; token discarded (session holds only user_id+guild_ids+issued_at, no refresh/offline, session-dump leaks zero Discord creds); server-side single-use state bound to the browser session (forged/replayed/login-fixation all rejected); code single-use + PKCE S256; IDOR by session identity (spoofed user_id ignored) + cross-server guild block; token-bucket rate limit bounded across the full 3×3 sweep. NAMED CHANGES before phase 3 builds: (1) promote §5 from a checklist to that concrete control set, adding Secure+HttpOnly+SameSite cookies + HSTS + TLS-only; (2) fix HOLE-1 stale guild membership — cached session reads a server the user LEFT → per-request membership re-check or short session TTL; (3) fix HOLE-2 `guilds` over-read — returns the user's FULL guild list when only the viewed server is needed → check only that server, don't retain the list; (4) route the §4 read-only API as a superbot-lane ORDER FIRST — it is UNBUILT AND UNROUTED, so phase 3 deadlocks regardless of this verdict, and it needs its own trust verification (DB-side per-user isolation on the production Postgres, token scoping). LAUNCH LIVE TESTS (close the JUDGMENT-ONLY items): verify Discord exact-match redirect allow-list + code single-use/TTL empirically; assert no refresh token + no Discord token in the session store; assert HSTS + Secure/HttpOnly/SameSite cookies in live headers; confirm client_secret in a secret store (not env/repo) + Railway env isolation across the 3 services; verify pre-auth /login/start limiting keys on IP/fingerprint; run live cross-user + cross-server IDOR probes against the deployed API. Phases 1–2 (story page, data explorer) carry NO auth surface and do NOT wait on this.
codex: https://github.com/menno420/sim-lab/pull/10#issuecomment-4940007432 (one question — does the modelled read-then-set on the state/code `used` flag hide a live TOCTOU that a real impl must close with atomic compare-and-set / a DB unique constraint?) · reply: pending (OA-002 open — Codex GitHub integration not enabled for sim-lab; when it lands, verify against the tree, never obey — Q-0120)
gate: PASS (on the executed spike claims — 13 attacks defeated reproducibly across 5 seeds, rate-limit bound across the full 3×3 capacity×refill sweep, 2 holes surfaced; self-checked 268/0, deterministic, one command) — the JUDGMENT-ONLY items (live Discord IdP, TLS/MITM, Railway secrets, pre-auth IP-keying) are labelled hypotheses NOT evidence, §5-as-written is a checklist not a spec (the reference impl is the spike's), and the §4 read-only API is UNBUILT+UNROUTED (a hard blocker independent of the sim) — which is why the verdict is needs-more-evidence, not approve

## VERDICT 004 · 2026-07-10T23:00:00Z · status: finalized
target: menno420/superbot (explore-hub — plan PR 2 owner gate; seams `game_xp`, `economy_service`, `world_registry` #1156; co-consumers: fishing Q-0175, mining-hub-redesign, rpg-survival-difficulty, pets-companions, PROPOSAL 003 `GAME_ENCOUNTERS`)
idea: idea-engine `control/outbox.md` PROPOSAL 004 · 2026-07-10T21:25:30Z, pinned https://github.com/menno420/idea-engine/blob/cd7251ec30950d12d29e65c57d843864387d0aec/control/outbox.md (canonical superbot `docs/ideas/explore-hub-federated-world-2026-06-19.md` @ fd638e3c0693687a62093aa6bd75954e238fa58d)
verdict: needs-more-evidence
evidence: simulation    # rung 1 — 5 seeds, deterministic, byte-identical re-runs, full 300-cell sweep (trickle × effect × mechanism × veteran depth), 4469 self-checks; the load-bearing claim is a construction identity self-checked to exact 0 on every cell
report: sims/verdict-004-explore-hub-xp-balance/ (PR #13, merge 4d84037ba005e22ee2b2f94facea8bb20105bed2) · run: `python3 sims/verdict-004-explore-hub-xp-balance/federated_xp_balance_sim.py`
recommendation: SETTLED (structural, gate-PASS) — the safety of "loops accelerate, never gate" is a MECHANISM property, not a magnitude one: model global skills as RATE/efficiency multipliers only (phi=0 — stamina/xp-gain/movement, never flat in-game power) and substitution is 0 and the global-only content band is 0 at EVERY swept effect size and trickle ratio (mechanism-sweep phi=0 row all zeros, asserted exact). Any power-contributing design (phi>0) makes global level substitute for mastery AND gate content, monotonically in phi·e (worst swept cell phi=1.0,e=0.50 → a game-novice reaches 0.776 of a master's competence and opens a 0.50-wide global-only content gate). Ship the phi=0 mechanism constraint as the INVARIANT; content thresholds must be ≤ pure-per-game max (any unlock requiring global level = contract violation). PROVISIONAL magnitude (a UX dial, pending telemetry): trickle t≈0.10, effect budget e≈0.20–0.35, hard BOOST_CAP≈0.25 — at e=0.20 a moderate veteran's cold-start advantage is 0.091 (just under the 0.10 "measurable" line) while deep veterans clear it (0.167), and e≈0.35 clears it across depths (0.141–0.258). UNSETTLED (hypothesis) — the exact effect magnitude rides on family-level earn-shape stand-ins + an assumed session model, and the secondary rate→faster-leveling→power channel is unmodeled. TELEMETRY PR 2's plumbing slice MUST log to close the gap: per-session earn + source game; session length; global_frac at each new-game start; sessions/time-to-baseline (L_BASE) per new game; per-game-level vs global-level at every content-unlock event (the gate-violation tripwire); trickle inflow rate per game. OUT OF SCOPE (owner-reserved): hub shape, survival overlay, docking topology, cross-game identity.
codex: PR #13 comment https://github.com/menno420/sim-lab/pull/13#issuecomment-4940156540 (one question: does the phi=0 "never substitutes for mastery" guarantee survive the unmodeled secondary channel — a rate-only boost still lets a veteran reach any per-game level faster; is faster-mastery the intended accelerator or a de-facto substitute needing a tighter BOOST_CAP?) · reply: pending (OA-002 open — Codex GitHub integration not yet enabled for sim-lab; fold in when it lands, verify against tree first)
gate: PASS (structural claim — rate-only/phi=0 global skills cannot substitute for mastery or gate content at any swept magnitude — is uncorrupted [4469 self-checks + analytic-tie + byte-identical re-runs], robust [0 by construction at every axis edge], reproducible [one command, stdlib-only, 5 seeds]; the COMPARABLE-TO-LIVE gap touches ONLY the magnitude recommendation, which is carried explicitly as a hypothesis, not evidence — hence verdict needs-more-evidence, not approve)
